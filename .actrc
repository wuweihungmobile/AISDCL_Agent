# monorepo 根層 act 預設組態（地端用 Docker 跑根層 .github/workflows/*）。
# 2026-07-10 合併自 AISDLC_SDD/.actrc（超集）+ AutoClaude/.actrc（-P ubuntu-latest 與
# --container-architecture 兩項為共同子集）；workflows 遷根層後 act 一律於 monorepo
# 根執行（AutoClaude/tools/run_act.sh|.ps1、AISDLC_SDD/scripts/act-ci.sh 已同步改根）。
#
# 以 catthehacker 中型 Ubuntu 鏡像貼近 GitHub ubuntu-latest（內含 python/node/
# git 等常用工具）。act 讀 .github/workflows/ 的 YAML，用本機 Docker 起虛擬環境
# 模擬雲端 CI，Push 前即可驗證 workflow 是否寫對。
#
# 注意（誠實警語）：act 不連 GitHub artifact 儲存，故 upload-artifact 步驟在
# act 中為 no-op；本次紅燈的真因（artifact 配額）act 模擬不到 → 真正可靠的
# 地端閘門是 docker compose ci-runner / scripts/ci-gate.sh（跑實際檢查命令）。
# act 的價值在於：抓 YAML 語法錯、步驟順序、缺套件、shell 相容性等結構問題。

-P ubuntu-latest=catthehacker/ubuntu:act-latest
-P ubuntu-24.04=catthehacker/ubuntu:act-latest
-P ubuntu-22.04=catthehacker/ubuntu:act-22.04
# Mac/Windows 相容性優化補註：此設定在 Apple Silicon（arm64）Mac 上會強制整條 act
# pipeline（含 runner 映像與內部起的 PG 容器）走 QEMU amd64 模擬，即使映像本身有原生
# arm64 版本也用不到，會明顯變慢、偶發不穩定。此為刻意設計、非疏漏：GitHub Actions
# 雲端 runner 一律是 amd64，改成 arm64 反而會破壞「地端 act ＝ 雲端 CI」的平價承諾，
# 不應為了本機效能犧牲這個保證。Apple Silicon 使用者請預期 act 執行較慢屬正常代價。
--container-architecture linux/amd64
# 用本地已拉的鏡像，避免 act 的 forcePull 帶 Docker 認證導致
# 「authentication required」失敗（公開鏡像但 act 送出 ~/.docker 認證）。
# 首次請先：docker pull catthehacker/ubuntu:act-latest（act-ci.sh 會自動預拉）。
--pull=false
# 🔴 容器內必須有 PID 1 reaper（本輪 Scan-F 實測落地）。act 起 runner 容器時的
# entrypoint 是 `tail -f /dev/null`，它**不回收殭屍子行程**；於是任何用
# `os.kill(pid, 0)` 判定「行程還在不在」的測試，在 act 內看到的殭屍一律回「還在」
# ⇒ 產生與真缺陷逐字同形的**假紅**（本輪實測：AutoClaude 的 test_perception.py
# 孫行程回收回歸鎖，雲端真 ubuntu-latest 零 failed、act 預設 1 failed）。
# 本機探針工作流實測（同一支 workflow、同一顆映像，唯一差異就是本行）：
#   加本行前 → PID1_COMM=tail        加本行後 → PID1_COMM=docker-init
# `--init` 讓 Docker 把 tini 塞成 PID 1，語意與 GitHub 真 runner（VM 內有 init）
# 對齊——這是**提高**地端與雲端的等價度，不是放寬判準。
--container-options=--init
