openapi: 3.1.0
info:
  title: Auth API (broken — missing password)
  version: 1.0.0
paths:
  /auth/login:
    post:
      summary: User login (intentionally missing password for A-31.4)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email }
      responses:
        "200": { description: OK }
