# EVOLUTION_LOG — AISDLC_SDD 版本演化紀錄

> 依 `docs/04_planning/AutoSDD_improving_01.md` §6 版本演化規則（Copy-on-Evolve）。
> 舊版 `AISDLC_SDD_v0.01/` ～ `AISDLC_SDD_v0.29/` 凍結唯讀；本目錄（v0.30）為演化後的可修改版本。
> 下方「凍結基線例外」章節記錄對已凍結版本的**原地回補例外**（非版本演化本身，但比照本檔既有欄位結構化存證，供未來查詢「Copy-on-Evolve 是否曾被打破、在什麼條件下」）。

## 凍結基線例外：v0.01～v0.29 P0 路徑穿越回補（R44，2026-07-25）

> **注意**：本節不是「複製產生新版本」的正常 Copy-on-Evolve 演化事件——沒有新版本目錄產生，而是對既有 29 個**凍結基線版本原地打補丁**。依 Copy-on-Evolve 規則，凍結版本本應唯讀不可修改；此為 43 輪跨平台複審帳本史上首次對凍結基線做例外回補，特此比照本檔既有版本演化記錄的結構化欄位存證。完整逐版驗證細節見根層 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-357／358／367（本節為其權威索引，不重複散文敘述）。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.01/` ～ `AISDLC_SDD_v0.29/`（29 個凍結基線版本）× `tools/fsm_runtime/` 7 支檔案（`hub_sync.py`／`production_monitor.py`／`hub_merge.py`／`spec_patch_proposer.py`／`production_to_fpl.py`／`sandbox_runner.py`／`counterfactual_replay.py`），共 203 處呼叫點；不含 v0.30（LATEST，本輪之前已修復） |
| **日期／signoff** | 2026-07-25（R44 跨平台相容性複審 FrozenPatch 階段）；🔴 人工 signoff：使用者本輪明確核准破例，核准範圍嚴格限定於「已證實可利用的路徑穿越／任意檔案讀取」（不含同批發現、性質不同的 DEF-101-358／359 等其他殘留項——見 DEF-101-359 記事，該項仍依既有凍結版紀律 wontfix，未獲本次例外覆蓋） |
| **打破 Copy-on-Evolve 的理由** | `rule_id`／`nfr_id`／`ac_id`／`fpl_id`／`divergence_kind` 等使用者可控字串未經淨化即組進 f-string 檔案路徑，構成生產路徑上真實可利用的任意檔案讀取（P0，對抗式驗證逐版證實可利用，非理論性）。嚴重度遠超過去 DEF-101-004／019／020／040／056／057 等凍結版缺口的 wontfix 判例（那些皆屬「需人工手動 cd 進舊版目錄」才會觸發的邊角情境）。 |
| **修法** | 逐版套用「呼叫該版本既有（較弱版）`_sanitize_component()`」的最小修法，與 v0.30 既有修復模式等價；不升版、不新增版本目錄，僅原地補丁既有 29 個凍結版本。 |
| **TLC 證據** | N/A——本次改動限於上述 7 支檔案的字串淨化呼叫點，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`/`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | 逐版 `python -m pytest tools/fsm_runtime/tests/ -m "not chaos" -q` 全綠（除既有無關失敗 DEF-03-001）；對每版 `hub_sync.py::diff()` 用 monkeypatch `_sanitize_component` 為恆等函式模擬修復前 vs 真實修復後雙軌對照，29 版一致證實修復生效。新增 repo-wide 前瞻回歸鎖 `tools/tests/test_sanitize_component_frozen_sdd_versions_lock.py`（DEF-101-367）鎖住全部 29 版 × 7 檔的淨化呼叫式存在，防未來任一版本意外還原無人發現。 |
| **殘留落差** | 29 版的 `_sanitize_component()` 本身仍是較弱版本（未比照 v0.30 做 Windows 保留裝置名／控制字元／長度上限強化）——足以阻斷本次驗證之路徑穿越攻擊面，但防護縱深不及 v0.30；是否 backport 強化版留待下輪評估（見 DEF-101-358，open/watch）。 |
| **回退指引** | 若日後需要回退（例如發現本次修法本身有誤），逐版還原 203 處呼叫點為修復前原始碼即可；**不建議回退**——回退會重新打開已證實可利用的任意檔案讀取漏洞。回退不影響任何版本的 API/FSM 狀態/`*.tla`（純字串淨化呼叫，無其他變更）。 |

## 凍結基線例外：v0.01～v0.29 `_sanitize_component()` 共享層抽取（R45，2026-07-25）

> **注意**：本節同 R44 章節，是對既有 29 個**凍結基線版本原地打補丁**的例外記錄（非正常 Copy-on-Evolve 演化）。這是繼 R44（P0 路徑穿越回補）之後，43+ 輪帳本史上**第二次**對凍結基線做例外回補；本次經使用者於架構層級明確核准（見下方 signoff 欄），且範圍是 R44 遺留的 DEF-101-358 殘留落差。完整逐版驗證細節見根層 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-358（本節為其權威索引，不重複散文敘述）。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.01/` ～ `AISDLC_SDD_v0.29/`（29 個凍結基線版本）+ LATEST（v0.30）共 30 個版本的 `tools/fsm_runtime/state_loader.py`；新增共用模組 `AISDLC_SDD/scripts/component_sanitizer.py`（不屬任何版本，比照既有 `copy_on_evolve.sh`／`sdd_version.py` 共享 CI infra 免 Copy-on-Evolve 先例） |
| **日期／signoff** | 2026-07-25（R45 跨平台相容性複審，架構師建議＋使用者裁決）；🔴 人工 signoff：主控以 `AskUserQuestion` 列出「抽共享層／手動逐版 backport／維持現狀」三案，使用者明確選擇「抽成共享層(架構最佳化)」 |
| **打破 Copy-on-Evolve 的理由** | R44 記載的 DEF-101-358 殘留落差：29 個凍結版本的 `_sanitize_component()` 只擋路徑分隔符（弱化版），未比照 v0.30 做 Windows 保留裝置名／禁用字元／控制字元／長度上限強化。Architect 架構複審指出：若繼續維持「30 份各自複本」的架構，未來每次發現新的淨化盲點都要重複走一次「打破凍結基線例外」流程（R43 WindowsApps guard、R44 路徑穿越已是連續兩次同構事故）。抽共享層一次性解決「同一函式 N 份拷貝」的結構性根因，往後同類強化只需改一處。 |
| **修法** | 新增 `AISDLC_SDD/scripts/component_sanitizer.py`（含 v0.30 既有強化版 `sanitize_component()` 實作，零版本特有狀態、零業務語意的平台/安全工具函式）；30 個版本的 `state_loader.py` 皆改為以 `importlib.util.spec_from_file_location` 依絕對路徑載入該共用模組（不透過 `sys.path` 插入，避免跨版本模組快取汙染），`_sanitize_component` 成為指向共用函式的薄委派；v0.30 原本內嵌的獨立實作一併移除，改與 29 個凍結版本共用同一份原始碼實作（同一支 `component_sanitizer.py` 檔案；因 `_load_shared_component_sanitizer()` 刻意不寫入 `sys.modules`（避免跨版本模組快取汙染），30 個版本各自對這份原始碼獨立 `exec_module()` 一次，故精確而言是「30 個各自獨立的函式物件執行同一份共用程式碼」，非跨版本共用同一顆記憶體物件）。**不升版、不新增版本目錄**，僅原地補丁既有 29 個凍結版本 + 修改 LATEST。 |
| **TLC 證據** | N/A——本次改動限於 `_sanitize_component()` 的取得方式（從「內嵌定義」改為「委派共用模組」），純函式的輸入輸出行為對 v0.30 而言不變，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`/`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | 新增 `tools/tests/test_component_sanitizer_shared_layer_lock.py`（4 case，behavioral 驗證 30 個版本皆委派同一份共用原始碼（同一支模組檔案）、且對已知危險輸入〔保留裝置名／禁用字元／控制字元／超長字串〕行為一致，subprocess 隔離避免同名模組跨版本快取誤判）；R44 既有回歸鎖 `tools/tests/test_sanitize_component_frozen_sdd_versions_lock.py`（6 case）與 v0.30 `test_state_component_sanitizer_parity.py`／`test_sanitize_component_call_site_lock.py` 皆重跑確認零回歸。全套驗證：`AISDLC_SDD_v0.30` 1721 passed／8 skipped；`ci-gate.sh` 全綠（含 v0.01 凍結基線 1478 passed）；`AutoClaude` 3668 passed；根層 `tools/tests` 467 passed（1 個既有已知失敗 DEF-101-351，非本輪回歸）。R45 四方複審（Architect/SA/SD/QA）一審獨立發現並訂正：(a) 本節與缺陷帳本／新測試 docstring 原皆誤寫「共用同一顆函式物件」，已訂正為上述精確描述；(b) 缺陷帳本歸檔 `archive_21.md` 初版誤含 3 筆實為 open 狀態的條目（DEF-01-007／DEF-101-242／DEF-101-243），已移回主帳本。 |
| **殘留落差** | DEF-101-358 本輪透過本次架構變更 fixed@R45（見缺陷帳本該筆記載），但這個修法本身引入一個新的架構取捨，誠實記載：**29 個凍結版本「`_sanitize_component()` 的行為」從此不再完全由各版本自己凍結的原始碼決定，而是委派給 `AISDLC_SDD/scripts/component_sanitizer.py`——一支明確不受 Copy-on-Evolve 保護、可被自由修改的共用檔案**。往後任何人修改 `component_sanitizer.py`，29 個凍結基線對這一項行為會立即、同步、無痕地跟著變動，不需要碰它們自己的原始碼樹。這正是本次重構「改一處、全版本立即生效」的設計目的，但代價是：對此函式而言，「凍結」僅止於原始碼層級（各版 `state_loader.py` 檔案本身逐位元組不變），不再涵蓋行為層級（實際執行結果可隨共用模組演進而變）。此取捨經使用者於架構決策時知情核准，非本輪新增的未揭露風險，僅在此明確記載供未來查詢「凍結基線的『凍結』確切涵蓋什麼」時參考。另，Architect 建議之「修復 LATEST 端 AST 掃描器兩個已知盲區並擴大唯讀掃描至全部凍結版本」（可偵測未來新增的未淨化呼叫點，非本次共享層要解決的問題）留待下一輪評估，見 DEF-101-378。 |
| **回退指引** | 若日後需要回退（例如共用模組載入機制本身出問題），30 個版本的 `state_loader.py` 皆可獨立還原各自原本的內嵌定義（29 版還原弱化版、v0.30 還原強化版），刪除 `AISDLC_SDD/scripts/component_sanitizer.py` 即可；**不建議回退**——回退會使 29 個凍結版本重新失去 Windows 保留裝置名等防護縱深。回退不影響任何版本的 API/FSM 狀態/`*.tla`（純函式取得方式變更，行為輸出對外呼叫端不變）。 |

## 凍結基線例外：v0.01～v0.29 `path_cost.py` 檔名淨化回補（R46，2026-07-26）

> **注意**：本節同 R44／R45 兩節，是對既有 29 個**凍結基線版本原地打補丁**的例外記錄（非正常 Copy-on-Evolve 演化）。這是繼 R44（P0 路徑穿越回補）、R45（`_sanitize_component()` 共享層抽取）之後，帳本史上**第三次**對凍結基線做例外回補。完整逐版驗證細節見根層 `docs/06_quality/AutoSDD_Defect_Log_archive_22.md` 的 `DEF-101-379`（與同輪 `DEF-101-378` 掃描器擴面互為因果；本節為其權威索引，不重複散文敘述）。
>
> 🔴 **本節為 R60 補記，不是 R46 當時寫的**：R46 落地時漏補本節，於是照 `DEF-101-370` 指定的路徑（本檔＝「Copy-on-Evolve 是否曾被打破」的權威索引）查「例外發生過幾次」只會查到 2 次——這正是 `DEF-101-392`／`DEF-101-401` 把次數寫成「兩度」的一條來源（見 `DEF-101-549`／`DEF-101-550`，以及 `docs/04_planning/ADR/ADR-XPLAT-001-copy-on-evolve-frozen-baseline-backport.md` §3.3／§7）。次數本體以 git 獨立覆核（R60 實跑，判準寫明以免下一位重算不出同一個數）：`git log --since=2026-07-25 -- 'AISDLC_SDD/AISDLC_SDD_v0.0[1-9]/**' '…v0.1*/**' '…v0.2[0-9]/**'` 只回 **3 支**——`68c159d`（R44）／`6b24eb7`（R45）／`aa5c075`（R46），故 R46 之後零第四次**破例回補**。🔴 **R60 round 3 補注（判準需人工判讀，不可純看 commit 數）**：R60 收輪 commit 之後，上列 `git log` 判準會回**第 4 支** commit——它是本檔下方〈凍結基線修改：v0.01 `test_slv_generator.py`…〉那一節，屬 `ADR-XPLAT-001 §4 步驟 0.3` 明文排除的「平台無關一般缺陷」且只動測試檔，**不是經核准的破例回補**。故「經核准的破例回補」次數仍為 **3**；判準本身不變，但讀出來的 commit 必須逐支對照本檔章節分類。⚠️ 不要改用「凍結版樹的全部歷史」當判準：那會一併撈到 `f40f5c7`（R40，修 bot 回寫 v0.01 的治理缺口）與各版建版／bot commit，那些**不是**經核准的破例回補。本次回補逐檔實查＝29 支 `path_cost.py`（`v0.01`~`v0.29`，**不含 v0.30**）。另註：R45 章節寫「這是繼 R44 之後…第二次」，該敘述在當時為真、不需訂正。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.01/`～`AISDLC_SDD_v0.29/`（29 個凍結基線版本）× `tools/fsm_runtime/path_cost.py::_write_milestone()`，每版兩個組檔名成分（`subagent`／`classification`）；不含 v0.30（LATEST 早在 R39 commit `d4c7d8d` 即已修好）。破例 commit＝`aa5c075`。 |
| **日期／signoff** | 2026-07-26（R46 跨平台相容性複審）；🔴 人工 signoff：使用者明確核准第三次打破 Copy-on-Evolve。**但核准依據事後被證偽**——核准當下引用的「P1／PoC 實測確認寫出樹外／`oqs_calibration.py` 與 `value_planner.py` 兩支 production 模組皆呼叫 `record_sample()` 觸達」諸項宣稱，經 R46 四方一審 SA 要求、由主控以 `git show HEAD:…/path_cost.py` 取修復前原始碼逐字重現＋全域 grep 複核後，證實**皆為誤植**（詳見「殘留落差」欄）。 |
| **打破 Copy-on-Evolve 的理由** | R44（`DEF-101-357`）回補同批次時只列了 3 個檔（`production_to_fpl.py`／`sandbox_runner.py`／`hub_merge.py`），**漏了 `path_cost.py` 這第 4 個**，故 29 份凍結副本從未回補、也從未被任何掃描器發現過。核准判準逐字：「機械套用與 v0.30 相同的一行改法（**比照 R44 判例**）……即使目前無可觸達路徑，仍是防禦縱深與跨版本一致性的正確修法」，並同時作為 `DEF-101-378` 掃描器擴面後的首個驗證標的。 |
| **修法** | 逐版加 `from .state_loader import _sanitize_component`（確認 29 版 `state_loader.py` 皆已匯出），組檔名行的兩個成分各包一層 `_sanitize_component()`；修復後 `diff v0.29 v0.30 path_cost.py` 零差異。**不升版、不新增版本目錄**，僅原地補丁既有 29 個凍結版本。 |
| **TLC 證據** | N/A——改動限於 `_write_milestone()` 的字串淨化呼叫點，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`/`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | 主控親手在 `v0.15/path_cost.py` **注入回歸**（拿掉 `_sanitize_component()` 包裝）證實同輪新落地的跨版本掃描鎖 `AISDLC_SDD/scripts/tests/test_sanitize_component_callsite_frozen_versions.py` 會真的轉紅，驗證後即還原；修復後全 30 版掃描零 offender。同輪 `AISDLC_SDD/scripts/tests/test_component_sanitizer_callsite_scan.py` 共 27 case（21 基礎 ＋ QA 一審 2 ＋ SD 一審 2 ＋ SD 二審 2）以獨立構造的 fixture 驗新 AST 邏輯（`BoolOp`／`IfExp` 遞迴拆解、有界別名追蹤 5-hop 邊界值、逐作用域隔離含 `ClassDef`）的鑑別力，而非只靠「掃現有程式碼零 offender」這種對新邏輯零鑑別力的整合測試。 |
| **殘留落差** | 🔴 **本次的真正教訓＝核准依據事後被證偽，但修復未回退**（理由：修法本身無害且是正確工程實務），只訂正帳本敘事：實際攻擊效果是 `FileNotFoundError`（DoS 類）而非可逃出樹外的路徑穿越——字面前綴 `CALIBRATION-MILESTONE-` 與 `{subagent}` 之間沒有分隔符，第一個路徑分段永遠是 `CALIBRATION-MILESTONE-..`，且 `_write_milestone()` 只對固定的 `out_dir` 呼叫 `mkdir`、從未對 `p.parent` 呼叫；且 `PathCostEstimator.record_sample()` 在全部 30 版的 `tools/fsm_runtime/` 生產碼**零消費者**（`oqs_calibration.py`／`value_planner.py` 僅在註解/docstring 提及「path_cost」字樣，未 import 或呼叫），嚴重度由 P1 下修為 **P3**。這代表**破例判準曾以錯誤前提被滿足過一次**——`ADR-XPLAT-001` §4 因此把「可觸達性／可利用性必須先取證」抬成破例的**前置**條件而非事後補說明（§3.3 判例）。另 `build_alias_map` 的「模組層級別名被函式引用」「巢狀函式 closure 引用外層別名」兩個更深的跨作用域案例，經 SD／Architect 三審皆確認接受「不修、誠實記載為已知限制」的收尾（見該模組 docstring 方法論邊界段落；比例原則，全 repo 現況零真實呼叫點）。 |
| **回退指引** | 若日後需要回退（例如發現本次修法本身有誤），逐版還原 29 支 `path_cost.py` 的 import 與兩處包裝即可；**不建議回退**——即使目前無可觸達路徑，回退會讓凍結版與 LATEST 再度分歧，並使 `DEF-101-378` 的跨版本掃描鎖轉紅。回退不影響任何版本的 API/FSM 狀態/`*.tla`（純字串淨化呼叫，無其他變更）。 |

## 凍結基線修改：v0.01 `test_slv_generator.py` 樹內共用暫存目錄根治（R60 round 3，2026-07-30）

> **注意**：本節與上方 R44／R45／R46 三節**不同類**，特此劃清界線以免被誤計為第四次破例——
> 那三節是走 `ADR-XPLAT-001` 核准程序的**跨平台缺陷 P0/P1 回補**（範圍 v0.01~v0.29 生產碼）；
> 本節是**單一凍結版（v0.01）測試檔**的並行競態根治，**依 `ADR-XPLAT-001 §4 步驟 0.3` 逐字
> 「平台無關的一般缺陷不走本 ADR」而不適用該程序**（缺陷是「兩個 pytest 行程共用同一個樹內固定
> 暫存目錄」，與 Windows／macOS 行為差異無關）。仍在本檔立節，因為本檔是
> `DEF-101-370` 指定的「Copy-on-Evolve 是否曾被打破、在什麼條件下」權威索引——
> **v0.01 的測試檔自 2026-06-12 初始匯入後從未被改過，本輪是第一次**（`git log` 實查），
> 這件事本身就該留痕，即使它不是 ADR 意義下的破例。
> 完整缺陷細節見根層 `docs/06_quality/AutoSDD_Defect_Log.md` 的 `DEF-101-595`。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.01/tools/fsm_runtime/tests/test_slv_generator.py`（3 處）＋ `AISDLC_SDD_v0.30/`（LATEST，可修改版）同檔（4 處，多一個 `FplDedupGateTests`／`_tmp_dedup_rules`）。**不含 v0.02~v0.29**（見「具名殘留」欄）。 |
| **日期／signoff** | 2026-07-30（R60 round 3 收斂波，回報者 `QA-R60R3-01`／`ARCH-R60R3-05` 二方獨立命中同一根因）；🔴 主控裁決採納，理由三條：①`ADR-XPLAT-001 §4 步驟 0.3` 明寫平台無關的一般缺陷不走本 ADR；②v0.01 是 `scripts/ci-gate.sh` **真的會跑**的那棵樹，不修等於留著一道會說謊的閘門；③改的是**測試檔的暫存目錄**、不改框架行為，與 R44/R45/R46 三次動生產碼的破例性質不同。 |
| **打破 Copy-on-Evolve 的理由** | 嚴格說本輪並未觸發 Copy-on-Evolve 例外程序（見本節開頭劃界）。動機是：該測試用 `Path(__file__).resolve().parent / "_tmp_rules"`／`_tmp_imm_rules` 當共用暫存區且 `tearDown` 呼叫 `rmdir()`，任何兩個並行的 pytest 行程必互踩；四方複審協定要求「四方各自重跑全套」，與此設計結構性衝突，於是每一輪都在製造無法歸因的假紅。 |
| **修法** | 改 `tempfile.mkdtemp()` ＋ `tearDown` 用 `shutil.rmtree(ignore_errors=True)`。**刻意不採「只加 `missing_ok=True`」**——那只讓競態不拋例外，兩個行程仍在同一個目錄裡互相污染資料。不升版、不新增版本目錄。 |
| **TLC 證據** | N/A——改動限於測試檔的暫存目錄取得方式，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`／`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | **修復前先證明載具有鑑別力**：v0.01 三輪並行、6 跑之中 **4 跑翻紅**（`test_verified_rule_cannot_be_overwritten` FAILED）。**修復後**：v0.01 ＋ v0.30 各三輪並行，**12/12 全綠**（30 passed／35 passed，rc 全 0）。載具刻意給兩行程不同 `PYTHONPYCACHEPREFIX`，把 `DEF-101-268`（位元碼快取競態）這個已知的另一成因排除，確保只量測「共用固定路徑」單一變因。`scripts/ci-gate.sh` 三軌計數完全未變。防復發鎖＝根層 `tools/tests/test_platform_neutral_paths.py::TestNoInTreeWritableTmpDir`（9 支，零新增檔）：對 `git show HEAD:` 的修復前原始碼跑判準 v0.01 **6 hits**／v0.30 **8 hits**（逐行指到違規行）、修復後 **0**；五棵樹實掃假陽性 **0**（唯一命中是 `AutoClaude/tests/equivalence/test_kernel_snapshot.py` 的入庫 golden 快照目錄，經判定合法 ⇒ 具名豁免 `# tmpdir-ok:` ＋ stale 自檢）。 |
| **具名殘留（不修，非疏漏）** | **`v0.02`~`v0.29` 共 95 個同型站點未修**。判準：`ADR-XPLAT-001 §2` 已機械確認這 28 版**不在任何自動閘門的執行面內**（`ci-gate` 只跑 v0.01 凍結基線與 LATEST），回補不會讓任何測試變綠或變紅。C3 鎖已明文排除該區，並在 docstring 寫明「那 95 個站點確實存在且未修，是待裁決項，不是本鎖宣稱乾淨的區域」。**另有一個同型但根因不同的殘留交 R61**：`AutoClaude/tools/hub_sync.py:478` 把 outbox 路徑寫死在**生產碼**裡，使 `test_hub_sync.py` 有同型並行競態——那不能靠改測試根治，見 `DEF-101-596`（routed@R61）。 |
| **回退指引** | 還原兩支測試檔的暫存目錄取得方式即可；**不建議回退**——回退會讓 `TestNoInTreeWritableTmpDir` 立刻轉紅，且重新打開四方複審並行重跑必假紅的路徑。回退不影響任何版本的 API／FSM 狀態／`*.tla`（純測試檔暫存目錄變更）。 |

## 凍結基線例外：v0.01 測試假 SHA drift 殘留檔移除（R107，2026-08-28）

> **注意**：本節經掌舵者明確核准（見 signoff 欄），但與 R44／R45／R46 三節性質不同——那三節是動**生產碼**的破例回補，本節是**純刪除 4 支從不該入庫的測試產物**（零程式碼變更）。依 R60 節補注的計數判準（`git log` 撈凍結版樹 commit 後逐支對照本檔章節分類）：**「經核准的破例回補」次數不因本節改變（仍以該判準逐支人工判讀）**，含本次刪除的 commit 應歸入本節、不得計為回補。完整缺陷沿革見根層 `docs/06_quality/AutoSDD_Defect_Log.md` 的 `DEF-101-338`（R40 發現、R60 查證仍 tracked、R107 掌舵者裁決結案）。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.01/build/reports/drift/` 下 4 支測試假 SHA 檔：`COMMIT-sha-3rd.yaml`／`COMMIT-sha-high.yaml`／`COMMIT-sha-low.yaml`／`COMMIT-testsha-001.yaml`。**不含**同目錄 `COMMIT-769eea4e3f66.yaml`（真 SHA 形態、性質不同〔`DEF-101-329` 族〕，保留不動）。 |
| **日期／signoff** | 2026-08-28（R107 帳本結案輪）；🔴 人工 signoff：掌舵者 2026-08-28 就 `DEF-101-338` needs-user 裁決，原話「核准 git rm（推薦）」——依 `ADR-XPLAT-001` 先例（R44/R45/R46）取得凍結版例外核准後執行。 |
| **打破 Copy-on-Evolve 的理由** | 4 檔命名為測試假 SHA（`sha-low`／`sha-high`／`sha-3rd`／`testsha-001`），是早期測試執行未隔離、誤寫入真實 repo 路徑而被意外 commit 的歷史殘留 artifact；現行 `AISDLC_SDD_v0.01/tools/fsm_runtime/tests/test_drift_monitor.py` 全數用 `tmp_path`（R107 實查 34 處、全檔零 `build/reports` 引用），寫出這些檔的根因已不存在。留著它們＝凍結版樹內掛著假資料誤導「drift 報告長什麼樣」的讀者。刪除不是回補，是把從不該入庫的東西移出去。 |
| **修法** | `git rm` 該 4 檔（index 與工作樹同步移除）；**不升版、不新增版本目錄、零程式碼變更**。 |
| **TLC 證據** | N/A——純刪除 4 支 YAML 測試產物，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`／`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | 刪除前查證：檔名全庫 Grep 僅 `docs/06_quality/` 三支帳本／證據檔命中（歷史記載，非消費者），**零程式碼消費者**；`reports/drift` 路徑字面全庫 `*.py` 零命中（無任何 Python 鎖／生產碼掃該目錄）。刪除後實跑：`git status --short` 該目錄 4 列 `D `；v0.01 `pytest tools/fsm_runtime/tests/test_drift_monitor.py -q` → **10 passed, rc=0**。 |
| **回退指引** | 自刪除前的 commit 以 `git checkout <該 commit> -- 'AISDLC_SDD/AISDLC_SDD_v0.01/build/reports/drift/COMMIT-sha-*.yaml' '.../COMMIT-testsha-001.yaml'` 逐檔取回即可；**不建議回退**——回退等於把假 SHA 測試產物重新放回凍結版樹。回退不影響任何版本的 API／FSM 狀態／`*.tla`（純資料檔增刪）。 |

## 凍結基線例外：v0.02～v0.04 測試假 SHA drift 殘留檔移除（R115，2026-09-01）

> **注意**：本節與上方 R107 節**同類**（純刪除從不該入庫的測試產物，零程式碼變更；非破例回補）——R78 Debt_Audit :122 已記載 DEF-101-338 的實際污染面比 v0.01 更廣（v0.02/v0.03/v0.04 各 4 支、共 12 支），R113 立呈報單、R114 掌舵者裁「下輪再議」、R115 重呈獲核。依 R60 節補注的計數判準：**「經核准的破例回補」次數不因本節改變**，含本次刪除的 commit 應歸入本節、不得計為回補。完整缺陷沿革見根層 `docs/06_quality/AutoSDD_Defect_Log.md` 的 `DEF-101-338`（R107 已結案；本節為其污染面掃尾）。

| 欄位 | 內容 |
|------|------|
| **範圍** | `AISDLC_SDD_v0.02/`～`AISDLC_SDD_v0.04/` 三版 `build/reports/drift/` 下各 4 支測試假 SHA 檔：`COMMIT-sha-3rd.yaml`／`COMMIT-sha-high.yaml`／`COMMIT-sha-low.yaml`／`COMMIT-testsha-001.yaml`，共 12 支。**不含**同目錄 `COMMIT-769eea4e3f66.yaml`（真 SHA 形態、`DEF-101-329` 族，三版皆保留不動）。 |
| **日期／signoff** | 2026-09-01（R115 技術債結案輪）；🔴 人工 signoff：掌舵者 2026-09-01 就 R114_HANDOFF 呈報單第 2 件互動裁決，選項原文「核准 git rm（Recommended）」——同 R107 判例（DEF-101-338 v0.01 四支之掌舵者核准先例）。 |
| **打破 Copy-on-Evolve 的理由** | 與 R107 節同一根因：早期測試執行未隔離、誤寫入真實 repo 路徑而被意外 commit 的歷史殘留 artifact，隨 Copy-on-Evolve 複製被帶進 v0.02～v0.04 三版。寫出這些檔的根因已不存在（R107 實查 `test_drift_monitor.py` 34 處全用 `tmp_path`）。刪除不是回補，是把從不該入庫的東西移出去；且 R89 判例已載明 Copy-on-Evolve 管「改」、刪除誤入污染物不在其射程。 |
| **修法** | `git rm` 該 12 檔（index 與工作樹同步移除）；**不升版、不新增版本目錄、零程式碼變更**。 |
| **TLC 證據** | N/A——純刪除 12 支 YAML 測試產物，未觸碰任一版本的 `_HAPPY_PATH` 或任何 `*.tla`／`.cfg`，各版既有五軌 TLC 證明維持有效。 |
| **驗證** | 刪除前查證：`COMMIT-sha-(high\|low\|3rd)`／`COMMIT-testsha-001` 樣式於 `AISDLC_SDD/` 全樹 Grep 僅命中本檔（存證文件自身），**零程式碼消費者**；`git ls-files` 現查 12 支確實仍被追蹤（R114 快照與 R115 開場皆現查一致）。刪除後實跑：`git status --porcelain` 該三目錄恰 **12 列 `D `**、真 SHA 檔與 `DAILY-*.md` 全數健在（`git ls-files` 逐列覆核）。根層全套 unittest 於 R115 收輪窗口統一重跑（ci-gate 不測中間歷史版，無版內測試軌可跑——此點與 R107 的 v0.01 凍結基線不同，如實記載）。 |
| **回退指引** | 自刪除前的 commit 以 `git checkout <該 commit> -- 'AISDLC_SDD/AISDLC_SDD_v0.0[234]/build/reports/drift/COMMIT-sha-*.yaml' '.../COMMIT-testsha-001.yaml'` 逐檔取回即可；**不建議回退**——回退等於把假 SHA 測試產物重新放回凍結版樹。回退不影響任何版本的 API／FSM 狀態／`*.tla`（純資料檔增刪）。 |

## LATEST 修改備忘：hub-push.yml sample action 升版＝「30 版同一 blob」不變量正式分裂（R107，2026-08-28）

> **注意**：本節**不是**凍結基線例外（只動 LATEST v0.30，LATEST 本就可原地改）；立節是因為
> `docs/04_planning/ADR/ADR-XPLAT-011-r99-known-boundary-declarations.md` §4 條件② 明文要求
> 「若升，須同時接受『30 版此檔不再是同一 git blob』並在該版 EVOLUTION_LOG.md 記錄」。
> 🔴 人工 signoff：掌舵者 2026-08-28 就 `DEF-101-559` needs-user 裁決，原話「是否以後會用,
> 最佳化是否該升? 若真的都用不到, 當然不升」——查證落在「會用」分支：該檔檔頭與
> `knowledge/hub/REGISTRY-SPEC.md` 明文指示下游「將此檔複製到 Hub repo 的
> `.github/workflows/hub-push.yml`」後真實執行，且 REGISTRY-SPEC 驗收清單要求其 4 job 全綠
> 才可 merge（非純展示），故升。
> **改動**＝`AISDLC_SDD_v0.30/.github/workflows/hub-push.yml` 對齊根層 `.github/workflows/`
> 基準：`checkout@v4→v5`（4 站點）、`setup-python@v5→v6`（3 站點）、`upload-artifact@v4→v6`
> （1 站點）；v0.01~v0.29 凍結版依 Copy-on-Evolve 不動（維持 Node20 世代）。機械鎖同步：
> 根層 `tools/check_gha_action_versions.py` 的 `_NESTED_DISCLOSED_GENERATION` 改登記兩世代
> 聯集（該檔〈掃描面邊界〉⚠️ 段同步改寫）。**回退**＝還原該檔 8 處版本字串並把上述快照
> 改回單世代登記；不影響任何版本的 API／FSM 狀態／`*.tla`（sample 在本 repo 永不執行）。


## v0.29 → v0.30

| 欄位 | 內容 |
|------|------|
| **版本** | v0.29 → v0.30 |
| **日期** | 2026-06-30（凍結來源：AutoSDD_improving_98.md，B 軌 P3 doc-lag 校正 DEF-62-001；🔴 人工 signoff：掌舵者 2026-06-30 AskUserQuestion 裁定本輪 W 項＝P3 清理批次、並裁 DEF-62-001 真修＝Copy-on-Evolve v0.30 校正註解，明確授權建新版改凍結本體一行註解） |
| **delta 清單** | **B 軌（DEF-62-001 真修）：auto_recovery call-site 滯後註解極性校正（純註解、零行為變更）。** 缺口：auto_recovery 自 v0.22（improving_57）翻預設 ON，但 `tools/fsm_runtime/fsm_runtime.py:420` call-site 註解仍寫「預設 OFF＝行為同 v0.05」，歷經每次 Copy-on-Evolve 逐字繼承至 v0.06~v0.29 每版（improving_62 Architect 鏡揪出、跨多輪 routed 之文件債）。同檔 L39 檔頭與 L51-55 `_auto_recovery_enabled()` 早已正確寫「v0.22 預設 ON」，僅 L420 一行 stale（`grep "預設 OFF"` 全檔僅命中此行）。本輪在 v0.30 改 **`tools/fsm_runtime/fsm_runtime.py:420-421` 唯一處**：「預設 OFF＝行為同 v0.05（停在 ESCALATION 等人）」→「v0.22 起預設 ON（顯式 opt-out=0 才還原 v0.05 停在 ESCALATION 等人）」，與 L39/L51 極性一致。**純註解時間差校正、零執行語意變更**（`_auto_recovery_enabled()` 實作 unset→True 本就正確）——無 agent/governance/workflow/FSM/`*.tla`/`.cfg` 變更，不取新 ACT/Rule。同根因家族 DEF-58-002/59-001/96-001「翻環/建版後 SSOT 同步」之文件變體。 |
| **TLC 證據** | **N/A — `_HAPPY_PATH` 與全部 `*.tla`/`.cfg` 對 v0.29 逐位元零差異**（純改一行 Python 註解，未動形式化模型 / FSM 狀態 / 規則），依 Rule 9.18.1 無重跑義務；既有五軌 TLC 證明維持有效。 |
| **驗證** | 見 improving_98.md §5（ci-gate exit 0 含 v0.01 + v0.30 雙軌、v0.30 not-chaos ＝ floor v0.29:1665；AutoClaude 端零碰、≥3607 passed；三鏡 zero-trust）。本輪同時端到端實證 improving_97（DEF-96-001）的「建版後自動重生 FRAMEWORK_STATUS.md」三同步 block 跨版運作。 |
| **回退指引** | 純向後相容（純註解）：還原 `fsm_runtime.py:420-421` 註解文字即還原；零執行語意、零 API/狀態/`*.tla` 變更。既有 FSM-STATE 狀態檔完全相容。 |

## v0.28 → v0.29

| 欄位 | 內容 |
|------|------|
| **版本** | v0.28 → v0.29 |
| **日期** | 2026-06-29（凍結來源：AutoSDD_improving_96.md，A 軌 PRD→playbook 橋接 backend-robust 化；🔴 人工 signoff：掌舵者 2026-06-29 AskUserQuestion 裁定本輪 W 項＝候選 (a) DEF-95-002 修，授權 Copy-on-Evolve v0.28→v0.29 改 Archy evaluator 政策） |
| **delta 清單** | **A 軌（DEF-95-002 修復）：Archy doc/spec 步 evaluator 政策由「keyword 回顯」改「artifact-existence」。** 缺口：improving_95 端到端真跑揭露——Archy 對 doc/spec 產檔步（如 SPEC.md）的把關靠 `expected_output_regex` 完成關鍵字（`[SPEC_DONE]`），而 pty 後端 `claude --output-format json` 對寫檔步擷取脆弱（DEF-81-001 族）會吞掉 keyword，致檔案明明寫對（SPEC.md 4151 bytes）卻 regex 不過 → escalated 整輪停（pty 0/5；sdk 5/5）。本輪在 v0.29 改 **`agent/specialized/sdd-prd-to-playbook-zh.yaml`（Archy）** 唯一檔：core_principles 補「evaluator backend-robust 化」原則 + outputs 契約改寫——doc/spec 產檔步改用 **artifact-existence evaluator** `python -m autoclaude.artifact_check <相對產出檔> --min-bytes N`（檢查檔案真的產生、後端無關），`expected_output_regex` 留空、不靠 keyword 回顯；version 欄 v0.28→v0.29。配合 AutoClaude 端（C 軌，非 v0.29 本體）：新增 `autoclaude/artifact_check.py`（pip 套件內葉 CLI，任意 cwd 可解析）+ fixture E-SPEC-1 改用 artifact evaluator + 連帶測試更新。**純改 1 個 agent YAML evaluator 指引 + version 欄**——無 governance/workflow/FSM/`*.tla` 變更，不取新 ACT/Rule。evaluator 白名單自律不變（仍 pytest/python 開頭、無 shell 元字元、fail-closed）。 |
| **TLC 證據** | **N/A — `_HAPPY_PATH` 與全部 `*.tla`/`.cfg` 對 v0.28 逐位元零差異**（純改 agent YAML evaluator 指引文字 + version 欄，未動形式化模型 / FSM 狀態 / 規則），依 Rule 9.18.1 無重跑義務；既有五軌 TLC 證明維持有效。 |
| **驗證** | 見 improving_96.md §5（ci-gate exit 0 含 v0.01 + v0.29 雙軌、v0.29 not-chaos ≥ floor v0.28；AutoClaude 端 artifact_check 6 單測 + 橋接 harness 測試更新 + sanitize 白名單回歸鎖 + bridge e2e pty 後端真跑證 DEF-95-002 closed；三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（additive evaluator 政策）：還原 `sdd-prd-to-playbook-zh.yaml` 的 core_principles 新增原則與 outputs 契約敘述（+ version 欄）即還原 v0.28 行為。既有 Archy 產物與 Compily 路徑零影響；既有 FSM-STATE 狀態檔完全相容（零碰狀態/`*.tla`/API breaking change）。 |

## v0.27 → v0.28

| 欄位 | 內容 |
|------|------|
| **版本** | v0.27 → v0.28 |
| **日期** | 2026-06-27（凍結來源：AutoSDD_improving_94.md，A 軌 PRD→playbook 雙向協作橋接；🔴 人工 signoff：掌舵者 2026-06-27 AskUserQuestion 三問裁定「SDD 全流程驅動器 + 重用 three_tier 薄橋接 + 直接完整入框架（Copy-on-Evolve）」，明確授權動框架凍結本體新增前端橋接 agent） |
| **delta 清單** | **A 軌（雙向協作橋接，連動 B 柱 Copy-on-Evolve）：新增 PRD→playbook 前端規劃 agent。** 缺口：框架既有 `sdd-playbook-compiler`（Compily）只吃流程末端「已凍結的 TEST-CONTRACT-SPEC」編成測試 playbook，**無「由原始 PRD 規劃整條 SDD 開發路徑」的前端 agent**；且 AutoClaude 既有 three_tier 規劃模型（Project→GoalTask→ExecutionItem）與可執行 Playbook 未接通。本輪在 v0.28 新增 **`agent/specialized/sdd-prd-to-playbook-zh.yaml`（Archy，前端橋接 agent）**：讀 PRD → 產出 three_tier 結構 YAML（goal_task=SDD 階段/產品目標、execution_item=驅動一道 SDD skill 的步驟 + evaluator 檢查該關 SCG 閘門產物），交 AutoClaude 端新增的 `tools/three_tier_to_playbook.py`（C 軌，非 v0.28 本體）攤平為可執行 `playbook.yaml`（workflow_type=aisdlc_sdd）。三處註冊：`agent/README.md`、`AISDLC_SDD_INIT.md`（Specialized 19→20、+2 橋接 agent 敘述、Specialized Agents 清單表列）、本 EVOLUTION_LOG + CHANGELOG。（本 agent 為 cross-scenario bridge，不綁特定場景，故不進 auto_load_config 場景載入區。）**純新增 agent 資產 + 註冊文件**——無 governance/workflow/FSM/`*.tla` 變更，不取新 ACT/Rule。與 Compily 清楚分界（前端規劃 vs 末端編譯，agent 檔頭與 INIT 敘述明示防混淆）。 |
| **TLC 證據** | **N/A — `_HAPPY_PATH` 與全部 `*.tla`/`.cfg` 對 v0.27 逐位元零差異**（純新增 agent YAML + 註冊文件，未動形式化模型 / FSM 狀態 / 規則），依 Rule 9.18.1 無重跑義務；既有五軌 TLC 證明維持有效。 |
| **驗證** | 見 improving_94.md §5（ci-gate exit 0 含 v0.01 + v0.28 雙軌、v0.28 not-chaos ≥ floor v0.27；AutoClaude 端 W-94-1/2 model additive + compiler 26 單測 + 端到端攤平驗證；三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（additive）：移除 `sdd-prd-to-playbook-zh.yaml` 與三處註冊即還原 v0.27 行為（既有 Compily 路徑零影響）。AutoClaude 端 `PlaybookTask.goal_task_id` / `ExecutionItem` 新欄皆 Optional 預設 None，缺欄行為逐位元不變。既有 FSM-STATE 狀態檔完全相容（零碰狀態/`*.tla`/API breaking change）。 |

## v0.26 → v0.27

| 欄位 | 內容 |
|------|------|
| **版本** | v0.26 → v0.27 |
| **日期** | 2026-06-27（凍結來源：AutoSDD_improving_85.md，A 軌 spec-format-version「生產端」閃閉；🔴 人工 signoff：掌舵者 2026-06-27 AskUserQuestion 裁定「Copy-on-Evolve v0.27（閃閉端到端）」，明確授權動框架凍結本體新增生產端版本宣告） |
| **delta 清單** | **A 軌（雙向協作橋接，連動 B 柱 Copy-on-Evolve）：spec-format-version「生產端」閃閉。** improving_67（W-67-2）已在 AutoClaude 消費端建 `SddToPlaybookAdapter._check_spec_format_version` 防漂移閘（擷取 `spec-format-version`、缺欄預設 1.0、不支援版本 fail-closed），但生產↔消費契約**單邊**：SDD 框架的 TCS 模板**從不宣告**版本（grep v0.26 docs_template 零命中）→ 真實框架 spec 永遠走「缺欄→靜默預設 1.0」分支（`declared=False`），防漂移閘對真實 spec 形同死碼。本輪在 v0.27 補上**生產端**：4 份被 adapter glob（`*CONTRACT*SPEC*` / `TCS-*`）命中、會被 `load_spec` 讀取的 spec 生產模板各加宣告行 `**spec-format-version**: 1.0`（格式經 adapter `_SPEC_VERSION_RE` 實測匹配為 "1.0"；table-cell / backtick 格式經實測不匹配故避開）：① `docs_template/sdd/testing/TEST-CONTRACT-SPEC-TEMPLATE.md`（主 TCS）② `CONTRACT-TEST-SPEC-INTEGRATION-TEMPLATE.md` ③ `CONTRACT-TEST-SPEC-MIGRATION-TEMPLATE.md` ④ `ENV-CONTRACT-SPEC-TEMPLATE.md`。配合 AutoClaude 消費端收尾（落 AutoClaude repo，非 v0.27 本體）：`SddSpec` additive `spec_format_version` 欄表面化已驗證版本（原 `load_spec` 驗證後即丟棄）+ `sdd_compile.main()` 接 `SpecFormatVersionError` 乾淨退碼 5（原漏接 → traceback）。**效果**：真實框架 spec 首次帶 `declared=True`，improving_67 防漂移閘端到端可驗證。**純模板 metadata 行**——無 agent/governance/workflow/FSM/`*.tla` 變更，不取新 ACT/Rule。 |
| **TLC 證據** | **N/A — `_HAPPY_PATH` 與全部 `*.tla`/`.cfg` 對 v0.26 逐位元零差異**（純 docs_template metadata 行新增，未動形式化模型 / FSM 狀態 / 規則），依 Rule 9.18.1 無重跑義務；既有五軌 TLC 證明維持有效。 |
| **驗證** | 見 improving_85.md §5（ci-gate exit 0 含 v0.01 + v0.27 雙軌、v0.27 not-chaos ≥ floor v0.26、4 模板宣告經真實 adapter regex 解析 "1.0"、AutoClaude 消費端 round-trip + MUT-85-1/2 突變實證非空殼、三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（additive）：生產端為模板 metadata 行，移除即還原 v0.26 行為（消費端缺欄仍預設 1.0）；AutoClaude 消費端 `spec_format_version` 欄有 default "1.0"，缺欄 spec 行為逐位元不變。既有 FSM-STATE 狀態檔完全相容（零碰狀態/`*.tla`/API breaking change）。 |

## v0.25 → v0.26

| 欄位 | 內容 |
|------|------|
| **版本** | v0.25 → v0.26 |
| **日期** | 2026-06-25（凍結來源：AutoSDD_improving_66.md，B 軌可解釋性轉向 GAP-Y2「最後一哩」實作輪；🔴 人工 signoff：掌舵者 2026-06-25 於 improving_65 §9 批准 §8 CLI closure 方案，明確授權實作 read-only 儀表板 CLI） |
| **delta 清單** | **B 軌：meta⁸ 可審批儀表板 runtime 觸達（GAP-Y2 closure，沿用 Phase Y / ACT-161 / Rule 9.37，不取新 ID）。** v0.25（含）以前 `render_recursion_topology_dashboard`（Phase Y / ACT-160/161）**只在測試裡被呼叫**、runtime 零 wire、無 CLI 入口——舵手實際 K=1 signoff proposed 互遞迴算子時，打開 `value-dimension-ledger.yaml` 看到的仍是原始 `recursion_inventions`（帶 rank 的鄰接 dict），盲簽問題只在測試裡解決、活迴圈裡沒有（improving_65 §2.2 grep 親查實證：dashboard 呼叫點僅 `tests/test_phase_y.py:421,423`、無 `__main__`、genesis 提案路徑零吐圖）。本輪以**唯讀薄殼 CLI** 斷此盲簽鏈。**W-66-1（唯一新增源碼）`tools/fsm_runtime/render_topology_dashboard.py`**（≈160 行含 docstring）：`python -m tools.fsm_runtime.render_topology_dashboard [--ledger PATH] [--rule-id RCR-xxx] [--page N] [--fold] [--json]`——讀 ledger 的 `recursion_inventions[].selected[].operator`（即 `RecursiveOperator.to_dict()`，**直接取已序列化 op_dict、不 import `RecursiveOperator` 重建**）→ `extract_topology(op_dict, budget, page)` →**必過** `guard_visualization_bounded`（render budget + PY-2 拓樸防偽 + 接地 fail-closed）→ 印 `render_recursion_topology_dashboard`。**紅線零弱化**：①**read-only 純觀察者**（Rule 9.37.4）——只讀 ledger、零寫 FSM-STATE / 不碰 churn / 不碰 meta-loop（測試 `test_cli_is_read_only_ledger_unchanged` 證跑後 ledger byte-identical）；②**對抗分離乾淨**（Rule 9.37.4）——只 import 渲染器（`recursion_topology_view`/`steersman_renderer`）+ guard（`meta_halt_monitor`）+ `state_loader.REPO_ROOT`，**零 import 任何 generator**（AST 測試 `test_cli_source_imports_no_generator` 機械守）；③**無繞過 guard 開關**（DoD #2）——呈現前必過 guard，違反即非零 exit + 不印儀表板（fail-closed，絕不 false-green）。**W-66-2（回歸鎖）`tools/fsm_runtime/tests/test_phase_y_dashboard_cli.py`**（9 測試）：happy path（exit 0 + 三視圖）/ rule-id 命中 / `--json`；**真實 guard 觸發**（`SDD_VIZ_CHAR_BUDGET=1000` → fan op 儀表板逃逸 char_budget → guard raise → exit 3 + stdout 空，證 guard 非空殼）+ guard 被 monkeypatch raise 亦 fail-closed（證 CLI 確實呼叫 guard）；fail-loud（缺檔 / 無 recursion_inventions 段 / rule-id 不存在 → 非零 exit + 明確訊息、絕不靜默空輸出）；read-only + 對抗分離 AST 守。**無 FSM 狀態/規則/拓樸變更**：`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.25 **逐位元零差異**（diff exit 0）——CLI 僅是既有 guarded 渲染管線的新「呼叫者」（讓人類觸發同一條 guarded path），未動形式化模型；**不取新 ACT/Rule**——CLI 為既有 Phase Y / ACT-161 / Rule 9.37 儀表板的 runtime 觸點，沿用既有 ID（鏡像 improving_23 / W-23-1 folding「隸屬既有 R-9.37、不取新 ACT/Rule」先例；improving_65 §8 issue 草案曾以 "ACT-162" 為標籤命名，但 ACT-162 已由 Phase Z〔v0.02〕徵用，故實作輪明確**不徵新號、不改 ID_REGISTRY**，避免撞號並維持 `test_id_registry_next_free_advanced_phase_y` 綠）。缺陷：本輪 B 軌無新框架缺陷（GAP-Y2 closure 屬「可審批性最後一哩」加固）。 |
| **TLC 證據** | **形式化模型逐位元不變**：`formal/*.tla`/`.cfg` 與 `transition_rules.py`（`_HAPPY_PATH`）對 v0.25 `diff` exit 0 → 依 Rule 9.18.1 **無重跑義務**，既有五軌證明維持有效。closure 完全複用既有 `VisualizationBounded`（Rule 9.37 / META_FSM.tla `guard_visualization_bounded`）——CLI 不過是既有 guarded 渲染管線的新呼叫者，未改 `_HAPPY_PATH`/任何 `*.tla`/FSM 狀態/meta-loop，故**免新 `VisualizationBounded` 證明、免五軌 TLC**（improving_65 §8 TLA+ 評估之結論）。 |
| **驗證** | 見 improving_66.md §階段四（ci-gate exit 0、v0.26 not-chaos ≥ floor v0.25:1656、CLI 9 測試 PASS、Rule 9.37 突變實證非空殼〔真實 char_budget 逃逸觸發 guard〕、三鏡 zero-trust）。 |
| **回退指引** | 純新增、純向後相容：CLI 為獨立新檔，刪除即還原 v0.25 行為（既有 `render_recursion_topology_dashboard`/guard 合約皆未動）；無 breaking change（API/狀態/`*.tla` 皆未動）。既有 FSM-STATE 狀態檔完全相容。 |

## v0.24 → v0.25

| 欄位 | 內容 |
|------|------|
| **版本** | v0.24 → v0.25 |
| **日期** | 2026-06-25（凍結來源：AutoSDD_improving_63.md，B 軌成熟度加固 L5 鷹架代謝活體化；🔴 人工 signoff：掌舵者 AskUserQuestion 裁定「B 軌 scaffold_gc 翻 ON」，明確授權翻轉鷹架代謝「自動提議退役」預設） |
| **delta 清單** | **B 軌：鷹架代謝「自動提議退役」活體化（B-axis 流程自治 L5 機制加固，非升級）。** improving_17/W-17-1 已把 `scaffold_gc.run_gc`（產 RetirementProposal proposed 提議 + Markdown 報告）接入 `enter_scaffold_gc` 主迴圈，但**預設 OFF（opt-in 鷹架）**＝代謝機具齊備卻未活體（arch_fitness FF-16 GAP-X2 點名「鷹架代謝 GC 從未產退役 ROI 提案／代謝肌肉從未收縮」）。承接 v0.24 fire/catch 遙測活體化使 `scaffold_roi.fire_count/catch_count` **真實累積**（資料就位），本輪把代謝由「失敗即停等人手動跑 GC」翻轉為「**進 SCAFFOLD_GC 態自動 draft proposed 草案為 default**」＝L5 代謝活體常態，鏡像 W-15 AUTO_RECOVERY（v0.22）/ W-16 SLV_AUTO_PROPOSE（v0.23）/ W-18/19 telemetry（v0.24）翻環先例。**W-63-1（唯一程式改動）`fsm_runtime.py:_scaffold_gc_auto_propose_enabled()`**：truthy-only → 未設環境變數時回 `True`（預設 ON）；**保留顯式 opt-out**（`SDD_ENABLE_SCAFFOLD_GC_AUTO_PROPOSE=0/false/no/off` → 還原 v0.07 只記 tracking、不算 GC，供除錯/保守場景）。**紅線零弱化**：①run_gc 只產 RetirementProposal + Markdown 報告（落 `build/reports/gc/SCAFFOLD-ROI-{date}.md`）、**永不呼叫 set_maturity**（active 規則退役仍 🔴 人工，R-9.20 #11）；②fail-closed（run_gc 失敗不阻塞已落定的進態、不偽造報告路徑）；③入口紀律不被 flag 弱化（非 RELEASE 源仍 raise）。**W-63-2（關鍵零退化護欄）`tests/conftest.py`**：翻 ON 後進 SCAFFOLD_GC 態會落盤 SCAFFOLD-ROI 報告至 `GC_REPORT_DIR`（預設 build/reports/gc/），測試套中未 redirect 的裸 `enter_scaffold_gc` 測試（test_phase_h::test_scaffold_gc_enter_exit_continue/::test_scaffold_gc_respec）若吃預設 ON 會寫穿真實工作區→髒樹+非確定。新增 session autouse fixture `_isolate_scaffold_gc_default`（鏡像既有 `_isolate_rule_telemetry_default` 隔離精神），測試套**預設**把 flag 顯式設 "0" → 既有測試行為 byte-identical v0.24、零污染工作區；wiring 測試以 delenv 覆寫驗 default-ON。**W-63-3 測試對齊新預設**：`test_scaffold_gc_auto_propose_wiring.py` Case 1 由「delenv→斷言無 auto_gc」改寫為 `test_default_on_enter_auto_runs_gc`（delenv 覆寫 conftest→自動跑 GC + 填 origin=auto/roi_report_ref，以 `_redirect_gc_report` 導向 tmp）、Case 2 改名 `test_explicit_opt_out_pure_tracking_no_auto_gc`（顯式 setenv "0"→還原 v0.07 純記 tracking 零退化逃生閥）；wiring 測試數不變（9 case）。**無 FSM 狀態/規則/拓樸變更**：`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.24 **逐位元零差異**（diff exit 0）——本輪僅翻轉「既有 side-effect 記帳的 runtime 預設」+ 測試基建隔離，未動形式化模型；ID_REGISTRY 不取新 ACT/rule。缺陷：本輪 B 軌無新框架缺陷（純預設活體化 + 測試隔離護欄）。 |
| **TLC 證據** | **形式化模型逐位元不變**：`formal/*.tla`/`.cfg` 與 `transition_rules.py`（`_HAPPY_PATH`）對 v0.24 `diff` exit 0 → 依 Rule 9.18.1 **無重跑義務**，既有五軌證明維持有效（run_gc 為 `enter_scaffold_gc` 進態後純 side-effect，零新增 reachable 邊、不改狀態圖；只產 proposed 報告不碰 churn/meta-loop/set_maturity 不變量）。 |
| **驗證** | 見 improving_63.md §階段四（ci-gate exit 0、v0.25 not-chaos 1656＝floor v0.24:1656、Rule 9 突變實證非空殼、三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（opt-out 保留）：回退即把 `_scaffold_gc_auto_propose_enabled()` 改回「unset→False」（truthy-only，指回 v0.24 邏輯），或全域設 `SDD_ENABLE_SCAFFOLD_GC_AUTO_PROPOSE=0` 即還原 opt-in 行為，零 breaking change（API/狀態/`*.tla` 皆未動）。既有 FSM-STATE 狀態檔完全相容。 |

## v0.23 → v0.24

| 欄位 | 內容 |
|------|------|
| **版本** | v0.23 → v0.24 |
| **日期** | 2026-06-25（凍結來源：AutoSDD_improving_62.md，B 軌成熟度加固 L5 telemetry 活體化；🔴 人工 signoff：掌舵者 AskUserQuestion 裁定「B 軸：telemetry 翻 ON」，明確授權翻轉規則命中遙測 fire/catch 兩 flag 預設） |
| **delta 清單** | **B 軌：規則命中遙測（fire + catch）活體化（B-axis 流程自治 L5 機制加固，非升級）。** improving_18/W-18-1 已把 on-watch fire 記帳接入 `transition()` 主迴圈、improving_19/W-19-2 已把 on-attribution catch 記帳接入 escalation 落點，但兩者**預設 OFF（opt-in 鷹架）**＝遙測機具齊備卻未活體（DEF-17-001 點名「fire_count=0」根因、DEF-18-001「catch 側」皆因 opt-in 未開而懸置）。本輪把規則命中遙測由「需手動設 env 才記」翻轉為「**進態/escalation 即記為 default**」＝L5 遙測活體常態，鏡像 W-15 AUTO_RECOVERY（v0.22）/ W-16 SLV_AUTO_PROPOSE（v0.23）翻環先例。**W-62-1/2（程式改動）`fsm_runtime.py:_rule_fire_telemetry_enabled()`/`_rule_catch_telemetry_enabled()`**：未設環境變數時回 `True`（預設 ON）；**保留顯式 opt-out**（`SDD_ENABLE_RULE_FIRE_TELEMETRY`/`SDD_ENABLE_RULE_CATCH_TELEMETRY`=0/false/no/off → 還原 v0.08/v0.09 不記行為）。**紅線零弱化**：①只增 fire_count/catch_count，**永不 set_maturity**（active 規則退役仍 🔴 人工，R-9.20 #11）；②fail-closed（記帳失敗絕不阻塞已落定的轉態/escalation；catch 缺證據不記、不污染 ROI）。**W-62-3（關鍵零退化護欄）`tests/conftest.py`**：fire 遙測在**每次 transition** 寫 `RULES_DIR`（凍結 governance/rules/ 真實 R-*.yaml），測試套中未隔離 RULES_DIR 的裸 transition 測試若吃預設 ON 會寫穿凍結本體→髒樹+非確定。新增 session autouse fixture `_isolate_rule_telemetry_default`（鏡像既有 `_isolate_meta_loop_ledger` 隔離精神），測試套**預設**把兩 flag 顯式設 "0" → 既有測試行為 byte-identical v0.23、零污染凍結 governance；wiring 測試以 delenv 覆寫驗 default-ON。**W-62-4 測試對齊新預設**：`test_rule_fire/catch_telemetry_wiring.py` Case 1 由「delenv→斷言不記」改寫為 default-ON 活體驗收（delenv 覆寫 conftest→記 fire/catch）、Case 2 為顯式 opt-out 零退化守；`test_w20/w37/w38_catch_wiring.py` 共 5 個 flag_off_zero_regression 測試由 `delenv` 改顯式 `setenv("0")`（翻環後 OFF 以 opt-out 表達）。**無 FSM 狀態/規則/拓樸變更**：`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.23 **逐位元零差異**——本輪僅翻轉「既有 side-effect 記帳的 runtime 預設」+ 測試基建隔離，未動形式化模型；ID_REGISTRY 不取新 ACT/rule。缺陷：本輪 B 軌無新框架缺陷（純預設活體化 + 測試隔離護欄）。 |
| **TLC 證據** | **形式化模型逐位元不變**：`formal/*.tla`/`.cfg` 與 `transition_rules.py`（`_HAPPY_PATH`）對 v0.23 `diff` exit 0 → 依 Rule 9.18.1 **無重跑義務**，既有五軌證明維持有效（fire/catch 記帳為 transition/escalation 後純 side-effect，零新增 reachable 邊、不改狀態圖；只增計數不碰 churn/meta-loop 不變量）。 |
| **驗證** | 見 improving_62.md §階段四（ci-gate exit 0、v0.24 not-chaos ≥ floor v0.23:1656、Rule 9 突變實證非空殼、三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（opt-out 保留）：回退即把兩 `_rule_*_telemetry_enabled()` 改回「unset→False」（指回 v0.23 邏輯），或全域設 `SDD_ENABLE_RULE_FIRE_TELEMETRY=0`／`SDD_ENABLE_RULE_CATCH_TELEMETRY=0` 即還原 opt-in 行為，零 breaking change（API/狀態/`*.tla` 皆未動）。既有 FSM-STATE 狀態檔完全相容。 |

## v0.22 → v0.23

| 欄位 | 內容 |
|------|------|
| **版本** | v0.22 → v0.23 |
| **日期** | 2026-06-24（凍結來源：AutoSDD_improving_59.md，B 軌成熟度推進 L4→L5；🔴 人工 signoff：掌舵者 AskUserQuestion 裁定「B→L5 自演化活體化」，明確授權翻轉規則自演化「自動提議」預設） |
| **delta 清單** | **B 軌：規則自演化「自動提議」活體化（B-axis 流程自治 L4→L5）。** improving_16/W-16-1 已把 `propose_slv_from_fpl`（trust_level:proposed 草案）接入 `exit_production_behavioral_signal("learn")` 主迴圈，但**預設 OFF（opt-in 鷹架）**＝自演化機具齊備卻未活體（B 軸卡 L4）。本輪把規則自演化由「失敗即停等人手動跑 CLI」翻轉為「**learn 出口自動 draft proposed 草案為 default**」＝L5 自演化活體常態。**W-59-2（唯一程式改動）`fsm_runtime.py:_slv_auto_propose_enabled()`**：未設環境變數時回 `True`（預設 ON）；**保留顯式 opt-out**（`SDD_ENABLE_SLV_AUTO_PROPOSE=0/false/no/off` → 還原 v0.06 純轉態，供除錯/保守場景）。**紅線零弱化**：①草案恆 trust_level=proposed（R-9.11）；②trust_level→verified 仍 🔴 人工（`exit_learning_commit` 既有強制檢查不動）；③meta-halt（R-9.24 ChurnBounded/GraduationRatchet）守門不動；④fail-closed 不偽造；⑤gate 仍需 `fpl_id`（無 fpl_id → 純轉態，逐字同翻環前）。**W-59-3 測試對齊新預設**：`test_slv_auto_propose_wiring.py` 把 v0.22 Case 1（`delenv`→斷言無 auto_slv）改寫為 `test_default_on_learn_auto_drafts_proposed_def_59`（unset→自動 draft proposed 活體）+ 新增 `test_default_on_redline_proposed_never_auto_promotes_def_59`（預設活體下 proposed 仍不自動升 verified，R-9.11 守界）+ `test_explicit_opt_out_is_sole_switch_even_with_fpl`（顯式 opt-out=0 純轉態，保守模式零退化）；9→10 case。**無 FSM 狀態/規則/拓樸變更**：`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.22 **逐位元零差異**——本輪僅翻轉「誰觸發既有 learn→LEARNING_COMMIT 邊後的非轉態 side-effect」之 runtime 預設，未動形式化模型；ID_REGISTRY 不取新 ACT/rule。缺陷：本輪 B 軌無新框架缺陷（純預設活體化）。 |
| **TLC 證據** | **形式化模型逐位元不變**：`formal/*.tla`/`.cfg` 與 `transition_rules.py`（`_HAPPY_PATH`）對 v0.22 `diff` exit 0 → 依 Rule 9.18.1 **無重跑義務**，既有五軌證明維持有效（side-effect 預設翻轉不改狀態圖；META_FSM 的 ChurnBounded/GraduationRatchet 為靜態不變量，TLC 已窮舉 MAX_CHURN 內全部 churn 值，與 runtime 預設無關）。 |
| **驗證** | 見 improving_59.md §階段四（ci-gate exit 0、v0.23 not-chaos 1656≥floor v0.22:1655、Rule 9 突變 M-W592 實證非空殼、三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（opt-out 保留）：回退即把 `_slv_auto_propose_enabled()` 改回「unset→False」（指回 v0.22 邏輯），或全域設 `SDD_ENABLE_SLV_AUTO_PROPOSE=0` 即還原 opt-in 行為，零 breaking change（API/狀態/`*.tla` 皆未動）。既有 FSM-STATE 狀態檔完全相容。 |

## v0.21 → v0.22

| 欄位 | 內容 |
|------|------|
| **版本** | v0.21 → v0.22 |
| **日期** | 2026-06-24（凍結來源：AutoSDD_improving_57.md，B 軌流程自治常態化；🔴 人工 signoff：掌舵者 AskUserQuestion 裁定「A①+B② 並進（真正推合體 L4）」，明確授權兩個治理/安全政策 flip〔自動 signoff + AUTO_RECOVERY 預設 ON〕） |
| **delta 清單** | **B 軌：AUTO_RECOVERY 常態化（B-axis 流程自治 L3→L4）。** improving_15/W-15-1 已把 `enter_auto_recovery`（ESCALATION→AUTO_RECOVERY_ATTEMPT，Rule 9.14 有界、TLA `T_EnterAutoRecover` 已模型化、五軌 TLC 已證有界停機）接入主迴圈，但**預設 OFF（opt-in）**＝B 軸卡 L3（失敗即停等人為常態）。本輪把流程自治由「opt-in」翻轉為「**有界自動恢復為 default 運作模式**」＝L4。**W-57B-1（唯一程式改動）`fsm_runtime.py:_auto_recovery_enabled()`**：未設環境變數時回 `True`（預設 ON）；**保留顯式 opt-out**（`SDD_ENABLE_AUTO_RECOVERY=0/false/no/off` → 還原 v0.05 停機行為，供除錯/保守場景）。fail-closed 紅線**零弱化**：`enter_auto_recovery` 內 Rule 9.14 全部守界不變（structural 禁、≤3/session、≤1/同因、失敗→ESCALATION_FINAL，例外落回 ESCALATION 等人）。**W-57B-2 測試對齊新預設**：`test_auto_recovery_wiring.py` 把 OFF-zero-regression 測試重構為 `DefaultOnNormalizationTests`（unset→ON 自動進 AUTO_RECOVERY_ATTEMPT + 顯式 opt-out env=0 仍停 ESCALATION〔保留逃生口〕）；6 個驗「純 retry-budget→escalation／tamper 抵抗」之既有測試（與 auto-recovery 正交）顯式 opt-out 隔離該關注點（test_transitions×3、test_e2e_smoke×1、test_chaos×2，各附註記）。**無 FSM 狀態/規則/拓樸變更**：`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.21 **逐位元零差異**（diff exit 0）——本輪僅翻轉「誰觸發既有邊」的 runtime 預設，未動形式化模型；ID_REGISTRY 不取新 ACT/rule。缺陷：本輪 B 軌無新框架缺陷（純預設常態化）。 |
| **TLC 證據** | **形式化模型逐位元不變**：`formal/*.tla`/`.cfg` 與 `transition_rules.py`（`_HAPPY_PATH`）對 v0.21 `diff` exit 0 → 依 Rule 9.18.1 **無重跑義務**，既有五軌證明維持有效。**仍實跑五軌 TLC 確認**（safety-default 翻轉之審慎舉證，`python -m tools.fsm_runtime.tlc_runner --module <各軌>`，v0.22 目錄）：見 improving_57.md §階段四 TLC 表（五軌 0 violation / No error found）。**runtime 有界停機實證**：v0.22 chaos **34 passed/0 failed**——`test_100_rounds_are_all_bounded` 等 bounded_ratio==1.0 核心場景在**預設 ON** 下跑過，證 auto-recovery 常態化仍 R-9.9 有界（非 bounded_ratio 違反）。 |
| **驗證** | 見 improving_57.md §階段四（ci-gate exit 0、v0.22 not-chaos 1655≥floor v0.21:1654、chaos 34 passed、三鏡 zero-trust）。 |
| **回退指引** | 純向後相容（opt-out 保留）：回退即把 `_auto_recovery_enabled()` 改回「unset→False」（指回 v0.21 邏輯），或全域設 `SDD_ENABLE_AUTO_RECOVERY=0` 即還原 opt-in 行為，零 breaking change（API/狀態/`*.tla` 皆未動）。既有 FSM-STATE 狀態檔完全相容。 |

## v0.20 → v0.21

| 欄位 | 內容 |
|------|------|
| **版本** | v0.20 → v0.21 |
| **日期** | 2026-06-24（凍結來源：AutoSDD_improving_55.md，B 軌設計探索 improving_54 signoff 後之實作輪；🔴 人工 signoff：掌舵者 AskUserQuestion 核可「開 improving_55 實作」+ 分類落點「per-rule yaml 欄」+ E 類誠實排除確認） |
| **delta 清單** | **B 軌：其他守門機制覆蓋度量（DEF-19-001 closed@improving_40 點名之後續標的；無 A/C 軌新功能、無 FSM/`_HAPPY_PATH`/`*.tla` 變更）。** improving_54 設計探索拍板：FSM-escalation catch 達結構天花板 7/7=100% 後，其餘 32 條（hook/lint_tlc/meta_loop/manual）守門覆蓋零度量；核心設計＝把不可能的「守門 runtime 是否有效」**誠實重構**為「每條規則宣稱守門機制是否真實分類 + (escalation 類) catch 是否接線」靜態-結構覆蓋（抓 guard bit-rot），對 manual 類**誠實排除於自動分母**。**W-54-1（DEF-54-001 fixed）守門機制分類機讀化**：① `rule_loader.Rule` 新增 additive 欄 `enforcement_mechanism`（取值 escalation\|hook\|lint_tlc\|meta_loop\|manual），`_load_rule_file` 解析、`_write_rule` **非空才寫回**（同 failure_mode 潔淨度模式，置 test_ref 後 failure_mode 前，避免 round-trip 對未分類規則注入空欄）；② 39 條 active R-*.yaml 各補 `enforcement_mechanism` 欄（W-39-1 五分類機讀化：escalation 7/hook 3/lint_tlc 3/meta_loop 14/manual 12）；③ `fsm_runtime.py` 新增 class 常數 `_ENFORCEMENT_MECHANISMS`（enum）+ `_AUTO_MEASURABLE_MECHANISMS`/`_DEFERRED_RUNTIME_MECHANISMS`/`_NON_AUTO_MEASURABLE_MECHANISMS`。**W-54-2 誠實守門覆蓋證書**：`FSMRuntime.comprehensive_governance_coverage()`（純讀、零副作用、零轉態、fail-closed、永不 set_maturity）逐類報 by_mechanism 分區 + escalation_coverage（沿用既有 catch-attribution，wired ⊆ escalation SSOT、100%）+ manual 誠實排除（non_auto_measurable）+ hook/lint_tlc/meta_loop 標 deferred（justified：無消費者+Rule 2+meta_loop 恐觸 TLC）+ denominator_note。**回歸鎖** `tools/fsm_runtime/tests/test_governance_coverage.py` **8 case**（分類完整 fail-closed／五分類分布鎖 7-3-3-14-12／**escalation 類 yaml 與 `_ESCALATION_ATTRIBUTABLE_RULE_IDS` 交叉鎖防兩 SSOT 漂移**／round-trip 保欄防靜默遺失／證書分區加總／escalation 沿用 catch-attribution／manual 誠實排除不灌假／deferred 標記不偽裝），**受控突變實證非空殼**（M1 竄改一條分類→3 測轉紅、M2 破 `_write_rule` round-trip→1 測轉紅，還原後 8 passed）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.20 逐位元零差異；純規則 metadata + 純讀證書，零新增 reachable 邊）；ID_REGISTRY 不取新 ACT/rule。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-54-001（fixed@v0.21）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.20 **逐位元零差異**（未觸碰）。改動限：規則 yaml metadata（enforcement_mechanism 欄）+ `rule_loader.py`（dataclass/load/write 純 additive）+ `fsm_runtime.py`（class 常數 + 純讀方法）+ 測試。零 FSM/狀態宇宙/reachable 邊變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | 見 improving_55.md §階段四（parent 親跑 ci-gate exit 0、≥ floor v0.20:1646、四鏡 zero-trust）。 |
| **回退指引** | 純向後相容（additive-only：規則新增可選欄、新 class 常數、新純讀方法、新測試；舊讀取者忽略 enforcement_mechanism）。回退即移除 enforcement_mechanism 欄與 dataclass/load/write 支援、3 class 常數、`comprehensive_governance_coverage`、`test_governance_coverage.py`；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

## v0.19 → v0.20

| 欄位 | 內容 |
|------|------|
| **版本** | v0.19 → v0.20 |
| **日期** | 2026-06-24（凍結來源：AutoSDD_improving_53.md，B 軌 dogfooding 缺陷回流；🔴 人工 signoff：掌舵者 AskUserQuestion 拍板「B 軌 清 routed RFC（v0.20）」＝清償上輪 routed 框架本體 RFC DEF-CLDREV-030） |
| **delta 清單** | **B 軌 dogfooding 框架本體缺陷清償（improving_53，無 A/C 軌新功能、無 FSM/規則/`*.tla` 變更）。DEF-CLDREV-030 fixed：`tools/fsm_runtime/hub_sync.py` 對外/快取 hub yaml 缺記憶體大小上限（billion-laughs／超大檔 DoS 深防禦缺口，improving_50 第八輪 SA 鏡 F-02 揪出後 routed 框架本體 RFC）**。zero-trust 親讀界定 6 處 `read_text` 中**僅 3 處消費對外不受信內容**：① L334 `_stamp_external_trust_level`（safe_load 剛從 hub 拉下之 rules/*.yaml＝惡意 hub 主攻擊面）、② `diff()` 的 cached 內容讀取、③ `promote()`（可為人工指定之 cached 外部路徑）；其餘 3 處（L141 PR-gated 本地 registry、L219 自寫 cache-meta、L246 **自寫且隨 push 事件累積增長**之 audit log）為受信／自產，刻意不 cap（尤其 audit log 一律 cap 會破壞合法累積）。修復：① 新增模組常數 `MAX_HUB_FILE_BYTES = 1 MiB`（SLV 規則僅 KB，~1000× headroom），可由 registry `sync_policy.pull.max_file_bytes` 覆寫（`__init__` 讀入 `self._max_file_bytes`，非正值 fallback 常數）；② 新增 `HubContentTooLarge(HubConfigError)` 與純函式 helper `_read_text_bounded(path)`（`stat().st_size` 超限即 raise）；③ 套用三對外點——L334 既有 `except Exception` 接住 → fail-soft skip + audit（兌現 pull `failure_mode=non_blocking`），diff/promote raise 給呼叫端（CLI `_cli` 既有 except 接 HubConfigory 子類回 exit 2）；**連帶縱深**：promote 同走 helper → 即使超大檔殘留 cache（L334 跳過 stamp）亦無法被 promote 進信任階梯（fail-closed 閉環）。④ `knowledge/hub-registry.yaml` 補 `sync_policy.pull.max_file_bytes: 1048576` 自我文件化（值＝常數預設，行為等同）。⑤ 回歸鎖 `tools/fsm_runtime/tests/test_hub_sync.py` `TestHubFileSizeCap` **+8 case**（預設常數＝1 MiB／client 預設 cap 取自常數／registry 覆寫／非正值 fallback／`_read_text_bounded` under-cap 回值 over-cap raise／pull 跳過超大外部 yaml 仍 stamp 正常檔〔hermetic：cache/meta/audit 導向 tmp + `force=True`〕／promote 拒絕超大 cached 規則／promote 正常尺寸仍可升），其中 3 anchor 經**受控突變實證非空殼**（暫關 cap → `_read_text_bounded`/pull-skip/promote-reject 三測轉紅、還原後 8 passed＝Rule 9 真能失敗）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.19 **逐位元零差異**；純對外輸入域防護，零新增 reachable 邊）；ID_REGISTRY 不取新 ACT/rule。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-CLDREV-030。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.19 **逐位元零差異**（未觸碰）。修復限 `hub_sync.py` 對外輸入域大小閘 + registry 元資料 + 測試，零 FSM/狀態宇宙/reachable 邊變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **exit 0**：逐軌 **v0.01:1478 / v0.20:1646（v0.19 1638 + 8 新 TestHubFileSizeCap，只增不減，0 failed＝零退化）/ scripts/tests:127**（不變）。arch_fitness fail=0；4 道 SSOT lint 重生後全 ✅（FRAMEWORK_STATUS fresh、skill_header `--check` 對齊 v0.20〔45 檔戳記同步〕、sync_exposed_skills 父層鏡像==LATEST 59 檔、router_hook_coverage 綠）；其餘 shared-infra lint（rfc/gitignore〔v0.20 block 已補〕/agent_template/collaboration_symmetry/scenario_frequency）全 ✅。Copy-on-Evolve 以 `scripts/copy_on_evolve.sh`（git archive 純 tracked）匯出 859 tracked 檔、零 runtime 夾帶。四鏡 zero-trust 複審 OVERALL PASS（詳見 `docs/06_quality/AutoSDD_ZeroTrust_Audit_53.md`）。 |
| **回退指引** | 純向後相容（additive-only：對外輸入加大小閘，正常 KB 級檔零影響；registry 新增可選 key；新增 helper/例外/測試）。回退即移除 `MAX_HUB_FILE_BYTES`/`HubContentTooLarge`/`_read_text_bounded` 與三對外點呼叫、`__init__` 的 `_max_file_bytes`、registry `max_file_bytes` key、`TestHubFileSizeCap`；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

## v0.18 → v0.19

| 欄位 | 內容 |
|------|------|
| **版本** | v0.18 → v0.19 |
| **日期** | 2026-06-22（凍結來源：使用者請求「AISDLC_SDD_v0.xx 中 .claude 的 hooks 與 skills 是否都可完整使用，徹底驗證並做架構調整」，整合層 improving_44 B 軌 dogfooding；🔴 人工 signoff：掌舵者 AskUserQuestion 拍板「立即開 v0.19 全修」DEF-A/C/D-1/DEF-B 四項） |
| **delta 清單** | **SDD `.claude` hooks 完整可用性修復（整合層 DEF-43-008；無 A/C 軌新功能、無 FSM/規則/`*.tla` 變更）。** 經 Architect/SA-SD/QA 三鏡 zero-trust 審查「monorepo 根 session 下各版 .claude hooks/skills 可用性」後，套用 4 項修復：① **DEF-A（P2）drift hook repo-root 對稱化**（`.claude/hooks/post_commit_drift.py`：原裸 `parents[2]` 指向版本目錄〔monorepo 收斂後無 `.git`〕→ `_write_warning` 的 `git_dir.exists()` 恆 False、drift 告警靜默蒸發 + `compute_drift` 掃錯目錄；對稱於姊妹 `closure_evidence.repo_root_from()` 分離 `_PKG_ROOT`〔供 import〕與 `REPO_ROOT`〔git toplevel 定位真實 monorepo 根〕）；② **DEF-C（P3）安裝器動態 LATEST**（`tools/install_hooks/install_post_commit.{sh,ps1}`：原寫死 drift→v0.01／closure→v0.12，致修了 drift bug 也裝不到、且與「指向 LATEST」原則不一致 → 改動態解析 LATEST〔對齊 ci-gate `sort -V \| tail -1`〕，修復立即生效、永不再 stale）；③ **D-1（P3）settings.json 標頭版本中性化**（`.claude/settings.json` description 原滯留「AISDLC-SDD v0.01 — Phase D…」〔全 18 版 Copy-on-Evolve 帶過來的 stale 標頭，DEF-AGTREV-021 同類〕→ 版本中性描述實際 hook 行為）；④ **DEF-B（P3）router 覆蓋機械守護**（新增 shared-infra `scripts/router_hook_coverage_lint.py`＋16 test，斷言最新演化版宣告之 CC hook event ⊆〔root router `_HOOK_MAP` 涵蓋 ∩ 根 settings wire〕，不可達即硬閘擋下；接入 `ci-gate.sh` 杜絕未來新增第 4 種 CC event 卻忘改根 router/settings 致治理 hook 靜默失效）。缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md`（DEF-43-008，fixed@v0.19）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.18 **逐位元零差異**（未觸碰）。改動限：v0.19 git post-commit hook 入口 `post_commit_drift.py`（git 原生 hook，與 CC session 解耦、不經 settings.json deny 層、非 FSM 路徑）+ 兩支安裝器 shell + `.claude/settings.json` description 字串 + `AISDLC_SDD/scripts/` 共用 lint（versioned 目錄外 shared infra，非凍結本體）。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **全綠**：逐軌計數 **v0.01:1478 / v0.19:1611（與 v0.18 同數，零退化）/ scripts/tests:109**（93→109，+16 `test_router_hook_coverage_lint`，實測）。`arch_fitness --strict` fail=0。rfc / gitignore（v0.19 block 已補）/ agent_template / collaboration_symmetry / scenario_frequency / FRAMEWORK_STATUS fresh / skills SSOT / **新 router_hook_coverage** 全 ✅。Copy-on-Evolve `git add -A -n` would-add 零 runtime 夾帶（v0.19 tracked 858 + 修改檔，.gitignore 補 v0.19 block）。drift hook 修復實證：以 git toplevel 解析回真實 monorepo 根、`.git/COMMIT_DRIFT_WARNING` 落點正確。三鏡獨立 zero-trust 複審 OVERALL PASS。 |
| **回退指引** | 純向後相容（git advisory hook 入口修正、安裝器動態化、description 元資料、新增 lint 不改既有閘門語意）。回退即還原 drift hook repo-root、2 支安裝器、settings.json description、移除 router lint＋test 與 ci-gate 接線；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

## v0.17 → v0.18

| 欄位 | 內容 |
|------|------|
| **版本** | v0.17 → v0.18 |
| **日期** | 2026-06-22（凍結來源：使用者臨時請求「agent/* 全面 SDD 與架構符規審查並修復」，B 軌 dogfooding；🔴 人工 signoff：本輪主柱「B 軌·agent 定義檔符規清償」+ 推進幅度「11 項可自主修復」+ SCG 命名方向「角色閘門改別名」） |
| **delta 清單** | **Agent 定義檔符規清償（無 A/C 軌新功能、無 FSM/規則/`*.tla` 變更）。** 由 Architect/SA/SD/QA 四專家 zero-trust 審查 v0.17 全 26 個 agent YAML 後，套用 11 項已驗證修復：① **pm-planner 幻影 id→pm-po**（`agent/specialized/sdd-orchestrator-zh.yaml:183` + `tools/fsm_runtime/subagent_contract.py:54` 雙端對稱改，原 registry 登記 `pm-planner` 但實際 agent id 為 `pm-po`、契約測試僅比對 yaml↔runtime 故 CI 盲區）；② **icon 去碰撞**（🔍 三方撞→ BA 🧭 / code-analyzer 🔬 / qa-tester 維持 🔍）；③ **agent.version 統一 v0.18**（原 v0.01/02/03 混雜，26 檔含模板）；④ **補 dependencies 區塊**（compliance-officer / security-engineer，四子鍵留空不引新 broken path）；⑤ **SCG-4/5/6 角色閘門別名化**（22 處：自創「Test Strategy/Security Spec/Performance Baseline Gate」佔用官方 SCG 槽位 → RG-TEST/RG-SEC/RG-PERF；`spec_gate` 純描述性 metadata、tools/ 零解析故零退化）；⑥ **RTM 觸發 Stage→SCG**（對齊 `SDD_Core_Principles`：FRD後=SCG-0、SRD後=SCG-1、測試計畫後=SCG-4；修 04.sa-analyst rtm_at_column 自相矛盾）；⑦ **qa-automation `# planned` 路徑值汙染修正**（移出引號成合法註解）；⑧ **generate_adr template 路徑修正**（docs/→docs_template/sdd/adr，output 產出路徑不動）；⑨ **WRONGDIR template 3 處重指**（SRD_Module→core/srd、Performance/Security_Test_Plan→core/tests，目標皆 test -f 確認存在）；⑩ **sd-web/sd-mobile-architect 孤兒載入**（INIT greenfield supporting_agents 加 extends 條件式載入）；⑪ **README/INIT 計數更正 + 雙 schema 宣告**（14→19 specialized 含 5 系統級 runtime agent、補列 sdd-* runtime、runtime schema 說明、合規稱呼統一）。**⑫ broken template_path 全面重新接線（掌舵者 signoff 方案一+配套）**：67 條 rewire 至 docs_template/ 既有最接近模板 + 26 條 Category D 正規化（移除 `../` 統一根相對）+ 9 條確無對應模板刪除（方案三 fallback）；功能性 broken 由 ~75 降 **0**。配套新增 `scripts/agent_template_lint.py` 硬閘並接入 `ci-gate.sh` 杜絕再生。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md`（DEF-AGTREV-001~004，001/002 fixed@v0.18）、裁決書 `docs/06_quality/Agent_BrokenTemplate_Disposition.md`（fixed@v0.18）。 |
| **TLC 證據** | **N/A** — 純改 agent YAML 描述性內容 + 1 行 runtime registry 常數（pm-po）+ README/INIT 文檔 + template 路徑字串。`transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.17 **逐位元零差異**（未觸碰）。`spec_gate`/RTM Stage 等被改字串皆為 agent metadata，無任一被 FSM/工具解析（grep tools/ 零引用）。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **全綠**：逐軌計數 **v0.01:1478 / v0.18:1611 / scripts/tests:56**（與 v0.17 同數，零退化；agent YAML 非 pytest 標的，唯一改動之 runtime 程式 `subagent_contract.py` 由 `test_subagent_contract` 19 passed 守住〔DEF-AGTREV-007 校正：原誤記 24，e796c1f 起逐位元實為 19〕）。`arch_fitness --strict` fail=0（exit<2 advisory 放行，與 v0.17 baseline 同 3 warn）。RFC lint / gitignore lint / **新 agent_template_lint** 全 ✅。26/26 agent YAML `safe_load` 通過、功能性 template broken=0。Copy-on-Evolve `git add -A -n` would-add **858 零 runtime 夾帶**（與 v0.17 tracked 858 平價；.gitignore 補 v0.18 block）。獨立 zero-trust 複審（QA+Architect+SD 合一、親跑全測）OVERALL PASS。詳見 `docs/06_quality/Agent_Conformance_Audit_v0.18.md`。 |
| **回退指引** | 純向後相容（agent 定義檔層改動，無行為語意變更：spec_gate/RTM 編號為描述性、pm-po 為 registry 名稱對齊修正、icon/version/README 為元資料）。回退即還原 11 項各檔；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

### v0.18 同日收尾補強（2026-06-22，使用者再次請求 agent/* 全面符規審查並修復；🔴 人工 signoff：就地修 v0.18）

| 欄位 | 內容 |
|------|------|
| **性質** | 獨立 zero-trust 重審（Architect/SA-SD/QA 三鏡，不採信前次審計文件）揪出 v0.18 首版**未竟殘留**並就地清償。純 agent 定義檔/共用 lint/文檔，**無 FSM/規則/`*.tla` 變更**。 |
| **delta 清單** | **DEF-AGTREV-005~007（詳見 `docs/06_quality/AutoSDD_Defect_Log.md` 重審輪追記）。** ① **broken template 殘留 34 條（lint 盲區假綠）**：前次「broken=0」僅在 `agent_template_lint` TOK regex（只認 `docs_template/` 前綴）窄範圍成立。實揪 12 條 core `document_responsibilities` 裸名 `template_path`（磁碟全不存在）+ 19 條 `sdd_skills.*.template` 誤指 `docs/`(輸出區) 下不存在 `*-TEMPLATE.md` + 3 條 `02.ba dependencies.templates` 裸名 → 12 方案一 rewire 至最近既有模板（1 條方案三刪欄）、19 全域重指 `docs_template/sdd/*`、3 條重指；**lint 盲區封閉**：加 `BARE` regex + YAML `dependencies.templates` 雙檢查。② **4 specialized persona agent 缺 `collaboration_rules`**（`qa-mobile/qa-web-tester`、`sd-mobile/sd-web-architect`；symmetry lint 只掃 core 故漏）→ 各補合角色 rules（peer 對互相對稱），`collaboration_symmetry_lint` 加 `find_missing_collaboration_rules` presence 檢查防復發。③ **誠實性校正**：`test_subagent_contract` 三處「24 passed」實為 **19**（`git show e796c1f` 逐位元證從未 24）→ 校正；README 同檔計數矛盾（樹狀圖/Q&A 14 vs 本文 19）→ 補列 5 sdd-* runtime、Q&A 校正。④ **新測試**：`test_agent_template_lint.py` 9 case（前次出貨此 lint 即零測試）+ `test_collaboration_symmetry_lint.py` +3 case。⑤ **DEF-AGTREV-008（QA 閉環複審補抓）**：④ 的 BARE regex 原只匹配 `.md`，漏 2 條 `.yaml` template（`05.sd:256`/`integration-specialist:536` 誤指 `docs/02_architecture/api/CONTRACT-TEMPLATE.yaml` → 重指 `docs_template/sdd/api/CONTRACT-TEMPLATE.yaml`）；BARE 改 `_TMPL_EXT`（md/yaml/yml/json）+2 測試，窮盡掃描 0 殘留（真實總 broken=36）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對 v0.18 首版**逐位元零差異**（未觸碰）。改動限 agent YAML 描述性內容 + `AISDLC_SDD/scripts/` 共用 lint（versioned 目錄外 shared infra，非凍結本體）+ README/EVOLUTION/Defect_Log 文檔。依 Rule 9.18.1 無重跑義務。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **exit 0**：逐軌 **v0.01:1478 / v0.18:1611（零退化，agent YAML 非 pytest 標的）/ scripts/tests:81**（69→81，+9 template lint〔含 .yaml〕 + 3 symmetry presence 測試）。`agent_template_lint` / `collaboration_symmetry_lint`（含新 presence 檢查）/ rfc / gitignore / FRAMEWORK_STATUS fresh 全 ✅；arch_fitness fail=0；FF-13 26/26 agent YAML 合法；26/26 safe_load OK、**功能性 template broken=0**。獨立 QA zero-trust 複審 OVERALL PASS。 |
| **回退指引** | 純向後相容（agent 定義檔 template_path 重指既有有效模板、additive 補 collaboration_rules、文檔校正、lint 加嚴）。回退即還原各 agent template_path/collaboration_rules、2 支 lint 增量與 2 個測試檔；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

### v0.18 三度+四度收尾補強（2026-06-22，使用者第三/第四次請求 agent/* 全面符規審查並修復；🔴 人工 signoff：就地修 v0.18）

| 欄位 | 內容 |
|------|------|
| **性質** | 連續兩輪獨立 zero-trust 重審（四鏡，不採信前輪文件）揪出語意/架構符規殘留並就地清償。純 agent 定義檔/共用 lint/文檔，**無 FSM/規則/`*.tla` 變更**。 |
| **delta（三度，DEF-AGTREV-009~013）** | ① **spec_gate 號碼/名稱張冠李戴**（7 處「SCG-1 Requirement」→ 分情境修：SRD 產物 SCG-1 Design、FRD/PRD/RTM 產物 SCG-0 Requirement）；② **scenario_usage 分母 /9→/10**（15 agent + GUIDE 14 處）；③ **sa-analyst 漏 Migration**（9/9→10/10）；④ **architect SRD 模板重複行去重**；⑤ **core/README SCG-5 owner 誤標 qa-tester→qa-lead**。另 5 類經親驗判 by-design（extends 繼承缺區塊、21 persona 歷史數、.md 標頭新鮮度、compiler schema 近似、workflow_name 邏輯標籤）誠實記分歧。 |
| **delta（四度，DEF-AGTREV-014~016，掌舵者 SSOT/scope 裁決）** | ① **collaboration upstream 反向斷鏈**（`ba` upstream←PM/PO 但 pm-po 不提 BA）→ pm-po 補 `downstream→BA` + `collaboration_symmetry_lint` 加 upstream 反向檢查（接受 down/peer 為對側）+2 test；SD-1 視角差判 by-design。② **scenario_usage frequency 與統計段 SSOT 系統性漂移**（integration 1/10 vs 自列 4 內部矛盾 + 6 agent off-by-one + pm-po 計入非法場景 Sprint Planning + sd-architect 漏 Migration）→ 掌舵者裁定「統計段為唯一 SSOT」，**8 agent 全對齊**（freq 分子＝統計段＝清單項數三者一致，逐場景表交叉核對成員）+ 新增 `scripts/scenario_frequency_lint.py`（SSOT 跨源 + 內部一致雙檢查）+6 test + 接入 ci-gate + mapping doc Refactoring 補 qa-automation 收斂雙視圖。③ **文件交叉引用/標頭殘留**（README:244 行號、core/README:68 複數 agents 斷鏈、core/README 標頭 v0.01、PHASE2_GUIDE:402 離群 /9）四處校正。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` 對前狀態**逐位元零差異**（未觸碰）。改動限 agent YAML 描述性內容 + `AISDLC_SDD/scripts/` 共用 lint（versioned 目錄外 shared infra）+ 同版 `scenarios/SCENARIO_AGENT_MAPPING.md` 描述性對應表 + 文檔。依 Rule 9.18.1 無重跑義務。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **exit 0**：v0.01:1478 / v0.18:1611（零退化，agent YAML/.md 非 pytest 標的）/ scripts/tests **56→81→89**（四度 +2 collaboration upstream 反向 case + 6 frequency case）；6 lint 全 ✅（含新 `scenario_frequency_lint`）；arch_fitness fail=0（僅 FF-16 GC advisory）；FRAMEWORK_STATUS fresh；FF-13 26 agent 全合法；26/26 safe_load OK；8 agent frequency 三者一致（QA 複審鏡親數吻合）；兩新/擴 lint 經突變實證非空殼。獨立 QA zero-trust 複審 OVERALL PASS。**臨時審查塊 DEF-AGTREV-001~016 全閉、零 routed 殘留。** |
| **回退指引** | 純向後相容（agent 定義檔描述性內容對齊、additive 補協作邊、新增 1 支 lint + 擴 1 支 lint + 2 測試檔、文檔校正）。回退即還原各 agent scenario_usage/collaboration_rules、`scenario_frequency_lint.py` 與 `collaboration_symmetry_lint` upstream 反向增量、2 測試檔、mapping doc Refactoring 行、文檔；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

### v0.18 五度收尾補強（2026-06-22，使用者第五次請求 agent/* 全面符規審查並修復；🔴 人工 signoff：就地修 v0.18）

| 欄位 | 內容 |
|------|------|
| **性質** | 第五度獨立 zero-trust 重審（Architect/SA/SD/QA 四鏡，不採信前四輪「全閉零殘留」宣稱）+ **parent 對鏡子發現再施 zero-trust**（駁回 4 類鏡子幻覺）。純文檔/共用 lint，**無 FSM/規則/`*.tla` 變更**。 |
| **delta（DEF-AGTREV-017~020）** | ① **README 摘要表 frequency 第三來源盲區**：`scenario_frequency_lint` 只查 yaml↔統計段↔清單，不查 `agent/README.md` 表 → DEF-015 對齊 yaml 後 README 表滯留舊值（ba 4≠3、dev 7≠4、qa 7≠8）→ 對齊 3 分子 + pm-po 標籤；lint 加 `check_readme_table`（以 yaml 為基準比對 README 表列）+2 test（突變實證）。② **SCG-5 owner 同族漏網**：`SDD_GUIDE.md:38` `qa-tester`→`qa-lead`（DEF-013 當輪只修 core/README，漏 SDD_GUIDE 同表；兩權威源皆 qa-lead）。③ **v0.18 導覽連結滯留 v0.01 絕對路徑**：`core/README:68-69`、`specialized/README:103` → 相對路徑（同段兄弟連結已相對；`AGENT_PHASE2_UPDATE_GUIDE:578` 屬歷史敘述段不動）。④ **角色 sub-gate 缺權威碼定義**：RG-TEST/SEC/PERF（v0.18 line 12 ⑤ 創的別名）agent 引用 21 處但 `SDD_GUIDE` 補充閘門表僅以名稱定義 → 補「代碼」欄（RG-TEST/SEC/PERF/DOC/PIPE 全 5 碼）+ 註腳定義，使引用可追溯。 |
| **parent 駁回鏡子幻覺（誠實記錄）** | (1) QA 鏡「FRAMEWORK_STATUS / 2 lint 不存在」＝假陰性（在 v0.18/ 內找，實在父層 `AISDLC_SDD/` 與 `scripts/`，本輪親跑全綠）；(2) SD 鏡「BA 協作非對稱」＝假發現（BA 實 up←PM/PO·down→SA+SD·peer~QA 對稱，symmetry lint 通過為機械證）；(3) Architect/SD 鏡「5 sdd-* runtime 缺 collaboration_rules/scenario_usage」＝by-design（README:107 明文 + 兩 lint 明確豁免無 persona/無 scenario_usage 者）；(4) QA 鏡「playbook-compiler 分類歧義」＝by-design（SSOT 計 5 runtime，INIT 4+1 細分仍 net 5/19）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg` **逐位元零差異**（未觸碰）。改動限 `agent/README.md`、`guides/system/sdd/SDD_GUIDE.md`、`agent/core/README.md`、`agent/specialized/README.md` 描述性內容 + `AISDLC_SDD/scripts/` 共用 lint（versioned 目錄外 shared infra）+ 文檔。依 Rule 9.18.1 無重跑義務。 |
| **驗證** | 完整 `bash scripts/ci-gate.sh` **exit 0**：v0.01:1478 / v0.18:1611（零退化，agent .md/.yaml 非 pytest 標的）/ scripts/tests **89→91**（+2 README 表 case）；6 lint 全 ✅；arch_fitness fail=0（僅 FF-16 GAP-X1/X2 既存 advisory）；FRAMEWORK_STATUS fresh；FF-13 26 agent 全合法；26/26 safe_load OK；擴充 `check_readme_table` 經突變實證非空殼（README 7 vs yaml 4 轉紅）。**臨時審查塊 DEF-AGTREV-001~020 全閉、零 routed 殘留；4 類鏡子幻覺 parent 親驗誠實駁回。** |
| **回退指引** | 純向後相容（README 表分子對齊、SDD_GUIDE owner/碼校正、導覽相對路徑、lint 加 README 第三來源檢查 +2 測試）。回退即還原 4 文檔與 `scenario_frequency_lint.check_readme_table`/2 測試；零觸碰 FSM/`*.tla`/既有測試拓樸。 |

## v0.16 → v0.17

| 欄位 | 內容 |
|------|------|
| **版本** | v0.16 → v0.17 |
| **日期** | 2026-06-18（凍結來源：AutoSDD_improving_39.md，B 軌 dogfooding；🔴 人工 signoff：本輪主柱「B 軌·分母正當性調查」+ 推進幅度「2–3 W 項」） |
| **delta 清單** | **B 軌 dogfooding 度量誠實化（improving_39，無 A/C 軌新功能）。DEF-39-001 fixed：catch_attribution_coverage 分母正當性透明化**。階段一機械證實 fsm_runtime.py 9 個生產 escalation 落點＝7 已接線 + 2 正交無規則，DEF-19-001 沿「1:1 落點接線」乾淨候選枯竭、7/39 達結構天花板。W-39-1 分類調查確立：39 條非 deprecated 規則中僅 **7 條** catch-可歸因（具唯一生產 escalation 落點：R-9.1/9.2/9.3/9.7/9.21/9.22/R-SELF-STRIDE，與 `rules_with_failure_mode` 精確吻合），其餘 32 條由 hook(3)/lint·TLC(3)/meta-loop guard(14)/人工·advisory·憲法(12) 守門＝**本質非 FSM-escalation catch-可歸因**（catch_count 恆 0 屬設計使然非缺口）。**W-39-2（純 additive）**：① `tools/fsm_runtime/fsm_runtime.py` 加 class 常數 `_ESCALATION_ATTRIBUTABLE_RULE_IDS`（7 條 SSOT，drift-proof 註解、以 `_record_escalation_catches([...])` 接線為準）；② `rule_fire_telemetry_stats()` 於 `catch_attribution_coverage` 加 `escalation_attributable_rule_ids`/`escalation_attributable_total`(=7)/`escalation_scoped_coverage_pct`(=100.0)/`non_escalation_governed_total`(=32)/`denominator_note`。**既有三欄位（rules_with_failure_mode=7/rules_total=39/attributed_rule_ids）逐字不變＝零退化**。③ 回歸鎖 `tools/fsm_runtime/tests/test_w39_coverage_denominator.py` **6 case**（註冊表釘 7 / 真實規則 escalation-scoped=100% / breakdown 誠實 32=39−7 / 舊欄位零退化 / numerator⊆正當分母 / **靜態掃描 `_record_escalation_catches([...])`==註冊表防漂移**）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.16 **逐位元零差異**；純度量無新增 reachable 邊）。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-39-001 / DEF-19-001（天花板達成、正式收尾留 🔴 人工）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.16 **逐位元零差異**（Architect 鏡 `diff -rq` 證）。W-39 純度量計算（讀 rule_loader、不寫 FSM-STATE、不 set_maturity、零新增狀態/轉換）。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.17 全套 `pytest -m "not chaos"` = **1611 passed / 4 skipped / 0 failed**（v0.16 1605 + 6 新 test_w39_coverage_denominator，只增不減）。Copy-on-Evolve 以 `scripts/copy_on_evolve.sh`（git archive 純 tracked）匯出 857 tracked 檔（零 runtime cruft、`git add -A -n` would-add 858 含本文件、零殘留 build/reports）。三鏡 zero-trust 全 OVERALL PASS、AutoClaude 3221/122/0 零觸碰、ci-gate exit 0「v0.01:1478 v0.17:1611 scripts:44」詳見 `docs/06_quality/AutoSDD_ZeroTrust_Audit_39.md`。 |
| **回退指引** | 純向後相容（additive-only：移除 5 個新增欄位 + class 常數 + `test_w39_coverage_denominator.py` 即還原 v0.16 行為；既有三欄位與 catch flag 語意不變）。零觸碰 FSM/`*.tla`/既有測試。 |

## v0.15 → v0.16

| 欄位 | 內容 |
|------|------|
| **版本** | v0.15 → v0.16 |
| **日期** | 2026-06-18（凍結來源：AutoSDD_improving_38.md，B 軌 dogfooding；🔴 人工 signoff：本輪主柱「B 軌續 DEF-19-001」+ 推進幅度「2–3 W 項」） |
| **delta 清單** | **B 軌 dogfooding 缺陷漸進清償（improving_38，無 A/C 軌新功能）。DEF-19-001 catch 歸因覆蓋 5/39 → 7/39（+2）**：延續 improving_19/20/37 catch 三要件契約（escalation 真實發生 ∧ 規則自描述 failure_mode ∧ 呼叫端結構化歸因 rule_id），對兩條**有唯一生產 escalation 落點、僅缺 failure_mode** 的凍結規則接線（嚴守 DEF-18-001 寧缺勿濫，非為衝覆蓋率臆測歸因）。**W-38-1（R-SELF-STRIDE，5/39→6/39）**：① `governance/rules/R-SELF-STRIDE.yaml` 補 `failure_mode`（SANDBOX_HARDENING_GATE policy_violation → ESCALATION structural，唯一生產落點；明文「verdict=pass 路徑不 escalate 不歸因、與既有 5 條零交集」）；② `tools/fsm_runtime/fsm_runtime.py` `exit_sandbox_hardening_gate` 的 `policy_violation` 分支於既有 `record_escalation` 後接 `self._record_escalation_catches(["R-SELF-STRIDE"])`。**W-38-2（R-9.3，6/39→7/39）**：③ `governance/rules/R-9.3-logical-consistency-guard.yaml` 補 `failure_mode`（record_spec_audit 的 SPEC_AUDIT 耗盡 → ESCALATION；**明文排除** (a) check_implementation_budget 的 implementation-budget-exceeded 直接 escalate〔正交、無規則承載〕、(b) R-9.1 gate-retry 落點，杜絕雙重歸因）；④ `record_spec_audit` 的 `spec_audit_count ≥ SPEC_AUDIT_MAX_PER_STAGE` escalate 分支於既有 `record_escalation` 後接 `self._record_escalation_catches(["R-9.3"])`。⑤ 回歸鎖 `tools/fsm_runtime/tests/test_w38_catch_wiring.py` **8 case**（R-SELF-STRIDE：flag ON catch+1 / flag OFF 零退化 / **非重疊守門：verdict=pass 不歸因** ；R-9.3：flag ON catch+1 / flag OFF 零退化 / **非重疊守門：implementation-budget-exceeded 落點不歸因 R-9.3**；真實 R-SELF-STRIDE、R-9.3 各具非空 failure_mode）。覆蓋率 runtime 實測 `catch_attribution_coverage` = **7/39**（5→7）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.15 **逐位元零差異**；catch 純記帳零新增 reachable 邊）；ID_REGISTRY 不取新 ACT/rule（沿用既有 R-9.3、R-SELF-STRIDE）。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-19-001。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.15 **逐位元零差異**。catch 歸因為純記帳（只增 catch_count、不寫 FSM-STATE、不 set_maturity、零新增狀態/轉換）；flag `SDD_ENABLE_RULE_CATCH_TELEMETRY` 預設 OFF＝行為逐字同 v0.15。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.16 全套 `pytest -m "not chaos"` = **1605 passed / 4 skipped / 0 failed**（v0.15 1597 + 8 新 test_w38_catch_wiring，只增不減）。Copy-on-Evolve 以 `scripts/copy_on_evolve.sh` 匯出 856 tracked 檔（零 runtime cruft、build/reports 未帶入、FSM 種子模板於 tools/fsm_runtime/templates/ 正常保留；`git add -A -n` would-add 856 零殘留）。AutoClaude / ci-gate / 潔淨度詳見 `docs/06_quality/AutoSDD_ZeroTrust_Audit_38.md`。 |
| **回退指引** | 純向後相容（catch flag 預設 OFF＝v0.15 逐位元行為；env 不設 `SDD_ENABLE_RULE_CATCH_TELEMETRY` 永不記 catch）。回退即移除 `R-SELF-STRIDE`/`R-9.3` 的 `failure_mode` 欄位、`fsm_runtime.py` 兩處 `_record_escalation_catches(["R-SELF-STRIDE"]/["R-9.3"])`、`test_w38_catch_wiring.py`。零觸碰 FSM/`*.tla`/既有測試。 |

## v0.14 → v0.15

| 欄位 | 內容 |
|------|------|
| **版本** | v0.14 → v0.15 |
| **日期** | 2026-06-18（凍結來源：AutoSDD_improving_37.md，C/B 軌 dogfooding；🔴 人工 signoff：本輪主柱「B 軌」+ 推進幅度「DEF-19-001 catch 覆蓋 +2（R-9.7+R-9.9），R-9.9 驗出歧義則降 +1」） |
| **delta 清單** | **B 軌 dogfooding 缺陷漸進清償（improving_37，無 A/C 軌新功能）。W-37-1（DEF-19-001 catch 歸因覆蓋 4/39 → 5/39）**：延續 improving_19/20 catch 三要件契約（escalation 真實發生 ∧ 規則自描述 failure_mode ∧ 呼叫端結構化歸因 rule_id），新增 **R-9.7（Phase E M1 精準停機·9.7.2 HUMAN_PENDING 逾時 ≥168h 自動 ESCALATION）** 一條無歧義路徑。① `governance/rules/R-9.7-precise-halt-m1.yaml`：補 `failure_mode` 欄位，**明文僅涵蓋 9.7.2**（HUMAN_PENDING 逾時），排除 9.7.3（AUTO_COMPACT per-stage 超限，其 escalate 落點歸 R-9.2 `trigger_auto_compact`），杜絕雙重歸因（DEF-18-001 寧缺勿濫）。② `tools/fsm_runtime/fsm_runtime.py`：新增 thin 方法 `escalate_human_pending_timeout(reason=...)`（record_escalation + flag-gated `_record_escalation_catches(["R-9.7"])` 同落點，與 R-9.1/R-9.2/R-9.21/R-9.22 四條既有接線同範式）。③ `.claude/hooks/session_start.py`：ACT-023 HUMAN_PENDING 逾時 ESCALATION 分支由 inline `rt.state.record_escalation(reason)` 改委派 `rt.escalate_human_pending_timeout(reason=reason)`（控制流收斂、catch 與 escalation 同落點才不漏記）。④ 回歸鎖 `tools/fsm_runtime/tests/test_w37_catch_wiring.py` **4 case**（flag ON catch+1 / flag OFF 零退化 / **非重疊守門：9.7.3 auto_compact 路徑只 R-9.2 catch+1、R-9.7 恆 0** / 真實 R-9.7 具非空 failure_mode）。**R-9.9 降級不接**：經親驗其無唯一生產 escalation 落點（state_loader 損毀為 `raise ValueError/FileNotFoundError` 非 record_escalation；chaos_runner 的 20+ record_escalation 屬測試載具模擬其他規則失敗模式，歸 R-9.9 即類別錯誤雙重歸因），依 DEF-18-001 寧缺勿濫不接（掌舵者預授權 fallback）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.14 **逐位元零差異**；catch 純記帳零新增 reachable 邊）；ID_REGISTRY 不取新 ACT/rule（沿用既有 R-9.7）。缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-19-001。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.14 **逐位元零差異**。catch 歸因為純記帳（只增 catch_count、不寫 FSM-STATE、不 set_maturity、零新增狀態/轉換）；flag `SDD_ENABLE_RULE_CATCH_TELEMETRY` 預設 OFF＝行為逐字同 v0.14。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.15 全套 `pytest -m "not chaos"` = **1597 passed / 4 skipped / 0 failed**（v0.14 1593 + 4 新 test_w37_catch_wiring，只增不減；coverage 4/39→5/39 無既有測試硬編而破）。Copy-on-Evolve 以 `git archive HEAD:AISDLC_SDD/AISDLC_SDD_v0.14` 匯出 855 tracked 檔（零 runtime cruft、build/reports 未帶入、FSM 種子模板於 tools/fsm_runtime/templates/ 正常保留）。AutoClaude / ci-gate / 潔淨度詳見 `docs/06_quality/AutoSDD_ZeroTrust_Audit_37.md`。 |
| **回退指引** | 純向後相容（catch flag 預設 OFF＝v0.14 逐位元行為；env 不設 `SDD_ENABLE_RULE_CATCH_TELEMETRY` 永不記 catch）。回退 W-37-1 即移除 `R-9.7` 的 `failure_mode` 欄位、`FSMRuntime.escalate_human_pending_timeout`（hook 改回 inline `rt.state.record_escalation`）、`test_w37_catch_wiring.py`。零觸碰 FSM/`*.tla`/既有測試。 |

## v0.13 → v0.14

| 欄位 | 內容 |
|------|------|
| **版本** | v0.13 → v0.14 |
| **日期** | 2026-06-17（凍結來源：AutoSDD_improving_23.md，B 軌 XAI 深化；🔴 人工 signoff：本輪主柱「B 軌」+ 增量「Folding 降維補完」） |
| **delta 清單** | **B 軌 dogfooding XAI 深化（improving_23，無 A/C 軌新功能）。W-23-1（Folding 降維補完，隸屬既有 R-9.37 PY-3，不取新 ACT/Rule）**：補完 `_26.md §A.4.2` + DoD §A.9 列入、§D 收官以 pagination 替代而**靜默未交付的 Folding 子項**。① `tools/fsm_runtime/recursion_topology_view.py`：新增 fold env 旋鈕 `SDD_VIZ_FOLD`(預設 OFF)/`SDD_VIZ_FOLD_MIN`(預設 3, clamp[2,64])；`RenderBudget` additive 加 `fold_enabled`/`fold_min`；`TopoNode` additive 加 `folded`/`folds`；新增純函式 `fold_topology(view)`——把窗內極大「內部簡單鏈」（連續 in==1∧out==1∧非 kept-node）塌縮為單一 `[+k more]` 折疊超節點（錨定鏈首、商圖重接邊），永不折疊 entry/critical/分支/匯聚/sink/未訪問節點；`extract_topology` 末尾 flag-gated 套用。② `render_mermaid` 新增折疊超節點專屬 `classDef fold` 渲染；`render_json` additive 輸出 `folded`/`folds`。③ **PY-2 fold-aware 誠實收縮稽核**：`verify_topology_consistency` 擴充——以 expanded（含折疊展開）對齊窗格錨定（f4 真實大小誠實 nothing dropped）、折疊合法性 f1（窗內成員 in==1∧out==1 + 連續真實呼叫邊）/f2（鏈 rank 嚴格遞減）/f3（不藏 entry）、商圖邊比對（折疊內部邊吸收、跨群邊保留，杜絕杜撰/漏畫）；**fold OFF 時 group=identity、expanded=shown，五道恆過 ⇒ 與 v0.13 逐位元行為相容**。④ `chaos_runner.py`：新增 `VISUALIZATION_FOLD_DRIFT_FLAP` + `_visualization_fold_drift_flap_is_bounded`（偽造折疊吞 sink / 丟成員必被攔、忠實折疊放行，納入 100 輪 bounded）。⑤ 回歸鎖 `tools/fsm_runtime/tests/test_phase_y.py` **+13 case**（fold OFF 零退化 / 鏈塌縮 / 不折要角 / 誠實 verify+guard / 4 道折疊偽造 fail-closed / env clamp / 10⁶ 有界 / fold_topology 零 while 零遞迴 / chaos fold-drift 註冊+bounded）。**DEF-23-002 fixed**：`build/planning/active/SDD_improving_Automation_26.md`(Phase Y §D 已收官)、`_27.md`(closure §6 標 archive) 兩份已完成 RFC `git mv` 入 `build/planning/archive/`（修生命週期漂移；v0.13 凍結不回改）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla`/`.cfg` 對 v0.13 **逐位元零差異**，`diff -rq formal/` 實測僅 v0.13 殘留 TLC runtime log 差異、源碼 ZERO-DIFF）；ID_REGISTRY 不取新 ACT/rule（沿用 _27/DEF-20-001 前例 + Rule 2）。RFC/缺陷：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-23-001/002。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`/`.cfg`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.13 **逐位元零差異**。Folding 為 `recursion_topology_view` 內 read-only 純投影轉換（不寫 FSM-STATE、不 churn）；`VisualizationBounded == churn<=MAX_CHURN` 不受影響（read-only ⇒ churn 永不變動之理由更強）。零新增 reachable 邊、零狀態宇宙變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。chaos 100 輪含新 `VISUALIZATION_FOLD_DRIFT_FLAP` 實測 **bounded_ratio==1.0**（34 passed）。 |
| **驗證** | v0.14 全套 `pytest -m "not chaos"` = **1593 passed / 4 skipped / 0 failed**（v0.13 1580 + 13 新 fold/chaos 測試，只增不減；fold OFF 既有 37 Phase Y 測試逐項續綠）。chaos **34 passed**（100 輪 bounded_ratio==1.0，含新 FOLD_DRIFT flap）。AutoClaude 全套 **3112 passed / 122 skipped / 0 failed**（B 軌未動 AutoClaude，持平基線零退化）；lint-imports **8 kept / 0 broken**。Copy-on-Evolve 以 `git archive HEAD:...v0.13` 匯出 tracked 子樹（精確 853 檔、零 runtime cruft），根除 DEF-11-002 stale 產物入庫風險。 |
| **回退指引** | 純向後相容（fold 預設 OFF＝v0.13 逐位元行為；env 不設 `SDD_VIZ_FOLD` 永不啟用）。回退 W-23-1 即移除 `fold_topology`/`RenderBudget.fold_enabled`/`TopoNode.folded,folds` 與 `verify_topology_consistency` 的 fold-aware 區段、`chaos_runner` 的 FOLD_DRIFT flap。零觸碰 FSM/規則/`*.tla`/既有測試。 |

## v0.12 → v0.13

| 欄位 | 內容 |
|------|------|
| **版本** | v0.12 → v0.13 |
| **日期** | 2026-06-17（凍結來源：AutoSDD_improving_22.md，缺陷清償輪；🔴 人工 signoff：W 項範圍「DEF-12-002 + DEF-15-001 深層重構」） |
| **delta 清單** | **B 軌 dogfooding 缺陷清償（improving_22，無 A/C 軌新功能）。W-22-2（DEF-15-001 深層重構，本版觸發者）**：FSM 種子模板 `FSM-STATE-TEMPLATE.yaml`（`state_loader._load_template()` 必需的**真輸入**）由 runtime 輸出目錄 `build/reports/fsm/` 移至 tracked 源碼位 `tools/fsm_runtime/templates/`（與 loader 同層）。① `tools/fsm_runtime/state_loader.py`：`TEMPLATE_PATH` 由 `REPO_ROOT/build/reports/fsm/...` 改 `Path(__file__).resolve().parent/"templates"/...`，docstring 同步；`DEFAULT_STATE_DIR`（runtime 狀態檔輸出位 = build/reports/fsm/）**不變**（輸入/輸出分離）。② 物理移檔 + 同步 3 處文件連結（`workflow/sdd-fsm-engine/SDD_FSM_ENGINE.md`、`AISDLC_SDD_UPGRADE_SOP.md`、`.claude/skills/test-failure-analyzer/SKILL.md`）。③ `AISDLC_SDD/.gitignore` 新增 v0.13 區塊：因模板已移出 build/reports，**整樹排除無需 negate idiom**（v0.05~v0.12 因模板寄居才須逐層 re-include）—— 消除 DEF-11-001/15-001 反覆打補丁的結構異味根因；`copy_on_evolve.sh` 補回模板特例對 v0.13+ 自然成 no-op（仍保留供 ≤v0.12 舊佈局相容）。④ 回歸鎖 `tools/fsm_runtime/tests/test_template_location.py` **3 case**（TEMPLATE_PATH 在 templates/ 非 build/reports / 模板 is_file 且 `_load_template` 載入非空 dict / DEFAULT_STATE_DIR 仍 build/reports/fsm）。**W-22-1（DEF-12-002 + DEF-22-001，shared infra `AISDLC_SDD/scripts/`，免 Copy-on-Evolve）**：⑤ `cross_version_guard._is_path_arg` 在 `os.path.exists` 前剝 `token.split("::",1)[0]`（修 pytest nodeid `path::test` 含 `::` 被誤判非路徑 → 誤展全版 false fire，DEF-12-002）。⑥ **dogfooding 當場揭露 DEF-22-001**（P2 真 bug）：`VERSION_RE=AISDLC_SDD_v0\.0\d+` 僅匹配 v0.00–v0.09，對現役 v0.10~v0.13 失效（DEF-19-002 同根十位數跨越，當時漏修此處）→ 放寬為 `v0\.\d+` 通則化。`scripts/tests/test_cross_version_guard.py` +2 case（nodeid 不誤觸 / 版本化 nodeid 仍正確偵測），scripts/tests 25→27。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla` 對 v0.12 **逐位元零差異**，diff 實測 ZERO-DIFF；模板移位純屬檔案位置 + 路徑常數）；ID_REGISTRY 不取新 ACT/rule。RFC：見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF 條目。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.12 **逐位元零差異**（`diff -q` 實測全 IDENTICAL）。模板移位為檔案位置 + `TEMPLATE_PATH` 常數變更，零新增 reachable 邊、零狀態宇宙變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.13 全套 `pytest -m "not chaos"` = **1580 passed / 4 skipped / 0 failed**（v0.12 1577 + 3 新 test_template_location，只增不減；模板由新源碼位成功 bootstrap）。雙軌 ci-gate exit 0：v0.01:1478 / **v0.13:1580** / scripts/tests:27（FF-17 動態偵測 v0.13 自證入閘）。AutoClaude 全套 **3112 passed / 122 skipped / 0 failed**（B 軌未動 AutoClaude，持平基線零退化）；lint-imports **8 kept / 0 broken**；LOC violations=0；snapshot 新鮮。v0.13 潔淨度 `git add -A -n` = **853 would-add 無 runtime 殘留**（build/reports 整樹忽略、arch-fitness.json/chaos-report.json/.pyc 皆 0 漏網；種子模板於 tools/fsm_runtime/templates/ 正常 tracked）；`git check-ignore` 證 build/reports/fsm runtime 狀態檔仍被忽略。 |
| **回退指引** | 純向後相容（模板內容零變更，僅位置 + 路徑常數）。回退 W-22-2 即把 `state_loader.TEMPLATE_PATH` 指回 `REPO_ROOT/build/reports/fsm/...`、模板移回該處、.gitignore 還原 negate idiom。回退 W-22-1 即移除 `cross_version_guard` 的 `::` 剝除與 VERSION_RE 通則化（注意 DEF-22-001 還原會使 guard 對 v0.10+ 重新失能）。皆零觸碰 FSM/規則/既有測試。 |

## v0.11 → v0.12

| 欄位 | 內容 |
|------|------|
| **版本** | v0.11 → v0.12 |
| **日期** | 2026-06-16（凍結來源：AutoSDD_improving_21.md，🔴 人工 signoff：落地位置「框架本體 v0.12」+ 機制「git 事實層 + 證據契約」兩項 W） |
| **delta 清單** | **框架程式新增（B 軌 DEF-20-001 反幻覺機械閘門 — 結案證據強制重推導，W-21-1/W-21-2）**：把反幻覺紀律（結案宣稱 pytest passed / commit hash / push / tag 只能來自真實 repo 狀態）由「agent 跨 session 自律」升級為框架機械可驗 hook。① 新增 `tools/fsm_runtime/closure_evidence.py`（純函式邏輯模組，同 `drift_monitor.py` 慣例）——**廉價層** `verify_git_facts`：對 improving_NN.md 末尾 `closure-evidence` 契約宣稱的 `claimed_commits`/`claimed_tag` 以 `git cat-file -e`+`merge-base --is-ancestor`+`rev-parse --verify` 就 monorepo 根真實狀態重推導（白名單正則消毒、list-form argv shell=False），任一無法重推導→FAIL（直擊「編造 commit/push/tag」）；**昂貴層** `verify_expensive_claims`：pytest passed / ci-gate floors **不重跑**，改驗「綁定當前 HEAD 的 rederive 證書」（`write_rederive_cert` stamp HEAD），契約 base_sha≠HEAD 或證書缺失/數字不符→INCONCLUSIVE（fail-closed 不綠勾，比照 embodied_grounding 零觀測語意，絕不假綠）；`synthesize_verdict` 三分支優先序（FAIL>INCONCLUSIVE>VERIFIED）。② 新增 `.claude/hooks/closure_evidence_verify.py`（thin git post-commit hook，同 `post_commit_drift.py`——`repo_root_from()` 以 git toplevel 定位 monorepo 根〔非 parents[2]〕、**永遠 exit 0 / <2s budget / fail-soft / 不阻擋 commit**，verdict 寫 `.git/CLOSURE_EVIDENCE_VERDICT` + `build/reports/closure/VERDICT-<sha>.yaml`）。③ `tools/install_hooks/install_post_commit.{sh,ps1}` 擴充串接 drift（指 v0.01）+ closure（指 v0.12，首個含此 hook 版本），opt-in 不經 settings.json deny 層。④ 結案契約 schema＝improving_NN.md 末尾 ```yaml ``closure-evidence`` 區塊。⑤ CLI 入口 `_main(argv, repo_root=None)` + `__main__`（`--rederive --observed '<json>'` stamp HEAD 落盤證書 / 無參數 evaluate 印 verdict；**DEF-21-003** dogfooding 揭露：hook 訊息承諾的 `--rederive` 原無 CLI 入口）。⑥ 測試 `tools/fsm_runtime/tests/test_closure_evidence.py` **22 case**（以 tmp 真實 git repo 驗 cat-file/merge-base/rev-parse 真實行為：真 commit PASS/編造 FAIL/非祖先 FAIL/注入拒絕/真 tag PASS/缺 tag FAIL；昂貴層 stale base_sha/缺證書 INCONCLUSIVE、證書符 VERIFIED、不符 FAIL；verdict 三分支；last-match 解析；端到端+持久化；CLI rederive 寫證書/bad-json 回 2/無參數 evaluate）。**dogfooding 衍生 DEF-21-001**（P3, fixed@v0.12）：improving_NN.md 多 `closure-evidence` yaml 區塊致解析歧義→`parse_closure_evidence` 改 **last-match**（對齊 DEF-02-002 `tlc_runner` findall[-1] 紀律）+ §4 schema fence 改 ```text 雙保險。**決策**：不新增 R-9.x（advisory hook 不需規則承載，避免牽動 RULES_INDEX/ID_REGISTRY 取號與五軌 reachable，同 DEF-10-002 前例 + Rule 2；治理規則承載 + catch 覆蓋面推進 + closure 接入 SCG-4/5 機械閘門 → routed 未來輪）。**無 FSM 狀態/規則拓樸變更**（hook 不新增狀態/轉換、不寫 FSM-STATE.yaml；`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla` 對 v0.11 逐位元零差異）；ID_REGISTRY 不取新 ACT/rule。RFC：`build/planning/active/SDD_improving_Automation_27.md`。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.11 **逐位元零差異**。closure hook 為 git post-commit advisory、不新增 FSM 狀態/轉換、不寫 FSM-STATE，零新增 reachable 邊、零狀態宇宙變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.12 全套 `pytest -m "not chaos"` = **1577 passed / 4 skipped / 0 failed**（v0.11 1555 + 22 新 closure 測試〔19 核心 + 3 CLI〕，只增不減；首跑曾偶發 `test_file_lock::test_parallel_writes` flaky〔並行寫競態，本輪零碰該檔，單跑 3 passed + 全套重跑 0failed 證 flaky 非退化〕）。雙軌 ci-gate exit 0：v0.01:1478 / **v0.12:1574**〔ci-gate 跑於 CLI 補完前；CLI 3 測試後全套 1577，下次 ci-gate 自證〕 / scripts:25（FF-17 動態偵測 v0.12 自證入閘）。AutoClaude 全套 **3112 passed / 122 skipped / 0 failed**（B 軌未動 AutoClaude，持平基線零退化）；lint-imports **8 kept / 0 broken**；LOC violations=0；snapshot 新鮮。v0.12 潔淨度 `git add -A -n` = 852+（commit A）無 runtime 殘留（build/reports 僅 negate 保留 FSM-STATE-TEMPLATE.yaml；arch-fitness.json/chaos-report.json/.pyc 皆 0 漏網；closure runtime 產物根 `/build/reports/closure/` 經 DEF-21-002 gitignore）。**dogfooding 自驗**：commit A `5f8b633` 後回填 §10 真實契約 + rederive 證書 → hook **VERIFIED**（commit fact PASS + 3 昂貴項 VERIFIED），本輪新機制驗本輪自身結案、反幻覺迴圈閉合。 |
| **回退指引** | 純向後相容（hook opt-in、advisory 不阻擋 commit、預設未安裝＝零影響）。回退即不執行 `install_post_commit.*` 的 closure 串接（移除 .git/hooks/post-commit 的 closure 行），或移除 `closure_evidence.py`/`closure_evidence_verify.py`/`test_closure_evidence.py`。closure 機制零觸碰 FSM/規則/既有測試，移除無 breaking change。 |

## v0.10 → v0.11

| 欄位 | 內容 |
|------|------|
| **版本** | v0.10 → v0.11 |
| **日期** | 2026-06-16（凍結來源：AutoSDD_improving_20.md，🔴 人工 signoff：清乾淨重來〔A〕後「照 W-20-1 catch 覆蓋補強 + W-20-2 glob 通則化兩項進行」） |
| **delta 清單** | **框架程式（B 軌「鷹架代謝」L4→L5 catch 覆蓋補強，W-20-1）**：沿用 v0.10（improving_19）已定義的 catch 三要件契約（① 規則自描述 `failure_mode` 非空 ∧ ② 對應攔截事件 ESCALATION/MONITOR_VIOLATION 真實發生 ∧ ③ 該事件結構化攜帶此 rule_id 顯式歸因，**非時序鄰近猜測**），把 catch 自動歸因從 v0.10 的 2/39（R-9.1 gate retry / R-9.21 monitor）**擴到 4/39**，新增閉合 DEF-19-001「catch 覆蓋 2/39 漸進缺口」的兩條確定路徑：① `governance/rules/R-9.2-context-budget.yaml` 補 `failure_mode`（per-stage `auto_compact` 超限 → `record_escalation` 進 ESCALATION）；② `governance/rules/R-9.22-adversarial-self-improving-phase-j.yaml` 補 `failure_mode`（`spec_patch` per-AC 上限耗盡 → 直升 ESCALATION）；③ `tools/fsm_runtime/fsm_runtime.py` 在**兩個既有 `record_escalation` 呼叫點**接 `self._record_escalation_catches([...])`（沿用 v0.10 helper，非新增方法）——`trigger_auto_compact` 的 `projected > max_per_stage` escalate 分支歸因 `["R-9.2"]`、`enter_spec_patch_proposal` 的 `prior >= MAX_SPEC_PATCH_PER_AC` escalate 分支歸因 `["R-9.22"]`（皆 record_escalation 後、後續流程前）。**沿用 v0.10 既有不變式**：flag `SDD_ENABLE_RULE_CATCH_TELEMETRY` 預設 **OFF＝行為逐字同 v0.10**（catch 全程 0）；fail-closed（記帳失敗不阻塞已落定 escalation）；只增 `catch_count`、**永不 `set_maturity`**（R-9.20 #11，退役仍 🔴 人工）。**框架程式（W-20-2 / DEF-19-002 通則化）**：`tools/arch_fitness/arch_fitness.py` 的 FF-17 驗證正則 `_CI_GATE_LATEST_GLOB_RE` 由寫死 `AISDLC_SDD_v0\.0\*` 放寬為通則 `AISDLC_SDD_v0\.[0-9\[*]`（接受 `v0.0*` / `v0.[0-9]*` / `v0.[1-9]*` / `v0.*` 等動態偵測慣用語），**解除 improving_19 雙 glob 修復被迫保留 `v0.0*` 子串的結構耦合異味**（ci-gate.sh 此後可清掉 `v0.0*` 子串改純通則化仍通過 FF-17）；同步更新兩處顯示字串（`ff17-no-versions` detail + `missing` 訊息）。**測試**：新增 `tools/fsm_runtime/tests/test_w20_catch_wiring.py` **6 case**（R-9.2 flag ON 真記+1 / R-9.2 flag OFF 零退化 / R-9.22 flag ON 真記+1 / R-9.22 flag OFF 零退化 / 真實凍結 R-9.2、R-9.22 已具非空 failure_mode×2）；`test_arch_fitness.py` 新增 **4 case**（FF-17 接受 4 種通則 glob 形式，含向後相容 `v0.0*`）。**新記/更新缺陷**：DEF-19-001 由 routed 推進（catch 覆蓋 2/39 → 4/39，仍漸進補強剩餘 35 規則）；DEF-19-002 由 routed → fixed@v0.11（子串耦合異味通則化解除）。**無 FSM 狀態/規則拓樸變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla` 對 v0.10 逐位元零差異，catch 記帳為 record_escalation 已落定後的非轉態 side-effect；arch_fitness 為純讀靜態分析工具，零 reachable 影響）；ID_REGISTRY 不取新 ACT/rule（接線既有 v0.10 catch helper 能力）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla`（SDD_FSM / META_FSM / FLEET_FSM / COMPOSITION_FSM / OPTIMIZATION_FSM）對 v0.10 **逐位元零差異**（`diff` 實測：全 ZERO-DIFF）。W-20-1 為既有 `record_escalation` 呼叫點後的非轉態記帳 side-effect、W-20-2 為純讀靜態分析正則放寬，零新增 reachable 邊、零狀態宇宙變更。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.11 全套 `pytest -m "not chaos"` = **1555 passed / 4 skipped / 0 failed**（v0.10 1545 + 10 新測試〔6 W-20-1 catch + 4 W-20-2 FF-17〕，只增不減）。雙軌 ci-gate exit 0：v0.01:1478 / **v0.11:1555** / scripts:25（FF-17 動態偵測 v0.11 自證入閘）。AutoClaude 全套 **3112 passed / 0 failed** 持平（B 軌未動 AutoClaude）；lint-imports **8 kept / 0 broken**；LOC violations=0；snapshot 新鮮。v0.11 潔淨度 `git add -A -n` = 848 would-add 無 runtime 殘留（build/reports 僅 FSM-STATE-TEMPLATE.yaml）。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.10 行為）。回退 W-20-1 即移除 `fsm_runtime.py` 兩處 `_record_escalation_catches(["R-9.2"/"R-9.22"])` 呼叫 + R-9.2/R-9.22 的 `failure_mode` 欄（兩規則退回不參與 catch 歸因），或運行時不設 `SDD_ENABLE_RULE_CATCH_TELEMETRY`＝永不啟用 catch 遙測（catch_count 維持 0）。回退 W-20-2 即把 `_CI_GATE_LATEST_GLOB_RE` 還原為 `AISDLC_SDD_v0\.0\*`（注意：ci-gate.sh 若已清掉 `v0.0*` 子串則須同步還原，否則 FF-17 假紅）。 |

## v0.09 → v0.10

| 欄位 | 內容 |
|------|------|
| **版本** | v0.09 → v0.10 |
| **日期** | 2026-06-16（凍結來源：AutoSDD_improving_19.md，🔴 人工兩段 signoff：方向「主推 B 軌 catch 側契約」+ SCG-2「否決時序匹配、採顯式可歸因 + fail-closed，本輪只真接 R-9.1/R-9.21 確定路徑」） |
| **delta 清單** | **框架程式新增（B 軌「鷹架代謝」L4→L5 信號 — catch 側契約定義並接入 FSM 主迴圈，W-19-1/W-19-2/W-19-3）**：閉合 DEF-18-001 點名的「catch 側（`caught=True`）語意框架從未定義」——16 接「增」(SLV)、17 接「減·決策」(run_gc)、18 接「減·遙測 fire 側」、本輪接「**catch 側**」完成對偶閉環。**catch 契約**＝三要件齊備才 `catch_count+1`（顯式可歸因，**非時序鄰近猜測**——時序鄰近≠因果歸因，否則污染 ROI「比不做更糟」）：① 規則自描述守望的 `failure_mode`（非空）；② 對應攔截事件（ESCALATION/MONITOR_VIOLATION）真實發生；③ 該事件結構化攜帶此 rule_id（呼叫端明確歸因）。① `tools/fsm_runtime/rule_loader.py`：`Rule` dataclass 新增 optional `failure_mode: str=""`（W-19-1，舊規則缺＝不參與歸因）；`_load_rule_file` 讀取；**`_write_rule` 條件寫回 failure_mode（非空才寫、排 test_ref 後 scaffold_roi 前）——持久化潔淨度陷阱解法：否則 fire/catch round-trip 重寫 YAML 會對全部舊規則插入空 failure_mode、污染凍結本體**；新增批次 helper `record_state_catches(attributed_rule_ids)`（對「歸因∩failure_mode非空∩非deprecated」子集各記 catch+1，回傳實記清單；fail-closed：空歸因/無 failure_mode 不記）。② `tools/fsm_runtime/fsm_runtime.py`：新增 module 常數 `_RULE_CATCH_TELEMETRY_ENV="SDD_ENABLE_RULE_CATCH_TELEMETRY"` + 純函式 `_rule_catch_telemetry_enabled()`（鏡像 fire flag）；新增 FSMRuntime 方法 `_record_escalation_catches(attributed_rule_ids)`（flag-gated、fail-closed）；接在**兩個 `record_escalation` 呼叫點**（W-19-2）——`record_attempt` escalate 分支歸因 `["R-9.1"]`（gate retry 耗盡）、`exit_monitor_violation` 歸因 `["R-9.21"]`（monitor 破壞）。**關鍵架構決策**：catch 記帳接在 `record_escalation` 呼叫點而**非** `transition()`——因 `record_escalation` 直接設 `current=ESCALATION` 繞過 `transition()`，只接 transition() 會漏記 gate_fail 主路徑。③ `rule_fire_telemetry_stats()`（W-19-3）：證書 **翻 `catch_side_wired=True`**（catch 側已接線，ROI=catch/fire 成雙側信號）+ 新增頂層 `total_catches` + 證書 `catch_attribution_coverage`（誠實揭露有 failure_mode 的規則覆蓋率，未覆蓋者 catch_count 恆 0 fail-closed）。④ `governance/rules/R-9.1-fsm-retry-budget.yaml` / `R-9.21-trustworthy-scaled-phase-i.yaml` 補 `failure_mode` 欄（兩條確定可歸因規則）。⑤ `tools/fsm_runtime/tests/test_rule_catch_telemetry_wiring.py` 新增 **11 case**（flag off 零退化×2 / helper 真記 R-9.1 / monitor violation 整合 R-9.21 / fail-closed / R-9.20 #11 紅線零 set_maturity / 要件①無 failure_mode 不歸因 / 空歸因寧缺勿濫 / helper 子集語意+deprecated 不記 / **持久化陷阱回歸鎖〔fire round-trip 不抹 failure_mode + 無 failure_mode 不冒空欄〕/ stats 覆蓋率揭露**）；`test_rule_fire_telemetry_wiring.py` Case 7 同步斷言（catch_side_wired False→True + coverage）。**紅線守界**：`record_state_catches` 只增 catch_count、**永不 set_maturity**（R-9.20 #11）；catch_count>0 正是 `propose_graduation` 保護有用規則不被誤退役的依據（閉合 arch_fitness FF-16 GAP-X2「GC 無退役提案」advisory 的真實資料缺口）。**無 FSM 狀態/規則變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla` 零改動，catch 記帳是 record_escalation 已落定後的非轉態 side-effect）；ID_REGISTRY 不取新 ACT/rule（接線既有 ACT-054 record_fire/scaffold_roi 能力）。**新記 DEF-19-001**（P3, routed）：catch 歸因目前僅覆蓋 2/39 規則（R-9.1/R-9.21 兩確定路徑），其餘 37 規則 failure_mode 未定義→catch 側未覆蓋；生產全面啟用前須逐步補各規則 failure_mode + 對應 escalation 呼叫點歸因（漸進補強，coverage 誠實揭露）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla` 對 v0.09 **逐位元零差異**（catch 記帳為 `record_escalation` 已落定後的非轉態 side-effect，零新增 reachable 邊、零狀態宇宙變更）。依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.10 全套 `pytest -m "not chaos"` = **1545 passed / 4 skipped / 0 failed**（v0.09 1534 + 11 catch wiring，只增不減）；新 catch wiring **11 passed** + fire wiring 8 passed。雙軌 ci-gate exit 0：v0.01:1478 / v0.10:1545 / scripts:25（FF-17 v0.10 自證入閘）。AutoClaude 全套 3112 passed / 0 failed 持平（B 軌未動 AutoClaude）；lint-imports 8 kept；LOC violations=0；snapshot 新鮮。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.09 行為）。回退即把 `fsm_runtime.py` 指回 v0.09（移除 `_rule_catch_telemetry_enabled`/`_record_escalation_catches`/兩呼叫點歸因 + stats 證書翻回 catch_side_wired=False）+ `rule_loader.py` 移除 `record_state_catches`、`Rule.failure_mode`、`_write_rule` 條件寫回 + R-9.1/R-9.21 移除 failure_mode 欄，或運行時不設 `SDD_ENABLE_RULE_CATCH_TELEMETRY`＝永不啟用 catch 遙測。flag OFF 時 catch_count 維持 0（與 v0.09 一致）。 |

## v0.08 → v0.09

| 欄位 | 內容 |
|------|------|
| **版本** | v0.08 → v0.09 |
| **日期** | 2026-06-16（凍結來源：AutoSDD_improving_18.md，🔴 人工 signoff：方向「主推 B 軌；閉合 GAP-X2 遙測迴圈」） |
| **delta 清單** | **框架程式新增（B 軌「鷹架代謝」L4→L5 信號 — 規則命中遙測接入 FSM 主迴圈，W-18-1/W-18-2）**：閉合 DEF-17-001 點名的「`fire_count=0` 根因」——上輪接 `run_gc`（代謝決策側）但 `record_fire` 在 FSM 主迴圈零自動呼叫致 39 規則 `fire_count=0`、GC 恆產零提議。本輪接「遙測側」使 `fire_count` 真實累積。① `tools/fsm_runtime/rule_loader.py` 新增批次 helper `record_state_fires(state, *, caught=False)`（單次 `load_all`、對 `load_for_state(state)` 命中規則一次性增 `fire_count` 各寫一次，避 per-rule O(N²) 重載；deprecated 不記）。② `tools/fsm_runtime/fsm_runtime.py`：新增 module 常數 `_RULE_FIRE_TELEMETRY_ENV="SDD_ENABLE_RULE_FIRE_TELEMETRY"` + 純函式 `_rule_fire_telemetry_enabled()`（env-flag 慣例同 `_scaffold_gc_auto_propose_enabled`）；`transition()` 於 `save_state` **後** flag-gated 呼 `rule_loader.record_state_fires(dst)`（**預設 OFF＝行為逐字同 v0.08**；fail-closed：記帳任何失敗絕不阻塞已落定的轉態）。**「fire（命中）」語意**＝FSM 經 `transition()` 進入狀態 dst 時，`load_for_state(dst)` 命中規則各記一次 on-watch fire（與框架既有「規則依狀態 lazy-load」模型同構）。③ 新增 `rule_fire_telemetry_stats()` 純讀 L5 可量測信號（自 `load_all` 獨立重算 fire 分佈、fire_ladder 降冪＝行使最多 critical path、`retirement_eligible`＝fire≥門檻∧catch=0）+ XAI 安全證書（`auto_retire=False` + **誠實揭露 `catch_side_wired=False`**）。④ `tools/fsm_runtime/tests/test_rule_fire_telemetry_wiring.py` 新增 **8 case**（flag off 零退化 ×2 / flag on 命中規則記 on-watch fire + 選擇性 / fire_count 累積持久化 / 記帳失敗 fail-closed / 遙測零 set_maturity 呼叫〔R-9.20 #11 紅線〕/ stats 度量穩健+證書誠實揭露 / fire_ladder 降冪+retirement_eligible 判定）。**紅線守界**：`record_state_fires` 只增 `scaffold_roi.fire_count` 計數、**永不呼叫 `set_maturity`**，active 規則退役仍 🔴 人工（R-9.20 #11 不弱化）；`rule_fire_telemetry_stats` 純讀不碰 meta-oracle。**新記 DEF-18-001**（P3, routed）：catch 側（`caught=True`）語意框架未定義，本輪只接 fire 側，ROI 為單側信號，生產啟用退役前須先定義 catch 契約。**無 FSM 狀態/規則變更**（`transition_rules.py`/`_HAPPY_PATH` 與全部 5 `*.tla` 零改動，僅在既有 `transition()` 加非轉態 side-effect）；ID_REGISTRY 不取新 ACT/rule（接線既有 ACT-054 record_fire 能力）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla` 對 v0.08 **逐位元零差異**（`diff` 實測：transition_rules.py + SDD_FSM/META_FSM/FLEET_FSM/COMPOSITION_FSM/OPTIMIZATION_FSM 全 ZERO-DIFF）。本輪僅在既有 `transition()` 完成轉態（已 `save_state`）**之後**加 flag-gated 非轉態 side-effect（記 fire_count + 純讀統計），零新增 reachable 邊、零狀態宇宙變更，依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.09 全套 `pytest -m "not chaos"` = **1534 passed / 4 skipped**（v0.08 1526 + 8 wiring，只增不減、0 failed）；新 wiring **8 passed**；flag OFF 既有 test_phase_h/test_transitions 零退化。雙軌 ci-gate exit 0：v0.01:1478 / v0.09:1534 / scripts/tests:25（FF-17 v0.09 自證入閘）。AutoClaude 全套 3112 passed / 0 failed 持平（B 軌未動 AutoClaude）；lint-imports 8 kept；LOC violations=0；snapshot 新鮮。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.08 行為）。回退即把 `tools/fsm_runtime/fsm_runtime.py` 指回 v0.08（移除 `transition()` flag-gated 區塊 + `_rule_fire_telemetry_enabled`/`rule_fire_telemetry_stats`）+ `rule_loader.py` 移除 `record_state_fires`，或運行時不設 `SDD_ENABLE_RULE_FIRE_TELEMETRY`＝永不啟用 fire 遙測。flag OFF 時 `fire_count` 仍維持 0（與 v0.08 一致，DEF-17-001 之零資料依賴留存且對齊 v0.08）。 |

## v0.07 → v0.08

| 欄位 | 內容 |
|------|------|
| **版本** | v0.07 → v0.08 |
| **日期** | 2026-06-16（凍結來源：AutoSDD_improving_17.md，🔴 人工兩段 signoff：放行「開跑 improving_17」+ 方向「主推 B 軌；W-17-1 GC 接主迴圈 + W-17-2 scaffold_gc_stats XAI」） |
| **delta 清單** | **框架程式新增（B 軌「鷹架代謝」L4→L5 信號，W-17-1/W-17-2）**：把既有 `scaffold_gc.run_gc()`（產 `RetirementProposal` proposed 退役提議，原測試-only / 手動）**接入 FSM 主迴圈**——`tools/fsm_runtime/fsm_runtime.py`：① 新增 module 常數 `_SCAFFOLD_GC_AUTO_PROPOSE_ENV="SDD_ENABLE_SCAFFOLD_GC_AUTO_PROPOSE"` + 純函式 `_scaffold_gc_auto_propose_enabled()`（env-flag 慣例同 `_slv_auto_propose_enabled`）；② `enter_scaffold_gc()` 進態 SCAFFOLD_GC **後** flag-gated 自動呼叫 `scaffold_gc.run_gc(self.state, today=...)` 算 ROI 並落 `SCAFFOLD-ROI-{date}.md` + 填 `scaffold_gc_tracking`（`origin/roi_report_ref/proposals_total/rules_scanned`）（**預設 OFF＝行為逐字同 v0.07**；fail-closed：run_gc 任何失敗進態仍成功、不偽造報告路徑）；③ 新增 `scaffold_gc_stats()` 純讀 L5 可量測信號（自 `compute_proposals` 獨立重算 ROI、roi_ladder 升冪＝最該退役 critical path、by_transition 分佈）+ XAI 安全證書（`safety_certificate.auto_retire=False`）。④ `tools/fsm_runtime/tests/test_scaffold_gc_auto_propose_wiring.py` 新增 **9 case**（flag off 零退化 ×2 / flag on 自走 run_gc+tracking / 報告真實落盤 / run_gc 失敗 fail-closed / GC 零 set_maturity 呼叫〔R-9.20 #11 紅線〕/ 非 RELEASE 源仍 raise〔入口紀律不被 flag 弱化〕/ 零提議度量穩健 / roi_ladder 升冪+by_transition）。**紅線守界**：`run_gc` 只產提議永不退役、active 規則退役仍 🔴 人工 `set_maturity(reviewed_by=)`（R-9.20 #11 不弱化）、`scaffold_gc_stats` 純讀不碰 meta-oracle（GC 是 ROI 統計層）。**無 FSM 狀態/規則變更**（`SCAFFOLD_GC` 既有 state、RELEASE→SCAFFOLD_GC→RELEASE 既有邊、`_HAPPY_PATH` 與全部 `*.tla` 零改動，僅改 Python 進態方法加非轉態 side-effect）；ID_REGISTRY 不取新 ACT/rule（接線既有 ACT-055 能力）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla` 對 v0.07 **逐位元零差異**（`diff` 實測：transition_rules.py + SDD_FSM/META_FSM/FLEET_FSM/COMPOSITION_FSM/OPTIMIZATION_FSM 全 ZERO-DIFF）。本輪僅在既有 `enter_scaffold_gc` 進態 SCAFFOLD_GC **之後**加非轉態 side-effect（run_gc + 記報告路徑 + 純讀統計），零新增 reachable 邊、零狀態宇宙變更，依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。 |
| **驗證** | v0.08 全套 `pytest -m "not chaos"` = **1526 passed / 4 skipped**（v0.07 1517 + 9 wiring，只增不減、0 failed）；新 wiring **9 passed**；flag OFF 既有 test_phase_h/test_transitions 零退化。雙軌 ci-gate：v0.01 凍結基線 + v0.08 自動偵測為最新版（FF-17 自證入閘）。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.07 行為）。回退即把 `tools/fsm_runtime/fsm_runtime.py` 指回 v0.07（移除 `enter_scaffold_gc` flag-gated 區塊 + `_scaffold_gc_auto_propose_enabled`/`scaffold_gc_stats`），或運行時不設 `SDD_ENABLE_SCAFFOLD_GC_AUTO_PROPOSE`＝永不啟用自動 GC 提議。 |

## v0.06 → v0.07

| 欄位 | 內容 |
|------|------|
| **版本** | v0.06 → v0.07 |
| **日期** | 2026-06-15（凍結來源：AutoSDD_improving_16.md，🔴 人工兩段 signoff：方向「B 軌→L5 規則自演化 meta-loop」+ scope「Block-1 + Block-3」） |
| **delta 清單** | **框架程式新增（B 軌「規則自演化」L4→L5 信號，W-16-1/W-16-2）**：把既有 `slv_generator.propose_slv_from_fpl()`（`trust_level:proposed` 草案合成，proposal-only / 手動 CLI）**接入 FSM 主迴圈**——`tools/fsm_runtime/fsm_runtime.py`：① 新增 module 常數 `_SLV_AUTO_PROPOSE_ENV="SDD_ENABLE_SLV_AUTO_PROPOSE"` + 純函式 `_slv_auto_propose_enabled()`（env-flag 慣例同 `_auto_recovery_enabled`）；② 新增 staticmethod `_auto_draft_slv(fpl_id)`（複用 `load_fpl_entry`/`propose_slv_from_fpl`/`write_rule_candidate` 純合成、不轉態、fail-closed）；③ `exit_production_behavioral_signal()` 加 optional `fpl_id`，**learn 分支**於轉態到 LEARNING_COMMIT **後** flag-gated 自動 draft proposed 草案 + 填 `learning_commit_tracking`（**預設 OFF＝行為逐字同 v0.06**；fail-closed：任何失敗停在 LEARNING_COMMIT 不偽造；附帶修 **DEF-16-001** learn 採納鏈結構性斷裂）；④ 新增 `learning_loop_stats()` 純讀 L5 可量測信號（auto_proposed/approval_rate/unattended_proposal_rate）+ XAI 良基終止證書（meta_halt ChurnBounded `churn_max` + GraduationRatchet）。⑤ `tools/fsm_runtime/tests/test_slv_auto_propose_wiring.py` 新增 **9 case**（flag off 零退化 ×2 / flag on 自走 draft+tracking / learn→人 verify→approve 鏈閉合〔DEF-16-001〕/ 未 verify approve→raise〔R-9.11 紅線〕/ FPL 不存在 fail-closed / 合成失敗 fail-closed / 零事件度量穩健 / 計數+churn_max 一致）。**紅線守界**：草案恆 `trust_level:proposed`（R-9.11）、trust_level→verified 仍 🔴 人工（`exit_learning_commit` verified 強制檢查不動）、採納經 `meta_halt_monitor`（R-9.24 不弱化）、`learning_loop_stats` 純讀不碰 meta-oracle（R-9.37）。**無 FSM 狀態/規則變更**（`LEARNING_COMMIT` 既有 state、`PRODUCTION_BEHAVIORAL_SIGNAL→LEARNING_COMMIT` 既有 learn 邊、`_HAPPY_PATH` 與全部 `*.tla` 零改動，僅改 Python 觸發者加非轉態 side-effect）；ID_REGISTRY 不取新 ACT/rule（接線既有能力）。 |
| **TLC 證據** | **N/A** — `transition_rules.py`（含 `_HAPPY_PATH`）與全部 5 `*.tla` 對 v0.06 **逐位元零差異**（`diff` 實測：transition_rules.py + SDD_FSM/META_FSM/FLEET_FSM/COMPOSITION_FSM/OPTIMIZATION_FSM 全 ZERO DIFF）。本輪僅在既有 `PRODUCTION_BEHAVIORAL_SIGNAL→LEARNING_COMMIT`（learn）轉態**之後**加非轉態 side-effect（draft proposed 規則 + 填 tracking + 純讀統計），零新增 reachable 邊、零狀態宇宙變更，依 Rule 9.18.1 無重跑義務（比 v0.06 更強：連觸發者狀態邊都不動）；既有五軌證明維持有效。 |
| **驗證** | v0.07 全套 `pytest -m "not chaos"` = **1517 passed / 4 skipped**（v0.06 1508 + 9 wiring，只增不減、0 failed）；新 wiring **9 passed**；flag OFF 既有 test_phase_i/test_slv_generator 相關 **37 passed 零退化**。雙軌 ci-gate 見 improving_16 階段四（v0.01 凍結基線 + v0.07 自動偵測為最新版，FF-17 自證入閘）。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.06 行為）。回退即把 `tools/fsm_runtime/fsm_runtime.py` 指回 v0.06（移除 learn 分支 flag-gated 區塊 + `_auto_draft_slv`/`_slv_auto_propose_enabled`/`learning_loop_stats`），或運行時不設 `SDD_ENABLE_SLV_AUTO_PROPOSE`＝永不啟用自動提議。DEF-16-001 的修復隨 flag ON 生效；flag OFF 時與 v0.06 行為一致（該潛在斷裂留存且與 v0.06 對齊）。 |

## v0.05 → v0.06

| 欄位 | 內容 |
|------|------|
| **版本** | v0.05 → v0.06 |
| **日期** | 2026-06-15（凍結來源：AutoSDD_improving_15.md，🔴 人工 signoff「核定 W-15-1 接入 auto_recovery 升 B 軸 L4」；首次真實 v0.06 演化） |
| **delta 清單** | **框架程式新增（B 軌「流程自治」L3→L4 升級，W-15-1）**：把既有 `tools/fsm_runtime/auto_recovery.py`（Rule 9.14 有界 1-shot 自癒，proposal-only）**接入 FSM 主迴圈**——`tools/fsm_runtime/fsm_runtime.py`：① 新增 `import os` + module 常數 `_AUTO_RECOVERY_ENV="SDD_ENABLE_AUTO_RECOVERY"` + 純函式 `_auto_recovery_enabled()`（讀環境變數開關，符 SDD 框架慣例 SDD_PROJECT/SDD_RUN_TLC…）；② `record_gate_result()` escalate 分支於 `record_escalation()` 後**flag-gated 自動嘗試** `enter_auto_recovery(escalation_reason=reason, resume_state=gate)`——把既有 `ESCALATION → AUTO_RECOVERY_ATTEMPT` 邊（TLA `T_EnterAutoRecover` 已模型化）從「需 orchestrator 手動觸發」升為「gate retry 耗盡自動觸發」；**預設 OFF＝行為與 v0.05 完全一致（零退化）**；fail-closed（structural/bounds 由 enter_auto_recovery 自轉 ESCALATION_FINAL、任何例外停在 ESCALATION）；③ 新增 `_gate_is_resumable()` 預檢（防 resume_state 非白名單致撕裂轉態）+ `auto_recovery_stats()` 純讀彙整 L4 可量測信號（recovery_success_rate / unattended_recovery_rate）。④ `tools/fsm_runtime/tests/test_auto_recovery_wiring.py` 新增 **9 case**（flag off 零退化 / flag on transient 自走進 recovery / 完整閉環 success 回 gate / structural→FINAL〔R-9.14.3〕/ bounds→FINAL〔R-9.14.1〕/ fail→FINAL〔R-9.14.4〕/ resumable 預檢 / 空 session 零率）。**無 FSM 狀態/規則變更**（`AUTO_RECOVERY_ATTEMPT` 既有合法 state、`ESCALATION→AUTO_RECOVERY_ATTEMPT` 既有合法邊、`_HAPPY_PATH` 與全部 `*.tla` 零改動，僅改 Python 觸發者非狀態宇宙，Rule 9.18.1 不啟動）；ID_REGISTRY 維持 next_free act=173 / rule="9.39"，**不取新 ACT/rule**（接線既有能力）。 |
| **TLC 證據** | **N/A** — `_HAPPY_PATH` 與全部 `*.tla` 零修改（diff 為空）。本輪僅把「既有且已被五軌 TLC 證為有界停機之 `ESCALATION→AUTO_RECOVERY_ATTEMPT` 轉換（`T_EnterAutoRecover`，recovery 計數器有界）」的**觸發者**由 orchestrator 手動改為 record_gate_result 自動——狀態宇宙、可達集、不變量完全不變，依 Rule 9.18.1 無重跑義務；既有五軌證明維持有效。Rule 9.14 有界性（≤3/session、≤1/同因、structural 禁、fail→ESCALATION_FINAL）為自動觸發後仍成立的停機保證。 |
| **驗證** | v0.06 全套 `pytest -m "not chaos"` = **1508 passed / 4 skipped**（v0.05 1499 + 9 wiring，只增不減、0 failed）；flag OFF 既有 test_auto_recovery/test_transitions/test_phase_h **86 passed 零退化**；新 wiring **9 passed**。**雙軌 ci-gate**：見 improving_15 階段四（v0.01 凍結基線 + v0.06 自動偵測為最新演化版，FF-17 自證）。 |
| **回退指引** | 純向後相容（預設 flag OFF＝v0.05 行為）。回退即把 `tools/fsm_runtime/fsm_runtime.py` 指回 v0.05（移除 escalate 分支的 flag-gated 區塊 + 3 helper），或運行時不設 `SDD_ENABLE_AUTO_RECOVERY`＝永不啟用自動恢復。DEF-15-001 對 `scripts/copy_on_evolve.sh`（共享 infra）的修正獨立於本版，不需回退。 |

> **共享 infra 同輪修（DEF-15-001，免 Copy-on-Evolve）**：`scripts/copy_on_evolve.sh` 原 `tar --exclude build/reports` 會誤殺 `build/reports/fsm/FSM-STATE-TEMPLATE.yaml`（state_loader 必需的 FSM 種子模板＝真輸入），致首次真實 v0.06 演化後整個 FSM runtime 無法 bootstrap（46+ 測試全紅當場揭露）。修法＝排除後補回該模板（來源存在時）+ `scripts/tests/test_copy_on_evolve.py` 加回歸鎖 case。詳見 AutoSDD_Defect_Log.md DEF-15-001。

## v0.04 → v0.05

| 欄位 | 內容 |
|------|------|
| **版本** | v0.04 → v0.05 |
| **日期** | 2026-06-15（凍結來源：AutoSDD_improving_11.md，🔴 人工確認 scope「(a)+(b) 全做，完整關閉 DEF-10-002」） |
| **delta 清單** | **框架程式新增（DEF-10-002b 回流，B 軌 dogfooding 於 improving_10 收尾提出之治理通則化建議）**：`tools/arch_fitness/arch_fitness.py` 新增 **FF-17「Copy-on-Evolve 演化版必納官方閘門」** 結構守門——把 improving_04 對 DEF-03-001 的「凍結基線 + 自動偵測最新版」雙軌**點修**固化為**結構不變量**（fitness function）。① 新增 module 常數 `CI_GATE_PATH = PROJECT_ROOT / "scripts" / "ci-gate.sh"`（共享 CI infra，versioned 目錄外）；② 新增純函式 `_latest_version_dir()`（磁碟 `AISDLC_SDD_v0.0*` 語意版本最高者）；③ 新增 `check_ff17_evolution_version_gate_coverage()`：靜態讀 `ci-gate.sh` 文字，斷言含四錨點動態最新版偵測慣用語（`ls -d AISDLC_SDD_v0.0*` / `sort -V` / `tail -1` / `FW_VERSIONS+=(…LATEST…)`），任一缺失＝退回靜態寫死＝DEF-03-001 回歸＝`structural fail`（fingerprint `ff17-static-pin`）；腳本不存在/無版本目錄＝INFO 略過（鏡像 FF-14 no-refs）；④ 註冊進 `ALL_CHECKS`；⑤ docstring 計數 16→17、exit-code structural fail 清單補 FF-17；⑥ `tools/fsm_runtime/tests/test_arch_fitness.py` 新增 **5 case**（真 repo 涵蓋最新版/合成雙軌 PASS/寫死單版 fail/漏 append-latest fail/腳本缺 INFO 略過）。與 FF-14 同源：靜態讀 CI 腳本、純讀無副作用、跨平台（不執行 shell，零新 subprocess 攻擊面）。**設計決策**：DEF-10-002(b) 原文提及 governance/R-*.yaml，惟新增 maturity=active 規則會連鎖 FF-8 test_ref + FF-10 可達性 + FF-12 severity 要求，且 R-9.x 絕對禁令係自演化 meta-loop 停機安全之異類關注點；arch_fitness 本即治理層 fitness-function 套件（structural fail 即阻擋官方閘門），故採最小正確實作＝新增 FF-17，**不另開 R-9.x**（Rule 2/3）。**無 FSM 狀態/規則變更**（`_HAPPY_PATH` 與 `*.tla` 零改動，Rule 9.18.1 不啟動）；ID_REGISTRY 維持 next_free act=173 / rule="9.39"，**不取新 ACT/rule**（純 fitness-function 新增）。**整合層（A 軌）DEF-10-002(a) 同輪修**：迭代範本 `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 階段一補檢核項 (f)「外部工具依賴須先確認 invocation 形態」（落 monorepo 根 docs，非 v0.05 本體；見 improving_11 §2）。 |
| **TLC 證據** | **N/A** — `_HAPPY_PATH` 與全部 `*.tla` 零修改（diff 為空），依 Rule 9.18.1 無重跑義務；五軌 TLC 既有證明維持有效。FF-17 為靜態 CI 腳本守門，不涉 FSM 狀態宇宙。 |
| **驗證** | v0.05 全套 `pytest -m "not chaos"` = **1499 passed / 4 skipped**（v0.04 1494 + 5 FF-17，只增不減）；`test_arch_fitness.py` 87 passed；`arch_fitness --only FF-17` exit 0（偵測最新版 v0.05、ff17-ok）；**雙軌 ci-gate exit 0**「版本：AISDLC_SDD_v0.01 AISDLC_SDD_v0.05、逐軌計數 v0.01:1478 v0.05:1499」——v0.05 作為最新演化版自動納入官方閘門，自證 FF-17 不變量成立。 |
| **回退指引** | FF-17 為向後相容純新增（新 FF + 新測試，不改既有 FF 行為）；回退即把 `tools/arch_fitness/arch_fitness.py` / `test_arch_fitness.py` 指回 v0.04（無 FF-17，arch_fitness 退為 16 FF，ci-gate 雙軌行為不變——DEF-03-001 點修仍在 ci-gate.sh，只是失去結構守門回歸防護）。ci-gate.sh 本身未改（共享 infra），不需回退。 |

## v0.03 → v0.04

| 欄位 | 內容 |
|------|------|
| **版本** | v0.03 → v0.04 |
| **日期** | 2026-06-14（凍結來源：AutoSDD_improving_03.md，同日 🔴 人工確認凍結方案 A，W1/W2 啟動） |
| **delta 清單** | **框架程式 bugfix（DEF-02-002 回流，B 軌 dogfooding 發現於 improving_02 收尾複核）**：`tools/fsm_runtime/tlc_runner.py` 計數解析修正——① 抽出 module-level 純函式 `parse_tlc_summary(out)`，以 `re.findall(...)[-1]`（**last-match**）取最終 summary 行，取代舊版 `re.search`（**first-match**）誤抓 TLC 執行中途 progress 行致 raw 計數不可靠（曾出現 `distinct=855 > generated=706` 違反恆等不變量）；② 加 **fail-closed 斷言**：`generated >= distinct`（窮舉先生成後去重），兩值皆非零且違反時 `raise RuntimeError`（僅守不變量，零值/邊界容忍不誤殺）；③ `run_tlc` 改呼叫 `parse_tlc_summary`；④ 新增 `tools/fsm_runtime/tests/test_tlc_runner_parsing.py`（4 case：last-match/正常不誤報/畸形 raise/無匹配回 0）。**無 FSM 狀態/規則變更**（`_HAPPY_PATH` 與 `*.tla` 零改動，Rule 9.18.1 不啟動）；ID_REGISTRY 維持 next_free act=173 / rule="9.39"，**不取新 ACT/rule**（純工具 bugfix）。**整合層（A 軌）DEF-01-008 同輪修**：AutoClaude `main.py` flag-gated brain 注入（落 AutoClaude repo，非 v0.04 本體；見 improving_03 §2）。 |
| **TLC 證據** | 雖未觸發 Rule 9.18.1（無 `_HAPPY_PATH`/`.tla` 改動），仍跑五軌 TLC 驗證**修正本身正確**（last-match 取對最終 summary、generated ≥ distinct）。實測（2026-06-14，`python -m tools.fsm_runtime.tlc_runner --module <各軌>`，v0.04 目錄，exit 0 / No error found）：<br>• **SDD_FSM**：DISTINCT=855 / GENERATED=**4706** / DEPTH=14<br>• **META_FSM**：13 / 24 / 6<br>• **FLEET_FSM**：7 / 8 / 7<br>• **COMPOSITION_FSM**：21 / 28 / 7<br>• **OPTIMIZATION_FSM**：12 / 21 / 5<br>**DEF-02-002 修復鐵證**：SDD_FSM GENERATED 由舊 first-match 誤報 **706** 修正為 last-match 真值 **4706**，`generated(4706) >= distinct(855)` 恆等不變量**現已成立**（舊「distinct 855 > generated 706」錯配徹底消除），且 fail-closed 斷言未誤殺（因已合法）。五軌 liveness（EventuallyTerminal/ObservationsTransient）+ safety 全過、0 violation。 |
| **回退指引** | tlc_runner 修正為向後相容純函式重構（對外行為僅「計數更準 + 畸形 fail-closed」）；回退即把 `tlc_runner.py` 指回 v0.03（first-match 版，計數不可靠但停機判準 `No error has been found` 不受影響）。AutoClaude 端 DEF-01-008 回退：`config.yaml` 移除 / 設 `minimax.enable_kernel_brain=False`（預設）即恢復 brain=None production 行為，零 breaking change。 |

## v0.02 → v0.03

| 欄位 | 內容 |
|------|------|
| **版本** | v0.02 → v0.03 |
| **日期** | 2026-06-13（凍結來源：AutoSDD_improving_02.md，同日 🔴 人工確認凍結，W1 啟動） |
| **delta 清單** | **新增 ACT-172（Phase Z′，自 ID_REGISTRY next_free=172 起算）— `AUTOCLAUDE_DELEGATED` 觀察態落地**（improving_01 §6 Phase Z 提案 → 本輪正式落地，BRIDGE.md §5 三前置條件全數滿足）：① `tools/fsm_runtime/transition_rules.py` — `_HAPPY_PATH` 新增 key `AUTOCLAUDE_DELEGATED`（出邊 `{IMPLEMENTATION, ESCALATION}`，入口走 runtime enter_* 故不改 IMPLEMENTATION 出邊集）+ `OBSERVATION_STATES` frozenset 新增成員；② `tools/fsm_runtime/fsm_runtime.py` — 新增 `enter_autoclaude_delegated()` / `exit_autoclaude_delegated()`（forced-transition，比照 `enter_memory_consolidation`；done→IMPLEMENTATION / failed→ESCALATION）；③ `tools/fsm_runtime/formal/SDD_FSM.tla` — `ObservationStates` + 入口 action `T_EnterAutoclaudeDelegated` + 出口 `T_AutoDelegToImpl`/`T_AutoDelegToEsc` + `Next` disjunction + Fairness `SF_vars(T_AutoDelegToImpl)`（破 IMPLEMENTATION↔AUTOCLAUDE_DELEGATED 2-cycle，保 EventuallyTerminal）；④ `workflow/sdd-fsm-engine/SDD_FSM_ENGINE.md` 狀態轉換表新增兩出口列；⑤ `tools/fsm_runtime/tests/test_phase_z.py`（8 case 驗 enter/exit/邊界/不變量）。**新增 FSM 狀態：AUTOCLAUDE_DELEGATED（1 個觀測態）**。ID_REGISTRY：Phase Z′ range [172,172] 登記、next_free 推進 act=173 / rule="9.39"（本輪不取新 rule，state 受 R-9.18 既有規則治理）。**整合層（A 軌）DEF-01-004 同輪修**：AutoClaude `ci.yml:189` pg-e2e cron 過濾（落 AutoClaude repo，非 v0.03 本體）。 |
| **TLC 證據** | **五軌 TLC 重跑全綠**（`_HAPPY_PATH` + `SDD_FSM.tla` 變更觸發 Rule 9.18.1 義務）。實測（2026-06-13，`python -m tools.fsm_runtime.tlc_runner --module <各軌>`，exit 0 / No error found）：<br>• **SDD_FSM**：DISTINCT=855 / GENERATED=706 / DEPTH=15<br>• **META_FSM**：DISTINCT=13 / GENERATED=24 / DEPTH=6<br>• **FLEET_FSM**：DISTINCT=7 / GENERATED=8 / DEPTH=7<br>• **COMPOSITION_FSM**：DISTINCT=21 / GENERATED=28 / DEPTH=7<br>• **OPTIMIZATION_FSM**：DISTINCT=12 / GENERATED=21 / DEPTH=5<br>liveness（EventuallyTerminal / ObservationsTransient）+ safety（含 ObservationStates ∩ Terminals = ∅）全過。<br>🔸 raw DISTINCT/GENERATED 為 `tlc_runner` 自報值（SDD_FSM 855/706 經 2026-06-14 親跑重現一致）；惟 `distinct > generated` 違反 TLC 不變量，係 `_grp` re.search 取首個 progress 行非最終 summary 之工具瑕疵（記 **DEF-02-002**，routed v0.0Y），**權威停機判準以穩定且跨 agent 一致的 `No error has been found / 0 violation` 為準**，raw 計數僅供參考。 |
| **回退指引** | AutoClaude 端：`sdd_compile --spec-dir` 與 SddGovernancePlugin 的 `workflow_path` 指回 `AISDLC_SDD_v0.02/`（或更早 v0.01）即完成回退（v0.03 對 AutoClaude 介面零 breaking change——新增的 `AUTOCLAUDE_DELEGATED` 為 SDD 框架內部 FSM 觀測態，AutoClaude 側不依賴其存在）。已知不相容點：v0.02 無 `AUTOCLAUDE_DELEGATED` 狀態與 `enter_autoclaude_delegated` API——回退後若有委派執行語意需求，僅能停留 IMPLEMENTATION（無專屬觀測態追蹤）；既有 FSM-STATE 狀態檔（未含此態）載入 v0.03 完全相容（純加法，舊 decision_trace 不變）。 |

## v0.01 → v0.02

| 欄位 | 內容 |
|------|------|
| **版本** | v0.01 → v0.02 |
| **日期** | 2026-06-12（凍結來源：AutoSDD_improving_01.md，同日 🔴 人工確認凍結） |
| **delta 清單** | **新增 ACT-162~171（Phase Z，自 ID_REGISTRY next_free=162 起算）**：① `workflow/sdd-autoclaude-bridge/SDD_AUTOCLAUDE_BRIDGE.md`（SDD→Playbook 標準作業，ACT-162）② `agent/specialized/sdd-playbook-compiler-zh.yaml`（編譯者角色，ACT-163）③ `governance/rules/R-9.38-playbook-translation-fidelity.yaml`（AT↔step 100% 雙向映射，違反→SPEC_AUDIT，ACT-164）④ 10 場景 SOP 各加「§AutoClaude 自動化執行」小節 + QuickRef 同步（ACT-165~171）。**新增 FSM 狀態：無**（`AUTOCLAUDE_DELEGATED` 維持提案，落地前置條件見 BRIDGE.md §5）。**缺陷修正（自 AutoSDD_Defect_Log 分流）**：DEF-01-001（RULES_INDEX.md 計數 35→39 檔 + next-act/next-rule 前緣同步）、DEF-01-002（run_tlc.sh 補「五軌請走 tlc_runner.py」legacy 註記）、DEF-01-003（`tools/__init__.py` 顯式 package 宣告）。ID_REGISTRY：Phase Z range [162,171] 登記、next_free 推進 act=172 / rule="9.39"。**根層文件計數同步（三專家審查 DEF-01-010）**：`AISDLC_SDD_INIT.md` / `README.md` 等根層文件 agent 計數 25/18 → 26/19 同步（v0.02 +sdd-playbook-compiler） |
| **TLC 證據** | **N/A** — `_HAPPY_PATH` 與全部 `*.tla` 零修改（diff 為空），依 Rule 9.18.1 無重跑義務；五軌 TLC 既有證明維持有效。`AUTOCLAUDE_DELEGATED` 落地時（v0.03+）必附五軌 TLC_DISTINCT/GENERATED/DEPTH 輸出 |
| **回退指引** | AutoClaude 端：`sdd_compile --spec-dir` 與 SddGovernancePlugin 的 `workflow_path` 指回 `AISDLC_SDD_v0.01/` 下的專案 docs 即完成回退（v0.02 對 AutoClaude 介面零 breaking change）。已知不相容點：v0.01 無 R-9.38 與 sdd-autoclaude-bridge workflow——回退後 AT↔step 保真檢查無治理規則承載（僅靠 AutoClaude 側 adapter 測試守護）；v0.01 `RULES_INDEX.md:9` 計數仍為過期值（屬凍結基線既有債務，不回改）。回退驗證：根層 `tools/integration_gate.ps1` 的 **[4/5] 回退驗證段**（`AutoClaude/tests/integration/test_sdd_bridge/test_rollback_compat.py`）——以 subprocess 用 v0.01 / v0.02 **各自的** `tools.fsm_runtime.state_loader` 產出真品 FSM 狀態檔（load_state → record_spec_frozen → save_state），再由 `autoclaude.tools.sdd_compile.compile_spec` 消費並斷言編譯出 3 步驟，證明指回 v0.01 後規格鏈路仍可編譯；[3/5] test_sdd_bridge 煙霧亦須全綠 |
