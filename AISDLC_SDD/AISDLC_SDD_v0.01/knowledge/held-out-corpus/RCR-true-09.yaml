case_id: RCR-true-09
expect: true_recursion
note: a 2-node inter-recursive operator (mul-fold over a rank-descending call graph
  of sq(sum), computing sum^4) rescues a low-magnitude high-quality candidate that
  the non-recursive operator family's existing_cost baseline misses; genuine inter-operator
  recursive interaction needed (call graph carries a well-founded rank measure, decidably
  terminating — NOT expressible by a single non-recursive operator)
candidates:
- real_quality: 0.49
  existing_cost: 2.3
  features:
    rollback_steps: 12.0
    blast_radius: 10.0
    canary_gap: 10.0
- real_quality: 0.92
  existing_cost: 6.0
  features:
    rollback_steps: 2.0
    blast_radius: 2.0
    canary_gap: 2.0
- real_quality: 0.66
  existing_cost: 3.6
  features:
    rollback_steps: 7.0
    blast_radius: 6.0
    canary_gap: 5.0
