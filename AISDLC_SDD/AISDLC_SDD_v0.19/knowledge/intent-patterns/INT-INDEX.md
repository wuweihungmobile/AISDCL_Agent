# Intent Patterns 索引（INT-*）

Phase K M-K1 / ACT-081 — `intent_decomposer` 的意圖分解模式知識庫。

對稱於 `failure-patterns/`(FPL)、`skill-patterns/`(SPL)、`adversarial-patterns/`(ADV)。
存放常見「意圖 → 規格骨架」分解模式 `INT-*.yaml`，由 `intent_decomposer` 於成功分解後
**結晶 proposed 草案**（≥3 次同型分解 → proposed，禁自動 verified，比照 SPL/ADV 治理）。

漸進式揭露：INT 模式經狀態感知 lazy load（`INTENT_DECOMPOSITION` 狀態），不汙染 eager 地圖。

| ID | 意圖類型 | 規格骨架 | 成熟度 |
|----|---------|---------|--------|
| —  | （v1 初始為空，待運行結晶） | — | — |
