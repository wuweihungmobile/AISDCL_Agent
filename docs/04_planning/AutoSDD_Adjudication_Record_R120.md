# AutoSDD 裁決工作坊記錄 — `R120`（DEF-200-212 結案死結一題）

> **日期**：2026-09-02
> **裁決者**：掌舵者——互動選項點選（「一次性擴到 5 格（推薦）」）。
> **效力聲明**：本檔為裁決存證；機械掛號載體＝DEF-200-241（結構根因列）。本檔不改
> 任何歷史文件的字面（`CrossPlatform_R118_Debt_Closure.md` §4b 與 `R118_HANDOFF.md`
> 的「僅 open」敘述在結案後成為過期史料，依「不改寫歷史文件」原則保留原文）。
> **命名說明**：刻意不叫 `R*_HANDOFF`——避開「最新交棒書」鎖族的掃描面；本檔輪號
> 字面住 `.md`，不在程式碼輪號鎖射程內（先例＝`AutoSDD_Adjudication_Record_R110.md`）。
> **編號說明**：本裁決定名 **D8**（接續 2026-09-01 循環令 D1~D7 序）。
> `CrossPlatform_R118_Debt_Closure.md:154` 的暫稱「候選 D5」與循環令既有 D5
> （`R113`/`R114` 守衛線分掛追認）撞號——以本句訂正，舊檔不改。

---

## 1. 裁決總表

| qid | 裁決 | 關鍵理由一句 | 解鎖項 |
|---|---|---|---|
| D8 | 一次性核准 `_CARRIER_DOC_EXEMPTIONS_MAX_ENTRIES` 3→5 ＋ 登記 2 筆豁免鍵（`tools/check_handoff_carriers.py`；即 R118 證據檔 §4b 的候選①）；本例外即時耗用、不得援引為再次調高的先例 | 兩條各自正確的紀律（帳本時鐘凍結 × 豁免面 shrink-only）相乘，把「程式碼已完工」的列卡成結構上不可結案；候選②＝永久掛帳違反清償目標、候選③＝改判準語意等於退回 lenient，皆更差 | DEF-200-212 得以結案；結構根因另立 DEF-200-241（治本方向二擇一：時鐘前進機制／祖父化改讀結案事實——過四方複審前不動碼） |

## 2. 連鎖影響（裁決當回合以唯讀模擬證實，非推測）

- 212 轉 fixed 會使 commit `0398226` 訊息的「已列 `R118` 交棒書呈報裁決」失去帳本
  承載（判準①無豁免面，pre-push 快層會擋）——由 DEF-200-241 狀態欄具名輪次補位。
  終態模擬（212 fixed ＋ 241 open ＋ 2 豁免鍵）：`check_handoff_carriers.py` 與
  `check_defect_log_crossref.py` 兩閘門 main() 皆 rc=0，五筆豁免逐筆承重（各自拿掉
  皆復發）。
- 豁免面 shrink-only 原則在 3→5 之後**繼續有效**：鎖測試上限字面同步重釘為 5，
  語意仍為只准調小。

## 3. 體例自查

- 本檔全文不使用裸「延後／承接／交棒／交由／交給／移交／留＋輪號」語式；提及輪次
  一律反引號包或用檔名字面。
- 機械掛號＝載體 DEF-200-241；本檔數字皆轉錄自當回合唯讀模擬與偵察實測，不新增
  量測、不得被當常數引用。
