# AutoSDD_improving_112（R95）— PRD v2.1 系統本體開發輪：修憲 ×2 ＋ 攤提窗尾修正 ＋ 喚醒鏈三層閉環

> **軌道①**（AISDLC-SDD × AutoClaude 深度整合）。本輪三柱分佈：
> **C 柱（指揮官 AutoClaude）** 全占——掌舵者開場直接質疑「為何都不全力專注 PRD 開發」，
> 本輪回到 AutoClaude 核心排程與 Token 治理系統本體；**A 柱／B 柱** 無。
>
> 🔴 本輪兩度被真實撞線事故打斷（08-16 深夜收尾包、08-17 午後修復包），
> 兩次事故本身成為本輪最有價值的輸入：喚醒鏈 root cause（ADR-XPLAT-004 §2.9）
> 與「主控閒置盲區」立案（見 §4 下輪）皆源自事故現場取證。

## §0 開場盤點（發現波，2 agent 唯讀）

PRD v2.1 逐節對照實作：收斂度估計 **65%**（盤點 agent 交件，證據檔級座標見其回報）。
Plugin 體系判定：**活著**——19 plugin 註冊、Kernel 27 phase 全走 EventBus、importlinter 9 kept。
「每輪瘦身」歸因：史料佔護欄檔 25~66% 行數＋棘輪是刻意設計；
AutoClaude 包總量 cap 餘裕僅 22 LOC ⇒「搬進包內」此路不通（重校須 ADR-SD07-001 §6.3 雙簽）。

## §1 本輪交付（全數經測試憑證，逐字輸出住各證據檔）

| 項 | 內容 | 憑證出處 |
|---|---|---|
| 修憲 v2.1.4 | 遙測 T5（/api/oauth/usage）升格認可主源＋紅線 1 收窄豁免四條件＋§12 憑證劃界（掌舵者 08-16 拍板） | PRD 修訂表；PrdDrainPercentMapsToTheBandsTest 3 passed |
| 修憲 v2.1.5 | §4.5.6 撞線零人工喚醒六規範＋驗收 A1~A5（掌舵者 08-17 立案） | PRD 修訂表＋ADR-XPLAT-004 §2.9 |
| 攤提窗尾修正 | `quota_pace.amort_relaxed`：長窗自軸未達 converge 錨點 ⇒ 出聲不收緊（掌舵者 08-16 當面立案「窗尾額度作廢不算節省」）；零改既有方向鎖斷言 | CrossPlatform_R95_Pace_Actuator_Evidence.md；本輪內已兩度於真實 --pace 輸出驗證生效 |
| 模型降級建議＋pace_index | `Decision.model_hint`（建構順序保證不影響 cap）＋PRD §4.2.8 比值原式＋`AUTOSDD_QUOTA_PACE_CEILING` | 同上；本輪首次真實遵循（E501 折行包用 sonnet） |
| 治理檔禁寫（紅線 10） | block_destructive_git.py GOV_TOOLS×保護面×AUTOSDD_UNATTENDED 判準；QA 探針補 `.env`（可注入 GUARD_OFF 族逃生口的繞道）；R87 事故（13 agent 全滅）的機械物終於落地 | CrossPlatform_R95_GovWrite_Evidence.md 九格 rc 矩陣 |
| resume 三態選路 | `choose_resume_route`：SESSION_RESUME／FRESH_SESSION_WITH_STATE（32MiB 上限＝1108 支逐字稿實測分佈定值）／REFUSE（任務書缺席拒武裝） | CrossPlatform_R95_Resume_Strategy_Evidence.md |
| 哨兵存活四修 | 修1 任務書覆寫保 RELAY（08-16 事故直接根因）；修2 自癒後才解除＋必出聲（複審補 read_text 第四分形）；修3 活性欄（**上線當日實戰抓到哨兵死亡**）；修4 halt 多軸最早 reset（halt_resets_at） | 同上 §L-4；A1~A5 全達成 |
| 收尾 | 護欄棘輪兩段式重釘：收尾包 84406→84362（−44）、複審修復對價後終值 **84399（整輪淨 −7）**、連升 streak 歸零；帳本推進 R95（DEF-200-141~146、148）；CLAUDE.md 守衛總表補治理面 matcher；ENV_SPEC＋.env.example | CrossPlatform_R95_Guard_Repin_Evidence.md §F |

## §2 四方審查閉環

Architect／SD／QA 三方 REJECT（各 1 major，互不重複且全為真問題）＋SA APPROVE：
ADR §2.9 stale 宣稱、read_text fail-quiet 形態（Windows 一次性觸發器不重排）、`.env` 保護面繞道（探針實證）。
修復包全修＋高共識 minor 批次（保護面補喚醒鏈同族檔、env 病態值退預設出聲、
證據檔 rc 抄錄訂正、PRD 殘留措辭註記）。複審驗收見帳本 R95 列。

## §3 事故雙生：本輪的兩次撞線都轉化為機械物

- **08-16 深夜**：halt 覆寫任務書砸 RELAY → 哨兵自我解除 → reset 零排程 → 空轉 8 小時。
  root cause 全鏈取證住 ADR-XPLAT-004 §2.9（哨兵當晚有武裝且巡邏 10 次，死因是狀態塊被砸——
  「mac 側從未成功」的印象被證據駁回，真相是「成功武裝、被自己人殺死」）。
- **08-17 午後**：修復包撞線，主控收到通知但無水位預警機制 ⇒ 立案「主控閒置盲區」（§4）。

## §4 下輪（R96）開場即辦

1. **水位預警哨兵擴充＋PRD v2.1.6 落款**（掌舵者 08-17 兩度立案）：哨兵巡邏（零 token 讀快取）加
   「usage ≥ prepare 錨點且有活躍背景工作 ⇒ 主動武裝喚醒排程＋寫收斂任務書」職責——
   堵「主控閒置等背景 agent 時 hook 全睡、水位無人量」的結構盲區。
2. Windows 側待驗清單（帳本承接列）：govwrite 九格矩陣重跑＋大小寫繞行探針＋修3/修4 schtasks 取證。
3. 引擎側額度軸接電（第三方刷新者）；前置＝AutoClaude 總量 cap 重校雙簽程序。
4. SD/Arch 順手項：prepare 閂鎖鍵、混軸鍵補記來源軸。

## §5 誠實劃界

- 本輪全程 mac 開發驗證，Windows 零等價驗證（§4-2 承接）。
- PRD 收斂度 65%→估 72~75%（P1 五項全落地），精確值下輪開場重盤。
- M2/M3/M4 成熟度判準本輪未量測（與既有基線一致）。

## §6 R113 結構性長債分軌（寄居附記）

<!-- guard-total:R113 --> R113 護欄層累積淨額＝ 89452 → 89910（+458）——結構性長債軌落地 +140（`AutoSDD_Structural_Debt_Log.md` 建軌＋主帳本 7 列遷軌＋`TestStructuralDebtLog` 九支＋外部軌真檔測試拆日期引信＋姊妹帳本擴面）＋ v2.1.13 G1 實作批 (a) 同輪追加 +141（`UnattendedPermissionPostureTest` V-a 六格＋鎖檔自身編修）＋ v2.1.13 G2 實作批 (b) 同輪追加 +177（`HandbackVisibilityTest`＋`HandbackSessionStartAnnounceTest` V-b 六格＋鎖檔自身編修）；逐項見 `CrossPlatform_R106_Scan_Findings.md` 的 R113 標記行與 `_GUARD_LINES_REPIN_LOG` 的 R113 三列。裁決存證＝`AutoSDD_TechDebt_Paydown_Playbook.md` §6 第 3 條（掌舵者 2026-08-30 核准）；G1 批施工圖＝`PRD_Amendment_R113_WakeChain_LastMile.md` §3(a)、G2 批 (b) 施工圖＝同檔 §3(b)（皆 2026-08-31 落款）。

## §7 R115 收斂棒（寄居附記）

<!-- guard-total:R115 --> R115 護欄層累積淨額＝ 90351 → 90344（-7＝收斂棒 -11＋收輪補釘 +10＋補釘二 -6）——收斂棒：三個修復棒（棒A／棒B／治理批）累積漂移的一次性合法收束，兌現款(11)「必須出現一次淨額 ≤ 0」（終止 R113(+458)／R114(+441) 連兩輪上升 streak）並同輪兌現款(12) 到期義務（cap 585→577，重新武裝 117／570）。手法＝類級 docstring 沿革搬遷（三檔合計 -930，全文搬至 `CrossPlatform_Guard_Line_History.md`〈R115 追加〉節，程式碼內只留指標，斷言／判準常數／測試邏輯零改動）抵銷功能面漂移 +777（`test_block_destructive_git_r83.py` DEF-200-238 修復＋`test_context_budget_guard.py` R115 修復 F1~F4／DEF-200-239／v2.1.13 C5）；另補回三支測試類各一句真實 `tools/lib/`／`tools/probe/` 路徑指標（+6），修復分桶棘輪 `prose` 桶因 collapse 副作用滑落排他歸屬（4182→4293）的誤判，回落至 4068；全套實跑另揪出全部指標行違反 `tools/ruff.toml` E501 上限（存量債棘輪 139→252），修法＝逐行拆成兩行（三檔合計 +111）；同批順手補上 DEF-200-239 現查測試自身兩處存量鎖（`_ps_engine` SSOT／subprocess encoding，各 +3）與收尾時再揪出的兩處殘留 E501（各改行歸零）＋本檔自身 +22。逐項見 `CrossPlatform_R106_Scan_Findings.md` 的 R115 標記行與 `_GUARD_LINES_REPIN_LOG` 的 R115 列。

R115 收輪補釘：90340 → 90350（+10，含 skip 行 EAW 寬度折行 +1）——雲端首紅修復（假後端 credential_key 對齊當平台＋DEF-200-239 skip 標籤 +2）＋稽核列與接鏈列自身（+7）；同輪合併 -2 仍 ≤0。逐項見 `CrossPlatform_R106_Scan_Findings.md` 的 R115 補釘標記行。

## §8 R129 護欄層重釘（寄居附記；收尾單人窗口）

<!-- guard-total:R129 --> R129 護欄層累積淨額＝ 92268 → 93610（+1342）——喚醒鏈零浪費（INV1~5／FIX1~4）＋T-f4b 洩漏止血四個包新增 +1300 行回歸鎖：`test_context_budget_guard.py 9860→11057（+1197）`＋`test_mac_endurance_r83.py 1784→1887（+103）`＋本檔自身重釘（`test_adr_xplat001_c1c2_lock.py 7298→7340`）。淨額走 DEF-200-208 一次性例外名冊（`_REPIN_APPROVED_ROUND_OVERAGE` 的 R129 那一列，主控本 session 核准，精確淨額逐字對上，名冊上限同步 1→2）；同輪兌現到期義務 `(129,552)`（cap 555→552）並重新武裝 131／550；款(11) 因前一輪 R127 淨額 −38 已斷 streak 而未觸發。Phase 2 §6 時效（越過到期輪 R127、`[維持觀察]` 名額由 R122 用罄）記一列 `[提案]`，四方複審由主控承接。逐項見 `docs/06_quality/CrossPlatform_R129_Scan_Findings.md` 與 `_GUARD_LINES_REPIN_LOG` 的 R129 列。此附記為收尾窗口為滿足 doc-total 對帳（≥2 站點）而寄居於本 improving 檔，同 §6／§7 的 R113／R115 寄居附記體例；R129 屬跨平台整合輪 R 系列、非本 improving 迭代序。
<!-- guard-total:R130 --> R130 護欄層累積淨額＝ 93610 → 93734（+124）——M-01 喚醒 tick 自標無人（規則 1 死碼修復）＋M-05 followup 接線端到端測試：`test_context_budget_guard.py 11057→11169（+112）`＋本檔自身重釘（`test_adr_xplat001_c1c2_lock.py 7340→7352`，含 U9 root-tools 舊尺債具名展延 130→132、鐵律七獨立重構）。淨額 124 ≤ cap 552（`net_cap_for_round(130)`）；連升 streak 第 1／2（前輪 R129 為 approved-overage 不計）。逐項見 `docs/06_quality/CrossPlatform_R130_Scan_Findings.md`。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R113／R115／R129 寄居體例；R130 屬跨平台整合輪 R 系列、非本 improving 迭代序。round-label-ok
<!-- guard-total:R131 --> R131 護欄層累積淨額＝ 93734 → 94454（+720）——喚醒鏈四方審計與三大問題最相關的一批發現經多位小幫手對抗查證，其中確認仍是破洞的一批（M-03/M-06/M-07/M-13/M-15/M-16/M-20）本輪修復並經主控獨立重跑驗證：`test_context_budget_guard.py 11169→11628（+459）`＋`test_run_root_unittests.py 2428→2451（+23）`＋本檔自身重釘（`test_adr_xplat001_c1c2_lock.py 7352→7373`），淨額 503 ≤ cap 550（到期輪兌現）；同輪再加喚醒鏈規則6崩潰根因修復（write_relay 任務書消失未捕捉例外炸穿無人巡邏行程）的回歸鎖 +210，及對抗式複審發現的收尾修復（rearm／sentinel_rearmed 分支不清 latch／不 loud alert）回歸鎖 +99（合計 +309，全額歸回歸鎖軌，`_REGRESSION_LANE_LOG` 同輪列，貼齊上限 309、不計入 cap 550）；三度收尾（掌舵者 2026-09-07 裁決拆除 INV2／INV3，`test_context_budget_guard.py 11830→11718（−112）`＋本檔自身棘輪 `test_adr_xplat001_c1c2_lock.py 7414→7432（+18）`）**−94**，其中 −112 ＝本批實刪 −179 ＋吸收「規則1／4 空字串繞過修補」尚未 commit 的 +67（HEAD 自身與釘選值一致，非 HEAD 漂移），累積 720。連升 streak 第 2／2（前輪 R130 為第 1／2，R129 為 approved-overage 不計）；同輪重新武裝下一段到期義務 133／549。逐項見 `docs/06_quality/CrossPlatform_R131_Scan_Findings.md`。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R113／R115／R129／R130 寄居體例；R131 屬跨平台整合輪 R 系列、非本 improving 迭代序。round-label-ok
<!-- guard-total:R132 --> R132 護欄層累積淨額＝ 94548 → 94834（+286）——收尾單人窗口 guard-line 記帳重釘：規則2/3分階段fan-out限制拆除／規則7 leak_fence決定性判死／規則8無值宣稱偵測／規則5補macOS真launchd串接測試四項修復回歸鎖首次入帳（`test_claim_provenance_r86.py 618→806（+188）`＋`test_context_budget_guard.py 11830→11861（+31）`＋`test_run_root_unittests.py 2451→2492（+41）`）＋本檔自身逐檔漂移（新增兩筆稽核列本身＋`_FROZEN_PREFIX_REWRITE_LEDGER`接鏈列＋U9到期輪展延註記）+26。淨額 286 全額歸回歸鎖軌（`_REGRESSION_LANE_LOG` R132 兩列合計 286，母項扣除後淨額為 0，未逾 `net_cap_for_round(132)=550`），連升 streak（R130／R131 已 2／2）本輪歸零；另起 R132 而非續記 R131 係因 R131 的 cap(550)／lane(309) 兩額度已由既有列用盡（詳 `docs/06_quality/CrossPlatform_R131_Scan_Findings.md` §5），非開新一輪迭代四件套。同輪具名展延 U9 root-tools 舊尺技術債到期輪 132→133（非重構持有面）。逐項見同檔 §5 與 `_GUARD_LINES_REPIN_LOG` 的 R132 列。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R113／R115／R129／R130／R131 寄居體例；R132 屬跨平台整合輪 R 系列 guard-line 記帳延伸、非本 improving 迭代序。round-label-ok
<!-- guard-total:R133 --> R133 護欄層累積淨額＝ 94834 → 94902（+68）——DEF-200-274（根層 `tools/run_root_unittests.py` 本機平行執行 opt-in，`AUTOSDD_PARALLEL_TESTS=1`）落地：新增 `tools/lib/parallel_shard.py`（非鎖檔，不進逐檔行數表）與回歸冒煙測試 `ParallelShardMergeSmokeTest`（AC8）：`test_run_root_unittests.py 2492→2540（+48）`＋`test_subprocess_encoding_hygiene.py 1599→1603（+4，`tools` 樹掃描檔數下限 131→156 重釘註記）`＋本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +16（新增兩筆稽核列＋凍結前綴延伸 124→126＋`_REPIN_LOG_HISTORY_SHA256` 重釘＋`_REPIN_NET_CAP_SCHEDULE` 到期義務兌現列 `(133, 549)`＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列）。淨額 68 遠低於 cap 549（到期輪兌現，cap 降到到期目標本身，同既有判例）。逐項見 `docs/06_quality/CrossPlatform_R131_Scan_Findings.md` §6 與 `_GUARD_LINES_REPIN_LOG` 的 R133 列；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129／R130／R131／R132 寄居體例；R133 本輪僅為單一功能落地附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok
<!-- guard-total:R134 --> R134 護欄層累積淨額＝ 94902 → 95520（+618）——DEF-200-274 收尾複審修復（第一批，+155）：`sentinel_lifecycle.leak_fence()` 的 shard 崩潰繞過修復（`parallel_shard.run_parallel()` 崩潰時改回傳失敗態 `_MergedResult` 而非 `raise SystemExit`，讓 `leak_fence()` 的 `rc = run()` 完整執行、收尾快照與洩漏比對不再被穿透）之回歸鎖：`test_run_root_unittests.py 2540→2679（+139）`（`ParallelShardCrashDoesNotRaiseTest`＋`ParallelShardCrashLeakFenceIntegrationTest` 兩支）＋本檔自身逐檔漂移 +16。續記第三輪對抗式複審收斂批（+463，同輪延續、非另立 R135：另立會觸發 Phase 2 §6 時效到期輪 R134、U9 root-tools 舊尺技術債到期輪 R135、`_REPIN_NET_CAP_DUE_ROUND=135` 三項無關到期義務同時逾期，故沿用 R134）：stderr backpressure 修復（`run_parallel()` 改執行緒併發 `communicate()`，不再依序阻塞排空 pipe）、`run_parallel()` 例外安全網（`try/except` 包住 Popen 迴圈與 `merge_results()`，含 Popen 失敗時清理已啟動的孤兒行程）、與不 mock `subprocess.Popen` 的真實協定通道整合測試：`test_run_root_unittests.py 2679→3116（+437）`（`ParallelShardStderrBackpressureRegressionTest`／`ParallelShardMergeResultsMissingKeyStillRaisesTest`／`ParallelShardRunParallelExceptionSafetyTest`／`ParallelShardMergeExceptionLeakFenceIntegrationTest`／`ParallelShardPopenFailureKillsAlreadyStartedProcsTest`／`ParallelShardRealSubprocessProtocolIntegrationTest`，其中最後一支 +158 全額歸回歸鎖軌 `_REGRESSION_LANE_LOG` 同輪列）＋本檔自身逐檔漂移 +26（新增三筆稽核列＋兩輪收斂＋凍結前綴延伸 132→133＋`_REPIN_LOG_HISTORY_SHA256` 重釘＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列）。合計淨額 618，扣回歸鎖軌 158 後 460 ≤ cap 549（未到期）。逐項見 `docs/06_quality/CrossPlatform_R131_Scan_Findings.md` §7～§8 與 `_GUARD_LINES_REPIN_LOG` 的 R134 列；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274，詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R133 寄居體例；R134 本輪僅為單一缺陷收尾複審附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R135 --> R135 護欄層累積淨額＝ 95520 → 95762（+242，全額歸回歸鎖軌）——DEF-200-274 第四輪四方複審收尾：`test_run_root_unittests.py` +147（work-stealing 動態派工重構回歸測試 +92，對抗式複審收斂追加的 `ParallelShardMultipleThreadFailuresAreAllReportedTest`＋`_FakeProc` fixture 修正 +55）、`test_platform_neutral_paths.py` +27（QA 阻斷條件要求的 TOCTOU 緩解）、本檔自身逐檔漂移 +68（含回歸鎖軌／到期義務五處新增）。同輪兌現 `_REPIN_NET_CAP_SCHEDULE` 到期義務（cap 549→548，`_REPIN_NET_CAP_DUE_ROUND=135`），同輪重新武裝下一段（`_REPIN_NET_CAP_DUE_ROUND=137`／`_REPIN_NET_CAP_DUE_TARGET=547`）；`_ROOT_TOOLS_OLD_SCALE_DEBT_DUE_ROUND` 具名展延 135→137（真拆屬獨立重構持有面，非本輪主軸）；`_PHASE2_REVIEW_LOG` 追加 `(135, [維持觀察], ...)`（未觸碰 ADR-XPLAT-013 方向 (c) 提案，同輪重新武裝下一個視窗）。逐項見 `docs/06_quality/CrossPlatform_R135_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274／DEF-200-273／DEF-200-275；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第四輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R134 寄居體例；R135 本輪僅為單一缺陷收尾複審附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R136 --> R136 護欄層累積淨額＝ 95762 → 95843（+81，全額歸回歸鎖軌）——DEF-200-274 第四輪第二次對抗式複審收斂（Architect＝REJECT、SA＝APPROVE_WITH_CONDITIONS、SD＝APPROVE、QA＝未回報）：`test_run_root_unittests.py` +61（新增回歸測試 `ParallelShardWorkerThreadBaseExceptionDoesNotEscapeTest`，涵蓋 Architect finding 1(a)：worker thread 內部拋出非 `Exception` 子類 `BaseException` 時 `run_parallel()` 不得讓它穿透；`LoadBalancingRegressionTest`／`ParallelShardStderrBackpressureRegressionTest` docstring 訂正）、本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +20（新增稽核列＋回歸鎖軌新列＋前綴重寫接鏈列本身的行數）。另起 R136 而非續記 R135，係因 R135 的 `_REGRESSION_LANE_LOG` 額度（cap 309）已由既有四列（119+51+55+17=242）用掉大半，本輪 81 若續記會使該輪 lane 合計達 323、超過上限。同輪亦修復：`tools/lib/parallel_shard.py` 抽出 `_crash_fallback()` 共用函式收斂 BaseException 型別缺口（Architect finding 1(a)，不進逐檔行數表）；模組層 docstring 改寫成與 `run_parallel()` 一致的動態工作竊取敘述（SA finding）；`MIN_TESTS` 由 4042 重釘為 4045（SA finding，`ONBOARDING.md` §7 已同步）；新開缺陷帳本 `DEF-200-276`（SA finding：pre-commit／pre-push venv 偵測修法缺具名理由）。逐項見 `docs/06_quality/CrossPlatform_R136_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274／DEF-200-276；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第四輪第二次對抗式複審〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R135 寄居體例；R136 本輪僅為單一缺陷第二次複審收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R137 --> R137 護欄層累積淨額＝ 95843 → 95925（+82）——DEF-200-274 第五輪四方獨立複審（Architect/SA/SD/QA）收尾：全庫排查同一 TOCTOU 病灶（平行測試 worker 寫入 `tools/tests/` 的合成暫存模組，與治理掃描器的檔案列舉／讀取競態）；四方複審僅點名一個呼叫點，收尾單人窗口修復時逐一核對整棵 `tools/tests/` 樹，另在多支姊妹檔各找到同型缺口並一併補上排除，逐檔清單見findings 檔；`tools/lib/parallel_shard.py` 同輪補齊 Ctrl-C 孤兒行程清理（drain pending＋kill 已啟動子行程＋等 thread 真的結束才 re-raise）與 per-module wall-clock 計時觀測（`run_root_unittests.py::report_module_timings()` 消費）；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +18（新增稽核列＋回歸鎖淨額上限到期義務兌現列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列本身的行數）。同輪兌現 `_REPIN_NET_CAP_SCHEDULE` 到期義務（cap 548→547，`_REPIN_NET_CAP_DUE_ROUND=137`），同輪重新武裝下一段（`_REPIN_NET_CAP_DUE_ROUND=139`／`_REPIN_NET_CAP_DUE_TARGET=546`）；`_ROOT_TOOLS_OLD_SCALE_DEBT_DUE_ROUND` 具名展延 137→142（真拆屬獨立重構持有面，非本輪主軸）。逐項見 `docs/06_quality/CrossPlatform_R137_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第五輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R136 寄居體例；R137 本輪僅為單一缺陷第五輪四方獨立複審收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R138 --> R138 護欄層累積淨額＝ 95925 → 95942（+17）——DEF-200-274 第六輪：掌舵者直接提問「多CPU測試頭重腳輕」，實測定位真正離群值是單一測試方法（`test_doc_loc_baseline_freshness_r60.TestR67R3ThisFileMakesNoUnstatedPlatformAssumption.test_every_lock_in_this_file_holds_under_every_simulated_platform` 佔全模組 157.52s 的 118.49s／≈75%），新增 `tools/lib/dispatch_granularity.py`（平行派工鍵改採白名單模組 (module, class) 細分，其餘模組維持整模組派工，`parallel_shard.py` worker 協定零改動）；白名單納入兩檔（`test_doc_loc_baseline_freshness_r60`／`test_archive_defect_log`，後者是修完第一熱點後浮現的新單一最重模組 224.5s）。端到端重驗時親自複現並修復第五輪排查漏掉的第 11 個同型 TOCTOU 站點：`test_platform_utils_dedup.py 1104→1112（+8）`，補 `_zzz_` 合成暫存模組排除；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +9（新增稽核列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列本身的行數）。4-worker 全套 wall-clock 由第三輪誠實揭露的「幾乎零加速比」基準（序列 572s／平行 570s）改善到約 274s（兩次獨立重跑一致，rc=0，M6 集合關係皆成立）。逐項見 `docs/06_quality/CrossPlatform_R138_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第六輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R137 寄居體例；R138 本輪僅為單一缺陷第六輪收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R139 --> R139 護欄層累積淨額＝ 95942 → 96117（+175，全額歸回歸鎖軌）——DEF-200-274 第六輪四方獨立複審（Architect/SA/SD/QA，各自不共享上下文分別跑）共同點名：`tools/lib/dispatch_granularity.py` 落地時零測試覆蓋，且其 docstring 宣稱「由 `test_run_root_unittests.py` 回歸鎖看守」查無實據——補上 `DispatchGranularityDispatchKeyTest`／`DispatchGranularityPlaceholderConstantStaysInSyncTest`／`DispatchGranularityWhitelistHasNoModuleLevelFixturesTest` 三個測試類別（`test_run_root_unittests.py` 3335→3486，+151，全額歸回歸鎖軌），docstring 訂正為據實描述；另修復證據帳本斷鏈（`CrossPlatform_DEF200274_Parallel_Tests_Evidence.md` 補〈第六輪〉章節）與主缺陷帳本 `AutoSDD_Defect_Log.md` DEF-200-274 列未同步兩項缺口；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +23（新增稽核列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列＋`_REGRESSION_LANE_LOG` 新列本身＋`_REPIN_NET_CAP_SCHEDULE` 到期義務兌現列）。同輪兌現 `_REPIN_NET_CAP_SCHEDULE` 到期義務（cap 547→546，`_REPIN_NET_CAP_DUE_ROUND=139`），同輪重新武裝下一段（`_REPIN_NET_CAP_DUE_ROUND=141`／`_REPIN_NET_CAP_DUE_TARGET=545`）。四方三題裁決一致：`VERDICT_LEDGER_RESOLVED=partial`／`VERDICT_FEATURE_COMPLETE=partial`／`VERDICT_LOAD_BALANCED=partial`（皆因 Windows 真機驗證未解，非本輪範圍）；QA 親測序列 794.00s vs 平行 4-worker ≈300s，加速比 ≈2.6x，推翻第三輪「幾乎零加速比」舊描述。逐項見 `docs/06_quality/CrossPlatform_R139_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第六輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R138 寄居體例；R139 本輪僅為單一缺陷第六輪四方複審收斂附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R140 --> R140 護欄層累積淨額＝ 96117 → 96240（+123，全額歸回歸鎖軌）——DEF-200-274 第七輪：掌舵者要求「設計自動偵測機制」取代第六輪〈誠實劃界〉記載的「靠人眼發現新熱點」，新增 `tools/lib/dispatch_imbalance.py`（純函式 `detect_imbalance()` 算出每個派工單位相對公平均分基準的倍率，超過門檻即回傳；`report_dispatch_imbalance()` 印出警告與建議，接線進 `run_root_unittests.py::run_with_floor()`）；`test_run_root_unittests.py` 3486→3570（+84，`DispatchImbalanceDetectionTest`／`ReportDispatchImbalanceTest` 兩個測試類別）＋3570→3587（+17，補齊下方缺陷回歸測試）；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +22。四方獨立複審（Architect/SA/SD/QA，各自不共享上下文分別跑）三題裁決：`VERDICT_LEDGER_RESOLVED`＝Architect partial／SA open（文件斷鏈已修復）；`VERDICT_FEATURE_COMPLETE=partial`（四方一致）；`VERDICT_LOAD_BALANCED`＝Architect/SA partial-open／QA fixed（親測全套 4062 支測試零誤報）。共同點名並已修復的真缺陷：`fair_share` 分母誤用未經 cap 的名目 `worker_count`（SD 合成場景實測證實：派工單位數 ≤ worker_count 時，即使完全均衡的資料也會被全數誤判為不均），改為 `min(worker_count, len(module_timings))` 並補回歸測試釘住。誠實記載本輪未達成的部分：這僅是「自動化了計算，沒有自動化觸發」——CI／pre-push 皆未在平行模式下執行 `tools/run_root_unittests.py`，偵測結果只有手動 opt-in 的開發者本機看得到，不算真正達成使用者要求的「不必再靠人眼」；`AutoClaude/tests/`／`AISDLC_SDD` 的 pytest 套件仍不受本機制惠及；Windows 真機驗證仍未解（掌舵者已表示會自行驗證）。逐項見 `docs/06_quality/CrossPlatform_R140_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第七輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R139 寄居體例；R140 本輪僅為單一缺陷第七輪四方複審收斂附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R141 --> R141 護欄層累積淨額＝ 96240 → 96451（+211，全額歸回歸鎖軌）——DEF-200-274 第八輪：commit 9478bda push 後四方（Architect/SA/SD/QA，各自不共享上下文分別跑）獨立審查第七輪產出：CI 真實結果為 windows-compat-ci／macos-compat-ci 皆 success（windows 首次真機驗證成功），root-infra-ci failure（根因＝`tools/run_root_unittests.py` special-tier LOC 棘輪超額 13 行）；SD／QA 共同點名 `worker_count()` 公式／環境變數覆寫／非法值退回三條路徑此前全數靠 mock 換掉函式本體、零直接覆蓋；SA 點名 CI 已接 `AUTOSDD_PARALLEL_TESTS=1` 卻零測試覆蓋，且兩支本機 nightly 腳本未同步接線；SA 並提出、本輪落地 GitHub Actions `::warning::` annotation。修復：special-tier 棘輪壓縮後仍超額 2 行、確認無法再壓縮而具名調高 775→777；新增 `WorkerCountFormulaTest`（`test_run_root_unittests.py` +129，含 `ReportDispatchImbalanceTest` 兩支 annotation 回歸鎖）；新增 `TestParallelTestsCiWiring`（`test_smoke_ci_sync.py` +46，新增 `import yaml` 使該檔納入 `min_tests_margin.PREREQ_DEPENDENT_MODULES`）；`run_local_nightly.sh`／`run_local_nightly.ps1` 補上 `AUTOSDD_PARALLEL_TESTS=1`；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +36。同輪兌現 `_REPIN_NET_CAP_SCHEDULE` 到期義務（cap 546→545，`_REPIN_NET_CAP_DUE_ROUND=141`），同輪重新武裝下一段（`_REPIN_NET_CAP_DUE_ROUND=143`／`_REPIN_NET_CAP_DUE_TARGET=544`）；`_PHASE2_REVIEW_LOG` 追加 R141 `[提案]` 列——🔴 誠實記載：該列不對 ADR-XPLAT-013 (c) 方向做任何新判斷，純因本輪把稽核痕跡機械推進到 R141、越過條文五 §6 的到期輪且『維持觀察』名額（R135）已用罄而觸發，R129 既存提案迄今仍待主控排定四方複審。誠實劃界：不可宣稱本輪修復後 CI 會轉綠——special-tier LOC 修復尚未經過新的 CI run 驗證。逐項見 `docs/06_quality/CrossPlatform_R141_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第八輪〉節。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R140 寄居體例；R141 本輪僅為單一缺陷第八輪四方獨立複審收斂附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R142 --> R142 護欄層累積淨額＝ 96451 → 96506（+55，全額歸回歸鎖軌）——DEF-200-275／DEF-200-277 收尾：主控（收尾單人窗口）確認 commit f61cb45 push 成功後，比對雲端 CI 發現 root-infra-ci 失敗，人工定位真因＝`test_doc_loc_baseline_freshness_r60.py` 的跨平台假設 meta-test 模擬 `sys.platform='darwin'` 時撞見 CPython `sysconfig` 行程級快取首次暖機時機，組出不存在的 sysconfigdata 模組名炸 `ModuleNotFoundError`（CI ubuntu 實測；本機 Mac 因 venv 已暖機測不出）；修法：迴圈前先呼叫 `sysconfig.get_config_vars()` 暖機真實平台快取，新增 `TestDEF200277SysconfigWarmedBeforePlatformSimulation` 源碼順序回歸鎖（production+test +30）；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +25（新增主表本列＋本軌新列的行數）。同軸另完成 DEF-200-275（SDD-FSM context/budget 計量表誤報）三輪修復——四方獨立複審第一輪 2 REJECT（190000~200000 誤鎖 ESCALATION）、第二輪 3 APPROVE／1 REJECT（QA/SA 各自獨立發現 `_SDD_MAX_CONTEXT_PINNED` 只問環境變數是否存在、未驗解析成功，操作者打錯字仍會誤鎖）、第三輪全數收斂，該部分行數落 AISDLC_SDD 子專案自身 LOC 分級、不計本檔淨額。誠實劃界：本輪為兩個獨立缺陷收尾附帶的 guard-line 記帳延伸，非開新一輪 CrossPlatform 掃描輪四件套；`_ROOT_TOOLS_OLD_SCALE_DEBT_DUE_ROUND` 具名展延 142→147（見該常數旁理由），真拆仍待獨立重構持有面窗口（鐵律七）。逐項見 `docs/06_quality/CrossPlatform_R142_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-275／DEF-200-277；詳細證據見 `docs/06_quality/CrossPlatform_DEF200275_Context_Metering_Evidence.md`〈第一~三輪〉節與 `docs/06_quality/CrossPlatform_DEF200277_Sysconfig_Platform_Sim_Evidence.md`。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R141 寄居體例；R142 本輪僅為兩個獨立缺陷收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R143 --> R143 護欄層累積淨額＝ 96506 → 97056（+550＝+238 收尾 ＋ +73 第 1 輪審查修復同輪追加 ＋ +312 DEF-200-278 同輪三度追加，全額歸回歸鎖軌）——DEF-200-275 第四輪（分子改讀本 session 逐字稿 API usage；≥95% 改 session 級拒絕非 compact 工具、不寫專案級 ESCALATION；AUTO_COMPACT_PENDING 於 usage 回落 <85% 自動出口；一行人工恢復指令）收尾：新增 `tools/tests/test_context_window_parity.py`（+145，根層 context_budget_guard ↔ SDD context_window parity 鎖）＋`test_adr_xplat001_c1c2_lock.py` 自身漂移 +17＋`test_platform_neutral_paths.py` +3（`_DIRENT_UNGUARDED_DEBT` 42→40 下修註解）；同輪追加（四方第 1 輪審查修復 SA-R4-03）：`tools/tests/test_find_git_bash_parity.py` 1266→1326（+60，整合閘門候選鏈 python3／.venv 退路／全無 fail-loud 三案，bug-injection 自證 2 紅）＋`test_adr_xplat001_c1c2_lock.py` 自身漂移 +13（主表同輪追加列＋回歸鎖軌列＋前綴鏈列）。同輪三度追加（DEF-200-278 喚醒鏈缺口收尾）：新增 `tools/tests/test_wake_chain_halt_r278.py`（+299，quota_gate halt 交棒 fallback／halt 標記讀寫／halt_verdict／sentinel_decide halt 分支／check_claim_provenance 詞表擴充的回歸鎖）＋`test_adr_xplat001_c1c2_lock.py` 自身漂移 +13（主表本列＋本軌列本身＋凍結前綴延伸 152→153）。逐檔清單見 `CrossPlatform_R143_Scan_Findings.md` §2／§3；證據見 `CrossPlatform_DEF200275_Context_Metering_Evidence.md`〈第四輪〉與 `CrossPlatform_DEF200278_Halt_Handoff_Evidence.md`。round-label-ok

<!-- guard-total:R144 --> R144 護欄層累積淨額＝ 97056 → 97488（+432＝+130 D15 判準新增 ＋ +24 同輪自身漂移收斂 ＋ +120 DEF-200-281 首輪收尾 ＋ +34 同輪自身漂移收斂 ＋ +92 DEF-200-281 第二輪收尾 ＋ +32 同輪自身漂移收斂，全額歸回歸鎖軌）——DEF-200-275 第五輪：commit ea6113d push 後掌舵者要求 Architect／SA／SD／QA 四方獨立複審（皆 Sonnet 5、唯讀、各自隔離重現）「已修好」的宣稱，四方一致判問題 1「新視窗一開就被擋」與問題 2「不用真實 /context 或 API 查數據」皆為 partial，收斂出 (a) PENDING×首擊 m=None 首擊 deny、(b) per-stage cap 仍寫專案級 ESCALATION 致新視窗全擋不自癒、(c) 指定值分母對 observed_model 零交叉（haiku 真實 125% 讀成 25.9%）、(d) Stop hook 無「被擋／水位」宣稱佐證判準 四項真缺陷，另立 (e) SD-06／(f) ARCH-06 兩項另案；裁決 D11～D16 分兩包（Dev-A：D11／D12／D13／D16 SDD 治理面；Dev-B：D14／D15 量測面＋根層 Stop hook）並行修復，先紅後綠；複審另抓到 R-D11（PENDING 放行未排除 Rule 9.6 規格寫入禁令）／R-D13（per-stage cap marker 未隨 stage 換新）兩個真缺陷，由 Dev-A2 修復並轉 APPROVE；SDD 全套 `tools/fsm_runtime/tests/ -m "not chaos"` 收尾 `1872 passed, 8 skipped, 34 deselected, 14 subtests passed`（含 D11/D13 複審修復＋Dev-C 稽核帳本效能修復＋DEF-200-281 喚醒鏈兩輪修復全數併入）；同輪另立 DEF-200-279（並行 subagent 共用專案級 FSM-STATE，open）、DEF-200-280（v0.30 settings.json PreToolUse matcher 缺 Agent|Workflow，open）、DEF-200-281（自願停機喚醒鏈斷裂，fixed）、DEF-200-282（稽核帳本 YAML 純 Python 編解碼效能瓶頸，fixed）。guard-line 淨額分三批：Dev-B 完成 D15 判準（`test_claim_provenance_r86.py` 806→936）與自身漂移收斂（97056→97210）；DEF-200-281 首輪收尾（halt 標記自檢＋`test_quota_policy.py` 隔離止血，`test_wake_chain_halt_r278.py` 299→399）與自身漂移收斂（97210→97364）；DEF-200-281 第二輪收尾（RC-3 per-sid 閂鎖＋naive-now 拒寫，`test_wake_chain_halt_r278.py` 400→492）與自身漂移收斂（97364→97488），另同輪重釘 `tools/lib/guard_bucket_policy.py` 的 `guard_self` shrink-only 分桶（3416→3431）。本輪期間發生一次治理 yaml 污染事故（探針漏 telemetry 前綴，11 支 `governance/rules/R-9.*.yaml` 被重排版）並已由主控以 `git show HEAD:` 逐支還原，僅 R-9.2 保留本輪合法的 `failure_mode:` 文字改動。逐項見 `docs/06_quality/CrossPlatform_DEF200275_Context_Metering_Evidence.md`〈第五輪〉節與 `docs/06_quality/CrossPlatform_R144_Scan_Findings.md`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-275／DEF-200-279／DEF-200-280／DEF-200-281／DEF-200-282。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R143 寄居體例；R144 本輪僅為單一缺陷第五輪四方獨立複審收斂＋一項喚醒鏈缺陷收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R145 --> R145 護欄層累積淨額＝ 97488 → 98236（+748＝+48 test_adr_xplat001_c1c2_lock＋18 test_check_hooks_liveness＋79 test_claim_provenance_r86＋48 test_context_budget_guard＋215 test_root_guard_known_model_r145＋141 test_sentinel_tick_e2e_r145＋199 test_wake_chain_halt_r278；分軌＝回歸鎖軌 392〔含本檔記帳 48〕＋功能軌 356〔兩支新判準能力鎖檔〕，回歸鎖軌超上限 309 走 `_REGRESSION_LANE_APPROVED_OVERAGE`、功能軌熄滅款(11)[只升不降] 走 `_REPIN_APPROVED_ROUND_OVERAGE` 第三格〔上限 2→3〕，皆四方記帳複審核准，見 §3）——DEF-200-275 第六輪：掌舵者三問（新視窗被擋／不用真實數據／數字與 /context 不符）四方複審（Architect／SA／SD／QA 皆 Sonnet 5、唯讀）全數 REJECT，收斂 F-ARCH-01（非 context 來源 ESCALATION 零溯源）、SD-01（cap 計數跨 session）、SD-02（PENDING 專案級）、F-ARCH-02（DEF-200-280 只改 v0.30 對根層零效果）、SD-09（根層無查表收斂）、SA-01～03（文件舊語意）等；裁決 D17～D26 由 Dev-A／B／C／D 四包並行修復，修後複審抓到 ARCH-R6-01（4 個直呼 transition 落點繞過溯源）、SA-F2（文件門檻 0.90 vs 程式 0.95）、QA-R6-01（帳本列超 700 bytes）由 Dev-A2／Dev-D2／主控修復，迷你複審 Architect／SA 皆 APPROVE；DEF-200-279／280 結案、DEF-200-283 新立即結；SDD 全套 `1907 passed, 8 skipped` rc=0、chaos `34 passed` rc=0。護欄層淨額全額申報回歸鎖軌並走 `_REGRESSION_LANE_APPROVED_OVERAGE` 一次性例外（詳見 `CrossPlatform_R145_Scan_Findings.md` §3）。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R144 寄居體例。round-label-ok

<!-- guard-total:R146 --> R146 護欄層累積淨額＝ 98236 → 98195（-41＝-25 主表逐檔漂移＋15 本檔自身自含式漂移＋（本輪續）-16 DEF-200-287／288 收尾造成的逐檔漂移收斂；DEF-200-275 第七輪收尾單人窗口重釘：package A（D27 根層⑥階查表收斂＋sdd_latest 快取）／B（D28 telemetry writeback 守衛）對 `test_context_window_parity.py`／`test_context_budget_guard.py`／`test_root_guard_known_model_r145.py` 的合法新增，加上 Dev-Trim7 十二支測試檔 docstring 散文搬遷抵銷，造成主表多檔逐檔漂移，[淨減法輪] 不受款(9)`[未附刪除清單]` 約束）——逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第七輪附記〉節；證據見 `CrossPlatform_DEF200275_Context_Metering_Evidence.md`〈第七輪〉節；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-275／DEF-200-284／DEF-200-285／DEF-200-286。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R145 寄居體例。**R146 續（Dev-D8 收尾單人窗口，2026-09-12）**：DEF-200-287（windows-compat-ci #220／#221 Windows 稽核帳本檔案鎖競態，三棒修復 D31／D31b／D31c）／DEF-200-288（status line harness 進料，D32／D32b 兩棒）收尾：新增 `test_statusline_context_feed.py` 等功能軌鎖檔＋`test_check_hooks_liveness.py` 的 `TestConversationLedgerChildTimeoutParity` 回歸鎖軌，Dev-Trim8 十五支 `tools/tests/*.py` 散文搬遷抵銷，98211 → 98195（-16）；doc-total 對帳兩站點（本檔＋`CrossPlatform_R145_Scan_Findings.md`）一致。round-label-ok

<!-- guard-total:R147 --> R147 護欄層累積淨額＝ 98195 → 98943（+748＝+638 內容成長〔`test_run_root_unittests.py` +610＋主控 CI 複驗修正 +10（Windows SIGTERM 不自殺平台分支＋`::warning::` 計數修正）／`test_pre_push_dispatcher.py` +18〕＋110 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（含 `_REGRESSION_LANE_LOG`／`_REPIN_NET_CAP_SCHEDULE` 到期兌現與重新武裝／`_PHASE2_REVIEW_LOG` 新列／U9 具名展延／凍結前綴延伸等自含式編修）；分軌＝回歸鎖軌 270〔P1/P0 zshrc 假紅根治 119＋既有缺陷修復 151，未使用任何一次性例外名冊〕＋功能軌餘額 478〔內容成長 368＝LPT 快取／自動細分／不均偵測 v2／worker cap 9／compileall 遷移回歸鎖＋主控 CI 複驗修正＋本檔自身記帳漂移 110〕，回歸鎖軌／功能軌皆未超上限）——DEF-200-274 第九輪收尾單人窗口：主控四方獨立複審（Architect/SA/SD/QA）收斂 P1 zshrc 假紅真因（測試對合成樹呼叫 `run_with_floor()` 觸發平行 worker）、`fair_share` 倍率稀釋、偵測→動作閉環缺口、pre-push leg② 未接平行、AutoClaude／AISDLC_SDD 零 xdist、SIGTERM 無清理、worker 數未印等發現；落地：`run_root_unittests.py` 預設自動多核心（`should_run_parallel()` 三條件真值表＋合成樹強制序列）、LPT（Longest-Processing-Time）派工排序讀 `tools/lib/parallel_timing_cache.py` 活體快取／`tools/lib/parallel_timing_seed.json` 種子、自動細分候選（`dispatch_granularity.auto_class_level_candidates()`）、不均偵測 v2（fair share 帶＋wall-clock 效率＋worker 數印出）、SIGTERM 清理競態修復、worker cap 8→9；AutoClaude／AISDLC_SDD pytest 套件接 pytest-xdist（`-n auto --dist worksteal`，PG 場景走 `--dist loadgroup`）。安靜機器實測：auto 平行 worker=9 LPT 排序 4246 支測試 wall 159s（此前序列基線約 12～13 分鐘）。誠實劃界：CI windows-latest 已在第八輪驗過、本輪 push 後會再驗；掌舵者 Windows 11 物理機親驗待補；`compileall -j0` 與 SIGTERM 在 Windows 的實機行為未驗；AutoClaude 真 Docker PG 下的 loadgroup 端到端未驗。逐項見 `docs/06_quality/CrossPlatform_R145_Scan_Findings.md`〈R147 沿用〉節；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第九輪〉節；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R146 寄居體例；R147 本輪僅為單一缺陷第九輪收尾單人窗口附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R148 --> R148 護欄層累積淨額＝ 98943 → 99419（+476＝+393 內容成長〔`test_run_root_unittests.py` +175：D2 兩支平行回歸鎖 `RunParallelStalenessAdvisoryCiSymmetryTest`／`ParallelMergeResultSkipCensusParityTest`／`test_doc_loc_baseline_freshness_r60.py` +61：D1 139.9s 臨界測試拆三個具名平台子類別＋一致性回歸鎖／`test_wake_chain_halt_r278.py` +36：包 C `HALT_MARKER_ACTIVITY_SKEW_SECONDS` 三支近界回歸鎖／新檔 `test_ci_gate_xdist_allowlist.py` +121：修 2 ci-gate.sh xdist 允許清單回歸鎖，遷自 AISDLC_SDD 樹、同 D32 status line 先例，全額歸回歸鎖軌並補 `_REGRESSION_LANE_LOG` R148 列 393，淨額超過軌上限 309 故同時走 `_REGRESSION_LANE_APPROVED_OVERAGE` 一次性例外（四方獨立審查核准，`_REGRESSION_LANE_APPROVED_OVERAGE_MAX_ENTRIES` 1→2）〕＋11 全套第三次驗證跑時四方複審再發現：`test_doc_loc_baseline_freshness_r60.py` 的 `python_symbol_index()`／`collect_symbol_claims()` 對 `glob()` 快照後才 `read_text()`，D1 拆分後平台模擬類別數增加、與 `LoadBalancingRegressionTest` 平行寫刪合成檔的既有競態暴露機率上升，補 `try/except FileNotFoundError` 兩處硬化（全額歸功能軌，非回歸鎖）＋3 全套第四次驗證跑時四方複審再發現：`TestR81GhostPathClaims._FLIP_PROBE` 固定檔名在 D1 拆分後被多個平行 worker 行程互撞，改帶 `os.getpid()` 隔離（同 `LoadBalancingRegressionTest` 既有慣例，全額歸功能軌）＋69 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（主表新增列＋`_FROZEN_PREFIX_REWRITE_LEDGER`／`_REGRESSION_LANE_LOG`／`_REGRESSION_LANE_APPROVED_OVERAGE` 追加列＋逐列補 CrossPlatform 逐檔清單指標＋凍結前綴延伸 204→213）；諸項皆回歸鎖軌／功能軌，全額計入，非淨減法輪）——DEF-200-274 第十輪：掌舵者要求四方獨立審查（Architect/SA/SD/QA，各自不共享上下文分別跑）「已修好」的宣稱，四方對第九輪 24 條發現逐條覆核，19 條成立、5 條駁回、1 條未驗，其中 SA-04（`halt_verdict()` 對「標記是否過期」用零容忍嚴格比較，hook 落盤 `at` 與逐字稿真實 mtime 之間天生的毫秒級時序雜訊會被誤判成「已續跑」，commit c4a7e00 當輪只把測試治具的 mtime 撥早規避現象、生產碼本身未動）與 SD-06（種子過期 advisory 與既有 dispatch_imbalance 的 `::warning::` 待遇不對稱）、SD-02／SA-01（139.9s 單一測試方法過長、無法被 `dispatch_granularity` 類別級白名單再細分）三項判定為需修復的真缺陷；5 包並行修復（包 A：CI workflow perf-baseline／nightly-full 的 xdist 開關；包 B：`ci-gate.sh` xdist 判準排除清單改允許清單，因 `_atomic_write_text` 修法僅存在於 LATEST、中間歷史版依規則不可原地補；包 C：`HALT_MARKER_ACTIVITY_SKEW_SECONDS=10` 生產面根治＋三支近界回歸鎖；D1：139.9s 測試拆三平台具名子類別；D2：staleness advisory CI `::warning::`對稱化＋`merge_results()` skip 語意平行回歸鎖）→ 四方複審 3 APPROVE／1 REJECT（SA-01 一次性例外未逐字附刪除清單，退回補正）→ 收尾單人窗口修復收斂、SD/QA 二審 APPROVE。逐項見 `docs/06_quality/CrossPlatform_R145_Scan_Findings.md`〈第十輪附記〉節；詳細證據見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第十輪〉節；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-274／DEF-200-289／DEF-200-290。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R147 寄居體例；R148 本輪僅為單一缺陷第十輪四方獨立複審收斂附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R149 --> R149 護欄層累積淨額＝ 99419 → 99726（+307＝+277 內容成長〔`test_pre_push_dispatcher.py` 704→981：DEF-200-293（census 通道改餵檔案路徑非 stdin）／DEF-200-294（AutoClaude 子 hook 直譯器候選鏈根層優先＋健康探針）／DEF-200-295（`--dist loadgroup` 判準改問 SSOT `pg_autodetect()`/`pg_dsn_in_effect()`）三筆缺陷的回歸鎖與 fake-repo stub：`_autoclaude_leg_stub`／`_census_stub` 兩支專屬 stub＋`TestAutoClaudeSubHookInterpreterChain`（4 測試）／`TestAutoClaudeSubHookPytestDistDecision`（4 測試）兩個純文字判準測試類別＋兩支 fake-repo 整合測試，含 E501 存量債棘輪折行 +29〕＋30 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（主表新增列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 追加列所造成的行數漂移，`--print-guard-lines` 反覆覆核收斂到本行本身也計入為止，凍結前綴 `_REPIN_LOG_FROZEN_PREFIX_LEN` 維持 217（本輪新增列全部涵蓋在內）、`_REPIN_LOG_HISTORY_SHA256`／`_FROZEN_PREFIX_REWRITE_LEDGER` 同步重釘），全額歸回歸鎖軌，`_REGRESSION_LANE_LOG` R149 列 307 未超軌上限 309、未使用任何一次性例外名冊；款(12) 到期義務同輪兌現，`_REPIN_NET_CAP_SCHEDULE` 追加 `(149, 541)`，重新武裝 `_REPIN_NET_CAP_DUE_ROUND=151`／`_REPIN_NET_CAP_DUE_TARGET=540`）——第四棒 root-infra leg 收尾：DEF-200-293（census 通道吃 stdin 重導向被轉呼叫鏈吞掉、未啟用 venv 時誤判「找不到剖面標記」擋下每次 push）／DEF-200-294（子 hook 直譯器候選鏈子專案優先撿到陳舊 `AutoClaude/.venv` 致 pytest rc=4）／DEF-200-295（`--dist loadgroup` 判準只看環境變數、與 conftest 的 `pg_autodetect()` 判斷不一致）三筆缺陷各自的回歸鎖與 fake-repo stub 落地，另隨 `tools/tests/test_subprocess_encoding_hygiene.py` 的 E501 存量債棘輪（`_E501_DEBT_CEILING=139`）折行 19 處過長行；逐項見 `tools/tests/test_pre_push_dispatcher.py`；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-293／DEF-200-294／DEF-200-295。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R148 寄居體例；R149 本輪僅為三筆 root-infra 缺陷收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R150 --> R150 護欄層累積淨額＝ 99726 → 99845（+119＝+99 內容成長〔`test_pre_push_dispatcher.py` 981→1016（DEF-200-297：子 hook 候選鏈零子專案 venv 鎖＋根 dispatcher ruff 候選鎖＋修復前片段反向自證）／`test_dev_start.py` 6529→6576（DEF-200-297：`TestStrayVenvScan` 4 測試）／`test_context_budget_guard.py` 12160→12177（DEF-200-299：`claim_once` age<0 確定性測試）〕＋20 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（主表新增列＋`_REGRESSION_LANE_LOG`／`_FROZEN_PREFIX_REWRITE_LEDGER` 追加列，凍結前綴 `_REPIN_LOG_FROZEN_PREFIX_LEN` 217→219、`_REPIN_LOG_HISTORY_SHA256` 同步重釘），全額歸回歸鎖軌，`_REGRESSION_LANE_LOG` R150 列 119 未超軌上限 309、未使用任何一次性例外名冊）——掌舵者「單一 .venv、其他刪除」指令＋CI #199 perf 假紅追因：Architect／SA／QA／SD 四方唯讀調查（皆 Sonnet 5）→ 三棒 Sonnet 開發（A1 子 hook 候選鏈零子專案 venv fallback＋刪除指令警告／B perf `environment` provenance 跨環境降 advisory／A2 `tools/lib/stray_venv.py`＋dev_start 淨零行接線＋DEF-200-296 鑑別力測試＋ONBOARDING §2.1 單一 .venv 原則）→ 四方唯讀審查（QA REJECT：advisory 蓋掉照算等級）→ B2 修復（等級照算、green→notice／warn・block→warning、CI 判準只認 `GITHUB_ACTIONS`、toml 寫出 LF）→ 迷你複審。樹外 3 份 `autoclaude_cleanvenv_*` 殘留當場刪除。DEF-200-298／300 修復全在 AutoClaude 側不計護欄層。逐項見 `docs/06_quality/CrossPlatform_R145_Scan_Findings.md`〈第十二輪附記〉；缺陷帳本見 `docs/06_quality/AutoSDD_Defect_Log.md` DEF-200-297／DEF-200-298／DEF-200-299／DEF-200-300。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R149 寄居體例；R150 本輪僅為四筆缺陷收尾附帶的 guard-line 記帳延伸、非開新一輪 CrossPlatform 掃描輪四件套。round-label-ok

<!-- guard-total:R151 --> R151 護欄層累積淨額＝ 99845 → 99812（-33＝兩支新鎖檔 `test_single_venv_identity.py`（DEF-200-301 hook 載具單一 .venv 身分鎖）／`test_clean_venv_carrier.py`（DEF-200-306 乾淨 venv 載具）＋`test_nightly_interpreter_determinism.py` 278→309（DEF-200-302 釘死根層 .venv、刪 PATH 剝除 A／D 兩類）＋`test_skip_ceiling_ratchet_direction.py` 706→724（DEF-200-303 re-key 方向鎖）的內容成長，以 Dev-Trim 兩棒對 tools/tests 鎖檔的史料散文搬遷抵銷〔逐字保全於 `CrossPlatform_R151_Guard_Prose_Migration.md`〕，含本檔（`test_adr_xplat001_c1c2_lock.py`）自身重釘漂移；**淨減法輪**，同輪兌現 cap 到期 541→540 並重新武裝 153／539，分桶 prose／guard_self 兩桶皆下修，未使用任何一次性例外名冊）——掌舵者「單一 .venv 徹底解決」指令＋nightly local_ci_gate 12 連紅追因：Architect／SA／SD／QA 四方唯讀調查（皆 Sonnet 5）→ 四棒 Sonnet 實作（F1 hook 載具 `../.venv`＋身分鎖／F2 掌舵者裁決 B 案兩支排程釘死根層 .venv／F3 剖面 pgextras 軸落地＋登記 nightly 剖面＋無人值守 advisory／F4～F6 perf 檔頭 provenance、seed_kb LF 與停寫 fixture、clean_venv_carrier 工具）→ B5 補 `-Unattended` 薄殼轉發鏈 → 四方唯讀複審（三 APPROVE、SD 唯一 blocking＝本重釘）→ Fix-A 修六筆 findings → Trim-B／Trim-C 散文搬遷 → 收尾單人窗口以 clean_venv_carrier 實跑 §7 回填。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十三輪附記（R151）〉節；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-301～DEF-200-306。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R150 寄居體例。round-label-ok

<!-- guard-total:R152 --> R152 護欄層累積淨額＝ 99812 → 100211（+399＝內容成長 +249〔`test_nightly_interpreter_determinism.py` 312→384（DEF-200-307 mac nightly／smoke 釘死根層 .venv 的 F 項斷言）／`test_dev_start.py` 6540→6649（DEF-200-308 雜散 venv 全樹遞迴掃描＋`step_venv` 擋下）／`test_clean_venv_carrier.py` 199→262（DEF-200-309 clean_venv_carrier 建立失敗必清理）三筆缺陷回歸鎖，全額歸回歸鎖軌；`test_windowsapps_guard_cross_consistency.py` 2051→2052（DEF-200-310 過期字面訂正折行，非回歸鎖，全額歸功能軌、不計入回歸鎖軌淨額）；`test_find_git_bash_parity.py` 1306→1310（DEF-200-312：釘死根層 .venv 後首晚 nightly 真跑抓到 schtasks 最高權限下 `_real_shim` symlink venv launcher ⇒ rc=106，改 Windows 一律 shebang 包裝，同輪追加、歸回歸鎖軌）〕＋37 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（主表新增列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列＋`_ROOT_TOOLS_OLD_SCALE_DEBT_DUE_ROUND` 到期義務具名展延 152→157〔鐵律七：真拆屬獨立重構持有面，展延理由逐字寫在檔內〕＋同輪追加列，凍結前綴 `_REPIN_LOG_FROZEN_PREFIX_LEN` 220→224、`_REPIN_LOG_HISTORY_SHA256`／`_FROZEN_PREFIX_REWRITE_LEDGER` 同步重釘），全額歸回歸鎖軌；`_REGRESSION_LANE_LOG` R152 列 285（244＋4＋37 自身漂移）未超軌上限 309、未使用任何一次性例外名冊，DEF-200-310 的 +1 明文排除於本軌淨額之外；cap 到期輪 R153 尚未到，本輪維持 540 不動；分桶 `prose`／`guard_self` 兩桶皆與上輪相同、未變動）——單一 .venv 四方審查殘餘收尾單人窗口：DEF-200-307（mac nightly／smoke 釘死根層 .venv 的 F 項斷言）／DEF-200-308（雜散 venv 全樹遞迴掃描＋`step_venv` 擋下）／DEF-200-309（clean_venv_carrier 建立失敗必清理）三筆缺陷回歸鎖收尾＋DEF-200-310（過期字面訂正折行）非回歸鎖的散文訂正。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十四輪附記（R152）〉節；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-307～DEF-200-310。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R151 寄居體例。R152 沿用追加
（+113，累計兩批）：第一批 +71——DEF-200-314（mac launchd nightly 三症狀修復）新增
`test_nightly_interpreter_determinism.py` G 項兩支斷言（+58）＋本檔自身逐檔漂移收斂
（+13）。第二批 +42（四方複審四項非阻斷修正）：Homebrew append-only 位置鎖三支斷言＋
run_stage 3 整行 unattended 鎖＋docstring 訂正（`test_nightly_interpreter_
determinism.py` +29）＋本檔自身逐檔漂移收斂（+13，凍結前綴 `_REPIN_LOG_FROZEN_
PREFIX_LEN` 226→228、`_REPIN_LOG_HISTORY_SHA256`／`_FROZEN_PREFIX_REWRITE_LEDGER`
同步重釘）。兩批皆全額歸功能軌（`[全額功能軌]` 標記）。逐項見
`CrossPlatform_R152_DEF200314_MacNightly_Evidence.md`；缺陷帳本見 `AutoSDD_Defect_Log.md`
DEF-200-314～DEF-200-316。round-label-ok

<!-- guard-total:R153 --> R153 護欄層累積淨額＝ 100211 → 100695（+484＝內容成長 +445〔`test_cpu_budget.py` 新檔 158（DEF-200-289 跨 leg CPU 預算 SSOT 契約鎖，DEF-200-317 addopts 鎖寄居其中，新判準能力鎖檔全額歸功能軌）／`test_ci_gate_xdist_allowlist.py` 121→213（DEF-200-318 ps1 fallback 不加 xdist 判讀鎖＋DEF-200-289 三處匯出接線鎖，歸回歸鎖軌）／`test_run_root_unittests.py` 4443→4458（DEF-200-289 WorkerCountFormulaTest 顯式控制 headless env，歸回歸鎖軌）／`test_workflow_permission_concurrency_lock.py` 1406→1580（DEF-200-292 告警自癒鎖 105 歸回歸鎖軌＋DEF-200-290 headSha 涵蓋新判準鎖 69 歸功能軌）／`test_dev_start.py` 6649→6655（DEF-200-319 SIGINT 兩支同型測試 pidfile TOCTOU 硬化，歸回歸鎖軌）〕＋39 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移收斂（主表新增兩列＋`_REGRESSION_LANE_LOG`／`_REPIN_NET_CAP_SCHEDULE` 兌現 (153, 539) 並重新武裝 155／538／`_PHASE2_REVIEW_LOG` `[提案]`／`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈，凍結前綴 228→230），全額歸回歸鎖軌；`_REGRESSION_LANE_LOG` R153 那筆 257 未超軌上限 309、未使用任何一次性例外名冊；🔴 R152／R153 連續兩輪上升名額用罄，R154 必須淨減）——多 CPU 四方獨立審查（Architect／SA／SD／QA 皆 Sonnet 5、唯讀）確認 DEF-200-274 核心機制與 CI 接線完備、無結構性頭重腳輕（最重派工單位 93s＜公平份額 123.3s；CI 不均偵測零命中），並收尾三筆 open：DEF-200-289（`tools/lib/cpu_budget.py` SSOT＋headless 不保留前景＋pre-push／ci-gate 匯出廣播）／DEF-200-290（root-infra-ci 哨兵補 headSha 涵蓋 advisory）／DEF-200-292（告警 label 自癒＋開單失敗不得靜默），新立 DEF-200-317（addopts 無鎖，同輪修）／DEF-200-318（ps1 fallback 無 LATEST 軌，open）／DEF-200-319（SIGINT 測試 pidfile TOCTOU，同輪修）。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十五輪附記（R153）〉節；證據見 `CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第十二輪〉；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-289／290／292／317／318／319。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R152 寄居體例。round-label-ok

<!-- guard-total:R154 --> R154 護欄層累積淨額＝ 100695 → 100691（-4＝內容 -2〔`test_dev_start.py` 6655→6653：DEF-200-319 三處同型 pidfile 輪詢抽成模組層 `_wait_pid_text`，第三支 killpg 測試同輪補硬化並移除 `sleep(0.3)`；`test_ci_gate_xdist_allowlist.py` 213→213：DEF-200-320 匯出接線鎖三支合併為 subTest 一支並擴到兩支 nightly-full workflow〕＋本檔（`test_adr_xplat001_c1c2_lock.py`）-2：新增主表列與 `_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈列的漂移以 `repin_growth_problems()` docstring 兩段史料搬遷 `CrossPlatform_Guard_Line_History.md`〈repin_growth_problems 分軌與例外名冊 WHY〉節抵銷；凍結前綴 230→231、`_REPIN_LOG_HISTORY_SHA256` 同步重釘）——**淨減法輪**：R152／R153 連續兩輪上升名額用罄後本輪淨額為負，款(11) 連續計數歸零；母項為負故不申報回歸鎖軌（同 R146／R151 體例）。四方（Architect／SA／SD／QA，皆 Sonnet 5、唯讀）獨立複審確認 R153 六筆宣稱屬實、CI worker=4／4／3 由 SA／QA 各自從 CI log 實測回填，新立並同輪修 DEF-200-320（nightly-full 內嵌 xdist 繞過 SSOT）。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十六輪附記（R154）〉；證據見 `CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第十三輪〉；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-319／320。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R153 寄居體例。round-label-ok

<!-- guard-total:R155 --> R155 護欄層累積淨額＝ 100691 → 101532（+841＝內容 +812〔`test_nightly_interpreter_determinism.py` 471→820（DEF-200-315 H 項新判準能力鎖：本機缺席 fail-loud／CI 容許 PATH／逃生口三情境）／`test_windowsapps_guard_cross_consistency.py` 2052→2178＋`test_windowsapps_guard_bash_parity.py` 953→1042（DEF-200-315 三情境行為測試）／`test_dev_start.py` 6653→6758＋`test_pre_commit_dispatcher_sigpipe.py` 936→969＋`test_clean_venv_carrier.py` 262→281（DEF-200-322 雜散 venv 守衛四處補洞回歸鎖）／`test_workflow_permission_concurrency_lock.py` 1580→1612（DEF-200-321 回歸鎖）／`test_find_git_bash_parity.py` 1310→1336／`test_git_hooks_install_common.py` 393→411／`test_ci_gate_xdist_allowlist.py` 213→221／`test_pre_push_dispatcher.py` 995→1002〕＋29 本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移（主表新增兩列〔內容＋收斂〕＋`_REGRESSION_LANE_LOG` 同輪新列＋`_REPIN_NET_CAP_SCHEDULE` 兌現 (155, 538) 並重新武裝 157／537＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈，凍結前綴 231→233、`_REPIN_LOG_HISTORY_SHA256` 同步重釘），全額歸回歸鎖軌；`_REGRESSION_LANE_LOG` R155 那筆申報 309（貼齊上限，同 R131 體例，優先納入 DEF-200-322／323 與 DEF-200-321 的回歸鎖段、DEF-200-315 的三情境行為測試段），母項扣除後主軌淨額 532 ≤ cap 538（到期輪兌現）；連升 streak 第 1／2（R154 為 -4 已歸零）——DEF-200-315（互動式入口改經 `pick_repo_python`／`Get-RepoPython` SSOT 化，掌舵者「最佳化、不考慮成本」裁決）／DEF-200-321（root-infra-ci never-started jq 判準排除 `conclusion=="skipped"`）／DEF-200-322（雜散 venv 守衛四處補洞：`.venv-cache-*` 根層限定剪枝／pre-commit CLI 閘／symlink `pyvenv.cfg` 查探）／DEF-200-323（`uv sync`／`uv run` 第二顆 venv：dev_start 兩殼 export `UV_PROJECT_ENVIRONMENT`）四方審查收尾。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十七輪附記（R155）〉節；證據見 `CrossPlatform_R152_DEF200314_MacNightly_Evidence.md`〈收尾（R155）〉；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-315／321／322／323。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R154 寄居體例。round-label-ok

<!-- guard-total:R156 --> R156 護欄層累積淨額＝ 101532 → 101607（+75）——DEF-200-324（`tools/integration_gate_core.py` 的 `sec_bridge()`／`sec_rollback()` 兩處 AutoClaude pytest 呼叫零 `--dist loadgroup` 判準，PG 在場即撞 DEF-200-274 X1 守門 rc=4，擋下一次真實 push）修復：新增 `_pg_dist_args()` 問 `AutoClaude/tools/local_ci_gate.py` 的 `pg_autodetect()`／`pg_dsn_in_effect()` SSOT（同 DEF-200-295 判例，探針失敗保守加），`test_pre_push_dispatcher.py` +53（`TestIntegrationGateCorePgDistArgs` 四支：PG 在場／缺席／探針失敗三情境＋兩呼叫點接線突變自證）；本檔（`test_adr_xplat001_c1c2_lock.py`）自身逐檔漂移 +22（主表新增兩列〔內容＋收斂〕＋`_REGRESSION_LANE_LOG` 同輪新列＋`_FROZEN_PREFIX_REWRITE_LEDGER` 接鏈，凍結前綴 233→235、`_REPIN_LOG_HISTORY_SHA256` 同步重釘）。**另起本輪**（判例＝R132／R136：R155 主軌額度僅剩 6 行不足容納）；分軌：`_REGRESSION_LANE_LOG` R156 列申報 53（測試全額為 DEF-200-324 缺陷回歸鎖），本檔自身漂移保守歸主軌，主軌淨額 75−53＝22 ≤ cap 538；連升 streak 第 2／2（R155 為 +532）已到頂，**R157 必須淨額 ≤0**。真機驗證：`bash tools/integration_gate.sh --skip-full`（PG 在場）rc=0，`[3/5]`／`[4/5]` 皆 PASS。逐項見 `CrossPlatform_R145_Scan_Findings.md`〈第十八輪附記（R156）〉節；缺陷帳本見 `AutoSDD_Defect_Log.md` DEF-200-324。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R155 寄居體例。round-label-ok

<!-- guard-total:R157 --> R157 護欄層累積淨額＝ 101607 → 101669（+62）——多 CPU 第十四輪＋DEF-200-325 對話框根治＋DEF-200-326～331 收尾：內容 +32（DEF-200-325／326／327 回歸鎖 +75、docstring 史料搬遷 −53、`_zzz_` 暫態排除 +6）＋`test_adr_xplat001_c1c2_lock.py` 自身漂移 +30；回歸鎖軌申報 62（子集上限＝同輪主表淨額），主軌 0，cap 到期兌現 (157, 537) 並重新武裝 159／536。逐檔清單見 CrossPlatform_R145_Scan_Findings.md〈第十九輪附記（R157）〉。round-label-ok

<!-- guard-total:R158 --> R158 護欄層累積淨額＝ 101669 → 102515（+846）——收尾單人窗口：六個並行修復包新增三支回歸鎖 `test_install_statusline.py`（297）／`test_sdd_hook_router_r158.py`（189）／`test_session_brief.py`（220）＋`test_context_budget_guard.py` +117／`test_platform_neutral_paths.py` +4（`tools/tests`／`tools` 兩桶腐化上界重釘 64→79、27→36）／`test_mac_endurance_r83.py` +3（P6 行為變更同步訂正既有測試）／本表自身 +16。回歸鎖軌申報 309（router 189＋context_budget_guard 117＋mac_endurance_r83 3，子集不得大於母項，貼齊 `_REGRESSION_LANE_ROUND_CAP`），主軌 846−309＝537，貼齊該輪上限。前綴重寫鏈接 DEF-200-333。逐檔清單見 `CrossPlatform_R158_SessionGate_Statusline_Evidence.md`；缺陷帳本見 `AutoSDD_Defect_Log.md`。此附記為 doc-total 對帳（≥2 站點）寄居本 improving 檔，同 R129～R157 寄居體例。round-label-ok
