# AutoSDD_improving_106 — R82 跨平台輪（Windows 11 真機）

> 軌道① 整合迭代第 106 份；本輪同時是**跨平台複審輪 R82**（輪號權威源＝
> `tools/check_defect_log_crossref.py::current_round()` 讀帳本「發現情境」欄，收輪前現查，
> 本檔寫成時實測為 **82**）。
>
> 🔴 **體例（沿用 R81 的兩條，不放寬）**：①凡述及「已通過／已達標／尚未做／已推送」這類狀態，
> 一律附**現查指令**，不寫快照結論；②本檔內每一個數字都要標明它是**誰量的**——
> `[本包實測]`＝收輪單人窗口當回合真跑／`[他包回報]`＝掃描包或修復包交件、本包未重跑／
> `[史料]`＝更早輪次快照。
>
> 🔴 **本輪再加嚴一條（R82 新增）**：凡述及「本輪做到了 X」，**必須同時寫出 X 的相反面**
> ——搬家要寫成搬家、部分要寫成部分、沒量的要寫成沒量。立案理由是本輪 §1 Q2 那一格：
> 生產碼在 `git diff` 下讀出 **−151**（一個漂亮的負數），而把 untracked 新檔算進來是 **+1278**。
> **假在「看起來像減法」這個方向上，也就是最不會有人去質疑的方向。**
>
> 記號約定：`$r`＝repo 根，`$p`＝`$r\.venv\Scripts\python.exe`。

---

## §0 本輪定位與唯一驅動器

三柱對齊（根 `CLAUDE.md`〈三條改進軌道〉）：本輪主體同 R80／R81，落在 **B 柱（手腳 AISDLC_SDD）
＋ C 柱（指揮官 AutoClaude）的跨平台基礎設施**；**A 柱（雙向協作橋接）本輪仍暫不動**（連三輪）。
唯一驅動器＝`docs/04_planning/AutoSDD_Iteration_Prompt_Template.md`，本檔即該範本的第 106 份實例。

### §0.1 🔴 本輪的編號與 R81 不同，動工前先對齊（免得又指錯）

R81 的 `Q6` 是**成熟度 M1~M6**，而 R82 掌舵者訴求裡的 `6` 是 **Token 監控與排程喚醒（a~f）**。
本檔一律照**本輪掌舵者的原始編號**，並把成熟度改列為獨立的 `M` 列：

| 本檔編號 | 主題 | 與 R81 的對應 |
|---|---|---|
| Q1~Q5 | 相容性掃描／架構減法／Mac↔Win 落差／Windows 低級錯誤／清技術債 | 同 R81 Q1~Q5 |
| **Q6（a~f）** | **Token 監控與排程喚醒** | ＝R81 表中的 a／b／c／d／e／f 六列 |
| **M** | **成熟度 M1~M6** | ＝R81 的 Q6 |
| AC | AutoClaude 三問題（舵手喚醒／example 跑不動／關不掉的通知） | R81 無此列 |
| S3 | pytest skipped 徹底解決 | 同 R81 S3 |
| L | 缺陷帳本容量 | R81 併在 Q5 內 |
| P7 | 哨兵／hook 彈窗 | 同 R81 P7 |

### §0.2 開場前置（每列標明量測來源）

| 前置 | 實測 | 量測來源 |
|---|---|---|
| 額度水位 | 開場 **50.0%**（`weekly_all`）；session／five_hour 皆 1% | `[他包回報]`（R82 開場）。🔴 這個「50%」本身就是本輪 P0 之一（HELM-04：取錯桶），見 §1 Q6-a |
| context 水位 | 11.9% | `[他包回報]` |
| 未結列水位 | 開場 **85**／warn 86／fail 98 | `[他包回報]`；收輪值見下方 §1 Q5 那一格 `[本包實測]` |
| 雲端 CI | 六支全 `failure`，annotation 逐字＝帳務拒絕 | `[他包回報]` ⇒ **本輪零雲端訊號**，改以 act ＋ Docker 本機驗證 |
| 當前輪時鐘 | `current_round()` 由 R81 推進到 **82** | `[本包實測]`（帳本 R82 第一列落地即前進） |
| 並行度 | **13 個 agent 交件**（六個修復包＋Q2 減法＋額度政策＋三鏡對抗複驗＋收斂＋接線＋帳本＋對抗稽核） | `[他包回報]` |

---

## §1 本輪要回答的題目（掌舵者原始訴求逐條，不得漏答）

> 🔴 **結論欄只能在收輪的單人窗口填**，且每一格都要附當回合真跑的輸出。
> R80 的教訓：多 agent 並行期間任何「全套閘門 rc=0」的宣稱都不成立。
> 🔴 **本檔由「文件面收尾者」填寫**，持有面只有本輪的 `.md`；凡結論落在程式面者，
> 一律標明「本包不判定實作面」並指出憑證由誰交——**不代別人宣稱**。

| 編號 | 訴求 | 驗收判準（機械可查者優先） | 結論 |
|---|---|---|---|
| **Q1** | 全面掃描 Mac／Win 11 部署執行與開發的相容缺陷 | 六個掃描面各自交出可重跑的 findings；每一筆都要指出「今天是哪一支判準在守它」，答不出來就是新的無機械物格 | **部分達成**。六面共 **80 筆** findings（5＋7＋5＋11＋39＋13，逐面拆解見 `docs/06_quality/CrossPlatform_R82_Scan_Findings.md` §A）`[他包回報]`。🔴 **本輪最有價值的不是筆數，是跨面重複出現的形態**：三個面各自獨立撞到同一件事——**鎖在守、但守的是別的東西**（Q4-01 交棒書鎖對整份文件收到 0 筆宣稱／MAC-02 後設鎖的分母裡沒有 macOS runner／CARRIER-01 skip 理由是自我宣稱而非事實）。⇒ 結論措辭沿用 R81 判準逐字：**Windows 側今日無已知未修相容缺陷；mac 側未量** |
| **Q2** | 架構簡潔－分工清楚－不重複模組；**拿掉不合理機制** | 本輪必須是**淨減法輪**：護欄層 raw-line 淨額 ≤ 0；否則走款(9)`[未附刪除清單]` 的登記手續（`[非淨減法輪]` ＋ 指名一份具名 `.md` 當逐檔清單的家），而走 ② 的輪次 Q2 一律判**未達成** | 🔴 **未達成，而且三個面沒有一個是淨減** `[本包實測]`。①**護欄層是加法**（逐檔清單見 findings §B）。🔴 **本格刻意不寫死淨額**（R82 收尾訂正：此前寫 `+3510／68423 → 71933`，而該值在同輪內已被重釘兩次以上，收尾當回合現查為 `72098 → 73145`＝**+1047**〔量測時點 2026-08-09，僅為當時讀數，程式面收尾者再重釘一次即過期〕）——受機械對帳的唯一那一行是本檔 §「本輪護欄層累積淨額」那一行（帶 `guard-total:` ＋本輪輪號的標記；🔴 此處刻意**不逐字寫出**那個完整字面——它就是判準的觸發條件，寫在敘述裡會讓這一格被當成一筆「引用現行總量」的宣稱而判 `[形態不符]`／`[總量不符]`，本包當回合實測踩過一次），其餘一律**現查**：`& $p "$r\tools\tests\test_adr_xplat001_c1c2_lock.py" --print-guard-lines`。**Q2 的結論靠的是方向（加法）不是量值**，重釘不改變方向；②**生產碼 +1278 raw**（`git diff --numstat HEAD` 對 hooks＋`tools/lib/`＋planner 讀出 **−151**，但那個數字是假的——`git diff` **看不到 untracked 檔**，而本輪三個最大產出全是新檔，合計 +1429 raw）；③**額度軸是搬家**：`.claude/hooks/context_budget_guard.py` 由 1451 降到 **952**（−499），但那 499 行搬進了 615 行的新檔 `tools/lib/quota_gate.py`。🔴 **「hook 從 1451 掉到 952」這句話單獨拿出來就是誤導，本輪明文禁止它單獨出現。** 本輪真正的減法候選（11 個純再匯出／296 行敘事去重／兩支鎖檔合併）**一項都沒做** |
| **Q3** | Mac 開發時 Win 不落差、反之亦然 | 落差面收斂到單一 SSOT；`.ps1`／`.sh` 對等性由**行為**而非**存在性**判定 | **本輪無進展（維持 R81 的「部分達成」，未再前進）** `[本包實測]`：`& $p "$r\tools\check_script_parity.py"` 本包**未重跑**，本輪亦無任何包宣稱動過介面對等鎖 ⇒ 行為等價的覆蓋面仍是 R81 那 3 對。🔴 **本列刻意不寫「維持」當成績**：Q3 的判準是覆蓋面要**擴大**，沒擴大就是沒進展 |
| **Q4** | Windows 常犯低級錯誤的根因徹底解決 | ①先驗證量測器符號；②重跑歸因**報絕對筆數不報百分比**；③最大桶必須有機械物在守 | **部分達成** `[他包回報]`。①符號先驗：`& $p "$r\tools\probe\audit_session.py" --selftest` 新判準判錯 **0／10**、舊判準 **6／10** ⇒ 符號正確，歸因可採信。②`& $p "$r\tools\probe\misstep_attribution.py"` n=1227，**絕對筆數**：CLAIM-FIRST **203** ＞ LOCKBLIND **176** ＞ BADPIPE **119** ＞ CARRIER **113**（OTHER 616）——最大桶**連兩輪**都是「宣稱先於查證」。③該桶本輪落地第二個機械物 `tools/tests/test_negative_existence_claims_r82.py`（治「否定存在宣稱」：尚未落地／零交付／零消費者——本 repo 有三面正向判準、**反向零判準**，而 R81 交棒書 §3.2 那句假話正好長這個形狀）。🔴 **判部分而非達成**：該桶多數形態（rc 讀數、inline 指令字串、口頭宣稱）**永遠不會變成 repo 檔案**，靜態掃描器結構上看不到，新鎖只覆蓋「寫進 `.md` 的否定存在宣稱」這一角。🔴 **百分比不得引用為常數**（分群是關鍵詞啟發式，量級穩健、小數不穩健） |
| **Q5** | 各專家挖深＋Developer 清技術債 | 未結列**真降**（非搬帳）：新增列數 < 結案列數 | **達成（方向對，水位仍高）** `[本包實測]`。`& $p "$r\tools\check_defect_log_crossref.py" --unresolved-count` → **未結列數＝70／全部 144 列｜warn=86 fail=98**（rc=0）；開場 85 ⇒ **淨降 15**。🔴 **本包自己的貢獻是 −0／+1**（為帳本稽核第 a 筆立了 `DEF-101-995`），誠實記在這裡、不藏進那個好看的 15。🔴 **誠實劃界**：85 筆裡有 **61 筆未逐筆到磁碟複驗**（以帳本自述的解鎖條件＋既有證據檔判讀）`[他包回報]` |
| **Q6-a** | 額度水位一律用 **%**，不是固定量 | 取數管道必須可重跑，且要能說出它取的是哪一種計費口徑 | **達成（且本輪推翻了一個更根本的前提）** `[他包回報]`。取數走**權威來源**：server 依帳號方案自己算 utilization 並回百分比 ⇒ 本 repo 不再擁有「分母」這個概念，換帳號／換方案／換機器零常數要改。🔴 R81 那道「四個候選分母都湊得出同一個百分比」是**偽命題**——等式本來就不成立（池跨產品共享、訂閱模式完全不走 token 桶） |
| **Q6-b** | **80%** 少派 agent、**95%** 停止並準備喚醒；並依 `(usage%, 距 reset 幾分鐘)` 決定可派 agent 數 | 兩道各自的**動作**要真的發生，不是印一行字；6b 演算法的輸入二元組必須真的拿得到。🔴 **門檻本身的驗收拆成兩半，缺一不得判達成**（R82 收尾依掌舵者明示補寫；此前這條讀起來像「交付一組固定門檻即可」，那與訴求相反）：**(一) 參數化**——`80`／`95`／`50`／`70` 這些數值不得是程式碼裡的常數，必須由 `AutoClaude/.env`（真正生效的那一份，已 gitignored）驅動；`.env.example` **只是範例、不是生效來源**，判準要能分辨這兩者。**(二) 最佳化**——`.env.example` 印出來的那組值是**起點不是答案**，驗收要問「這組值是照什麼量測調出來的」，答不出來就只算完成 (一)。⇒ 本輪只可能宣稱 (一) 的進度；(二) 需要一段真實用量的觀察期，列入 R83 | 🔴 **本輪最大的單一修復，也是本輪最大的一次自我推翻**。掌舵者當場抓到：`worst()` 跨桶取 pct 最大值當「額度水位」，與 reset 期程無關 ⇒ **兩個方向都會錯**（該加速時減速／該停時不停），且伺服器給的 per-bucket `resets_at` 在正規化時被丟掉 ⇒ **6b 的演算法結構上算不出來**。⇒ **R81 宣稱 6b 落地，那個宣稱在這一點上不成立。** 已落地 `[他包回報]`：`tools/lib/quota_policy.py` 純函式判讀層（count_loc **391／400**）＋ `tools/lib/quota_gate.py` 接線 ＋ meter schema `/2` 吐 `axes[]`（每格帶 `resets_at`／`group`）。本包複驗 `[本包實測]`：於 `tools/tests/` 跑 `& $p -m unittest test_quota_policy` → **rc=0，Ran 90 tests，OK**；`test_context_budget_guard` → **rc=0，Ran 273 tests，OK**。🔴 **三鏡對抗複驗全 FAIL、抓到 10 筆**，含「多軸下加速訊號被跨軸 `min` 吃掉」＝**本案要治的病在治它的新程式碼裡復發**；收斂後錨點「0%＋30m ⇒ 多派」由 `{4}` 回到 `{16, 8, 4}` |
| **Q6-c** | 「Token 盡量不要用盡就要停止任務，**記錄所有狀態**」 | 停止＝扇出被機械擋下（`exit 2`，不是印字）；記錄＝任務書落磁碟且含四項 | **部分達成，且本輪首次有實戰證據** `[他包回報]`：節流守衛在本輪實戰中**擋下過主控自己的扇出呼叫**，訊息逐軸列出 %／剩餘分鐘／band／cap ⇒ 「真的擋得下來」這一半今天有事實而不只有設計。🔴 **未做的那一半照實記**：任務書裡的死者清單是**給人讀的交棒單**，不是可直接回放的 handle ⇒ 「記錄所有狀態」今天記得到「哪幾個 run 死了」，記不到「怎麼自動接回去」 |
| **Q6-d** | 「Token 用盡時，下一輪 Reset 要能**喚起任務繼續執行**」 | 憑證＝`NextRunTime` 這個**值**（不是 rc）；且「喚起後真的繼續」要與「喚起了但什麼都沒做」可區分 | **未達成（半自動，與 R81 同）** `[他包回報]`。喚起那一段成立（SessionStart 預防性武裝＋巡邏＋逐字稿撞線偵測，巡邏零 token）；**繼續執行那一段仍斷在跨 session**（`resumeFromRunId` 是 same-session only）。🔴 本輪另揭一個更根本的問題：那支哨兵**自己就是 P7 彈窗的真凶**（見 P7 列）⇒ 「喚起機制」在被修好之前，先製造了一個使用者三度回報的缺陷 |
| **Q6-e** | 「同 session／同 token 池／單次上限 1 小時／Reset 五小時，**要如何度過五小時**（可否每 50 分鐘喚醒）」 | 先驗證「五小時」這個前提本身；採不採用要給**量出來的**理由 | **已答覆（沿用 R81 結論，本輪未重量）** `[史料]`：①**沒有「五小時」這回事**——15 個相異撞線 episode 的實際停機 median 59.8 分／max 253.2 分，**超過 5 小時者 0 個**；②不採用每 50 分鐘（它把最壞死等由 15 分放大到 50 分而換不到任何東西，且斷電期間它自己也會被擋）；③**正確答案是「不要走到需要度過五小時」**——在 95% 主動收斂。🔴 重量入口＝`& $p "$r\tools\probe\reset_window_distribution.py"`，**本輪未跑**，上列數字一律不得當常數 |
| **Q6-f** | 「搜尋最新前沿 AI Agent 如何設計來參考」 | 只寫從本輪量測**直接推得**的結構結論，不寫二手轉述；做不到就明說 | **本輪未做**（R81 曾做過一次概略搜尋並自標「背景不是決策依據」）。🔴 照實寫未做，不拿 R81 的那一次充數 |
| **M** | 成熟度 M1~M6（判準 SSOT＝`docs/06_quality/CrossPlatform_Maturity_Criteria.md`） | 逐條實測，附量測載具與當輪 rc | **本包未逐條重跑，故不填達標欄** `[本包實測]`：M1／M5／M6 的量測載具都需要跑根層 unittest 全套（>10 分鐘），而本輪護欄層棘輪**本來就是紅的**（等程式面重釘），在那之前跑出來的 rc 沒有歸因價值。🔴 **可確定的只有一件事：本輪一格都沒動**——M1 的護欄層行數**繼續成長**（+3510）⇒ 「連續三輪不上升」的計時**又一次從本輪重新起算**；M4 的門檻是「一輪內 0 筆散文與實作不符」，而本輪光是本包就抓到 3 筆（P7 那句、`DEF-101-308` 的理由、`ledger_rotation` 的取值紀律）。**不得把「沒重跑」寫成「維持」** |
| **AC** | AutoClaude 三問題：(a) 舵手必須被喚醒回來／(b) example 跑不動／(c) 關不掉的通知 | 每一問各自指出今天卡在哪一行 | 🔴 **三問的狀態不同，本格不再用一句「已定位，未修」蓋過去**（R82 收尾訂正：該措辭與磁碟不符——工作樹顯示 (b)(c) 確實改了）。**(a) 已定位、未修** `[他包回報]`：AutoClaude 側**沒有**任何 OS 級喚醒機制，只有行程內 `sleep`；行程一死就沒有人會回來。**(b) 開箱阻塞已排除，但尚未端到端實跑** `[本包實測，2026-08-09]`：`claude.extra_args` 的非法旗標已修（`AutoClaude/config.yaml` 由 `- "--yes"` 改為 `[]`，並在註解裡給出實測存在的替代旗標）／T01 要讀的規格檔已建（`AutoClaude/scripts/example_workspace/docs/sdd_auth_spec.md`，`Test-Path` → **True**）／playbook 的產出路徑全部改落 `scripts/example_workspace/` 以免污染引擎自己的 `tests/`／`autoclaude_pg` 容器現查 **Up 2 days (healthy)**。🔴 **誠實降級**：以上是「開箱不再第一秒就死」的證據，**不是**「它跑得完」的證據——本輪**沒有**做過一次端到端實跑（真跑會呼叫 Claude Code 寫檔並燒額度），故 `run_id`／逐步 rc 一律不存在。**端到端實跑列入 R83 交棒**（見 §「交棒」）。**(c) 已修** `[本包實測]`：出廠預設改為不彈窗（`notification.enabled: false`，臨時想看設 `AUTOCLAUDE_DESKTOP_NOTIFY=1`），彈窗停留秒數由硬編 10 改為 `duration_seconds` 且預設 3。🔴 **更重要的一筆（未修）**：AutoClaude 既有 Token Guard 的 80／90 兩道門**分母是 context window 不是帳號額度** ⇒ 訴求 6a 在 AutoClaude 側**完全未滿足**，而「名字像有人守」比沒守更危險 |
| **S3** | pytest skipped 徹底解決、全部可測 | 逐筆分類；(a) 環境未啟用類**歸零**；判準是「逐支問它為什麼不跑、**然後真的讓它跑一次**」 | **部分達成（診斷完整、修復未完）** `[他包回報]`。三棵樹基線：根層 `tools/tests` 2600／**41 skipped**；AutoClaude `tests/` 4222 passed／**37 skipped**；AISDLC_SDD v0.30 `fsm_runtime` 1746 passed／**6 skipped**（這一棵**完全不在 skip 治理射程內**）。🔴 本輪最有價值的一步是**真的讓它跑一次**——這一步直接推翻**三處 reason 的自我宣稱**：84 支裡約 **21 支是假的結構型**（載具解錯／環境變數沒設，補上就真的會跑，已逐一實測綠）、11 支已實測在 Linux 容器內全綠、**27 支只有 macOS 跑得到而 macOS CI 自 2026-08-05 起 8 連跑一步都沒執行**。⇒ (a) 歸零**未達成** |
| **L** | 缺陷帳本容量 | 未結列不得越 warn 線；主檔體積不得越 fail 線 | **達成（本輪唯一無條件達成的一列）** `[本包實測]`：未結列 85 → **70**（warn 86、fail 98 ⇒ 距 warn 尚有 16 筆餘裕，R81 收輪時只剩 2 筆）；`& $p "$r\tools\check_defect_log_crossref.py"` 全套當回合 **rc=0**（本包三處帳本編輯後複驗）。🔴 **但這一格的「達成」有代價**：85 筆裡有 **62 筆結構上結不掉**——它們與 `tools/lib` 三條逐字相等基線硬耦合，追加一句改派就會讓基線失準、閘門當場紅 ⇒ 只持有帳本的包做不到（已立 `DEF-101-992`，承接 R83） |
| **P7** | 哨兵／hook 彈窗（掌舵者注意事項 7，**三度回報**） | 驗收要正負兩面一起看；「不彈窗」不算通過 | 🔴 **已修（三面），且真凶與所有人的假設都不同** `[他包回報]`。**真凶是本 repo 自己的額度哨兵，不是 AutoClaude**：`tools/lib/quota_escalation.py` 的 `notify()` 走 Windows `msg.exe * /TIME:600`（**模態**），而 `tools/session_resume_planner.py` 在「逐字稿不存在」時**硬編** `escalate`。**根因**：SessionStart **無條件**武裝哨兵，但**開了沒做事就結束的 session 不會產生 `.jsonl`** ⇒ **一次正常結束換來一個模態彈窗**。歸因憑證＝使用者截圖的畫面逐字與該分支的 `reason` 字串完全一致；殘骸規模＝`%TEMP%` 內 **26 份**任務書。已三面修：判定改 **silent disarm**／武裝面收緊前提／載具改非模態。🔴 **本列刻意不宣稱「不閃窗了」**——那個螢幕表徵與守衛靜默失效完全相同（§4 最後一條） |

---

## §2 本輪方法（沿用 R81，兩點新增）

1. **多維並行深掃 → 每筆發現派獨立懷疑者反駁**；懷疑者預設「它是假的」，不確定時傾向 `is_real=false`。
2. **掃描階段全程只讀不改**（並行改樹造成假紅，本 repo 已三次判例）。
3. **修復階段序列化**；收輪閘門一律在**所有包停工後的單人窗口**取得。
4. 🔴 **本輪新增①：三鏡對抗複驗**——對本輪最大的單一新機制（額度政策判讀層）派三個獨立的鏡
   去**打它**而不是驗它。結果：**三鏡全 FAIL**，抓到 10 筆，其中一筆是本案要治的病在新程式碼裡復發。
   ⇒ **閘門全綠不能替代對抗式複驗**，這一輪有了新的實證。
5. 🔴 **本輪新增②：扇出寬度由本 repo 自己的節流守衛節制**（不再是主控單方面的決定），
   且它在實戰中擋下過主控自己的呼叫。⇒ 「本輪為什麼只派了這幾個包」從此是一個**可查的量**。

---

## §3 本輪已落地的包（**未經本包複驗者一律標明**）

| 包 | 對應訴求 | 落地物 | 複驗指令與 rc | 複驗者 |
|---|---|---|---|---|
| 額度政策判讀層 | **Q6-a／Q6-b** | `tools/lib/quota_policy.py`（純函式，count_loc 391／400）＋ `tools/lib/quota_gate.py` 接線 ＋ meter schema `/2` 的 `axes[]` | 於 `tools/tests/`：`& $p -m unittest test_quota_policy` → **rc=0，Ran 90 tests，OK**；`test_context_budget_guard` → **rc=0，Ran 273 tests，OK** | **本包當回合實測** |
| 哨兵三面修 | **P7／Q6-d** | 判定改 silent disarm／武裝面收緊／載具非模態 | 本包**未重跑**（實作面在 `tools/lib/` 與 planner，非本包持有面） | `[他包回報]` |
| ADR-XPLAT-002 §6 補 R82 列 | **Q1** | 逐輪覆蓋表補上 R82 那一列（平台／雲端 CI／未覆蓋面三欄） | 於 `tools/tests/`：`& $p -m unittest test_adr_xplat001_c1c2_lock` → 失敗數由 **14 降到 3**，**11 條 SC-\* 全綠**（SC-10 缺列＋10 條被連帶打紅的零串音斷言）；殘餘 3 條屬 `TestGuardLayerRatchet`／`TestShrinkOnlyRatchet`，需程式面重釘 | **本包當回合實測** |
| 帳本對抗稽核四筆收斂 | **Q5／L** | (a) 立 `DEF-101-995`；(b) 兩處壞檔名指針修畢；(c) `DEF-101-308` 換理由不改狀態；(d) §8 標題不再寫死條數＋補完整性註記 | `& $p "$r\tools\check_defect_log_crossref.py"` → **rc=0**（三處帳本編輯後）；`--unresolved-count` → **70／144** | **本包當回合實測** |
| 過期數字訂正 | **Q2** | `lint-imports` 的 `8 kept` 三處全過期（實測 **9 kept**，R82 新增 `no-harness-import`）⇒ 三處一律改指 SSOT `AutoClaude/.importlinter`；ONBOARDING live 基線回填 | `lint-imports` → **rc=0，逐字 `Contracts: 9 kept, 0 broken.`**；`& $p "$r\tools\sync_onboarding_baselines.py" --check` → **rc=0** | **本包當回合實測** |

> 🔴 **本表刻意不替沒複驗過的包寫「已通過」。** 本包持有面只有 `.md`，凡程式面結論一律標 `[他包回報]`。

<!-- guard-total:R82 --> **本輪護欄層累積淨額＝ 68423 → 73823（+5400）** `[最終收尾者當回合實測]`
（`& $p "$r\tools\tests\test_adr_xplat001_c1c2_lock.py" --print-guard-lines` 當回合 rc=0；
本輪重釘兩次：程式面收尾者 +3675、最終收尾者 +1668，兩列理由皆在稽核痕跡表內）。
逐檔清單的家＝`docs/06_quality/CrossPlatform_R82_Scan_Findings.md` §B。
🔴 本輪走的是款(9) 的**登記手續**（`[非淨減法輪]`），依 §4 禁止事項第一條，**Q2 一律判未達成**
——登記不是及格線。🔴 **生產側同樣不得寫成淨減**：`git diff` 讀出的 −151 是假的（看不到 untracked 檔），
把三個新檔算進來是 **+1278 raw**，逐檔證據見 findings §D。

---

## §4 禁止事項（沿用 R81 §4，加一條）

- ❌ 不准為了讓數字好看而調高任何門檻／棘輪／體積上限。合法出口只有兩條：①同一次變更內刪等量以上的行；
  ②走款(9) 的**登記手續**。② **不是**「也算及格」，凡走 ② 者 Q2 判未達成。
- ❌ 不准 `--no-verify`／`AUTOCLAUDE_SKIP_HOOKS=1`／跳過或註解掉失敗測試。
- ❌ 不准把「已通過／已驗證／零損失」寫進任何文件，除非同一則回覆貼得出當回合真跑的輸出。
- ❌ 不准在 Windows 用 Bash 工具；不准裸 `cd`／`Set-Location` 帶相對路徑；讀 rc 不接管線。
- ❌ 不准在多 agent 並行期間宣稱「全套閘門 rc=0」。
- ❌ 不准以「act／Docker 本機全綠」代替 mac 真機結論——act 跑的是 ubuntu，**Linux 綠不蘊含 mac 綠**。
- ❌ 不准把「hook 不閃窗了」當成 P7 的驗收通過（exec form 載具解析不到時 CC fail-open，
  六支守衛靜默失效的螢幕表徵與修好完全相同）。
- ❌ 🔴 **本輪新增**：不准用 `git diff` 的行數當「本輪淨額」——**它看不到 untracked 檔**，
  而新檔正是本輪最大的產出。任何淨額宣稱必須同時交出 `git status --porcelain` 的未追蹤面。

---

## §5 交棒（收輪時填）

> 承接檔名＝ docs/04_planning/R82_HANDOFF.md（**本檔寫成時尚未建立**，故刻意不以反引號寫成既有路徑）。
> 內容至少含四項：①本輪已驗證什麼（附實測數字與 rc）／②還沒做什麼／③下一步的**確切指令**／④禁止事項。

### §5.1 本包（文件面）留給程式面收尾者的具名事項

| # | 事項 | 為什麼本包做不到 |
|---|---|---|
| H-1 | 把 `docs/06_quality/CrossPlatform_R82_Scan_Findings.md` 登記進 `tools/lib/governance_docs.py` 的 `_GOVERNANCE_DOCS` | 該檔在 `tools/lib/`，不在文件包持有面。🔴 **這一筆現在是紅的**（`check_defect_log_crossref.py` rc=1 且**早退，尚有 12 道檢查未執行**）。**不要用改名繞開**——`docs/06_quality/CrossPlatform_R82_Ledger_Closure.md` 檔頭逐字記著那次判例 |
| H-2 | 重釘 `_FROZEN_GUARD_LINES` ＋ 補 `_GUARD_LINES_REPIN_LOG` 一列（含 `[非淨減法輪]` ＋ 指名 findings §B），並**同步本檔與 findings 的 `guard-total:` 標記兩行**（本輪輪號；此處刻意不逐字寫出「標記＋輪號」的完整字面——那個字面就是判準的觸發條件，寫在待辦列上會讓一列敘述文字被當成一筆「引用現行總量」的宣稱而判 `[形態不符]`，當回合實測過） | 同上；且重釘紀律要求在所有包停工後做一次 |
| H-3 | `DEF-101-995`：`tools/lib/ledger_rotation.py` 的 `ratchet_history_problems()` 前綴不可變判準 | 同上；探針四組對照與修法方向見 findings §F |
| H-4 | `tools/tests/test_context_budget_guard.py` 三個幽靈符號引用（`TestR78GhostSymbolClaims` 當回合紅） | 該檔不在文件包持有面 |
| H-5 | ~~`docs/04_planning/R81_HANDOFF.md` §3.2 的假宣稱訂正~~ **已由「散文 ↔ 磁碟」收斂包做掉**（2026-08-09）：§3.2 二次訂正（錨由單一檔名改成掃 hook＋`tools/` 整個活躍 Python 面找符號）＋現查指令移到條目**之前**（區塊語意所需）。複驗：於 `tools/tests/` 跑 `& $p -m unittest test_doc_loc_baseline_freshness_r60.TestR78HandoffClaimsCarryLiveCommands` → **rc=0，Ran 7 tests，OK**；`& $p -m unittest test_negative_existence_claims_r82` → **rc=0，Ran 12 tests，OK** | （已完成，保留列以免下一輪重做） |
| H-6 | `docs/AISDLC_Agent_UserGuide.md` 與 `docs/myPrompt.md` 內殘留的 `8 kept` | 同上（本包清單只涵蓋根 `CLAUDE.md` 與 `ONBOARDING.md` 兩處） |

### §5.2 R83 的第一動作（依既有紀律）

1. **切 mac 真機**——本輪 mac 面 100% 是靜態推論，findings §E 五格逐一等一次真機裁決
   （其中 MAC-05 只需要在 mac 上跑一行 `date +%s%N`，0 成本）。
2. **先結列再開新戰場**——未結列 70，距 warn 86 有 16 筆餘裕，但 `DEF-101-992` 記載的
   62 筆硬耦合仍在，那才是真正的堵點。
3. **重跑而不是引用**：本檔 Q4 的桶筆數、Q6-e 的 episode 分佈都是會漂移的量測值。
4. 🔴 **AutoClaude example 的端到端實跑**（R82 收尾新增）——本輪只做到「開箱阻塞已排除」
   （見 AC 列的三項憑證），**沒有**任何一次真跑，故 `run_id`／逐步 rc 全不存在。
   R83 要跑一次並把 `run_id` 與逐步 rc 落進證據檔；真跑會呼叫 Claude Code 寫檔並燒額度，
   所以要先確認 `AutoClaude/config.yaml` 的 `claude.extra_args` 已填成實測存在的旗標
   （出廠是空清單，`claude -p` 非互動模式下沒有提示可答）。
5. 🔴 **`tools/lib/quota_policy.py` 檔頭那個加速範例對它自己舉的例子不成立**（R82 收尾實測，
   逐格數字與復現指令見 findings §G）。本輪**刻意只記誠實劃界、不動程式碼**（該檔有並行包在改）。
   R83 二擇一：把檔頭那段改寫成與實測一致，或調整 `rec` 的聚合讓那句話為真——
   **兩者都可以，但不可以繼續讓散文與行為不符**。
6. 🔴 **額度門檻的「最佳化」那一半**（Q6-b 驗收判準 (二)）——本輪只可能完成參數化；
   要判達成必須說得出「這組值是照什麼量測調出來的」，那需要一段真實用量的觀察期。

---

## §6 收輪產物

- `docs/06_quality/CrossPlatform_R82_Scan_Findings.md` — 六面掃描 80 筆、護欄層逐檔清單（§B）、
  舵手親查四筆（§C）、Q2 誠實帳（§D）、mac 就緒度（§E）、帳本稽核探針（§F）、誠實劃界（§G）
- `docs/06_quality/CrossPlatform_R82_Ledger_Closure.md` — 帳本清債逐列處置＋§10 收尾對抗稽核四筆
- `docs/06_quality/AutoSDD_Defect_Log.md` — 本輪缺陷列（單列 ≤ 700 bytes，詳情進具名證據檔）
- docs/04_planning/R82_HANDOFF.md — 交棒 R83（**尚未建立**）
- docs/06_quality/CrossPlatform_R82_Review.md — 四方複審結論（**本輪尚未跑四方複審**）

> 🔴 **最後兩份刻意不以反引號寫成既有路徑**——它們是本輪的**產出目標**，不是既有檔案。
> 本 repo 對「以反引號寫出不存在的路徑」有機械物在守（`tools/tests/test_doc_loc_baseline_freshness_r60.py`）。
