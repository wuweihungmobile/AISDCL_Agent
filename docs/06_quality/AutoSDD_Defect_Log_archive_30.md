# AutoSDD Defect Log — Archive 30

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **41 筆已結列**，於 R60 **動工前**（2026-07-28）搬遷。
>
> **為何在動工前搬、且一次搬足**：R59 收尾把主檔留在 **256,073 bytes**，距 256KB（262,144）fail 硬閘僅剩 **6,071 bytes**，並在 `archive_29` 標頭明文交棒「**R60 動工前務必先歸檔**」。歷輪反覆撞閘（R56 一輪兩次、R57 一輪三次、R59 一輪兩次）的成因始終同一個：**只釋出「剛好夠退出 warn」的量**，於是本輪毛增（20~45KB）必然把壓力轉回同輪或下一輪。本次改為**一次搬足全部符合判準的已結列**，把主檔壓到 `256073 → 174447` bytes（釋出 **81626**）。⚠️ **本句原接一段餘裕預測（「使本輪即使毛增 45KB 仍不觸及 240KB warn 帶，單輪內不需要第二次歸檔」），該推算已被同輪自己證偽——原句與訂正見下方 🔴 揭露段第 4 項**（處置比照 R59 SA-R59-P2-1 對已證偽定性宣稱的慣例：不刪原文、原地指向訂正處）。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（R60 新建，程式即判準）。本次實際套用的是該工具的判準①②③：① 狀態分類 ∈ {`fixed`, `wontfix`, `closed-by-decision`}；② 狀態欄**不含** `open`／`routed`／`deferred`／`watch`／`workaround` 任一活躍字樣——**以 ASCII 邊界 lookaround 判定，不是子字串包含**（照字面子字串執行會把狀態欄含 `OpenMutexW` 的 `DEF-101-504` 誤擋，見該工具 docstring）；③ **不在**「四份 crossref 掃描目標對其做出可辨識狀態宣稱」的 ID 集合內——此集合以閘門 `tools/check_defect_log_crossref.py` 自身的 `_CLAIM_RE` ＋ `_classify` ＋ `_ID_RE` 實算（本次實算結果為 **16 個 ID**）。
>
> 🔴 **本次歸檔自身有四項缺陷，於同輪被四方掃描／四方複審抓出後修正（如實記載，不美化為一次做對）**：前三項由同輪七維掃描抓出，**第 4 項由 round 1 四方複審（SA-R60-06）抓出**——原本這段只寫「三項」，本身就是一次漏列。
>
> 1. **「有一支機械檢查腳本」的宣稱當時是空的**。本標頭初稿（與 `archive_29` 標頭）都寫「由歸檔腳本以閘門自身邏輯機械檢查、逐筆顯式 assert」，但那支腳本只存在於某次 session 的暫存目錄，**repo 內零載具、不可重跑**；而且散文所載的判準與實際執行的判準並非同一個東西（見上段②的邊界問題）。**宣稱一道機械檢查存在、而它不可重跑，等同沒有檢查**。R60 已把它落地為 `tools/archive_defect_log.py` ＋ `tools/tests/test_archive_defect_log.py`，往後歸檔一律走該工具。
> 2. **判準缺第四項：只掃狀態欄，對散文裡的活 backlog 全盲**。本次因此把 `DEF-101-517`（散文明寫「下一輪先評估這兩條較便宜的路徑」）與 `DEF-101-526`（散文明寫「**R60 候選**」）當一般已結列搬走，**主檔零承接者**。新工具補上判準④（散文交棒偵測），命中者不自動搬、須以 `--ack-handoff <ID>` 逐筆具名承認——刻意設計成「需具名承認」而非硬擋，因為硬擋會讓帳本永遠搬不動（歷史已結列的散文常提到 backlog），而完全不管就是本次踩到的靜默埋葬。**那兩筆 backlog 已於 R60 本輪實際做掉並在主檔立新條目承接**（見 DEF-101-529（現居 archive_33）／見 DEF-101-530（現居 archive_33））。
> 3. **行尾被靜默改壞**。臨時腳本用 `Path.write_text()` 寫檔，其 `newline` 預設為 `None`，在 Windows 上把 LF 譯成 `os.linesep`（CRLF），於是主檔與本檔從 `w/lf` 變成 `w/crlf`、與 `.gitattributes` 宣告的 `eol=lf` 相反，**還讓「位元組總量守恆」那項自我斷言在最終產物上按字面重驗為假**（多出的位元組正是每行一個 CR）。已正規化回 LF，新工具一律走 bytes 層寫入並在落地後 assert 磁碟實體零 CR，且 `--check` 會稽核整個帳本家族的行尾。
> 4. **本標頭自己做了它同一段末尾宣稱「不做」的事：對餘裕下定性宣稱，而且當輪就被推翻**。原句逐字：「把主檔壓到 `256073 → 174447` bytes（釋出 81626），**使本輪即使毛增 45KB 仍不觸及 240KB warn 帶，單輪內不需要第二次歸檔**」。同輪的 `CrossPlatform_R60_Fix_Evidence.md:6` 已逐字證偽：第一版把六個修復包的完整證據灌進帳本表格列，28 列共約 **99.5KB**（毛增約為預測的兩倍），主檔當場由 176,296 衝到 **275,979 bytes**、**直接撞破 262,144 fail 硬閘**。而本標頭末段（`位元組變化` 段）自己寫著「本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被自己推翻）」——:5 做的正好是它宣稱自己不做的事，且真的在同輪被自己推翻。**結論僥倖仍成立（round 2 實測主檔仍 < 245,760 warn 線）純粹是因為緊急改成兩層外移（列精簡 ＋ 完整證據移入 `CrossPlatform_R60_Fix_Evidence.md`），不是因為 :5 的推算正確。**訂正手段：:5 原句就地標 ⚠️ 並指向本項（不刪原文，比照 R59 慣例）；教訓與第 1 項同型——**帳本標頭只該寫「怎麼量」，不該寫「量出來會是多少」**。見 DEF-101-558（現居 archive_33）。
>
> 🔴 **本次刻意納入 R59 自身條目（判準③之「本輪保留」不再適用）**：`archive_29` 曾把 `DEF-101-502`~`520` 留主檔「供下輪對帳」，**R60 就是那個下輪**。本輪的 Scan-G（backlog 接續稽核）已把 R59 全部條目納入稽核輸入，對帳鏈閉合；且搬遷非刪除，四方複審與後續輪次查 R59 條目一律來本檔（主檔仍留 R59 全部 `open`／`deferred`／`routed` 活躍列，live 追蹤不需讀 archive）。**仍留主檔的 R59 活躍列**即為 Scan-G 的真正稽核標的。
>
> **搬遷清單（41 筆）**：`DEF-101-225`、`DEF-101-262`、`DEF-101-265`、`DEF-101-289`、`DEF-101-300`、`DEF-101-361`、`DEF-101-374`、`DEF-101-375`、`DEF-101-376`、`DEF-101-390`、`DEF-101-394`、`DEF-101-395`、`DEF-101-396`、`DEF-101-408`、`DEF-101-413`、`DEF-101-426`、`DEF-101-429`、`DEF-101-430`、`DEF-101-437`、`DEF-101-502`、`DEF-101-503`、`DEF-101-504`、`DEF-101-505`、`DEF-101-506`、`DEF-101-509`、`DEF-101-510`、`DEF-101-511`、`DEF-101-512`、`DEF-101-513`、`DEF-101-514`、`DEF-101-515`、`DEF-101-516`、`DEF-101-517`、`DEF-101-507`、`DEF-101-508`、`DEF-101-519`、`DEF-101-520`、`DEF-101-522`、`DEF-101-523`、`DEF-101-525`、`DEF-101-526`
>
> **位元組變化**：主檔 256073 → 174447（釋出 81626）。餘裕請一律以 `python tools/check_defect_log_crossref.py` 印出的 warn／pass 訊號為權威，本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被自己推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-225 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`run_local_nightly.sh` RunId log 缺觸發來源記錄 + 去重 check-then-act 無鎖（TOCTOU）**：本 repo 真實 log 顯示 2026-07-21 同日出現兩輪完整 PASS=4 執行且無 skip 訊息，因記錄不完整無法判斷是合理手動重跑還是真正去重漏洞；且 launchd `RunAtLoad` 與 `StartCalendarInterval(02:00)` 兩觸發源時間重疊時，兩行程可能同時通過「今日尚未有心跳」檢查，導致重複執行整套 nightly gate | P2 | BEGIN 行加印觸發來源；去重檢查外包檔案鎖 | fixed@R16：`TRIGGER_SRC` 判定（--force/`XPC_SERVICE_NAME`/互動終端/未知四態）寫入 BEGIN 行；本機無 `flock`，改用 POSIX `mkdir` atomic lock，陳舊鎖以 PID 存活判斷清除（比照 `dev_start.py` `_acquire_bootstrap_lock()`），`trap EXIT` 保證釋放。驗證：兩行程近乎同時呼叫，第二個正確被擋＋印跳過訊息；陳舊死 PID 鎖正確清除重取；三種觸發來源分支獨立驗證正確 |
| DEF-101-262 | 2026-07-22 | R23 四方前置掃描（P3/P4）：ruff 存量 baseline 數字新鮮度掃描 | `docs/06_quality/AutoSDD_Defect_Log.md`（DEF-101-018 條目）、`ONBOARDING.md` §9 表尾對照段（約第 260 行）、`AutoClaude/pyproject.toml:32` 行內註解三處皆寫「baseline 1,339 筆」，但本輪於 `AutoClaude/` 下實跑 `.venv/Scripts/python.exe -m ruff check . --statistics`（鎖版 0.15.21）實測得 **1,147** 筆——純描述性文字漂移（`grep -rn "1339"` 只命中這三處敘述文字，無任何測試/gate 拿 1,339 當機械斷言比對） | P3～P4（純文件數字新鮮度；無機械斷言受影響） | 三處皆更新為實測數字，並註明「R23 複審實測更新，原數字為舊測值」避免未來誤以為機械釘選 | fixed@R23：`AutoClaude/pyproject.toml:32` 行內註解、`docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-018 列、`ONBOARDING.md` §9 表尾對照段三處皆訂正為「baseline 1,147 筆」並註記 R23 實測來源；`grep -rn "1339\|1147" docs/ ONBOARDING.md AutoClaude/pyproject.toml` 複核三處新數字一致、無殘留舊值 |
| DEF-101-265 | 2026-07-23 | R24 四方前置掃描（P3）：Scan-C CI/CD 與排程機制掃描，`windows-compat-ci.yml` 與 `ONBOARDING.md` 揭露對稱性核對 | **`windows-compat-ci.yml`／`ONBOARDING.md` 未揭露 GitHub-hosted `windows-latest` runner 實際對應 Windows Server 2022（伺服器 SKU）、與宣稱標的「Windows 11」用戶端環境的潛在落差**：mac 側 `ONBOARDING.md`（macos-compat-ci.yml 段落）已有明文揭露句「GitHub-hosted `macos-latest` runner 與開發者個人 Mac 的實際硬體／OS 版本仍可能有落差」，但 Windows 側自稱目標更具體（明確宣稱「Windows 11」而非泛稱作業系統）卻反而沒有對等揭露句，屬揭露不對稱 | P3（文件揭露缺口，不涉程式邏輯；電源管理/排程相關 API 如 `schtasks`／`New-ScheduledTaskSettingsSet` 行為在伺服器 SKU 與用戶端 SKU 間可能有出入，見 DEF-101-002／249） | 於 `ONBOARDING.md` §8 比照 mac 側句式補一段對等揭露 | fixed@R24：`ONBOARDING.md` §8 新增「R24 對稱補揭露」一段，明文揭露 `windows-latest` 現行對應 Windows Server 2022、與開發者個人 Windows 11 用戶端環境仍可能有落差，純文件補強。**一審訂正（R24，QA 一審獨立核實行號後命中）**：原「R10 Windows 側對等補償段落**後**」方向寫反，實際插入位置在 R10 段落**之前**（緊接 R11 補驗紀律段落後），內容本身正確、僅描述方向性筆誤 |
| DEF-101-289 | 2026-07-24 | R32 全面掃描 Scan-D | **`ONBOARDING.md` §7 pytest 基線唯一真相源字面數字已落後實測 +11（3,608→3,619，R27~R31 累積新增測試未回填）**：與 DEF-101-266（曾因 +1 落差記為 P4）同一根因、同一機制（無自動同步機制），僅這次落差擴大。R31 commit（`009c5fe`）本身的最終回歸驗證結果已寫明 3619 passed，但未同步觸碰 ONBOARDING.md | P3 | 重新量測並更新 §7 數字 | fixed@R33：R33 Scan-D 在**全新臨時目錄**建立乾淨 venv（`pip install -e '.[dev,notifications]'`，未裝 postgres/pgvector 選配，`pip list` 確認乾淨），巢狀 session 下實測 **3,644 passed / 210 skipped**；與 R32 污染值（3742/146）交叉比對 skip 數落差方向吻合污染假說，判定本次數字可信。`ONBOARDING.md` §7 已同步更新為此數字並訂正輪次標記為 R33（`bootstrap 出廠環境`〔非巢狀 session〕分項數字不在本次驗證範圍，維持原「待重驗」標示） |
| DEF-101-300 | 2026-07-24 | R34 Architect 架構深度評估 | **`tools/dev_start.ps1` 的 WindowsApps 空殼別名排除 guard（第 38-41 行）是第 4 個獨立實作**（與 `tools/bootstrap.ps1`、`tools/bootstrap_core.py::_is_windows_apps_stub()` 同規則、各自獨立寫），且**零測試覆蓋**——`tools/tests/test_dev_start.py` 只測 Python 核心，`check_wrapper_thinness.py` 只釘 SHA256 不驗語意，屬 DEF-101-273/279/281 同一缺陷類別第 4 次出現的結構性復發風險 | P2（同一缺陷類別的結構性復發；三處目前內容一致，但下次任一處改動不會觸發交叉告警） | 比照 DEF-101-295 手法新增交叉一致性測試鎖（不合併程式碼——`dev_start.ps1` 需在呼叫 Python 核心「之前」自行找到直譯器，屬 bootstrapping 先有雞或蛋的架構限制） | fixed@R34：新增 `tools/tests/test_windowsapps_guard_cross_consistency.py`（4 case：3 個靜態文字擷取交叉核對三處 guard 語意一致 + 1 個 dev_start.ps1 行為回歸鎖，比照 `test_bootstrap_ps1.py` 手法，複製到零 `.venv` 臨時目錄執行避免真實 `.venv` 短路 guard 分支）。bug-injection 驗證：拔掉 guard 後兩個靜態測試立即抓到（如實記載已知侷限：behavioral 測試在 macOS `pwsh` 上因 `Get-Command` 不做 Windows PATHEXT 解析而對此特定 bug 無鑑別力，只在真 Windows PowerShell/`windows-compat-ci.yml` 上才有效——`test_bootstrap_ps1.py` 同款既有測試亦有此侷限，非本輪新增問題）。**自我發現的連鎖回歸**：新測試檔以路徑直接引用 `tools/dev_start.ps1`，`AISDLC_SDD ci-gate.sh` 全套回歸時 `test_ci_paths_cover_root_consumers.py` 機械攔下 `macos-compat-ci.yml` paths 未涵蓋此消費檔（DEF-101-042 同構，比照 R27 `tools/bootstrap.ps1` 同款前例），已於 push/PR 兩份 paths 補上一行修復，10/10 passed |
| DEF-101-361 | 2026-07-25 | R44 ActiveFix 步驟 4（同批掃描找出的 LATEST／可修改版本零可用性判斷缺口） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/arch_fitness/run_self_evolution.sh`／`.ps1` 與 `AISDLC_SDD/scripts/copy_on_evolve.sh` 三支「python 呼叫零可用性判斷」**：前兩者為 LATEST（v0.30）版本，第三支為 monorepo 根層 bash-only 工具（`_SINGLE_SIDED_EXEMPT` 已登記無 `.ps1` 對等），皆裸呼叫 python 未排除 WindowsApps 空殼 | P2 | 本輪修復（LATEST／可修改版本，非凍結基線，不受 Copy-on-Evolve 限制） | fixed@R44：`run_self_evolution.sh` dot-source `tools/lib/windowsapps_guard.sh` 後 `is_real_python_candidate python \|\| exit 5`；`run_self_evolution.ps1` dot-source `tools/lib/WindowsAppsGuard.ps1` 後 `Test-IsRealPython`，找不到 `exit 7`；`copy_on_evolve.sh` 因既有孤立測試 harness（`test_copy_on_evolve.py::_run()` 會把腳本複製到與 monorepo 結構脫鉤的 tmp git repo）會讓 guard 檔案在隔離環境物理不存在，改採本檔既有的「sibling 缺席即優雅略過並 warn」慣例（非直接 fail-loud）、`PYTHON` 環境變數已指定時跳過判斷。驗證：對抗式 bug-injection（`env -i` 清空 PATH 執行 `run_self_evolution.sh` 確認 exit 5；塞入假 WindowsApps 空殼確認未被誤執行；正常 PATH 下 dry-run 正常）；`copy_on_evolve.sh` 在完整佈局（PATH 缺 python 時 exit 1、`PYTHON=` 指定時繞過）與孤立測試 harness（guard 檔案缺席時優雅略過不中止）兩種情境下皆驗證行為正確；`python -m pytest tools/tests/test_windowsapps_guard_bash_parity.py tools/tests/test_windowsapps_guard_cross_consistency.py -q` 47 passed；`cd AISDLC_SDD/AISDLC_SDD_v0.30 && python -m pytest tools/fsm_runtime/tests/ -m "not chaos" -q` 1721 passed/8 skipped（無回歸）；`cd AISDLC_SDD && python -m pytest scripts/tests/ -q` 191 passed/1 skipped（含 `test_copy_on_evolve.py` 11 項） |
| DEF-101-374 | 2026-07-25 | R45 全新掃描 agent 回報，主控 zero-trust 複驗（不預設 agent 結論為真） | **[已複驗為假陽性] 掃描 agent 聲稱 `AutoClaude/tools/scaffold_sprint_section.py:148` `print(skeleton)` 印 emoji（🟡）在 Windows 非 UTF-8 主控台會 `UnicodeEncodeError` 崩潰**：主控直接以 `PYTHONIOENCODING=cp1252 python tools/scaffold_sprint_section.py --sprint 99 --title test --dry-run` 實際重現，結果 exit code 0、正常輸出，未崩潰。根因：該檔 `if __name__ == "__main__":` 區塊已在呼叫 `main()` 前呼叫 `tools/lib/platform_utils.py::init_utf8_streams()`（以 `io.TextIOWrapper` 重新包裝 `sys.stdout`/`sys.stderr` 為強制 UTF-8），只是防護手法與 agent 預期的 `.reconfigure()`／`tools/_stdio_utf8.py` 不同，agent 只檢查了後者兩種手法是否存在便下結論，未實際執行驗證 | P4（agent 誤報，非程式缺陷；記載以防未來輪次重複調查同一假陽性） | 不修復（本來就不是缺陷） | closed-by-decision：主控實測驗證為假陽性，`tools/lib/platform_utils.py::init_utf8_streams()` 保護已足夠，未做任何程式變更 |
| DEF-101-375 | 2026-07-25 | R45 全新掃描 agent 回報，主控 zero-trust 複驗（不預設 agent 結論為真） | **[已複驗為假陽性] 掃描 agent 聲稱 `AutoClaude/tools/c6_staging_validator.py` 用 `logging.basicConfig()`（未用專案 `_EncodingSafeStreamHandler`）記錄 emoji（✅/❌）在 Windows 非 UTF-8 環境會拋未捕捉例外崩潰，且聲稱已「實測驗證」推翻「logging 會靜默吞掉編碼錯誤」的預設假設**：主控獨立以檔案腳本（非 inline -c，避免 shell 轉義干擾）在 `PYTHONIOENCODING=cp1252` 下重現 `logging.basicConfig()` 預設 handler 記錄 emoji，exit code 0、正常執行完畢；進一步用診斷腳本確認 `sys.stderr.errors` 預設為 `backslashreplace`（CPython 對 stderr 的內建預設值，不受 `PYTHONIOENCODING` 覆寫、不分平台），而 `logging.basicConfig()` 未指定 `stream=` 時預設輸出至 `sys.stderr`（非 stdout）——故該檔全部 `logger.*()` 呼叫（含第 281-282／323 行）天生受 CPython 這道預設防護，不會如 agent 所稱崩潰。`_EncodingSafeStreamHandler`（DEF-87-002）要防護的是**明確綁定 `sys.stdout`** 的 handler（stdout 預設 `errors='strict'`，才會真的崩潰），與本檔用預設 stderr handler 是不同情境 | P4（agent 誤報，非程式缺陷；記載以防未來輪次重複調查同一假陽性） | 不修復（本來就不是缺陷） | closed-by-decision：主控實測驗證為假陽性，CPython stderr 預設 `backslashreplace` 已提供防護，未做任何程式變更 |
| DEF-101-376 | 2026-07-25 | R45 Architect 架構複審建議（建議 4：WindowsApps guard regex 加 canary 自我檢查），主控查證既有測試檔內容後發現建議已被既有程式碼滿足 | **[已複驗為既有覆蓋] Architect 建議「`_has_zero_guard_python_call` 等 regex 缺自我檢查 canary fixture」，實查 `tools/tests/test_windowsapps_guard_bash_parity.py::TestHasZeroGuardPythonCall`（6 case）／`TestHasUnmigratedRawCheck`（5 case）與 `tools/tests/test_windowsapps_guard_cross_consistency.py::TestInvokesPythonInPs1`（7 case）／`TestAllPythonInvocationsAreSsotProtected`（7 case）已對這些 regex 做了詳盡的已知會命中/不命中 fixture 字串單元測試**，建議所指的缺口實際不存在——Architect 應為依一般印象推論、未逐檔開啟這些測試檔核對內容細節 | P4（Architect 建議依據不足，非程式缺陷；記載以防未來輪次重複調查） | 不新增（本來就已覆蓋） | closed-by-decision：主控逐檔核對既有 fixture 測試內容確認已充分覆蓋，未新增任何測試 |
| DEF-101-390 | 2026-07-26 | R48 四方複審跨平台掃描（Scan-A／Scan-B／Scan-C） | `AutoClaude/autoclaude/utils/checkpoint_manager.py:129`（修復前）`checkpoint_path()`/`exists()` 繞過檔名淨化，直接用 `self._to_id(playbook_path)` 組檔名，與 `save()`/`load()`/`clear()` 委派 `FileStateRepository._path()`（R47 DEF-101-384 已改用 `_sanitize_log_filename`）不一致——`playbook_path="con.yaml"` 時 `save()` 實際寫入 `_con.checkpoint.json`，`checkpoint_path()` 卻回傳未淨化的 `con.checkpoint.json`，`exists()` 因此對錯檔名判斷、恆回報 `False` | P2（Windows 保留裝置名／NTFS 禁用字元檔名下 `exists()` 誤判，非常態路徑但影響斷點續跑判斷正確性） | 本輪修復：`checkpoint_path()` 改呼叫 SSOT `_sanitize_log_filename`（`autoclaude/utils/logger.py`），`exists()` 委派 `checkpoint_path()` 自動一併修正 | fixed@R48：新增 `tests/test_token_checkpoint.py::TestCheckpointManager::test_checkpoint_path_matches_actual_saved_filename_for_reserved_device_name`／`test_checkpoint_path_matches_actual_saved_filename_for_forbidden_char_stem` 兩條回歸測試；`python -m pytest tests/ -k checkpoint -q` 171 passed / 11 skipped；`ruff check autoclaude/utils/checkpoint_manager.py` all checks passed；clean venv 全量 `python -m pytest tests/ -q` 3669 passed / 210 skipped（較 ONBOARDING.md R47 基線 3667/210 恰好 +2，對應本輪新增之 2 條測試，無退化） |
| DEF-101-394 | 2026-07-26 | R50 全新五維掃描（Scan-C：路徑/編碼/子行程平台語意）＋四方一審 Architect/SA/SD/QA 一致判定必修 | `AutoClaude/autoclaude/evolution/playbook_evolver.py`／`minimax_evolver.py` 的 `_derive_part_a_evaluator()`（各自獨立重複實作）對非 pytest 的 `evaluator_command` 產生 `f"{{ {cmd}; }} \|\| true"`（POSIX brace-grouping＋`\|\|`），此字串經 `execution/evaluator.py::Evaluator.run()` 以 `subprocess.run(shell=True)` 執行——Windows 上呼叫 cmd.exe，該殼不支援 `{ ; }` 分組語法、亦無內建 `true`，必回傳非 0 exit code，使 SPLIT_STEP 演化出的 Part A 評估在 Windows 上恆報失敗，與註解明言「確保不因 Part A 僅涵蓋一半任務而誤報失敗」的設計意圖直接相反；另一同源缺陷：Gap-033 `ENV_INIT_GLOBAL` 步驟 `evaluator_command="python -c 'import fastapi' && echo 'OK'"` 用單引號包 `python -c` 參數，cmd.exe 不視單引號為字串分隔符，會被拆成兩個獨立引數恆語法錯誤。既有測試 `tests/test_gap021_028.py::test_derive_part_a_evaluator_other_cmd` 僅斷言回傳字串含子字串 `"true"`/`"\|\|"`，從未真正執行該字串，對此問題零鑑別力 | P1（Windows 上 SPLIT_STEP 演化 Part A 驗證與 Gap-033 全域注入步驟兩者皆必定失敗，非低機率邊角情境） | 本輪修復：`_derive_part_a_evaluator` 改用 `python -c "import subprocess, base64, sys; subprocess.run(base64.b64decode('<base64>').decode('utf-8'), shell=True); sys.exit(0)"`（base64 包裝原指令避開殼層轉義問題，兩平台皆用 `python -c` 執行、無條件 `sys.exit(0)` 保留原設計語意）；Gap-033 步驟改雙引號 `python -c "import fastapi" && echo OK` | fixed@R50（round 1）：既有測試改為真跑 subprocess 驗證 exit 0 且斷言結果不含 POSIX-only token（`{ `／`; }`／`\|\| true`）；為 MinimaxEvolver 獨立實作補上先前零覆蓋的對等測試；Gap-033 測試新增 evaluator_command 不含單引號斷言＋真跑 import 成功/失敗兩種情境。修復當輪一度連帶新增 4 處 `subprocess.run(text=True)` 缺 `encoding` 參數、違反既有 `tools/tests/test_subprocess_encoding_hygiene.py` 機械鎖（四方二審發現，round 2 補上 `encoding="utf-8", errors="replace"`）。主控複驗：`cd AutoClaude && ../.venv/bin/python -m pytest tests/ -q` 3778 passed／146 skipped（全綠，含 round 3 SSOT 收斂後之最終態，見 DEF-101-397）；`.venv/bin/python -m pytest tools/tests/test_subprocess_encoding_hygiene.py -q` 9 passed |
| DEF-101-395 | 2026-07-26 | R50 round 1 修復（四方一審四份措辭略異但實質相同的獨立報告，經核實為同一結構性缺陷） | `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 的消費關係掃描器（`_scan_dir_for_root_paths()`／`_scan_import_consumed_paths()`）只認得「字面路徑字串」與「import 陳述式」兩類消費形態，對 `tools/tests/test_windowsapps_guard_cross_consistency.py` 用 `_tracked_files(pattern)`（即 `git ls-files -- pattern`）動態掃描全部 git 追蹤檔案這種第三類消費形態（盲區 C）結構性零偵測；實測 10/10 全綠卻攔不下 `.github/workflows/macos-compat-ci.yml` 的 push／pull_request paths 漏列 6 支被該掃描消費的 `.ps1` 檔案（`AutoClaude/tools/{reschedule_g0_gatecheck,local_ci_gate,run_act,install_git_hooks}.ps1`／`tools/{integration_gate,lib/GitHooksInstallCommon}.ps1`）——windows-compat-ci.yml 因有 `**/*.ps1` 萬用字元不受影響，僅 macOS 側不對稱 | P1（macOS 側 push/PR 閘門對這 6 支 `.ps1` 的相關回歸驗證延遲至 nightly 非阻塞層） | 本輪修復：`.github/workflows/macos-compat-ci.yml` push／pull_request 各補 6 行；新增 `_KNOWN_GLOB_SCAN_CONSUMERS` 顯式登記字典＋`test_known_glob_scan_consumers_covered_by_ci_paths()`（動態即時執行 `git ls-files -- pattern` 取得真實結果、逐一機械斷言被對應 workflow paths 覆蓋，非寫死清單） | fixed@R50：`cd AISDLC_SDD && ../.venv/bin/python -m pytest scripts/tests/test_ci_paths_cover_root_consumers.py -v` 修復前 10 passed → 修復後 **11 passed**；bug-injection（暫時移除 macos-compat-ci.yml 一行 path）確認新測試由綠轉紅、訊息精準指出未覆蓋檔名，還原後 diff+sha256 核對一致；主控複驗 `bash scripts/ci-gate.sh` 全綠，逐軌計數 v0.01:1478／v0.30:1729／scripts/tests:227（較修復前 226 +1） |
| DEF-101-396 | 2026-07-26 | R50 全新五維掃描（Scan-D：SSOT Guard 家族涵蓋率完整性） | 兩處「僅靠人工鏡射／只鎖常數不鎖行為」的殘留缺口，尚未被任何機械 parity 測試涵蓋：①`tools/install_mac_nightly.sh::report_heartbeat()`（心跳三態判斷）與 `tools/dev_start.py::_check_nightly_heartbeat`（同語意獨立 bash/py 重寫）僅由 `TestCrossSiteLiteralLocks` 鎖住門檻常數與 launchd label 字面值，三態分支邏輯本身（未偵測/過期/新鮮）從未被跨檔行為等價測試鎖定；②`tools/macos_smoke_local.sh`／`tools/windows_smoke_local.ps1` 各自獨立內嵌 `-c core.longpaths=true` git flag，`check_script_parity.py` 的 `_SINGLE_SIDED_EXEMPT` 只驗證兩檔皆存在且已登記，未對旗標內容本身做跨檔一致性機械比對 | P3（皆為「日後單邊修改不會有任何機械訊號」的縱深防禦缺口，非現行行為缺陷） | 本輪修復：①新增 `TestNightlyHeartbeatCrossSiteBehavioralEquivalence`（從 `install_mac_nightly.sh` 原始碼動態擷取 `report_heartbeat()` 函式本體、在獨立 bash 子行程對同一心跳檔執行，與 `dev_start.py._check_nightly_heartbeat()` 分類結果比對 4 個代表性年齡）；②新增 `check_script_parity.py::_check_git_longpaths_flag_parity()`（剝除註解後比對兩側旗標字面值） | fixed@R50：①bug-injection（`install_mac_nightly.sh` 的 `-gt` 誤改 `-lt`）證明 4 個 subTest 由綠轉紅，還原後 sha256 核對相符；②兩輪 bug-injection（首版未剝註解誤判仍綠，修正後正確轉紅），新增 `TestGitLongpathsFlagParity` 4 個情境測試；主控複驗 `.venv/bin/python tools/check_script_parity.py` 全綠（`✅ git longpaths 旗標鎖：兩側皆含 '-c core.longpaths=true'`），`.venv/bin/python -m pytest tools/tests/ -q` 486 passed／4 skipped／40 subtests passed |
| DEF-101-408 | 2026-07-26 | R52 round 2/3 四方複審發現（Scan-D 延伸／SA-SD 交叉確認） | `AutoClaude/autoclaude/evolution/_evaluator_derivation.py::derive_part_a_evaluator()` 用 `re.search(r'\bpytest\b', cmd)` 判斷是否為 pytest 指令（`'python(3) -m pytest ...'` 形態因含 `pytest` 字串而命中），但取可執行檔頭時直接用 `base.split()[0]`，對此形態拿到 `'python'`/`'python3'` token 而非 `'pytest'`，產出語法上不存在的 `'python --collect-only'`（實測 rc=2 unknown option），與函式「pytest 指令僅做 collect-only 確認語法正確」設計意圖相反；三層架構白名單明文允許 `pytest`/`python`/`python3` 三種頭部形態，屬生產可達路徑（`playbook_evolver.py`/`minimax_evolver.py` 皆委派至此共用函式） | P2（SPLIT_STEP Part A evaluator 推導在特定合法輸入形態下產出無效指令） | 已修復：改用 `tokens.index("pytest")` 定位 `pytest` token 本身，保留其前綴（如 `python -m`）一併帶入頭部；找不到時保留舊行為兜底（理論上不會發生） | fixed@R52（`tests/test_gap021_028.py` 新增 parametrize 測試；round 4 SA 親自呼叫函式驗證產出可執行的 `--collect-only` 指令，rc=0，3915 tests collected） |
| DEF-101-413 | 2026-07-26 | R53 Workflow 五維掃描（Scan-B＋Architect-Design 兩維度交叉獨立發現）＋round 1 四方（Architect/SA/SD/QA）一致確認 | `AutoClaude/autoclaude/infra/adapters/sdd_to_playbook_adapter.py` 的 `_EVALUATOR_TEMPLATES` 白名單（docstring 自稱「唯二合法 evaluator 模板」）中，`[0]` 已於 R52（DEF-101-403）修復移除裸字面值 `python` 前綴，但 `[1]`（`"python -m tools.{module} {args}"`）仍原封不動保留與修復前完全相同的裸字面值 `python -m` 前綴——同一白名單抽象內部安全等級不一致；`_build_evaluator_cmd()`（唯一消費點）自始只索引 `[0]`，故 `[1]` 為全倉零呼叫點、零測試涵蓋的死碼，一旦未來被啟用會在零示警下重現 R52 剛修復的同一 rc=127 缺陷。Architect 進一步指出此非能機械比照 `[0]` 修法（去前綴留 pytest）或姊妹 evolution 模組（`sys.executable`）直接套用的個案——`tools.{module}` 為 repo 相對 Python 模組路徑非可安裝 console script，本檔 R52 docstring 已明文排除 `sys.executable`（編譯期/執行期兩段式設計） | P2（白名單內部一致性缺口；死碼但屬同一缺陷型態未同步修復） | 已修復：四方一致建議「移除死碼 vs 保留並專屬設計」二選一並記錄理由，採納移除死碼——`_EVALUATOR_TEMPLATES` tuple 簡化為單一字串常數 `_EVALUATOR_TEMPLATE`，docstring 由「唯二合法」訂正為「唯一合法」，`_build_evaluator_cmd()` 同步移除索引；原地留下 R53 決策理由註解防未來重新引入第三種修法 | fixed@R53（`tests/infra/test_sdd_to_playbook_adapter.py` 82 passed；round 2 四方 Architect/SA/SD/QA 逐一複驗死碼徹底移除、無殘留呼叫點，一致 APPROVE） |
| DEF-101-426 | 2026-07-27 | R55 Workflow 五維掃描（Scan-A／Scan-B 交叉獨立發現）＋round 1 四方（Architect/SA/SD/QA 四位皆獨立命中同一根因）一致確認 | `tools/macos_smoke_local.sh:53` 用裸 `command -v python` 判斷 python 是否存在，未經全 repo 唯一的 WindowsApps guard SSOT（`tools/lib/windowsapps_guard.sh::is_real_python_candidate`）保護；其在 `tools/tests/test_windowsapps_guard_bash_parity.py` 內的豁免理由（「macOS 上不會出現 WindowsApps 假殼」）與同檔自身檔頭註解、`ONBOARDING.md` 記載的「本腳本亦會在 Windows Git Bash 下實跑」情境自相矛盾；四方以構造 WindowsApps 目錄下假 python stub 的隔離 PATH 皆各自獨立重現「裸判斷誤判存在」的行為 | P3（bug-injection 已重現真實假陰性行為，四方交叉獨立命中） | 已修復：改用 `. tools/lib/windowsapps_guard.sh` + `is_real_python_candidate python` 取代裸判斷（比照 `integration_gate.sh`／`bootstrap.sh`／`dev_start.sh` 既有呼叫慣例），並將本檔自 `test_windowsapps_guard_bash_parity.py` 的 `_EXEMPT_SH_FILES` 移至 `_CALLER_FILES` 正式列管 | fixed@R55 round1（`bash tools/macos_smoke_local.sh` 端到端 PASS=13/FAIL=0；`python3 -m pytest tools/tests/test_windowsapps_guard_bash_parity.py -v` 28 passed/24 subtests passed；bug-injection 反向驗證修復前後行為差異真實存在，round 2 QA 獨立重現確認） |
| DEF-101-429 | 2026-07-27 | R55 Workflow 五維掃描（Scan-E Architect-Design 發現）＋round 1 四方一致確認，DEF-101-238（R16 起 39 輪未清償收斂債）擴散實證 | `AutoClaude/autoclaude/infra/adapters/sdd_to_playbook_adapter.py` 的 shell 注入黑名單字元集（DEF-101-238 記載的原始基準）已擴散至 `goal_freeze_gate.py`／`tools/three_tier_to_playbook.py` 兩處呼叫端各自重複宣告字面值，彼此註解互相宣稱「⊇ 基準集合」卻零機械驗證超集關係是否成立 | P3（SSOT 收斂債持續擴散，架構層一致性風險） | 新增 `AutoClaude/autoclaude/utils/shell_deny_chars.py` 匯出 `BASE_DENY_CHARS` 作 SSOT，三處呼叫端改為 `BASE_DENY_CHARS \| {專屬追加字元}` 顯式聯集寫法；刻意不動 `execution/mutation_applier/_conditional.py`／`core/services/mutation/_conditional_evaluator.py`（CONDITIONAL 家族，白名單 regex 為主的不同機制，理由詳見程式碼內 R55 決策註解） | fixed@R55 round1（逐字元集合比對三處新舊 `_DENY` 值 100% 相等；新增 `tests/test_shell_deny_chars_parity.py` 3 passed；`python3 -m pytest tests/ -q` 3811 passed/146 skipped/0 failed；`tools/check_loc_budget.py` total=20437≤cap=20438；round 2 QA／SA 各自獨立在全套 pytest 重跑中遇到 1 次同一測試的不可重現偶發失敗，經排查判定為多位審查子代理共用同一份工作樹時的瞬間並行雜訊〔同 DEF-101-217 已記載模式〕，非真實回歸——後續共 10+ 次重跑〔QA 6 次＋SA 獨立重跑＋主控收尾重跑〕皆乾淨通過） |
| DEF-101-430 | 2026-07-27 | R55 Workflow 四方複審 round 2（SD 獨立發現，複核 round 1 DEF-101-427 新增工具自身的掃描邊界） | round 1 新增的 `tools/check_gha_action_versions.py` 有兩項掃描邊界缺口：(1) `scan()` 只 `glob("*.yml")`，靜默漏掃 GitHub Actions 官方同樣支援且語意等效的 `.yaml` 副檔名——現況 11 支 workflow 皆為 `.yml` 故不觸發，但與工具自身存在的目的（防止「新增 workflow 沿用舊版本」被機械鎖看見）直接矛盾；(2) `_USES_RE` 未先剝離行內 `#` 註解，若未來有人暫時註解掉一行舊版本 `uses:` 宣告會被誤判為現行宣告，產生假紅燈 | P3＋P4（前者為真實漏判風險，後者為未來假陽性風險，皆未在現況觸發） | (1) `scan()` 改為同時掃描 `*.yml`／`*.yaml`；(2) 新增 `_strip_yaml_comment()`（比照同輪 `_strip_bash_comment` 手法，只在引號外才承認 `#` 起始註解），掃描前先剝離註解 | fixed@R55 round2（原始重現腳本二次確認：修復前 `.yaml` 內容漏判、修復後正確偵測；`python3 -m pytest tools/tests/test_check_gha_action_versions.py -q` 12 passed；`python3 tools/check_gha_action_versions.py` 對真實 repo 仍全綠 56 處一致，確認未產生假陽性；`python3 tools/run_root_unittests.py` 496 passed） |
| DEF-101-437 | 2026-07-27 | R56 Workflow 五維掃描（Scan-A）＋round 1 四方確認，落地 R55 裁定不修的 DEF-101-432 | `AutoClaude/autoclaude/models/escalation.py::generate_handover_checklist()` 產生的「接手行動清單」由 `to_markdown()` 包進 bash 圍欄落盤成 escalation dump，人類會原樣複製到終端執行，但內容多處 POSIX-only：`python -m py_compile tests/test_*.py`（cmd.exe/PowerShell 不對外部程式做 glob 展開，py_compile 收到字面 `tests/test_*.py`）、`cat <log> \| tail -50`（無 tail；單引號在 cmd.exe 非引號字元）、`grep -rn ... \| head -20`（無 grep/head）、`autoclaude {playbook_path}` 未引用（Windows 路徑常含空白）。另 `plugins/checkpoint/_escalation.py:55` 為第 5 處 | P2（Windows 11 使用者照抄必失敗；R55 曾因 LOC 零餘裕裁定不修） | 本輪修復 | fixed@R56：採**零邏輯行淨增**手法（行尾註解併入 Windows 等效寫法，`count_loc()` 只跳過空行與行首 `#`），完整等效指令與論證記於函式開頭註解區塊。修復中另訂正一項回報者未察覺的細節：`Select-String -Path tests\*` **無遞迴語意**，實測命中 `assert.*==` 的檔案非遞迴 36 個 vs 遞迴 237 個（漏 85%），照抄卻以為已檢查完比沒有提示更危險，故改用 `gci -r tests\|sls`。`playbook_path` 加雙引號（雙引號是 sh 與 cmd.exe 唯一通用的引用方式）。註解明載未來改為 `sys.platform` 條件產生指令屬架構正解，須走 ADR-SD07-001 §5.2/§6.2 per-file override 核准，而非續用行尾註解規避 tier 上限 |
| DEF-101-502 | 2026-07-28 | R57 落地後首次真 Windows 開機開工（原 R58 發現；**R58 整輪經使用者判定失控作廢**〔`reset --hard 75aab89`，備份 tag `r58-pre-reset-head`／`r58-wip-backup`〕，R59 回收其**已提交**的真缺陷修復並逐筆重新獨立驗證） | `tools/dev_start.py::_toml_deps_snapshot` 的依賴 hash 白名單只取 `project.dependencies`／`project.optional-dependencies`／`build-system`，**不含 `[project.scripts]`／`gui-scripts`／`entry-points`**。這三塊是「安裝當下才產生實體 shim」的內容，純新增 console script 時 hash 不變 → `step_venv` 判「依賴新鮮」跳過重裝 → **既有 venv 永遠長不出那支命令**。實證：R52 為修 evaluator 裸 `python` 而新增的 `autoclaude-artifact-check` 在 git pull 後缺席，playbook evaluator 會以 rc=127 失敗，**症狀與 R52 所修的原始缺陷完全同形**、極難歸因。與本函式當初修過的 build-system 缺口同一 bug class | P1 | 根層工具修復（`tools/dev_start.py`）+ 回歸鎖 | **fixed@R59（回收 R58 已提交修復並重驗）**：白名單補三塊 + `tools/tests/test_dev_start.py::TestDepsHashEntryPointGap`。**R59 獨立驗證（不採信 R58 宣稱）**：`_toml_deps_snapshot` 雙向實測——加一個 `[project.scripts]` 條目 → hash 改變（True）；只改 `description` 純中繼資料 → hash 不變（True）。**本輪另一項獨立佐證**：開工時 dev_start 曾因「依賴檔 hash 不符」重跑 bootstrap，追根因後確認正是**本修復被 reset 掉造成的演算法回退**（狀態檔存的是新演算法的 hash、程式碼退回舊演算法），即此修復確實有效果、且其缺席可觀測 |
| DEF-101-503 | 2026-07-28 | 同上（查 dev_start 心跳哨兵所報的 nightly 失敗根因） | nightly `sdd-fsm-chaos` stage **恆紅**（2026-07-25、07-27 連兩輪 exit=1），但 chaos 測試本身全過（`pytest_rc=0 sweep_rc=0`），紅的是取證那行：`AutoClaude/tools/run_local_nightly.ps1` 用 `%`-formatting 印 bounded 摘要，而 `$script:PyExe` 解析到的 `python` 在 pyenv-win 下是 **python.bat shim**，batch 先吃掉命令列的 `%` → Python 收到 `'...'(...)` → `SyntaxWarning: 'str' object is not callable` + `TypeError`、`parse_rc=1`。危害是訊號污染而非少印一行——它讓每天早上 dev_start 心跳哨兵都報「上一輪 nightly 有失敗」，真失敗被淹在常亮紅燈裡 | P1 | 載具修復（`run_local_nightly.ps1`）+ repo-wide 機械鎖 | **fixed@R59（回收 + 重驗）**：改 `.format()`；機械鎖 `tools/tests/test_python_c_percent_shim.py` 掃全 repo `.ps1`（不排除凍結版，遵 R44/R45/R46 教訓）。**R59 以具鑑別力的載具獨立重驗**（R58 自承其原始驗證「綠得沒有鑑別力」）：以 **pyenv-win `python.bat` shim** 實跑三組對照——舊寫法 `%` → **rc=1 + TypeError**（bug 重現）；新寫法 `.format()` → **rc=0 + 正確輸出 `bounded=100/100 avg=1604.2`**；對照組真 `.exe` 跑舊寫法 → rc=0 正常（證實本 bug 為 shim 專屬，故用真 exe 驗證確實無鑑別力）。**過程紀實**：主控首次重驗誤用 Git Bash 載具，該載具會吞掉 shim 的 stdout（連 `print('HELLO')` 都零輸出、只有 stderr 透得出來），一度誤判「修復後零輸出＝比原 bug 更糟」；改原生 PowerShell 後結論才成立。這是本 session 第三次踩到「載具本身是肇事者」，見 DEF-101-511 與下方流程紀實 |
| DEF-101-504 | 2026-07-28 | 同上（本次開工流程自身即為肇事者，事後時間線比對揭露） | **開工流程與 nightly 補跑撞車 → 整批假紅 + 心跳長期污染**：本機 nightly 走 schtasks 補跑（WakeToRun／StartWhenAvailable），機器一喚醒就補跑，正是使用者開工的同一分鐘。實測時間線：nightly 18:41:11 起跑，提示詞第 1 步的 `git merge --ff-only` 於 **18:41:26** 抽換 113 個檔案，落在該輪 `local-ci-gate` pytest 執行區間（18:41:10~18:42:50）正中間 → 5 支測試假紅（單獨重跑 10 passed 證偽）。假紅寫入 `nightly_latest.log` 後，**之後每天早上**的心跳哨兵都會據以報「上一輪失敗」。既有防護為零 | P1 | 根層工具 + 提示詞雙修（機械鎖優先於人治） | **fixed@R59（回收 + 重驗）**：① `dev_start._nightly_running()` 三態偵測，查 nightly 腳本**自己持有的去重鎖**（Windows 具名 Mutex／mac 鎖目錄 + pid 存活）而非猜 log mtime；② `step_sync` 偵測到在跑時**不自動 pull**；③ 新增 `--check-nightly` 旗標供提示詞在手動 merge **之前**呼叫；④ `useMacWin.md` 兩版提示詞補查核步驟與「假紅判別法」。**R59 獨立驗證**：實跑 `.venv/Scripts/python.exe tools/dev_start.py --check-nightly` → 印 `idle：沒有 nightly 在跑，可以安全同步`、rc=0。**R59 QA 複審補正（本列初稿的驗證不足）**：idle 正是三態中**什麼都不做**的那一個，真正提供保護的是 True 分支，而既有 `TestNightlyRunningDetection` 自陳只走 posix 分支→ Windows 的 `OpenMutexW` 路徑在本輪之前從未被任何證據觸及。QA 補做實機驗證：以原生 PowerShell 建立並持有具名 Mutex `Global\AutoClaude_Nightly_Run` 後呼叫 `--check-nightly` → 印 `NIGHTLY-RUNNING：偵測到 nightly 正在執行…`、**rc=1**，True 分支確實可達且語意正確；另查 `Get-ScheduledTask -TaskName 'AutoClaude_Nightly'` → `RunLevel=Limited LogonType=S4U UserId=wuwei`＝排程與互動 session 同帳號、非提權，故不存在「跨完整性層級開 `Global\` 物件被拒 → 回 None → 保護空轉」的風險。**並已補常駐正控測試**（`test_dev_start.py::test_nightly_running_true_branch_on_windows_mutex`，以測試專用名稱 `..._R59TestOnly` 自持 Mutex，不碰真排程用的正式鎖） |
| DEF-101-505 | 2026-07-28 | 同上（回報環境狀態時，發現「無警告」與實況不符） | `dev_start` 的「工作樹不乾淨」警告**只在同時落後 origin（真的要 pull）時才發**：`step_sync` 於 `behind == 0` 分支直接印「✅ 已是最新」並 return，`dirty` 算了卻不用。後果：使用者/agent 易把「沒看到髒工作樹警告」誤讀為「工作樹是乾淨的」 | P3 | 文件揭露（**不改行為**：不需要 pull 時擋下來沒有意義，改了反而製造常亮噪音） | **fixed@R59（回收；文件揭露）**：`useMacWin.md` 兩版第 3 步新增一條「沒看到『工作樹不乾淨』警告不等於工作樹是乾淨的」，並明示要確認請自己跑 `git status`。R59 覆核該行確實存在於回收後的檔案內 |
| DEF-101-506 | 2026-07-28 | 手動重跑 nightly 複驗 DEF-101-503 時，自身踩到「用錯直譯器」，事後追根因 | **nightly 載具的直譯器不具決定性，且不可取證**（紀律 #14 的漏網面）：`run_local_nightly.ps1` 把直譯器存成字面 token `$script:PyExe = 'python'`，每個呼叫點由 PATH **現場解析** → 同一支 nightly 在 schtasks 下用 pyenv `python.bat`、在已啟用 monorepo `.venv` 的終端機／agent 下用 `.venv\Scripts\python.exe`。兩者**依賴集不同**（`.venv` 未裝 `[postgres,pgvector]` → `pg-e2e` 因 `ModuleNotFoundError: psycopg2` 假紅）、**shim 語意不同**（`.bat` 做百分號展開、真 `.exe` 不做）。**更隱蔽**的是它讓 DEF-101-503 的修復「綠得沒有鑑別力」。而 log 只印字面 token「python」，兩種啟動方式的 log 長得一模一樣、事後無法歸因。mac 側不受害（`.sh` 早已釘死絕對路徑）＝**平台不對稱** | P1 | 載具修復（`run_local_nightly.ps1`／`.sh`）+ 紀律 #14 增列 1b + 機械鎖 | **fixed@R59（回收 + 重驗）**：① ps1 偵測 `$env:VIRTUAL_ENV` 即自本行程 PATH 移除其 Scripts（移除後若無其他 python 則**還原並警告**）；② 兩支載具皆改印**解析後絕對路徑＋版本＋venv 狀態**；③ 機械鎖 `tools/tests/test_nightly_interpreter_determinism.py`；④ 紀律 #14 增列 1b（完整版 v1.11）並同步 `AutoClaude/CLAUDE.md` 摘要（就地改寫不增行，維持 400 行硬上限——R59 覆核回收後仍為 400 行）。**R59 獨立驗證**：`test_nightly_interpreter_determinism` + `test_python_c_percent_shim` 共 15 tests OK |
| DEF-101-509 | 2026-07-28 | R59 主控實測（Windows 11 實機跑 `tools/run_root_unittests.py` 後逐支檢視 skip 明細，非 agent 掃描） | **一支 Windows 專屬腳本的語法閘門，恰恰在 Windows 上不跑**：`tools/tests/test_install_windows_nightly.py` 的 `TestInstallWindowsNightlySyntax` 以 `@unittest.skipUnless(shutil.which("pwsh"))` 守門（**只認 PS 7**），而 `ONBOARDING.md` §1 明列 pwsh 7 為**選用**、Windows 11 內建的是 Windows PowerShell 5.1。實測本機 `shutil.which('pwsh') is None` → 該類別 skip。同目錄兩支姊妹測試（`test_bootstrap_ps1.py::_windows_pwsh_available`、`test_dev_start_ps1_lastexitcode.py`）**早就寫成 `powershell or pwsh`**，唯本檔偏離。且該 skip **未帶 `[WINDOWS-NATIVE-ONLY]` 標籤**，故 `run_root_unittests.py` 的可見度機制看不到它（見 DEF-101-510）。唯一還會跑到它的環境是 GitHub-hosted runner（ubuntu/windows 皆預裝 pwsh）——而 CI 因帳務停擺不啟動 runner（DEF-101-081/208），**此閘門現況零活體覆蓋**。深一層：即使 CI 活著，解析用的也是 **PS 7 文法**，而該腳本在生產是以 `powershell -File`（＝5.1）執行，`tools/` 樹又受 `test_ps51_compat.py` 的 PS 5.1 相容政策約束——**這道閘門從來沒用目標引擎的文法驗過** | P1（Windows 專屬腳本的語法閘門在 Windows 上失效，屬 DEF-101-343~345／R43 同一缺陷類別） | 本輪修復（`tools/tests/test_install_windows_nightly.py`）+ 回歸鎖 | fixed@R59：抽出 `_ps_engine()`＝`which("powershell") or which("pwsh")`（Windows 上優先 5.1，與生產執行引擎一致、亦與 `test_ps51_compat.py` 政策一致；R59 實測 PS 5.1 `Parser::ParseFile` 對該腳本 **errs=0**），`skipUnless` 改用之。回歸鎖 `TestSyntaxGateEngineSelection` 2 支：① `test_syntax_gate_is_not_skipped_on_windows` **直接斷言 `TestInstallWindowsNightlySyntax.__unittest_skip__` 為 False**（斷言判定結果而非條件文字，故任何等價改寫的退回都抓得到）；② `test_engine_selection_prefers_windows_powershell` 鎖住「兩者皆有時必須選 5.1」。**bug-injection**：把 `_ps_engine()` 改回 `which("pwsh")` → 兩支鎖同時 FAILED 且原缺陷狀態（`skipped=1`）重現；還原 → 13 tests OK。**修復效果可觀測**：本機 root unittest 的 skipped 由 **11 降為 10**，即該語法解析確實開始真跑 |
| DEF-101-510 | 2026-07-28 | R59 主控實測（同上；DEF-101-509 為什麼藏得住 → 追工具層根因） | **skip 可見度機制只有單一方向**：`tools/run_root_unittests.py::report_windows_native_skips` 只照亮「這支測試只在原生 Windows 才有驗證價值、這次環境不符」（R43 DEF-101-348 建的 `[WINDOWS-NATIVE-ONLY]` 標籤）。**反方向零機制**——「因為我們正跑在 Windows，所以失去了某些覆蓋」沒有標籤、沒有摘要區段、沒有計數，只會併進 `unittest` 預設的 `skipped=N` 一個數字。R59 實測：Windows 11 上 `skipped=11`，**11 支全部無標籤**，其中兩支是真正的覆蓋損失而非平台語意使然——① DEF-101-509（語法閘門因無 pwsh 7 而 skip）；② `test_env_changed_removes_cache_dir_and_symlink` 因無 symlink 權限（`[WinError 1314]`，**標準未提權 Windows 11 的預設狀態**）而 skip。這正是 R43 想根治的缺陷類別，只是方向相反，R43 只補了一半 | P2（守門可見度單向；DEF-101-509 能藏住多輪的直接工具層成因） | 本輪修復（`tools/run_root_unittests.py`）+ 回歸鎖 | fixed@R59：新增純函式 `all_skips()` 與 reporter `report_all_skips()`（**沿用該檔既有的「純函式／列印副作用分離」紀律**，R43 二審 SA 立的規矩：純函式不得有列印副作用，否則自測時會把 fixture 假 id 印進真實終端混淆複審者），在 `run_with_floor` 內於既有 WNO 區段之後呼叫。**設計取捨明說**：刻意**不**發明第二套標籤分類（那需要為每支 skip 判定「by-design 平台語意」vs「環境降級」，判準本身會成為新的漂移來源），改為**全部印出、把分類交給讀者**——與紀律 #2「log 必須含完整統計、不信任預設 dump」同一精神，也等於把 pytest `-rs` 早就免費提供的東西補進這個 unittest runner。回歸鎖 5 支（`tools/tests/test_run_root_unittests.py`）：未標籤 skip 須含理由／reporter 須印出 id+理由+`[未標籤]` 標示／零 skip 時須完全靜默（不製造常亮噪音）／`all_skips` 純度（無 stdout）。**實跑驗證**：修復後 runner 確實印出「本次 skip 明細（共 10 支）」並逐支列出 id 與理由 |
| DEF-101-511 | 2026-07-28 | R59 主控實測（**主控本人當場踩到**：跑 Windows 側補償控制拿到假紅，追根因） | **`tools/windows_smoke_local.ps1` 在非原生載具下產生假紅，而該約束只有文件、零機械強制**：主控以 Git Bash 呼叫 `powershell -File tools/windows_smoke_local.ps1` 得到 **PASS=11 FAIL=2**（`[6]` 中文路徑情境的子行程看到的路徑變成 `?皜祈岫`；連帶觸發 `$MinPass=12` 下限失敗），改用**原生 PowerShell** 重跑則 **PASS=12 FAIL=0 exit 0**。該檔檔頭自 R10 起就寫著「🔴 載具要求：請以原生 PowerShell 執行……（R10 實測：Git Bash 載具 PASS=7 假紅）」——**即這個約束早就已知，卻只寫在註解裡，對「工具只有 Bash 載具可用」的呼叫者毫無攔阻力**，R59 於是原地復發一次。假紅與漏測同等有害：它會讓人去追一個不存在的生產缺陷，或反過來讓人習慣性忽略這支腳本的紅燈 | P2（Windows 側唯一的執行級補償控制會回報假紅；純文件約束已被實證無攔阻力） | 本輪修復（`tools/windows_smoke_local.ps1` fail-fast 守門）+ 檔頭升級為機械強制 | fixed@R59：腳本開頭（既有 git／python fail-fast 守門之前）新增載具守門——偵測 `$env:MSYSTEM` 非空即印出原因與正確指令後 `exit 1`。**偵測手法經實測選定**：R59 對兩種載具各跑一次探針，`MSYSTEM` 在 Git Bash 下為 `MINGW64`／原生下為空，`[Console]::OutputEncoding` 為 `big5`／`utf-8`。**刻意不用「非 ASCII 引數 round-trip 能力探針」**——實測該探針在兩種載具下都回 `equal=True`（Git Bash 下看到的亂碼只是終端顯示層、引數本身完好），**零鑑別力**；真正壞掉的環節是 `[6]` 的 `git clone` 到含中文路徑時的 msys 路徑轉換，無法用一行探針忠實模擬，故改用身分訊號。**刻意不做「跳過 [6] 並降低 MinPass」的降級路徑**——那會產生一支「少驗一項卻仍回報成功」的閘門，正是本輪在 `ci-gate.ps1` 上修掉的反面示範（DEF-101-512）。**實測驗證**：Git Bash 呼叫 → 立即 exit 1、一支測試都沒跑；原生 PowerShell 呼叫 → PASS=12 FAIL=0 exit 0（守門不誤擋） |
| DEF-101-512 | 2026-07-28 | R59 Workflow 五維掃描 Scan-E（Architect-Design）提出，對抗式複驗員**判為推翻**，主控覆核後**部分翻回** | **`AISDLC_SDD/scripts/ci-gate.ps1` 的降級 fallback 會在稽核取證上冒充完整閘門**：偵測不到 Git Bash 時走 fallback，只跑 **v0.01 凍結基線單軌**的 3 個 stage（`$fw = AISDLC_SDD_v0.01`）——不含 LATEST 軌、不含 `ci-gate.sh` 的 10 道 lint 硬閘、不含 `scripts/tests` 共享 infra 測試（245 支），覆蓋率差距是數量級。**掃描員原稱兩者收尾訊息「逐字相同」；主控親自比對後確認此宣稱不成立**（`.sh` 為 `✅ 本機 CI 閘門全數通過（版本：…）` 並多印一行逐軌計數），複驗員的事實訂正正確。**但**該 fallback 的收尾訊息是完整閘門錨點的**前綴子字串**，而實務稽核取證（人工回報、歷輪帳本引用、grep）用的錨點正是那段前綴 → **兩條路徑在該錨點下無法分辨**。只有 Windows 走得到 fallback（mac 一律有 bash）＝單邊平台的取證可信度缺口。命中機率經查證為低（`Find-GitBash` 同時查 PATH 與常見安裝路徑，標準 Git for Windows 安裝會被找到），故不上調嚴重度 | P3（取證可信度；修法成本一行，依「不要無謂延後」紀律當輪修） | 本輪修復（`AISDLC_SDD/scripts/ci-gate.ps1`）+ 機械鎖 | fixed@R59：fallback 收尾訊息改為 `✅ fallback 3-stage 通過（僅 AISDLC_SDD_v0.01 凍結基線單軌；**未含** LATEST 軌、未含 ci-gate.sh 的 lint 硬閘與 scripts/tests 共享 infra 測試——非完整閘門）`。**修法取捨**：選「讓字串在結構上無法冒充」而非「加更多 warning」——起頭已有一行 ⚠️ 警告，但人讀 log 取的是**最後那行結論**，讓結論自己說出降級事實才不依賴讀者記得往上看。機械鎖放 `AISDLC_SDD/scripts/tests/test_ci_gate_version_resolution.py`（該檔已在讀 `ci-gate.sh`，且屬 `scripts/tests/` 共享 infra 套件、隨 ci-gate 自己每輪執行＝**閘門自己鎖住自己的誠實性**）2 支：① 完整閘門錨點不得出現在 `.ps1` 的**功能碼行**（剝整行註解後比對——本輪註解刻意保留一次完整閘門字面值以說明缺陷，不剝會自我命中，同 `zsh-glob-ok:`／`baseline-ok:` 家族處理「文件必須引述壞形態」的既有做法）；② 反向鎖：fallback 訊息必須自陳含 `fallback` 與 `未含`（否則改成含糊的 `✅ 完成` 同樣會誤導）。**bug-injection**：還原舊訊息 → 2 支鎖同時 FAILED；還原 → 9 passed。另實測 PS 5.1 `ParseFile` errs=0、UTF-8 BOM 保留 |
| DEF-101-513 | 2026-07-28 | R59 Workflow 五維掃描 Scan-D（對抗式複驗存活） | **`VAR=value <指令>` 前綴語法在 PowerShell 不存在，但兩份活文件仍只給 bash 形態**：`docs/AISDLC_Agent_UserGuide.md` §1.4（標題明寫「驗證安裝（**強制**）」）與根 `CLAUDE.md`「測試 / Lint」區皆寫 `PYTHONUTF8=1 lint-imports`，Windows 使用者照抄得到 `The term 'PYTHONUTF8=1' is not recognized`。R57 已為同一件事修過 `ONBOARDING.md` §7（補 PowerShell 形態並附實測說明），**但只修了那一處**——使用者最先讀的入門文件與最常被讀的根 CLAUDE.md 原樣留存 | P3（單邊平台文件缺陷；照抄必失敗，錯誤訊息不指向真因） | 本輪修復（兩份活文件） | fixed@R59：① `UserGuide.md` §1.4 拆成 **macOS/Linux（bash・zsh）** 與 **Windows（PowerShell）** 兩塊（後者 `$env:PYTHONUTF8=1; lint-imports`），並加一段說明「兩塊的差別不是排版偏好」；② 根 `CLAUDE.md` 於該 bash 區塊後補一行對照註記（該檔 224 行、無行數閘門，加一行安全）。刻意**不**改 bash 區塊本身（它對 mac 使用者是正確的），採「兩形態並列」而非「改寫成單一形態」 |
| DEF-101-514 | 2026-07-28 | R59 Workflow 五維掃描 Scan-D（對抗式複驗存活） | **基線數字守門存在結構性盲區：使用者最先讀的入門文件不在掃描面內**。`tools/check_pytest_baseline_sites.py` 的 `_SCAN_FILES` 自 R13 收斂以來只有 5 份（根/AutoClaude CLAUDE.md、ONBOARDING、useMacWin、AutoClaude/README），**不含 `docs/AISDLC_Agent_UserGuide.md`**——而該檔 §1.4 標題明寫「驗證安裝（強制）」且自帶一組寫死的舊基線數字，實測早已落後數百支（skipped 差 80+）。因不在掃描面內，這個數字腐化了很多輪**都不可能翻紅**。後果具體：照著「強制驗證」步驟做的新使用者，量到的數字與文件差幾百支，合理反應是判斷「安裝壞了」 | P2（入門文件的強制步驟寫著錯誤基線，且守門結構上抓不到） | 本輪修復（文件去數字 + 守門擴掃描面 + roster 鎖同步） | fixed@R59：① `UserGuide.md` 移除寫死數字、改指向 SSOT（`ONBOARDING.md` §7）並說明為何不重複數字；② `check_pytest_baseline_sites.py::_SCAN_FILES` 加入該檔（納入判準沿用該清單既有語意「基線數字歷史上**實際漂移過**的高風險文件」——它不只是有風險，是已成既成事實）；③ **同步擴充 roster 鎖** `test_check_pytest_baseline_sites.py::TestRealRepoConfigPinning`（否則加了掃描面卻沒 roster 鎖，等於把新加的那一項留在「刪掉也沒訊號」的狀態）。**該 roster 鎖在我改完 `_SCAN_FILES` 後當場翻紅、逼我同步**——既有機械鎖有效運作，如實記載。**bug-injection**：把一行舊基線數字塞回 `UserGuide.md` → 守門 ❌ 指名該檔該行；移除 → ✅ 6 份掃描檔通過 |
| DEF-101-515 | 2026-07-28 | R59 主控實測（Windows 11 實機建立全套基線，四組數字有四組與 SSOT 記載不符）+ 五維掃描 Scan-D（D-3）獨立命中同一問題 | **自稱「全 repo pytest 基線數字唯一站點」的 `ONBOARDING.md` §7 只有 macOS 數字、從無 Windows 側基線**，而這是一份雙平台 onboarding 文件，兩平台數字在結構上**永遠不可能相同**。R59 Windows 11 實測 vs 記載：AutoClaude pytest **3740/208** vs 3738/210；root unittest **616 OK/skipped=10** vs 616/4；SDD v0.30 **1725** vs 1729；scripts/tests **245** vs 244。**更糟的是文件的容差宣稱漏掉一個維度**：§7 只說「`ci-gate.sh` 逐軌計數對 docker daemon 可用性敏感（停用時 v0.01／v0.30 各 -3），±3 屬環境因素非退化」——**完全沒有平台維度**，於是 Windows 實測的 v0.30 −4 落在宣稱容差之外 → 依文件字面應判定為「退化」，而實況是 2 支 `requires_docker_success`（`sys.platform.startswith("win")` **硬排除，與 docker 是否可用無關**，DEF-101-062）＋2 支 `test_post_commit_drift_worktree.py`（POSIX shebang，`skipif(win)`），零退化。**在容差宣稱裡漏掉一個維度，比沒有容差宣稱更糟——它會主動把正確的平台差異誤導成迴歸** | P2（雙平台文件的基線 SSOT 單邊；容差宣稱主動誤導） | 本輪修復（`ONBOARDING.md` §7 補 Windows 側基線表 + 訂正容差宣稱） | fixed@R59：§7 新增「Windows 11 側基線」表，逐項列出兩平台數字**與實測歸因**（含 docker 執行中時仍會少 2 支的原因、v0.01 為何相同）。**方法論遵循本 repo 既定紀律**：只填實測值、**不做加減推算**（R57 三度以算式推得根層測試數皆當場與實況不符），故 AutoClaude pytest 的 ±1 殘差明文標示「未歸因，需一台 macOS 同時量測才能對帳」而非硬湊。同時訂正 §7 那句 docker 容差宣稱 |
| DEF-101-516 | 2026-07-28 | R59 主控實測（動工前依 §7 規定做污染檢查時，發現檢查本身沒有鑑別力） | **§7 規定的污染檢查手法在 uv 建的 venv 上零鑑別力**：R56 註尾訂下「往後污染檢查一律改用在乾淨環境會**印出東西**的寫法（如 `python -m pip list \| grep -E "psycopg2\|sqlalchemy"`）」，判準本身正確，但**選定的指令不滿足它自己的判準**——根 `CLAUDE.md` 明載 `tools/bootstrap.*` 偵測到 `uv` 時一律用 `uv venv` 建置（＝**預設路徑**），而這種 venv **內部沒有 `pip` 模組**：`python -m pip list` 直接報 `No module named pip`、stdout 為空 → grep 零命中 → 讀起來與「乾淨」**一模一樣**。R59 於本機實測重現。這正是 R56 想根除的「假乾淨」，只是換了一條路徑進來（同族第四次：前三次為「用錯 venv」、「既有 venv 渾然不覺被污染」、「檢查手法本身無鑑別力」） | P3（量測方法論；會讓後續所有基線量測的前提失真） | 本輪修復（`ONBOARDING.md` §7 改訂手法） | fixed@R59：改用不依賴 pip 是否存在、且乾淨/污染兩種情況都必定印出東西的寫法——`python -c "import importlib.util as u; [print(m, 'PRESENT' if u.find_spec(m) else 'ABSENT') for m in ('psycopg2','sqlalchemy')]"`，判準是「兩行都必須是 ABSENT 才算乾淨；**任何一行印不出來就是探針壞了**（而非乾淨）」——關鍵差異在於它**沒有「靜默零輸出」這個狀態**。另註明若堅持用 pip 清單形態，在 uv venv 下必須改用 `uv pip list`。R59 本輪的基線量測即以新手法確認 venv 乾淨（兩行皆 ABSENT） |
| DEF-101-517 | 2026-07-28 | R59 Workflow 五維掃描 Scan-C（對抗式複驗存活）＋主控逐項實測核實 | **nightly 的「去向帳目」只有單向，且 ONBOARDING §8 的框架與實況在三項上相反**：R11 為 mac 側 `run_local_nightly.sh` 補了「七軌去向帳目」，逐軌交代它不跑哪幾軌、由誰承載；**反方向從未有對應帳目**，而 §8 又把兩平台框成「Windows 深度版完整可用／macOS 刻意非對等的薄聚合器」。R59 逐項 grep 實測確認 `run_local_nightly.ps1` 對下列三者**皆零呼叫**：① **平台 smoke**——mac `[1/4]` 每日自動跑 `macos_smoke_local.sh`，Windows 對等物 `windows_smoke_local.ps1`（PASS=12）**只能手動觸發**；這是三項中最要緊的，因為它正是 DEF-101-139 為「雲端 CI 帳務停擺」而建的 Windows 側**執行級補償控制**，沒有自動觸發器＝**補償控制自己沒有心跳**（也解釋了為何它會腐化到讓 R59 踩到 DEF-101-511）；② **根層 unittest**——mac `[2/4]` 每日跑，Windows nightly 不跑（其 `local-ci-gate` stage 走 `local_ci_gate.py`，範圍是 pytest + LOC + lint-imports，**皆 AutoClaude scope**）；③ **SDD 完整閘門**——mac `[4/4]` 每日跑雙軌全套，Windows 只有 `sdd-fsm-chaos`（chaos 子集） | P2（文件宣稱方向與實況相反；Windows 側補償控制無自動觸發器） | 本輪修復（帳目補齊）；**補 stage 列 backlog，理由明說** | fixed@R59（帳目部分）：① `run_local_nightly.ps1` 檔頭新增「反向去向帳目」逐項交代三個缺口與理由；② `ONBOARDING.md` §8 於原框架句後補訂正段。**本輪刻意只補帳目、不補 stage（非無謂延後，理由具體）**：新增 stage 需同步改 summary 行／summary JSON／exit-decision 清單／`Format-Rc` 標籤共四處，而 summary 行被 `tools/dev_start.py` 的心跳哨兵以**跨檔字面正則**解析（DEF-101-263②／R25 已建跨檔字面鎖），改動 summary 契約會連帶動到那組鎖；且本檔是 CI 停擺期間**唯一的活體驗證管道**、排程在 02:00，本輪無法觀測真正的排程執行結果。**解鎖條件（R59 四方一審 ARCH-R59-06／SA-R59-06 交叉指出後補正）**：原本只寫「需獨立一輪＋一次真實排程執行」，但那只論證了「把 smoke 塞成 `run_local_nightly.ps1` 第 8 個 stage」這一條最貴的路徑很貴，**沒有論證「讓補償控制有心跳」很貴**——兩位審查員各自獨立指出至少兩條完全不觸及 summary 契約的替代路徑：① 由 `tools/install_windows_nightly.ps1` **另註冊一支獨立 schtasks 任務**直接跑 `windows_smoke_local.ps1`（與 nightly summary 零耦合，且該安裝器已有 install／`-Status`／`-WhatIf` 冪等骨架可複用，`-WhatIf` 更可在不等 02:00 的情況下取得驗證證據——這恰好抵銷了本列原本引為主要理由的「本輪無法觀測真實排程執行」）；② `dev_start` 心跳哨兵加一條「windows smoke 上次成功距今 > N 天」的提醒（機制已存在，只多讀一個時間戳）。**故正確的解鎖條件是：下一輪先評估這兩條較便宜的路徑，再決定是否需要走 stage 路線**；不可讓下一輪誤以為唯一選項是最貴的那條 |
| DEF-101-507 | 2026-07-28 | R59 Workflow 五維掃描 Scan-A（對抗式複驗員盡力推翻失敗，並自行挖出更硬的證據） | **具名套件 extras 未加引號，在 macOS zsh 下整條指令不執行**：zsh 自 Catalina 起為預設登入 shell、`nomatch` 預設開啟，`autoclaude[postgres]` 是合法 glob（literal `autoclaude` + 一個取自 {p,o,s,t,g,r,e} 的字元），repo 內無匹配檔名 → **zsh 在呼叫 pip 之前就中止整條命令列**，印 `zsh: no matches found:`。pip/uv 根本沒被執行，使用者看到的是一個與套件安裝完全無關的錯誤。全 repo **40 處實修**（另 1 處 `docs/internal/README_Prompt_v0.1_history.md` 為歷史快照，依 `_EXCLUDED_SUBSTRINGS` 政策不改）；修完後掃描面內「加了引號的具名形態」共 42 處＝40 新修 + R57 就寫對的 `ONBOARDING.md` 1 處 + 本鎖 assertion message 自帶樣本 1 處。**R59 QA 複審訂正**：本列初稿把「修完後現存的加引號形態總數（42）」當成「修了幾處」在報，而同輪三份文件因此出現 42／41／40 三個數字；QA 以 `git diff HEAD -U0` 逐檔統計「被移除的未加引號具名行」＝41、其中 1 行是測試 docstring 敘述文字（非站點）→ 真實站點 40。其中 **19 處是執行期 raise/print 給使用者的唯一修復指引**（`factory.py` 4 支 build_*、五個 `Pg*` repository/adapter、`_pg_models` import 時、`alembic/env.py`、`migrate_file_to_pg.py`、`pg_dump_to_yaml.py` 等）——使用者已經卡在缺依賴，照唯一提示做又得到第二個看不懂的錯，形成迴圈（同 R57 round 1 對 pre-push／local_ci_gate 判定「比文件更要命」的情境）。R57 已對**裸 `.[extras]`** 形態修過（DEF-101-479），但機械鎖明文排除具名形態，理由寫「**repo 內無此寫法**」——**該前提已被證偽**：不只本輪實測 42 行，複驗員另查出 `ONBOARDING.md` 上 R57 自己就寫了加引號版 `pip install -e 'AutoClaude[dev,notifications,lint]'`（可見它認得這個 glob 風險），卻在鎖的 docstring 裡宣稱該寫法不存在。此即 `CrossPlatform_Scan_Dimensions.md` §「邊界宣稱必須實測，且不得使用未窮舉的絕對詞」所立法禁止的失效模式，故不能引「已裁決邊界」免責 | P3（單邊平台缺陷；對齊同族先例 DEF-101-479 的既有評級，不因站點更多而上調） | 本輪修復（42 處加單引號 + 機械鎖擴面 + 訂正被證偽的邊界宣稱） | fixed@R59：① 全部 42 行改加**單引號**（bash/zsh/PowerShell 三殼皆為引號字元，與 R57 修法一致），優先序為執行期站點 → 設定檔 → 文件；② 機械鎖 `tools/tests/test_extras_quoting_zsh_safety.py` 擴面——正則共同前綴抽出後分四支 target（裸 `.[…]`／路徑前綴／具名套件／**未加引號的插值 target**），掃描面加 `*.toml`／`*.yaml`／`*.yml`（`pyproject.toml` 此前連掃描面都進不去），計數下限拆為裸形態與具名形態兩支（只釘一支則另一支被整段刪掉仍會綠）；③ docstring 刪去被證偽的「repo 內無此寫法」，改為判例三段式（已實測涵蓋 8 項／已實測不涵蓋 5 項／明文聲明非窮舉）。**bug-injection**：同時注入三處（具名／`.toml` 掃描面／插值）→ 鎖翻紅並逐一點名；以 Edit 逐行還原（**未用 `git checkout --`**）→ 4 tests OK。**驗證**：AutoClaude 全套 3740 passed / 208 skipped（與基線相同、零 failed、skipped 未變動）；`check_loc_budget` total=20356 完全未變（只在字串內加引號、未增行）；ruff 逐檔 before/after delta=0；另在本 venv（確認無 sqlalchemy）**真的觸發** 6 處錯誤路徑，實見印出 `pip install 'autoclaude[postgres]'`。**修復包主動揭露一項我方遺漏**：`rg` 預設跳過 dotfile 目錄，故 `AutoClaude/.claude/skills/integration-redis/SKILL.md` 的 `pip install redis[hiredis]`（bash code fence，真的會被複製貼上）**在主控與掃描員的清單裡都漏了**，已一併修 |
| DEF-101-508 | 2026-07-28 | R59 Workflow 五維掃描 Scan-A（自 DEF-101-507 分列：形態不同、且逃出既有錨的方式不同） | **`tools/bootstrap_core.py` 在安裝失敗時印給使用者的修復指令本身是壞形態**：`_err(f"❌ 依賴安裝失敗（rc={rc}）：pip install -e {autoclaude_target}")`，其中 `autoclaude_target` ＝ `<abs>/.[dev,notifications,lint]`。macOS zsh 下照著做同樣 `no matches found`。**此形態逃出既有錨的方式與 DEF-101-507 不同**：既有錨要求 `.[` 緊接在 `pip install [flags]` 之後，此處中間隔了一段絕對路徑；**更關鍵的是在原始碼字面上方括號根本不存在**（f-string 插值），純文字掃描無論把路徑前綴錨修得多好都抓不到那個站點 | P3（同 DEF-101-507 家族；命中時機是使用者已經在處理安裝失敗的當下） | 本輪修復（`tools/bootstrap_core.py`）+ 機械鎖新增第四支分支 | fixed@R59：訊息內插值加單引號。機械鎖為此**另立第四支「未加引號的插值 target」分支**（`pip install -e {var}` / `${VAR}`）——這才是真正守得住本站點的那支，經 bug-injection 坐實。**修復包順手多修一行並說明理由**：同函式 `pip install -r {sdd_req}` 亦為裸插值路徑，雖無 extras 方括號，但 repo 被 checkout 到含空白的路徑（如 macOS `/Users/John Doe/`）時貼上去照樣壞，同函式同性質同 WHY 一併加引號。**修復包自陳的殘留風險（如實記載）**：第四支分支的判準（「印出的 pip 指令裡有裸的 `{…}`／`${…}` 就算違規」）比另外三支寬，將來若有人合理地寫 `pip install -e ${SOME_VALIDATED_VAR}` 會被判違規而需寫豁免；方向上寧可誤報是對的，但它比另外三支更容易讓後人覺得吵。另 `_MAX_EXEMPTIONS` 現為 10、已用 9（絕大多數是本輪三段式邊界宣稱本身必須引述壞形態所致），緩衝偏緊，下輪若再新增引述行需同步評估上限 |
| DEF-101-519 | 2026-07-28 | R59 Workflow 五維掃描 Scan-B 提出（B-1），主控**先派唯讀 agent 查核前提**再決定是否修 | **NTFS 禁用檔名／保留裝置名淨化家族（4 份生產實作）不滿足本 repo 對 parity 鎖的常設前瞻性要求**：`docs/06_quality/CrossPlatform_Scan_Dimensions.md` 明文要求必要重複的家族「改為『資料抽 SSOT + 機械 parity 鎖』，並**確認 parity 鎖確實有前瞻性（能抓到「新增第 N+1 份」而非只驗白名單內既有幾份**——R43 曾因此翻修過一次）」。實測 `test_windows_forbidden_filename_parity.py`／`test_ntfs_trailing_space_device_name.py`／`test_component_sanitizer_shared_layer_lock.py` 及 SDD 側對應測試對 `ls-files`／`rglob`／`glob(` **零命中**，全靠具名枚舉——「未來有人寫出第 5 份較弱的獨立重寫」零機械訊號。相對地姊妹家族 WindowsApps guard 三份跨語言實作**有**這種前瞻鎖。**動工前先查核前提（唯讀 agent）**：① 生產實作恰為 4 份（四組交叉 grep 收斂一致）；② **當前零違規**——沒有第 5 份，也沒有漏淨化呼叫點（借 repo 既有 AST 掃描器並放寬判準後掃 AutoClaude／根層／SDD 共 292 檔，12 筆 raw 命中逐筆讀原文證實全為假陽性：或整條 f-string 已包在淨化函式內、或前一行已淨化、或 id 由內部固定前綴產生器組成）；③ 姊妹鎖成本＝**868 行、橫跨 R40→R57 約 6 輪翻修、至今仍掛一筆永久 open 的 P3（DEF-101-333）**。另據帳本，本家族「新站點」已復發 7 次以上（DEF-101-219／295／343／346／349／384／390／442／478） | P3（前瞻性防護缺口，非現存缺陷；但常設要求未落到本家族，且威脅模式有 7 次以上實證復發） | 本輪修復（折中：**不新建掃描器檔案**，在既有 parity 測試內加一段 repo-wide 等值枚舉斷言） | fixed@R59：`tools/tests/test_windows_forbidden_filename_parity.py` **+162 行 / -0**（未新建檔案）。候選＝`git ls-files`（rc≠0 即 fail-loud「掃描邊界不得靜默縮小」）取 `*.py`／`*.sh`／`*.ps1` ＋三處 git-hooks 目錄（涵蓋無副檔名的 `pre-commit`），排除 SDD 非 LATEST 版（**LATEST 版號**走 `sdd_version.py` SSOT、不自寫；但「排除非 LATEST」這一半**確實自寫**了 `_FROZEN_SDD_VERSION_RE`，即該家族第 5 份複本——見該檔就地標註與 DEF-101-521。**二審 SA-R59-03 訂正**：本括號初稿寫「不自寫版本正則」，讀起來像整段都沒自寫）與測試檔，實測候選 **444 份**。錨為兩類聯集（保留名清單依序／禁用字元集合依序，`re.IGNORECASE`）——取聯集的理由是 DEF-101-343 的真實形狀是「只淨化字元、完全沒有保留名防護」，那種第 5 份只有字元錨看得到。**斷言用等值而非下限**：等值天然同時得到 stale 自檢（少一份也會紅）。**bug-injection 雙向**：① 在未登記的 `tools/git-hooks/post-commit` 插入第 5 份 → 翻紅並點名（同時坐實無副檔名 hook 面有效）；② 移除註冊表一筆 → 翻紅；皆以 Edit 還原（**未用 `git checkout --`**）。**驗證**：該檔 24 tests OK；根層 `run_root_unittests.py` **653 tests OK / skipped=10 / EXIT=0**；ruff 該檔全過。**兩項與原始敘述不符、以實況為準（如實留痕）**：① 註冊表不是 4 筆而是 **7 筆**——錨在生產面另命中 3 個**只在註解／docstring 提及清單**的檔案；修復包刻意**不做剝註解**（那正是姊妹檔 868 行的無底洞），改為「粗粒度錨 + 註冊表每筆強制帶角色註記」逼登記者當場分診。② 原假設「R43 疑似翻修過一次」實為**翻修不只一次、同輪就翻修兩次**。**修復包自陳最脆的三處（如實記載，供下輪）**：字元錨的 `.{0,5}` 間隙寬度是對現有 bash 寫法量出來的、非從語意推出（換寫法可能剛好超過而漏掉）；7 筆裡 3 筆是註解＝43%，若未來文件式註解變頻繁，本鎖可能變常亮紅燈而被加豁免、重演姊妹檔的腐化路徑；未在 macOS/Linux 實跑過。**行數預算超支已被 surface 並由主控裁定接受**：162 行 vs 交辦的 ~90 行，超支全在主控自己要求的三段式邊界宣稱逐項列舉與三段 WHY，純邏輯約 45 行仍在交辦的 30~60 內，故不砍 |
| DEF-101-520 | 2026-07-28 | R59 流程紀實（非程式/文件缺陷，記事存證——比照 DEF-101-436／492 先例） | **本輪三項流程實況值得留痕**：① **R58 整輪作廢的處置方式**——R58 經使用者判定失控（`reset --hard 75aab89`；根因記於 `[[cross-platform-r58-aborted]]`：假前提撐出規模＋複審自我發散〔blocking 7→1→10〕無停損線，1.7k 生產碼 vs 7.9k 鎖檔換 5 條真缺陷）。但 R58 **已提交**的部分（3 commits／659 insertions）內含 5 筆真缺陷修復，與失控的部分（未提交 WIP，10,811 insertions）性質完全不同。② **本輪三度踩到「驗證載具本身是肇事者」**。③ **agent 回報的一項誤報**（紀律 #17 要求誤報與真缺陷同樣留證） | —（流程紀實） | 本列僅存證，供下輪對帳 | no_action_needed（流程紀錄）。**① R58 回收的判準與執行**：只回收已提交的 code/doc 檔（`dev_start.py`／兩支 nightly 載具／三支測試／`useMacWin.md`／`AutoClaude/CLAUDE.md`／紀律完整版／`macos-compat-ci.yml`），**刻意排除兩個衍生產物**——`AutoClaude/.perf_baseline.toml`（其 `git_sha` 欄位指向 `4954404`，一個已不存在於任何分支的 commit；把指向死 commit 的基線寫回去是不誠實的取證）與 `AutoClaude/.drift_log_history.jsonl`（nightly 每輪自行追加，今晚即會重新產生）。**回收後逐筆重新獨立驗證、不採信 R58 的宣稱**（見 DEF-101-502~506 各列），其中 DEF-101-503 特別以具鑑別力的載具重驗，因 R58 自承其原始驗證「綠得沒有鑑別力」。**② 三次載具事故**：(a) 以 Git Bash 呼叫 `windows_smoke_local.ps1` 得 PASS=11 FAIL=2 假紅，原生 PowerShell 得 PASS=12 FAIL=0（已修為機械強制，DEF-101-511）；(b) 以 Git Bash 經 pyenv `python.bat` shim 驗 DEF-101-503 時，**該載具會吞掉 shim 的 stdout**（連 `print('HELLO')` 都零輸出、只有 stderr 透得出來），一度誤判「修復後零輸出＝比原 bug 更糟」，改原生 PowerShell 後結論才成立；(c) 以 `pip list \| grep` 做污染檢查在 uv venv 上零鑑別力（已修，DEF-101-516）。**共同教訓**：`[[verification-carrier-must-trigger-bug]]` 這條在本輪三種不同形狀下各命中一次——**每次都是「綠/空輸出」被誤讀為好消息**；判準應固定為「這個載具在缺陷存在時會不會說話」，而非「指令有沒有報錯」。**③ agent 誤報留證**：extras 修復包回報「`test_install_windows_nightly.py` 目前工作樹裡有主控自己留著的 BUG-INJECTION 未還原」——主控親驗該檔第 214 行實為正確的 `which("powershell") or which("pwsh")`、全檔零 `BUG-INJECTION` 字樣，該 agent 讀到的是主控還原**之前**的快照。方向無害（提醒比漏報好），但正是紀律 #17「zero-trust 須雙向」的實例：agent 對工作樹狀態的宣稱同樣需要親驗，尤其在多方並行編輯同一棵樹時。同輪另有一次由 NTFS 鎖修復包回報的併行假紅（`test_check_pytest_baseline_sites` 在主控補 roster pin 的同一分鐘翻紅，單獨重跑 OK）——符合既有已知模式 `[[parallel-mutation-audit-collision]]`。**④ 帳本體積治理**：動工前主檔 243,212 bytes（距 warn 線僅 2,548），依 R57 交接指示先歸檔 `archive_28`（10 筆）→ 218,976；寫入本輪條目後再視實況決定是否二次歸檔（見歸檔索引 archive_28 條目） |
| DEF-101-522 | 2026-07-28 | R59 四方一審 SD-R59-01（逐 hunk 讀 + 原生 PowerShell 探針），**主控獨立探針證實** | **本輪回收的 DEF-101-506 修復①，在文件主推的 Windows 開發流程上是死碼，而且會印出一句與事實相反的取證**：`run_local_nightly.ps1` 的 venv PATH 自我正規化用 `Join-Path $env:VIRTUAL_ENV 'Scripts'`（產生**反斜線**）去比對 PATH 項，但 `.venv\Scripts\Activate.ps1` 插入 PATH 的字串是 `"$env:VIRTUAL_ENV/Scripts"`（**正斜線**）——字串比對必然不等 → venv Scripts 原封不動留在 PATH → `Test-IsRealPython` 找到的仍是 venv 的python → **走進成功分支印出「已自本行程 PATH 移除…，使直譯器解析與 schtasks 排程等價」**。而 `. tools\dev_start.ps1`（Windows 開發者的文件指定入口）正是走這條路。**主控原生 PowerShell 探針實測**：`PATH[0]=D:\...\.venv/Scripts`（正斜線）vs `Join-Path=D:\...\.venv\Scripts`（反斜線）、`before=31 kept=30`（少的那一項是**空字串**，不是 venv）、`resolved python = D:\...\.venv/Scripts\python.exe`（仍是 venv 的）。**為何 R58 與 R59 回收時都沒測出來**：從 Git Bash 啟動 powershell 時 msys 會把該項轉成反斜線、比對剛好成立——**又一次「載具剛好會過」**（本輪第四次現形）。**既有機械鎖為何抓不到**：`test_nightly_interpreter_determinism.py` 20 支斷言全是形狀檢查（有沒有 `-split ';'`／`Join-Path`／`$pathBefore`），對「比對永遠不成立」結構上零鑑別力 | P1（本輪自己列為 P1 的缺陷，修復無效且同時對外宣告已修好——屬本輪反覆在抓的「宣稱與實況不符」） | 本輪修復（`run_local_nightly.ps1` + 行為級鎖） | fixed@R59：① 比對前兩邊都正規化（`.Replace('/', '\\').TrimEnd('\\')`）；② 加後置條件——數**真的比對成功幾項**（`$removed`），為 0 即走 WARN 分支、**不得宣告已等價**（刻意不用「總項數變化」自檢：實測 `$_ -and` 會濾掉尾端空字串項造成 31→30 的假象）；③ 新增**行為級鎖** `TestPathNormalisationBehaviour`（自 `.ps1` 抽出比對式本體，在 PowerShell 子行程中對一個含**正斜線**寫法的合成 PATH 執行，斷言該項真的被判相符並移除；路徑以 `tempfile` 動態取得——寫死假磁碟路徑會被 `test_platform_neutral_paths.py` 的鎖攔下，R59 落地時當場被攔）＋`test_zero_hit_must_not_claim_equivalence`＋形狀面補「必須有 `.Replace('/','\\')`」斷言；④ QA-R59-03 另補**位置鎖**（正規化區塊必須在 `$script:PyExe` 解析之前——整塊搬到之後會讓 7 支斷言全綠但完全無效，屬「存在但無效」另一成因類別）。**修復後主控原生 PowerShell 實測**：`removed=2`、`BRANCH=SUCCESS`、**venv Scripts 已離開 PATH（count=0）**、`resolved python = C:\Users\...\pyenv-win\shims\python.bat`＝正是 schtasks 等價的直譯器。11 tests OK |
| DEF-101-523 | 2026-07-28 | R59 收尾驗證期間主控觀察到的非決定性翻紅（依 DEF-101-499／R57 ARCH-R57R3-03 的留證紀律記載） | `tools/tests/test_dev_start.py::TestOrphanChildLockRegression::test_peek_reflects_real_child_process_lifetime` **偶發假紅**：`AssertionError: 1976 is not None : ② 子行程結束後 peek 應回傳 None`。根因＝`del proc` 只丟掉 Python 端參照，**Windows 核心釋放行程物件仍有極短延遲**，期間 `OpenProcess` 仍可能成功 → `_pid_alive()` 回 True。生產程式碼無缺陷（正式場景是另一個全新 dev_start 行程檢查陌生 PID），是**測試端的競態** | P3（假紅來源；不修則未來每次都可能讓人去追一個不存在的缺陷，或養成忽略紅燈的習慣） | 本輪修復（測試端改有界等待） | fixed@R59：斷言前加 ≤2s／20 次輪詢的有界等待，語意仍是「子行程結束後必須回 None」，但不再把「核心尚未釋放」誤判為缺陷。**驗證**：修前於連續執行中出現 1 次紅、隨後 3 次重跑皆綠（確認非決定性）；修後連續 **4 次**全綠。留證理由：本 repo 對非決定性翻紅有明文紀律（R57 曾遇 1/14 翻紅但當時 runner 不保留失敗細節、失敗測試名隨行程消失而無法歸因），本次有完整訊息故當場歸因並修掉，不留給下一輪重新追 |
| DEF-101-525 | 2026-07-28 | R59 收尾實際下 commit 時被 pre-commit 的 ruff leg 擋下 | 本輪 DEF-101-507（extras 加引號）在 5 個檔各只改了 docstring／執行期訊息的 **1~2 個字元**（`AutoClaude/autoclaude/utils/config.py:260`、`infra/adapters/sdk_executor_adapter.py:4`、`infra/repositories/pg_{state,memory,playbook}_*.py` 的檔頭與 ImportError 訊息），但 `AutoClaude/tools/git-hooks/pre-commit:56` 的 ruff leg 是 `ruff check "${staged_py[@]}"`——掃描面為**整個已暫存檔**，不是 diff。於是 DEF-101-018 已登記的既有存量債被整批拖進閘門，`git commit` 得到 **47 errors** 而中止。分佈：`pg_state_repository.py` 18／`sdk_executor_adapter.py` 17／`pg_memory_store.py` 8／`pg_playbook_repository.py` 3／`utils/config.py` 1。**逐筆核對確認 47 筆沒有一筆是本輪造成的**（`git diff --cached -U0` 顯示我的改動落在 L4／L260 等處，而錯誤落在 L28／L31／L113 等處）。🔴 這是一個**治理層形狀**而非單點缺陷：整檔掃描面讓 DEF-101-018 的存量債變成地雷——任何跨平台文字修復只要碰到帶債的檔就被擋，而擋下來的內容與跨平台毫無關係。 | P2 | 依 `AutoClaude/pyproject.toml` 既有慣例就地清理（該檔 per-file-ignores 的註解明寫R16 那次同型「跨平台複審一審發現的既有債」是**同輪一併清理**，非長期豁免） | **fixed@R59**（未使用 `--no-verify`／`AUTOCLAUDE_SKIP_HOOKS=1`，全程照閘門走）。`ruff --fix` 修掉 **43 筆**（連鎖後總數由 47 增為 53）：UP045 `Optional[X]`→`X \| None`、UP037 去掉多餘的字串化註記、UP017 `timezone.utc`→`datetime.UTC`、UP035 `typing.Callable`→`collections.abc.Callable`、I001 import 排序。**剩 10 筆逐筆人工判斷**：①**F401 `AsyncEngine` 刻意不刪** — 它在本檔僅出現於 docstring，但這行 import 是 `sqlalchemy.ext.asyncio`（需 greenlet）的**可用性探針**：上方三行 `sqlalchemy` / `sqlalchemy.exc` / `sqlalchemy.dialects.postgresql` 都不涵蓋該子套件，缺 greenlet 時唯有本行會拋 ImportError 使 `_SQLALCHEMY_AVAILABLE=False`；刪掉會讓「sqlalchemy 有裝但 async 支援缺失」的環境從 import 期延後到執行期才炸 → 改 `# noqa: F401` 並就地寫 4 行理由（ruff 建議的 `importlib.util.find_spec` 改寫屬更大動作，不在本輪）。②**E402** — `from .pg_async_utils import _make_retry, _run_async` 原在 L79（helper 函式之後），先實查 `pg_async_utils.py` 只 import `asyncio`／`concurrent.futures`／`logging`／`sqlalchemy.exc`、**不反向 import 本檔**確認無循環風險後上移至頂部 import 區。③**8 筆 E501** — `sdk_executor_adapter.py`／`pg_playbook_repository.py`／`utils/config.py` 共 4 筆走正規斷行或無損縮寫（`W-68-1 守門 / W-70-1` 的空格斜線改全角頓分隔，文義不變）；`pg_state_repository.py` 的 4 筆撞上 LOC 天花板，另立 **DEF-101-526**。**過程中自傷一次並留痕**：寫給人看的說明註解裡直接寫出「井號＋noqa」的完整字面，被 ruff 解析成真的 noqa 指令並印 `Invalid noqa directive` warning——說明文字自己製造 lint 噪音；已改寫並在該處註明原因。收尾驗證：5 檔 `ruff check` **All checks passed 且零 warning**、已暫存的 AutoClaude 側 `.py` 全數綠、`check_loc_budget` violations=0。 |
| DEF-101-526 | 2026-07-28 | 修 DEF-101-525 的 E501 時當場撞上另一道硬閘 | `autoclaude/infra/repositories/pg_state_repository.py` 在 `tools/check_loc_budget.py` 的 `adapter<=400` 分級下**正好卡滿 400/400、零餘裕**（`count_loc` 只跳過空白行與 `#` 開頭行，docstring 與續行都計入）。E501 的正規修法是斷行，**每處至少 +1 行**；4 處斷行後實測 `[TIER] adapter<=400 … 406 > 400 (+6)` → `violations=1`，換成 LOC 閘門紅。+6 的分解：+5 來自我的 4 處斷行（`_ensure_run_id(...)` 呼叫 +2、`values: dict[...]` 字典 +2、logger 格式字串拆兩段 +1），**+1 來自 ruff 自己的 I001 自動修復**把函式內 `import os, warnings` 拆成兩行。亦即在**滿載檔**上「修 lint」與「守 LOC 預算」互斥，而兩者都是硬閘；此衝突現況下**只有踩到才會發現**，沒有任何機械物會事先警告。 | P2（治理衝突，非單點程式缺陷） | 本輪取行內豁免緩解；衝突本身列 R60 候選 | **fixed@R59（緩解）／衝突未解**。改法＝該檔 4 處 E501 全部用**行內 noqa**（0 行成本，且 E501 對該檔其他行仍然有效）。**為何不用 `per-file-ignores`**：`pyproject.toml` 現有兩筆豁免（`prompt_builder.py`／`scaffold_sprint_section.py`）的正當性都是**語意理由**——三引號模板是資料內容、斷行會改變送往 LLM／寫入檔案的實際文字；本例是**預算理由**，性質不同，且 per-file-ignores 會讓整檔**永久**失去 E501 保護（含未來新寫的行），行內 noqa 只豁免這 4 行。**解鎖條件**已就地寫在該檔 logger 定義之後的 `#` 註解區：本檔哪天被拆分、或 adapter tier 經 ADR 重新評估而有餘裕時，改回斷行並移除這 4 個 noqa。該說明**刻意用 `#` 而非塞進模組 docstring**——docstring 行會被 `count_loc` 計入、`#` 不會，把說明寫進 docstring 等於再吃掉 6 行預算、當場復發本缺陷。**R60 候選**：把「LOC tier 滿載檔 × lint 斷行」列為固定掃描檢查點（如 `check_loc_budget` 對餘裕 ≤ 3 行的檔印 warning），讓下一個踩到的人事先知道。 |
