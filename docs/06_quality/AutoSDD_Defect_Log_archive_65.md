# AutoSDD Defect Log — Archive 65

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **61 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-271`、`DEF-101-470`、`DEF-101-308`、`DEF-200-039`、`DEF-200-028`、`DEF-200-032`、`DEF-200-038`、`DEF-200-007`、`DEF-200-027`、`DEF-200-029`、`DEF-200-034`、`DEF-200-018`、`DEF-101-412`、`DEF-200-036`、`DEF-200-033`、`DEF-200-035`、`DEF-101-999`、`DEF-200-037`、`DEF-200-011`、`DEF-200-013`、`DEF-101-977`、`DEF-200-025`、`DEF-101-967`、`DEF-101-993`、`DEF-101-348`、`DEF-200-017`、`DEF-200-024`、`DEF-200-009`、`DEF-200-031`、`DEF-53-001`、`DEF-200-030`、`DEF-200-019`、`DEF-101-309`、`DEF-101-214`、`DEF-200-004`、`DEF-101-400`、`DEF-101-296`、`DEF-200-006`、`DEF-101-997`、`DEF-42-001`、`DEF-200-008`、`DEF-101-925`、`DEF-101-961`、`DEF-101-984`、`DEF-101-055`、`DEF-101-994`、`DEF-101-313`、`DEF-101-996`、`DEF-101-982`、`DEF-101-990`、`DEF-101-987`、`DEF-200-001`、`DEF-101-983`、`DEF-200-005`、`DEF-200-002`、`DEF-200-003`、`DEF-101-989`、`DEF-101-985`、`DEF-101-986`、`DEF-200-014`、`DEF-101-988`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：（未填）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-271 | 2026-07-23 | R26 Architect 架構專項檢視 Part A6（本輪使用者明確要求之架構最佳化評估附帶挖出） | **LOC 分級治理範圍未涵蓋「薄殼化」收斂後產生的根層 `tools/` Python 核心檔**：`AutoClaude/tools/check_loc_budget.py:91` 的 `SCAN_ROOT` 寫死為 `"autoclaude"`，只掃 `AutoClaude/autoclaude/` 套件本身；R12/R16 已將雙原生腳本（`.sh`/`.ps1`）大幅收斂為「薄殼＋Python 核心」架構（`tools/check_wrapper_thinness.py` 機械鎖住 10 支薄殼），但收斂出的 Python 核心檔（`tools/dev_start.py` 現已 **1772 行**、`tools/bootstrap_core.py` 348 行、`tools/integration_gate_core.py`）完全不在任何 LOC 分級政策（`service ≤500`／絕對紅線 `≤750`）管轄範圍內——複雜度沒有消失，只是從「跨平台橫向重複」搬成「單檔案垂直堆疊」，且無機械閘門守護這個新檔案類別的成長上限 | P4（非緊急 bug：`dev_start.py` 目前 56 個 top-level 函式/類別、平均每個約 30 行，仍屬模組化，非失控義大利麵；四支既有機械治理工具本輪實測皆 exit 0，無任何測試失敗或功能錯誤，純粹是治理範圍縫隙） | 擴充 `check_loc_budget.py` 的 `SCAN_ROOT`（或另建輕量版）涵蓋根層 `tools/` 下經薄殼化收斂出的核心檔，並針對「這類檔案該設多寬的上限」（例如比照 `service` 500、或另訂更寬鬆的 `orchestrator` 級 1500~2000）— 屬需要人工拍板分級門檻的設計決策，不當場動 | closed-by-decision ｜🔴 R81：前提不成立（原狀態欄全文逐字保全於 `CrossPlatform_R81_Ledger_Triage.md` §5）。R81 當回合實跑 `AutoClaude/tools/check_loc_budget.py` rc=0，輸出含 `violations=0 (absolute=0 tier=0 special=0 root_tools=0 total=0)` ⇒ 根層 `tools/` 已有自己的分級（`guardrail_cli`／`guardrail_lib`，`root_tools` 獨立計數）， 本列「收斂出的 Python 核心檔完全不在任何 LOC 分級政策管轄範圍內」今日為假（治理缺口已由 R68／R69 補上）。⚠️ 同一次量測揭露 `[special<=2000] ../tools/dev_start.py: 1999`（餘裕 1 行）——拆分責任由 `DEF-101-398` 承接，正確處置不是調高門檻 |
| DEF-101-470 | 2026-07-27 | R56 round 4 QA B-1（回報者自標 platformRelevant=false，依本輪授權範圍未動手） | `AutoClaude/tests/test_shell_deny_chars_parity.py::test_goal_freeze_gate_rejects_each_deny_char`（R56 新增的「行為層鎖」）**只餵單一 prompt**：把 `goal_freeze_gate.py` 的 `for i, prompt in enumerate(prompts):` 退化為 `enumerate(prompts[:1])`，該測試 16 passed 全綠、`test_goal_decomposer.py` 33 passed 亦全綠——完全偵測不到。實害：gate 對「多步驟拆解」逐步掃描才有意義（`prompts` 一步一筆、上限 12），只掃第 0 筆等於步驟 1~11 的注入字元一律自動放行，而該測試的自述目的正是「黑名單真的接在裁決路徑上」 | P3（測試保護力缺口，但與作業系統平台無關） | 本輪刻意不修，記帳待其他軌處理 | fixed@R80 ｜🔴 R81 結案（原狀態欄全文逐字保全於 `CrossPlatform_R81_Ledger_Triage.md` §5）：回報者給的建議修法已逐字落地於 `AutoClaude/tests/test_shell_deny_chars_parity.py:96-120`（多步驟 prompts ＋ 斷言「步驟 1」出現在 `verdict.reason`），R81 當回合實跑 `16 passed`／rc=0。R60 判的「死信」是狀態欄沒跟上，不是工作沒做 |
| DEF-101-308 | 2026-07-24 | R35 四方二審 Architect（對追加的靜態一致性鎖再次 bug-injection） | 純字面計數比對可被同檔任何位置的 decoy 註解繞過（繞過手法 #1）；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.5 | P3（低機率對抗性繞過，Architect 判定非阻擋） | 未來若要堵死可改用解析式驗證，成本較高、超出比例原則 | closed-by-decision@R82：依 DEF-101-400 已訂下的決策準則（第 6 個繞過手法出現前不再疊正則特例），當回合實查已知繞過手法計 5 個 ⇒ 準則未觸發，本列無行動項｜R82：AST 佐證那半句撤回，結論不動（證據檔 §8.5-R82） |
| DEF-200-039 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P5 | `test_platform_neutral_paths::TestShebangImpliesLfLineEndings::test_no_new_shebang_file_carries_a_non_lf_line_ending`：乾淨 checkout 重現，逐字 `[] != ['AISDLC_SDD/AISDLC_SDD_v0.30/tools/arch_fitness/arch_fitness.py']`。欠債清單是**相等**語意且釘在 Windows 側量到的集合 ⇒ 換一次 checkout 就多一支 | P2 | blob 層零容忍承接平台中立那一半；工作樹層改**子集合**語意（登記的每筆理由仍須成立） | fixed@R83（複驗＝`test_no_tracked_blob_pairs_a_shebang_with_a_carriage_return`＋`test_every_registered_debt_entry_still_has_a_platform_neutral_reason`，rc=0） |
| DEF-200-028 | 2026-08-10 | R83 訴求 1（reset 後由真舵手自己接手）技術可行性裁決 | **架構天花板，登記而非修**：launchd／schtasks 叫起來的一跑沒有 TTY ⇒ 回來的必然是受限 headless 代理（`session_resume_planner.py` 走 `claude -p -r <sid>`，spawn 時注入 `AUTOSDD_UNATTENDED=1`，PreToolUse 守衛據此硬擋 commit／push）。**真舵手回來只能由人跑 `claude -r <sid>`**（該檔兩處逐字指路） | P1 | 不實作。續航鏈的定位是「保住可重啟點並把人叫回來」，不是「代替人決策」 | closed-by-decision@R83：載體沒有 TTY，非取捨；要改變它需 harness 層支援，不在本 repo 射程 |
| DEF-200-032 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P3 | `test_dev_start::TestMacNightlyPlistCapabilityTable::test_healthy_plist_passes_every_capability_row`：乾淨 checkout 重現，逐字 `'項與期望不符' unexpectedly found in …`，肇因兩列＝`⚠️ WakeToRun 對等 = 無 pmset`／`⚠️ NextRunTime 對等 = 無`。控制組把「plist 內容」與「這台機器排過 pmset 沒有」混進同一個全綠斷言 ⇒ 沒排 pmset 的 mac 必紅 | P2 | 拆 stub pmset 測試縫（預設回報健康），plist 內容與機器電源狀態分開判 | fixed@R83（`test_dev_start.py` 的 `set_pmset()` 縫＋三態語料，rc=0；兩列仍留在斷言內，非刪列） |
| DEF-200-038 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P5 | `test_platform_neutral_paths::TestActiveSourceEolIsRatchetedSeparatelyFromTheFrozenSurface::test_the_repo_wide_scale_is_measured_not_quoted`：乾淨 checkout 重現，逐字 `0 not greater than 0 : 全庫零漂移？請確認取數管道（本判準不該恆綠）`。**反空轉判準自己**掛在「本機工作樹漂移總量 > 0」上，而乾淨 checkout 必為 0 ⇒ 反空轉成了工作樹相依 | P2 | 反空轉載體換成在兩個平台都非零的正控 | fixed@R83（同名測試現行版 docstring 逐字「反空轉的載體換成了**在兩個平台都非零**的正控」，rc=0；判準未改成不比較） |
| DEF-200-007 | 2026-08-10 | R83 六包並行共用一棵工作樹（**真實事故**，非假想） | 一個 subagent 為做失敗歸因執行 `git stash -q -u --keep-index`，瞬間清空 16 個修改檔 ＋ 4 個未追蹤檔（全是別包正在寫的）。靠 `stash pop` 還原、未偵測到資料遺失，但**那是運氣不是設計**：破壞性 git 原語的射程是整棵工作樹，而當時零守門 | P0 | PreToolUse 阻斷 ＋ ADR 記載並行扇出的快照語意 | fixed@R83（`.claude/hooks/block_destructive_git.py`；鎖 `tools/tests/test_block_destructive_git_r83.py` 含行接續繞過向；設計 `docs/04_planning/ADR/ADR-XPLAT-006-parallel-agent-snapshot-fanout.md`）|
| DEF-200-027 | 2026-08-10 | R83 首次在 mac 上真的跑 `tools/run_shellcheck.py` | R82 的豁免代碼寫錯：`tools/git-hooks/pre-commit` 的動態 source 寫 `disable=SC1091`，而 shellcheck 對該行實際報的是 **SC1090**（source 路徑是變數）⇒ **從未被壓下**。兩輪沒被發現的原因是這道閘門的載具是 **docker**、雲端 CI 自 2026-08-05 帳務停擺、R82 當輪在 Windows（docker 未必開）⇒ **沒有人看過它的 rc** | P3 | 豁免改 `SC1090,SC1091`（兩者都是刻意的動態 source，同 `_BASELINE` 內既有兩筆） | fixed@R83（現查 `git diff tools/run_shellcheck.py` 為**空**＝`_BASELINE` 未放寬，仍 11 筆／7 鍵） |
| DEF-200-029 | 2026-08-10 | R83 複審 A-01（mac 續航武裝接通、回收沒接） | 落地當時 `tools/lib/sentinel_lifecycle.py` 的 GC 硬走 `powershell.exe`：import 數＝**0**、mac 上 rc=**127**、`sentinel_task_names()`＝`[]` ⇒ GC 逐字回「沒有任何…工作」＝**假陰性**；同一刻 `launchctl list` 有 2 支活哨兵（本 session ＋ 探針孤兒 `AutoSDD_Sentinel_s`） | P1 | 回收臂改問 `schedule_backend.select()` | fixed@R83（收斂回合複驗：`sentinel_task_names()` 現回**兩支**真實 label、後端＝`LaunchdBackend`）。🔴 殘留機器動作：該孤兒落筆時仍活著，需人跑 `--remove-schtasks --task-name AutoSDD_Sentinel_s` |
| DEF-200-034 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P4 | `test_doc_loc_baseline_freshness_r60::TestR81GhostPathClaims::test_no_ghost_path_claims`：乾淨 checkout 重現 4 筆，逐字「`docs/04_planning/R76_HANDOFF.md` 指名一個不存在的路徑 `AutoClaude/.g0_readiness.json`」。該檔是 **gitignored 機器本地生成物**：跑過 G0 的機器有、別台沒有 ⇒ 同文件兩機一綠一紅 | P2 | 判準加第三態 `ignored`（`git check-ignore` 合取）：量測面由機器檔案系統換成 repo 的宣告 | fixed@R83（複驗＝`test_the_third_state_does_not_move_when_the_file_appears_or_disappears`；現查 ghost=0／stale=0、豁免表 17/17 未放寬） |
| DEF-200-018 | 2026-08-10 | R83／W7 Architect 對額度軸「接電」的獨立設計 | 額度軸外套**三層同義過濾器**：①註冊面 matcher ②hook 呼叫點的 `if blocking and …` ③`tools/lib/quota_gate.py::quota_gate` 的 `tool not in blocking` ⇒ **只改第②層等於零改變**，產出假交付（判例 #3「機制蓋好沒接電」第四次復發）。證明刻意用**計數 `decide()` 幾次**而非看 rc（兩者皆 0 會誤判）：紅端 PostToolUse×{Read,Bash,Grep,Glob,Task} 一律 **0 次** | P1 | 三層同時處理；第③層改為只在 PreToolUse 看名單 | fixed@R83（鎖＝`test_context_budget_guard.py::QuotaGateIsWiredToTheBurnPathTest`） |
| DEF-101-412 | 2026-07-26 | R52 Architect 架構最佳化評估（使用者任務②，非缺陷掃描） | evaluator_command 有「同進程生成即消費」與「編譯期產出、執行期可能跨行程消費」兩種本質不同場景，sys.executable 慣例只適用前者；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.9 | P3（架構層前瞻判準記錄，非現行缺陷） | 建議把該判準補入正式架構文件當第一道分診問題 | closed-by-decision@R82：判準已逐字落在證據檔 §8.9（受體積守門與指針稽核）；本條刻意無機械物——它是人動手前要問自己的分診問題，蓋一支掃描器只會得到恆綠 |
| DEF-200-036 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P4 | `test_doc_loc_baseline_freshness_r60::TestR67R3ThisFileMakesNoUnstatedPlatformAssumption::test_every_lock_in_this_file_holds_under_every_simulated_platform`：逐字「本檔有鎖的結果隨 sys.platform 改變」。**連帶紅**：它把同模組其餘鎖在 darwin／linux／win32 各跑一次，幽靈路徑那支使三平台各列 1 筆（本回合逐平台實測 1／1／1，訊息同 034） | P2 | 隨 034／035 的第三態一併消解；**不得**加 skip（該測試訊息自己寫：加 skip＝該平台永遠沒覆蓋） | fixed@R83（同名測試現行版 rc=0；子套件 >50 支的空轉自證仍在） |
| DEF-200-033 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P3 | `test_dev_start::TestMacNightlyPlistCapabilityTable::test_status_prints_exactly_the_rows_static_extraction_predicts`：乾淨 checkout 重現，逐字 `False is not true : 健康 plist 的每一列能力都應為 ✅，實得 […⚠️ WakeToRun…⚠️ NextRunTime…]`。與 `DEF-200-032` 同根因的第二個受害者：抽取器對帳單也把機器 pmset 狀態算進「每列皆 ✅」 | P2 | 同 032：pmset 拆縫後三態可分辨（已排定／有 pmset 無排程／無 pmset） | fixed@R83（同名測試現行版，該類別 4 支 rc=0；仍逐列比對抽取器預測，非放寬成「多數 ✅」） |
| DEF-200-035 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P4（第 10 支，工作樹狀態相依） | `test_doc_loc_baseline_freshness_r60::TestR81GhostPathClaims::test_the_baseline_is_not_stale`：**只在帶機器本地生成檔的工作樹上紅**——本回合以 HEAD 版判準對主工作樹重跑得 stale=**1**（逐字「`AISDLC_SDD/.claude/settings.local.json` 已在基線豁免表上，但它現在**解析得到**了」）、對乾淨 checkout 得 **0**：同一筆登記兩台機器判決相反 | P2 | 同 034 第三態；該筆自豁免表移除（gitignored 生成物不是欠債） | fixed@R83（複驗同 034；現查該路徑 `resolve_doc_path`＝`ignored`） |
| DEF-101-999 | 2026-08-10 | R82 首次在 macOS 真機啟動（切換 SOP 實走） | 切換後段五處必踩坑：(a) 主 `.venv` 必然汙染致回填 rc=2 拒跑而提示詞未言明；(b) B 段建法用 `uv`，本機無 `uv`；(c) zsh 兩陷阱（`PIPESTATUS` 空、變數不分詞）曾誤報三支守門為紅；(d) nightly FAIL 判讀二分法漏「舊紅已修＋新紅冒出」第三態；(e) 回填排在閘門前致根層檔被自己的紅鎖住，同段亦缺 docker 前置。證據見 `CrossPlatform_R82_Mac_Switch_Obstacles.md` | P2 | useMacWin.md 一次修畢；機械化列 Next Action | fixed@R82（文件層修畢；五子項零機械物，三鎖見該證據檔） |
| DEF-200-037 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P5 | `test_platform_neutral_paths::TestActiveSourceEolIsRatchetedSeparatelyFromTheFrozenSurface::test_active_surface_python_eol_does_not_grow`：乾淨 checkout 重現：「活躍面 .py 行尾漂移：實測 0 已低於重釘下界 189（上限 220）」＋凍結面同型。判準量**工作樹位元組**、界釘在 Windows 值 | P2 | 拆兩平面：blob 層（`ls-files --eol` 的 `i/` 欄，每機同值）為平台中立零容忍閘門；工作樹層降為本機健康度 | fixed@R83（複驗＝`test_no_tracked_blob_carries_a_carriage_return`＋`test_a_clean_checkout_is_green_but_one_stray_file_is_not`，rc=0） |
| DEF-200-011 | 2026-08-10 | R83 舵手實跑 `python tools/run_root_unittests.py` | `tools/lib/windows_skip_tags.py` 總表頭印 `len(problems)`＝七類問題的總和，而後面那個明細迴圈只涵蓋兩類 ⇒ 唯一那筆問題落在別類時，讀者看到「發現 1 個問題：」之後**一片空白**，於是去找一個不存在的第二筆；「掃描面為空」那一類從頭到尾沒有任何程式碼印它 | P2 | 計數與明細改由單一資料結構驅動（`{類別: [明細…]}`），表頭取 `sum(len(v))` | fixed@R83（F3 包；`render_problem_report()` ＋ `test_run_root_unittests.py::ProblemReportItemizationTest` 以 AST 雙向比對類別鍵）|
| DEF-200-013 | 2026-08-10 | R83／W2-A 排程後端介面對稱性審視 | `list_jobs()` 只住 `LaunchdBackend`／`NoCarrierBackend`、`SchtasksBackend` 沒有 ⇒ 有人寫 `select().list_jobs(…)` 時，同一行在 mac 工作、在 Windows `AttributeError` | P3 | **減法**：當時實查全庫零呼叫端零測試＝死碼 ⇒ 從兩後端刪除，非補一支給 `SchtasksBackend` | fixed@R83（F2 包當時刪除）。🔴 R83 收斂訂正（A-06 預言成真）：`DEF-200-029` 的 GC 移植需要列舉原語 ⇒ 同輪**回補**為三後端對稱，現查 `def list_jobs`＝**3 個定義**、消費者＝`sentinel_task_names()`。原減法在當時為真，非推翻 |
| DEF-101-977 | 2026-08-08 | R81 開場（歸檔後閘門轉紅） | 歸檔把 3 列搬離主檔，OVERSIZE_ROW_GRANDFATHERED 那 3 筆當場過期、判準轉紅；歸檔器既不偵測也不提示 ⇒ 每輪歸檔都復發、每輪手動修 | P2 | 建議 archive_defect_log 在 --plan／--apply 就地列出「本次會讓哪幾筆豁免過期＋三個常數該下修到多少」 | fixed@R82：ledger_rotation.expiring_oversize_waivers 為純讀計算，--plan 逐筆預告過期 ID 與三個新值；紅綠兩向注入自證見 tools/tests/test_archive_defect_log.py::TestR82RotationSideEffectsAreAnnounced，複驗與逐字原文見 CrossPlatform_R82_Ledger_Closure.md §9 |
| DEF-200-025 | 2026-08-10 | R83／W8 驗證者點名（非自己發現） | `tools/lib/skip_group_policy.py` 兩句散文被**同一個 commit** 證偽：①「本輪沒有 mac 真機…健康值今天無論如何取不到」而本輪就是第一輪有 mac 真機；②「分子（`_MEASURED_RUNNERS_MIN`）不動」而下一行已是 `4`。兩者皆屬「註解裡的反向假事實」，與「把有人守的寫成沒人守」同型 | P3 | 就地訂正並保留原判斷沿革（不刪 R82 原話，它在當時為真） | fixed@R83（兩處逐字訂正皆在該檔）。🔴 附帶登記：**這一族散文零機械物在守**——註解自我證偽不會有東西轉紅 |
| DEF-101-967 | 2026-08-08 | R80 包 A（QA-03） | exec form 只轉了根層那份，`AutoClaude/.claude/settings.json` 的 6 條仍是 shell form（該 session 閃窗一次沒少），而 CLAUDE.md 寫成通則、判準掃描面只有根檔 ⇒ 那 6 條退回 shell form 永遠不會轉紅 | P2 | 立 `SHELL_FORM_CENSUS`：掃描面現查磁碟、判準相等（多＝退步、少＝沒下修基準），凍結版具名排除；CLAUDE.md 與該檔 description 明寫射程 | fixed@R81：轉成 12 條 exec form，啟動器沿用根層唯一那支（`../`），普查表兩格皆 0；CLAUDE.md 與該檔 _comment 同步訂正。剩餘：判準 A~F 掃描面仍只有根檔 |
| DEF-101-993 | 2026-08-09 | R82 帳本清債包實測帳本三條基線的「只准往下改」宣稱 | OVERSIZE_ROW_GRANDFATHERED／OVERSIZE_ROW_CEILING／OVERSIZE_ROW_EXCESS_CEILING 三者散文皆自稱 shrink-only、零成長容忍，實測**零觀測者**：唯一釘住它們的測試是與當下實測逐字相等，帳本一長就要求把常數調高，調高後四向判準全綠。探針輸出見 CrossPlatform_R82_Ledger_Closure.md §9 | P2 | 補 shrink-only 觀測者：把三條基線的重釘史做成單調不增的具名序列 | fixed@R82：三條基線各配一條重釘史序列＋單調判準，往下釘綠、往上釘紅，兩向皆合成注入自證 |
| DEF-101-348 | 2026-07-24 | R42 SA 一審提出「為何本機 pre-push 未攔下 R37 回歸」 | 根因待查：git 不記錄 hook 是否執行；R47 於 scratch worktree 實測 0.97s PASSED 重現了「POSIX 路徑下該斷言 trivially 通過」；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.7 | P3（流程／方法論疑問，非程式碼缺陷） | 待查，非本輪程式碼可解 | closed-by-decision@R82：方向② 結構上不可回溯（git 不記錄 hook 執行，本列自述）；方向① 今日已有載體（[WINDOWS-NATIVE-ONLY] 標籤族＋兩支 compat workflow，當回合實查 7 支檔命中）⇒ 本列無殘餘可執行動作 |
| DEF-200-017 | 2026-08-10 | R83 收尾（掌舵者察覺「token 沒再增加」） | `until ! pgrep -f <字面>; do sleep N; done` 等待迴圈，兩支以上並行時**互相匹配對方的 pgrep**（pattern 字面就寫在每支 `pgrep -f` 自己的 argv 裡；`man pgrep` 的 `-a` 條目說預設只排除自己與**祖先**、不排除兄弟）⇒ 條件永不成立且**完全靜默**。三支殼因此卡死。實測與兩筆訂正見 `CrossPlatform_R83_Scan_Findings.md` §F | P2 | 正解配方（字元類自我否定 `run_root[_]unittests`）落證據檔 §F | fixed@R83（孤兒殼已殺）。§F-6 劃界：**無機械物**，寫回裸字面不會轉紅 |
| DEF-200-024 | 2026-08-10 | R83／W8-G1 驗證者對「測試釘的是狀態不是性質」修法的複驗 | **同型病第二版**：改寫本身仍用 `next()` 從真表挑語料 ⇒ 等 linux 兩列依配方畢業後 `next()` 拋 `StopIteration`、**測試轉紅且訊息為空**（實測逐字只有 `StopIteration`），比原版更難接手 | P3 | 判準改為對整張豁免表求性質，不從真表挑單筆 | fixed@R83（`AutoClaude/tests/tools/test_local_ci_gate.py::test_the_darwin_runner_graduated_and_every_remaining_gap_still_has_a_recipe`：全表迴圈判準 `_recipeless_exemptions()` ＋三條 monkeypatch 注入紅向自證，每條帶訊息） |
| DEF-200-009 | 2026-08-10 | R83／W2-B AutoClaude pytest skip 成因歸因（可發現性） | skip 的最大單一類**全部**只因「docker daemon 沒開」——不是缺件、不是平台差、不是退化；解法是一行 `docker compose -f docker-compose.ci.yml up -d`（零程式改動、零環境變數，`tests/conftest.py` 的 PG autodetect 自己注入 DSN）。實測 172 → 76 skipped。缺的是指路：此前任何 skip 報表都沒說 | P2 | ONBOARDING §7.1 定為唯一站點；可發現性錨上鎖 | fixed@R83（`test_skip_discoverability_r83.py::TestPgSkipRemedyStaysDiscoverable`；`useMacWin.md` 只指向 §7.1、刻意不重抄）|
| DEF-200-031 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P2 | `test_context_budget_guard::MeterFailureShapesTest::test_unreadable_credentials_and_http_401_are_different_answers`：乾淨 checkout 重現，逐字 `(None, 'http-401') != (None, 'no-credentials')`。「憑證讀不到」被寫成單一期望值，而 darwin 走 Keychain、不讀那個檔 ⇒ 該臂在 mac 結構性量不到 | P2 | 補憑證來源注入點 `token_detail(platform, runner)`；判準改 `win32`／`darwin` **顯式雙欄矩陣**（兩欄在任何主機都跑） | fixed@R83（`tools/lib/quota_meter.py` 的 `token_detail`＋雙欄；rc=0。**非** skip 掉 mac 那欄） |
| DEF-53-001 | 2026-06-23 | C 軌 `.claude` 審查輪八（SA 鏡 P3-2） | `hub_merge.py` `_read_yaml`/remote→local copy 未 cap 讀 pull-cache yaml；`detect_conflict`/`resolve_or_record` 經查僅測試呼叫、未在任何 runtime 路徑 wire → 無 runtime 消費者、無活路徑可觸發（latent） | P3 | RFC（框架本體 `tools/fsm_runtime/`，`build/planning/active/SDD_improving_Automation_NN.md`） | fixed@R81：`_read_yaml` 補讀取上限，復用 `hub_sync` 的 `MAX_HUB_FILE_BYTES`（不另立第二份常數）。新增 `TestMergeReaderFileSizeCap`（4 支，含紅綠注入），v0.30 PR 閘門 1746 passed／rc=0。原文見 §5 |
| DEF-200-030 | 2026-08-10 | R83 動工前 mac 首跑 10 紅／P1 | `test_check_hooks_liveness::TestHookLauncherContract::test_stdin_argv_and_cwd_match_the_old_shim`：於 HEAD `7975140` 乾淨 checkout 重現，逐字 `PosixPath('/private/var/folders/…') != PosixPath('/var/folders/…')`。判準以**字串**比 hook 子行程回報的 cwd 與 `tempfile` 路徑，而 mac 的 `/var`→`/private/var` 是 symlink ⇒ 同一目錄兩個字面 | P2 | 問檔案系統不問字串：`os.path.samefile`（比核心的 `(st_ino, st_dev)`），零平台分支 | fixed@R83（同名測試現行版，本回合 rc=0；**非**把 mac 字面加進期望值） |
| DEF-200-019 | 2026-08-10 | R83／W7 驗證者對實作者交件的獨立複驗 | 實作者未申報的閘門紅：程式註解 **14 處**自稱 `R84`，而 `current_round()` 現查為 **83** ⇒ 「程式碼自稱輪號超前帳本當前輪」那道鎖轉紅（`tools/tests/test_check_defect_log_crossref.py::TestR71CodeRoundLabelsNeverExceedLedgerCurrentRound`）。根因是鐵律四「宣稱先於查證」的實例——**採信任務書寫的輪號框架而未查權威源**，不是筆誤 | P2 | 全數改回 R83；輪號一律現查權威源不得從任務書轉抄 | fixed@R83（現查 `grep -rn R84 tools/ .claude/hooks/`＝**0 命中**） |
| DEF-101-309 | 2026-07-24 | R36 一審 SD 首先發現，二審 Architect／SA／QA 三方獨立覆核 | 未錨定 re.search 掃全檔可被行內註解誘餌繞過，且繞過門檻較舊版實質降低（繞過手法 #2）；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.5 | P3（測試鑑別力縫隙；有 ruff F401 正交防線） | 建議改用 ast.get_source_segment() 鎖定目標函式再比對 | closed-by-decision@R82：依 DEF-101-400 決策準則歸檔（同 DEF-101-308）；ruff F401 正交防線今日仍在，殘餘風險以繞過手法計數登記於證據檔 §8.5，達 6 個即觸發 AST 方案評估 |
| DEF-101-214 | 2026-07-21 | R15 四方一審（QA／SD 各自親歷；Architect 修復階段主控自身重演） | 共享單一工作樹的多 agent 併發審查存在真實交叉污染，同輪三度實例；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.2 | P2（方法論／流程風險，非程式缺陷） | 建議複審改用 Agent 工具 isolation: worktree 隔離 | closed-by-decision@R82：方法論列，其建議早已成慣例且無獨立行動項；同根因的現行載體＝DEF-101-886（多 agent 共用工作樹使閘門 rc 成為別人鍵盤的函數），殘餘的機械化需求併入該列追蹤 |
| DEF-200-004 | 2026-08-10 | R83／QA 複驗（launchd 憑證對抗） | `LaunchdBackend.verify_cli` 只驗「存在」卻逐字印「run interval …〔與請求相符〕｜argv 回讀 N 項相符」——那兩句從未被比對過（`_descriptor_problems` 只在 `arm()` 裡跑）。實測把 job 換成每 7 秒跑 `/bin/echo I-AM-NOT-THE-SENTINEL` 的冒牌 plist 再 bootstrap，本指令仍 **rc=0** | P1 | verify 側只驗「不需要原始請求就驗得到」的那幾件，驗不到的（argv 逐項）明寫未比對 | fixed@R83（W2-A 包；`schedule_backend.py::LaunchdBackend.verify_cli` docstring 逐字記載）|
| DEF-101-400 | 2026-07-26 | R50 Architect 全面架構複審（非缺陷掃描） | WindowsApps guard 的 repo-wide 防增生掃描呈現正則軍備競賽訊號，五輪疊加；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.5 | P3（架構層已知邊界，非缺陷） | 決策準則：若出現第 6 個繞過手法，優先評估 AST-based 掃描而非再疊正則特例 | closed-by-decision@R82：準則列本身無行動項；行動項已轉為可查登記——已知繞過手法逐個具名列於證據檔 §8.5（現計 5，觸發門檻 6），並吸收 DEF-101-308／309／313／333 四列 |
| DEF-101-296 | 2026-07-24 | R33 四方一審 Architect；二審 Architect 定位出可重現根因 | 多支 pytest 並行對同一測試檔跑出間歇假紅，baseline 2/40 fail、設 PYTHONDONTWRITEBYTECODE=1 後 0/40；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.4 | P3（非阻斷） | 審查階段統一設 PYTHONDONTWRITEBYTECODE=1 規避競態 | closed-by-decision@R82：與 DEF-101-268 同一根因（__pycache__ 寫入競態）、同一組三選一拍板選項、同一個處置動作，併入 268 單一載體承接；本列 2/40 對照數據作為 268 的量測佐證保留於證據檔 |
| DEF-200-006 | 2026-08-10 | R83／QA 複驗（方法論層，最貴的一筆） | `tools/tests/test_mac_endurance_r83.py::_print_output` fixture 原為**扁平**，而真機 `launchctl print` 是**巢狀** ⇒ 該檔 30 條綠全部成立在一個比真實世界簡單的世界裡，`DEF-200-005` 那個缺陷結構上不可能被它抓到。**fixture 與被測世界形狀不符＝最貴的一種假綠**：檔案在、測試綠、分母卻是假的 | P1 | fixture 改巢狀並保留巢狀對照組 | fixed@R83（W2-A 包；`test_mac_endurance_r83.py::_print_output` docstring 逐字記載訂正理由）|
| DEF-101-997 | 2026-08-09 | R82 複審 QA B-1 | sealed_prefix_problems() 的封印表自己零觀測者：只判「封印比史料長」，對「封印被砍短」無判準，而 _SEAL_TAIL_MAX=1 剛好讓砍掉最後一格合法 ⇒ 把超長列上限 85 放寬成 90 只要改兩行（史料＋封印）就全綠（實測 A 紅、B 綠） | P2 | 封印表本身要有總長度下限＋內容摘要，兩者須在同一次變更內一起重釘 | fixed@R82：tools/lib/ledger_rotation.py 的 seal_table_problems() 三向判準；紅綠兩向注入自證見 tools/tests/test_check_defect_log_crossref.py |
| DEF-42-001 | 2026-06-21 | improving_42 階段一 Re-Audit（ci-gate v0.17 pytest） | 框架測試 flaky（Windows 並行檔案鎖）：`v0.17/tools/fsm_runtime/tests/test_file_lock.py::test_parallel_writes_do_not_lose_increments` full-suite 並行下間歇 PermissionError，ci-gate v0.17 偶報 1 failed；主 agent 隔離 3/3 全綠 → 確認**非回歸** | P3 | routed（flaky 隔離追蹤、非回歸；框架本體 RFC） | closed-by-decision｜R81：凍結版 v0.17 依 Copy-on-Evolve 不改，原文已記隔離 3/3 全綠＝非回歸 flaky；LATEST 若復發另立新列。原文見 §5 |
| DEF-200-008 | 2026-08-10 | R83／W2-B「示範指令只在單一平台成立」掃描 | 立案站點＝`AutoClaude/tests/conftest.py` 的 PG DSN 修法訊息：一則**專門把人導向正解**的訊息自己只印 `$env:…`（PowerShell 專屬），mac 讀者照抄無效；同一判準反向亦命中（只印 `export …`）。同族共 6 檔（現查 `git diff --stat AutoClaude/`） | P2 | 同一則訊息兩平台都印並標明平台；掃描器上鎖並附陳舊標記反向鎖 | fixed@R83（`tools/tests/test_skip_discoverability_r83.py::TestUserFacingMessagesWorkOnBothPlatforms`）|
| DEF-101-925 | 2026-08-07 | R79 ARCH 包（架構減法） | 護欄層行數棘輪自述「淨增一行即紅」，但其稽核列逐字記著 R77 加 3505 行、R78 加 2243 行，連兩輪向上重釘且閘門全程綠；精確結論＝它對靜默成長有牙，對「重釘加補一列理由」零方向約束 | P2 | 加跨輪累積淨額判準（本輪只以真減法往下壓 915 行，判準形狀未改） | closed-by-decision@R82（併入 DEF-101-947：同一判準缺陷，947 的雙單邊判準是本列的超集；本列 R77／R78 數字已見於 947 原文，資訊零損失） |
| DEF-101-961 | 2026-08-08 | R80 掌舵者當輪新提（訴求 a/b） | 額度水位改用 **%**（帳號不同 ⇒ 絕對量不可比）、80% 少派 agent、95% 停止並準備喚醒。三方設計完成但 ADR 與實作兩階段皆在額度上限陣亡 ⇒ 本輪零交付且 repo 內零載體 | P1 | 先解「分母的取數管道」再談門檻：單一校準點解不開「口徑」與「分母」兩個未知數 | fixed@R82（「repo 內零載體」今日不成立：quota_meter／limits／ledger／escalation 四支＋80／95 兩軸門檻；ADR 半邊由 DEF-101-980 承接） |
| DEF-101-984 | 2026-08-09 | R81 Architect 複審收斂 | 根層 `.claude/hooks/` 不在任何 LOC 治理面內且無明文豁免，於是巨檔長到絕對紅線兩倍以上而零訊號；壓力只作用在已被量的那一層 | P2 | 走納入治理而非明文豁免，門檻取納管當下實際行數的 shrink-only 棘輪 | fixed：`ROOT_GUARD_ROOTS` 納管，四支走 tier、一支走 raw-line 棘輪。🔴 誠實：當下不會讓任何東西變小。拆分那支巨檔不在本輪射程，承接輪次：**R82**。證據見 `CrossPlatform_R81_Scan_Findings.md` §B-2 |
| DEF-101-055 | 2026-07-12 | DEF-101-053 修復收緊 match= 時揭露 | ORM CheckConstraint 宣告名與 DB 實際 constraint 名分歧（0001 的兩個匿名 inline CHECK）；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.1 | P3 | 記事存證不阻塞；CRUD 契約測試已改綁 DB 實際名 | closed-by-decision@R82：當回合實查 AutoClaude/tests/contract/test_pg_existing_schema_lock.py:405 與 :424 逐字 match= DB 實際名 ⇒ 該列自述的唯一動作項已完成；對齊 ORM 名屬 cosmetic 且 PG 未上線，日後真要做另立新列 |
| DEF-101-994 | 2026-08-09 | R82 帳本新增 DEF-101-993 一列後，歸檔器測試當場轉紅 | tools/tests/test_archive_defect_log.py 的合成列 ID 寫死**真實帳本正在使用的家族號** 101-（合成號 992／993／996／997）⇒ 帳本號碼加上來就與沙箱合成列撞號，而失敗訊息長得像「bullet 樣式不被解析」，與真因無關。實測：帳本新增 993 後該測試立刻 rc=1 | P2 | 合成 ID 改用真實帳本不會用到的家族號 | fixed@R82：抽 _SYNTH_FAMILY 常數改為 999-，五個站點共用，撞號面歸零 |
| DEF-101-313 | 2026-07-24 | R37 一審 SD（對 WindowsApps guard 收斂做 bug-injection） | 呼叫端以額外 -or 條件覆蓋共用函式回傳值可繞過存在性檢查（繞過手法 #3）；原文逐字保全於 CrossPlatform_R82_Ledger_Closure.md §8.5 | P3（有 check_wrapper_thinness.py 內容 hash 正交防線有效補位） | 可將存在性檢查由 regex 升級為條件運算式解析 | closed-by-decision@R82：依 DEF-101-400 決策準則歸檔；該列自述的正交防線（內容 hash 釘選）今日仍接在本機 CI 對等與 pre-push 上 |
| DEF-101-996 | 2026-08-09 | R82 複審 SA B-1 | 平台注入接縫是 mock.patch.object(<模組>.os, "name", …)，改到的是 stdlib 那一個 os 物件＝行程全域 ⇒ 同一份判準在 unittest 與 pytest 下判決相反（pytest 的 AssertionRewritingHook 對每支新 import 呼叫 Path()，Windows 上拋 NotImplementedError） | P2 | 注入接縫收回模組層自己的名字，不得動行程全域 | fixed@R82：改走 tools/lib/windows_skip_tags.py 的 running_on_windows()；鎖＝tools/tests/test_run_root_unittests.py 的 CarrierVerdictParityTest |
| DEF-101-982 | 2026-08-09 | R81 Architect 複審收斂 | 額度節流閘在快取過期／不存在時對任意規模的扇出全放行；而過期是常態（唯一刷新點在該支線上且不等它、哨兵零刷新、TTL 僅 180 秒） | P0 | 量不到的第一次扇出改同步量一次（有界逾時），並把文件宣稱的逐字稿地板真的接進閘門 | fixed：加 `claim_refresh_slot`＋`refresh_quota_blocking`＋`quota_floor_reading`；鎖＝`QuotaUnmeasurableFanoutTest`。證據見 `docs/06_quality/CrossPlatform_R81_Scan_Findings.md` §B-2 |
| DEF-101-990 | 2026-08-09 | R81 SD 複審收斂 | 三筆非 blocking：a 節流帶不看 reset 有多遠；b ADR-XPLAT-005 §3.3 planner 第二處改動沒做且無處標注未做；c `fanout_cap` 覆寫註解的不變量是空話 | P2 | a 只把差別說出來不動 cap 階梯；b 至少標注未做；c 訂正註解不得複述假話 | fixed（a c）：`throttle_horizon_line()` 三支字串各異（鎖＝`ThrottleBandSaysHowLongItLastsTest`）、覆寫註解改寫。b 僅標注（planner 749／750 無空間）；b 與 cap 分檔政策承接輪次：**R82** |
| DEF-101-987 | 2026-08-09 | R81 SD 複審收斂 | 額度軸「量不到」時完全靜默，`QUOTA_UNMEASURABLE` 在 production 是死碼；token 過期／斷網／schema 升版／meter 失聯與「額度很健康」外觀一致，使 B3 B4 皆不可偵測 | P0 | pct-None 分支須出 stderr 加 jsonl 痕跡並帶 source，且要有閂鎖免得每次都吵 | fixed：`note_degraded()`（per-source TTL 原子閂鎖）；四種失效注入前實測 rc=0 stderr 0 bytes 零痕跡，修後各自具名出聲。鎖＝`QuotaDegradationIsAudibleTest` |
| DEF-200-001 | 2026-08-10 | R83／W2-A mac 續航武裝實走 | `tools/session_resume_planner.py` 的 `--arm-sentinel` 在 mac 上印「❌ 本功能只在 Windows 成立」卻回 **rc=0** ⇒ 失敗被讀成成功，呼叫端（SessionStart hook）無從得知哨兵根本沒掛上。與〈反事後諸葛〉那條「憑證是值不是 rc」同型，只是這次連 rc 都在說謊 | P1 | 平台差異收斂進 `tools/lib/schedule_backend.py`，mac 側走 launchd | fixed@R83（W2-A 包；回歸鎖 `tools/tests/test_mac_endurance_r83.py`）|
| DEF-101-983 | 2026-08-09 | R81 Architect 複審收斂 | 額度快取的檔案契約有三份互不相關的字面複本，而生產綁定零覆蓋：既有測試全部傳明確 path，改掉取數器檔名會使寫讀分家而永遠不節流，全套照綠 | P1 | hook 端不得再持有第二份字面，並補鎖綁定兩端 | fixed：`quota_cache_path`／`quota_schema` 改為委派 `tools/lib/quota_meter`，desync 結構上不存在；鎖＝`QuotaCacheContractHomeTest`。證據見 `CrossPlatform_R81_Scan_Findings.md` §B-2 |
| DEF-200-005 | 2026-08-10 | R83／QA 複驗（同一次對抗） | 憑證的 `state` 取到 `launchctl print` **巢狀子區塊**（`resource coalition`／`jetsam coalition`，depth 2）的值＝恆 `active`：實測同一刻 PID 欄為 `-`、depth-1 `state = not running`，而憑證印 `active`。「掃到就覆蓋、最後一個贏」的解析器對巢狀輸入必然取到最深那一層 | P2 | 解析限定 depth-1 | fixed@R83（W2-A 包；`schedule_backend.py::LaunchdBackend._readback` 的 depth-1 訂正逐字記載）|
| DEF-200-002 | 2026-08-10 | R83／W2-A 同一次實走 | `.claude/hooks/context_budget_guard.py` 四支武裝臂各自 `os.name != "nt"` 早退 ⇒ mac 上三條武裝路（SessionStart 自動武裝／額度哨兵巡邏／reset 續航）全部**靜默**失明：hook 照跑、照 exit 0，畫面上與「這台機器沒有額度風險」完全相同 | P1 | 四支武裝臂改由 `schedule_backend.select()` 分派載具 | fixed@R83（W2-A 包；鎖＝`tools/tests/test_context_budget_guard.py` 的武裝臂集合斷言）|
| DEF-200-003 | 2026-08-10 | R83／W2-A launchd 取證管道逐指令實測 | `launchctl load` 對**不存在**的 plist 也回 **rc=0**（假綠，與 `Get-ScheduledTask` 對不存在工作回 rc=0 同型）⇒ rc 從來不是憑證，查得到查不到才是；有鑑別力的是回讀（`launchctl print` 對不存在的 job 回 113） | P2 | 憑證一律走 `launchctl print` 回讀，rc 不當憑證 | fixed@R83（W2-A 包；證據＝`test_mac_endurance_r83.py::test_an_absent_job_is_not_a_credential` 逐字）|
| DEF-101-989 | 2026-08-09 | R81 SD 複審收斂 | 額度憑證單一來源、零平台分支、失效靜默：`fetch_usage` 分得出 401 與斷網，唯一呼叫端卻把 status 丟掉，四種失效一律回同一個 None | P1 | 先把「憑證讀不到」與「HTTP 哪個碼」分開回報；沒有真機前不得憑猜寫平台分支 | fixed（可觀測那一半）：`measure_detail()` 回 (讀數, reason)；鎖＝`MeterFailureShapesTest`。🔴 非 Windows 憑證來源仍未驗，承接輪次：**R82** |
| DEF-101-985 | 2026-08-09 | R81 Architect 複審收斂 | ADR-XPLAT-005 的 TTL 推導掛在已被自己實作證偽的量上（兩點外推出漂移率，而實作端記載該量非單調）；同份 ADR 別處已有訂正區塊，只有這格沒做 | P2 | 訂正成「TTL 是挑的值」並指名重量入口，不得逐字複述原假話 | fixed：ADR §1.3 A3 與 §2.4 改寫（留真實讀數、刪外推鏈），指名重量入口 `quota_meter.py --watch`；並記下 B1 已使過期不再等於放行 |
| DEF-101-986 | 2026-08-09 | R81 SD 複審收斂 | 扇出派發帳跨行程掉行且撕行（8x40 實測掉 30.9%），撕行又被計數器靜默跳過；掉 undo 留幽靈計數把遠低於 cap 的單次派發誤擋 | P0 | 一次派發改為一次目錄項建立，撤銷改 unlink 自己那一個；回歸鎖須多行程 barrier | fixed：新增 `tools/lib/quota_ledger.py`（該檔 docstring 載三組實測 LOST=0）；鎖＝`FanoutLedgerConcurrencyTest`／`PhantomCountNoLongerBlocksTest` |
| DEF-200-014 | 2026-08-10 | R83／W2-A 常數居所盤點 | `CRED_KEY_LAUNCHD` 住兩個家（後端與消費端各一份）⇒ 同一份知識兩個家、只有一個會被改（同 R73 `Find-GitBash` 判例） | P3 | 收斂到 `tools/lib/schedule_backend.py` 單一家，消費端一律引用 | fixed@R83（W2-A 包；現查 `grep -n CRED_KEY_LAUNCHD tools/session_resume_planner.py` 兩處皆為 `schedule_backend.` 引用、零字面複本）|
| DEF-101-988 | 2026-08-09 | R81 SD 複審收斂 | `claim_refresh_slot()` 是 check-then-act 零原子性：16 個壁鐘 barrier 行程實測 CLAIM=16 SKIP=0（意圖 1）。用 `Pool.map` 量會得到 1，那是既有測試看不到它的原因 | P1 | stamp 改原子建立，過期先 unlink 再搶；回歸鎖同樣要 barrier | fixed：委派 `quota_ledger.claim_once()`，修後實測 CLAIM=1 SKIP=15；鎖＝`RefreshSlotConcurrencyTest` |
