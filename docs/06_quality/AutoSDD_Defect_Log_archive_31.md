# AutoSDD Defect Log — Archive 31

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **22 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實四項判準（狀態已結／狀態欄無活躍字樣／未被 crossref 目標做過狀態宣稱／
> 散文帶交棒字樣者需 `--ack-handoff` 具名承認），並在落地後以 `--check` 稽核行尾、
> 同檔內 ID 無重複列、跨檔狀態分類無矛盾、帳本家族全部「立帳見」指針帶可解析 ID
> 且居所宣稱與實況一致。**歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-552`、`DEF-101-548`、`DEF-101-549`、`DEF-101-527`、`DEF-101-540`、`DEF-101-536`、`DEF-101-531`、`DEF-101-533`、`DEF-101-532`、`DEF-101-547`、`DEF-101-543`、`DEF-101-538`、`DEF-101-546`、`DEF-101-542`、`DEF-101-537`、`DEF-101-535`、`DEF-101-553`、`DEF-101-528`、`DEF-101-539`、`DEF-101-545`、`DEF-101-544`、`DEF-101-551`
>
> **本次操作備註**：R60 收輪前第二次歸檔。動工前 archive_30 一次搬足 41 筆後主檔為 174,447，但本輪四方複審兩輪（round 1 全數 REJECT/21 blocking、round 2 修 34 項）＋殘留收尾共寫入 30 餘筆條目，主檔長到 244,675、距 warn 線僅 1,085 bytes。本次由 tools/archive_defect_log.py 執行（archive_30 之後新建，判準即程式），22 筆四判準全過；另 4 筆（DEF-101-529/530/555/558）因判準④ 偵測到散文帶交棒字樣而被攔下、刻意不加 --ack-handoff，留主檔供 R61 承接——這正是 archive_30 那次靜默埋葬 DEF-101-517/526 之後補上判準④ 的用意，本次是它第一次在真實資料上發揮作用。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-552 | 2026-07-28 | R60 Scan-F F-02（CONFIRMED；Pkg-7 落地） | **v0.30 `tools/fsm_runtime/tests/test_phase_h.py:215-219 requires_docker_success` 以 `not _DOCKER or sys.platform.startswith("win")` 硬排除全部 Windows，排除面遠大於證據面**。DEF-101-062 的不穩定證據全部來自 GitHub-hosted windows-latest（LCOW 堆疊），但判準寫成 OS 級，把本機 Windows 11 + Docker Desktop WSL2（`docker info` → `linux / 29.5.3`，完全不同的堆疊）一起掃掉。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（覆蓋損失 + 診斷訊息不精確；不會讓任何測試由紅變綠） | 本輪修復（Pkg-7；只動 v0.30 LATEST）。**ADR-XPLAT-001 §4.3 為 N/A、非豁免**：`requires_docker_success` 是 v0.30 才引入——實查 `grep -c` 在 v0.01~v0.29 **全數 0**、v0.30 **9**，凍結版用 `requires_docker`（v0.29:205，只看 docker 可用性、本無 Windows 硬排除）⇒ **不存在凍結版落差**，§9 不得為此虛構一列。本輪依實查訂正，並刻意不在本列留下 §4.3.1 的觸發字面（否則行級 grep 仍會命中自己）⇒ §4.3.2 ① 整列粗篩 **13→12**、§4.3.1 欄位級判準 **12→11**（本輪實測；ADR §4.3.2 現載的「13 vs 12」因此 stale，已具名 routed 給 ADR 獨佔包）。⚠️ 順帶暴露 §4.3.1 的判準限制：它是**行級關鍵字**，分不出「宣稱不予回補」與「引述並否定該宣稱」——未來 C1／C2 機械鎖必須帶具名豁免＋WHY，否則任何引述舊措辭的訂正列都會被誤判為漏補 §9 列 | fixed@R60：① 排除條件由「平台硬排除」改為「能力偵測 ＋ 窄環境例外」——新增 `_windows_ci_runner()`（Windows 且 `GITHUB_ACTIONS` 或 `CI` 有值），核心判準仍是 `docker_available()`（該函式本身即以 DockerBackend 的完整安全 profile 實跑一次容器，比平台名稱強得多），DEF-101-062 的 GH windows runner 例外原樣保留。② reason 拆成**互斥兩段**，讀者一眼看出是「docker 不可用」還是「Windows CI runner 環境例外」。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-548 | 2026-07-28 | R60 Scan-E（E-refuter-2 自找）落地包 Pkg-6 查證；該檔由 R60 Pkg-3 獨佔，Pkg-6 依分包鐵律不得改動 | `tools/tests/test_windowsapps_guard_cross_consistency.py:57-58` `def _pwsh_exe() -> str \| None: return shutil.which("pwsh") or shutil.which("powershell")` 以 **pwsh 7 優先**選引擎，與 R59 DEF-101-509 拍板並寫在 `test_install_windows_nightly.py:193-214` 的判準（「生產是以 `powershell -ExecutionPolicy Bypass -File` 執行＝5.1…`tools/` 樹受 `test_p…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（與 DEF-101-509 同類判準錯誤、方向相反；本機零活體，實害限於同時裝有兩引擎的環境） | 跨包 routed 給 R60 Pkg-3（該檔獨佔者）；Pkg-6 側已把判準本體落成 SSOT 並鎖住方向 | fixed@R60（判準側 Pkg-6 落成 SSOT ＋ 呼叫端由主控遷移完成）：🔴 **R60 round 2 訂正（round 1 SA-R60-08）**——本欄原在合法首詞後又接一段 `partial@R60（判準側已修、呼叫點待 Pkg-3）`：`partial` 非《格式定義》合法值（且會被 `_classify` 的 `fixed` 前綴比對吞掉），而「呼叫點待 Pkg-3」**已不成立**——呼叫端已由主控遷移完畢，實查 `tools/tests/test_windowsapps_guard_cross_consistency.py:43` `from _ps_engine import production_engine  # R60 DEF-101-548`、`:69` `return production_engine()`（round 1 SA／SD／QA 三方各自獨立覆核同一事實）。**殘留（刻意不由本列宣稱）**：該檔在 `_PENDING_MIGRATION_SITES` 的豁免於遷移完成後未移除，導致注入「改回 pwsh 優先」仍全綠——那是另一筆缺陷（round 1 QA-R60-07），由該檔擁有包於 round 2 處理，本列不代為宣稱已修。Pkg-6 已把「生產引擎 5.1 優先」寫成唯一具名述詞 `tools/tests/_ps_engine.production_engine()` 並以**合成雙引擎**情境鎖死方向（bug-injection 實證：優先序對調即 `兩引擎皆在時選了 pwsh 7——與 DEF-101-509 判準方向相反` 翻紅，而本機既有鎖同時仍全綠）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-549 | 2026-07-28 | R60 Scan-E（E-refuter-1 自找）落地包 Pkg-6 查證；帳本由主控獨佔書寫，Pkg-6 只提供查證結果 | `docs/06_quality/AutoSDD_Defect_Log.md:86`（DEF-101-392，日期 2026-07-26／R48 Architect 架構複審）逐字寫「**Copy-on-Evolve 凍結基線政策已兩度（R44／R45）被迫打破鐵律回補**」；同檔 `:91`（DEF-101-401，2026-07-26／R50）寫「亦未發現 R49 之後出現第三次被迫打破鐵律回補的新事證…DEF-101-357/358（R44/R45）**兩次**破例根因同構」。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（兩筆 open watch item／PM 待決依據的事實記載錯誤；同型判例 DEF-101-336「帳本記載與現況不符」亦 P3） | 主控（帳本獨佔書寫）；依帳本「只增不刪」政策應以**補記訂正列**處理，不得改寫 DEF-101-392／401 原文 | fixed@R60（查證完成並產出 ADR）：查證完成@R60（Pkg-6）：**正確次數＝3（R44／R45／R46）**，三次的標的／嚴重度／核准形式／實際判準／修法／代價已逐一整理進本輪產出 `docs/04_planning/ADR/ADR-XPLAT-001-copy-on-evolve-frozen-baseline-backport.md` §3.1~3.3，次數矛盾與影響另立於該檔 §3.4。載具鑑別力自檢：若帳本一致寫 3 次，該 grep 只會命中「三度／三次」、不會出現「兩度」與「第三次打破」並存 ⇒ 非「一律命中」。Pkg-6 **未觸碰任何帳本檔案**（`git status` 對 `docs/06_quality/` 零本包改動）。（完整紅綠對照與逐條指令輸出見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-527 | 2026-07-28 | R60 動工前歸檔（主控 Pkg-1）＋同輪 Scan-D／Scan-G 反駁者交叉抓出本次歸檔自身缺陷 | **帳本歸檔的搬遷判準與保全檢查，歷輪都是在對話裡臨時寫一支腳本、跑完即丟，repo 內零載具**。本輪被四方掃描抓到三件事：① `archive_29`／`archive_30` 標頭都宣稱「由歸檔腳本以閘門自身邏輯機械檢查、逐筆顯式 assert」，但那支腳本只存在於某次 session 的暫存目錄、**不可重跑**——宣稱一道機械檢查存在而它不可重跑，等同沒有檢查；② 標頭散文所載判準與實際執行的判準**不是同一個**：判準②照字面（子字串）執行會把狀態欄含 `OpenMutexW` 的 `DEF-101-504` 誤擋，實際執行版用 ASCII 邊界 lookaround 不會；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（治理機制缺口：歷輪反覆撞 256KB 硬閘〔R56 兩次、R57 三次、R59 兩次、R60 一次〕，而判準與保全檢查從未有可重跑載具，且文件所載判準與執行判準不一致） | 本輪落地為正式工具（主控 Pkg-1） | fixed@R60：新建 `tools/archive_defect_log.py`（判準即程式，直接 import `check_defect_log_crossref` 的 `_CLAIM_RE`／`_classify`／`_ID_RE`／`_ROW_RE` 當 SSOT，不另寫正則）＋ `tools/tests/test_archive_defect_log.py`（10 支，**`unittest.TestCase` 類別風格**——同輪 C-01 證實 pytest 函式風格會被四道 unittest discover 閘門整檔零收集）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-540 | 2026-07-28 | R60 Scan-C C-02（掃描者提出、反駁者以 pyyaml 逐 job 稽核與 git log -S 獨立重現並加強） | **R57 已判定失實並訂正過的「本 workflow 全部／其餘步驟一律 `shell: pwsh`」宣稱，在同一檔案內以 step name ＋兩處註解逐字存活——訂正只改了檔頭**。站點（改動前行號）：windows-compat-ci.yml:904（step **name**，會出現在 GitHub Actions run 的步驟標題上）、:907、:939-940。pyyaml 逐 job 實測：windows-smoke（windows-latest）pwsh 19／bash 1；windows-nightly-full（windows-latest）**powershell 2**／pwsh 3；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（維持裁決評級。保守的一端：L904 是外顯於 CI run 標題的宣稱、非僅內部註解，覆核者若上調 P2 不反對；壓在 P3 的理由是它緊鄰自己的反例） | 本輪修復（Pkg-4 Scan-C）：三處據實改寫 ＋ 新增全檔掃描鎖 | fixed@R60：① 三處改寫，**沿用 R57 政策不寫死支數**——step name 與註解改為「以 `shell: pwsh` 為主要引擎，實際分佈與三類刻意例外見檔頭逐 job 稽核表（本步驟自己就是該表 ③ 的例外之一）」；R48 引述段據 `git log -S` 證據訂正為「該概括在寫下的當時即失實」，保留真正成立的窄斷言（「在本步驟補上之前，本 workflow 沒有任何一步用原生 PS 5.1 執行過 install_post_commit.ps1」）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-536 | 2026-07-28 | R60 Pkg-3 Scan-B B-02（掃描者原判 CONFIRMED、任務書指示「四處統一處理前導空白」）；修復者以自身實測推翻修法方向 | 「保留裝置名 + **前導**空白」形態四份實作 1 擋 3 放，且樣本電池無此鏡像形態（無任何一側設鎖）。實測對照（21 樣本，bash 載具先過 CON.txt=BLOCK/hello.txt=pass 自檢）：' CON.txt' → check_ntfs pass／pre-commit pass／logger ' CON.txt'（原樣）／sanitize_component '_CON.txt'（因首行 `.strip()` 剝掉前導空白而暴露保留名）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（文件與鎖的缺口，非程式行為缺陷：四處行為分歧無任何記載也無機械鎖，導致每輪掃描都會重新把它當鏡像缺口回報；對齊 DEF-101-336「帳本/文件記載與現況不符」判例） | 四處生產實作註解 + 兩份樣本電池 + 三支新斷言 | fixed@R60（Pkg-3，**修法方向與掃描者提案相反、依自身實測決定**）：刻意**不**加擋前導空白（採反駁者裁決的 (B) 案），改把「三放一擋」從『疑似漏修』定案為『刻意不對稱』並**雙向**設鎖。① 新增 SSOT 樣本組 `LEADING_SPACE_RESERVED_SEGMENTS`（6 筆，含前導+尾隨疊加 ' NUL .log' 與新裝置名 ' CONIN$.log'），AISDLC_SDD 側鏡一份 `LEADING_SPACE_RESERVED`，TestCrossSubprojectSampleParity 新增第三支斷言逐字鎖住。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-531 | 2026-07-28 | R60 Scan-A 廣泛掃描 A-01（掃描者原判 P3、反駁者裁 PARTIAL 並主張升 P2）＋反駁者自找 #1；Pkg-2 修復時第三度獨立重跑並訂正機制歸因 | tools/tests/test_bash_probe_spec_contract.py:84（TestUsableBashEndToEndWithRestrictedPath.test_usable_bash_rejects_candidate_when_path_lacks_dirname）在官方閘門 tools/run_root_unittests.py（pre-push:210 + root-infra-ci.yml:293 + macos-compat-ci.yml:314 + windows-compat-ci.yml:420 共四處呼叫）下**誤綠**：unittest 載具 REAL_RC=0「Ran 5 tests…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（維持反駁者訂正值；帳本原判 P3 應升。理由：pre-push＋3 支 CI 共用閘門的安全網破洞，且可自發翻紅並指向錯誤的生產碼結論） | 本輪修復（Pkg-2 Scan-A）；檔案 tools/tests/test_bash_probe_spec_contract.py。生產碼 AISDLC_SDD/scripts/bash_probe.py 刻意不動——「無法 spawn 的 bash 本來就不可用」，回 None 語意正確，缺陷純在測試的鑑別力 | fixed@R60（Pkg-2）：①新增 usable_bash_with_probe_spy() 把 None 的兩種來源分流（completed / spawn_errors）；②wiring 測試載具改為「PATH 指向真實存在但空無一物的 temp dir」——本機實測 rc=127、stderr `dirname: command not found`、Win32 區塊維持 87 筆，於是**首次**在 Windows 上真正驗到生產端 wiring（不需退讓成 skip）；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-533 | 2026-07-28 | R60 Scan-A A-04（掃描者原判 P3、反駁者裁 PARTIAL 並建議「不值得為此動 LATEST 生產碼、只寫註解」）；主控明確指派本包修復，Pkg-2 依「所有問題必須徹底全部修復」驗收標準落地程式修復並補齊反駁者留白的可達性論證 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/hub_sync.py:_mirror_local() 用裸 `shutil.rmtree(dst_sub)` 緊接 `shutil.copytree(src_sub, dst_sub)`，未套用 R15 SCAN-B-2 建立的 _rmtree_windows_safe 硬化先例（grep 實查：修復前該先例只存在於 AISDLC_SDD/scripts/sync_exposed_skills.py:50/148 與其測試檔，v0.30 tools/fsm_runtime 零命中）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（維持；反駁者主張實質可對齊 P4/wontfix 慣例，但主控指派修復，且修法成本極低、已附雙層回歸鎖，故不降） | 本輪修復（Pkg-2 Scan-A），僅動 LATEST v0.30。v0.01~v0.29 共 29 版同檔同款裸 rmtree，依 Copy-on-Evolve 不回補 | fixed@R60（Pkg-2）：**就地硬化而非跨樹複用**——先例位於非版本化的 AISDLC_SDD/scripts/，從 v0.30/tools/fsm_runtime import 會新開跨樹依賴（違反各版自足與 Copy-on-Evolve），且先例把失敗包成 RuntimeError 是為人機互動 CLI，而 hub_sync 的 pull() 把任何例外收進 PullResult.error 當非阻擋警告、換型別會打壞呼叫端 except 形狀。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-532 | 2026-07-28 | R60 Scan-A A-02（掃描者原判 P2、反駁者裁 PARTIAL 降 P3）；Pkg-2 修復時另揪出修法本身的陷阱 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/file_lock.py 兩處移除 sentinel 的 unlink（:80-83 陳舊回收、:93-96 finally）只捕 FileNotFoundError。Windows 上只要第三方持著該 sentinel 的 handle（防毒／搜尋索引器／備份代理，或本模組 docstring 自己邀請的 post-mortem 讀取者），unlink 就丟 PermissionError [WinError 32]。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（採反駁者訂正：兩支 CONTEXT-LEDGER hook timeout=5.0 且對 TimeoutError 早有 append-only sidecar 降級路徑，非資料遺失；但 PermissionError 不在其例外契約內） | 本輪修復（Pkg-2 Scan-A），僅動 LATEST v0.30。v0.01~v0.29 共 29 版同檔各有 3 處同款窄捕，依 Copy-on-Evolve 不回補（歷輪破例皆需使用者核准） | fixed@R60（Pkg-2）：新增 _try_unlink(path)->bool（FileNotFoundError→True 視為已成功、OSError→False 表示仍被持有），兩處移除點共用；陳舊回收分支改為 `if _is_stale(lock_path) and _try_unlink(lock_path): continue`，刪不掉就落回 deadline/sleep 讓 timeout 維持權威（避開無窮忙迴圈）。新增 3 支回歸鎖於 tests/test_file_lock.py：①平台中立注入 PermissionError 驗 finally 不逸出；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-547 | 2026-07-28 | R60 Scan-E（E-A-03）落地包 Pkg-6；反駁者裁決 PARTIAL、評級由 P2 降 P3，且指出**掃描者自己的 grep 輸出漏掉整個檔案**（4 種語意／9 處／2 個具名 helper 三個數字全部低估） | `tools/tests/` 內「本機有哪個 PowerShell 引擎、拿哪一個去跑」在 R60 實查為 **6 檔／10 處功能碼／5 種語意**，無具名 SSOT、亦無防「第 N+1 份選錯」的鎖（`grep -rn 'which("powershell")\|which("pwsh")' tools/tests/*.py tools/*.py` 完整輸出：語意①生產引擎 5.1 優先 6 處＝test_bootstrap_ps1:46／test_dev_start_ps1_lastexitcode:35／test_git_hooks_install_common:447,472,495／test_install_window…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（常設前瞻性要求未落到本家族、非現存缺陷；同構判例 DEF-101-519 判 P3。掃描者引「兩次都判 P1」支撐 P2 是混淆層級——那兩次是有活體後果的**實例**，本項是**預防機制缺席**） | 本輪修復（Pkg-6）；1 處（語意⑤所在檔）跨包 routed 給 R60 Pkg-3 | fixed@R60（9/10 處）：新建 SSOT `tools/tests/_ps_engine.py`——`PRODUCTION_ENGINE_PRECEDENCE=("powershell","pwsh")`（順序即判準）＋五個具名述詞 `production_engine()`／`any_engine_available()`／`windows_with_engine()`／`native_ps51()`＋`windows_with_native_ps51()`／`available_engines()`；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-543 | 2026-07-28 | R60 Scan-D D-01（掃描者提報、反駁者三次嘗試推翻皆失敗判 CONFIRMED；Pkg-5 落地修復並根治） | ONBOARDING.md:214（§7 Windows 11 基線表 LOC 列）寫死 `total=20356 cap=20438 violations=0` 且 Windows 欄標「完全相同」，實測為 20359——且該值在 R59 自己的收尾 commit 樹上就已 stale。三棵樹獨立實測分離：R57 樹(75aab89)=20356、R59 樹(f9435c5)=20359、HEAD=20359，baseline 與計數器在兩 commit 間零變動，成因為被計數原始碼淨增 3 邏輯行。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（同唯一直接先例 DEF-101-289 實測 severity 欄；刻意不上調至 515 的 P2——本筆只是既有表內一個數字未回填，非整表單邊缺席＋容差宣稱主動誤導） | root（monorepo 根層文件＋根層 tools/tests 機械鎖；零碰 AutoClaude 生產碼、零碰 AISDLC_SDD） | fixed@R60（Pkg-5）：① ONBOARDING.md:214 填入親測最終值 total=20361（非任務書給的 20359——並行包改 logger.py 後實測已變，依 MIN_TESTS「填最終工作樹實測值、不做加減推算」紀律填我自己量到的值）；② 同格加 loc-baseline-live 錨點＋機械鎖說明＋明文寫入「填值時點比照 MIN_TESTS 重釘紀律（並行 agent 全停工後才填）」，把本格的結構性易腐性外顯成成文規則；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-538 | 2026-07-28 | R60 Pkg-3 修復者自我複核時發現（本輪自己的修復一度造成，非既有缺陷；含「第一版鎖注入不紅」的二次自我打臉） | **聯集錨會讓單錨失明且完全無訊號**（新缺陷類別）。tools/tests/test_windows_forbidden_filename_parity.py::test_registered_sites_match_repo_scan_exactly 取錨①（保留名清單 CON→PRN→AUX→NUL 依序、間隙 ≤5 字元）∪ 錨②（禁用字元集合 `<>:"\|?*`）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（潛在鎖弱化：功能不受影響、當下無實害，但既有前瞻掃描的鑑別力在無任何訊號的情況下對三份實作歸零） | 四處生產實作（交替清單順序 + 🔴 註解）+ tools/tests/test_windows_forbidden_filename_parity.py（新機械鎖） | fixed@R60（Pkg-3）：① 四處一律把新裝置名移到交替清單**尾端**（`^(...)$` 與 case 皆完全錨定，交替順序不改變匹配集合，功能零變更已實測：bash 側 21 樣本 BLOCK/pass 逐筆與 reorder 前相同、`bash -n`／`sh -n` 皆 rc=0），四處各加 🔴 註解說明順序有意義、勿「整理」。② 新增機械鎖 test_each_authoritative_impl_keeps_base_device_names_adjacent，從註冊表以「角色＝實作」推導 4 份（不另建第 5 份硬編清單，並斷言恰 4 份以防前提漂移）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-546 | 2026-07-28 | R60 Scan-E（E-A-02）落地包 Pkg-6；反駁者裁決 PARTIAL、評級由 P2 降 P3，且**明確反對掃描者原始提議**（「無條件執行等於部分復活 R10 刻意退場的黑名單枚舉、對正當維護產生常態噪音」），本輪落地為訂正版而非原始理由 | `tools/check_wrapper_thinness.py`（修復前 `:249-254`）的 `for keyword in _FORBIDDEN.get(rel, ())` 整段**縮排在 `if actual != pinned:` 內**——「hash 釘選」與「業務邏輯關鍵字」兩道防線是**串聯而非並聯**：一旦有人以檔案自己 docstring 指示的 `--print-hash` 工作流更新 pin（正常維護動作），關鍵字偵測**整組同時失效**。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（既有設計取捨留下的殘餘窗口、今日無活體；對照 DEF-101-519「前瞻性防護缺口非現存缺陷」判 P3。掃描者原報 P2 為浮報） | 本輪修復（Pkg-6） | fixed@R60：`for keyword …` 整段自 `if actual != pinned:` **反縮排為並聯**，並把比對對象由原始 `text` 改為 `norm`（正規化內容，與 hash 同一份文字）。此二改動同時回應反駁者的兩點：(a) 檔案 docstring 早已記載「只在 hash 已紅時」⇒ 本輪 docstring 改寫為「**訂正串聯設計缺陷**」而非「補上未記載行為」；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-542 | 2026-07-28 | R60 Pkg-4 Scan-C 修復包自找（動 -Status 區塊時發現既有修復在 PowerShell 語意下不成立） | **`install_windows_nightly.ps1 -Status` 的結束代碼恆為 0，DEF-101-248 的修復被 PowerShell 輸出串語意打敗，而它的回歸鎖只比對原始碼字面、從不執行，故連續多輪零訊號**。根因：`Show-NightlyStatus` 一支函式同時 `Write-Output` 報表又 `return $true/$false`，PowerShell 把函式內**所有**輸出併入回傳值 ⇒ `$loaded = Show-NightlyStatus` 實得 `Object[]`（報表字串＋布林），`if ($loaded)` 對非空陣列恆為真。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（既有修復實際未生效且被記為 fixed〔Rule 12〕；本輪新增的 Windows smoke 任務缺席偵測也依賴這條 exit code，若不修則「補償控制沒註冊」永遠看不出來） | 本輪修復（Pkg-4 Scan-C）：拆分純函式／印出函式 ＋ 把字面比對鎖升級為真的執行 | fixed@R60：① 拆成「純查詢零輸出」的 `Test-TaskPresent`（`return [bool]$t`）與「只印報表、刻意不回傳值」的 `Show-TaskDetail`／`Show-NightlyStatus`，決策改為 `$loaded = (Test-TaskPresent -Name $TaskName) -and (Test-TaskPresent -Name $SmokeTaskName)`。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-537 | 2026-07-28 | R60 Pkg-3 Scan-B B-01（掃描者原判 P2、反駁者訂正 P3 並指出三處理由要修） | WindowsApps 空殼 guard 家族的 repo-wide「零 guard 前瞻掃描」只做 bash 與 .ps1 兩側，.py 側缺一條軸。兩側各有兩條（.sh：test_repo_wide_scan_finds_no_unmigrated_sh_scripts + test_repo_wide_scan_finds_no_zero_guard_python_calls；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（前瞻性防護缺口、repo 現況 live 違規＝0；對齊 DEF-101-483／DEF-101-478 判例。掃描者原評 P2 浮報：同家族 DEF-101-364 給 P2 是因當場掰出 9 筆 live 違規） | tools/tests/test_windowsapps_guard_cross_consistency.py（.py 側，與既有 .sh／.ps1 兩側同政策：只排凍結版與測試檔、無前綴縮面） | fixed@R60（Pkg-3）：新增 .py 側第二條軸——`_BARE_PY_COMMAND_RE`（字串常數首個空白分隔 token 恰為裸 python/python3）+ `_docstring_constant_ids()`（AST 天然排除註解、另主動排除 docstring）+ `_bare_python_command_literals()`（parse 失敗 fail-loud）+ `_has_zero_guard_bare_python()` + 角色註記註冊表 `_ZERO_GUARD_BARE_PY_SITES`（8 筆：2 筆真兜底 `sys.executable or "python3"`／6 筆非呼叫…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-535 | 2026-07-28 | R60 Pkg-3 Scan-B（反駁者自找 #1）；修復者先於系統 temp 拋棄式 repo 獨立取證後落地 | 四份 NTFS 淨化/驗證實作全數放行 Windows 主控台裝置名 `CONIN$`／`CONOUT$`：tools/check_ntfs_paths.py:_RESERVED_RE、tools/git-hooks/pre-commit:_ntfs_seg_bad 的 case pattern、AutoClaude/autoclaude/utils/logger.py:_WIN_RESERVED_NAME_RE、AISDLC_SDD/scripts/component_sanitizer.py:_WIN_RESERVED_NAME_RE。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（前瞻性防護缺口：repo 現況 27441 個 tracked 路徑內零此類檔名，四道閘門對此形態一律放行；對齊同家族判例 DEF-101-478。若未來有 mac 真機驗到 mac 側 git 直接接受入庫，可考慮升 P2） | 四處生產實作 + 兩份樣本電池（根層 tools/tests 與 AISDLC_SDD/scripts/tests） | fixed@R60（Pkg-3）：四處正則/case 加入 `CONIN$`／`CONOUT$`（正則 `$` 轉義；bash 以單引號界定 `'CONIN$'` 使 `$` 成字面值，POSIX 標準、bash 3.2 相容，不依賴「$ 後接非展開字元視為字面」邊角行為）；刻意不納入 `CLOCK$` 並於 benign 樣本常駐 `CLOCK$ .txt`、NON_RESERVED_NAMES 常駐 `CLOCK$`/`CONIN` 釘住此決策。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-553 | 2026-07-28 | R60 Scan-F F-01（PARTIAL→P3；Pkg-7 落地） | **`tools/macos_smoke_local.sh` 在非 Darwin 平台把兩項無法驗證的子測試以「SKIP-計-PASS」計入 PASS，收尾三個訊號（`===== 彙總：PASS=13 FAIL=0 =====`／`全部通過 ✅`／rc=0）與真 macOS 滿版全驗逐字相同、計數相同**（兩平台皆恰 13——`test_smoke_ci_sync.py::_SH_EXCLUSIVE_PASS_GROUPS` 已把那兩組互斥分支逐字登記），事後稽核無從分辨「在 Windows 上跑過」與「在 macOS 上全綠」。R59 由 DEF-101-511/512 立的原則（讓結論自己說出降級事實）該輪未回頭套用到這支腳本。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（取證可信度；不影響 FAIL 判定） | 本輪修復（Pkg-7） | fixed@R60：① 新增獨立計數器 `SKIPPED_AS_PASS`，兩處 SKIP-計-PASS 站點各遞增一次；**刻意不併入 `pass()`／不新增 helper**——`test_smoke_ci_sync.py` 的 MIN_PASS 語意鎖以「pass 加雙引號訊息」字面呼叫點計數，包一層會多算一次（PASS 計數與 `MIN_PASS=13` 語意完全不動，只動輸出面）。② 兩處彙總行改印 `SKIP=$SKIPPED_AS_PASS`。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-528 | 2026-07-28 | R60 主控 Pkg-1 自我 zero-trust 複驗 ＋ Scan-D／Scan-G 反駁者交叉指認 | **主控動工前那支臨時歸檔腳本用 `Path.write_text()` 寫檔，`newline` 預設 `None`，Windows 上把 LF 譯成 `os.linesep`（CRLF）**，帳本主檔與 `archive_30` 由 `w/lf` 變 `w/crlf`（與 `.gitattributes:34 `*.md text eol=lf`` 相反，`git diff` 直接印警告），**並讓該腳本「位元組總量守恆」自我斷言按字面重驗為假**——多出的位元組正是每行一個 CR（主檔 163 個、archive_30 61 個），也是三個互異數字（174,447 LF／174,609 CRLF／176,459 含索引）的來源…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（版控檔行尾被靜默改壞 + 自我斷言失效 + 跨檔狀態矛盾使閘門誤判） | 本輪修復（主控 Pkg-1） | fixed@R60：① 主檔與 `archive_30` 以 bytes 層正規化回 LF、落地後 assert 零 CR，`git ls-files --eol` 複驗皆回 `w/lf`；`archive_05`／`21` 一併正規化，**`git diff` 為空證明語意零變更**（index 本就是 LF）。② 新工具一律 bytes 層寫入並 assert 零 CR，`--check` 稽核整個帳本家族行尾。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-539 | 2026-07-28 | R60 Scan-C C-01（掃描者提出、反駁者獨立重現並訂正 title 失實）＋本包程式化全掃複核 | **「兩平台排程能力對等契約鎖」寫成 pytest 模組層函式風格，而四道執行 tools/tests 的閘門全部走 unittest discover ⇒ 這道鎖從落地起從未跑過一次**。tools/tests/test_schedule_capability_parity.py（R22 為 DEF-101-233/259 落地）以 `def test_*` 寫在模組層；`unittest.TestLoader().discover()` 只收 TestCase 子類。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（鎖存在但從不執行＝形同不存在；同型判例 DEF-101-509「Windows 專屬腳本語法閘門在 Windows 上 skip」於 R59 評 P1，本筆守的面較窄故維持 P2） | 本輪修復（Pkg-4 Scan-C）：載具形態改寫 ＋ 補治本層 repo-wide 前瞻鎖 | fixed@R60：① `test_schedule_capability_parity.py` 6 支改寫為 `unittest.TestCase` 方法（斷言主體與訊息逐字保留 R22 原文，只換載具——形態才是缺陷），並補上該檔在 43 檔中唯一缺席的「執行：python3 -m unittest discover」docstring 行（坐實是漏接線而非刻意選 pytest）；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-545 | 2026-07-28 | R60 Scan-E 架構最佳化（E-A-01）落地包 Pkg-6；反駁者裁決為 PARTIAL、評級由 P2 降 P3（同形狀先例 DEF-101-173／182 兩次皆 P3 且**當時都有活體漏管**，本項活體漏管為 0） | `tools/check_script_parity.py:336` 的 enrollment 掃描名冊 `_PAIR_SCAN_DIRS = ("tools","tools/lib","AutoClaude/tools","AISDLC_SDD/scripts")` 為本檔自持、**非遞迴** glob（`:485-493` `d.glob("*.sh")/("*.ps1")`；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（前瞻性防護缺口；同形狀判例 DEF-101-173／DEF-101-182 皆 P3 且當時各有活體漏管，本項活體 0，比先例更輕，故不取 P2） | 本輪修復（Pkg-6） | fixed@R60：① 新建 SSOT `tools/_script_scan_surface.py`（`SCRIPT_SCAN_ROOTS` 三棵樹＋`iter_tree_scripts()` 為**唯一**遞迴列舉實作，遞迴性只寫在一處）；② `check_script_parity._PAIR_SCAN_DIRS` 改指 SSOT（`tools/lib` 由遞迴涵蓋、不再單獨列名），`_discover_scripts()` 改走 SSOT，成對 key 改「repo 相對路徑去副檔名」（與舊「同目錄內 stem」語意等價，既有納管登記逐字不變）；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-544 | 2026-07-28 | R60 Scan-D D-02（掃描者提報、反駁者四次嘗試推翻皆失敗且把證據強化一級判 CONFIRMED；Pkg-5 落地修復並根治） | AutoClaude/README.md:355 仍只給 bash 形態的 `PYTHONUTF8=1 lint-imports`，是 DEF-101-513 家族第 4 個活文件站點、第三次復發。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（同家族 DEF-101-513 實測 severity 欄；不因站點數多而上調，比照 DEF-101-507 的評級理由） | root＋autoclaude 文件（AutoClaude/README.md 為 AutoClaude 側文件，但機械鎖落在根層 tools/tests 以覆蓋跨子專案活文件名冊；零碰 AutoClaude 生產碼） | fixed@R60（Pkg-5）：① AutoClaude/README.md §測試補一塊 powershell 圍欄（$env:PYTHONUTF8=1; lint-imports）＋🔴 WHY 段（PS 無此語法、照抄的實際錯誤訊息、R57/R59/R60 三次復發史、指向新機械鎖），純新增 15 行零刪除；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-551 | 2026-07-28 | R60 Scan-F 反駁者自找 #1（Pkg-7 落地） | **`AutoClaude/tools/local_ci_gate.ps1:20` 寫死過期的 `$PytestArgs` 預設值 `'tests/ -q --tb=short'`，把 R59（ARCH-R59-01）加進核心 `local_ci_gate.py:63 DEFAULT_PYTEST_ARGS` 的 `-rs` 在整個 Windows 側靜默吃掉**（含 `run_local_nightly.ps1:452` Stage L、根 CLAUDE.md 列為 Windows 唯一本機 CI 指令的那條路）。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（兩平台閘門報告面不等價 + 被機械鎖宣稱等價；覆蓋可見度損失，不會讓紅變綠） | 本輪修復（Pkg-7） | fixed@R60：① `.ps1:20` 預設值 → `''`（無參數時零附加，核心 `DEFAULT_PYTEST_ARGS` 為唯一真相源），檔頭與參數映射處補 WHY。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
