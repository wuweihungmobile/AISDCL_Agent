# AutoSDD 缺陷帳本（Defect Log）— 跨輪累積、只增不刪

> **用途**：AutoSDD 雙軌迭代（A 軌整合 / B 軌 Dogfooding）行進中發現的框架缺點/Bug/摩擦，
> 依「發現即記、絕不累積」紀律入帳，並依分流規則回流改進（見
> `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 的「🐶 自我迭代模式」）。
> **建立**：2026-06-12（首輪 N=01；首輪或帳本不存在時於階段一建立含表頭的帳本）。
>
> **格式定義**（每筆一列）：
> `ID`｜`發現日期`｜`發現情境`（FSM 狀態 / SCG 閘門 / hook / skill / nightly stage / 審計）｜
> `現象與證據`（file:line 或命令輸出）｜`嚴重度`｜`分流去向`｜`狀態`
> - **ID**：`DEF-{{N}}-{seq}`，N = 迭代輪編號（01/02…），seq 為該輪流水號。
> - **嚴重度**：依 v0.01 官方
>   `docs_template/sdd/testing/DEFECT-CLASSIFICATION-SPEC-TEMPLATE.md` 之 Priority 分級：
>   **P0**（Urgent — 必須立即修復，阻擋當前測試/發布）/ **P1**（High — 當前 Sprint 修復）/
>   **P2**（Normal — 下個 Sprint 修復）/ **P3**（Low — Backlog，擇機修復）。
> - **狀態**：`open`（未分流）/ `routed`（已分流待修）/ `fixed@<版本或載體>`（已修，附證據）/
>   `wontfix+理由`。
> - **分流去向**（對應官方回流機制）：規格/文檔缺陷 → Phase J SPEC-PATCH（併入 v0.0(X+1)）；
>   治理規則缺陷 → FPL→SLV→LEARNING_COMMIT；框架程式/模板/hook 缺陷 → RFC + v0.0(X+1)；
>   整合層（AutoClaude 側）缺陷 → 下輪 A 軌 W 項。

## 缺陷總表

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-01-007 | 2026-06-12 | W8 整合驗收 | `cc-switch` 未安裝（`Get-Command cc-switch` 不存在），計畫 §5.2 多模型後端 A/B 驗收無法執行；`integration_gate.ps1` [5/5] 段落以 SKIP + 指引處理（非偽綠：SKIP 明示於輸出） | P3 | 環境工具補裝後依 §5.2 手動執行 A/B（指標：一次通過率 / CORRECTION 次數 / SDD_CONTRACT_VIOLATION 次數 / token 峰值）；俟環境補裝 | open（2026-06-14 improving_06 結案盤點複驗：`which cc-switch`=NOT FOUND 仍重現；屬環境工具缺裝非純程式可修；`integration_gate.ps1 [5/5]` SKIP 非偽綠；**不阻擋 A 軌主鏈結案**——功能路徑齊備，僅缺多後端對比驗收；2026-06-14 improving_08 階段一複驗 `which cc-switch`／`command -v cc-switch` 皆 NOT FOUND 仍重現，屬環境工具缺裝非純程式可修，維持 open 俟補裝；2026-06-14 improving_09 階段一複驗 `command -v cc-switch`=NOT FOUND 仍重現，維持 open〔🔴 人工確認本輪選「輕量結案盤點」，未補裝〕；**2026-06-14 improving_10：🔴 人工選「補裝 cc-switch 關閉」**。使用者實裝 cc-switch，但裝的是主流 GUI 版（farion1231）→ `command -v cc-switch`=**NOT FOUND**（GUI app 不上 PATH，實證 **DEF-10-001(b)**）。本輪移除**所有倉內阻塞**（DEF-10-001 fixed：建 A/B 載具 + 硬化 gate），DEF-01-007 由此**縮窄**為純「環境/API 動作」：僅待使用者裝 cc-switch **CLI 變體**（SaladDay/cc-switch-cli）+ 配 2 個 model profile（含 API key）+ 授權 token 花費後，於 AutoClaude/ 一鍵跑 `cc-switch use <profile> && autoclaude scripts/sdd_bridge_smoke.yaml --fresh` 收 4 指標。維持 **open**（非純程式可修，倉內已零阻塞）） |
| DEF-01-009 | 2026-06-12 | 三專家審查（Architect） | sdd_governance_plugin.py raw 250 行恰貼 plugin_entry ≤250 上限（工具計非空行 217 過關）——後續任何擴充必須先拆 <feature>_plugin/ package | P3 | 下輪擴充前置作業（watch item） | open watch（2026-06-14 improving_06 結案盤點複驗：raw 仍 250、受控指標非空行 **224 < 250**、`check_loc_budget` violations=0＝已自癒；續 watch：對該 plugin 任何擴充前必先拆 package；本輪零擴充不觸發；2026-06-14 improving_08 階段一複驗 raw 仍 250、violations=0 已自癒持平，零擴充不觸發，維持 open watch；2026-06-14 improving_09 階段一複驗 raw 仍 250、`check_loc_budget` violations=0 已自癒持平，本輪零擴充不觸發，維持 open watch；**2026-06-15 improving_14：watch 首次真正觸發並處置**——W-14-2 拓樸橋接擴充 `sdd_governance_plugin.py`，count_loc 自 224→恰貼 250 上限（零餘量）。依本 watch「擴充前先拆」紀律，將拓樸載入邏輯抽出 `plugins/_sdd_topology_signoff.py`（27 行 helper，非 importlinter Rule 1 independence 清單成員 → plugin→helper 合法），plugin 降回 **243 < 250（餘 7）**、`check_loc_budget` violations=0、`lint-imports` 8 kept。watch 紀律有效運作，維持 open watch 續守後續擴充）。**improving_22 複驗：本輪零擴充 sdd_governance_plugin（W 項為 AISLDC_SDD 框架側），watch 不觸發，維持 open watch**；**improving_98 複驗（2026-06-30）：raw 277、`check_loc_budget` violations=0（count_loc 受控指標 < cap 250、自癒持平），本輪 W 為框架側 Copy-on-Evolve（零擴充該 plugin），watch 不觸發；正確處置＝維持 open watch、不投機拆 package（守工程紀律 Rule 2/3）** |
| DEF-13-002 | 2026-06-15 | improving_13 前一輪 zero-trust 稽核定案（split-brain 修復決策：觀察期斷檔處置） | monorepo 觀察期 #2/#3 因 DEF-13-001 split-brain 於 06-13~06-15 斷檔。**明確決策＝不 backfill**：舊副本在 branch `sprint/sd_09_phase9`，程式碼已與 monorepo main 分岔；把舊碼採集的 06-13~06-15 觀察 jsonl 注入 monorepo＝以「舊碼數據」充當「monorepo 現碼穩定證據」，違反 Nightly 取證紀律 #1（rc/數據須反映現碼真實執行）與 #3（PASS 須引現碼 RunId）。故**不複製／不覆寫／不注入任何 jsonl**；舊副本資料保留原處 | P1 | 修復決策（不 backfill）；排程修正後自今晚起從現碼重新連續採集，達標日順延 | **resolved@2026-06-15（PM 拍板）**：① backfill 決策＝**確認不 backfill、從現碼重新採集**（守取證紀律 #1/#3）；② 孤立舊副本 `d:\CursorProject\AutoClaude` 已**歸檔重新命名**為 `d:\CursorProject\AutoClaude_ARCHIVED_pre_monorepo_20260615`（杜絕未來再 split-brain；修前掃描確認無其他 schtasks 引用舊路徑；歸檔後複驗排程仍指 monorepo 不受影響）。觀察期 #2/#3 達標日順延、待現碼重新連續累積（追蹤點，非 open 缺陷） |
| DEF-17-001 | 2026-06-16 | improving_17 W-17-1 B 軌 dogfooding（接入鷹架代謝 GC、驗 flag ON 端到端時揭露） | 代謝閉環「半接」結構摩擦：W-17-1 把 `scaffold_gc.run_gc()` 接入 `enter_scaffold_gc` 主迴圈後，flag ON 確實自動產 SCAFFOLD-ROI 報告，**但 `compute_proposals()` 依 `rule_loader.propose_graduation()` 需 `fire_count ≥ GRADUATION_MIN_FIRES ∧ catch_count==0` 才提議**（`scaffold_gc.py:72-73`/`rule_loader.py:150-159`）。FF-9 實測 39 條 active 規則 aggregate `fire_count=0`（`record_fire` 在 FSM 主迴圈零自動呼叫——僅測試觸發），故**即使 flag ON，生產端 GC 恆產零提議**＝GAP-X2「代謝肌肉從未收縮」**未真正閉合**：接 `run_gc` 是必要但不充分，尚需把 `record_fire(rule_id, caught=…)` 遙測接入「規則實際 fire/catch」的 FSM 執行點，ROI 帳本才有非零資料驅動退役提議。非阻擋（W-17-1 機制正確、報告真實落盤、紅線守界皆達成；零資料零提議是誠實的下游依賴而非 bug） | P3（成熟度閉環一致性/遙測接線缺口，非阻擋；與 B 軌「機制齊備但遙測未接」家族同根） | routed 未來輪 B 軌：把 `record_fire` 規則命中記帳接入 FSM 規則執行/守門點（`rule_loader.load_for_state` 命中後記帳），使 scaffold_roi 累積真實 runtime 資料；屬獨立 W 項（遙測接線），非本輪 scope（本輪聚焦「GC 提議產出鏈」接入＝代謝收縮的決策側） | **routed（fire 側 fixed@improving_18 / catch 側 routed → DEF-18-001）**（即記即分流；本輪 W-17-1 收縮「決策側」已閉合並測試覆蓋。**2026-06-16 improving_18 W-18-1/W-18-2 閉合「遙測 fire 側」**：`record_state_fires` 接入 `transition()` 主迴圈，flag ON 時 `fire_count` 真實累積〔`test_rule_fire_telemetry_wiring.py::test_flag_on_fire_count_accumulates_persisted` 驗 3 次轉態累積至 3〕，GC 已有非零資料可驅動提議＝DEF-17-001 點名的「`fire_count=0` 根因」已閉合。**殘留 catch 側**〔`record_fire(caught=True)` 何時觸發之語意框架未定義〕轉記為獨立 **DEF-18-001**。**2026-06-25 improving_62（v0.24）：fire 側遙測翻預設 ON 活體化**——`_rule_fire_telemetry_enabled()` unset→True（鏡像 AUTO_RECOVERY/SLV 翻環），fire_count 由 opt-in 升為生產常態累積，DEF-17-001 點名根因「生產端 fire_count 恆 0」**實質閉合**（掌舵者 signoff「B 軸 telemetry 翻 ON」；保留顯式 opt-out=0；conftest autouse 隔離護凍結 governance）） |
| DEF-19-001 | 2026-06-16 | improving_19 W-19 B 軌 dogfooding（閉合 catch 側契約時，揭露覆蓋面缺口） | catch 歸因覆蓋面不足（DEF-18-001 閉合後之殘留面）：W-19 定義 catch 三要件契約並接入兩確定可歸因路徑（R-9.1 gate retry 耗盡 / R-9.21 monitor 破壞），但目前僅 **2/39** active 規則補了 `failure_mode` 並接線；其餘 37 規則 `failure_mode` 未定義 → 其守望的失敗模式即使觸發 escalation 也不參與 catch 歸因（`catch_count` 恆 0、fail-closed 不記）。後果：這 37 規則的 ROI 仍為單側信號，`propose_graduation` 對它們仍可能偏頗提議退役。本輪**刻意只接 2 條無歧義路徑**（Rule 2 簡單優先、寧缺勿濫不污染 ROI）；覆蓋率以 `rule_fire_telemetry_stats().safety_certificate.catch_attribution_coverage` 程式內誠實揭露。非阻擋（catch 機制正確、紅線守界、flag 預設 OFF＝生產零影響） | P3（成熟度閉環覆蓋面缺口，非阻擋；與 B 軌「機制齊備但覆蓋漸進」家族同根） | routed 未來輪 B 軌：逐步為其餘規則補 `failure_mode` 自描述 + 對應 escalation 呼叫點歸因（每條須有「無歧義映射」證據，不可為衝覆蓋率而臆測歸因——延續 DEF-18-001 紀律）；**生產全面啟用退役前須提升覆蓋率至可接受門檻**。屬漸進補強 W 項，非單輪可竟 | routed（漸進補強進行中：**improving_20 W-20-1 將 catch 歸因從 2/39 推進至 4/39**——新增 R-9.2〔auto_compact per-stage 超限→ESCALATION〕、R-9.22〔spec_patch per-AC 上限耗盡→ESCALATION〕兩條無歧義路徑，各補 `failure_mode` + 接對應 `record_escalation` 呼叫點顯式歸因〔fixed@v0.11，6 case 測試 test_w20_catch_wiring.py〕；`catch_attribution_coverage` 程式內誠實揭露。剩餘 35 規則續漸進補強，生產全面啟用退役前須提升覆蓋率至可接受門檻）。**improving_22 階段一複驗仍 4/39**；🔴 人工本輪 scope 選「DEF-12-002 + DEF-15-001 深層」未含 catch 補強，維持 routed 漸進，未推進。**improving_37 W-37-1 推進 4/39 → 5/39**（新增 R-9.7·9.7.2 HUMAN_PENDING 逾時 ≥168h ESCALATION 無歧義路徑，fixed@v0.15；明文僅 9.7.2 排除 9.7.3〔歸 R-9.2〕防雙重歸因，非重疊守門測試鎖定；R-9.9 親驗無唯一生產落點降級不接）。**improving_38 W-38-1/W-38-2 推進 5/39 → 7/39（+2）**（新增 **R-SELF-STRIDE**〔SANDBOX_HARDENING_GATE policy_violation → ESCALATION，唯一生產落點、與既有 5 條零交集〕、**R-9.3**〔record_spec_audit 的 SPEC_AUDIT 耗盡 → ESCALATION；failure_mode 明文排除 implementation-budget-exceeded 直接 escalate〔正交無規則〕與 R-9.1 gate-retry 落點，防雙重歸因〕兩條無歧義路徑，fixed@v0.16；`test_w38_catch_wiring.py` 8 case 含兩條非重疊守門〔R-SELF-STRIDE verdict=pass 不歸因 / R-9.3 implementation-budget 落點不歸因〕；runtime `catch_attribution_coverage` 實測 7/39）。**improving_39 揭露結構天花板**：grep `record_escalation(` v0.16 fsm_runtime.py＝9 生產 escalation 落點＝7 已接線 + 2 正交無規則（515 implementation-budget、2401 spec_patch-unable-to-draft），「1:1 無歧義落點接線」乾淨候選**已枯竭**；W-39-1 機械分類 39 條＝(A)FSM escalation 7 / (B)hook 3 / (C)lint·TLC 3 / (D)meta-loop guard 14 / (E)manual·advisory·憲法 12，證實 escalation-scoped 真實覆蓋＝**7/7=100%**（DEF-39-001 透明化）。**→ closed@improving_40（milestone，🔴 掌舵者 AskUserQuestion 拍板正式收尾）**：FSM-escalation catch 機制覆蓋已達結構天花板（7/7=100%）；其餘 32 條由 hook/lint/TLC/meta-loop/人工守門＝本質非 FSM-escalation catch-可歸因（`catch_count` 恆 0 為設計使然非缺口）。「其他守門機制覆蓋度量」另立新標的（B 軌未來輪，非本缺陷 scope） | 
| DEF-25-001 | 2026-06-17 | improving_25（B 軌 meta⁸ 視覺化飽和認定） | FSM-STATE 視覺化反射觸 R-9.37.4 邊界（視覺化模組不得篡改 meta⁸ 既有合約、反射狀態為唯讀邊界） | P2 | wontfix（by-design 邊界，非缺陷） | **wontfix**（R-9.37.4 設計邊界；歷史脈絡見 `AutoSDD_Defect_Log_archive_01.md` improving_25 段） |
| DEF-42-001 | 2026-06-21 | improving_42 階段一 Re-Audit（ci-gate v0.17 pytest） | 框架測試 flaky（Windows 並行檔案鎖）：`v0.17/tools/fsm_runtime/tests/test_file_lock.py::test_parallel_writes_do_not_lose_increments` full-suite 並行下間歇 PermissionError，ci-gate v0.17 偶報 1 failed；主 agent 隔離 3/3 全綠 → 確認**非回歸** | P3 | routed（flaky 隔離追蹤、非回歸；框架本體 RFC） | **routed**（archive_01 improving_42 段；非回歸 flaky） |
| DEF-42-003 | 2026-06-21 | improving_42 W-42-1 設計期（負向狀態碼跨路徑搶救被既有哨兵測試攔下而揭露） | 引號與負向狀態碼共存時負向被丟棄＝under-specify（引號路徑早 return、負向不評估）；屬 W-32-1 刻意設計決策 + 哨兵 `test_quoted_wins_over_negative_status` 鎖定 | P3 | wontfix（by-design，surface 供掌舵者未來輪決策） | **wontfix+by-design**（archive_01 improving_42 段；哨兵鎖定、非缺陷） |
| DEF-52-006 | 2026-06-23 | C 軌 improving_52（SA 鏡 FIND-4） | stderr 中文 UnicodeEncodeError（理論）：`_init_utf8_streams()` 僅 `__main__` 呼叫；ascii-strict console 直接 import 呼叫會拋。生產 wiring 經實證皆 subprocess→`__main__` 必走→風險為零 | P3 | wontfix（生產路徑必為 subprocess） | **wontfix+理由**（archive_01 improving_52 段；生產必走 subprocess、風險零） |
| DEF-53-001 | 2026-06-23 | C 軌 `.claude` 審查輪八（SA 鏡 P3-2） | `hub_merge.py` `_read_yaml`/remote→local copy 未 cap 讀 pull-cache yaml；`detect_conflict`/`resolve_or_record` 經查僅測試呼叫、未在任何 runtime 路徑 wire → 無 runtime 消費者、無活路徑可觸發（latent） | P3 | RFC（框架本體 `tools/fsm_runtime/`，`build/planning/active/SDD_improving_Automation_NN.md`） | **routed**（latent；archive_01 審查輪八段；**註**：與 DEF-CLDREV-030〔hub_sync yaml 大小上限 fixed@v0.20〕同家族，本項 hub_merge 路徑仍 latent） |
| DEF-100-002 | 2026-06-30 | improving_100 階段一 mutation backlog 分析（查 `.mutmut-cache` sqlite） | `autoclaude/plugins/token_guard/thresholds.py` `should_compact_decision` L49 `if in_correction_loop and correction_history_len <= 1: return token_pct >= threshold` 為**死分支**——到達 L49 時 `token_pct >= threshold` 已恆成立（L48 已 return False 攔掉 `<`），故該分支 ≡ `return True` ≡ 下方 `return True`；因此 `.mutmut-cache` #122-124（`and`→`or`、`<=`→`<`、`1`→`2`）皆為**等價變異**，補測試殺不掉、非真測試缺口（受控突變 EQUIV-CHECK 證實 GREEN） | P3 | routed 下輪評估重構移除冗餘分支（改源碼需驗證對齊 `PlaybookRunner._should_compact_now` 契約，本輪守 Rule 2/3 不動源碼） | routed（improving_100 已加等價性實證測試 RTM-100-5〔`TestShouldCompactL49DeadBranchEquivalence`，parametrize 證 token_pct≥threshold 時恆 True 不受 correction 參數影響〕pin 住死分支語意並保護未來重構；重構移除待下輪） |
| DEF-101-003 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 B zero-trust 盤點（凍結版 ps1 BOM 普查） | **凍結版 ps1 無 UTF-8 BOM 共 47 支（known-gap 豁免）**：`AISDLC_SDD_v0.01~v0.29/tools/fsm_runtime/formal/run_tlc.ps1`（29 支）+ `v0.12~v0.29/tools/install_hooks/install_post_commit.ps1`（18 支）含非 ASCII 且無 BOM，zh-TW Windows PowerShell 5.1 直跑會 parser 斷裂；v0.30 對應兩檔本輪已補 BOM，`scripts/ci-gate.ps1` 同輪補齊 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝改用 v0.30 對應檔；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-004 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D macOS 實測 | **凍結版 `verify_traceability.sh` 用 `declare -A`（bash 4+ 關聯陣列）→ macOS 內建 bash 3.2 必炸**（v0.01~v0.29 全數；v0.30 本輪已改 bash 3.2 相容） | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝用 v0.30 同檔，或 `brew install bash` 後以 bash 4+ 執行；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-005 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D 審視 `verify_traceability.sh` 相容化時發現既有缺陷 | **`verify_traceability.sh` 的 `set -euo pipefail` + `grep` 無匹配（rc=1）→ 腳本提前靜默退出**：任一 grep 掃描零命中即中止整支驗證、不報錯不輸出後續檢查結果（驗證鏡子靜默失真，違取證紀律 #4「驗證鏡子自身要被驗證」）；**所有 bash 版本皆然、v0.30 亦未修**（本輪 bash 3.2 相容化僅處理 `declare -A`，未動 pipefail/grep 行為） | P2 | 本輪修復（同 DEF-101-218，同一支腳本同一根因） | fixed@R16（詳見 DEF-101-218：`AISDLC_SDD/AISDLC_SDD_v0.30/tools/verify_traceability.sh` 9 處賦值敘述式補 `\|\| true`〔含本筆點名的 grep 零命中靜默 exit 1〕，「未找到」WARN 分支恢復可達；對照組／反例組／框架真實 `docs/` 三組皆驗證跑到底 exit 0；**R18 帳本一致性複核**：實測 `grep -n "\|\| true" verify_traceability.sh` 命中 9 處，確認與 DEF-101-218 記載一致，本列此前殘留 open + 軌道② RFC 分流敘述與同日 DEF-101-218 fixed@R16 自相矛盾，予以訂正） |
| DEF-101-018 | 2026-07-10 | 跨平台修復輪四方複審第三輪（自 DEF-101-006 帳目拆分） | **ruff 存量 baseline 待清理**：鎖版（DEF-101-006 fixed）已消除跨機器漂移，但 `ruff check .` 對全倉仍報既有 issue（E501/I001/UP045 等，遍布 HEAD 既有檔案）——根 CLAUDE.md 教學指令在新環境滿版報錯的體感問題仍在 | P3 | 擇期分批清理存量或調整教學指令範圍（沿 DEF-101-006 原分流） | open（帳目拆分自 DEF-101-006：漂移根因已除、本列只追存量清理；**R23 複審重新實測 baseline 為 1,147 筆**〔`.venv` ruff 0.15.21，見 DEF-101-262〕，原「1,339 筆」為舊測值，非機械斷言、純描述性存量，數字會隨新修復輪持續變動） |
| DEF-101-019 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點） | **凍結版 v0.01 subprocess 無 encoding 同病**：`AISDLC_SDD_v0.01/tools/fsm_runtime/sandbox_runner.py:252`、`tlc_runner.py:69` 與 DEF-101-015 同病（`text=True` 無 encoding），zh-TW Windows cp950 下可能 UnicodeDecodeError | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝ci-gate `.sh`/`.ps1` 已設 `PYTHONUTF8=1` 使凍結版子程序同獲 UTF-8；v0.30 對應處已 fixed@DEF-101-015） |
| DEF-101-020 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **v0.12~v0.29 中間凍結版 `test_closure_evidence.py` fixture 未清洗 env**：與 DEF-101-007 同源——中間凍結版測試的真 git fixture 未清 GIT_DIR/GIT_WORK_TREE；惟官方閘門路徑（ci-gate 雙軌＝v0.01＋LATEST）不執行中間版，hook 層 `env -u` 清洗亦已保護，僅剩「人工在中間版目錄帶敵意 env 手跑 pytest」邊角情境 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝閘門路徑不執行中間版＋hook 層 `env -u` 已清洗；v0.30 已 fixed@DEF-101-007） |
| DEF-101-021 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-010 殘餘） | **mutation artifact 累積鏈 90 天上限**：GitHub artifact retention 上限 90 天——連續 90 天無 token_guard 源碼變動觸發 `autoclaude-mutation-on-change.yml`，鏈上 `mutation-history` 過期 → 累積歸零重累；且 GitHub 側與 Windows 本機 nightly（`run_local_nightly.ps1`）為兩條互不同步的獨立累積點 | P3 | 平台上限如實揭露；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；緩解＝Windows 本機 nightly 為另一獨立累積點；限制已照實寫入 workflow 檔頭「已知限制」段） |
| DEF-101-022 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **`closure_evidence._run_git` 自身繼承呼叫端 env**：v0.30 `closure_evidence.py:89` `_run_git` 未自清 GIT_DIR/GIT_WORK_TREE——屬 read-only git 查詢（rev-parse 類），實害受限；現行防線為 hook 層 `env -u` 清洗 | P3 | 縱深防禦（函式內自清 env）留待後續輪；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；防線＝hook 層清洗已阻斷已知傳染路徑，函式內自清為縱深防禦補強項） |
| DEF-101-025 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-012 修復之語意註記） | **根層基建 bash -n leg 檢查「工作樹版本」而非 staged blob**：`tools/git-hooks/pre-commit` root-infra leg 以 `bash -n "$TOPLEVEL/$f"` 檢查工作樹檔——staged 壞＋工作樹好的罕見組合會本機假綠入庫 | P3 | 與既有 ruff-on-worktree 慣例一致故不改本機行為；known-gap 記入 `ONBOARDING.md` §9 | open（known-gap；緩解＝雲端 `root-infra-ci.yml` 對 push 後實際內容機械攔回） |
| DEF-101-040 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect 終裁 (e)，凍結版豁免家族；SA 終裁補充 cwd-wiring 面向） | **凍結版 v0.01~v0.29 settings.json 無 `PYTHONUTF8` env、hook command 仍為裸相對路徑**：直啟凍結版 session 時 ① hooks 的 cp950 stdin/stdout 風險不受 env 保護；② cwd 漂移時同樣有 DEF-101-028 的 exit-2 deny-lock 風險（僅 v0.30／根層已補 env＋shim） | P3 | 凍結版依紀律不回改；known-gap 記入 `ONBOARDING.md` §9（含兩面向） | wontfix+凍結版紀律（known-gap 已列；緩解＝根層 router 路由情境由根層/v0.30 覆蓋、直啟屬 dogfooding 邊角情境，操作時保持 cwd 於版本根） |
| DEF-101-051 | 2026-07-12 | 排程 nightly 紅燈追查（DEF-101-049 修後 pg-contract 真實狀態出土）；本輪深度根因 + 方向定案 | **三層 goal_task_id 為半成品功能（非單純約束矛盾）**：0010 migration 的 CHECK（`goal_task_id IS NOT NULL OR started_at < '2026-05-20'`）+ FK 欄 + validated + `test_alembic_0010` T8/T9/T10 契約測試皆齊，**但應用層從未接線**——全庫 `cp.goal_task_id` 零賦值、`playbook_runs.goal_task_id` 零寫入路徑、`PlaybookRun` ORM 連該欄都沒定義（run↔goal 現走另一張 `goal_progress` 表）。故 cutoff 後 **100%** checkpoint save 撞 CHECK；兩支 contract test 要求互斥（`test_alembic_0010`＝裸 run 必被拒 vs `test_pg_state_repository_contract` M4＝save_checkpoint 必成功建裸 run）。cutoff＝migration 撰寫日、PG production 未上線（ADR-SD09-001 gated） | P2 | **方向定案（使用者拍板）＝反向補完三層功能**（run 應有 goal_task_id 為正確設計意圖），**非放寬約束**（放寬＝放棄三層追蹤，已否決存證）；屬跨 plugin/DB/契約測試真功能 + 需 orphan-run 政策設計，SD_10 PG-track 落地。**使用者拍板 orphan-run 政策 (c)＝CHECK 加判別欄**（run_kind∈{standalone,three_tier}，CHECK 改 `run_kind<>'three_tier' OR goal_task_id IS NOT NULL`，無時間依賴→消除時間炸彈）。本輪落地並轉硬閘 | **fixed@nightly 追查輪·四方審查閉環**（證據：alembic **0017** run_kind 判別欄取代 0010 時間炸彈 CHECK〔fresh pg17 upgrade→head、downgrade 可逆〕；`PlaybookRun` ORM 補 goal_task_id+run_kind 欄；`_ensure_run_id` 依 cp.goal_task_id 標 three_tier；5 條 checkpoint 落地路徑〔_builder+_token_halt+_interrupt+_evolution+auto_resume〕接通 task.goal_task_id〔離線三層 playbook 端到端可達；runtime 動態分解 GoalDecomposer 未接為已知 gap〕；另修被 CheckViolation 遮蔽之 `schedule_resume` str→TIMESTAMPTZ 型別 bug；`_ensure_run_id` guard：非 UUID goal_task_id（fixture GT-xxx）退回 standalone+warn，不弄垮 checkpoint save。契約測試：`test_pg_state_repository_contract` 13 passed/1 skip〔含 three_tier 真實流程 seed projects→goal_tasks〕、`test_alembic_0010` T8/T10/T10b/T10c 更新至 0017 語意、builder threading 直測 3 case。移除 `autoclaude-ci.yml` pg-contract `continue-on-error`〔CI 模擬 13P/1S 綠〕。LOC=0、importlinter 8 kept、full suite 3632P/0F。四方獨立審查〔DBA/Dev/QA/Architect〕→全修〔P0 LOC 405>400 削行、P0 downgrade VALIDATE 回退陷阱改 NOT VALID、P1 5路徑接線、builder 直測〕→複審。canonical〔#4〕：`playbook_runs.goal_task_id`＝run 首 goal audit 標記〔UUID〕、`goal_progress` ledger＝per-goal 事件流〔TEXT，兩軌型別不同軌〕，見 NextAction。**衍生 follow-up：DEF-101-053／054**） |
| DEF-101-055 | 2026-07-12 | DEF-101-053 修復收緊 `match=` 時揭露 | **ORM `CheckConstraint(name=...)` 與 DB 實際 constraint 名分歧**：ORM `_pg_models.py` 宣告 `ck_playbook_runs_status`／`ck_kb_outcome`，但 DB（alembic 0001 inline CHECK）實際名為 `playbook_runs_status_check`／`knowledge_entries_outcome_check`（PG 匿名 inline CHECK 預設名）。因 production schema 由 `alembic upgrade` 建置（非 ORM `create_all`），這兩個 ORM CHECK name 對 DB **從未實現＝裝飾性**。影響：任何 migration 若 `DROP CONSTRAINT ck_playbook_runs_status` 會失敗；離線 DDL snapshot 測 ORM name 通過但不保證 DB 有該名。0017/0018 新 CHECK 用顯式 `ck_*` 名故 DB 一致，分歧僅限 0001 兩個 inline CHECK | P3 | 記事存證，不阻塞：CRUD 契約測試已改綁 **DB 實際名**（正確、真測 CHECK）；離線 DDL 測 ORM name（分層合理，二者測不同層）。未來若需對齊，另開 rename migration（屬 cosmetic，PG 未上線無急迫） | open（SD_10 PG-track 記事；本輪貢獻＝辨識 + CRUD 測試改綁真實名 + 探針取證真名） |
| DEF-101-056 | 2026-07-13 | Mac/Windows 相容性優化四方複審（SA 建議記帳，比照既有慣例） | **`install_post_commit.ps1` 用 `Out-File -Encoding ascii` 損毀非 ASCII 路徑，v0.01～v0.29 共 29 份歷史副本未修復**：bug 本體（heredoc 寫 hook 時非 ASCII 字元被靜默替換為 `?`）已在 LATEST（`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`）修復（改 `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)` + LF 正規化）；v0.01～v0.29 依框架自身治理規則「凍結基線不可在原地修改、框架改動一律走 Copy-on-Evolve」不予回改。實務風險分層：v0.12～v0.29（18 份）因缺 UTF-8 BOM，zh-TW Windows PowerShell 5.1 會在 parser 階段先行斷裂、根本跑不到 ascii 編碼那行；v0.01～v0.11（11 份，有 BOM）若被直接執行仍會完整重現原始 P1（LATEST 動態解析設計＝實務上唯一會被安裝流程呼叫的版本〔現為 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`；R9 訂正原文誤植之 `AutoClaude/tools/install_hooks/...` 路徑——該路徑全 repo 歷史從未存在，見 DEF-101-107〕，故此為理論殘留風險而非已知會被觸發的路徑） | P3 | 記事存證，不修復：凍結基線治理規則優先於個別 bug 回補；若未來有文件/流程引導使用者手動 cd 進舊版目錄執行 hook 安裝，屆時需重新評估 | wontfix+凍結版紀律（known-gap；與 DEF-101-019/020/040 同款情境，本輪 Architect/SA 複審統一標籤：v0.01～v0.29 原地不修，記事存證；LATEST v0.30 已 fixed，見同輪 commit；**R15 事實訂正（SCAN-A-2＋SCAN-D-1 兩獨立 agent od 實測交叉）**：本列「v0.01～v0.11（11 份，有 BOM）」敘述失真——該 11 份實為**無 BOM 且純 ASCII**（首 3 bytes `23 20 49`、bytes>127=0，自初始 commit 未變），不炸 PS 5.1 parser 的真因是純 ASCII 而非有 BOM；風險結論不變（被直接執行仍會完整重現原始 P1），惟未來 BOM 普查勿以「有 BOM」為判準） |
| DEF-101-057 | 2026-07-13 | Mac/Windows 相容性優化 Wave 2（文件層級收尾複查，比照 DEF-101-056 既有慣例） | **`install_post_commit.{sh,ps1}` 在 git worktree 下硬編 `.git/hooks/post-commit`（`.ps1`：`.git\hooks\post-commit`）路徑，v0.01～v0.29 共 29 份歷史副本未回補**：bug 本體＝worktree checkout 下 `<worktree>/.git` 是指向主 repo 的純文字檔而非目錄，腳本硬編 `HOOK_TARGET="$REPO_ROOT/.git/hooks/post-commit"`（`.sh`）／`Join-Path $RepoRoot ".git\hooks\post-commit"`（`.ps1`）對其寫入會直接炸「Could not find a part of the path」；任何 Windows/macOS/Linux 開發者在 worktree 中對舊版目錄執行 install_post_commit 皆會重現。已在 LATEST（`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.sh` / `.ps1`）修復（改用 `git rev-parse --path-format=absolute --git-common-dir` 動態解析回主 repo 真正的 `.git`，不受 `core.hooksPath` 影響，worktree／一般 repo 皆正確；見 commit 7162959）；v0.01～v0.29 依框架自身治理規則「凍結基線不可在原地修改、框架改動一律走 Copy-on-Evolve」不予回改，29 份歷史副本仍為硬編寫法原樣。實務風險分層：`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.{sh,ps1}` 為動態解析 LATEST 版設計＝正常安裝流程實務上唯一會被呼叫的版本，不受影響；僅當使用者手動 cd 進 v0.01～v0.29 任一舊版目錄、且該 checkout 恰為 git worktree（需同時滿足「舊版目錄」+「worktree」兩條件，均非官方安裝 SOP）時才會踩到，屬理論殘留風險而非已知會被觸發的路徑 | P3 | 記事存證，不修復：凍結基線治理規則優先於個別 bug 回補；若未來有文件/流程引導使用者手動 cd 進舊版目錄執行 hook 安裝，屆時需重新評估 | wontfix+凍結版紀律（known-gap；與 DEF-101-019/020/040 同款情境，本輪 Architect/SA 複審統一標籤：v0.01～v0.29 原地不修，記事存證；LATEST v0.30 已 fixed，見 commit 7162959） |
| DEF-101-058 | 2026-07-13 | Mac/Windows 相容性優化 Wave 1（`macos-compat-ci.yml` 新增 `macos-nightly-full` 在全新環境首次觸發） | **`AutoClaude/pyproject.toml:30` 的 `hypothesis>=6.0` 未鎖版本上限，全新環境 `pip install` 會解析到最新版並觸發連鎖測試失敗**：`macos-nightly-full` job 無既有 `.venv`、走 `pip install -e ".[dev]"` 全新依賴解析，裝到當時最新 `hypothesis==6.156.6`，其內部模組 `hypothesis.internal.conjecture.optimiser` 已改名/位移，導致約 19 個 property-based 測試連鎖失敗。**跨平台依賴版本漂移風險，非 macOS 特有**：`windows-compat-ci.yml` 亦有等價全新 `pip install -e ".[dev]"`／`".[dev,lint]"` 步驟（L124、L254），任何平台的全新安裝都會中招；目前多數開發者/CI 沿用既有 `.venv` 快取（`bootstrap.sh` 偵測到既有 `.venv` 即跳過重裝）掩蓋了此風險，故先前未被普遍注意到。**四方複審重要訂正（本輪，2026-07-14）**：SA/SD 兩方獨立以全新 venv 重新驗證，皆**無法重現**上述 19 個測試失敗——`hypothesis.internal.conjecture.optimiser` 模組現行仍存在且可正常 import，`hypothesis==6.156.6` 下全套測試（3,566 passed/196 skipped）與 hypothesis 相關測試皆綠燈；且 `macos-nightly-full` 這個「首次觸發」場景在本 repo 實際上**從未真正跑過一次**（僅綁 schedule/workflow_dispatch）。原始 19 個失敗的診斷缺乏可查證的 CI log 佐證，應視為未經證實的記事，不宜再被後續 audit 當既定事實引用 | P2 | 原建議「加 `<7` 上限」經 SA 獨立驗證證實**無效**——PyPI 從未發布過 hypothesis 7.x（現行最新即 6.156.6），`<7` 不改變任何解析結果。已改採本檔案 `dev` 區塊既有的 pytest/ruff 精確鎖版慣例（DEF-101-006），鎖定為經本機全新 venv 驗證綠燈的 `hypothesis==6.156.6` | **fixed@Mac/Windows 四方複審 2026-07-14**（`AutoClaude/pyproject.toml:30` 改精確鎖版 `hypothesis==6.156.6`；根因「19 個測試失敗」本輪無法重現，已如實訂正上一版記事，鎖版本身仍具備防止未來浮動安裝到未經驗證版本的預防價值） |
| DEF-101-060 | 2026-07-14 | Mac/Windows 相容性四方複審（QA 覆核 DEF-101-058 修復範圍時發現） | **`AutoClaude/pyproject.toml` 除 `hypothesis` 外，另有 ~18 條相依宣告同樣未鎖版本上限**：`pydantic>=2.0`、`pyyaml>=6.0`、`httpx>=0.27`、`wexpect>=4.0`、`keyboard>=0.13`、`pytest-mock>=3.14`、`cachetools>=5.3`（出現兩次）、`click>=8.0`、`import-linter>=2.0`、`plyer>=2.1`、`win10toast>=0.9`、`sqlalchemy>=2.0`、`asyncpg>=0.29`、`psycopg2-binary>=2.9`、`alembic>=1.13`、`tenacity>=8.2`、`pgvector>=0.3`、`claude-agent-sdk>=0.2.110`。DEF-101-058 只補了剛好在 CI 炸掉的那一個套件，屬頭痛醫頭；任一條在未來全新環境 `pip install` 時都有同樣的連鎖破壞風險 | P3 | 記事存證＋排入下一輪：候選處置為 (a) 對已知易破壞性升版的套件（如 pydantic/sqlalchemy/httpx）逐一升版驗證後精確鎖定；(b) 或新增機械檢查（比照 `tools/check_script_parity.py` 精神）強制「無上限宣告」需顯式列入白名單並附理由，而非預設放任。本輪範圍限 Mac/Windows CI paths／nightly 提醒／hypothesis 鎖版，全面依賴鎖版屬另一輪工作範圍，不逕行處理 | open（記事存證，待後續輪逐一評估鎖版；交叉參照 DEF-101-058） |
| DEF-101-068 | 2026-07-14 | R3 Mac/Windows 相容性複審（四方「架構最佳化建議」彙整，非本輪阻擋項，記事存證供下一輪參考） | **跨平台架構優化建議清單（本輪不逕行實作，範圍限於解決本輪 P0~P2 阻擋項）**：(a) Architect＝CI paths 白名單應由「條列法」改為「單一事實來源」（擴充 `test_ci_paths_cover_root_consumers.py` 涵蓋全部 4 個 CI workflow，而非只查自己）；(b) Architect/SD＝腳本雙軌維護（`.sh`+`.ps1` 各自完整實作）長期宜降為「薄 wrapper + 共用 Python 核心」（`tools/dev_start.py` 為既有示範模式），候選：`bootstrap`/`integration_gate`/`local_ci_gate`/`install_git_hooks`/`install-hooks`；(c) SD＝git hooks liveness 偵測邏輯在 `integration_gate.{sh,ps1}` 與 `local_ci_gate.{sh,ps1}` 重複 4 份、Git Bash/WSL 偵測樣板在至少 3 支 `.ps1` 重複，建議抽共用模組；(d) SD＝`check_script_parity.py` 目前只比對 step 標籤序列（docstring 自承不比語意），可選擇性加深至「雙邊實際執行 + 粗粒度輸出 diff」；(e) SA＝建議寫 `check_defect_log_crossref.py` 機械掃描文件內 `DEF-\d+-\d+` 引用，與帳本實際狀態比對揪跨文件同步落差（DEF-101-066 這類問題的治本解）；(f) QA＝「生產端接線」驗證應制度化為三段式檢核（觸發/執行/斷言），建議寫成可重用的 meta-lint 而非每輪人工複審碰運氣；(g) QA＝nightly-alert 類「等真實失敗才走到的分支」宜於設計時就內建可測試性（本輪已用 `simulate_nightly_failure` 落地此建議，其餘同類機制可比照）；(h) SA＝`gh issue create`（windows/macos-nightly-alert）缺 `--label`，與既有慣例（`aisdlc-sdd-fsm-chaos-nightly.yml` 有 `--label "p0,chaos,fsm-runtime"`）不一致 | P3（架構建議，非缺陷） | 記事存證，排入下一輪 B 軌/C 軌評估；(c)(g) 已落地 | open（記事存證；(g) 已於本輪 `simulate_nightly_failure` 落地；**(c) 已於 S11 落地**：liveness 偵測抽出 `tools/check_hooks_liveness.py`（四呼叫點 `local_ci_gate.{ps1,sh}`/`integration_gate.{ps1,sh}` 改一行呼叫）+ Git Bash 偵測抽出 `tools/lib/Find-GitBash.ps1`（三呼叫點 `ci-gate.ps1`/`install_git_hooks.ps1`/`install-hooks.ps1` dot-source 改一行呼叫），單元測試 `tools/tests/test_check_hooks_liveness.py`；其餘子項 **R14 補記（2026-07-20 Architect 檢視 ARCH-GAP-5，訂正帳本滯後實況）**：(a) **已落地**（`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py:10` S6 明文本子項、已擴至 4 份 CI workflow）；(e) **已落地**（`tools/check_defect_log_crossref.py`，`root-infra-ci.yml:44` 明文 DEF-101-068(e)）；(h) **已落地**（windows/macos-compat 兩 workflow `gh issue create` 已帶 `--label "p1,…,nightly"`）；(b) **部分落地**（local_ci_gate 已於 R12 收斂薄殼＋Python 單核心；bootstrap／integration_gate 仍雙原生實作，維持 defer）；(d) **維持 defer 並明文觸發判準**——下一個「同名 step 語意漂移」實證出現才投資「雙邊執行＋輸出 diff」重型基建；(f) **降級處置**——可機械子集（守門工具三處清單同步）已由 `tools/tests/test_root_infra_parity.py` 覆蓋，泛用 meta-lint 難寫準，殘餘改為迭代範本 checklist 條目、不硬寫泛用 lint。另註（守門侷限實證）：本列子項進度低於 `check_defect_log_crossref.py` 守門粒度（該工具只比對狀態字、無法偵測列內子項滯後），本次滯後即此侷限之實證；**R22 校正**：經 R22 四方一審核實，(b) 候選已於 R16（DEF-101-232）完整落地——`bootstrap_core.py`（348 行）／`integration_gate_core.py`（260 行）／`AutoClaude/tools/run_act_core.py` 皆已收斂為 Python 核心＋薄殼，本列 R14 快照所稱「仍雙原生實作，維持 defer」已過期，訂正為 fixed@R16（(b) 全部候選） |
| DEF-101-081 | 2026-07-15 | 獨立重新複審（不預設上輪 APPROVE 為真）：以 `gh run list`／`gh api .../annotations` 親查 CI 實際執行紀錄，而非讀帳本 | **本輪「四方複審終裁全 APPROVE」的最終 commit `1175555` 及其前一輪 `a8e4b73`，在真實 GitHub Actions 上從未成功執行過一次——CI 因帳戶帳單問題全面停擺，且此狀態完全未被記錄或揭露**：`gh run list --workflow=windows-compat-ci.yml` 顯示 `a8e4b73`（push run `29335402759`、workflow_dispatch run `29335409369`）與 `1175555`（push run `29378155616`）的 Windows smoke job 皆在 4~7 秒內 failure 收場；`gh api repos/.../check-runs/87235788504/annotations` 證實 annotation 為 `"The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings"`（runner 從未被配置，非測試失敗）。交叉核對 `gh run list --branch main --limit 15`：同一時窗（`a8e4b73` push 當下 `2026-07-14T13:10:41Z` 起）macos-compat-ci／root-infra-ci／AutoClaude CI／aisdlc-sdd-ci 五個 workflow 同步失敗、同款帳單 annotation；本輪複審當下重跑仍為同狀態（`root-infra-ci`／`AutoClaude CI` push run `29378473172`／`29378473200`，`2026-07-15T00:14:10Z`，對應 HEAD `bda34da`），故障持續中、非單次偶發。之前最後一次真實成功執行是 `2026-07-14T13:03:47Z` 的 windows-compat-ci workflow_dispatch run `29334937643`（真實測試失敗 exit code 1，促成 `a8e4b73` 修復）。`grep -rni "billing\|帳單\|spending limit"` 對 `docs/06_quality/AutoSDD_Defect_Log.md`／`ONBOARDING.md`／`CLAUDE.md` 皆零命中——即這個涵蓋 2 個關鍵 commit、持續 11+ 小時的 CI 全面停擺，從未被四方複審任何一方發現或記錄 | P0 | **無法以程式碼變更修復**：`windows-compat-ci.yml`（及其餘 4 個 workflow）本身無 bug，annotation 顯示 job 在 workflow 任何 step 執行前就被 GitHub Actions 平台攔下，workflow 檔內沒有、也不可能有任何 step 能偵測或繞過帳戶帳單問題；根因修復需**帳號所有者**至 GitHub「Billing & plans」還清欠款或調高 spending limit，超出本 repo 程式碼與本次 agent 權限範圍。本行之補救動作僅限「補齊揭露缺口」——不留其未被記錄的狀態 | **open（需人工於 GitHub Billing & plans 處理，非本輪程式碼可解）**：本行本身即為「揭露」動作，之後任何一輪複審／nightly 若見 `windows-compat-ci.yml`／`macos-compat-ci.yml`／`root-infra-ci.yml`／`AutoClaude CI`／`aisdlc-sdd-ci` 五者同步在數秒內 failure，應先查 annotation 是否為本行同款帳單訊息，而非誤判為程式碼回歸；帳單問題解除後，須對 `1175555`（或其後續 HEAD）**重新觸發一次 workflow_dispatch 或等待下一次 push**以取得這五份 workflow 對本輪四方複審實際變更的第一次真實機械驗證結果，在那之前「四方複審 APPROVE」不應被視為已通過機械 CI 驗證。**R14 複核（2026-07-20，Architect 親查 `gh run list`＋`gh api .../runs/29688470830/jobs`）：停擺持續中**——7/19 R13 commit `44d7c25` push 觸發五 workflow 全部 2~3 秒 failure、job `steps=[]` 即殞落（runner 零配置，同款帳單 annotation）；R12／R13／R14 三輪變更均未獲任何一次雲端機械驗證，ONBOARDING §9 各「雲端 CI 機械攔回」型緩解句在停擺解除前均為紙上防線；帳務解除後須對 HEAD 補跑一次 workflow_dispatch 取得首次真實驗證。**R42 複核（2026-07-24，SA／QA 一審各自獨立以 `gh run list --branch main --limit 8`＋`gh api .../jobs`＋`gh api .../check-runs/{id}/annotations` 重新即時驗證）：停擺持續中，未解除**——本輪最終 commit `33733a0`（R41 收尾）push 觸發之 `macos-compat-ci`／`windows-compat-ci`／`aisdlc-sdd-ci`／`root-infra-ci` 四 workflow（run `30088637281`／`30088637246`／`30088637224`／`30088637250`，`2026-07-24T11:10:08Z`）與前一 commit（R40 收尾）push（run `30085411368`／`30085411445`／`30085411530`／`30085411371`，`2026-07-24T10:13:26Z`）皆在 3~6 秒內 failure；`gh api repos/.../check-runs/89466619843/annotations` 證實 annotation 仍為同款「recent account payments have failed or your spending limit needs to be increased」訊息，job `steps=[]`（runner 未配置，非測試回歸）。距 DEF-101-081 首次記錄（`2026-07-14T13:10:41Z`）已持續 **9 天 22 小時+**，橫跨 R14~R42 共 20+ 輪皆未獲雲端機械驗證。**本輪起建議**：往後每輪四方複審至少執行一次 `gh run list --branch main --limit 8` 即時重新驗證（而非僅沿用舊帳本敘述），避免「停擺已解除但帳本未更新」或「仍停擺但誤判已解除」兩類過期風險 |
| DEF-101-200 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-2） | **排程「初次安裝」能力反向不對等——mac 有一鍵安裝器、Windows 沒有**：`AutoClaude/tools/fix_nightly_catchup.ps1:16-25` 以 `$ErrorActionPreference='Stop'`＋`Get-ScheduledTask` 開場，任務不存在直接炸＝僅能校正既有任務；新 Windows 機器（或任務誤刪後）無一鍵重建路徑，亦無 `--status` 心跳三態對等物 | P2 | 排下輪 Windows 輪（需實機驗證，勿在 mac 輪盲改）：升級為 ensure 語意（不存在→`Register-ScheduledTask` 建立）＋加 `-Status`，或另立 `install_win_nightly.ps1` 並更新 parity 登記 | open（routed 下輪 Windows 輪；ARCH-OPT-5 設計已載於本列分流欄；**R15 補三筆 Windows 輪同捆項**：ARCH-R15-5〔`run_local_nightly.ps1` 每日 RunId log 無保留期 prune、mac 側 R15 已落 14 天，兩平台政策須對齊〕、ARCH-R15-1 Windows 側〔`nightly_latest.log` 為全量 log 非 3 行契約，FAIL 內容哨兵需另設計〕、SCAN-C-11〔windows-compat nightly-full 取消語意與 macos 側 R13 CI-5 不對齊，僅 dispatch 連發情境〕）；**R22 校正**：核心訴求（ensure 語意安裝＋`-Status`）fixed@R19（`tools/install_windows_nightly.ps1` 新建，DEF-101-245）+R20 硬化（DEF-101-248/249/253/255）。三個 R15 riders 逐項訂正：ARCH-R15-5（nightly log 無保留期）本輪 fixed@R22——`run_local_nightly.ps1` 補 14 天輪替並排除 `nightly_latest.log`（比照 mac 側既有政策），新增 `test_nightly_log_retention_rotation_present` 靜態鎖；**R23 校正**：ARCH-R15-1（Windows FAIL 內容哨兵設計）**fixed@R23**——先讀 `run_local_nightly.ps1` 確認收尾處本已存在穩定機械可讀行 `Log ("END exit decision: exit={0} (failed stages: ...)" / "exit=0 (no failed stages...)")`（R9 複審 (c) 既有設計，非新增），故不需先補寫摘要行；新增 `tools/dev_start.py::_windows_heartbeat_fail_note()` tail 位元組窗格（16KB）掃描此行、`_check_nightly_heartbeat()` windows 分支改呼叫（原邏輯只判 mtime 新鮮度，讀不到「持續紅燈但仍在跑」狀態），新增 `TestWindowsHeartbeatFailSentinel`（原 4 tests：exit=1 帶 failed stages 告警＋summary／exit=0 零告警／大型全量 log 雜訊下仍命中尾端錨點／找不到錨點安全回 None；**R23 一審後追加至 7 tests**——SD 一審 bug-injection 命中原正則對空白數量/大小寫零容忍造成假陰性〔冒號後多空白／`END`小寫／`END`後多空白三種真實變體皆偵測不到 FAIL〕，`_WINDOWS_EXIT_DECISION_RE` 改為 `\s+`+`re.IGNORECASE` 並補 3 支對應測試；SD 二審另指出放寬後假陽性面擴大〔任何以 end 結尾單字緊接 `exit decision: exit=N` 皆會誤觸發，如 append/recommend/weekend〕，判定為既有設計弱點被放大而非新類別風險、且只影響 advisory 訊息，記入 DEF-101-263 供下輪視情況加 `\b` 邊界收斂）；SCAN-C-11（nightly-full 取消語意對齊）**fixed@R23**——`windows-compat-ci.yml` 原 workflow 層級單一 concurrency group 改為 job 層級三組獨立 concurrency（比照 `macos-compat-ci.yml` R13/CI-5 既有修復模式：windows-smoke per-ref+cancel=true、windows-nightly-full／windows-nightly-alert 各自固定字串 group+cancel=false），修復「同 event_name 內連續 workflow_dispatch 互踩取消」風險；`test_workflow_permission_concurrency_lock.py` 等既有 workflow 相關測試全數複跑通過，`yaml.safe_load` 驗證語法正確 |
| DEF-101-201 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-3） | **mac nightly 取證留存性系統性弱於 Windows**：①（已修）plist stdout/stderr 原導 `/tmp/autoclaude_nightly.{log,err}`——macOS 週期清理＋重開機清空，深夜失敗數日後排查 log 已散失；②（殘）心跳檔僅 3 行彙總、無 Windows 側 RunId log＋latest pointer 對等物，取證紀律 #3「PASS 聲稱必須引 RunId log:L」在 mac 結構上無法履行 | P2 | ① 本輪修復；② 排下輪（mac RunId log 對等） | fixed@R14（①：`install_mac_nightly.sh` heredoc 改導 `AutoClaude/logs/nightly_mac_launchd.{log,err}`〔gitignored、與心跳檔同目錄〕＋install 時 `mkdir -p` 防 launchd 缺目錄靜默丟 log＋ONBOARDING §8 範本同步＋已載入 plist 冪等重裝生效實證＋smoke 11 綠；**②殘項 open 記於本列**，下輪評估 RunId log；**② fixed@R15**：`run_local_nightly.sh` RunId log 落地——`AutoClaude/logs/nightly_mac_<ts>.log`（`exec >>` 改道＋BEGIN 首行 run_id、心跳檔第 4 行 `log=` 指標、`find -mtime +14` 14 天輪替；**心跳前 3 行契約與檔名零改動**）＋真機取證 `nightly_mac_20260720_182753.log` 四 stage PASS=4 FAIL=0（AutoClaude 3684/144、SDD 1475+1674；取證紀律 #3 mac 側自此可履行，`Nightly_Forensic_Discipline.md` v1.8 同步 mac 註記）；**ARCH-R14-REV-2（launchd log 無輪替）以需求物理消滅結案**——輸出改道後 launchd log 僅剩 exec 前啟動錯誤自限量，不建輪替機制（Rule 2）） |
| DEF-101-205 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-8） | exec-bit 政策（ONBOARDING §6 644/755 範圍句）無機械守門——R14 抽查現況合規（git index 755 集合＝6 hook＋run_local_nightly.sh 與政策句一致），漂移無訊號 | P4 | 低 ROI 單獨立項不值；可併入既有 NTFS/EOL 類掃描順手做，擇機 | open（記事存證） |
| DEF-101-206 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-9） | 深度 nightly 五軌（mutation/pg-e2e/perf/drift/obs）單押 Windows 一台實體機——R12 已如實揭露「心跳只能守本機」；CI 停擺＋R14 GitHub 深度軌降週頻雙重放大此單點（Windows 機斷載時五軌全球僅剩每週一 GitHub 窗口，且該窗口自身停擺中） | P4 | 記事存證；根本解＝帳務恢復（DEF-101-081）＋跨機心跳（超本輪範圍） | open（記事存證，與 DEF-101-081 交叉參照） |
| DEF-101-214 | 2026-07-21 | R15 四方一審（QA／SD 兩位獨立審查各自親歷；Architect 修復階段主控自身重演） | **方法論發現（非程式缺陷）：共享單一工作樹的多 agent 併發審查／修復存在真實交叉污染風險，本輪三度實例佐證**——①QA 親歷 `tools/dev_start.py` 在無自身寫入操作下短暫（約 2 分鐘）恢復成 git HEAD 版本又自行變回 R15 版本，事後對照 Architect 報告「過程中一度誤用 `git checkout` 把整份 R15 改動清空，已重建並還原」證實為同一事件；②SD 親歷 `run_local_nightly.sh.qa-backup` 殘留檔案與「上方註解 14 天／下方功能碼 30 天」暫態內部矛盾，係 QA 進行中的 bug-injection 尚未還原時被撞見（QA 隨後乾淨復原，三方交叉核對真正落地基線一致無誤）；③主控自身在修復 QA-R15-REV-4 時對 `autoclaude-ci.yml` 誤用 `git checkout --` 精確還原單一檔案，未料該檔案本身在 R15 輪已有正常增量修改（concurrency 區塊），指令把整份 R15 異動打回 HEAD（見 DEF-101-213 記載，當場發現並重新套用）。三起事件根因相同：**`git checkout`/`git restore` 對「尚未 commit 的異動」是全有全無的還原，無法選擇性只還原自己剛注入的 bug**，共享工作樹下極易誤傷其他審查者／自己稍早的合法異動。 | P2（方法論／流程風險，非本輪程式缺陷） | **建議下輪起四方複審／一審採 `Agent` 工具的 `isolation: "worktree"` 隔離每位審查者**（各自在獨立 git worktree 工作，互不干擾、bug-injection 還原失誤也不會波及他人或主控本身異動）；若仍需共享工作樹，還原紀律改為「`cp` 精確備份/回填單一檔案」而非 `git checkout`/`git restore`（後者是全域操作，即使目標只寫一個檔名，仍會抹除該檔案自己所有未提交的修改，不只抹除本次注入的部分） | open（記事存證＋流程建議送下輪／未來輪參考；本輪三起事件皆已確認未造成不可回復損害——最終工作樹經逐檔 diff --stat 核對與 git HEAD 基線一致） |
| DEF-101-217 | 2026-07-21 | R15 四方複審（QA／Architect 各自獨立親歷；SA/SD 兩次背景執行逾時/API中斷佐證同根風險） | **DEF-101-214 併發污染風險於複審輪再次真實重演，且新揭露「共享 scratchpad 目錄本身也有碰撞風險」這個 DEF-101-214 原文未提及的面向**：Architect 複審過程中以 `python3 -m unittest` 平行執行測試時，`test_workflow_permission_concurrency_lock.py::test_workflow_level_contents_read_present` 短暫轉紅（斷言找不到 `aisdlc-sdd-arch-fitness.yml` 的 permissions 區塊），經 `stat` 查 mtime 為複審進行中 2 分鐘前、Architect 自陳全程未編輯此檔，判定為「另一方（同時在跑的 SA/SD/QA 背景 agent）對同一檔案即時 bug-injection 造成的瞬間污染，已自行復原」；同時檢視共享 scratchpad 目錄，發現大量非 Architect 建立的檔案（`qa-backup/`、`imn_injected.sh`、`run_local_nightly.sh.orig_backup`、`repro_tee_exec.sh` 等，數個與其 mtime 幾乎同刻），證實該目錄確為本 session 四位審查者共用而非各自隔離。另主控自身於本輪帳本編輯過程中亦收到至少 2 次「檔案已在磁碟被修改」系統提示（`macos-compat-ci.yml`／`AutoSDD_Defect_Log.md`），逐一核實後確認皆為此同根污染的無害副作用（crossref 58 筆、diff --stat 增量皆與預期吻合，無實質內容遺失）。**複審階段另有 2 次背景 agent 執行逾時/API 中斷**（SA 第一次 stall 600s、SD 第一次 stall 600s＋第二次 API 連線中斷），時間點與內容（SD 中斷前訊息「Now let me inject a bug...」）高度提示同為共享工作樹資源競爭之外顯症狀，經主控立即查核工作樹皆確認未受損（zi 3 次重新派遣皆縮小範圍後於較短時間內成功完成）。 | P3（方法論／流程風險，非本輪程式缺陷；累計本輪已 4 起實例，風險評估由 DEF-101-214 的「觀察」提升為「確認模式」） | **Architect 建議：DEF-101-214 的「未來優先 isolation: worktree」應從「下輪參考」提升為「下輪起強制採用」**；且應一併檢討是否需要為每位審查者分配獨立 scratch 子目錄（本輪 4 起實例中至少 1 起〔SD 兩次逾時/中斷〕與共享 scratchpad 資源競爭時間點高度相關） | open（記事存證＋流程建議累加至 DEF-101-214 同一根因；本輪最終驗證確認全部 4 起實例皆無實損——工作樹與帳本內容經逐項核對與 crossref 機械複核，最終狀態正確完整） |
| DEF-101-218 | 2026-07-21 | R16 全面掃描（Shell/PowerShell 維度） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/verify_traceability.sh` `set -euo pipefail` + `grep \| sort -u \| wc -l` 管線靜默崩潰**：`grep` 找不到匹配時回傳 exit 1（正常「無匹配」語意），`pipefail` 讓整條管線視為失敗，`set -e` 因此在賦值敘述式上立即終止腳本；對空白 docs 樹與框架自己的真實 `docs/` 樹皆重現，在印出區段標題後、任何 PASS/WARN/FAIL 之前就中止 exit 1，作者原本設計的「找不到→印 ⚠️ 警告」分支完全不可達。此工具未接入 ci-gate.sh 或任何 pytest，無自動化閘門會執行到它 | P0 | 對每個受影響賦值敘述式補 `\|\| true` | fixed@R16：9 處（含複查追加發現的第 240 行 markdown-link-check 迴圈同款 bug）皆補 `\|\| true`；對照組（有匹配）、反例組（全新空白 docs）、框架真實 `docs/` 三組皆驗證跑到底且 exit 0 |
| DEF-101-225 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`run_local_nightly.sh` RunId log 缺觸發來源記錄 + 去重 check-then-act 無鎖（TOCTOU）**：本 repo 真實 log 顯示 2026-07-21 同日出現兩輪完整 PASS=4 執行且無 skip 訊息，因記錄不完整無法判斷是合理手動重跑還是真正去重漏洞；且 launchd `RunAtLoad` 與 `StartCalendarInterval(02:00)` 兩觸發源時間重疊時，兩行程可能同時通過「今日尚未有心跳」檢查，導致重複執行整套 nightly gate | P2 | BEGIN 行加印觸發來源；去重檢查外包檔案鎖 | fixed@R16：`TRIGGER_SRC` 判定（--force/`XPC_SERVICE_NAME`/互動終端/未知四態）寫入 BEGIN 行；本機無 `flock`，改用 POSIX `mkdir` atomic lock，陳舊鎖以 PID 存活判斷清除（比照 `dev_start.py` `_acquire_bootstrap_lock()`），`trap EXIT` 保證釋放。驗證：兩行程近乎同時呼叫，第二個正確被擋＋印跳過訊息；陳舊死 PID 鎖正確清除重取；三種觸發來源分支獨立驗證正確 |
| DEF-101-233 | 2026-07-21 | R16 Architect 架構檢視 | **排程機制無共用抽象層**：mac `install_mac_nightly.sh` 是成熟 ensure 語意安裝器，Windows 無對等安裝器；全庫無 `schedule_adapter` 抽象層，兩邊功能對等純靠人工在文件與 DEF 帳本互相校對 | P2（架構） | 建 `install_win_nightly.ps1`；補輕量「排程能力對照契約」機械測試（斷言 mac 支援子命令集合 ⊆ Windows 支援子命令集合），非重量級 ScheduleAdapter 介面 | open/routed（Architect 建議 C）：本輪未動，該不對稱已於 R14 記為 **DEF-101-200**（P2, open，已排入下一輪 Windows 輪）；本輪架構審查僅補充佐證與契約測試的具體設計建議，與 DEF-101-228 同批次一併處理；**R22 校正**：安裝器缺口已 fixed@R19（同 DEF-101-200 訂正）。「排程能力對照契約」機械測試本輪落地：新增 `tools/tests/test_schedule_capability_parity.py`（install/uninstall/status/render-preview 四能力語意對照表，非字面旗標字串集合比對——mac `--render-only` 與 windows `-WhatIf` 語意對等但字面不同，字面比對會產生假紅），DEF-101-233 全部訴求至此收斂完成 |
| DEF-101-234 | 2026-07-21 | R16 Architect 架構檢視 | **`init_project`/`run_self_evolution` 雙原生實作**（LATEST tools 下另兩對「雙原生實作」腳本，複雜度高：GitHub clone 精靈、FSE 有界驅動器），已有文件化豁免與人工審查覆蓋（`_EXEMPT_PAIRS` 附決策依據） | P4（架構，低優先） | 不建議現在主動重寫；等下次真的要改邏輯時「touch it, fix it」 | open watch（Architect 建議 D）：本輪不排入行動項，維持現狀觀察；下輪若有人異動其中任一對邏輯，應順手做 Python 核心化搬遷 |
| DEF-101-235 | 2026-07-21 | R16 全面掃描殘留 P3 觀察項（未修復，記事存證） | 本輪掃描發現但評估為低優先、暫不修復的殘留項：① `tools/dev_start.py` `_pid_alive`/`_list_pid_ppid_pairs_windows` 的 `ctypes.windll.kernel32.OpenProcess`/`CreateToolhelp32Snapshot` 未設定 `restype`（預設 32-bit，實際回傳 64-bit HANDLE），理論殘留無已知真實觸發；② `tools/windows_smoke_local.ps1:136` `Test-InstallRoundtrip` 假設 `.git` 是目錄，在 linked worktree 下會誤判，目前未觸發（呼叫點皆傳完整 clone）；③ `AutoClaude/tools/run_local_nightly.ps1:921` `Copy-Item` 更新 `nightly_latest.log` 指標無重試（不像同檔 `Add-LogLineSafe` 有退避重試），窄競態視窗；④ PS 5.1 版本守衛（DEF-101-124 家族）仍在 `run_tlc.ps1`/`run_self_evolution.ps1` 兩處逐字重複，未抽出共用模組 | P3 | 各項成本/風險評估後暫緩，留待下輪視是否觸發或有餘裕時處理 | open（記事存證，四項皆為理論性/低機率殘留，非本輪修復範圍） |
| DEF-101-238 | 2026-07-21 | R16 四方一審（SD 獨立審查，全庫搜尋比對兩個姊妹實作） | **`conditional.py` 的 `_SAFE_COND_PATTERN`（不含 `!`）與姊妹實作 `execution/mutation_applier/_conditional.py` 的白名單（含 `!`）字元集不完全一致**：後者無 `_DENY_CHARS` 第二層黑名單，純靠白名單過濾，理論上會放行 `!`。SD 確認 POSIX/cmd.exe 非互動殼下 `!` 無特殊展開意義，非安全漏洞 | P4（資訊性，兩套白名單字元集不一致但無安全危害） | 下一輪一併收斂兩份白名單字元集，避免未來各自演化出更大分歧 | open（記事存證，非阻斷；SD 一審判定資訊性問題） |
| DEF-101-242 | 2026-07-21 | R17 四方一審（QA 獨立 bug-injection 反向驗證） | QA 複審 DEF-101-241 ①③ 兩項修復時，用 bug-injection 揪出兩個「測試提供的保護力比表面看起來弱」的殘留缺口（非本輪引入的新回歸，但本輪修復恰好觸及）：**①（P2）`tools/tests/test_find_git_bash_parity.py` 只做結構等價比對（regex 抽取兩份原始碼的候選路徑/排除字面片段），不驗證行為**——QA 把 `find_git_bash()` 呼叫點改回舊版 `"system32" not in found.lower()`（只改呼叫點、不動 `_has_system32_segment()` 定義），parity 測試仍全綠，因 regex 抽取到的仍是死代碼裡的同一段文字；對呼叫點本身的行為迴歸零防護力。**②（P2）DEF-101-241① 手動補上的三行 CI paths**（`tools/lib/platform_utils.py`／`tools/bootstrap_core.py`／`AutoClaude/tools/run_act_core.py`）**本身未受機械鎖保護**——QA 對稱移除 `windows-compat-ci.yml` 兩側的 `platform_utils.py` 一行，`test_ci_paths_cover_root_consumers.py` 9 個測試仍全部 PASS（根因：`test_platform_utils_dedup.py` 用 `rglob` 掃描消費這三檔，掃描器的 `_IMPORT_RE` BFS 只認得 `import X` 陳述式，看不到這種消費模式，故對稱誤刪無法被現有覆蓋率檢查抓到；先前單側移除是被另一支對稱性檢查意外攔到，非真被覆蓋率檢查抓到） | P2×2（QA 一審判定均非阻斷，本輪合入不受影響） | ①排下輪：補一支對 `find_git_bash()`/`_has_system32_segment()` 直接呼叫的行為測試（不依賴 regex 結構比對）；②排下輪：仿 `test_known_consumers_detected()`「已知消費檔必被掃出」手法，替這三檔補一條專屬機械存在性鎖 | open（QA 一審明確判定非阻斷，記事存證排入下一輪 backlog；四方一審 Architect/SA/SD/QA 對本輪其餘修復之獨立覆核皆確認有效）；**R22 校正**：①②經核實已 fixed@R19（見 DEF-101-244①②：`test_find_git_bash_parity.py::TestFindGitBashBehavior` 行為測試、`test_ci_paths_cover_root_consumers.py::_resolve_sys_path_insert_dirs`/`_KNOWN_SUBPROCESS_ONLY_CONSUMERS` 存在性鎖），本列狀態文字過期未同步，本輪訂正；QA 一審 bug-injection 複驗 `TestFindGitBashBehavior` 回歸鎖確實有鑑別力 |
| DEF-101-243 | 2026-07-22 | R18 四方一審（QA 獨立 bug-injection 反向驗證，複審時合記三項非阻斷測試保護力缺口） | QA 複審 R18 修復（`windows_smoke_local.ps1` 新增 `[7/7]` 步驟、`ONBOARDING.md`/`AutoClaude/README.md`/`AutoClaude/CLAUDE.md` 文件同步）時，用 bug-injection 與靜態核查揪出三項非阻斷測試保護力縫隙：**①（P3）`$MinPass` 數值本身的語意正確性無機械鎖**——`tools/tests/test_smoke_ci_sync.py` 只交叉比對 `ONBOARDING.md` 宣稱數字與腳本 `$MinPass`/`MIN_PASS` 釘選值是否一致，不驗證釘選值是否等於實際步驟數；QA bug-injection 實測「整段刪除 `[7/7]` 步驟本體、保留 `$MinPass=10` 不變」會被 `test_scenario_groups_consistent_and_floored`（分組標籤與總數不符）攔到，但「只改錯 `$MinPass` 數值、步驟仍在」這種情境無專屬鎖。**②（P3）`AutoClaude/README.md` sprint-verified badge 與 `AutoClaude/CLAUDE.md` 檔尾「最後更新」日期皆為純人工欄位、零機械新鮮度檢查**——`grep -rn "sprint-verified\|sprint_verified"` 全 repo 零命中任何驗證腳本，不同於同檔已被 `tools/check_pytest_baseline_sites.py` 機械鎖住的 pytest 基線數字。**③（P3）`tools/tests/test_dev_start.py::TestLaunchdNightlyLoaded` 對 `_launchd_nightly_loaded()` 只測了 `"darwin"`/`"linux"` 兩態，缺 `"win32"` 專屬測試案例**（R18 本輪把該函式判斷式改用 `platform_utils.is_macos()`，行為等價、既有測試仍全過，但 Windows 分支從未被專屬案例直接驅動過） | P3×3（QA 二審判定均非阻斷，R18 本輪合入不受影響） | ①排下輪：仿 `test_known_consumers_detected()`手法，替 `$MinPass` 數值本身補一條「等於步驟數」的語意鎖；②排下輪：評估是否比照 pytest 基線模式（`check_pytest_baseline_sites.py`）替這兩個日期欄位加機械新鮮度告警；③排下輪：補一個 `mock.patch.object(sys, "platform", "win32")` 的獨立測試案例 | open（QA 二審明確判定非阻斷，記事存證排入下一輪 backlog；四方複審 Architect/SA/SD/QA 對 R18 其餘修復之獨立覆核皆確認有效）；**R22 校正**：①③經核實已 fixed@R19（見 DEF-101-244③⑤：`test_min_pass_equals_actual_step_count`、`test_win32_returns_none_without_spawning`），QA 一審 bug-injection 複驗①之語意鎖確實有鑑別力。②（README badge／CLAUDE.md 日期機械新鮮度鎖）複核後確認仍未建，維持 open，非本輪 scope |
| DEF-101-249 | 2026-07-22 | R20 全面掃描 + 真機驗證（真 Windows 11 機器，本輪首次對此腳本做非 `-WhatIf` 抽象層之外的真實 cmdlet 呼叫） | **P1，真機專屬缺陷**：`tools/install_windows_nightly.ps1` 呼叫 `New-ScheduledTaskSettingsSet -DisallowStartIfOnBatteries:$false -StopIfGoingOnBatteries:$false` 建立排程設定物件——這兩個參數名在此「建構」cmdlet 上根本不存在（真機實測 `(Get-Command New-ScheduledTaskSettingsSet).Parameters.Keys` 證實：正確參數名極性相反、名稱也不同，是 `-AllowStartIfOnBatteries`／`-DontStopIfGoingOnBatteries`；物件本身的屬性名才叫 `DisallowStartIfOnBatteries`／`StopIfGoingOnBatteries`，`fix_nightly_catchup.ps1` 讀寫既有任務走的正是這兩個屬性名，那裡沒錯，兩者極易混淆）。真機呼叫 `powershell -File tools\install_windows_nightly.ps1 -WhatIf` 直接拋 `ParameterBindingException`（NamedParameterNotFound）——**連 `-WhatIf` 預覽路徑都會 crash，並非只影響真安裝**，因為 PowerShell 在决定是否 ShouldProcess 之前就先做參數綁定。R11~R19 歷輪皆只對此腳本做語法解析（`[Parser]::ParseFile`）與靜態文字斷言，從未真的呼叫過這個 cmdlet（`test_install_windows_nightly.py` 檔頭本就明文「非 Windows 主機無法真的執行」），故此缺陷從 R19 引入至今從未被發現，**是本輪身為「首次真 Windows 機器複審」而非純靜態/跨平台鏡射複審的最高價值發現** | P1 | 改用正確參數名 `-AllowStartIfOnBatteries`／`-DontStopIfGoingOnBatteries` | fixed@R20：`$settings = New-ScheduledTaskSettingsSet -StartWhenAvailable -WakeToRun -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries`；真機驗證：`New-ScheduledTaskSettingsSet` 建構後讀回 `.DisallowStartIfOnBatteries`/`.StopIfGoingOnBatteries`/`.StartWhenAvailable`/`.WakeToRun` 四屬性值皆為 `False/False/True/True`（符合預期極性）；`-WhatIf` 真機重跑成功印出 `What if: ...Register-ScheduledTask...` 且 exit=0（原本的安裝路徑本身也缺一個明確的結尾 `exit 0`，一併補上，對齊 `-Uninstall`/`-Status` 兩分支既有的明確 exit 慣例）。新增 `tools/tests/test_install_windows_nightly.py::TestInstallWindowsNightlySettingsConstruction`（`platform.system()=="Windows"` 才跑，windows-latest CI runner 可安全真跑此 cmdlet、不需系統管理員權限）：直接從原始碼抽出 `$settings = New-ScheduledTaskSettingsSet ...` 該段文字、原封不動丟給真正的 PowerShell 執行並斷言四屬性值——bug-injection 驗證：把參數名改回原錯誤版本，此測試與既有 `test_catchup_settings_match_fix_nightly_catchup_target_values`（同步訂正為斷言正確參數名、禁止錯誤參數名）皆確認變紅，還原後核對逐位元組一致 |
| DEF-101-251 | 2026-07-22 | R20 全面掃描複核 R19 殘留 backlog（DEF-101-246⑤⑥⑦、DEF-101-247②③④） | 逐項複核並落實「能當場驗證當場修就別再排下一輪」：**⑤/④（DEF-101-246⑤ 方法論限制 + DEF-101-247④ QA 提案，已實作）** `_SH_EXCLUSIVE_PASS_GROUPS` 顯式登記表可被謊報繞過——QA 提案的輕量緩解（斷言兩錨點間距 ≤30 行且存在 `else`/`;;` 分支關鍵字）本輪落地為 `test_exclusive_pass_groups_are_genuinely_branch_separated`，bug-injection 驗證（插入兩個無條件相鄰的假互斥 `pass` 並謊報登記）確認會變紅。**⑦（DEF-101-246⑦，已修復）** 見 DEF-101-248。**③（DEF-101-247③，已修復）** 見上方 `test_win32_returns_none_without_spawning` 更新——已補 `os.spawnv`（`create=True`，跨平台安全，`os.posix_spawn` 為 POSIX-only）。**⑥（DEF-101-246⑥，複核後維持 backlog，非懶惰延後）**：`_resolve_sys_path_insert_dirs()` 只認固定 `sys.path.insert(0, str(Path(__file__).resolve().parents[N] / "seg"))` 寫法——此為輕量 regex 掃描器的方法論邊界（真正泛化需要控制流/資料流分析，與該檔案已放棄通用剖析器方向矛盾），且現行零風險（僅防未來假設性寫法變體），本輪判定維持 backlog 合理。**②（DEF-101-247②，複核後維持 backlog，非懶惰延後）**：`_FROM_IMPORT_RE` 只堵行首 `from X import Y`，未堵縮排的 `try/except ImportError` 防禦性匯入——與同檔既有 `_IMPORT_RE` 同款限制對稱存在（非本次修復引入的新不對稱），SD 於 R19 已確認現行 `tools/tests/`／`AISDLC_SDD/scripts/tests/` 零實例，純理論風險，維持 backlog | P2×1（⑤/④）／P3×2（⑦已併入 248、③已修）／P3×2（⑥②維持 backlog） | ⑤④⑦③本輪落地；⑥②經複核後明確維持 backlog（附具體理由，非無謂延後） | fixed@R20：詳見 DEF-101-248（⑦）；`test_smoke_ci_sync.py::test_exclusive_pass_groups_are_genuinely_branch_separated`（⑤④）；`test_dev_start.py::test_win32_returns_none_without_spawning`（③）皆已落地並經 bug-injection 驗證；⑥②經 Architect/SD 既有理由複核後維持 open（backlog，理由如上，非新缺陷） |
| DEF-101-252 | 2026-07-22 | R20 四方一審（QA 對抗式 bug-injection，Architect/SA/SD 皆 APPROVE） | QA 對 DEF-101-228 Mutex 去重鎖修復做 bug-injection：把 `if (-not $NightlyMutexAcquired) { ... }` 判斷式改成 `if ($false -and (-not $NightlyMutexAcquired))`（鎖邏輯完全廢掉、恆不觸發 skip，`Global\AutoClaude_Nightly_Run`／`System.Threading.Mutex`／`AbandonedMutexException` 字面全數保留），既有靜態測試 `test_concurrency_guard_mutex_present`（只做字面比對）仍全綠——真實假陽性，對「鎖存在但邏輯被廢掉」這種回歸零防護力 | P1 | 補一支會真的執行鎖判斷邏輯的 Windows-only 真機測試（不必等 SD_10 W0 Pester 自動化），比照 `TestInstallWindowsNightlySettingsConstruction` 的「抽取原始碼片段真機執行」手法 | fixed@R20：`AutoClaude/tests/tools/test_run_local_nightly_static.py` 新增 `TestConcurrencyGuardBehavior`（`platform.system()=="Windows"` 才跑）：`_extract_mutex_guard_snippet()` 從原始碼抽出鎖判斷片段、代換成測試專用 Mutex 名稱（`Global\AutoClaude_Nightly_Run_TestOnly`，避免撞真實排程用的正式鎖）後真機執行，`test_guard_blocks_when_another_process_holds_the_mutex`（先起一個持鎖 6 秒的背景行程，斷言主行程不會執行到鎖之後的標記）與 `test_guard_proceeds_when_mutex_is_free`（鎖是空的時斷言會執行到標記）兩個測試皆真機驗證通過；QA 原始注入（判斷式恆假）重現後兩測試皆變紅（抽取正則因結構改變而找不到片段、fail-loud），與既有靜態測試形成互補（靜態測試守字面存在，新測試守真實行為），逐位元組核對還原一致 |
| DEF-101-253 | 2026-07-22 | R20 四方一審（QA 對抗式 bug-injection，Architect/SA/SD 皆 APPROVE） | QA 對 DEF-101-249 `New-ScheduledTaskSettingsSet` 參數名修復做 bug-injection：保留正確參數名字面（`-AllowStartIfOnBatteries`／`-DontStopIfGoingOnBatteries`），但加上 `:$false` 反轉極性（`-AllowStartIfOnBatteries:$false -DontStopIfGoingOnBatteries:$false`——實際行為與原始錯誤參數名效果相同，真正把「筆電吃電池時擋啟動」打開），`tools/tests/test_install_windows_nightly.py::test_catchup_settings_match_fix_nightly_catchup_target_values` 的 `assertIn` 只做子字串比對仍判定為 PASS——只有真機執行測試 `TestInstallWindowsNightlySettingsConstruction` 能擋下，非 Windows CI job（ubuntu-latest／macos-latest）上該真機測試被跳過，只剩會被繞過的靜態測試 | P2 | 補負向斷言直接堵 `:$false` 後綴反轉極性，不讓靜態測試單獨對值極性毫無防護 | fixed@R20：`test_catchup_settings_match_fix_nightly_catchup_target_values` 新增 `assertNotRegex` 兩條，堵 `-AllowStartIfOnBatteries\s*:\s*\$false` 與 `-DontStopIfGoingOnBatteries\s*:\s*\$false`；QA 原始注入（加 `:$false` 後綴）重現後測試變紅，還原後逐位元組核對一致 |
| DEF-101-254 | 2026-07-22 | R20 四方一審（SD／SA／Architect 三方交叉獨立提出非阻斷建議，皆已落地） | 三項非阻斷建議：**①（SD＋SA）** `AutoClaude/tools/run_local_nightly.ps1` 的 Mutex 建構 `try/catch` 只接 `AbandonedMutexException`，罕見環境（跨權限層級 session 互開同一個 `Global\` 具名物件撞 `UnauthorizedAccessException` 等）會讓例外未被捕捉、直接讓整支 963 行 nightly 腳本在任何 stage 執行前崩潰，鎖機制本身失敗不該讓整晚驗證訊號開天窗。**②（SA）** `tools/windows_smoke_local.ps1` `[8/8]` 只斷言 `-WhatIf` 呼叫 `exit=0`（不 crash），未驗證輸出內容是否真的反映「將執行 Register-ScheduledTask」，涵蓋深度較淺。**③（SD）** `tools/tests/test_smoke_ci_sync.py` 新測試 `segment = lines[start:end]` 的行內註解「不含兩端」與實際切片行為（排除第一錨點自身行、但包含第二錨點自身行）有一行落差，對相鄰 case 分支而言測試通過是巧合（命中第二錨點自身行尾的 `;;`）而非文件宣稱的邏輯 | P3×1（①）／P3×1（②）／P4×1（③） | 逐項本輪落地 | fixed@R20：①新增 `catch { Write-Warning ...; $NightlyMutexAcquired = $true }` 兜底子句，任何非 `AbandonedMutexException` 的例外皆降級為警告並視同已取得繼續執行；②`[8/8]` 改用獨立子行程呼叫（`& powershell.exe -File ... -WhatIf`，同行程內 `& script` 呼叫因 ShouldProcess 的 WhatIf 訊息走 host-only 輸出無法用一般串流重導向捕捉，真機實測獨立子行程可正確捕捉）並斷言輸出含 `Register-ScheduledTask`，真機重跑 `windows_smoke_local.ps1` PASS=11 FAIL=0 確認生效；③訂正行內註解精確描述切片實際排除/包含範圍，如實記錄此邊界對 case 分支的巧合有效性，非本輪修復範圍 |
| DEF-101-255 | 2026-07-22 | R20 四方複審（QA 二輪對抗式 bug-injection，換新角度戳修復本身） | QA 複審刻意不重複一審手法：**新角度大小寫混寫**——把 DEF-101-253 修復注入的 `-AllowStartIfOnBatteries:$false` 改成 `-AllowStartIfOnBatteries:$False`（大寫 F）。PowerShell 對 `$false`/`$False` 完全等價（真機驗證行為相同：兩個屬性皆變 `True`，功能一樣壞掉），但 `assertNotRegex(r"...\$false")` 正則字面比對是大小寫敏感，找不到大寫變體而誤判為 PASS——與 DEF-101-253 同一根因（正則字面比對 vs 真實語意等價）的新變體，一審漏了大小寫這個維度 | P2 | `assertNotRegex` 兩處補 `re.IGNORECASE` | fixed@R20：`tools/tests/test_install_windows_nightly.py` 的兩條 `assertNotRegex` 改用 `re.compile(pattern, re.IGNORECASE)`；QA 原始注入（`:$False` 大寫變體）重現後測試變紅，還原後 `git diff --stat` 核對與提交前一致（19 insertions/4 deletions）；`tools/tests/test_install_windows_nightly.py` 10 passed／1 skipped |
| DEF-101-256 | 2026-07-22 | R21 全面掃描（B 維度：Python 跨平台）＋四方一審 Architect/SA/SD/QA 交叉技術指引 | `tools/tests/_platform_helpers.py::copy_functional_interpreter()` 只複製直譯器 exe 本體＋條件式 `pyvenv.cfg`，沒有複製同層相依 DLL；當 `sys.executable` 本身**不是**透過 venv 執行時（範圍：任何未啟用 venv 的官方支援直譯器安裝路徑——`ONBOARDING.md` §1 列出的 winget／python.org 安裝器版型、與 pyenv-win，皆會命中同一情境；uv 管理的直譯器因走 `pyvenv.cfg` 分支已被涵蓋），複製出的孤立 exe 因缺少同層 DLL（如 `python311.dll`、`vcruntime140.dll`）在 Windows 上啟動失敗，`subprocess` 回傳 `rc=3221225781`（`0xC0000135` `STATUS_DLL_NOT_FOUND`），導致 `tools/tests/test_dev_start.py` 6 個呼叫 `copy_functional_interpreter` 模擬「健康既有直譯器」情境的測試假紅——純測試基建缺陷，不影響 production（已核實 `tools/dev_start.py` 全檔 `_venv_healthy()` 及全檔 grep `shutil.copy/copytree` 皆與此無關，production 從不複製直譯器） | P2 | `copy_functional_interpreter()` 新增無條件（不做 `if is_windows()` 平台分支）DLL glob-and-copy：來源目錄用獨立命名變數 `interpreter_dll_dir = Path(sys.executable).parent`（與既有 pyvenv.cfg 分支的 `.resolve().parent.parent` 完全分開，避免混用同一表達式致 glob 恆空的隱蔽退化），限縮為具名 pattern `python3*.dll`／`vcruntime140*.dll`（非裸 `*.dll` 全複製，避免誤複製 sqlite3/libssl/tcl-tk 等不必要的 DLL）；macOS/Linux 上 `sys.executable` 同層通常沒有 `.dll` 副檔名檔案，glob 自然空手，天生 no-op，三平台行為一致 | fixed@R21：`tools/tests/_platform_helpers.py::copy_functional_interpreter()` 新增 DLL 複製邏輯並更新 docstring；`tools/tests/test_dev_start.py` 新增 `TestCopyFunctionalInterpreterDllCopy`（環境無關自證測試，monkeypatch `sys.executable` 指向暫時假來源目錄）兩支測試：`test_copies_named_dll_patterns_beside_dest`（斷言具名 pattern 被複製、無關 DLL 不被複製）與 `test_no_dlls_present_is_a_safe_noop`（無 DLL 來源時安全 no-op）皆綠燈；既有 6 個呼叫點測試（`test_dev_start.py` 全套 136 tests OK, skipped=9）不受影響。**真機 e2e 驗證**（用本機真實 pyenv-win 直譯器 `C:\Users\wuwei\.pyenv\pyenv-win\versions\3.11.9\python.exe`，monkeypatch `sys.executable` 模擬「PATH 只掛 pyenv-win、未啟用 `.venv`」情境）：修復前（暫時 `git stash` 還原舊版邏輯）複製出的直譯器 `--version` 執行 `rc=3221225781`（`hex=0xc0000135`，與缺陷描述完全吻合），且 `tools/tests/test_dev_start.py` 在此情境下用 pyenv-win 直譯器跑會有 7 個測試 FAILED（既有 6 個呼叫點 + 新增 1 個自證測試）；`git stash pop` 還原修復後，同一情境複製出的直譯器 `--version` 執行 `rc=0`、`stdout=b'Python 3.11.9\r\n'`，`test_dev_start.py` 用 pyenv-win 直譯器全套 136 tests OK（skipped=9）全綠 |

| DEF-101-257 | 2026-07-22 | R22 全面掃描（Scan-C：CI/排程基建維度）＋四方一審 Architect/SA/SD 一致判定本輪必修 | `AutoClaude/tools/run_local_nightly.ps1` 的 dated nightly log（`nightly_<date>_<time>.log`）**零保留期**——mac 側 `run_local_nightly.sh:207` 早於 R15 即有 `find ... -name 'nightly_mac_2*.log' -mtime +14 -delete`，Windows 側全檔搜尋無任何 prune/retention 邏輯，`Copy-ItemWithRetry` 只覆寫 `nightly_latest.log` 指標檔，帶時間戳的本體從未刪除；真機驗證：本機 `AutoClaude/logs/` 累積 10 份，最舊 24 天前仍在。本項並非全新缺口，是 **DEF-101-200 於 R15 掛號的 ARCH-R15-5 rider**（本表原列未曾隨後續 R16~R21 多輪「Windows 輪」修復其他子項而回頭處理，R22 SA/SD 一審各自獨立追出同一段舊帳） | P2 | 比照 mac 側 14 天政策，`Get-ChildItem` + `Where-Object LastWriteTime` + `Remove-Item` 對稱實作，務必排除 `nightly_latest.log` | fixed@R22：`run_local_nightly.ps1` 於 latest pointer 更新後新增 14 天輪替區塊（`Get-ChildItem -Filter 'nightly_2*.log' -File \| Where-Object { $_.Name -ne 'nightly_latest.log' -and $_.LastWriteTime -lt (Get-Date).AddDays(-14) } \| Remove-Item -Force`，包 try/catch 失敗降級 WARN、不影響 exit 語意）；新增 `tools/tests/test_run_local_nightly_static.py::test_nightly_log_retention_rotation_present` 靜態鎖（28 tests 全綠）；獨立於暫存目錄實機驗證：20 天前的假 log 被刪除、5 天前的假 log 保留、`nightly_latest.log` 即便本身逾 14 天亦不被誤刪，三案例皆符預期。同步訂正 `Nightly_Forensic_Discipline.md:100` 誤導措辭（原文暗示 Windows 側早有「保留策略輪替」機制，此前實為子虛烏有，見 DEF-101-200 本輪訂正）。**二審 QA 對抗式驗證補充（非阻斷，記入下一輪 backlog）**：QA 親自 bug-injection 拿掉 `-ne 'nightly_latest.log'` 排除條件重跑，證實目前 `-Filter 'nightly_2*.log'` 本身（字首非 `nightly_l`）已是唯一實際生效防線、`-ne` 現屬冗餘防護，不會誤刪 latest；但配套 `test_nightly_log_retention_rotation_present` 是純結構/regex 測試，非真執行輪替邏輯的行為測試——若未來 Filter 改寬（如改成 `'nightly_*.log'`），現有測試偵測不到「`-ne` 從冗餘轉為唯一防線」這種語意轉變。排入下一輪 backlog（P4）：比照 `test_find_git_bash_parity.py::TestFindGitBashBehavior` 模式補一支真執行輪替邏輯的行為測試 |
| DEF-101-258 | 2026-07-22 | R22 全面掃描（Scan-B：Python 跨平台維度）＋四方一審 QA 對抗式驗證確認零呼叫端 | 4 個孤兒除錯腳本（`AutoClaude/tools/_inspect_cli.py`／`_inspect_lint.py`／`_inspect_settings.py`／`_run_lint_imports_utf8.py`）以裸 `io.TextIOWrapper` 包裝 stdout（未用 `tools/lib/platform_utils.init_utf8_streams()`），自 2026-06-12（`improving_01`）首次入庫後從未被任何 CI workflow／hook／文件／其他腳本引用（QA 一審全庫 grep 交叉複驗確認零引用，並特別排除 `tmp_lint_check.py`——該檔雖同款寫法但被兩份 compat-CI workflow paths 與 `test_subprocess_encoding_hygiene.py` 實際消費，非孤兒，不在本次處置範圍） | P3 | 刪除（無呼叫端、`git rm` 零副作用，優於保留死碼） | fixed@R22：`git rm` 移除上述 4 檔（`_inspect_cli.py`／`_inspect_lint.py`／`_inspect_settings.py`／`_run_lint_imports_utf8.py`），全庫 grep 複驗刪除前後零引用差異、`tmp_lint_check.py` 保持不動 |
| DEF-101-259 | 2026-07-22 | R22 Architect 一審（架構專項檢視指出治理縫隙）＋SD 一審給出語意對照表設計 | 承接 **DEF-101-233** 殘留訴求「排程能力對照契約」機械測試從未落地：`tools/check_script_parity.py` 把 `install_mac_nightly.sh`／`install_windows_nightly.ps1` 登記為 `_SINGLE_SIDED_EXEMPT`，舊註解宣稱「行為對等由 `test_install_windows_nightly.py` 守門」，但該檔實際只驗證 Windows 側自身結構，從未跨檔比對 mac 側能力集合——形成「兩邊都以為對方在管」的治理縫隙（Architect 一審用語） | P3 | 新增獨立測試檔（不塞進 `check_script_parity.py`，該檔已誠實自陳只比對 step 標籤序列）；用「語意能力→各平台實際承載物」對照表比對，不可用字面旗標字串集合比對（mac `--render-only` 與 windows `-WhatIf` 語意對等但字面不同，字面比對會產生假紅——SD 一審明確指出的陷阱） | fixed@R22：新增 `tools/tests/test_schedule_capability_parity.py`（6 tests，install/uninstall/status/render-preview 四能力語意對照表，靜態 regex 從 mac `case "${MODE}" in` 區塊與 windows `param()` 區塊各自抽取實際承載物比對，並含 2 支「鏡子自證」測試防抽取 regex 本身失準）；bug-injection 驗證（模擬 windows 端漏 `Uninstall` switch）確認 regex 邏輯真能偵測缺失情境，非裝飾性覆蓋；同步訂正 `check_script_parity.py` 對 `install_windows_nightly.ps1` 的豁免註解，移除失實的「行為對等由 test_install_windows_nightly.py 守門」措辭，改指向新測試檔 |
| DEF-101-260 | 2026-07-22 | R23 四方前置掃描（P3）：`tools/check_ntfs_paths.py` 全庫 subprocess.run 慣例掃描 | `tools/check_ntfs_paths.py:83-90` 的 `_tracked_files()` 呼叫 `subprocess.run(["git", ..., "ls-files", "-z"], ..., encoding="utf-8", check=True)` 只設 `encoding="utf-8"`，缺 `errors="replace"`——全庫其餘所有同款 `subprocess.run(..., text=True, encoding="utf-8", ...)` 呼叫（`tools/dev_start.py`／`tools/bootstrap_core.py`／`AutoClaude/autoclaude/execution/evaluator.py` 等）皆一致補 `errors="replace"`，唯獨此檔沒補；tracked 路徑含非法 UTF-8 位元組序列時 `.stdout` 解碼會拋未捕捉 `UnicodeDecodeError`，讓「NTFS 敵意檔名防護」CI 腳本自己先崩潰，而非印出乾淨的 `❌ 違規清單` | P3 | 補 `errors="replace"`，與本檔其餘慣例一致 | fixed@R23：`tools/check_ntfs_paths.py:88` 補 `errors="replace"`；新增 `AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py::test_tracked_files_passes_errors_replace_and_survives_invalid_utf8`（mock `subprocess.run` 依實際傳入 kwargs 對含非法 UTF-8 位元組的假輸出解碼，證明修復前 `errors` 缺省走 `strict` 會炸 `UnicodeDecodeError`、修復後安全通過並確實斷言 kwargs 含 `errors="replace"`）；`test_ntfs_length_gate.py` 全套 15 tests 綠 |
| DEF-101-262 | 2026-07-22 | R23 四方前置掃描（P3/P4）：ruff 存量 baseline 數字新鮮度掃描 | `docs/06_quality/AutoSDD_Defect_Log.md`（DEF-101-018 條目）、`ONBOARDING.md` §9 表尾對照段（約第 260 行）、`AutoClaude/pyproject.toml:32` 行內註解三處皆寫「baseline 1,339 筆」，但本輪於 `AutoClaude/` 下實跑 `.venv/Scripts/python.exe -m ruff check . --statistics`（鎖版 0.15.21）實測得 **1,147** 筆——純描述性文字漂移（`grep -rn "1339"` 只命中這三處敘述文字，無任何測試/gate 拿 1,339 當機械斷言比對） | P3～P4（純文件數字新鮮度；無機械斷言受影響） | 三處皆更新為實測數字，並註明「R23 複審實測更新，原數字為舊測值」避免未來誤以為機械釘選 | fixed@R23：`AutoClaude/pyproject.toml:32` 行內註解、`docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-018 列、`ONBOARDING.md` §9 表尾對照段三處皆訂正為「baseline 1,147 筆」並註記 R23 實測來源；`grep -rn "1339\|1147" docs/ ONBOARDING.md AutoClaude/pyproject.toml` 複核三處新數字一致、無殘留舊值 |
| DEF-101-265 | 2026-07-23 | R24 四方前置掃描（P3）：Scan-C CI/CD 與排程機制掃描，`windows-compat-ci.yml` 與 `ONBOARDING.md` 揭露對稱性核對 | **`windows-compat-ci.yml`／`ONBOARDING.md` 未揭露 GitHub-hosted `windows-latest` runner 實際對應 Windows Server 2022（伺服器 SKU）、與宣稱標的「Windows 11」用戶端環境的潛在落差**：mac 側 `ONBOARDING.md`（macos-compat-ci.yml 段落）已有明文揭露句「GitHub-hosted `macos-latest` runner 與開發者個人 Mac 的實際硬體／OS 版本仍可能有落差」，但 Windows 側自稱目標更具體（明確宣稱「Windows 11」而非泛稱作業系統）卻反而沒有對等揭露句，屬揭露不對稱 | P3（文件揭露缺口，不涉程式邏輯；電源管理/排程相關 API 如 `schtasks`／`New-ScheduledTaskSettingsSet` 行為在伺服器 SKU 與用戶端 SKU 間可能有出入，見 DEF-101-002／249） | 於 `ONBOARDING.md` §8 比照 mac 側句式補一段對等揭露 | fixed@R24：`ONBOARDING.md` §8 新增「R24 對稱補揭露」一段，明文揭露 `windows-latest` 現行對應 Windows Server 2022、與開發者個人 Windows 11 用戶端環境仍可能有落差，純文件補強。**一審訂正（R24，QA 一審獨立核實行號後命中）**：原「R10 Windows 側對等補償段落**後**」方向寫反，實際插入位置在 R10 段落**之前**（緊接 R11 補驗紀律段落後），內容本身正確、僅描述方向性筆誤 |
| DEF-101-266 | 2026-07-23 | R24 四方前置掃描（P4）：Scan-D 文件契約帳本一致性掃描，`ONBOARDING.md` §7 AutoClaude 巢狀 session pytest 基線抽查 | `ONBOARDING.md` §7（R22 校正段）宣稱巢狀 Claude Code session 下 full pytest 基線為「3,607 passed / 207 skipped」，本輪實跑 `python -m pytest tests/ -q`（同款巢狀 `CLAUDECODE=1` 環境）得 **3,608 passed / 207 skipped**——passed +1，R23 之後有新測試合入但本節未回填，純文字新鮮度問題（`tools/check_pytest_baseline_sites.py` 鎖的是「數字唯一站點」此治理結構本身，非鎖死具體數字，不影響任何機械閘門判準） | P4（純描述性數字，落差 0.03%，無任何機械斷言依賴此值） | 順手更新為實測數字 | fixed@R24：`ONBOARDING.md` §7 R22 校正段內數字更新為「3,608 passed / 207 skipped」並註記「R24 再訂正 +1」 |
| DEF-101-268 | 2026-07-23 | R25 四方二審/三審（SA 獨立在無其他審查員並行干擾下仍重現同款假紅，鎖定更精確根因） | **`tools/__pycache__/*.pyc` 位元碼快取寫入競態導致 pytest 偶發假紅**：R25 四方一審四位審查員並行執行時，SA／SD／QA 皆回報 `run_root_unittests.py`/`test_dev_start.py` 偶發假紅（`_WINDOWS_EXIT_DECISION_RE` 誤判「backend」/「high-end」為真收尾錨點）。主控最初判定根因為「並行審查員彼此對共用主樹做暫時性 bug-injection 互踩」（見上方 DEF-101-263⑤ 段落，Architect 二審親身重演佐證：`git checkout --` 誤用一度造成主樹真的短暫存在壞版本）。**三審階段 SA 進一步驗證**：在**單一 pytest 呼叫、無其他審查員干擾**的隔離情境下，仍以約 1/15 機率重現同款假紅；純 Python 緊迴圈直接呼叫 `_check_nightly_heartbeat` 連跑 3000 次零誤判（排除正則邏輯本身缺陷）；改查 `__pycache__/dev_start.cpython-311.pyc` 與原始碼 mtime 幾乎同一分鐘、加 `PYTHONDONTWRITEBYTECODE=1` 重跑 15 次零失敗——佐證另有一條獨立成因：pytest 對同一份共用磁碟 `.pyc` 的位元碼快取讀寫存在競態，短暫視窗內可能載入到修復前的舊編譯版本。**兩條根因並存、互不排斥**：跨審查員主樹突變互踩（[[parallel-mutation-audit-collision]]）與位元碼快取寫入競態，皆屬「多個行程同時存取同一份共用檔案系統資源」這一大類，只是載體不同（原始檔本身 vs `.pyc` 快取） | P4（純測試執行環境層面的間歇性噪訊，不影響任何生產邏輯正確性；R25 四方三審對本輪 4 項程式修復本身已一致 APPROVE，此項純屬環境可靠性觀察） | 是否要 repo-wide 設定 `PYTHONDONTWRITEBYTECODE=1`（或等效：`conftest.py` 設 `sys.dont_write_bytecode = True`）消除此類位元碼快取競態，涉及「測試執行速度 vs 可靠性」的取捨與是否影響其他既有工具鏈（如 `bootstrap_core.py` 的 pyc 快取假設），需要人工決策，不當場動；下一輪可評估是否要做，或先觀察此假紅是否在正常（非四方並行複審）的日常開發/CI 流程下也會出現 | open（記事存證，R25 三審發現；不影響本輪任何修復項目的正確性判定） |
| DEF-101-263 | 2026-07-22 | R23 四方一審/二審（QA 一審記錄 4 項＋SD 二審記錄 2 項，共 6 項非阻斷發現，本輪補記存證避免審查產出隨裁決收斂憑空消失） | **六項非阻斷 backlog，逐項記錄供下輪追蹤**：①`tools/check_ntfs_paths.py:88`（DEF-101-260 修復後）——tracked 路徑若真含非法 UTF-8，違規清單印出的檔名會混入 U+FFFD 替代字元亂碼，人類辨識度打折，建議在該檔既有「已知侷限」段落補充說明（QA 一審）；②`tools/dev_start.py::_windows_heartbeat_fail_note()`（DEF-101-200 rider ARCH-R15-1）——偵測邏輯靠字面正則對齊 `AutoClaude/tools/run_local_nightly.ps1:978,980` 的 `Log(...)` 字面量，兩邊無跨檔一致性機械鎖，任一方未來改版摘要行格式會讓哨兵靜默永久回 `None`（QA 一審）；③`.github/workflows/windows-compat-ci.yml`／`macos-compat-ci.yml`（DEF-101-200 rider SCAN-C-11 修復後）——job 層 concurrency 的 `group:`/`cancel-in-progress:` 字面值目前無機械回歸鎖（`test_workflow_permission_concurrency_lock.py` 只鎖 `aisdlc-sdd-arch-fitness.yml`/`autoclaude-ci.yml`），macos 側此缺口早於 R13 即存在、非本輪新增（QA 一審）；④dot-source 陷阱防護（DEF-101-261）——`pwsh -Command ". ./realscript.ps1"`（`-Command` 包裝一支真正生產腳本檔的 dot-source）呼叫寫法下，`(Get-PSCallStack)[-1].ScriptName` 仍判為互動情境（誤判 False），若此類 CI/自動化呼叫寫法未來被採用會退回「失敗時 return 而非 exit」；全庫 grep 目前零命中，純理論性（QA 一審）；⑤`tools/dev_start.py::_WINDOWS_EXIT_DECISION_RE`（DEF-101-261 追加修復，R23 一審後把正則改寬容後）——新正則 `end\s+exit\s+decision:\s*exit=(\d+)...`（`re.IGNORECASE`）的 `end` 前無 `\b` 單字邊界，任何以 end 結尾單字（append/recommend/weekend/backend…）緊接 `exit decision: exit=N` 皆會誤觸發，假陽性面比修復前（僅比對大寫字面 `END`）顯著擴大；風險評估為低（人工撰寫的專屬收尾語，真實雜訊輸出中巧合出現機率極低），是既有設計弱點被放大而非新類別風險，只影響 advisory 警告文字（SD 二審 bug-injection 實測）；⑥`tools/lib/GitHooksInstallCommon.ps1` 模組頂層分支的 `[Environment]::Exit(1)`（DEF-101-261 追加修復）——經 SD 二審探針腳本證實會跳過外層 `try/finally`（.NET Environment.Exit 語意即立即終止行程、不做受控堆疊展開），`throw` 可在同樣正確終止＋正確 exit code 前提下不犧牲 `finally` 語意，理論上更穩健；已查證兩支生產呼叫端（`AutoClaude/tools/install_git_hooks.ps1`、`AISDLC_SDD/scripts/install-hooks.ps1`）dot-source 位置在任何 `try`/資源配置之前，此分支在現有呼叫路徑下無 `finally` 可跳過，唯一有 `try/finally` 包住巢狀呼叫處（`tools/windows_smoke_local.ps1` 的 `Test-InstallRoundtrip`）在 python 缺失情境下會更早失敗、不可能活著跑到此分支，現況無害（SD 二審探針驗證） | P3/P4（六項皆 advisory 或需額外測試基礎建設，不影響本輪功能正確性；四方二審 Architect/SA/SD/QA 對本輪修復本身皆已 APPROVE，此六項純屬「記錄以供下輪追蹤」而非本輪必修） | 排入下一輪跨平台複審視情況處理（①③可能為文件/測試補強、②⑤可考慮加 `\b` 邊界或跨檔字面鎖測試、④⑥為觀察性記事非必修） | open watch（六項發現分別於 R23 四方一審 QA〔①②③④〕與二審 SD〔⑤⑥〕實測記錄，皆非阻斷、不影響本輪四方 APPROVE 結論；本列存在目的是避免審查產出隨裁決收斂而遺失，供下一輪 Windows/跨平台複審排查）。**R25 全面掃描逐項覆核並收斂 4/6 項（不再無謂延後）**：**①fixed@R25**——`tools/check_ntfs_paths.py` 已知侷限段補一句說明 U+FFFD 替代字元僅影響訊息可讀性、不影響偵測本身。**②fixed@R25**——`tools/tests/test_dev_start.py::TestWindowsHeartbeatFailSentinel::test_ps1_literal_end_exit_decision_lines_present_and_matched` 新增跨檔字面鎖（鏡子自證模式，先斷言 `.ps1` 字面量存在再驗證正則命中）。**③fixed@R25**——`tools/tests/test_workflow_permission_concurrency_lock.py` 新增 `TestCompatCiConcurrencyLock`（6 tests，windows/macos compat CI 各 3 個 job 層 concurrency 區塊皆納管；經手動 bug-injection〔修復後暫時改壞字面值重跑再還原〕確認皆有真實鑑別力，測試檔本身未內建自動化負向案例——四方一審 SA 指出原措辭易誤讀為「檔內含自動化 bug-injection 案例」，此處訂正用詞）。**⑤fixed@R25**——`_WINDOWS_EXIT_DECISION_RE` 加 `\b` 單字邊界（`tools/dev_start.py`），新增 `test_word_boundary_prevents_false_positive_on_end_suffixed_word` 回歸鎖；四方一審 SA 指出 `\b` 對「連字號結尾單字」（`high-end`/`front-end`，連字號非單字字元）仍會誤判，四方二審 SA 進一步指出此修法成本低（改用負向後顧 `(?<![\w-])` 即可一併關閉，不影響既有兩處真實命中）、依「不要無謂延後」紀律應當場補——**已於二審後追加修復**：`_WINDOWS_EXIT_DECISION_RE` 由 `\b` 改為 `(?<![\w-])`，新增 `test_hyphenated_end_word_prevents_false_positive` 回歸鎖，bug-injection（還原為 `\b`）確認新測試會變紅。**過程記事（誠實揭露）**：本次 bug-injection 驗證後誤用 `git checkout -- tools/dev_start.py` 還原，把該檔尚未 commit 的 R25 全部修改（含 `\b`／負向後顧兩版修復與所有註解）一併抹回 R24 committed 狀態（[[git-checkout-mutation-revert-hazard]] 已記錄過的已知陷阱再次發生）；已用 Edit 逐段重建修復內容並重跑全套（根層 unittest 359 tests OK, skipped=10）確認與意圖一致，無殘留遺漏。**④維持 backlog（R25 Scan-A 複核，非無謂延後）**：修法需新設計判斷（如改看 `CommandOrigin`/呼叫棧深度）並驗證無新假陽性/假陰性，屬需要人工決策的變更，全庫仍零實例、純理論性，維持觀察。**⑥reclassify（R25 Scan-A 複核，非未修缺陷）**：親讀兩支生產呼叫端（`AutoClaude/tools/install_git_hooks.ps1:27`、`AISDLC_SDD/scripts/install-hooks.ps1:13`）確認 dot-source 位置前皆無 `try` 包裹，現況已查證無害，非「backlog 待修」，狀態訂正為「已查證無害、觀察性記事」，不再列為待修項目。**四方一審訂正**：Architect 指出 `test_ps1_literal_end_exit_decision_lines_present_and_matched` 讀 `run_local_nightly.ps1` 原用 `encoding="utf-8"`，與同檔既有讀取同檔的 `test_windows_reader_filename_matches_ps1_writer` 慣例（`utf-8-sig`，因該檔含 BOM）不一致，已訂正為 `utf-8-sig`（Rule 11 比照既有慣例；功能上無影響，純風格一致性）。**四方一審 SA/SD/QA 三方各自獨立回報 `run_root_unittests.py`/`test_dev_start.py` 偶發假紅（`_WINDOWS_EXIT_DECISION_RE` 誤判「backend」）**：主控親自調查——反組譯確認 `tools/__pycache__/dev_start.cpython-311.pyc` 已正確內含 `\b` 修復字面量、靜態驗證 `\bend` 對 "backend" 不匹配、連續 52 次獨立 concurrent repro（cold-cache + warm-cache 混合，最高 8-way 並行 × 多輪）皆 0 失敗，排除 pyc 寫入競態與正則邏輯本身的持續性缺陷。**根因判定**：本輪一審四位審查員（Architect/SA/SD/QA）以單一訊息並行派出，SD／QA 在審查過程中各自對 `tools/dev_start.py`／`windows-compat-ci.yml` 做暫時性 bug-injection（改壞後驗證再還原），與同時段其他審查員的全套回歸執行產生時間窗重疊——符合既有已知模式 [[parallel-mutation-audit-collision]]（並行對同一主樹做突變會與其他並行讀取者互踩，產生「像 flaky 但其實是真實暫時狀態」的假紅），而非 `\b` 修復本身或新測試邏輯的缺陷。**方法論記事**：四方審查若涉及 bug-injection 需暫時改動共用主樹檔案，日後應序列化執行或改用 worktree 隔離，避免審查階段本身重演此已知陷阱。**R27 訂正④（見 DEF-101-272）**：原判定「全庫零命中、純理論性」被本輪主控手動重跑 `windows_smoke_local.ps1`（非 `-File` 頂層呼叫方式）實測推翻——`& $installer` 同行程巢狀呼叫一樣會觸發同款誤判，不需要真的用 `pwsh -Command ". ./realscript.ps1"` 逐字寫法；已修復並補回歸測試，狀態由 open watch 改為 fixed@R27，詳見 DEF-101-272。 |
| DEF-101-269 | 2026-07-23 | R26 全面掃描 Scan-C（CI/排程基建維度） | **`windows-compat-ci.yml` 從未在真實 GitHub Actions 執行 `tools/install_windows_nightly.ps1`（即使是安全的 `-WhatIf` 預覽）**：對照 `macos-compat-ci.yml` 第 253-256 行，`macos-smoke` job 在真正的 `macos-latest` runner 上會執行 `bash tools/install_mac_nightly.sh --render-only ...` + `plutil -lint`；但 `windows-compat-ci.yml` 的 `windows-smoke`／`windows-nightly-full` 兩個 job 皆無任何一步呼叫 `install_windows_nightly.ps1`，唯一會執行它（`-WhatIf` 預覽）的地方只有開發者手動本地跑的 `tools/windows_smoke_local.ps1` [8/8]——沒人手動跑就永遠不會被驗證，形成「文件宣稱雙平台對稱、CI 自動化實際不對稱」的落差 | P2（既有大量結構驗證與真機 settings-construction 測試皆非「真實 GitHub Actions windows-latest runner 自動觸發」這層，與本 repo「compensating control 不能取代真正閘門」一貫紀律不符） | 比照 `windows_smoke_local.ps1` [8/8] 既有寫法，在 `windows-smoke` job 新增一步以 `-WhatIf` 呼叫該腳本並斷言輸出含 `Register-ScheduledTask` | fixed@R26：`.github/workflows/windows-compat-ci.yml` 於 `tools/run_root_unittests.py` 步驟後新增「install_windows_nightly.ps1 -WhatIf 預覽」步驟，以獨立子行程 `powershell.exe -File ... -WhatIf` 呼叫並 `2>&1` 合流捕捉輸出（子行程 ShouldProcess 訊息屬原生 stdout），斷言 `$LASTEXITCODE -eq 0` 且輸出含 `Register-ScheduledTask`（逐字鏡射 `tools/windows_smoke_local.ps1` [8/8] 既有驗證手法）；YAML 經 `yaml.safe_load` 語法驗證通過；`tools/tests/test_pre_push_dispatcher.py`／`test_smoke_ci_sync.py`／`test_workflow_permission_concurrency_lock.py` 三份既有引用該檔內容的測試複跑 20 tests 全綠、無回歸（新增步驟未改動既有 job/step 結構，僅插入一步）。**🔴 一審 QA bug-injection 揭露鑑別力侷限（誠實記載，非阻斷）**：QA 一審實測將 `install_windows_nightly.ps1` 真正的 `Register-ScheduledTask` cmdlet 呼叫整段刪除、只留 `$PSCmdlet.ShouldProcess($TaskName, 'Register-ScheduledTask')` 字串字面量，重跑 `-WhatIf` 後 CI 新步驟的斷言**依然通過**（PowerShell `ShouldProcess()` 的 "What if:" 訊息只是印出呼叫時傳入的字串參數，與是否真的執行對應 cmdlet 無關）。故此步驟只驗證「`-WhatIf` 預覽路徑的字面量沒被改壞」，**不驗證「非 WhatIf 的真實安裝邏輯」本身是否正確**——若未來只有第 146 行真實呼叫被改壞（例如打錯參數）而第 141 行字串未動，此 CI 步驟會誤判通過。此為「從零覆蓋提升至部分覆蓋」的淨改善，而非「完整覆蓋」，記載於此避免未來輪次高估其鑑別力（呼應 SA 一審獨立指出的「驗證強度弱於 macOS 側 `plutil -lint`」同一類觀察，QA 以實際 bug-injection 坐實）。 |
| DEF-101-270 | 2026-07-23 | R26 全面掃描 Scan-D（文件/帳本一致性維度） | **`AutoClaude/docs/06_quality/Nightly_Forensic_Discipline.md` 版本元資料落後於實際編修**：該檔案「文檔元數據」欄（第 202 行）與紀律 #14（第 100 行）在 R22（commit `0053f2a`）確實被修改過（訂正一段對 log 消失原因的誤導措辭，R22 commit 訊息自陳「訂正 Nightly_Forensic_Discipline.md 誤導措辭」），但這次編修沒有同步遞增版本號（仍是 v1.9）也沒有更新「最後更新」日期，該檔自身 v1.5~v1.9 逐版標註對應輪次與日期的慣例在這次被跳過；另外 `AutoClaude/CLAUDE.md:260` 引用此檔版本號時仍寫 v1.8，比檔案本身實際的 v1.9 又更落後一版 | P4（純文件元資料落差，不影響內容正確性，紀律條文本身內容無誤） | 補一筆 v1.10 版本紀錄追溯記載 R22 這次措辭訂正並更新「最後更新」日期；同步訂正 `AutoClaude/CLAUDE.md` 的版本號引用 | fixed@R26：`Nightly_Forensic_Discipline.md` 文檔元數據新增 v1.10 條目追溯記載 R22 措辭訂正、「最後更新」日期改為 2026-07-23；`AutoClaude/CLAUDE.md:260` 版本號引用由 v1.8 訂正為 v1.10（該檔為 CI `claude-md-budget` job 機械鎖住的 400 行上限、修改前後皆仍為 400 行，未破線）；`AutoClaude/tools/snapshot_sync.py --check` 複跑確認 Architecture Snapshot 區段與 sprint 骨架仍對齊一致 |
| DEF-101-271 | 2026-07-23 | R26 Architect 架構專項檢視 Part A6（本輪使用者明確要求之架構最佳化評估附帶挖出） | **LOC 分級治理範圍未涵蓋「薄殼化」收斂後產生的根層 `tools/` Python 核心檔**：`AutoClaude/tools/check_loc_budget.py:91` 的 `SCAN_ROOT` 寫死為 `"autoclaude"`，只掃 `AutoClaude/autoclaude/` 套件本身；R12/R16 已將雙原生腳本（`.sh`/`.ps1`）大幅收斂為「薄殼＋Python 核心」架構（`tools/check_wrapper_thinness.py` 機械鎖住 10 支薄殼），但收斂出的 Python 核心檔（`tools/dev_start.py` 現已 **1772 行**、`tools/bootstrap_core.py` 348 行、`tools/integration_gate_core.py`）完全不在任何 LOC 分級政策（`service ≤500`／絕對紅線 `≤750`）管轄範圍內——複雜度沒有消失，只是從「跨平台橫向重複」搬成「單檔案垂直堆疊」，且無機械閘門守護這個新檔案類別的成長上限 | P4（非緊急 bug：`dev_start.py` 目前 56 個 top-level 函式/類別、平均每個約 30 行，仍屬模組化，非失控義大利麵；四支既有機械治理工具本輪實測皆 exit 0，無任何測試失敗或功能錯誤，純粹是治理範圍縫隙） | 擴充 `check_loc_budget.py` 的 `SCAN_ROOT`（或另建輕量版）涵蓋根層 `tools/` 下經薄殼化收斂出的核心檔，並針對「這類檔案該設多寬的上限」（例如比照 `service` 500、或另訂更寬鬆的 `orchestrator` 級 1500~2000）— 屬需要人工拍板分級門檻的設計決策，不當場動 | open watch（R26 Architect 架構專項檢視發現，記事存證；不影響本輪任何機械閘門判準，四支既有治理工具本輪複跑皆綠；排入下一輪 C 軌候選，待人工決定新分級門檻後落地）。**R26 一審 Architect 追加具體觸發門檻（避免變成無限期開放式延後）**：`tools/dev_start.py` 現況 1772 行，**若未來任一輪複審實測其超過 2000 行，即升級為該輪必修**（須完成 `check_loc_budget.py` `SCAN_ROOT` 擴充與新分級門檻拍板落地，不得再以「需人工決策」為由延後）；在達到此門檻前維持 open watch 觀察 |
| DEF-101-274 | 2026-07-23 | R27 Architect 架構專項檢視（跨平台架構最佳化評估延續） | **`AutoClaude/tools/check_loc_budget.py` 的 `SCAN_ROOT` 掃描範圍縫隙（DEF-101-271）本身缺乏機械後盾**：DEF-101-271 訂出「`tools/dev_start.py` 超過 2000 行即升級為該輪必修」的具體觸發門檻，但該門檻的偵測完全依賴審查員每輪手動 `wc -l`——若某輪遺漏比對或 `dev_start.py` 在輪次之間悄悄膨脹，沒有任何機械閘門會攔下，等同「用人工紀律守護人工紀律」，與本倉其餘一切皆有機械 gate 的風格不一致（R27 實測 `dev_start.py` 現況仍為 1772 行、零成長，尚未觸發必修門檻） | P3（本輪未觸發必修，`dev_start.py` 距 2000 行門檻尚有 228 行餘裕；純屬治理機制本身的健壯性縫隙，非緊急功能缺陷） | 將 `SCAN_ROOT` 擴為清單或另建輕量掃描涵蓋根層 `tools/`／`AutoClaude/tools/`，給薄殼化後的 Python 核心檔訂一個 orchestrator 級上限（Architect 建議 1500，使 `dev_start.py` 現值 1772 立即顯性化為既有超標、需拆），使 2000 門檻自動可執行而非靠記憶；`AutoClaude/tools/ab_compare_backends.py`（720 行）距絕對紅線 750 僅 30 行，納管後可提早預警 | open watch（R27 Architect 架構專項檢視發現，記事存證；不影響本輪任何機械閘門判準；屬需要人工拍板新分級門檻的設計決策，與 DEF-101-271 同批候選，排入下一輪 C 軌一併處理）。**四方一審 Architect 補充**：擬議的 orchestrator 級門檻（2000 行，或建議的 1500）與本倉既有 `ABSOLUTE_LIMIT = 750`（`check_loc_budget.py:90`，任何層級不得超）落差達 2.7 倍，`dev_start.py` 現況 1772 行已是絕對紅線的 2.4 倍、只因在 `SCAN_ROOT` 掃描範圍外才「不算違規」；下一輪拍板新門檻時應一併說明為何「薄殼化收斂後的核心檔」可以豁免既有絕對紅線，而非讓兩套數字並存造成認知落差 |
| DEF-101-275 | 2026-07-23 | R27 四方一審 QA（對抗式驗證回歸數字時發現） | **`tools/tests/test_git_hooks_install_common.py` 的 `_usable_bash()` 對 bash 可用性驗證不夠嚴謹**：該函式只用 `bash -c "echo ok"` 驗證候選 bash 是否可用，但 Git for Windows 在某些安裝/PATH 組合下會選中只有 `bin/bash.exe`（缺 `usr/bin` 底下完整 coreutils）的精簡版 bash——這種 bash 能通過 `echo ok` 但呼叫 `dirname` 等 coreutils 命令會失敗（`dirname: command not found`），導致 `tools/lib/git_hooks_install_common.sh` 相關測試（如 `test_sh_interactive_style_failure_does_not_kill_shell`）在此類環境下產生與程式碼本身無關的假紅。QA 在獨立 git worktree 複審環境下親自重現：`python tools/run_root_unittests.py` 顯示 `359 tests` 但 `failures=1`（非本輪任何檔案改動所致，R26 在同一環境下應同樣失敗），本機主環境重跑則 359 tests 全綠（未踩到此 PATH 組合），確認屬環境相依假紅、非 R27 回歸 | P4（環境相依測試基礎建設縫隙，非生產缺陷；不影響本輪任何修復的正確性判定，且本機/CI 皆未觸發） | `_usable_bash()` 除 `echo ok` 外，加驗一項實際依賴的 coreutils 命令（如 `command -v dirname` 或直接嘗試呼叫 `dirname` 驗證非空輸出），确保挑中的候選具備完整可用的 bash 環境而非僅「能開機」；此邏輯依檔頭註記於三處鏡射（`tools/tests/test_git_hooks_install_common.py`、`tools/tests/test_pre_push_dispatcher.py`、`AISDLC_SDD/scripts/bash_probe.py`），修復需三處同步 | fixed@R32：連續 5 輪（R27~R31）未收斂，本輪 Architect 架構最佳化評估判定為結構性根因復發，改用共用資料規格 `tools/lib/bash_probe_spec.py`（`PROBE_CMD` 由 `echo ok` 改為 `echo`+`dirname` 兩段 coreutils 探測，三處消費者各自 import 取得同一份規則資料但驗活執行邏輯仍獨立寫死，維持三份回歸鎖彼此獨立鑑別力）三處（`bash_probe.py`／`test_pre_push_dispatcher.py`／`test_git_hooks_install_common.py`）同步落地。R32 一審 SA/QA 交叉獨立發現三處既有測試對 `PROBE_CMD` 全用 `mock.patch.object(subprocess, "run")` 手填回傳值、從未驗證其字面是否真的依賴 coreutils（裝飾性斷言——SA/QA 各自把 `PROBE_CMD` 改回 echo-only 過渡態，既有 43~51 個 case 全數維持綠燈），已補 `tools/tests/test_bash_probe_spec_contract.py`（5 case，不 mock subprocess，含資料層字面斷言＋真實 subprocess 行為＋`usable_bash()` 端到端 wiring 三層防線）堵住，經 bug-injection 證實 3/5 新測試對此退化有鑑別力。四方一審/二審皆 APPROVE，詳見下方「R32 四方複審裁決總結」 |
| DEF-101-276 | 2026-07-23 | R27 收尾階段最終 `AISDLC_SDD ci-gate.sh` 全套重跑（四方複審已結束後、準備提交前） | **`macos-compat-ci.yml` 的 paths 過濾清單漏收新增測試檔案 `tools/tests/test_bootstrap_ps1.py` 直接消費的根層腳本 `tools/bootstrap.ps1`**：`scripts/tests/test_ci_paths_cover_root_consumers.py::test_all_root_consumers_covered_by_ci_paths[macos-compat-ci.yml]` 由綠轉紅——本輪新增的 `test_bootstrap_ps1.py` 以 `REPO_ROOT / "tools" / "bootstrap.ps1"` 直接引用該 Windows 專屬腳本，但此測試檔同活在 `windows-compat-ci.yml`／`macos-compat-ci.yml` 兩份 workflow 皆會執行的 `tools/tests/` 目錄下（macOS runner 預裝 `pwsh`，測試不會被 `@unittest.skipIf` 跳過），`macos-compat-ci.yml` 的 paths 清單卻缺這一項——若未來只改 `tools/bootstrap.ps1` 本身，macOS 側這個回歸鎖不會被觸發（DEF-101-042 同構假綠盲區，此為該機械鎖第四次於本 repo 抓到同款缺陷、且首次由**新增測試檔本身**觸發而非由改動根層腳本觸發） | P2（機械鎖已攔下，未進入 CI 才被真人發現；但反映本輪四方複審範圍未涵蓋「新增測試檔對 CI paths 覆蓋契約的連帶影響」這個治理面向） | `macos-compat-ci.yml` 的 push／pull_request 兩份 paths 清單皆補上 `tools/bootstrap.ps1`，比照既有 `tools/install_windows_nightly.ps1` 等三支 .ps1 顯式列舉慣例 | fixed@R27：`.github/workflows/macos-compat-ci.yml` push/PR 兩處新增 `tools/bootstrap.ps1`；`test_ci_paths_cover_root_consumers.py` 10 tests 全綠，`AISDLC_SDD ci-gate.sh` 雙軌 1478+1673+174 回到全綠。**教訓（記事存證）**：下一輪若再新增消費根層檔案的測試，應在該輪收尾前主動重跑 `test_ci_paths_cover_root_consumers.py`，而非等最終 ci-gate 全套重跑才發現 |
| DEF-101-277 | 2026-07-23 | R28 全面掃描（使用者要求全面驗證 Mac/Windows 11 相容性架構）Architect 架構複審交叉比對程式碼現況時揪出 | **`ONBOARDING.md` §6「雙腳本對等機械守護」段落描述與程式碼現況不同步**：該段開頭原文宣稱「上表三對 `.sh`/`.ps1`（bootstrap／integration_gate／run_act）的 step 標籤清單由 `tools/check_script_parity.py`...機械比對」，但實際上這三對已於 R16（Architect 建議 B）收斂為「Python 單核心＋雙薄殼」（`tools/bootstrap.sh`／`.ps1` 已改為僅轉呼叫 `tools/bootstrap_core.py`；`integration_gate`／`AutoClaude/tools/run_act` 同構），`check_script_parity.py` 原始碼 docstring 本身也明載「R16...三對業務邏輯漂移面同樣物理消滅，改由 `check_wrapper_thinness.py` hash 釘選守門，`_MARKER_PAIRS` 標籤比對隨之退場——`_MARKER_PAIRS` 目前為空清單」。本輪派出的 Architect 子 agent 依 ONBOARDING.md 文字判斷，誤將此列為「待收斂的架構債」重新提案；主控親自讀取 `bootstrap.sh`／`.ps1`／`run_act.sh` 及 `check_script_parity.py`／`check_wrapper_thinness.py` 原始碼後證實該建議已完全落地，唯獨 ONBOARDING.md 文字未同步更新，屬純文件漂移（未影響任何機械閘門判準——`check_wrapper_thinness.py`／`check_script_parity.py` 的 enrollment 完整性掃描本身皆正確、只是外部說明文件的敘述落後程式碼一輪以上） | P3（文件與現況不一致會誤導後續審查者重複提案已完成的工作、浪費審查資源；不影響任何測試/CI 判準） | 訂正 `ONBOARDING.md` §6 該段文字，明確列出 bootstrap／integration_gate／run_act／local_ci_gate 四對皆已收斂為薄殼＋Python 核心、由 thinness hash 釘選守門，`_MARKER_PAIRS` 對這四對已退場 | fixed@R28：`ONBOARDING.md` §6 對應段落已改寫為與 `check_script_parity.py` docstring 一致的現況描述；未新增/變更任何程式邏輯或機械閘門，純文件訂正 |
| DEF-101-278 | 2026-07-23 | R28 四方一審 Architect（複核 DEF-101-277 修復完整性時延伸檢查） | **ONBOARDING.md §6.1 `root-infra-ci.yml` 條目第 6 項與 `.github/workflows/root-infra-ci.yml:25` 檔頭註解，皆仍用「腳本對等閘：成對 `.sh`/`.ps1` 的 step 標籤」概括描述 `check_script_parity.py`**——與 DEF-101-277 訂正的 §6 本體同款簡化措辭，但 Architect 複核後判定：此處是**兩處彼此一致**的既有精簡摘要（ONBOARDING 本行明文「詳細內容以 workflow 檔頭註解為準」，刻意不重複展開機制細節以避免雙處同步負擔），非本輪新引入的漂移，且 `check_script_parity.py` 整體仍保留 step 標籤比對機制本身（僅 bootstrap/integration_gate/run_act 四對目前改掛 thinness hash、`_MARKER_PAIRS` 對這幾對是空清單，機制本身未被移除），故此摘要句並非錯誤陳述 | P4（記事存證；非本輪新漂移、非錯誤陳述，兩處一致且互為對照，不影響任何機械閘門判準） | 若下一輪要進一步精確化，可在此摘要句補一句「（現無任何 pair 掛此機制，見 §6 本體）」；非必要 | open watch（R28 四方一審 Architect 發現，記事存證；Architect 本人判定優先度低於 DEF-101-277、不必立即修，僅供未來若再擴充此段時參考） |
| DEF-101-279 | 2026-07-23 | R29 全面掃描（根層基建範圍）親自 PowerShell 實機重現揪出 | **`tools/bootstrap.ps1` 第 17-34 行（修復前行號）`python3` 候選分支缺少與 `python` 候選同款的 WindowsApps 空殼排除 guard**：R27（DEF-101-273）已替 `python` 候選補上 `-notlike '*\WindowsApps\*'` 排除，但 `elseif` 分支的 `python3` 候選當時沒同步補——全新 Windows 11 機器上 `python.exe` 與 `python3.exe` 常常都是系統自動註冊的 App execution alias 空殼；當使用者未裝 `py` launcher、也未裝真正 Python，但兩個空殼皆在 PATH 上時，舊邏輯會選中 `python3` 空殼並嘗試呼叫它跑 `bootstrap_core.py`，重現與 DEF-101-273 相同的失敗模式（跳出 Microsoft Store 提示、永遠不會真正整備環境），只是換了個分支重現。已用實際 PowerShell 執行重現確認（非僅讀碼推論） | P2（全新機器 onboarding 入口的環境整備會靜默失敗，且是同一缺陷類型第二次以不同分支現形） | `python3` 候選比照 `python` 候選補上同款 `-notlike '*\WindowsApps\*'` guard | fixed@R29：`tools/bootstrap.ps1:34-42`（修復後行號；`python3` 候選補齊 WindowsApps 排除 guard，並補一句註解說明）；新增回歸測試 `tools/tests/test_bootstrap_ps1.py::test_windowsapps_python_and_python3_stubs_both_skipped_and_reports_not_found`（修復前用 `.cmd` 假直譯器證實測試為紅——`FAKE_PYTHON3_STUB_INVOKED` 真的會被印出；修復後轉綠）；連帶更新 `tools/check_wrapper_thinness.py` 的 `tools/bootstrap.ps1` 正規化內容 hash 釘選（`d82f5aba…`→`0aaaf64e…`，wrapper 內容變動但仍屬「選直譯器」薄殼職責範圍內）；`python -m unittest discover -s tools/tests -p "test_*.py" -v` 全套 362 tests 全綠（10 skipped，與本次修改無關） |
| DEF-101-280 | 2026-07-23 | R30 全面掃描（根層基建範圍）派出的稽核 agent 掃描 ONBOARDING.md 與程式碼現況交叉比對揪出，主控親自核實 | **`ONBOARDING.md:148`「雙腳本對等機械守護」段落自我矛盾**：同一段落前半明確宣稱「`local_ci_gate` 對…兩薄殼改由 thinness hash 釘選守門〔與 parity 登記另有鍵集合交叉鎖〕，**與 dev_start 同類**」，但段落後半又說「`dev_start` 對不在上述任一類…兩薄殼仍各有直譯器選擇／venv 啟用等樣板（**無標籤錨點、跨側暫無機械比對**），改任一邊須人工同步另一邊」——前後兩句直接互相矛盾。親自核實程式碼：`tools/check_wrapper_thinness.py` 的 `_PINNED_SHA256` 字典**第一組**就是 `tools/dev_start.sh`／`.ps1`（依該檔 docstring，`dev_start` 其實是本 repo「薄殼＋Python 核心」模式的**原始範例**，早於 local_ci_gate 等後續收斂案）；`tools/check_script_parity.py` 的 `_THINNESS_ENROLLED` 集合亦明確包含 `"tools/dev_start"`，並有 `_check_thinness_cross_lock()` 機械斷言其與 `_PINNED_SHA256` 鍵集合一致（實跑印出「✅ thinness 交叉鎖：5 對薄殼登記…一致」，5 對＝dev_start＋local_ci_gate＋bootstrap＋integration_gate＋run_act）。故後半句「無標籤錨點、跨側暫無機械比對」與程式碼現況不符，屬與 DEF-101-277 同類的文件描述落後程式碼問題，唯獨那輪未修到這句 | P3（文件自相矛盾會誤導審查者以為 dev_start 缺乏機械防護、需額外投資，浪費審查資源；不影響任何機械閘門判準，`check_wrapper_thinness.py`／`check_script_parity.py` 本身皆正確運作） | 訂正 `ONBOARDING.md:148` 該句，明確指出 dev_start 對同樣由 thinness hash 釘選守門（且是原始範例），僅 `_MARKER_PAIRS` 標籤比對不適用（與前段已退場範圍一致） | fixed@R30：`ONBOARDING.md:148` 該句已改寫為與 `check_wrapper_thinness.py`／`check_script_parity.py` 程式碼現況一致的描述；未新增/變更任何程式邏輯或機械閘門，純文件訂正 |
| DEF-101-281 | 2026-07-24 | R31 全面掃描（Scan-B Python 跨平台維度，使用者要求 Architect 全面檢視多平台架構最佳化） | **`tools/bootstrap_core.py::pick_python()` 缺少 WindowsApps 空殼別名排除 guard，與 `tools/bootstrap.ps1` 不對稱**：R27（DEF-101-273）／R29（DEF-101-279）連續兩輪替 `bootstrap.ps1` 的 `python`/`python3` 候選補上 `-notlike '*\WindowsApps\*'` 靜態路徑排除 guard（全新 Windows 11 機器上這兩個名字常是系統自動註冊的 App Execution Alias 空殼，`shutil.which()`/`Get-Command` 找得到、實際執行只跳出 Microsoft Store 提示），但作為單一真相源的 `bootstrap_core.py::pick_python()`（R16 架構收斂後真正決定建立 `.venv` 用哪個直譯器的核心邏輯）完全沒有這道 guard，只靠 `_probe_ok()` 執行結果判斷（此手法對空殼別名不可靠，正是 `.ps1` 改用靜態路徑比對的原因） | P2（全新機器 onboarding 環境整備可能靜默失敗，且是同一缺陷類型第三次以不同載體現形） | `pick_python()` 對 Windows 下裸名候選 `"python"`/`"python3"` 比照 `.ps1` 邏輯補上 `PureWindowsPath` 逐段比對排除 WindowsApps（`py` launcher 候選不受影響） | fixed@R31：新增 `_is_windows_apps_stub()` helper + `pick_python()` 迴圈補 guard；新增 `tools/tests/test_bootstrap_core.py`（8 case，含 bug-injection 正反驗證）。四方一審/二審（Architect/SA/SD/QA）皆 APPROVE，SD 二次獨立 bug-injection（guard 順序退化）複驗仍有效 |
| DEF-101-282 | 2026-07-24 | R31 全面掃描（Scan-B） | **`AISDLC_SDD/scripts/bash_probe.py::usable_bash()` 與其在根層 `tools/tests/test_git_hooks_install_common.py`／`test_pre_push_dispatcher.py` 內鏡射的兩份獨立副本，System32 排除沿用已在 `tools/integration_gate_core.py::_has_system32_segment()`（DEF-101-236）判定為缺陷的寬鬆寫法** `"system32" not in bare.replace("/", "\\").lower()`（任意子字串命中即排除，會誤傷路徑含 "system32" 子字串但非該目錄段的合法候選，如 `C:\MySystem32Tools\bash.exe`）：DEF-101-236 的修復（改用 `PureWindowsPath` 逐段精確比對）當時只套用到 `integration_gate_core.py`，未傳播到這三份獨立實作（本 repo 對「子專案間刻意不跨 import、各自獨立實作＋人工同步」政策的已知代價） | P3（三份獨立副本皆為測試基礎設施函式，非 production 路徑；QA 一審親驗證實此前退化在 `tools/tests/` 兩份鏡射副本完全不會被任何既有測試抓到） | 三處皆改用 `PureWindowsPath` 逐段精確比對，對齊 `_has_system32_segment()` 語意 | fixed@R31：`bash_probe.py` 新增 `_has_system32_segment()` + 新增 `AISDLC_SDD/scripts/tests/test_bash_probe.py`（6 case）；兩份 `tools/tests/*.py` 鏡射副本同步修復，並各補 `TestUsableBashSystem32Guard`（QA 一審必修條件 2：此前這兩處鏡射副本退化不會被任何測試抓到，已用 bug-injection 證實新測試有鑑別力）。四方一審/二審皆 APPROVE |
| DEF-101-283 | 2026-07-24 | R31 全面掃描（Scan-D 文件/帳本一致性維度） | **`AutoClaude/CLAUDE.md:325` footer 版本沿革落後於 R26 實際內容編修**：R26（commit 08ee85c）已將本檔「Nightly 完整版連結」由 v1.8 訂正為 v1.10（實質內容變更），但同一 commit 未同步更新 footer 的「最後更新」日期（仍留 2026-07-22）與版本沿革（未新增條目記載這次變更）——修復「版本元資料落後」問題的那次 commit，自己也產生了同款落後 | P3（純文件性，不影響任何機械閘門判準；`claude_md_freshness.py` hook 只查行數上限與 snapshot drift，不查 footer 日期本身） | footer 日期改為 2026-07-23，版本沿革補記 R26 這次 Nightly 連結版本號訂正 | fixed@R31：`AutoClaude/CLAUDE.md` footer 更新為 v7.12（日期 2026-07-23、補記 R26 漏同步事由）；改寫後確認仍在行數上限（400 行）與單行 codepoint 上限（771 < 800）內，`test_claude_md_no_long_lines.py`／`test_claude_md_freshness.py` 全綠。四方一審/二審皆 APPROVE |
| DEF-101-284 | 2026-07-24 | R31 Architect 架構深度評估（使用者要求 Part A 架構最佳化評估為重點） | **Windows／macOS 兩支 nightly 核心腳本的靜態行為回歸測試嚴謹度不對稱**：Windows 側 `AutoClaude/tools/run_local_nightly.ps1` 有 `test_run_local_nightly_static.py`（25 個結構化 pytest case，含負向測試）；mac 側 `run_local_nightly.sh` 同款行為此前只在 `tools/macos_smoke_local.sh` [7/7] 以粗粒度 grep 驗證 5 個錨點字串（打包在 shell smoke 腳本當一個 pass/fail 項），無 pytest 靜態層級測試、無負向測試，降低 mac 側 nightly 契約走樣（尤其近幾輪才新增的 mkdir atomic lock／trigger 歸因欄位）的偵測靈敏度。Architect 判定：此為此前 30 輪收斂下「三塊地基（薄殼收斂／平台抽象層／機械防腐化工具分工）已穩定，不需大改架構」結論下**唯一**建議局部補強項，其餘四面向（雙原生腳本收斂、平台邊界抽象層、機械防腐化工具生態分工、CI/排程/hooks 三層防護分工）皆判定維持現狀 | P3（測試覆蓋縫隙，非功能缺陷；mac 側 `.sh` 刻意只做 4-stage 薄聚合器，7-stage 深度 stage 不移植是既有架構決策非本輪範圍） | 新增 pytest 靜態測試檔涵蓋 mac 側既有 5 個 smoke 錨點之外的 mkdir atomic lock、trigger 四態枚舉、終端 exit 語意、心跳三站點契約 | fixed@R31：新增 `AutoClaude/tests/tools/test_run_local_nightly_sh_static.py`（11 case）。QA 一審親驗發現其中 2 個「bug-injection」case 為裝飾性斷言（只對測試檔內手刻字串斷言，未真突變 `sh_content` 本體），必修後改為對真實 `sh_content` 文字替換模擬退化＋重跑共用判斷式 `_has_stale_lock_liveness_check()`／`_rotation_pattern_excludes_heartbeat()`（後者改用 `fnmatch` 真 glob 語意比對，修正原純字面子字串比對抓不到「`nightly_mac_*.log` 字面不含但 glob 語意會誤配 latest 心跳檔」這個真實退化的縫隙）。四方一審 QA APPROVE-with-conditions（此項 + DEF-101-282 鏡射副本測試缺口共 2 項必修條件）、Architect/SA/SD APPROVE；二審四方全數 APPROVE |
| DEF-101-285 | 2026-07-24 | R32 全面掃描 Scan-C（CI/排程/hooks 基建維度） | **`tools/tests/test_bootstrap_ps1.py` 兩支測試的 `skipIf` 只檢查 `powershell`/`pwsh` 執行檔是否存在，未檢查是否真的在 Windows 平台上跑**：這兩支測試依賴 Windows PATHEXT 機制（寫 `.cmd` 假直譯器讓 `Get-Command python3` 命中），但 `.cmd` 需要 `cmd.exe` 解譯，裝有 pwsh 的 macOS（`/opt/homebrew/bin/pwsh`）呼叫 `.cmd` 檔案會靜默無回應（無輸出、`$LASTEXITCODE` 為空），導致確定性失敗而非雜訊。Scan-C 親自在本機 macOS+pwsh 7.6.3 實跑 `pytest tools/tests/test_bootstrap_ps1.py -q` 重現 `1 failed, 2 passed` | P1（本地 pre-push root-infra leg 對任何根層檔變更皆會誤擋無關的 push；`macos-compat-ci.yml` 的 macos-smoke job 在 GitHub-hosted macOS runner〔預裝 pwsh〕上一旦目前的帳務停擺問題解決即會確定性失敗，且會被誤判為新回歸） | `skipIf`/`skipUnless` 改為同時要求 `sys.platform.startswith("win")` | fixed@R32：新增 `_windows_pwsh_available()` 判準函式（`tools/tests/test_bootstrap_ps1.py:22-34`），兩支受影響測試改用 `@unittest.skipUnless(_windows_pwsh_available(), ...)`；本機重跑 `1 passed, 2 skipped`（原 1 failed）；`tools/tests/` 全套 377→386 passed（含後續 R32 其他新增測試）無回歸。四方一審 Architect 一度誤判此修復未接上（親跑重現原始 fail），二審複核判定為並行 bug-injection 互相污染的暫態雜訊（Architect/SD 皆獨立佐證），非真實回歸；四方一審/二審皆 APPROVE |
| DEF-101-286 | 2026-07-24 | R32 全面掃描 Scan-B（Python 跨平台維度） | **`AutoClaude/tools/run_act_core.py`（232 行，含 Windows winget 路徑 glob 偵測、`_gh_act_extension_installed()`、`ensure_images()`、`main()` 六步驟編排）完全零自動化測試覆蓋**：全庫 `grep -rl` 搜尋 `test_*.py` 對 `run_act_core\|resolve_act\|ensure_images\|_gh_act_extension_installed` 零命中；四個「Python 核心＋雙殼」收斂案（`bootstrap_core.py`／`integration_gate_core.py`／`local_ci_gate.py`／`run_act_core.py`）中唯獨此檔沒有對應測試，唯一「保護」是 `check_wrapper_thinness.py`／`check_script_parity.py` 對薄殼文字內容的 hash 釘選，完全不觸及核心邏輯本體 | P2（若 Windows winget 分支邏輯發生迴歸，現有 CI/pytest 全綠也偵測不到） | 比照 `test_bootstrap_core.py`/`test_find_git_bash_parity.py` 先例補一支測試，mock `shutil.which`/`subprocess.run`，涵蓋 `resolve_act()` winget glob 分支、`_gh_act_extension_installed()` 等 | fixed@R32：新增 `AutoClaude/tests/tools/test_run_act_core.py`（28 case，涵蓋 `resolve_act()`／`_gh_act_extension_installed()`／`ensure_images()`／`check_docker()`／`image_ready()`／`run_act()`／`main()` 全部關鍵分支），全數用 mock 隔離真實子行程/檔案系統呼叫。四方一審 SD／二審 SA 皆各自對此檔做獨立 bug-injection（`sorted()` 反轉、`gh-act` 判斷反轉、成功訊息字串改動等）證實有鑑別力；四方一審/二審皆 APPROVE |
| DEF-101-287 | 2026-07-24 | R32 全面掃描 Scan-B | **`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 的 `_KNOWN_SUBPROCESS_ONLY_CONSUMERS` 字典裡 `tools/bootstrap_core.py` 項已過期**：R31（DEF-101-281）新增 `tools/tests/test_bootstrap_core.py` 已直接 `import bootstrap_core`，該檔已被一般 import 掃描器 `_scan_import_consumed_paths()` 自動偵測涵蓋，不再屬於「執行期子行程消費、掃描器看不到」的盲區 B；與同批次的 `AutoClaude/tools/run_act_core.py`（仍屬盲區 B）不再對稱，屬與 DEF-101-283「修復自身即產生新落後」同款模式 | P3（不影響任何機械閘門判準，`test_known_subprocess_only_consumers_covered_by_ci_paths()` 本身仍 PASS，純粹是清單條目存在理由已過期） | 移除 `tools/bootstrap_core.py` 項，僅保留 `run_act_core.py`，註解訂正措辭 | fixed@R32：主控直接訂正 `_KNOWN_SUBPROCESS_ONLY_CONSUMERS`（移除過期項＋補充 R32 訂正說明註解），`test_ci_paths_cover_root_consumers.py` 10/10 passed |
| DEF-101-288 | 2026-07-24 | R32 全面掃描 Scan-D（文件/帳本一致性維度） | **`AutoClaude/CLAUDE.md:325` footer 日期再度落後一天**：footer 寫「最後更新 2026-07-23」，但 `git log -1 --format='%ad' -- AutoClaude/CLAUDE.md` 顯示該行所屬 commit（`009c5fe`，R31 用來修復 DEF-101-283 的那次 commit）實際日期為 2026-07-24——修復「footer 落後」問題的那次 commit，自己又產生同款落後，與帳本自述模式完全同構。修復過程中主控直接訂正日期＋精簡敘事時，一度不慎讓 line 325 增至 902 字元（超過 `test_claude_md_no_long_lines.py` 800 字元上限），觸發 `test_claude_md_no_long_lines.py`／`test_loc_budget_check.py::test_real_claude_md_under_limit` 兩支既有 contract test 失敗（自己造成的新回歸，非原始 Scan-D 發現範圍） | P3（純文件性，不影響任何機械閘門判準本身；行長回歸一度真實破壞既有 contract test，經即時發現修復） | footer 日期訂正為 2026-07-24；行長問題比照既有慣例（見 sprint_history.md R48/R53 同類先例）將較舊版本明細下沉至 `sprint_history.md §1.7.3` 指標，精簡 v7.9 段落 | fixed@R32：footer 更新為 v7.13（日期 2026-07-24、補記本次訂正事由），v7.9 明細下沉至歷史指標；最終行長 684 字元 < 800、全檔 400 行（達上限但未超），`test_claude_md_no_long_lines.py`／`test_loc_budget_check.py` 5 個相關測試全過 |
| DEF-101-289 | 2026-07-24 | R32 全面掃描 Scan-D | **`ONBOARDING.md` §7 pytest 基線唯一真相源字面數字已落後實測 +11（3,608→3,619，R27~R31 累積新增測試未回填）**：與 DEF-101-266（曾因 +1 落差記為 P4）同一根因、同一機制（無自動同步機制），僅這次落差擴大。R31 commit（`009c5fe`）本身的最終回歸驗證結果已寫明 3619 passed，但未同步觸碰 ONBOARDING.md | P3 | 重新量測並更新 §7 數字 | fixed@R33：R33 Scan-D 在**全新臨時目錄**建立乾淨 venv（`pip install -e '.[dev,notifications]'`，未裝 postgres/pgvector 選配，`pip list` 確認乾淨），巢狀 session 下實測 **3,644 passed / 210 skipped**；與 R32 污染值（3742/146）交叉比對 skip 數落差方向吻合污染假說，判定本次數字可信。`ONBOARDING.md` §7 已同步更新為此數字並訂正輪次標記為 R33（`bootstrap 出廠環境`〔非巢狀 session〕分項數字不在本次驗證範圍，維持原「待重驗」標示） |
| DEF-101-295 | 2026-07-24 | R33 Architect 架構深度評估；一審 QA 發現措辭誇大與真實子缺口，已一併修復 | **Windows 禁用檔名邏輯三處獨立實作（`tools/git-hooks/pre-commit` 的 `_ntfs_seg_bad()`、`tools/check_ntfs_paths.py:50-51`、`AutoClaude/autoclaude/utils/logger.py:80-81`）缺交叉一致性鎖**：禁用字元集合／保留裝置名正則兩個維度內容一致但無機械測試鎖住；`check_script_parity.py` 只管 `.sh`/`.ps1` 腳本對，不掃 `AutoClaude/` 下純 Python 生產模組。另确认 `logger.py` 因屬獨立可 pip 安裝套件（`autoclaude`，套件邊界限制——`AutoClaude/pyproject.toml` 無 `[tool.hatch.build]` 客製，預設只打包 `autoclaude/` 目錄，不含 monorepo 根層 `tools/`），不適合比照 `bash_probe_spec.py` 模式 import `tools/lib/*.py`（會讓純 pip 安裝情境下失效，一審 Architect 獨立驗證此因果性成立）。**一審 QA 額外揪出**：本列一審初版曾籠統宣稱「三處**目前內容一致（尚未漂移）**」，經 QA 實測發現**不實**——`logger.py` 原本完全沒有控制字元（`ord(ch)<0x20`／`0x7F`）淨化，與 bash／`check_ntfs_paths.py` 皆有的控制字元檢查不對稱，且此為具體、非罕見觸發情境（`task.step_id` 由 playbook 作者於 YAML 自訂，可能誤含 `\t`/`\x1b` 等 escape，與本檔案頭既有 R16 註解描述的原始問題同構）。**二審 QA 再揪出第二項真實子缺口**：`logger.py` 原用 `sanitized.rsplit(".", 1)[0]`（只切**最後一個**點）剝副檔名，`check_ntfs_paths.py`／bash 皆用「**第一個**點起」剝離（`split(".", 1)` / `${seg%%.*}`），對多重副檔名的保留名（如 `lpt5.tar.gz`）三者不對稱——`logger.py` 算出 `stem="lpt5.tar"` 不匹配保留名正則因而漏判，另兩處正確攔下。觸發情境同樣具體（playbook 作者以點號分層命名 step_id，如 `aux.setup.retry`，`aux`/`con` 為常見英文詞彙非罕見敵意輸入） | P3（架構層級交叉一致性缺口）＋ P3（QA 一審揪出的措辭誇大＋控制字元子缺口）＋ P3（QA 二審揪出的多重副檔名子缺口） | 局部補強（非根本重做，Architect 已論證合併效益與風險不成比例）：①新增交叉一致性測試鎖字元集合/保留名稱正則；②`logger.py` 補套件邊界 WHY 註解；③（QA 一審必修）補齊 `logger._sanitize_log_filename()` 控制字元淨化＋鎖此第三維度＋訂正本列措辭與行號；④（QA 二審必修）`logger.py` 副檔名剝離改 `split(".", 1)[0]` 與另兩處一致＋補多重副檔名保留名測試案例 | fixed@R33：新增 `tools/tests/test_windows_forbidden_filename_parity.py`（13 case，含控制字元交叉鎖＋多重副檔名保留名交叉鎖）；`logger.py` 補套件邊界 WHY 註解＋`_sanitize_log_filename()` 追加控制字元淨化（`ord(ch) < 0x20 or ord(ch) == 0x7F` 比照 `check_ntfs_paths.py` 判準）＋副檔名剝離由 `rsplit` 改 `split`；主控與 SD/QA 各自獨立 bug-injection（拿掉禁用字元／拿掉 `re.IGNORECASE`／拿掉保留名／拿掉控制字元淨化／改回 `rsplit`／竄改 bash 函式簽名令抽取失敗）皆證實有鑑別力且抽取失敗會 fail loud；四方一審 APPROVE/APPROVE-with-conditions（QA 必修已修復）、二審 SD/SA APPROVE，QA 二審再發現第二項必修並已當輪修復，詳見 DEF-101-297（bash 版 `\n` 控制字元既有狹窄限制，backlog） |
| DEF-101-296 | 2026-07-24 | R33 四方一審 Architect；二審 Architect 定位出可重現根因 | **`tools/tests/test_windows_forbidden_filename_parity.py` 在多支 `pytest` 行程並行對同一測試檔執行時，可重現間歇性 FAILED**（一審觀察到 1 次孤立假紅；二審改用「兩支 `pytest` 行程同時起跑同一測試檔」做 40 組對照重驗，**baseline 2/40 fail**，且不只 1 個測試方法——涉及 `test_bash_flags_every_char_in_python_forbidden_set`（1 次）、`test_python_regexes_agree_on_reserved_names`（連續 6 次，此測試**不涉 bash subprocess**、純 Python 靜態比對）、`test_bash_flags_every_reserved_name_python_flags`（連續 4 次），~17 次孤立重跑約 11 次失敗）。**根因高度疑似 `__pycache__` bytecode 快取並行寫入競態**：設 `PYTHONDONTWRITEBYTECODE=1` 後同款 40 組（80 次行程）**0/40 fail**；純序列孤立重跑 100+ 次全綠；純背景 CPU 負載對照組 40 次全綠——三組對照排除「bash 子行程時序」與「一般系統負載」，指向 bytecode 編譯/寫入競態。此條件（多位審查 agent 同時在同一 working tree 對同一測試檔跑 pytest）正是四方複審協定的常態操作，非罕見情境 | P3（不影響本輪其餘驗證結論；純序列執行下未見復發，非阻斷） | 四方複審時避免多位審查員同時對同一測試檔跑 `pytest`，或審查階段統一設 `PYTHONDONTWRITEBYTECODE=1` 環境變數規避競態；若未來仍在純序列執行下復現，才需要進一步用 `strace` 深入根因 | open（backlog，根因假說已有具體對照數據佐證、非阻斷 APPROVE；具體規避手法已記載供下一輪四方複審參考） |
| DEF-101-297 | 2026-07-24 | R33 修復 DEF-101-295 控制字元淨化時，主控親自撰寫交叉測試發現 | **`tools/git-hooks/pre-commit` 的 `_ntfs_seg_bad()` 偵測不到路徑段內嵌的 `\n`（0x0A）控制字元**：`printf '%s' "$p" \| LC_ALL=C grep -q '[[:cntrl:]]'` 逐行比對時，換行本身被 `printf` 當成實際換行輸出、成為 grep 的行分隔符而被「消耗」，不會出現在任一行的內容裡讓 `[[:cntrl:]]` 字元類別比對到；兩個 Python 版（`ord(ch) < 0x20` 逐字元迭代，見 `check_ntfs_paths.py:71`／`logger._sanitize_log_filename`）不受此限、皆能正確偵測。已用 `bash -c` 實測重現（`CLEAN` vs 預期 `BAD`） | P4（狹窄既有限制，非本輪引入；CI 端 `check_ntfs_paths.py`（root-infra-ci.yml 全變更觸發）仍會擋下含 `\n` 的路徑，非完全繞過，只是本機 `pre-commit` 少了一層即時攔截，非本輪範圍內改寫 bash 邏輯的理由——改寫需換一套非逐行比對的偵測法，屬另一輪範圍） | 如實記載，暫不修復（backlog）；`tools/tests/test_windows_forbidden_filename_parity.py` 的控制字元交叉鎖測試已明確排除 `\n` 並在程式碼註解說明原因，避免此已知限制被誤判為測試缺陷 | open（backlog，如實記載存證，CI 端防線仍在，不影響本輪其餘驗證結論） |
| DEF-101-291 | 2026-07-24 | R32 修復包 3（bash_probe 三鏡射資料驅動收斂）落地後、主控收尾前重跑 `AISDLC_SDD ci-gate.sh` 全套自我發現 | **`.github/workflows/aisdlc-sdd-ci.yml` 的 paths 過濾清單漏收新增的共用資料規格檔 `tools/lib/bash_probe_spec.py`**：`AISDLC_SDD/scripts/tests/test_bash_probe.py` 直接 `import` 該新檔，但 `aisdlc-sdd-ci.yml` 的 push/pull_request paths 未列入，`scripts/tests/test_ci_paths_cover_root_consumers.py::test_all_root_consumers_covered_by_ci_paths[aisdlc-sdd-ci.yml]` 機械攔下（DEF-101-042 同構假綠盲區） | P2（機械鎖已攔下，未進 CI 才被發現） | `aisdlc-sdd-ci.yml` push/PR 兩份 paths 皆補上 `tools/lib/bash_probe_spec.py` | fixed@R32：主控直接修復，`test_ci_paths_cover_root_consumers.py` 10/10 passed |
| DEF-101-292 | 2026-07-24 | R32 四方一審 Architect（獨立審查時親自呼叫掃描器函式重現） | **`windows-compat-ci.yml`／`macos-compat-ci.yml` 同樣漏收 `tools/lib/bash_probe_spec.py`**（與 DEF-101-291 同一新檔、不同 workflow）：`tools/tests/test_pre_push_dispatcher.py`／`test_git_hooks_install_common.py` 亦直接 import 此新檔，這兩份測試由 `tools/tests/**` 掃描觸發，但這兩支 workflow 的 paths 未同步補上。Architect 親自呼叫 `_consumed_root_paths("tools/tests")` 驗證 `bash_probe_spec` 未被偵測到——根因是這兩份測試檔用 `REPO_ROOT / "tools" / "lib"`（先賦值變數）的 `sys.path.insert` 慣用法，屬掃描器 `_SYSPATH_PARENTS_RE` 認不出的第三種寫法（既有盲區，非本輪新增），`test_all_root_consumers_covered_by_ci_paths[windows/macos-compat-ci.yml]` 因此維持假綠 PASS 而非真正涵蓋 | P2（同 DEF-101-291 性質，唯獨此次未被機械鎖攔下、全靠 Architect 人工挖出） | 比照 `platform_utils.py`/`bootstrap_core.py`/`run_act_core.py` 既有手動列舉先例，兩份 workflow 各補上 `tools/lib/bash_probe_spec.py`；不修掃描器正則本身（檔頭註解明載「S6→S12→S26 三輪加新正則打地鼠」為應避免的反模式，二審 Architect 複核同意維持此判斷） | fixed@R32：`.github/workflows/macos-compat-ci.yml`／`windows-compat-ci.yml` push/PR 各補一行，緊接既有 `platform_utils.py` 條目後、沿用同段既有註解；四方二審 Architect/SD 皆獨立重驗 `git diff` 內容合理、`test_ci_paths_cover_root_consumers.py` 10/10 passed |
| DEF-101-293 | 2026-07-24 | R32 四方一審 SA 與 QA 各自獨立發現（交叉印證） | **`bash_probe`/`_usable_bash` 三份消費者對 `PROBE_CMD` 的 coreutils 驗證測試是裝飾性斷言**：既有 43 個相關測試全部用 `mock.patch.object(subprocess, "run")` 手填回傳值，從未驗證 `PROBE_CMD` 字面內容本身是否真的依賴 `dirname`。SA 與 QA 各自獨立把 `PROBE_CMD` 改回 echo-only 退化版（`"echo probe_ok &amp;&amp; echo /tmp/probe_dir"`），43~51 個既有 case 全數維持綠燈，證實 DEF-101-275 的整個修復可被悄悄撤回而無測試發現 | P2（meta 層級測試完整性缺口——生產邏輯本身正確，但保護該邏輯的測試套件本身有效性不足） | 至少一處補「驗證 `subprocess.run` 實際收到 `PROBE_CMD`」的斷言，或新增不 mock 的真實 subprocess 整合測試 | fixed@R32：新增 `tools/tests/test_bash_probe_spec_contract.py`（5 case，三層防線：資料層字面斷言 `"dirname" in PROBE_CMD`＋行為層真實 subprocess 執行〔受限 PATH 模擬缺 dirname〕＋`usable_bash()` 端到端 wiring 驗證）。主控與 SA/SD 三方各自獨立 bug-injection（echo-only 退化、`PROBE_EXPECT_DIRNAME` 改錯、生產端 wiring 繞過 spec 直接寫死字串）皆證實有鑑別力；四方一審 APPROVE-with-conditions（此項）、二審全數 APPROVE |
| DEF-101-294 | 2026-07-24 | R32 四方二審 QA | **`tools/lib/bash_probe_spec.py` 頂部 WHY 文檔字串未列出第四份消費者 `tools/tests/test_bash_probe_spec_contract.py`**（DEF-101-293 修復新增），落入本 repo 反覆出現的「文件落後程式碼」模式 | P4（純文件性，不影響任何機械閘門判準） | WHY 段落補充第四消費者存在理由 | fixed@R32：主控直接補上一段說明（前三份消費者測試皆 mock subprocess、未驗證 PROBE_CMD 真實依賴 coreutils，第四份補上不 mock 的真實驗證）；QA 最終確認裁決 APPROVE |
| DEF-101-298 | 2026-07-24 | R34 Scan-B（Python 跨平台一致性掃描） | **`tools/tests/test_windows_forbidden_filename_parity.py`（R33 DEF-101-295 新增）的 6 個測試方法直接無條件呼叫 `subprocess.run(["bash", ...])`**，未比照本 repo 既有慣例（`test_pre_push_dispatcher.py`／`test_git_hooks_install_common.py` 的 `_usable_bash()` + `@unittest.skipIf(_BASH is None, ...)`）做守門，在 bash 不可用或為 WSL 佔位版的環境會拋出未攔截的 `FileNotFoundError`（ERROR 而非優雅 SKIP） | P2（測試基礎設施缺口；本質是「保護 Windows 相容性的新測試，自己卻違反了 repo 已建立的 Windows bash 可用性判準慣例」） | 於該檔複製一份 `_usable_bash()`（鏡自 `test_pre_push_dispatcher.py`，共用 `tools/lib/bash_probe_spec.py` 規則資料），模組層級 `_BASH = _usable_bash()`，6 個方法補 `@unittest.skipIf(_BASH is None, ...)`，`_run_bash_seg_check` 改用 `_BASH` 而非寫死 `"bash"` | fixed@R34：修復落地，13/13 passed（本機 bash 可用時全通過，行為不變；bash 不可用環境改為優雅 SKIP） |
| DEF-101-299 | 2026-07-24 | R34 Scan-C（CI/排程/hooks 基建掃描） | **`tools/integration_gate.sh`（macOS/Linux 側）從未在 `macos-compat-ci.yml` 被實際執行**，只靠 `root-infra-ci.yml` 的 `bash -n` 語法檢查頂替；對等的 Windows 側 `integration_gate.ps1` 先前已修過同一缺陷模式（P1 Architect 發現，見上方歷史記錄）並補上 `windows-smoke` 實跑 `-SkipFull` 步驟，macOS 側漏補形成不對稱 | P2（與 windows 側修復前的原始曝險狀態同構） | `macos-compat-ci.yml` 的 macos-smoke job 新增一步 `bash tools/integration_gate.sh --skip-full`，對齊 windows-compat-ci.yml 既有寫法與註解慣例 | fixed@R34：本機實測 `bash tools/integration_gate.sh --skip-full`（2 PASS / 1 SKIP，cc-switch CLI 未裝屬既有 DEF-01-007 已知情況），YAML 語法驗證通過 |
| DEF-101-300 | 2026-07-24 | R34 Architect 架構深度評估 | **`tools/dev_start.ps1` 的 WindowsApps 空殼別名排除 guard（第 38-41 行）是第 4 個獨立實作**（與 `tools/bootstrap.ps1`、`tools/bootstrap_core.py::_is_windows_apps_stub()` 同規則、各自獨立寫），且**零測試覆蓋**——`tools/tests/test_dev_start.py` 只測 Python 核心，`check_wrapper_thinness.py` 只釘 SHA256 不驗語意，屬 DEF-101-273/279/281 同一缺陷類別第 4 次出現的結構性復發風險 | P2（同一缺陷類別的結構性復發；三處目前內容一致，但下次任一處改動不會觸發交叉告警） | 比照 DEF-101-295 手法新增交叉一致性測試鎖（不合併程式碼——`dev_start.ps1` 需在呼叫 Python 核心「之前」自行找到直譯器，屬 bootstrapping 先有雞或蛋的架構限制） | fixed@R34：新增 `tools/tests/test_windowsapps_guard_cross_consistency.py`（4 case：3 個靜態文字擷取交叉核對三處 guard 語意一致 + 1 個 dev_start.ps1 行為回歸鎖，比照 `test_bootstrap_ps1.py` 手法，複製到零 `.venv` 臨時目錄執行避免真實 `.venv` 短路 guard 分支）。bug-injection 驗證：拔掉 guard 後兩個靜態測試立即抓到（如實記載已知侷限：behavioral 測試在 macOS `pwsh` 上因 `Get-Command` 不做 Windows PATHEXT 解析而對此特定 bug 無鑑別力，只在真 Windows PowerShell/`windows-compat-ci.yml` 上才有效——`test_bootstrap_ps1.py` 同款既有測試亦有此侷限，非本輪新增問題）。**自我發現的連鎖回歸**：新測試檔以路徑直接引用 `tools/dev_start.ps1`，`AISDLC_SDD ci-gate.sh` 全套回歸時 `test_ci_paths_cover_root_consumers.py` 機械攔下 `macos-compat-ci.yml` paths 未涵蓋此消費檔（DEF-101-042 同構，比照 R27 `tools/bootstrap.ps1` 同款前例），已於 push/PR 兩份 paths 補上一行修復，10/10 passed |
| DEF-101-301 | 2026-07-24 | R34 Scan-D（文件/帳本一致性掃描） | **帳本「已歸檔內容」標題寫「（六檔）」，但實際已列出 8 個 archive bullet**（`archive_01`~`archive_08`）——用 `git log -p` 追蹤，R33 建立 `archive_07`、R34 建立 `archive_08` 時皆漏了同步遞增此計數 | P4（純文件性，不影響任何機械閘門判準） | 訂正計數字樣為「（八檔）」 | fixed@R34：主控直接訂正 |
| DEF-101-302 | 2026-07-24 | R34 四方一審 Architect（DEF-101-300 修復的複審）＋二審再度用第三種手法確認 | **`test_windowsapps_guard_cross_consistency.py` 的 `-and`/雙分支靜態測試對「尾端疊加恆真子句」無鑑別力**：在既有 `-notlike '*\WindowsApps\*'` 子句「後面」追加 `-or $true`（PowerShell 運算子優先序使整條 `if` 判斷式恆真，guard 形同虛設），一審發現的 `-and`/`findall` 兩項測試只錨定運算式「中段」，未驗證 WindowsApps 排除後條件式是否立即收尾，該手法可繞過兩者 | P2（測試鑑別力殘餘缺口，非生產邏輯本身有問題） | 新增兩項測試錨定**整條** `if (...)` 判斷式，要求 `-notlike '*\WindowsApps\*'` 之後必須緊接 `)`（僅容許空白），中間不得插入任何 token | fixed@R34：新增 `test_dev_start_ps1_guard_condition_closes_immediately_after_windowsapps_check`／`test_bootstrap_ps1_guard_conditions_close_immediately_after_windowsapps_check`；主控與 SD 二審皆用 `-or $true` 注入獨立重驗兩測試正確變紅，還原後 8/8 passed；Architect 三審最終確認 |
| DEF-101-303 | 2026-07-24 | R34 四方二審 SD（用新角度 bug-injection 發現） | **`bootstrap.ps1` 若把 `$PyCand`/`$Py3Cand` 的賦值互換（`Get-Command python` 結果存入 `$Py3Cand`、`Get-Command python3` 結果存入 `$PyCand`，guard 邏輯本身逐字不動）**，`test_windowsapps_guard_cross_consistency.py` 現有三項靜態測試（含 DEF-101-300/302 修復的四項）皆只驗證「變數名與自己的 guard 配對」，不驗證右手邊 `Get-Command` 呼叫的命令名稱是否與變數語意相符，故全數漏放行。此為手誤型 bug，觸發機率低（需刻意寫反或複製貼上時看錯變數名），SD 判定非阻擋 | P3（低機率邊界情況，backlog；SD 明確判定不阻擋本輪 APPROVE） | 未來可補一條「候選命令名稱與變數語意對應」的靜態鎖，或依賴既有 Windows-only 行為回歸鎖（`test_bootstrap_ps1.py::test_real_python_outside_windowsapps_is_used_even_when_windowsapps_stub_present_first`）在真 Windows CI 上的巧合覆蓋 | **fixed@R37**：Architect 架構深度評估（R37）判定此缺陷根因是 `bootstrap.ps1`/`dev_start.ps1` 三處 WindowsApps guard 各自內嵌 `$PyCand`/`$Py3Cand` 中繼變數複製貼上，比照既有 `tools/lib/Find-GitBash.ps1` 先例收斂成共用函式 `tools/lib/WindowsAppsGuard.ps1::Test-IsRealPython`，三處呼叫端改為 `Test-IsRealPython -CandidateName 'python'`／`'python3'` 單行字面值呼叫，不再有可互換的中繼變數。四方一審 Architect/SA/SD/QA 皆親自驗證兩檔已無殘留 `$PyCand`/`$Py3Cand`，且新測試 `test_bootstrap_ps1_calls_shared_function_for_both_python_and_python3` 機械鎖住呼叫端字面值恰為 `'python'`/`'python3'` 各一次。Architect 二審補充措辭精確度：此修復消滅的是「兩變數互換」這一特定錯配類型，非涵蓋所有可能的呼叫端手誤（如字面值本身打錯，惟此類錯誤可見度遠高於原本埋在判斷式裡的變數名，且已被上述測試鎖住）。SD 一審另以 bug-injection 找到「呼叫端用額外 `-or` 條件覆蓋共用函式回傳值」這一新繞過手法對本輪兩份 WindowsApps guard 測試檔無鑑別力，但確認既有 `tools/check_wrapper_thinness.py` 正規化內容 hash 釘選（跨平台可用、已接入本機 CI 對等與 pre-push 紀律）構成有效正交防線，判定 P3 非阻擋，見 DEF-101-313 |
| DEF-101-304 | 2026-07-24 | R35 Scan-A（Shell/PowerShell 跨平台一致性掃描） | **`tools/dev_start.ps1` 兩個早期失敗分支（找不到 repo 根、找不到 Python 直譯器）在 dot-source 情境下只執行裸 `return`，未對 `$LASTEXITCODE` 賦值**，違反自身 `.NOTES`（第 15-17 行）明載的「呼叫端判斷成功/失敗請讀 `$LASTEXITCODE`」契約——`$LASTEXITCODE` 停留在呼叫前殘值（可能是 0），使 dot-source 呼叫端誤判失敗為成功。對等的 `tools/dev_start.sh` 兩個對應分支皆用 `return 1` 正確傳遞。實測重現：乾淨 pwsh 子行程 dot-source 修復前版本，`$LASTEXITCODE` 輸出為空字串；`_ds_main` 對等 `.sh` 分支則正確傳遞 rc=1 | P2（違反自身文檔契約的真實行為缺陷，觸發情境為全新機器缺 Python 時 onboarding） | 兩處失敗分支 `if ($DotSourced) { return }` 前補 `$global:LASTEXITCODE = 1` | fixed@R35：`tools/dev_start.ps1` 兩處補齊；新增 `tools/tests/test_dev_start_ps1_lastexitcode.py`，含兩個測試類別：`TestDevStartPs1DotSourceLastExitCode`（pwsh 子行程實際 dot-source 執行驗證「找不到 Python」分支）+ 四方一審 Architect/QA/SD 交叉獨立發現「找不到 repo 根」分支無測試覆蓋後追加的 `TestDevStartPs1BothFailureBranchesSetLastExitCode`（該分支需磁碟根寫入權限才能安全實際觸發、不適合自動化模擬，改用靜態文字比對鎖住兩分支同時修復）。bug-injection（分別還原兩分支的修復版本）確認兩測試皆正確變紅，還原修復後 diff 逐位元組核對一致、測試轉綠 |
| DEF-101-305 | 2026-07-24 | R35 Scan-D（文件/帳本一致性掃描） | **帳本「已歸檔內容」標題寫「（八檔）」，但實際已列出 9 個 archive bullet**（`archive_01`~`archive_09`）——R34 才把計數從「六檔」訂正為「八檔」（DEF-101-301），但 R34 本輪自己又新增 `archive_09.md` 卻同樣漏了同步遞增，與 DEF-101-301 同一根因、同一位置第三次復發 | P4（純文件性，不影響任何機械閘門判準） | 訂正計數字樣為「（九檔）」 | fixed@R35：主控直接訂正 |
| DEF-101-306 | 2026-07-24 | R35 Scan-D（文件/帳本一致性掃描） | **帳本描述 `AutoSDD_Defect_Log_archive_06.md`「≈6KB」，但 `wc -c` 實測為 10,953 bytes（≈10.7KB）**，偏差近 79%；交叉比對其餘 8 個 archive 的大小描述皆在合理捨入誤差內，唯獨此筆明顯異常 | P4（純文件性，不影響任何機械閘門判準） | 訂正大小描述為「≈10.7KB」 | fixed@R35：主控直接訂正，`wc -c` 核對 10953 bytes 吻合 |
| DEF-101-308 | 2026-07-24 | R35 四方二審 Architect（對追加的靜態一致性鎖再次 bug-injection） | **`TestDevStartPs1BothFailureBranchesSetLastExitCode`（DEF-101-304 修復追加的靜態鎖）純字面計數比對可被刻意繞過**：把「找不到 repo 根」分支的裸 `return` 格式偽裝成 `if ($DotSourced) {  return }`（多一個空格）閃避 `bare` 字面比對，同時在檔案別處插入無害註解行 `# decoy: if ($DotSourced) { $global:LASTEXITCODE = 1; return }` 把 `fixed` 計數補回 2，兩個測試皆維持綠燈，但該分支是貨真價實的回歸。Architect 判定：純字面計數本質上可被同檔案任何位置的字面複製繞過，是這類鎖的結構性限制；真正觸發需「刻意插入 decoy」的對抗性動作，非一般開發者手誤會踩到的情境，且本輪 `dev_start.ps1` 兩分支修復本身正確，此僅為「測試的測試」層面鑑別力縫隙 | P3（低機率對抗性繞過，backlog；Architect 明確判定非阻擋本輪 APPROVE） | 未來若要堵死可改用「解析式」驗證（逐一解析 `if (-not $Root)`／`if (-not $Py)` 兩區塊內容各自含 `$LASTEXITCODE` 賦值語句，而非整檔字面計數），成本較高、超出本輪 P2 缺陷比例原則 | open（backlog，如實記載，列入下一輪追蹤） |
| DEF-101-307 | 2026-07-24 | R35 Architect 架構深度評估；R36 Architect 複核並落地收斂 | **WSL System32 路徑段排除規則分裂成兩個互不相通的 SSOT 孤島**：島 A（`tools/lib/bash_probe_spec.py::SYSTEM32_SEGMENT`，供 `bash_probe.py`／`test_pre_push_dispatcher.py`／`test_git_hooks_install_common.py`／`test_bash_probe_spec_contract.py` 消費）與島 B（`tools/lib/Find-GitBash.ps1` 的 regex `\System32\`／`integration_gate_core.py::_has_system32_segment()` 硬編 `"system32"`，已由 `test_find_git_bash_parity.py` 互相鎖住），但沒有任何測試跨島比對——任一島字面值改掉（如筆誤 `"sys32"`）不會讓另一島有訊號。R35 Architect 評估實測風險偏低（兩島各自都有行為測試，單島內部筆誤會被自己的測試打紅），列為 backlog；**R36 Architect 複核發現低成本收斂路徑已具備**（`integration_gate_core.py` 已有 `sys.path.insert(0, tools/lib)`，可直接 import 同目錄的 `bash_probe_spec`），且指出「收斂管道已具備卻遲遲未收斂」本身會變成另一種技術債，建議本輪落地而非續留 backlog | P3（低風險架構性缺口，R36 已收斂） | `integration_gate_core.py::_has_system32_segment()` 改 `import bash_probe_spec as _spec` 取代硬編字面值；`Find-GitBash.ps1` 因無法直接 import Python 常數，維持獨立字面值不變（既有限制，本輪不處理） | **fixed@R36**：`tools/integration_gate_core.py` 新增 `import bash_probe_spec as _spec`，`_has_system32_segment()` 由 `part.lower() == "system32"` 改為 `part.lower() == _spec.SYSTEM32_SEGMENT`；`tools/lib/bash_probe_spec.py::SYSTEM32_SEGMENT` 常數上方註解補上新消費者；`tools/tests/test_find_git_bash_parity.py::_extract_py_system32_word()` 同步改為斷言原始碼依賴 `_spec.SYSTEM32_SEGMENT`（防退回硬編字面值）並 import 取得 runtime 值比對。四方一審／二審（Architect 兩種 bug-injection：硬編字面值還原、SSOT 值漂移成 `"system64"`，皆被正確攔截；SA/SD/QA 各自獨立覆核）皆 APPROVE，島 A/B 確認真正合一（非換皮不換骨），PS1 側維持獨立字面值由既有 parity 測試交叉鎖 |
| DEF-101-309 | 2026-07-24 | R36 一審 SD 首先發現，二審 Architect（ruff F401 正交防線）／SA（嚴重度精修）／QA 三方獨立覆核交叉確認 | **`test_find_git_bash_parity.py::_extract_py_system32_word()`（DEF-101-307 修復新增）的未錨定 `re.search` 掃全檔文字可被誘餌行內註解繞過**：在 `_has_system32_segment()` 函式內插入一行含 `part.lower() == _spec.SYSTEM32_SEGMENT` 字樣的行內註解，同時把真正執行的 `return` 陳述式改回硬編字面值 `"system32"`，11 個測試仍全數 PASSED——因為該函式現在的邏輯是「①未錨定搜尋確認樣式**存在**於全檔文字（不分辨是否為真正執行的程式碼或僅為註解/docstring），②但實際回傳值不取匹配內容，而是直接 `import bash_probe_spec` 讀取常數本身」。**SA 二審精修嚴重度定性**：舊版（`m.group(1)` 直接回傳匹配到的文字）繞過需要偽造出「內容剛好等於正確值」的誘餌，等同攻擊者需先知道正確答案；新版繞過只需貼上樣式相符的誘餌文字、**不需知道任何正確值**即可讓測試回傳 golden 值放行——比對值與被測程式碼真實行為已完全脫鉤，繞過門檻較舊版**實質降低**，非單純沿用舊限制。**Architect 二審發現正交緩解因子**：`ruff check`（AST-based，CLAUDE.md 明文必跑 CI 檢查）在此特定失效模式下會因 `_spec` import 變成未使用觸發 `F401`，非完全無防線。SD 補充：此縫隙亦有非對抗性的自然觸發路徑（merge conflict／rebase 手誤保留說明性註解但程式碼行被誤還原的「stale comment drift」），非僅限刻意攻擊 | P3（測試鑑別力縫隙，本輪 SSOT 收斂本身正確；有 ruff F401 正交防線；四方一致判定非阻擋） | 建議未來把 `test_find_git_bash_parity.py` 內 `_extract_py_system32_word`（及同檔 `_extract_py_candidates`／`_extract_ps1_candidates`／`_extract_ps1_system32_word`）的搜尋範圍改為先用 `ast.get_source_segment()` 鎖定目標函式的原始碼片段再比對，而非掃全檔文字；範圍明確、成本可控，但超出本輪「收斂一個常數 SSOT」的範圍，依 surgical-changes 原則不在本輪順手處理 | open（backlog，四方一致判定非阻擋，如實記載列入下一輪追蹤） |
| DEF-101-310 | 2026-07-24 | R37 Scan-A（路徑/檔案系統相容性掃描） | **`AutoClaude/autoclaude/models/escalation.py::EscalationDump.save()` 組檔名時未淨化 `self.step_id`**（`PlaybookTask.step_id` 為完全自由格式、無驗證的 `str` 欄位，playbook 作者手寫 YAML 時很自然會寫出含冒號等 Windows 禁用字元的字串，如 `"Step 1: Setup"`），與 DEF-101-219（R16）／DEF-101-295（R33）同根因（Windows 禁用檔名字元／保留裝置名），是同一缺陷類別第 4 個獨立未覆蓋位置——`RawStreamLogger`（`logger.py`）與 `tools/`/`check_ntfs_paths.py` 等既有三處交叉一致性鎖（`test_windows_forbidden_filename_parity.py`）皆未涵蓋此第四處。觸發後果：`_escalation.py` 的 `try/except` 包住 `dump.save()` 不會讓 process 崩潰，但會使 ESCALATION 診斷報告（本專案「失敗復盤/人工接手」關鍵材料）在 Windows 上靜默存檔失敗。另有次要伴生症狀：`_escalation.py` 顯示用的 `last_log_path` 字串同樣未淨化，在三平台上都可能與 `RawStreamLogger` 實際落地檔名不一致 | P2（真實可觸發、非對抗性情境即可命中，命中的是核心可觀測性/失敗復盤路徑；有 try/except 兜底不致 process 崩潰） | `EscalationDump.save()` 與 `_escalation.py` 的 `last_log_path` 皆改用既有 `logger.py::_sanitize_log_filename()`，不新增第五份獨立淨化實作 | fixed@R37：`escalation.py`／`_escalation.py` 改為直接 import 並呼叫 `logger.py::_sanitize_log_filename()`（`assertIs` 鎖住兩處確實共用同一函式物件、非另寫一份形似邏輯）；新增 `AutoClaude/tests/test_models.py`（5 case：冒號空白／全部禁用字元／控制字元／`save()` 回傳路徑回歸／shared-sanitizer identity）與 `AutoClaude/tests/plugins/test_checkpoint_plugin.py`（1 case：驗證 `last_log_path` 與 `RawStreamLogger` 實際落地檔名逐字元一致）；`tools/tests/test_windows_forbidden_filename_parity.py` 新增 `TestEscalationModulesReuseSharedSanitizer`（2 case，鎖 import 共用而非新增第四個獨立條目）。四方一審 Architect/SA/SD/QA 對修復本身與所有新測試皆親自 bug-injection 驗證鑑別力屬實（拿掉淨化呼叫後測試正確變紅，`cp`/`diff` 還原核對一致），皆 APPROVE 無異議。**QA 一審另發現真實新邊角案例**：`_sanitize_log_filename()` 只淨化禁用字元、不截斷長度，超長 `step_id`（300 字元）會讓 `save()` 拋出未捕捉的 `OSError: File name too long`，屬同一條「診斷快照存檔」關鍵路徑的不同類缺口（檔名長度而非禁用字元）。已一併修復：新增 `write_text_with_fallback()` 共用 helper（`logger.py`，比照既有 `RawStreamLogger` fallback 精神），`EscalationDump.save()` 改用此 helper，失敗時 fallback 寫入系統暫存目錄雜湊檔名，避免 ESCALATION 快照完全遺失；新增 `test_escalation_dump_save_falls_back_when_step_id_too_long_for_filesystem`（`AutoClaude/tests/test_models.py` 第 6 case），bug-injection（暫時讓 fallback 分支提前 `raise`）驗證測試正確變紅，`cp`/`diff` 還原核對一致。此次重構同時把 `escalation.py` 的新增邏輯行數收斂回 `tools/check_loc_budget.py` 的 `data` tier（≤150）預算內（原直接內嵌 fallback 邏輯一度超支 +15，抽出共用 helper 後 0 violation）。**QA 二審再發現一個更嚴重的真實新缺口**：`_sanitize_log_filename()` 的禁用字元集合不含 `/`，`step_id` 含 `/`（如自然階層式命名 `"setup/init"`，或刻意的 `"../../../../../../tmp/pwned"`）會被 `pathlib` 解讀成額外路徑層級，輕則在 `dump_dir` 下建出非預期子目錄，重則使 `path.parent.mkdir()` 拋出未捕捉的 `PermissionError`（QA 親自重現路徑穿越情境）——`write_text_with_fallback()` 的 try/except 保護不到 `mkdir()` 這一行，等同修復要根除的「未捕捉例外導致快照完全遺失」症狀原地重演。已修復：於 `logger.py::_sanitize_log_filename()` 內獨立於 `_WIN_FORBIDDEN_CHARS`（該常數與 `check_ntfs_paths.py`／pre-commit `_ntfs_seg_bad()` 三方鎖定同一組「已切割路徑片段」判準，加 `/` 會破壞該 parity）之外，額外淨化 `/`（函式輸入語意是「尚未切割的任意字串」，與三方鎖定的「已切割片段」屬不同問題域，不影響既有 parity 測試）。新增 `test_escalation_dump_save_step_id_with_slash_stays_flat_file_not_subdir`／`test_escalation_dump_save_step_id_with_path_traversal_stays_within_dump_dir`（`AutoClaude/tests/test_models.py` 第 7、8 case），bug-injection（暫時拿掉 `/` 淨化）驗證兩測試皆正確變紅、`cp`/`diff` 還原核對一致 |
| DEF-101-311 | 2026-07-24 | R37 主控（跑 `AISDLC_SDD/scripts/ci-gate.sh` 全套回歸時機械測試攔下） | **新增消費檔 `tools/lib/WindowsAppsGuard.ps1`（DEF-101-303 修復產物）未列入 `.github/workflows/macos-compat-ci.yml` 的 `paths:` filter**——`test_ci_paths_cover_root_consumers.py` 正確攔下：只改該新檔不會觸發 `macos-compat-ci.yml` 對應回歸鎖重跑（DEF-101-042 同構，比照 R27 `bootstrap.ps1`／R34 `dev_start.ps1` 同款前例，每次新增被 `test_windowsapps_guard_cross_consistency.py` 消費的根層檔案都需同步手動補列） | P2（機械閘門已攔下，未 push 前即發現；若未修復會讓此新共用檔案的回歸失去 macOS CI 覆蓋） | push／PR 兩份 `paths:` 各補一行 `tools/lib/WindowsAppsGuard.ps1` | fixed@R37：主控直接補上並重跑 `ci-gate.sh` 確認轉綠（`scripts/tests` 184/184 passed）；四方一審 Architect 親自核對 push/PR 兩區塊皆補齊、`windows-compat-ci.yml` 側已有 `**/*.ps1` 兜底 glob 不需重複列舉 |
| DEF-101-312 | 2026-07-24 | R37 SA 一審（帳本/文件誠實度審查） | **本輪（R37）兩項實質修復（DEF-101-310 escalation 淨化、DEF-101-303 WindowsApps guard 收斂）落地後，`docs/06_quality/AutoSDD_Defect_Log.md` 一度未同步新增/更新對應條目**（僅訂正 archive_07 大小描述文字），且 `ONBOARDING.md` §7 pytest 基線因本輪新增 6 條 AutoClaude 測試而過時（乾淨基線應為 3,650/210，非原記載的 3,644/210）——SA 判定此為本輪放行前必須補齊的收斂缺口（非新程式碼缺陷），REJECT 待補 | P3（帳本/文件同步缺口，程式碼本身已驗證正確） | 補齊帳本條目（本列即為其一）＋於全新乾淨 venv 重驗 pytest 基線並更新 SSOT | fixed@R37：主控補齊 DEF-101-310／DEF-101-311 帳本條目、DEF-101-303 狀態更新為 fixed；於全新臨時目錄乾淨 venv（`import psycopg2`/`sqlalchemy` 皆 `ModuleNotFoundError` 確認乾淨，`CLAUDECODE=1`）重跑 `pytest tests/ -q`，已更新 `ONBOARDING.md` §7 SSOT 並保留舊基線為歷史註記；`tools/check_pytest_baseline_sites.py` 重跑確認仍為唯一站點無漂移。**QA 一審後又新增 1 條測試**（DEF-101-310 超長 step_id fallback 修復），最終乾淨基線收斂為 **3,651 passed / 210 skipped**（較 R33 基線 +7），SSOT 已同步更新為此最終數字 |
| DEF-101-313 | 2026-07-24 | R37 一審 SD（對 WindowsApps guard 收斂做 bug-injection 找新繞過手法） | **`test_windowsapps_guard_cross_consistency.py`／`test_bootstrap_ps1.py` 對「呼叫端用額外 `-or` 條件覆蓋 `Test-IsRealPython` 共用函式回傳值」這類新繞過手法無鑑別力**：把呼叫端改成 `$isRealPython -or (Get-Command python -ErrorAction SilentlyContinue)`（偽裝成「企業 GPO 環境下 `.Source` 可能為空字串」的防禦性寫法），本輪新增的存在性檢查（純字面 regex 確認呼叫語法存在）與行為測試（只測共用函式本身，不測呼叫端如何消費回傳值）皆抓不到，端到端測試在本機 macOS pwsh 上因既有 PATHEXT 解析侷限本就對此無鑑別力（已知既有限制，非本輪新增）。SD 另行驗證：此類呼叫端邏輯異動會改變 `tools/check_wrapper_thinness.py` 的正規化內容 hash 釘選並正確變紅，構成有效的跨平台正交防線（已接入本機 CI 對等與 pre-push 紀律） | P3（本輪兩份 WindowsApps guard 測試檔本身鑑別力有限，但有正交防線有效補位；觸發需「看似合理的防禦性寫法」，非典型手誤但也非需惡意規避意圖的高門檻）| 若要讓 `test_windowsapps_guard_cross_consistency.py` 自身具備此類鑑別力，可將存在性檢查從 regex 升級為 AST/簡易解析確認 `if` 判斷式的條件運算式恰為 `Test-IsRealPython(...)` 呼叫本身、無其他 `-or`/`-and` 修飾 | open（backlog，SD 明確判定非阻擋本輪 APPROVE，有正交防線緩解） |
| DEF-101-314 | 2026-07-24 | R38 Scan-A（檔名/路徑相容性掃描） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/production_monitor.py::ingest_slo_violation()` 的 `nfr_id` 欄位原本不在 HMAC-SHA256 簽章覆蓋範圍（`REQUIRED_FIELDS`）、也未淨化就組入 `_report_path()` 的 `PBS-DRIFT-{nfr_id}-{date}.md` 檔名**：攻擊者可在既有合法簽章 SLO 事件上事後附加任意 `nfr_id`（簽章驗證仍通過，因 canonical payload 原只涵蓋 `REQUIRED_FIELDS`），造成路徑穿越（`nfr_id` 含 `/`、`..`）或 Windows 檔名崩潰（含 `< > : " \| ? *` 或保留裝置名） | P0（生產回饋層 inbox 讀取外部事件、可被攻擊者控制輸入，路徑穿越/靜默寫檔失敗屬安全與可用性雙重風險） | 本輪修復 | fixed@R38：`signed_fields_default()` 改回傳 `REQUIRED_FIELDS + ("nfr_id",)`，使 `nfr_id` 併入 HMAC 簽章覆蓋範圍（事後夾帶不重簽即 `signature_mismatch`）；新增獨立淨化函式作第二層防護（後於四方一審 Architect 建議下收斂為重用 `state_loader._sanitize_component()` 薄包裝，見 DEF-101-315）。新增 `test_production_monitor.py::NfrIdSecurityTests`（13 case，含簽章夾帶攻擊、路徑穿越、保留裝置名、超長截斷、控制字元），四方一審＋複審（Architect/SA/SD/QA）皆親自 bug-injection 驗證後全數 APPROVE |
| DEF-101-315 | 2026-07-24 | R38 Scan-A（檔名/路徑相容性掃描）；一審 SD 對修復本身 bug-injection 追加發現 | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/state_loader.py::_sanitize_component()` 原本只擋 `/` 與 `\`，未擋 Windows 禁用字元/保留裝置名/路徑穿越/超長截斷**，覆蓋度遠弱於 AutoClaude 側已鎖定 parity 的 `_sanitize_log_filename()`，用於組 FSM 治理閉環 SSOT 狀態檔 `FSM-STATE-{project}-{track_id}.yaml`（`project`/`track_id` 皆外部可控：環境變數/並行 track 標籤）。**首次修復擴充淨化邏輯後，R38 四方一審 SD 對新版做 bug-injection 又找到 padding-bypass 順序缺陷**：保留裝置名檢查發生在截斷之前、截斷後又獨立做第二次 rstrip 卻不重新檢查，`_sanitize_component("CON" + " "*77 + "X")` 會回傳裸露的 `"CON"`（guard 被繞過），`PRN`/`AUX`/`NUL`/`COM1` 等其餘保留名同樣可繞過 | P1（首次淨化不足）→ 二階段修復期間發現順序缺陷（P2，目前僅有兩個呼叫點皆用固定非空前綴包住，非立即可用攻擊，但共用函式自身文件與測試明確宣稱的不變量可被證偽） | 本輪修復 | fixed@R38（兩階段）：階段一擴充 `_sanitize_component()` 涵蓋 Windows 禁用字元/保留裝置名/路徑穿越/超長截斷，`spec_patch_proposer.py` 改用同一函式（見 DEF-101-316），新增跨模組 parity 測試 `test_state_component_sanitizer_parity.py` 比對與 AutoClaude 側 `_sanitize_log_filename` 安全性質等效。階段二（四方一審 SD 發現後）修正操作順序為單輪「淨化禁用字元→截斷→最終 rstrip→保留裝置名檢查」，新增回歸測試 `test_sanitize_component_reserved_name_padding_bypass_regression`（鎖 CON/PRN/AUX/NUL/COM1 五個 padding-bypass 攻擊輸入）。SD 複審獨立重跑 720 組 fuzzer（5 保留名×24 padding 長度×9 結尾字元）確認 0 bypass，四方複審全數 APPROVE |
| DEF-101-316 | 2026-07-24 | R38 Scan-A（檔名/路徑相容性掃描） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/spec_patch_proposer.py` 組 `SPEC-PATCH-{ac_id}-{date}.md` 檔名時，`ac_id`（來自缺陷回流訊號/test-failure-analyzer 映射，外部可控性較低但同款風險）未套用任何淨化** | P2（觀察項，外部可控性遠低於 DEF-101-314/315，一併修復以求一致） | 本輪修復（隨 DEF-101-315 一併收斂） | fixed@R38：改為 `from .state_loader import _sanitize_component`，組檔名前呼叫該函式（重用同套件內已收斂的共用淨化邏輯，未另造第三份實作）；新增 `test_phase_j.py` 2 個測試（禁用字元/路徑穿越/超長 `ac_id` 不逃逸 `out_dir`；保留裝置名 `ac_id="CON"` 正確加前綴）。四方一審＋複審皆 APPROVE |
| DEF-101-317 | 2026-07-24 | R38 Architect 架構最佳化評估 | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1` 裸 `Get-Command python` 前置檢查會誤判 WindowsApps 空殼 `python.exe`（Store App Execution Alias）為「有 python」**，是 DEF-101-273/279/300/303 同一缺陷類別（R27~R37 反覆修復）第 5 個獨立未覆蓋位置，且首次出現在 **AISDLC_SDD 子專案**（此前四次皆在 AutoClaude/根層）。**首次修復在本檔內獨立實作空殼排除**（理由：AISDLC_SDD 有獨立 `releases/` 打包發布機制，不可硬相依 monorepo 根路徑），**R38 四方一審 Architect 查證後推翻此理由並 REJECT**：實際檢查 `releases/v0.01/*.tar.gz` 內容根本沒有 `tools/install_hooks/` 目錄，且本檔自身已透過 `$MainCheckoutRoot` 強相依 monorepo 根路徑結構定位 hook 來源檔，獨立實作等於重新製造 R37 剛收斂掉的同一類副本 | P2（首次修復）→ 架構收斂缺口（Architect 一審 REJECT 理由） | 本輪修復 | fixed@R38（兩階段）：階段一於本檔內嵌 `-like '*\WindowsApps\*'` 判斷；階段二（Architect REJECT 後）改為 dot-source 根層共用函式 `. (Join-Path $MainCheckoutRoot "tools\lib\WindowsAppsGuard.ps1")` 後呼叫 `Test-IsRealPython`（dot-source 前 `Test-Path` 檢查，缺席則 `Write-Error`+`exit 1`），消滅第 4~5 個獨立副本；連帶修復兩份假 monorepo fixture（`test_install_post_commit_windowsapps_guard.py` 新檔、既有 `test_install_post_commit_exec_bit.py`）補上 `tools/lib/WindowsAppsGuard.ps1`，以及 `.github/workflows/aisdlc-sdd-ci.yml` 的 CI paths 補上此新根層消費者（`test_ci_paths_cover_root_consumers.py` 機械鎖抓到的連帶缺口）。四方複審（含 Architect 本人）皆 APPROVE |
| DEF-101-318 | 2026-07-24 | R38 Scan-D（文件/帳本一致性掃描） | **R37 commit（d7164a7）只新增了缺陷總表列（DEF-101-310~313）與 archive 大小訂正，未依 R16 起既定慣例補寫對應的「## R37 四方複審裁決總結」敘事章節**——主檔與全部 archive 檔皆無「R37」標題字串，打破每輪皆有專屬敘事章節的慣例（機械閘門 `check_defect_log_crossref.py` 仍轉綠，資訊未遺失，commit message 本身已含濃縮版四方一二三審意見，僅未落地進帳本本身） | P3（文件/帳本完整性缺口，非機械閘門失效） | 本輪修復 | fixed@R38：主控於本輪（R38）動工時一併補寫「## R37 四方複審裁決總結」敘事章節（見下方，依 commit d7164a7 訊息與前一輪對話紀錄回填） |
| DEF-101-319 | 2026-07-24 | R39 Scan-A（檔名/路徑相容性掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/production_to_fpl.py::generate_fpl_draft()` 組 `FPL-PROD-{ac_id}-{divergence_kind}.md` 檔名時，生產遙測衍生、外部可控的 `ac_id`/`divergence_kind` 未淨化即組入，可致 Windows 保留裝置名崩潰或路徑穿越（`divergence_kind="../../evil"` 可逃出 `FAILURE_PATTERNS_DIR`）；掃描時判定 `process_drift_events()` 為目前唯一呼叫端、暫無真實外部輸入路徑（dormant scaffold），仍列為本輪範圍修復 | P2 | 本輪修復 | fixed@R39：改用既有 `state_loader._sanitize_component()`，`fid = fpl_id or f"FPL-PROD-{_sanitize_component(ac_id)}-{_sanitize_component(divergence_kind)}"`；新增 `test_phase_i.py::test_production_to_fpl_sanitizes_path_traversal_ac_id`，四方一審皆 APPROVE（Architect 親自 bug-injection 驗證測試鑑別力） |
| DEF-101-320 | 2026-07-24 | R39 Scan-A（檔名/路徑相容性掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/sandbox_runner.py::_write_flaky_report()` 組 `FLAKY-{date}-{spec.app_id}.yaml` 檔名時，`SandboxSpec.app_id`（docstring 明言由 Test-Contract 推導，未來將接外部規格文字）未淨化即組入 | P2 | 本輪修復 | fixed@R39：改用 `_sanitize_component(spec.app_id)`；新增 `test_phase_i.py::test_hermetic_flaky_report_sanitizes_app_id_path_traversal`，四方一審皆 APPROVE |
| DEF-101-321 | 2026-07-24 | R39 Scan-A（檔名/路徑相容性掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/hub_merge.py::_write_conflict_report()` 組 `CONFLICTS/{rule_id}-{timestamp}.yaml` 檔名時，`rule_id` 未淨化即組入——此模組存在前提正是「Hub 內容不可信」（`hub_sync.py` 已明文視為 untrusted），為四處中風險模型最貼近真實外部攻擊面者 | P2 | 本輪修復 | fixed@R39：`safe_rule_id = _sanitize_component(rule_id)` 後組檔名（payload 內 `rule_id`/`conflict_id` 欄位維持原樣供稽核追溯，僅檔名淨化，同 `production_monitor.py::safe_nfr_id` 既定模式）；新增 `test_hub_sync.py::TestConflictResolver::test_conflict_report_sanitizes_rule_id_path_traversal`，四方一審皆 APPROVE |
| DEF-101-322 | 2026-07-24 | R39 Scan-A（檔名/路徑相容性掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/path_cost.py::_write_milestone()` 組 `CALIBRATION-MILESTONE-{subagent}-{classification}-{date}.yaml` 檔名時，`subagent`/`classification` 未淨化即組入；同檔 `record_dispatch_rejection()` 因固定寫入 `REJECTED-{date}.yaml`（不含此二欄於檔名）確認不受影響、不需修 | P3（可控性較低，語意上應為固定 agent 名/分類列舉） | 本輪修復 | fixed@R39：改用 `_sanitize_component(subagent)`/`_sanitize_component(classification)`；新增 `test_path_cost.py::test_milestone_sanitizes_subagent_path_traversal`，四方一審皆 APPROVE |
| DEF-101-323 | 2026-07-24 | R39 一審 SD（對 DEF-101-319 修復做新角度 bug-injection 追加發現） | **`generate_fpl_draft()` 的 `fpl_id` 顯式覆寫參數完全繞過淨化**：`fid = fpl_id or f"..."` 僅 fallback 組裝分支經 `_sanitize_component`，呼叫端顯式傳入 `fpl_id` 時原樣進檔名，`fpl_id="../../escaped-evil"` 會逃出 `out_dir` 兩層；掃描全 repo 確認目前唯一呼叫端 `process_drift_events()` 從未傳入 `fpl_id`，屬「未上膛但存在的槍」，非現役可觸發路徑，但為 DEF-101-319 同一函式同一缺陷類別未收斂完整的部分 | P2（本輪範圍內縱深防禦補強，非新引入洞） | 本輪修復（隨 DEF-101-319 一併收斂） | fixed@R39：`fid = _sanitize_component(fpl_id) if fpl_id else (f"FPL-PROD-{_sanitize_component(ac_id)}-{_sanitize_component(divergence_kind)}")`；新增 `test_phase_i.py::test_production_to_fpl_sanitizes_explicit_fpl_id_override`，主控 bug-injection 驗證測試鑑別力 |
| DEF-101-324 | 2026-07-24 | R39 一審 SD（新角度 bug-injection：碰撞而非穿越） | **`state_loader._sanitize_component()` 對不同輸入的多對一碰撞會造成靜默覆蓋**：淨化把 `<>:"\|?*` 與控制字元統一替換為 `_`，`"AC:042"` 與 `"AC/042"` 淨化後皆為 `"AC_042"`；`production_to_fpl.py::generate_fpl_draft()` 因檔名不含時間戳，兩個自然可能同時存在（不同分隔符慣例、非惡意構造）的生產遙測識別碼會靜默覆蓋彼此的 advisory FPL 草案（非路徑逃逸，是資料遺失）；`hub_merge.py`/`sandbox_runner.py`/`path_cost.py` 因檔名含 timestamp/date 緩衝，觸發門檻較高（需同秒/同日巧合） | P3（同 DEF-101-308/309/313 同類：測試鑑別力縫隙，四方一致判定非阻擋；`production_to_fpl.py` 因無時間戳緩衝觸發門檻略低） | 若要根本解需為碰撞情境加唯一性後綴（如短 hash）或偵測到既存同名檔時提高門檻改名，超出本輪「檔名淨化」範圍比例原則，列 backlog | open（backlog，四方一致判定非阻擋，如實記載列入下一輪追蹤） |
| DEF-101-325 | 2026-07-24 | R39 Scan-D（文件/帳本一致性掃描，主控自我 housekeeping 產物） | 主控本輪 housekeeping 新建 `docs/06_quality/AutoSDD_Defect_Log_archive_11.md` 時，帳本「已歸檔內容」的 bullet 描述其大小為「≈2.9KB」，但實際 `wc -c` 為 9,028 bytes（≈8.8KB），偏差約 3 倍——與 DEF-101-306（R35 發現 archive_06 大小描述誤差 79%）同一根因、同一位置第三度復發 | P4（純文件性，不影響任何機械閘門判準） | 本輪修復 | fixed@R39：訂正為「≈8.8KB」，SA/Architect 一審獨立核對 `wc -c` 實測 9,028 bytes 與訂正後描述吻合 |
| DEF-101-326 | 2026-07-24 | R39 Scan-D（文件/帳本一致性掃描）；一審 SA 精修範圍 | **帳本「R38 四方複審裁決總結」段落兩處（前置基線、收尾驗證）引用「AutoClaude pytest 3751 passed/146 skipped」皆未加註污染澄清**，與緊鄰的「R37 四方複審裁決總結」段落已正確標註「（乾淨 venv 3653/210）」的既定慣例不一致；`146` 為 R32 已記載的受污染 venv（`psycopg2`/`sqlalchemy` 意外裝入 PG-gated 測試從 skip 轉 pass）特徵值，QA 一審獨立以 `import psycopg2` 實測確認本輪根層共用 `.venv` 確實仍受此污染，`ONBOARDING.md` §7 官方乾淨基線為 3,653/210（R37 校正）| P3（文件性，數字曾用於「與前一輪一致、無回歸」之相對比對，非本輪新測，不影響本輪程式碼正確性判定） | 本輪修復 | fixed@R39：依 SA 一審條件，於「R38」段落兩處引用皆補「**R39 校正**」callout 說明受污染 venv 現況與官方基線出處，格式比照 `ONBOARDING.md` 既有「R33 校正」/「R37 校正」慣例 |
| DEF-101-327 | 2026-07-24 | R40 Scan-A（檔名/路徑相容性掃描，全新呼叫點） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/hub_sync.py::HubSyncClient.diff()` 的 `rule_id` 未淨化即組進 `cache_dir.rglob(f"{rule_id}.*")` 與 `local_paths` 兩處路徑**，構造 `rule_id="../"*N+"secret_dir/leak"` 實測成功讀出 repo 樹外任意檔案內容並透過 `difflib.unified_diff` 回傳給呼叫端（CLI 直接可達，非 dormant scaffold）——本輪最嚴重發現 | P0（路徑穿越／任意檔案讀取，活路徑） | 本輪修復 | fixed@R40：`safe_rule_id = _sanitize_component(rule_id)` 接線（`DiffResult.rule_id` 顯示欄位維持原始值，僅路徑組裝淨化）；新增 `TestDiffPathTraversal` 4 個測試（路徑穿越×2、保留裝置名、正常值回歸），四方一審二審皆 APPROVE，含 SD 對抗式驗證（雙重編碼/NUL/UNC/padding-bypass/大小寫混合保留名等向量）與 Architect 獨立核對無循環 import |
| DEF-101-328 | 2026-07-24 | R40 Scan-A（檔名/路徑相容性掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/counterfactual_replay.py::write_report()` 組 `REPLAY-{patch.ac_id}-{date}.md` 檔名時，`patch.ac_id` 未淨化即組入；Windows 禁用字元（如 `ac_id="AC:042"`，DEF-101-324 backlog 已記載為真實存在樣式）會產生 Windows 非法檔名（保留裝置名/路徑穿越因固定前綴融合關係不構成實際可利用漏洞，已驗證排除）；目前無生產呼叫路徑（dormant scaffold），但淨化缺口與同目錄其餘檔案已收斂的 SSOT 模式不一致 | P2 | 本輪修復 | fixed@R40：`safe_ac_id = _sanitize_component(patch.ac_id) if patch.ac_id else "unknown"`（報告內文標題仍顯示原始 `ac_id`，僅檔名淨化，`None`/空字串 fallback 行為不變）；新增 `test_counterfactual_replay_sanitizer.py` 5 個測試，四方一審二審皆 APPROVE |
| DEF-101-329 | 2026-07-24 | R40 Scan-C（CI 腳本雙軌對等性掃描，追查凍結基線持續漂移根因） | **`AISDLC_SDD_v0.01/`（凍結基線）持續被自動化 bot 原地寫入**：`sdd-drift-bot`／`sdd-arch-fitness-bot` 每日/每週對 `build/reports/drift/DAILY-*.md`（31 個）與 `fse/TREND.yaml`（1 個）執行 commit/push；根因為 v0.01 `.gitignore` 是 v0.05+ 建立「`build/reports/*` 整段排除」政策前的舊版窄清單，從未涵蓋這兩類檔名 | P2（凍結基線治理缺口，「凍結＝不會再變」假設被實證打破） | 本輪修復 | fixed@R40：`AISDLC_SDD/.gitignore` 補上排除規則；`git rm --cached` untrack 32 個既有追蹤檔案（磁碟保留，比照 v0.05+ 既定政策，`git ls-files` 確認 26 個既有版本一致不追蹤 TREND.yaml）；`ci-gate.sh` 全綠（v0.01:1475、v0.30:1718、共享 188），四方一審二審皆 APPROVE（含 Architect 獨立查證 `write_trend()`/`write_daily_report()` 純磁碟輸出邏輯不受影響） |
| DEF-101-330 | 2026-07-24 | R40 一審 SA（發現 DEF-101-329 修復的真實副作用） | **DEF-101-329 修復後，`.github/workflows/aisdlc-sdd-drift-daily.yml`／`aisdlc-sdd-arch-fitness.yml`（nightly-strict）對 v0.01 的 `git add` 因 gitignore 生效而靜默失效**，兩支 workflow 從此「照跑但永遠無實質效果」卻仍每日/每週消耗 CI 分鐘，與使用者既有「節省 GitHub CI 額度」偏好衝突 | P2 | 本輪修復 | fixed@R40：兩支 workflow 改用 `actions/upload-artifact@v5`（90 天保留）取代 commit/push；同步移除不再需要的 `main-push-serialize` concurrency 群組與 `contents: write` 權限（降為 `contents: read`）；`--strict` structural-fail 判紅邏輯不受影響；四方複審皆 APPROVE（Architect：移除推送競爭根因優於序列化補丁，架構方向更健康） |
| DEF-101-331 | 2026-07-24 | R40 Architect 架構最佳化深度評估（依 R39 建議落地） | `tools/tests/test_windowsapps_guard_cross_consistency.py` 既有的 `TestWindowsAppsGuardEnrollment` 只鎖 3 個已知具名呼叫端，非 repo-wide 掃描——WindowsApps guard 缺陷類別已復發 5 次（DEF-101-273/279/300/303/317），新呼叫點無機械鎖攔截 | P2（架構前瞻性補強） | 本輪新增 | fixed@R40：新增 `TestNoOrphanWindowsAppsImplementation` 三個測試方法（PS1 側 SSOT dot-source 委派檢查、已知呼叫端防腐化、Python `_is_windows_apps_stub` 唯一定義檢查），依 Architect 給出的完整技術方案落地，掃描範圍正確排除 AISDLC_SDD 凍結版本；四方一審二審皆用 bug-injection 獨立驗證鑑別力（注入偽造違規檔案/移除已知檔案/重複定義），詳見 DEF-101-332/333 一審二審過程中發現的殘留繞過向量 |
| DEF-101-332 | 2026-07-24 | R40 一審 SD 對抗式驗證（阻擋項） | **DEF-101-331 新增測試的判定邏輯可被繞過**：`test_ps1_mentions_of_windowsapps_all_go_through_ssot` 原本只驗證「文字是否同時提及兩個 SSOT 相關子字串」，SD 構造出一個完全獨立重寫判斷邏輯、從未 dot-source SSOT、只在**行尾裝飾性註解**裡提及這兩個子字串偽裝的 `.ps1` 檔案，成功騙過測試（PASSED，本該 FAILED）——形同虛設 | P1（安全把關測試本身的鑑別力缺口，阻擋本輪收斂） | 本輪修復（阻擋項，四方複審前必須收斂） | fixed@R40：改用 `_DOT_SOURCE_SSOT_RE`／`_TEST_IS_REAL_PYTHON_CALL_RE` 正則比對「陳述式開頭的真實 dot-source 語法＋非註解非字串常值的實際呼叫」，並新增 `_strip_trailing_line_comment()` 濾除行尾裝飾性註解（QA 二審用此手法構造第二種繞過案例後追加）；SD 原始攻擊案例與 QA 追加案例皆重新驗證轉紅，既有 3 個合規呼叫端維持綠燈，四方複審 APPROVE |
| DEF-101-333 | 2026-07-24 | R40 二審 Architect／QA 各自獨立構造（新角度攻擊，非本輪必須修復） | **DEF-101-332 修復後仍有兩類殘留繞過向量，三方（SD/Architect/QA）分別於一二審獨立構造成立**：(a) 檔案中存在「真實但死碼」的 dot-source SSOT 陳述式，實際生效判斷邏輯是另一個完全獨立重寫的函式，`Test-IsRealPython` 只出現在從未被呼叫的死碼分支（Architect 二審構造）；(b) 把兩段魔法字串包進 PowerShell here-string（`@"..."@`）當誘餌，逐行引號奇偶追蹤不追蹤跨行 here-string 開闔狀態（QA 二審構造）。四方一致判定：這是逐行正則靜態掃描的方法論邊界，需要真正的 PowerShell AST 解析才能完全封閉，依 Rule 2 比例原則本輪不強制修復 | P3（已知限制，非阻擋；已在測試 docstring 誠實記載，防止被誤讀為「已完全防禦」） | 若未來需徹底封閉需 AST 層解析（追蹤變數賦值實際使用、正確處理 here-string 狀態機），列 R41 backlog | open（backlog，四方一致判定非阻擋，已在 `test_windowsapps_guard_cross_consistency.py::TestNoOrphanWindowsAppsImplementation` docstring 誠實記載殘留限制） |
| DEF-101-334 | 2026-07-24 | R40 一審 QA（測試鑑別力審查） | **`test_hub_sync.py::test_diff_windows_reserved_device_name_does_not_crash` 對保留裝置名淨化邏輯無真實鑑別力**：把 `hub_sync.py::diff()` 的 `_sanitize_component()` 呼叫整個拔掉，此測試仍是綠燈——只驗證「不崩潰」，因測試 fixture 本來就沒有 `CON.yaml` 這類檔案，「找不到對應檔案」在淨化與否兩種情境下皆為真 | P2（測試鑑別力縫隙） | 本輪修復 | fixed@R40：改為雙層驗證——① 直接斷言 `_sanitize_component("CON")` 輸出確有淨化痕跡（`_CON`，非原樣）；② 在淨化後檔名下建立真實檔案，驗證 `diff()` 呼叫鏈真的用淨化後檔名找到該檔案。QA 二審用「只破壞其中一層」的新角度重新攻擊，確認兩層各自獨立具鑑別力，Architect 複審獨立驗證轉紅轉綠，皆 APPROVE |
| DEF-101-335 | 2026-07-24 | R40 一審 SD 對抗式驗證（新呼叫點碰撞情況擴大範圍，同 DEF-101-324 類別） | `hub_sync.py::diff()` 與 `counterfactual_replay.py::write_report()` 兩個新收斂的 SSOT 呼叫點同樣命中既有 DEF-101-324（`_sanitize_component()` 多對一碰撞）：`rule_id="SLV:001"`/`"SLV/001"`/`"SLV\001"` 皆淨化為同一 `SLV_001`；`ac_id="AC:042"`/`"AC/042"`/含 NUL 皆淨化為同一 `AC_042`。SD 評估：`diff()` 情境下呼叫者本就有完整檔案系統存取權，非跨權限邊界的資訊洩漏，與 DEF-101-324 既有判定同級 | P3（同 DEF-101-324 類別，四方一致判定非阻擋） | 併入 DEF-101-324 既有 backlog 追蹤，不需獨立修復 | open（backlog，記錄 DEF-101-324 命中範圍擴大至本輪兩個新呼叫點，現況與既有判定一致） |
| DEF-101-336 | 2026-07-24 | R40 Scan-C（CI 腳本雙軌對等性掃描，追查凍結基線歷史一致性） | **DEF-101-056 記載已與現況不符**：該筆宣稱「v0.01～v0.29 依框架治理規則不予回改」，但兩天後的 commit `687abac`（2026-07-15）實際回改了 28 份「凍結版」`install_post_commit.ps1`（`Out-File -Encoding ascii` 改 `WriteAllText`），直接違反凍結基線 Copy-on-Evolve 鐵律，且**選擇性**回改（同批姊妹缺陷 DEF-101-057 未被回改，`v0.15` 現況第 4 行仍硬編 `.git\hooks\post-commit`）——DEF-101-056 記載成為與實況矛盾的假記事 | P2（帳本記載與實況矛盾，且揭露凍結基線鐵律曾被實際打破而無機械訊號攔截） | 本輪記事訂正 | open（如實記載此矛盾，不回填 DEF-101-056 原文，比照帳本「只增不刪」政策；是否需要為凍結版新增「禁止 commit」機械鎖列 R41 backlog 評估） |
| DEF-101-337 | 2026-07-24 | R40 一審 SA／二審 Architect（範圍精修） | **`main-push-serialize` concurrency 過時描述殘留於 62 個文件**：DEF-101-330 修復移除該 concurrency 群組後，30 個版本目錄各 1 份 `README.md`＋1 份 `cicd/SDD_CICD_BASE_LAYER.md`（共 60）＋根層 `ADR-001-local-first-ci-parity.md`（1）＋本帳本自身（1）仍描述舊設計。二審 Architect 區分：**v0.01～v0.29 共 29 個凍結版本目錄**依 Copy-on-Evolve 鐵律延後合理；但 **`AISDLC_SDD_v0.30`（LATEST，非凍結）之 `README.md`／`cicd/SDD_CICD_BASE_LAYER.md`，以及根層 `ADR-001`** 是現行/決策歷史文件，範圍性質不同 | P3（文件性，v0.30/ADR-001 部分不應與凍結版歷史敘述一併延後） | v0.30 兩檔本輪立即訂正；ADR-001 依 DEF-101-198 既定慣例（歷史敘述不改，追記現況說明）處理；v0.01～v0.29 共 29 個凍結版本目錄列 R41 backlog | fixed@R40（v0.30/ADR-001 部分）：`AISDLC_SDD_v0.30/README.md`／`cicd/SDD_CICD_BASE_LAYER.md` 訂正為現行狀態描述；`ADR-001-local-first-ci-parity.md` 追加「R40 後續更新」callout（原歷史敘述不改）。**open（v0.01～v0.29 凍結版本目錄，backlog R41）** |
| DEF-101-338 | 2026-07-24 | R40 修復 v0.01 gitignore 缺口時副帶發現（未處理，供追蹤） | `AISDLC_SDD_v0.01/build/reports/drift/COMMIT-sha-low.yaml`／`COMMIT-sha-high.yaml`／`COMMIT-sha-3rd.yaml`／`COMMIT-testsha-001.yaml` 命名像測試假 SHA，疑似測試執行時未用 `tmp_path` fixture、誤寫入真實 repo 路徑而被意外 commit 追蹤，與 DEF-101-329（bot 每日產生）性質不同 | P3（疑似測試隔離缺陷，非本輪掃描範圍） | 建議下一輪調查根因（是否有測試未用 `tmp_path`） | open（backlog R41，本輪未處理，不在四方複審範圍內） |
| DEF-101-339 | 2026-07-24 | R40 Scan-D（文件/帳本一致性掃描） | 帳本「已歸檔內容」archive_02／archive_03 大小描述精度低於近期慣例：`archive_02.md` 描述「≈177KB」實測 183,179 bytes=178.9KB（差 1.9KB／1.07%）；`archive_03.md` 描述「≈110KB」實測 109,563 bytes=107.0KB（差 3.0KB／2.7%）——其餘 11 個 archive 描述皆在 ±0.5KB 內 | P4（純文件性，相對誤差遠低於 DEF-101-306/319/325 案例） | 本輪修復 | fixed@R40：訂正為「≈178.9KB」／「≈107.0KB」，主控直接核對 `wc -c` 實測值後訂正 |
| DEF-101-340 | 2026-07-24 | R40 Scan-C（CI 腳本雙軌對等性掃描） | `AutoClaude/tools/sd06_w3_staging_dryrun.sh` 檔頭註解（「不支援在非 GNU date 的老舊 BSD 環境執行，Windows 請走 WSL」）與 `ONBOARDING.md:137`（歸類為「bash-only 工具…Windows 上以 Git Bash 執行」）表述不一致；查證 Git for Windows 內建 Git Bash 之 `date` 實為 MSYS2 移植的 GNU coreutils（支援 `%N`），故 Git Bash 實際可正常執行，純屬文件用詞層級不一致，非功能性缺陷 | P3（文件性，實務風險低） | 本輪修復 | fixed@R40：腳本註解補充「Windows 上 Git Bash 內建 MSYS2 移植的 GNU coreutils date（支援 %N），與 ONBOARDING.md §6 一致，R40 跨平台輪實測確認」，與既有文件記載對齊 |
| DEF-101-341 | 2026-07-24 | R41 Architect 架構全面檢視 | **`_sanitize_component()`（AISDLC_SDD 側檔名淨化 SSOT，定義於 `state_loader.py`）呼叫點從未獲得 repo-wide 前瞻防增生鎖**：同類檔名淨化缺陷歷經 R38～R40 多輪（DEF-101-219/295/324/327/328/334）在不同呼叫點被逐一發現修復，其中 DEF-101-327 更是真實可利用路徑穿越 P0，但直到 R41 都只有「Scan 掃到才補」的被動模式，未像 WindowsApps guard 類別（R37 DEF-101-303／R40 DEF-101-331）那樣建立機械化前瞻鎖防止「未來新增呼叫點忘記淨化」 | P2（架構層系統性預防缺口，非現存可利用漏洞，但屬「單點修復掩蓋系統性設計缺陷」的教科書案例） | 本輪修復 | fixed@R41：新增 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_sanitize_component_call_site_lock.py`。**四方一審對此新測試的三項獨立發現皆已修復**（非僅記載，實質補強）：① Architect 一審發現 `_iter_module_files()` 用非遞迴 `glob("*.py")` 只掃頂層、遺漏 `modality/`/`formal/`/`meta_halt/`/`templates/` 四子目錄共 86 檔的真實涵蓋率破口→改用 `rglob("*.py")`（排除 `tests/`/`__pycache__`/`__init__.py`）；② SD 一審對抗式驗證構造出 `+`/`%`/`.format()` 字串拼接與 `ast.Subscript`（`record["rule_id"]`）四種非 f-string 組檔名寫法完全偵測不到（日常寫法、非刻意規避）→新增 `_binop_add_leaves`/`_mod_format_operands`/`_format_call_operands` 三個輔助函式擴充偵測，`_raw_risky_reference` 擴充涵蓋 `Subscript`；③ QA 一審發現最根本的設計缺陷——原始版本用「即時自舉」風險名單（每次從當前程式碼重新掃描哪些識別字仍被 `_sanitize_component()` 呼叫），移除某識別字**唯一**呼叫點時該識別字會同時從名單消失，導致測試巧合通過（9 個風險識別字中 6 個僅單一呼叫點、完全失效）→改為 `_FROZEN_RISKY_NAMES` 凍結快照（不隨呼叫點增減變動），新增 `test_live_bootstrap_is_subset_of_frozen_list` 做新鮮度檢查防止清單腐化。`_ADDITIONAL_RISKY_NAMES`（登記透過委派 wrapper 間接淨化的 `nfr_id`）、`_GENERIC_STOPWORDS`（排除 text/value/name/path/content/data 等通用詞）、`_KNOWN_EXEMPTIONS`（登記 `slv_generator.py::fpl_id`，經核實其 `_fpl_path()` 先有 `FPL_ID_RE=r"^FPL-\d{3,}$"` allow-list 格式驗證擋下非法輸入，屬等效防護）三項機制維持不變。Bug-injection 雙向驗證（一審+複審皆各自獨立重新構造）：重演 DEF-101-327 情境（`hub_sync.py` 裸用 `rule_id`）即刻變紅；清空 `_KNOWN_EXEMPTIONS` 變紅；QA 對 6 個單一呼叫點識別字（app_id/classification/divergence_kind/fpl_id/subagent/track_id）逐一複驗唯一呼叫點消失後仍能偵測；Architect/SD 複審各自在 `modality/`/`formal/` 子目錄獨立構造繞過案例確認 rglob 與四種非 f-string 寫法偵測皆生效。**複審新發現 1 項未修復、誠實記載為已知邊界**（SD 二審）：`%`/`.format()` 偵測要求樣板字面值緊鄰運算子，若樣板存於模組層具名常數會繞過——全 repo 現況零實例、比照 R40 WindowsApps guard 判例（具體案例收斂、假設性更深繞過記載不強修）僅記載於 docstring，留待下輪視是否出現真實呼叫點再評估。四方複審最終裁決：Architect APPROVE、QA APPROVE、SA APPROVE-with-conditions（帳本治理條件，見 DEF-101-342；另發現的 `production_to_fpl.py` 疑慮經核實為並行 agent 暫態污染、非真實缺陷）、SD APPROVE-with-conditions（上述 `%`/`.format()` 邊界記載條件，已如實補上）。全套 `fsm_runtime` 回歸 1722 passed/7 skipped（較 R40 基線 1718 多 4：本項 3 + 新鮮度檢查測試 1） |
| DEF-101-342 | 2026-07-24 | R41 SA 一審獨立發現（帳本治理流程） | **DEF-101-314 測試案例數 off-by-one 訂正手法違反本帳本 R40 剛立下的 DEF-101-336 先例**：Scan-D 發現 DEF-101-314 記載「`NfrIdSecurityTests` 13 case」與親跑 `pytest --collect-only` 實測「12 個 `test_` 方法」不符，主控最初直接在 DEF-101-314 原條目內覆寫「13 case」為「12 case」；但 DEF-101-336 明文立下「不回填原文，比照帳本『只增不刪』政策」的處理慣例（同輪帳本標頭亦明文「跨輪累積、只增不刪」），直接覆寫違反此剛確立的先例 | P4（帳本治理流程一致性，不影響任何機械閘門判準或程式碼/測試正確性——後者皆已由 SA/QA 獨立親驗無誤） | 本輪訂正 | fixed@R41：DEF-101-314 原文「13 case」已改回不動；本條另立記載此次記載數字落差核實結果（`pytest tools/fsm_runtime/tests/test_production_monitor.py -k NfrIdSecurityTests --collect-only -q` 由 Scan-D／SA 兩位獨立 agent 各自核實皆為 12 個 collected test_ 方法），日後若需訂正 DEF-101-314 本文，應依 DEF-101-336 先例另開新條目，不覆寫原文 |
| DEF-101-343 | 2026-07-24 | R42 本機真實 nightly（2026-07-24 21:17，`AutoClaude/logs/nightly_2026-07-24_211738.log`）實測失敗，四方一審彙整交付修復 | **`AutoClaude/tests/plugins/test_checkpoint_plugin.py::TestW3PersistenceRequestPositive::test_escalation_dump_sanitizes_step_id_with_windows_forbidden_chars`（R37 新增）斷言範圍過大，在任何真實 Windows 檔案系統上物理上不可能通過**：斷言 `for ch in _WIN_FORBIDDEN_CHARS: assert ch not in dump.last_log_path` 檢查的是整條絕對路徑字串，而 `last_log_path = str(Path(log_dir) / last_log_filename)`（`_escalation.py:54`）在 Windows 上必然含磁碟機代號 `:` 與路徑分隔符 `\`，兩者皆在 `_WIN_FORBIDDEN_CHARS` 內。本機（真實 Windows 11）重跑確認 FAIL：`AssertionError: ':' is contained here: C:\Users\...\attempt0.log` | P1（測試設計缺陷，非 production 邏輯缺陷；但阻擋本機 nightly 全綠，且理論上任何 Windows 開發機重跑此測試皆會炸） | 本輪修復 | fixed@R42：斷言範圍收斂為只檢查檔名分量（`Path(dump.last_log_path).name`），比照同檔案上一行（368 行）與 `AutoClaude/tests/test_models.py:198/209` 既有慣例寫法。驗證：修復前重跑 `pytest tests/plugins/test_checkpoint_plugin.py::TestW3PersistenceRequestPositive::test_escalation_dump_sanitizes_step_id_with_windows_forbidden_chars -v` 確認 FAIL（`':' is contained here`）；修復後重跑確認 PASSED |
| DEF-101-344 | 2026-07-24 | R42 QA 一審用真實 PowerShell 重現，Architect/SD/QA 四方交付判斷正確修法方向 | **`tools/tests/test_windowsapps_guard_cross_consistency.py::TestDevStartPs1WindowsAppsGuard::test_real_python_outside_windowsapps_is_used_even_when_windowsapps_stub_present_first`（R37 新增）測試期待本身與 `dev_start.ps1`/`Test-IsRealPython` 現行架構物理矛盾**：`dev_start.ps1` 只有單一 `python` 候選名稱，`Test-IsRealPython`（`tools/lib/WindowsAppsGuard.ps1:46-56`）只取 `Get-Command $CandidateName` 的**第一個**解析結果，呼叫端拿到布林值後一律用**候選名稱字面值**（非解析出的完整路徑）去實際呼叫（`& $Py` 其中 `$Py='python'`）。測試建構「WindowsApps 空殼排前面、真直譯器排後面」的 PATH，卻期待 dev_start.ps1 最終會呼叫到後面那個真直譯器——本機真實 PowerShell 實測必定 FAIL（`$Py` 保持 `$null`，程式印出「❌ 找不到 Python 直譯器」）。本機額外構造實驗驗證：對此 PATH 佈局，`& 'python'`（裸名稱呼叫）與 `Get-Command python` 皆解析到 PATH 最前面的 WindowsApps 空殼（`Program 'python.exe' failed to run: The specified executable is not a valid application for this OS platform`），只有直接用完整路徑呼叫才會命中後面的真直譯器——證實「若要讓 guard 跳過空殼，`Test-IsRealPython` 必須改回傳完整路徑且三個呼叫端都要跟著改用該路徑呼叫」，否則「guard 說安全、實際執行還是空殼」的靜默失敗比現狀「誠實回報找不到並停下」更危險 | P0（測試設計缺陷；判斷錯誤修法方向的風險等級為 P0——若誤修 production 邏輯而未同步三處呼叫端，會製造比現狀更嚴重的靜默失敗） | 本輪修復（修測試期待，不動 production 邏輯） | fixed@R42：`tools/tests/test_windowsapps_guard_cross_consistency.py` 內該測試更名為 `test_windowsapps_stub_present_first_is_rejected_not_silently_bypassed`，斷言改為驗證「WindowsApps 空殼排前面時，guard 正確拒絕、印出『找不到』、且標記字串 `FAKE_PYTHON_INVOKED` 不出現」，docstring 詳述判斷理由與既有迴避手法記載（`tools/tests/test_bootstrap_ps1.py` 同名測試因 `bootstrap.ps1` 有 `python`／`python3` 兩個候選名稱，把空殼與真直譯器分屬不同候選名稱而迴避掉此問題；`dev_start.ps1` 只有單一候選名，無法比照迴避）。**複查發現同構第二例**：`AISDLC_SDD/scripts/tests/test_install_post_commit_windowsapps_guard.py::test_real_python_outside_windowsapps_is_used_even_when_windowsapps_stub_present_first_real_pathext` 因 `install_post_commit.ps1`（第 61 行 `Test-IsRealPython -CandidateName 'python'`）同樣只有單一候選名稱，繼承同一設計缺陷，一併同款修正（見 DEF-101-345）。驗證：修復前重跑確認 FAIL（`assertIn("FAKE_PYTHON_INVOKED", ...)` 失敗，實際輸出為「找不到」錯誤）；修復後 `tools/tests/test_windowsapps_guard_cross_consistency.py` 全部 17 個測試 PASSED |
| DEF-101-345 | 2026-07-24 | R42 QA 一審在本機（繁體中文 Windows 11，Big5/950 codepage）重現 | **`AISDLC_SDD/scripts/tests/test_install_post_commit_windowsapps_guard.py` 兩處測試 fixture 缺陷（非 `install_post_commit.ps1` production 邏輯缺陷）＋一處與 DEF-101-344 同構的測試期待缺陷**：(A) `_run_with_shadowed_python()` 未比照同檔案 `_run_with_path()` 加上 `[Console]::OutputEncoding=[System.Text.Encoding]::UTF8;` 前綴，本機繁中 codepage 下 3 支測試（`test_windowsapps_stub_python_is_rejected_with_clean_error`／`test_windowsapps_segment_match_is_case_insensitive`／`test_missing_python_still_reports_clean_error`）斷言 `"找不到" in output` 因輸出被錯誤編碼解讀成亂碼而 FAIL；(B) `_run_with_path()` 把子行程 PATH 整個換成只有 stub_dir／[stub_dir, real_dir]，完全排除 git.exe 所在目錄，導致 `install_post_commit.ps1` 第 16 行 `git rev-parse --git-common-dir` 在尚未執行到 guard 邏輯前就先因「'git' 詞彙無法辨識」中止，2 支 `*_real_pathext` 測試 FAIL；(C) 其中 `test_real_python_outside_windowsapps_is_used_even_when_windowsapps_stub_present_first_real_pathext` 修完 (A)(B) 兩層 fixture 後仍 FAIL，追查為與 DEF-101-344 完全同構的測試期待缺陷（`install_post_commit.ps1` 同樣只有單一 `python` 候選名稱）。本機重跑 `pytest AISDLC_SDD/scripts/tests/test_install_post_commit_windowsapps_guard.py -v` 修復前確認 5 failed / 1 passed | P0（(A)(B) 為測試 fixture 缺陷；(C) 判斷錯誤修法方向風險同 DEF-101-344 為 P0） | 本輪修復 | fixed@R42：(A) `_run_with_shadowed_python()` 補上與 `_run_with_path()` 一致的 `[Console]::OutputEncoding` 設定；(B) 新增 `_git_dir()` 輔助函式（`shutil.which("git")` 解析所在目錄）併入 `_run_with_path()` 建構的 PATH；(C) 該測試更名為 `test_windowsapps_stub_present_first_is_rejected_not_silently_bypassed_real_pathext`，比照 DEF-101-344 同款修正邏輯與理由改寫斷言。底層 `install_post_commit.ps1` guard 邏輯本身經 QA 一審重現驗證無 bug（純測試 fixture／期待問題），本輪未改動該檔案。驗證：修復後全部 6 支測試 PASSED |
| DEF-101-346 | 2026-07-24 | R42 Architect 一審發現的系統性缺口 | **AutoClaude 套件內三處（`infra/adapters/rtm_file_sink.py::_sanitize_name`／`rtm_file_feedback_source.py::_sanitize`／`translation_learning_sink.py::_sanitize`）各自獨立重寫檔名淨化邏輯，皆缺 Windows 保留裝置名（CON/PRN/AUX/NUL/COM[0-9]/LPT[0-9]）防護**，與 SSOT `autoclaude/utils/logger.py::_sanitize_log_filename`（docstring 明文要求套件內呼叫端須 import、不得另寫一份）不對稱，是與 DEF-101-219/295 反覆復發根因同構的系統性缺口。目前唯一呼叫端 `rtm_writeback_plugin.py` 一律用固定字面前綴呼叫（如 `f"RTM-COVERAGE-{project}"`），故目前不可利用，但無機械鎖攔住未來新呼叫點繞過前綴直接命中保留名 | P2（系統性預防缺口，非現存可利用漏洞） | 本輪修復 | fixed@R42：三處改為委派 `from ...utils.logger import _sanitize_log_filename`。委派後發現兩個既有行為需保留（皆已用實際測試驗證，非猜測）：① `_sanitize_log_filename` 只 `rstrip` 尾端空白/句點、不清前導句點/底線，直接委派會讓 `test_path_traversal_sanitized`／`test_append_sanitizes_name`（`tests/infra/test_rtm_file_sink.py`）FAIL（`'..' in p.name`，雖仍在 base_dir 內、無實際路徑穿越，但為既有更保守的字面斷言）——補一層 `.lstrip("._")` 還原此保證；② `_sanitize_log_filename` 對「淨化後整段為空」回傳 `"untitled"`，三模組既有對外行為分別為 `"rtm-report"`（rtm_file_sink／rtm_file_feedback_source）與 `"proposals-report"`（translation_learning_sink），委派後在各自 wrapper 內改寫回原字面值。`tools/tests/test_windows_forbidden_filename_parity.py::TestEscalationModulesReuseSharedSanitizer` 新增三個 `assertIs` 存在性檢查（`test_rtm_file_sink_reuses_shared_sanitizer` 等）鎖住此決策不被靜默推翻。驗證：`python -m pytest AutoClaude/tests/ -k "rtm or translation_learning" -q` 133 passed / 3 skipped（改動前 2 failed，即上述 traversal 兩測試）；`tools/tests/test_windows_forbidden_filename_parity.py` 17 passed（不含既有無關的 `test_bash_flags_every_control_char` `\r` 環境性失敗，見下方說明） |
| DEF-101-347 | 2026-07-24 | R42 SD 一審實驗腳本＋QA 一審 bug-injection 驗證 | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_sanitize_component_call_site_lock.py` 既有「已知盲點」文件段落遺漏三種真實可觸發的組檔名寫法繞過與一項既有記載語氣過輕的盲點**：SD 一審用實驗腳本驗證 (a) 巢狀 f-string（`f"{f'{rule_id}'}.yaml"`）、(b) pathlib `/` 運算子鏈（`out_dir / rule_id / "leaf.yaml"`，`ast.BinOp` 分支只認 `ast.Mod`/`ast.Add` 無 `ast.Div`）、(c) `str.join()`（`"-".join([..., rule_id]) + ".yaml"`，`_format_call_operands` 只認 `.format` 方法名）三者現行 AST 偵測邏輯完全偵測不到；QA 一審額外用 bug-injection 驗證既有 docstring 記載的「重新指派變數再使用」盲點，實測只需多一行變數指派（如 `_renamed_ac_id = ac_id` 再用於 f-string）即可完全繞過偵測，門檻遠比原文語氣暗示的低。全 repo 現況零真實呼叫點採此四類寫法，純理論繞過 | P3（文件記載缺口，比照 Rule 2 比例原則不強制修復偵測邏輯，僅補記） | 本輪修復（僅文件） | fixed@R42：於該檔案「方法論邊界」段落（原第 57-84 行）追加上述三種寫法繞過的具體技術理由，以及「變數重新指派繞過比想像中容易觸發，非需刻意規避構造的邊界案例」的語氣修正，皆註明零真實呼叫點、比照 Rule 2 不新增偵測邏輯。驗證：`python -m pytest tools/fsm_runtime/tests/test_sanitize_component_call_site_lock.py -q`（於 `AISDLC_SDD/AISDLC_SDD_v0.30/` 下執行）4 passed，僅文件變更不影響既有測試通過 |
| DEF-101-348 | 2026-07-24 | R42 SA 一審提出「為何本機 pre-push hook 未攔下 DEF-101-343 的 R37 回歸」疑問，主控有限時間查證 | **待查（誠實記載，未能在本輪時限內確認確切根因）**：已確認的正向事實——本機 `git config --get core.hooksPath` 現況正確指向 `tools/git-hooks`（根層 dispatcher），該檔對 push 範圍含 `AutoClaude/` 時會正確轉呼 `AutoClaude/tools/git-hooks/pre-push`，該子 hook 第 5 步跑 `env -u GIT_DIR -u GIT_WORK_TREE python -m pytest tests/ -q --tb=short`（無路徑過濾、無 marker 子集限縮，涵蓋 `test_checkpoint_plugin.py`）；`tools/git-hooks/pre-push` 檔案 mtime 為 `2026-07-22 18:37`，早於引入回歸的 R37 commit `d7164a7`（`2026-07-24 14:59:45`），故 hooks 基礎設施在該次 commit 前即已就緒。**無法確認的部分**：該次 push 是否確實觸發本機 hook 執行（是否用 `--no-verify`／`AUTOCLAUDE_SKIP_HOOKS=1` 跳過）、或該次 push 的來源環境（是否為同一台本機、是否為同一份 `.git` 設定）無法從 git 版本歷史回溯還原（git 不記錄 hook 是否執行或被跳過）。**已排除的替代解釋**：`test_escalation_dump_sanitizes_step_id_with_windows_forbidden_chars`（DEF-101-343）斷言 `ch not in dump.last_log_path` 是對整條路徑字串比對；經程式碼檢視確認此斷言**只有在真正的 Windows 檔案系統路徑（含磁碟機代號 `:` 與 `\` 分隔符）下才可能觸發失敗**——若當時執行 `pytest` 的環境是 POSIX 路徑慣例（如 Linux/WSL/macOS 容器化 agent sandbox），`tmp_path` fixture 產生的路徑不含任何 `_WIN_FORBIDDEN_CHARS` 字元，此斷言會**在任何程式碼版本下都trivially 通過**，即使 hook 確實跑了完整 pytest 也不會抓到——這是本輪能給出、有程式碼層級佐證的最可能解釋，但無法回溯證實 R37 commit 當時的實際執行環境是否即為此情況 | P3（流程／方法論疑問，非程式碼缺陷；不影響本輪任何修復項的正確性） | 待查，非本輪程式碼可解 | open（待查）：若未來要徹底封閉此類回歸，建議兩個方向並行評估（留待下輪）——① 對 Windows 專屬字元/路徑類回歸測試，於 CI／nightly 層級加一道「本測試必須在原生 Windows 檔案系統下執行」的環境前提檢查（防止在非 Windows sandbox 下 trivially 通過而未被察覺）；② 稽核歷次「四方複審」round 的 push 是否確實逐次經過本機 pre-push 完整 pytest（例如檢查各輪 commit 訊息／agent 執行紀錄是否有 `--no-verify` 或 `AUTOCLAUDE_SKIP_HOOKS=1` 使用痕跡） |
| DEF-101-349 | 2026-07-24 | R42 二審 Architect 與 SD 各自獨立實測發現（訂正 DEF-101-346 一審宣稱） | **DEF-101-346 一審宣稱「三處委派 SSOT 後 Windows 保留裝置名防護已生效」為誤判**：Architect 與 SD 二審各自獨立實測 `FileRtmSink(td).write_report("CON", "x", fmt="yaml")`，發現落地檔名為裸的 `CON.yaml`，保留名防護完全沒生效。根因：一審在委派 `_sanitize_log_filename` 後補的 `.lstrip("._")`（用途是保留既有「路徑穿越不留字面 `..` 前綴」斷言）會把 SSOT 為保留裝置名補上的前導逃逸底線（`"CON"` → `"_CON"`）一併剝除，`"_CON".lstrip("._")` 又變回裸 `"CON"`——wrapper 自己把 SSOT 的保留名防護後製抵銷掉。DEF-101-346 新增的三個 `assertIs` 鎖（`tools/tests/test_windows_forbidden_filename_parity.py::TestEscalationModulesReuseSharedSanitizer`）只驗證「是否 import 到同一顆 SSOT 函式物件」（import identity），未驗證「wrapper 有沒有把 SSOT 的防護效果後製抵銷掉」，故完全抓不到此問題——這正是本缺陷能在 R42 一審收斂後仍溜過的盲區 | P1（一審宣稱已修復但實際未生效，阻擋級誤判；經二審抓到後已於本輪內收斂，不影響最終交付正確性） | 本輪內（R42）追加修復 | fixed@R42（二審後追加）：三處 wrapper（`rtm_file_sink.py::_sanitize_name`／`rtm_file_feedback_source.py::_sanitize`／`translation_learning_sink.py::_sanitize`）改為在 `.lstrip("._")` 之後，對非 fallback 結果**再委派一次** `_sanitize_log_filename`，讓保留名偵測在 lstrip 之後重新執行、補回逃逸前綴（`sanitized = _sanitize_log_filename(name)` → `result = sanitized.lstrip("._") or <fallback>` → 非 fallback 時 `return _sanitize_log_filename(result)`）。修復前後皆實測驗證（非猜測）：`CON`/`con`/`NUL`/`PRN`/`COM1`/`LPT9` 修復前一律裸露輸出（如 `"CON"→"CON"`），修復後一律加前導底線逃逸（如 `"CON"→"_CON"`，大小寫不敏感）；`../../etc/passwd` 路徑穿越防護不受影響（仍消毒為 `etc_passwd`，無 `..` 前綴殘留）；`RTM-COVERAGE-con` 非裸保留名，維持原樣不誤傷；空字串／`...` 仍正確 fallback 為各模組原本字面值（`rtm_file_sink`/`rtm_file_feedback_source`＝`"rtm-report"`，`translation_learning_sink`＝`"proposals-report"`，兩者不同、未假設一致）。新增 3 個行為級回歸測試（`tests/infra/test_rtm_file_sink.py::TestFileRtmSink::test_reserved_device_name_not_written_bare`、`tests/infra/test_rtm_file_feedback_source.py::TestReservedDeviceNameSanitization::test_sanitize_reserved_name_writes_safe_real_file`、`tests/infra/test_translation_learning_sink.py::test_sanitize_reserved_name_writes_safe_real_file`），皆實際落地寫入磁碟檔案後檢查真實檔名 stem 不得裸等於保留名，補上 DEF-101-346 assertIs 鎖漏掉的「wrapper 後製行為」驗證缺口。驗證：`python -m pytest AutoClaude/tests/ -k "rtm or translation_learning" -q` 136 passed/3 skipped；`python -m pytest AutoClaude/tests/ -q` 3659 passed/207 skipped（無回歸）；`python -m pytest tools/tests/test_windows_forbidden_filename_parity.py -q` 17 passed（既有與本次修復無關的 `test_bash_flags_every_control_char`（`\r` 環境性失敗）在乾淨樹上同樣失敗，非本次改動引入）|

## R41 四方複審裁決總結（2026-07-24）

本輪使用者要求同 R16 起既有固定格式：全面掃描四維度＋Architect 架構最佳化評估，發現問題即修復，再經 Architect/SA/SD/QA 四方獨立審查至全數 APPROVE。

- **前置基線**：帳本主檔動工前 267,135 bytes，超過 256KB 上限，動工收尾前優先執行 housekeeping：把 R40 敘事段落搬遷至新建 `AutoSDD_Defect_Log_archive_15.md`。
- **全面掃描（Scan-A/B/C/D 四維度 + Architect 架構深度評估，皆背景 agent 獨立平行執行）**：Scan-A（安全漏洞路徑穿越）、Scan-B（檔名淨化一致性）、Scan-C（CI/CD 排程機制）皆零新發現；Scan-D（文件帳本一致性）找到 DEF-101-314 測試案例數 off-by-one 記載誤差（P4）；Architect 深度評估發現系統性缺口——`_sanitize_component()` 呼叫點歷經 R38～R40 多輪反覆點狀修復（DEF-101-219/295/324/327/328/334），卻從未像 WindowsApps guard 類別（R37/R40）那樣獲得 repo-wide 前瞻防增生鎖，落地新測試 `test_sanitize_component_call_site_lock.py`（DEF-101-341）。
- **修復落地（第一輪）**：Scan-D 發現的帳本數字誤差，主控最初直接覆寫 DEF-101-314 原文，後由 SA 一審發現此舉違反 R40 剛立下的 DEF-101-336「只增不刪」先例，已訂正為原文不動、另立 DEF-101-342 記載核實結果。

### 四方一審（Architect/SA/SD/QA 獨立審查，皆於主工作樹直接操作、不使用 `isolation: worktree`）

- **Architect**：**APPROVE-with-conditions**——核實 `_KNOWN_EXEMPTIONS`（`slv_generator.py::fpl_id`）例外理由站得住腳、bug-injection 雙向驗證通過；發現①掃描邏輯非遞迴 `glob` 只掃頂層、遺漏 4 個子目錄共 86 檔的真實涵蓋率破口（bug-injection 證實），②工作樹內有並行審查者遺留的暫存探針檔導致當下測試無法重現全綠。
- **SA**：**APPROVE-with-conditions**——獨立核實掃描結論屬實、DEF-101-314 數字精確；發現 DEF-101-314 覆寫原文違反 DEF-101-336 先例（帳本治理流程問題）、Architect 本輪系統性發現未登錄帳本（帳本完整性缺口）。
- **SD**：**APPROVE-with-conditions（含 2 項阻擋）**——構造 5 種繞過手法：停用詞繞過、動態派送、例外清單濫用（皆判定為既有方法論邊界，非阻擋）；**阻擋項**：`+`/`%`/`.format()` 字串拼接與 `ast.Subscript` 兩類非 f-string 組檔名寫法完全偵測不到，且屬日常寫法（非刻意規避），與 R40 WindowsApps guard「需刻意繞過」的天生邊界性質不同，判定為真實偵測覆蓋率缺口。
- **QA**：**APPROVE-with-conditions（阻擋）**——發現最根本的設計缺陷：原始「即時自舉」風險名單每次從當前程式碼重新掃描，移除某識別字**唯一**的淨化呼叫點時，該識別字同時從名單消失，測試巧合通過——9 個風險識別字中 6 個（僅單一呼叫點）完全失效，且此縫隙未被誠實揭露。

**針對一審發現的修復（第二輪，主控直接處理，未另派修復包）**：① 掃描邏輯改用 `rglob("*.py")`（排除 `tests/`/`__pycache__`/`__init__.py`）修復非遞迴破口；② 新增 `_binop_add_leaves`/`_mod_format_operands`/`_format_call_operands` 三個輔助函式擴充偵測 `+`/`%`/`.format()`，`_raw_risky_reference` 擴充涵蓋 `Subscript`；③ 風險名單改為 `_FROZEN_RISKY_NAMES` 凍結快照（不隨即時掃描變動），新增 `test_live_bootstrap_is_subset_of_frozen_list` 做新鮮度檢查；④ 帳本治理：DEF-101-314 原文還原、新增 DEF-101-341（Architect 系統性發現）與 DEF-101-342（帳本治理流程記載）。

### 四方複審（SendMessage 保留一審上下文複審）

- **Architect**：**APPROVE**——親自重建 `modality/` 子目錄繞過案例確認 rglob 修復生效；獨立重新構造 SD/QA 的繞過案例確認皆修復；完整回歸 1722 passed 無新迴歸；提醒本輪多位 agent 共用同一物理工作樹造成瞬時污染（已知現象，非本輪缺陷），建議下輪起改用 isolated worktree。
- **SA**：**APPROVE-with-conditions**——核實 DEF-101-341/342 文字精確、DEF-101-314 原文確實已還原；bug-injection 驗證新測試鑑別力；提醒收尾前應確認 `production_to_fpl.py` 未受並行審查暫態污染影響，經主控核實為虛驚（該檔案當下狀態乾淨）。
- **SD**：**APPROVE-with-conditions**——獨立重新構造原 5 種案例，確認案例 3（拼接）與案例 5（Subscript）兩項阻擋項修復生效；新發現案例 6（`%`/`.format()` 樣板存於模組層具名常數會繞過偵測），全 repo 現況零實例，比照 R40 WindowsApps guard 判例（具體案例收斂、假設性更深繞過記載不強修）處理，已補記載於 docstring。
- **QA**：**APPROVE**——逐一複驗全部 6 個單一呼叫點識別字（app_id/classification/divergence_kind/fpl_id/subagent/track_id），凍結快照修復對全部 6 個皆生效；新增的新鮮度檢查測試 bug-injection 驗證有效；完整回歸 1722 passed 與宣稱數字吻合。

**四方複審最終結論：Architect/QA 全數 APPROVE，SA/SD APPROVE-with-conditions（皆為文件層級條件，已如實補上記載，無阻擋程式碼問題殘留）**。本輪 R41 全部異動（`docs/06_quality/AutoSDD_Defect_Log.md`、新檔 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_sanitize_component_call_site_lock.py`、新檔 `AutoSDD_Defect_Log_archive_15.md`）可放行。**已知限制（如實記載於受審測試檔 docstring）**：識別字重新指派後本檢查不追蹤、動態派送（`globals()[...]`）呼叫無實例但屬盲點、`_KNOWN_EXEMPTIONS` 無自動交叉驗證等效防護是否仍在、`%`/`.format()` 樣板存於具名常數會繞過（案例 6，無現存真實呼叫點）。

> 過程中多位並行複審 agent 共用同一物理工作樹進行 bug-injection 驗證，數次觀察到暫態污染（其他 agent 的暫存探針檔／暫改真實檔案的中間態），每次皆自行收斂乾淨、經核實非真實缺陷，與本 repo 既有已知模式一致（見 [[four-party-review-loop]] 記憶）；Architect 複審建議下輪起改用 isolated worktree 降低此類雜訊。

**收尾驗證**：`AISDLC_SDD_v0.30` `fsm_runtime` 全套 1722 passed/7 skipped（較 R40 基線 1718 多 4）、`git status --short` 收尾前確認僅剩本輪交辦異動、無殘留探針檔。

> **R40「四方複審裁決總結」敘事段落已搬遷至 [`AutoSDD_Defect_Log_archive_15.md`](AutoSDD_Defect_Log_archive_15.md)（R41 帳本 housekeeping，逐字保全）。**

> **R39「四方複審裁決總結」敘事段落已搬遷至 [`AutoSDD_Defect_Log_archive_14.md`](AutoSDD_Defect_Log_archive_14.md)（R40 帳本 housekeeping，逐字保全）。**

> **R38「四方複審裁決總結」敘事段落已搬遷至 [`AutoSDD_Defect_Log_archive_13.md`](AutoSDD_Defect_Log_archive_13.md)（R40 帳本 housekeeping，逐字保全）。**

> **R37「四方複審裁決總結（三輪）」敘事段落已搬遷至 [`AutoSDD_Defect_Log_archive_12.md`](AutoSDD_Defect_Log_archive_12.md)（R39 帳本 housekeeping，逐字保全）。**

> **R36「四方一審／二審裁決總結」敘事段落已搬遷至 [`AutoSDD_Defect_Log_archive_11.md`](AutoSDD_Defect_Log_archive_11.md)（R39 帳本 housekeeping，逐字保全）。**

---

## 歷史複驗註記 — 已歸檔（improving_99 起）

> **歸檔政策（DEF-99-001 fixed@improving_99）**：本帳本「跨輪累積、只增不刪」，但中文長列使單檔逼近工具讀取上限（曾達 468KB > Read 256KB）。自 improving_99 起，**歷史敘事複驗/收尾註記**（其缺陷現況已被上方缺陷總表 live 狀態取代，或為已結歷史）搬遷至分檔，**原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）；主檔只保留標頭 + 完整缺陷總表（live SSOT）+ 本指標。**輪替規則：主檔與「每一個」 archive 檔皆以 < 256KB（Read 工具上限）為界**——主檔再逼近即開新 archive；單一 archive 逼近即拆下一個 archive（故本輪一次就把 619 行歷史拆成 archive_01 + archive_02 兩檔，而非單一超限檔）。
>
> **已歸檔內容**（十六檔）：
> - **`AutoSDD_Defect_Log_archive_01.md`**（原主檔 93-543 行，≈170KB）：rounds 24-63 各輪複驗/收尾敘事註記 + 2026-06-22 agent/* 符規審查臨時塊（DEF-AGTREV-001~013 全 fixed@v0.18）+ 早期 `.claude` hooks/skills 審查輪（一~六）。
> - **`AutoSDD_Defect_Log_archive_02.md`**（原主檔 544-711 行，≈178.9KB）：improving_45 追記 + 2026-06-23 `.claude` hooks/skills 審查輪（三~九，DEF-CLDREV-* 系列收斂至零缺陷）。
> - **`AutoSDD_Defect_Log_archive_03.md`**（R9 跨平台複審 2026-07-16 建立，≈107.0KB）：缺陷總表中 **DEF-01～DEF-100 系列已結列（fixed / closed-by-decision）64 列**逐字搬移（主檔當時 272KB 已超 256KB 界線，DEF-101-123）。
> - **`AutoSDD_Defect_Log_archive_04.md`**（R14 跨平台輪 2026-07-20 建立，≈210KB）：缺陷總表**已結列（fixed / closed-by-decision）169 列**逐字搬移（主檔 258KB 進 240KB 預警區；含 R9 時因 crossref 引用留存、現已無引用的 DEF-20-001，及 DEF-101 系列已結列至 DEF-101-187）。
> - **`AutoSDD_Defect_Log_archive_05.md`**（R27 跨平台輪 2026-07-23 建立，≈42KB）：**R16~R26 各輪「四方一審／二審／三審裁決總結」歷史敘事段落**逐字搬移（主檔本輪加入 R27 段落後達 264268 bytes，`check_defect_log_crossref.py` 首次由警告轉為硬性 FAIL；本次搬移對象為敘事段落而非缺陷總表列，性質與前四檔〔搬總表已結列〕不同，屬同一輪替政策下的另一類可安全搬移內容——這些敘事的缺陷現況皆已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_06.md`**（R32 跨平台輪 2026-07-24 建立，≈10.7KB）：**R27 「四方一審／二審裁決總結」歷史敘事段落**逐字搬移（主檔加入 R32 段落後達 258952 bytes，`check_defect_log_crossref.py` 印出「已逼近輪替上限」警告；搬移對象與 archive_05 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_07.md`**（R33 跨平台輪 2026-07-24 建立，≈6.7KB）：**R32 「四方一審／二審裁決總結」歷史敘事段落**逐字搬移（R33 動工前主檔已達 248,860 bytes、距 256KB 僅剩 5.1% 餘量，Scan-D 掃描發現後本輪立即搬移；搬移對象與 archive_05/06 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_08.md`**（R34 跨平台輪 2026-07-24 建立，≈5.3KB）：**R33 「四方一審／二審裁決總結」歷史敘事段落**逐字搬移（R34 動工前主檔已達 254,679 bytes、距 256KB 僅剩 0.5% 餘量，R33 二審 SA 已預先提醒本輪優先規劃歸檔；搬移對象與 archive_05/06/07 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_09.md`**（R34 跨平台輪 2026-07-24 建立，≈51.9KB）：**缺陷總表中 R15~R27 各輪已結列（fixed）12 列**逐字搬移（R34 新增四筆缺陷後主檔達 264,583 bytes、超過 256KB 上限，`check_defect_log_crossref.py` 由警告轉 FAIL；搬移對象為總表已結列，性質同 archive_03/04，非敘事段落；搬遷前逐一確認狀態皆 `fixed@R...` 且未被 `ONBOARDING.md`／兩份 compat-ci workflow 以「DEF-ID(狀態宣稱)」樣式引用，不觸發跨文件矛盾）。
> - **`AutoSDD_Defect_Log_archive_10.md`**（R38 跨平台輪 2026-07-24 建立，≈14.6KB）：**R34、R35「四方一審／二審／三審裁決總結」歷史敘事段落**逐字搬移（R38 動工前主檔已達 246,971 bytes、距 256KB 僅剩 5.8% 餘量，Scan-D 掃描發現後本輪立即搬移；搬移對象與 archive_05/06/07/08 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_11.md`**（R39 跨平台輪 2026-07-24 建立，≈8.8KB）：**R36「四方一審／二審裁決總結」歷史敘事段落**逐字搬移（R38 收尾已提醒帳本主檔逼近 256KB 上限、建議 R39 優先歸檔；R39 動工前主檔 250,943 bytes、已逼近 240KB 預警線，動工前立即搬移；搬移對象與 archive_05/06/07/08/10 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_12.md`**（R39 跨平台輪 2026-07-24 建立，≈3.2KB）：**R37「四方複審裁決總結（三輪）」歷史敘事段落**逐字搬移（R39 本輪自身新增大量四方一二審敘事後主檔達 258,874 bytes、距 256KB 僅剩 1.25% 餘量，動工中即時發現並立即二度搬移；搬移對象與 archive_05/06/07/08/10/11 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_13.md`**（R40 跨平台輪 2026-07-24 建立，≈9.4KB）：**R38「四方複審裁決總結」歷史敘事段落**逐字搬移（R39 收尾已提醒帳本主檔逼近 256KB 上限、建議 R40 優先歸檔；R40 動工前主檔 257,388 bytes、距 256KB 僅剩 1.8% 餘量，動工前立即搬移；搬移對象與 archive_05/06/07/08/10/11/12 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_14.md`**（R40 跨平台輪 2026-07-24 建立，≈9.2KB）：**R39「四方複審裁決總結」歷史敘事段落**逐字搬移（R40 本輪自身新增大量四方一二審敘事＋新增 14 筆 DEF-101-327~340 缺陷列後主檔達 269,820 bytes、超過 262,144 bytes 上限，`check_defect_log_crossref.py` 由警告轉 FAIL，收尾動工中即時發現並二度搬移；搬移對象與 archive_05/06/07/08/10/11/12/13 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_15.md`**（R41 跨平台輪 2026-07-24 建立，≈8.7KB）：**R40「四方複審裁決總結」歷史敘事段落**逐字搬移（R41 動工前主檔已達 267,135 bytes、超過 256KB 上限；搬移對象與 archive_05/06/07/08/10/11/12/13/14 同類，缺陷現況已被上方缺陷總表 live 狀態取代）。
> - **`AutoSDD_Defect_Log_archive_16.md`**（R41 跨平台輪 2026-07-24 建立，≈48.2KB）：**缺陷總表中 42 筆已結列（fixed/wontfix）**逐字搬移（archive_15 搬遷後主檔仍達 265,683 bytes、仍超過 256KB 上限；搬移前逐一核對皆未被 `ONBOARDING.md`／兩份 compat-ci workflow／`SD10_PG_Contract_NextAction.md` 以「DEF-ID(狀態宣稱)」樣式引用，不觸發跨文件矛盾——初版誤含 DEF-101-051〔被 `SD10_PG_Contract_NextAction.md` 引用〕，`check_defect_log_crossref.py` 跑出矛盾後即時發現並移回主檔；搬移對象為總表已結列，性質同 archive_03/04/09）。
>
> **已知歷史重疊（R16 文件帳本一致性掃描發現，記事不刪除）**：`archive_01.md` 與 `archive_03.md` 各自完整搬移了 **DEF-37-001／DEF-62-001** 兩列（內容一致、結案狀態相同，非矛盾——推測為兩次獨立搬遷批次各自涵蓋範圍重疊所致）。因 `check_defect_log_crossref.py` 只掃描本主檔與少數指定目標檔，不掃 archive 間互相重疊，此重複未被既有機械守門攔下。依「archive 只增不刪、原文逐字保全」政策不予去重，僅此記事存證，供未來若真要整併 archive 時參考。
>
> **重要**：歸檔當下，所有**仍未結（open / routed / wontfix）缺陷與被 crossref 掃描目標引用的列均留在上方缺陷總表**（R14 輪替搬移當下保留 30 列既有，其後各輪陸續新增——**主檔現列數一律以 `grep -c '^| DEF-'` 實數為準，本註記不再快照具體列數/新增區間**：快照寫法已兩輪連續 stale（ARCH-R14-RR-1 訂正後 DEF-101-211 自身入帳又差 1，R15 SCAN-D-2 實證），改為方法引用消滅漂移面），故 live 追蹤**無須**讀 archive；archive 僅供已結缺陷之完整歷史佐證與審計回溯。
>
> **查詢某缺陷現況**：先看上方「缺陷總表」（完整 SSOT）；需歷史脈絡再查對應 archive 檔。
