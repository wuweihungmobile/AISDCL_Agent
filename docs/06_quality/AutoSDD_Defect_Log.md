# AutoSDD 缺陷帳本（Defect Log）— 跨輪累積、只增不刪

> **用途**：AutoSDD 雙軌迭代（A 軌整合 / B 軌 Dogfooding）行進中發現的框架缺點/Bug/摩擦，
> 依「發現即記、絕不累積」紀律入帳，並依分流規則回流改進（見
> `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 的「🐶 自我迭代模式」）。
> **建立**：2026-06-12（首輪 N=01；首輪或帳本不存在時於階段一建立含表頭的帳本）。
>
> **格式定義**（每筆一列）：
> `ID`｜`發現日期`｜`發現情境`（FSM 狀態 / SCG 閘門 / hook / skill / nightly stage / 審計）｜
> `現象與證據`（file:line 或命令輸出）｜`嚴重度`｜`分流去向`｜`狀態`
> - **ID**：`DEF-{{N}}-{seq}`，N = 迭代輪編號（01/02…），seq 為該輪流水號。
> - **嚴重度**：依 v0.01 官方
>   `docs_template/sdd/testing/DEFECT-CLASSIFICATION-SPEC-TEMPLATE.md` 之 Priority 分級：
>   **P0**（Urgent — 必須立即修復，阻擋當前測試/發布）/ **P1**（High — 當前 Sprint 修復）/
>   **P2**（Normal — 下個 Sprint 修復）/ **P3**（Low — Backlog，擇機修復）。
> - **狀態**：`open`（未分流）/ `routed`（已分流待修）/ `fixed@<版本或載體>`（已修，附證據）/
>   `wontfix+理由`。
> - **分流去向**（對應官方回流機制）：規格/文檔缺陷 → Phase J SPEC-PATCH（併入 v0.0(X+1)）；
>   治理規則缺陷 → FPL→SLV→LEARNING_COMMIT；框架程式/模板/hook 缺陷 → RFC + v0.0(X+1)；
>   整合層（AutoClaude 側）缺陷 → 下輪 A 軌 W 項。

## 缺陷總表

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-01-001 | 2026-06-12 | zero-trust 審計 | `AISDLC_SDD_v0.01/governance/RULES_INDEX.md:9` 表頭「共 35 檔」過期——`ls governance/rules/` 實數為 38 檔（R-9.x 37 條 + R-SELF-STRIDE）；2026-06-12 複驗仍存在 | P2 | v0.02（凍結本體文檔修正，Copy-on-Evolve） | fixed@v0.02（證據：`AISDLC_SDD_v0.02/governance/RULES_INDEX.md:8-11` 改記 39 檔〔R-9.1~9.38 + R-SELF-STRIDE〕+ next-act/next-rule 前緣同步 172/9.39；v0.01 凍結基線依紀律不回改） |
| DEF-01-002 | 2026-06-12 | zero-trust 審計（nightly/CI 腳本） | `AISDLC_SDD_v0.01/tools/fsm_runtime/formal/run_tlc.sh` 僅實裝 2 軌（SDD_FSM L73-77 + FLEET_FSM L119-137），與五軌真相源 `tlc_runner.py` 不對稱且全檔無說明註記；2026-06-12 複驗仍存在 | P3 | v0.02（補註記「五軌請走 tlc_runner.py」或補齊五軌） | fixed@v0.02（證據：`AISDLC_SDD_v0.02/tools/fsm_runtime/formal/run_tlc.sh:4-7` LEGACY 兩軌快驗註記 + 指向 tlc_runner 五軌） |
| DEF-01-003 | 2026-06-12 | zero-trust 審計（nightly/CI 腳本） | `AISDLC_SDD_v0.01/tools/` 缺顯式 `__init__.py`（`Test-Path` = False，2026-06-12 複驗），`python -m tools.…` 依賴 py3.3+ implicit namespace package | P3 | v0.02 | fixed@v0.02（證據：`AISDLC_SDD_v0.02/tools/__init__.py` 已建立，v0.02 pytest not-chaos 全綠驗證無回歸） |
| DEF-01-004 | 2026-06-12 | zero-trust 審計（AutoClaude CI） | AutoClaude `.github/workflows/ci.yml:189` pg-e2e-nightly 的 `if` 僅檢 `github.event_name == 'schedule'`，未以 `github.event.schedule` 過濾 cron——03:00 UTC 的 mutation cron（`ci.yml:11-13` 兩條 cron）也會觸發 pg-e2e，每晚雙跑（隱性成本，非錯誤）；2026-06-12 複驗仍存在；2026-06-13 improving_02 階段一複驗仍重現 | P3 | 下輪 A 軌 W 項（AutoSDD_improving_02.md W2） | fixed@improving_02（證據：`ci.yml:191` 改為 `(github.event_name == 'schedule' && github.event.schedule == '0 2 * * *') \|\| workflow_dispatch`，綁定 pg-e2e 自身 02:00 cron；對齊 mutation `0 3`(:281)/perf-baseline `0 2`(:444) 既有過濾〔行號含 W2 新增 2 行註解後之實際位置〕；YAML safe_load 通過、過濾字串計數=2） |
| DEF-01-005 | 2026-06-12 | 整合規劃流程（原始 Prompt vs 系統實況比對） | 原始整合 Prompt 假設與系統實況偏差：`GoalSynthesisPlugin` 已存在（`goal_synthesis_plugin.py:29`）卻被假設為待建；Port 實名為 `IBrain`/`IEvaluator`（`core/ports/brain.py:72`、`core/ports/evaluator.py:20`）卻寫作 BrainPort/EvaluatorPort——屬流程缺陷「規劃前未做 zero-trust 偵察」（證據：`AutoSDD_ZeroTrust_Audit_01.md` §2 比對表） | P2 | 已由迭代範本「階段一強制重偵察」修復 | fixed@範本v2（證據：`AutoSDD_Iteration_Prompt_Template.md` 階段一「Zero-Trust Re-Audit」條款 + 硬閘） |
| DEF-01-006 | 2026-06-12 | W6 實作（zero-trust 開檔複驗 kernel 派發實況） | 凍結計畫 §4.1/§4.2 錨定之 `POST_EVALUATE` / `ON_ESCALATION` 為合法 `KernelPhase` 枚舉（`hookspec.py:33,39`）但 **kernel 現況不派發**——全 codebase `phase=KernelPhase.X` emit 點僅 11 處（`kernel.py:87,120,136,153,174,176,204,222` + auto_resume/checkpoint 3 處），評估後實際派發為 `POST_ATTEMPT`/`ON_FAILURE`/`ON_SUCCESS`。計畫凍結前複驗只驗了「枚舉存在」未驗「實際派發」 | P2 | W6 已實作補償：`SddGovernancePlugin` 雙訂閱（計畫 phase 向前相容 + 實際派發 phase 真實載體，`sdd_governance_plugin.py` docstring 載明）；計畫文件已凍結不回改，偏差記入 `AutoSDD_ZeroTrust_Audit_01.md` §7；下輪迭代範本「階段一」應加「枚舉存在 ≠ 實際派發」檢核項 | fixed@W6（證據：`tests/plugins/test_sdd_governance.py` 22 case 以實際派發 phase 驗證全行為） |
| DEF-01-007 | 2026-06-12 | W8 整合驗收 | `cc-switch` 未安裝（`Get-Command cc-switch` 不存在），計畫 §5.2 多模型後端 A/B 驗收無法執行；`integration_gate.ps1` [5/5] 段落以 SKIP + 指引處理（非偽綠：SKIP 明示於輸出） | P3 | 環境工具補裝後依 §5.2 手動執行 A/B（指標：一次通過率 / CORRECTION 次數 / SDD_CONTRACT_VIOLATION 次數 / token 峰值）；俟環境補裝 | open（2026-06-14 improving_06 結案盤點複驗：`which cc-switch`=NOT FOUND 仍重現；屬環境工具缺裝非純程式可修；`integration_gate.ps1 [5/5]` SKIP 非偽綠；**不阻擋 A 軌主鏈結案**——功能路徑齊備，僅缺多後端對比驗收；2026-06-14 improving_08 階段一複驗 `which cc-switch`／`command -v cc-switch` 皆 NOT FOUND 仍重現，屬環境工具缺裝非純程式可修，維持 open 俟補裝；2026-06-14 improving_09 階段一複驗 `command -v cc-switch`=NOT FOUND 仍重現，維持 open〔🔴 人工確認本輪選「輕量結案盤點」，未補裝〕；**2026-06-14 improving_10：🔴 人工選「補裝 cc-switch 關閉」**。使用者實裝 cc-switch，但裝的是主流 GUI 版（farion1231）→ `command -v cc-switch`=**NOT FOUND**（GUI app 不上 PATH，實證 **DEF-10-001(b)**）。本輪移除**所有倉內阻塞**（DEF-10-001 fixed：建 A/B 載具 + 硬化 gate），DEF-01-007 由此**縮窄**為純「環境/API 動作」：僅待使用者裝 cc-switch **CLI 變體**（SaladDay/cc-switch-cli）+ 配 2 個 model profile（含 API key）+ 授權 token 花費後，於 AutoClaude/ 一鍵跑 `cc-switch use <profile> && autoclaude scripts/sdd_bridge_smoke.yaml --fresh` 收 4 指標。維持 **open**（非純程式可修，倉內已零阻塞）） |
| DEF-01-008 | 2026-06-12 | 三專家審查（Architect） | production 入口 main.py 呼叫 build_kernel 未傳 brain=（grep 證實非測試呼叫點僅 wiring 內部）→ SddGovernancePlugin._on_failure 的 IBrain.decide_escalation 升級諮詢於正式 CLI 路徑為死碼（plugin 已優雅降級 brain=None 直接 return，既有 EvolutionPlugin 升級鏈不受影響）。main.py 注入 MinimaxBrain 會同時啟用 kernel.decide_correction 路徑、改變既有修正行為，須獨立評估 | P1 | 下輪 A 軌 W 項（AutoSDD_improving_03 W1：main.py brain 注入影響評估 + e2e） | fixed@improving_03（證據：影響評估證實 `build_kernel` 把 brain 同時下發 `kernel`(wiring.py:315) 與 `SddGovernancePlugin`(wiring.py:307→202)，盲注入會改 production correction 語意〔kernel.py:198 區塊由跳過轉啟用 + API 故障 ESCALATE〕→ 採 **flag-gated** 落地：`MinimaxConfig.enable_kernel_brain` 預設 `False`＝brain=None 維持現況〔零退化〕，顯式啟用才注入。`main.py:~96` flag-gated 構造 `MinimaxBrainAdapter(minimax)`；`tests/integration/test_def_01_008_brain_injection.py` 9 case 鎖定〔預設 False / brain=None 雙消費者皆 None / flag-on 同一 brain 抵達 kernel+governance / correction 可達 / API 故障 ESCALATE / governance escalation 可達〕；full pytest 3060→3069 passed 0 failed、lint-imports 8 kept；死碼轉為 flag-gated 可選能力） |
| DEF-01-009 | 2026-06-12 | 三專家審查（Architect） | sdd_governance_plugin.py raw 250 行恰貼 plugin_entry ≤250 上限（工具計非空行 217 過關）——後續任何擴充必須先拆 <feature>_plugin/ package | P3 | 下輪擴充前置作業（watch item） | open watch（2026-06-14 improving_06 結案盤點複驗：raw 仍 250、受控指標非空行 **224 < 250**、`check_loc_budget` violations=0＝已自癒；續 watch：對該 plugin 任何擴充前必先拆 package；本輪零擴充不觸發；2026-06-14 improving_08 階段一複驗 raw 仍 250、violations=0 已自癒持平，零擴充不觸發，維持 open watch；2026-06-14 improving_09 階段一複驗 raw 仍 250、`check_loc_budget` violations=0 已自癒持平，本輪零擴充不觸發，維持 open watch） |
| DEF-01-010 | 2026-06-12 | 三專家審查（SA-SD） | v0.02 新增 compiler agent 後 AISDLC_SDD_INIT.md 仍宣稱 25 Agents / 18 specialized（磁碟實數 26/19），arch_fitness FF-7 advisory 已偵測 | P2 | 本輪即修（v0.02 文件計數同步） | fixed@v0.02（證據：INIT.md 計數更正 + EVOLUTION_LOG delta 補記） |
| DEF-02-002 | 2026-06-14 | improving_02 收尾 zero-trust 主 agent 親跑複核（TLC 計數欄位誠實性） | `tlc_runner.py:82-83,111-112` 報 `TLC_DISTINCT=855 > TLC_GENERATED=706`，違反 TLC「generated ≥ distinct」恆等不變量（主 agent 2026-06-14 親跑 SDD_FSM 重現 855/706/depth14，與原審計 855/706/depth15 之 generated/distinct 完全吻合、僅 depth 差 1 屬 noise）。根因：`_grp` 用 `re.search` 取**首個**匹配，疑抓到中途 progress 行而非最終 summary，致計數標籤接反/不可靠（副審查 agent 另一次跑得 49/115 即不穩定佐證）。**權威停機判準為 `No error has been found`（五軌穩定、跨 agent 一致）**，raw 計數僅供參考 | P3 | 框架程式缺陷 RFC + v0.0Y（修 `tlc_runner` 改取 last-match 最終 summary + 斷言 `generated ≥ distinct`，否則 raise） | fixed@v0.04（證據：`AISDLC_SDD_v0.04/tools/fsm_runtime/tlc_runner.py` 抽出 module-level `parse_tlc_summary(out)`，`re.findall(...)[-1]` 取最終 summary 取代 `re.search` first-match + fail-closed 斷言 generated>=distinct 違反即 raise RuntimeError；`tests/test_tlc_runner_parsing.py` 4 case 全綠〔last-match/正常不誤報/畸形 raise/無匹配回 0〕；v0.04 五軌 TLC 重跑修正後 generated>=distinct〔見 EVOLUTION_LOG v0.03→v0.04〕。Copy-on-Evolve：v0.03 凍結） |
| DEF-02-001 | 2026-06-13 | B 軌 dogfooding（W1-d 跑 v0.03 一致性測試時） | Copy-on-Evolve 結構摩擦：v0.01/v0.02/v0.03 各保留**同名測試模組**（`tools.fsm_runtime.tests.test_md_python_sync` 等），從 v0.03 cwd 跑 pytest 時 traceback **顯示** `..\AISDLC_SDD_v0.02\...test_md_python_sync.py:85` 路徑（誤導為在測 v0.02），但實際受測為 v0.03（直接 import `transition_rules.__file__` = v0.03、collect-only 為 v0.03 相對路徑、斷言偵測到 v0.03-only 的 AUTOCLAUDE_DELEGATED 邊已證實）。功能正確、僅 traceback 路徑顯示誤導；潛在 sys.modules 同名模組 collision 風險（官方 ci-gate.sh 寫死 cd v0.01 + 各版獨立跑 → 不觸發跨版同跑，故官方閘門無此問題） | P3 | RFC：v0.0Y 評估每版測試 rootdir 隔離（或 conftest 注入版本前綴）；非阻塞，記為結構性 watch item | fixed@improving_05（**根因比原記更深**：improving_05 §2.2 沙箱實證——即便最新版改 importlib/移除 `__init__.py`，跨版同跑仍壞，因各版**生產套件 `tools.fsm_runtime.*` 同名**，單一 process sys.modules 只能裝一版 → 測試誤載別版生產模組〔情境3 `assert 'vA'=='vB'` 失敗〕。∴「單一 process 跨版同跑」**結構性不可支援**，唯一正解＝每版獨立 process〔官方 ci-gate.sh `cd vX` 本就正確隔離〕。採 **Rule 12 Fail-Loud guard**（非徒勞「讓跨版能跑」）：`AISDLC_SDD/conftest.py`〔repo 根、versioned 目錄外＝共享 CI infra，**免 Copy-on-Evolve**，同 DEF-03-001 精神〕以 `pytest_configure` 偵測本 session 觸及 >1 版 → raise `pytest.UsageError`，把 cryptic `ImportPathMismatchError` 轉為可行動訊息〔具名版本+正確用法〕。偵測邏輯抽 `scripts/cross_version_guard.py` 純函式 + `scripts/tests/test_cross_version_guard.py` 8 case 鎖定。驗證：bare `pytest` from root 實測 fire〔exit 4 + DEF-02-001〕；雙軌 ci-gate `cd vX` exit 0 證零干擾；AutoClaude 3069 持平、lint-imports 8 kept。**殘留**：顯式 `pytest vX/ vY/` 雙路徑因 version `pytest.ini` 捕獲 confcutdir、根 conftest 不載入而攔不到，列文件化已知限制〔原生錯誤已具名兩版可診斷〕。零觸碰任一 v0.0X 凍結本體） |
| DEF-05-002 | 2026-06-14 | improving_05 收尾 zero-trust 審計（SA-SD 鏡，三鏡親跑複核） | 流程缺口：DEF-05-001 把 hook 由 `pytest_load_initial_conftests` 改 `pytest_configure` 時，同輪 `AISDLC_SDD/scripts/tests/test_cross_version_guard.py:92` 引用該 hook 名的測試 docstring **未同步更新**（仍寫舊 hook 名），測試本體斷言/被測行為正確、無功能影響，但文件與實作不一致，直到收尾 zero-trust 審計才被抓到。根因：介面/hook 重命名時缺「同步掃描所有引用該名稱之註解/docstring」檢核步驟 | P3 | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）補檢核項「重命名/hook 變更後 grep 引用點同步註解/docstring」；屬流程改善非框架程式缺陷 | fixed@improving_05（證據：docstring 已改 `pytest_configure` 與 `conftest.py:26` 一致；獨立 QA agent 重跑 `test_cross_version_guard.py` 8 passed、grep 無殘留舊 hook 名〔`conftest.py:10` 對照說明為刻意保留〕） |
| DEF-05-001 | 2026-06-14 | B 軌 dogfooding（improving_05 W1-c 實作 guard 時） | pytest hook 語意摩擦：guard 原設計用 `pytest_load_initial_conftests` 攔截，沙箱偶然 fire 但**真 repo 不 fire**——根因 chicken-and-egg：rootdir conftest 是在該 hook 的 default 實作*之內*才被載入，故自身對該 hook 的實作不會被回呼（真 repo `pytest --co` 仍走到 collection 撞 `ImportPathMismatchError`）。改用 `pytest_configure`（configure 時 conftest 必已註冊，版本目錄碰撞在其後 collection → configure 先 raise）即正確 fire。屬 pytest 內部機制摩擦，非 AISDLC_SDD 框架缺陷 | P3 | 實作期即修（hook 改 `pytest_configure`）；記錄供後續 conftest-level guard 參考 | fixed@improving_05（證據：`AISDLC_SDD/conftest.py` 用 `pytest_configure`；`test_bare_pytest_from_root_fires_guard` subprocess 整合測試綠，退化即紅） |
| DEF-03-001 | 2026-06-14 | B 軌 dogfooding（improving_03 階段一重偵察跑 ci-gate 時） | Copy-on-Evolve 版本治理缺口：`AISDLC_SDD/scripts/ci-gate.sh:16-18` 寫死 `FW_DIR="${REPO_ROOT}/AISDLC_SDD_v0.01"` → 官方 CI 閘門（腳本 header 自述為 `.github/workflows/ci.yml`/docker ci-runner/pre-push/act 的單一真相源）**永遠只測 v0.01 凍結基線**；實際承載框架演化的 v0.02/v0.03/v0.04（EVOLUTION_LOG 自述「可修改版本」）**不在官方閘門/CI/pre-push 覆蓋內**，僅靠人工 `cd vX && pytest -m "not chaos"` 守護（improving_02/03 皆手動驗）。最新版＝實際改動版反而無自動化閘門，與「地端=CI=ubuntu-latest 同一組檢查」初衷相違。與 DEF-02-001 同根（Copy-on-Evolve 跨版治理）。2026-06-14 實測：`ci-gate.sh` 從 AISDLC_SDD 根跑通過 exit 0，但開檔證實受測為 v0.01 | P2 | 框架程式缺陷 RFC + v0.0Y（ci-gate.sh `FW_DIR` 參數化／環境變數選版，或新增「演化版測試軌」使最新版納入官方閘門；與 DEF-02-001 rootdir 隔離合併評估） | fixed@improving_04（證據：使用者凍結方案「雙軌：凍結基線 v0.01 + 自動偵測最新演化版」。`AISDLC_SDD/scripts/ci-gate.sh` 改 `FROZEN_BASELINE=AISDLC_SDD_v0.01` 恆測 + `LATEST=$(ls -d AISDLC_SDD_v0.0*｜sort -V｜tail -1)` 自動偵測最高版〔現 v0.04〕，包成 `run_gate_for_version()` 對 `FW_VERSIONS` 迴圈；新增 `SDD_FW_VERSION` 單版覆寫 + `SDD_GATE_DRY_RUN` dry-run。腳本在 `scripts/`〔versioned 目錄外＝共享 CI infra，**免 Copy-on-Evolve**〕。新增 `scripts/tests/test_ci_gate_version_resolution.py` 4 case 鎖定解析意圖〔雙軌含 v0.01+最新／最新=磁碟最高版非寫死／覆寫收斂單版〕。驗證：`bash scripts/ci-gate.sh` exit 0「版本：AISDLC_SDD_v0.01 AISDLC_SDD_v0.04」雙軌各 pytest not-chaos 全綠 + arch_fitness exit<2；4 測試全綠；獨立審計 agent 突變測試〔LATEST 改空→test_dual_track/test_latest_is_highest 立即 2 failed〕證非假測試。零退化：AutoClaude 3069 passed/0 failed 持平、lint-imports 8 kept。見 `AutoSDD_improving_04.md` / `AutoSDD_ZeroTrust_Audit_04.md`） |
| DEF-07-001 | 2026-06-14 | improving_07 收尾 zero-trust 三鏡審查（SA-SD 鏡） | 流程摩擦（同一資訊雙處維護而 drift，與 DEF-05-002 同根）：實作期把 ci-gate 收斂格式由 `${VER}:${PASSED}passed` 微調為無後綴 `${VER}:${PASSED}`（取證可讀性），但**計畫載體 `improving_07.md` §2.2/§2.3/§3.3/RTM 的介面描述未同步**，仍寫帶 `passed` 後綴字樣（落地碼 `ci-gate.sh` 與 Defect_Log/Audit_07 皆已正確，僅計畫文件殘留）。無功能影響，收尾審查才抓到。根因：介面/輸出格式於實作期調整後缺「回掃計畫文件介面 delta 描述」檢核 | P3 | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）補檢核項「實作期調整介面/輸出格式後 grep 計畫文件同步介面描述」（與 DEF-05-002「重命名後 grep 引用點」合併為同一類「實作後回掃文件引用」紀律）；屬流程改善非框架程式缺陷 | fixed@improving_07（證據：improving_07.md §2.2(a):63 / §2.3(b):75 / §3.3 / RTM 已全部對齊為無後綴 `v0.01:1478 v0.04:1494`，與 `ci-gate.sh` GATE_SUMMARY 落地碼一致；同輪修復、無 partial 殘留） |
| DEF-06-001 | 2026-06-14 | improving_06 結案盤點 zero-trust 三鏡複核（QA 鏡，獨立審查 agent 親跑 ci-gate 取證時） | 取證友善性缺口（共享 CI infra `AISDLC_SDD/scripts/ci-gate.sh`）：雙軌閘門尾段僅印總結「✅ 全數通過（版本：v0.01 v0.04）+ CI_GATE_EXIT=0」，**未在收斂彙總行印出各軌 `N passed` 計數**——審查 agent 因輸出截斷無法在 tail 視窗獨立複核到 v0.01:1478 / v0.04:1494 逐軌數字（須主 agent 另跑 `tee+grep` 補證，見 ZeroTrust_Audit_06 §4.4 觀察）。硬閘判據（exit 0 + 雙版本名 + arch_fitness exit<2）不受影響，**非阻擋、非偽綠**，純屬零信任取證時需多一道撈取的摩擦 | P3 | 框架/CI 流程改善（下一輪 improving_07 按需）：`ci-gate.sh` 收斂時為每軌補印 `==> <version>: N passed`（或彙總成單行 `v0.01:1478 v0.04:1494`），使單次輸出即自證逐軌結果，免審計捲動截斷輸出 | fixed@improving_07（證據：(1) `AISDLC_SDD/scripts/ci-gate.sh` `run_gate_for_version()` 改 `python -m pytest … \| tee "$PYTEST_LOG"` 保留串流的同時擷取該軌 passed 數，新增逐軌 `==> [1/3] <VER>: N passed（not chaos）` + 全域 `GATE_SUMMARY` accumulator，迴圈後於收斂行下印 `逐軌計數：AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494`〔對齊本缺陷原建議格式〕，單次輸出即自證逐軌結果。(2) 擷取邏輯抽純函式 helper `AISDLC_SDD/scripts/pytest_passed_count.sh`〔讀 stdin→印整數，取最終 `N passed`、fail-soft 無匹配回 0〕，共享 infra 免 Copy-on-Evolve〔同 DEF-03-001/DEF-02-001 精神〕。(3) `scripts/tests/test_pytest_passed_count.py` 7 case 鎖意圖〔plain / **subtests 邊界 1478 非 14** / 多行取最終 summary / **多 `N passed` 匹配取最後（釘 tail -1 防退化）** / fail-soft 回 0 / 含 failed 仍取 passed〕，`scripts/tests/` 19 passed〔12 既有 + 7 新〕。(4) 修復前 `/tmp/cigate_before_07.log:50` 收斂行無逐軌計數 → 修復後實跑雙軌 exit 0 + 收斂行自證 `v0.01:1478 v0.04:1494`〔`/tmp/cigate_final_07.log`〕。`set -o pipefail` 保留 → 任一軌紅燈彙總前即中止，硬閘語意不變。零退化：AutoClaude 3069 passed/0 failed 持平、lint-imports 8 kept；2026-06-14 improving_08 階段一複驗無回歸：ci-gate 收斂段自證印出「逐軌計數：AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494」〔`/tmp/cigate_08.log` 尾 3 行〕，取證友善性修復穩定生效；2026-06-14 improving_09 階段一複驗無回歸：ci-gate 收斂段自證逐軌計數行「AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494」〔`/tmp/cigate_09.log` 尾 3 行〕正常顯示，修復穩定生效） |
| DEF-10-001 | 2026-06-14 | improving_10 階段一 zero-trust 重偵察（為關閉 DEF-01-007 而盤點 A/B 關閉路徑可執行性） | A/B 關閉路徑**不可執行**（兩缺口）：**(a)** §5.2 / `tools/integration_gate.ps1:65-76` [5/5] 引用的 A/B 載具 `sdd_bridge_smoke.yaml` **全倉不存在**（`find . -iname "sdd_bridge_smoke*.yaml"` 零命中，bridge 僅有 pytest `tests/integration/test_sdd_bridge/test_bridge_smoke.py`）→ 即使裝 cc-switch，文件化指令 `autoclaude sdd_bridge_smoke.yaml --fresh` 必 `❌ 找不到 Playbook 檔案`（`main.py:42` SystemExit）。**(b)** `integration_gate.ps1:66` 用 `Get-Command cc-switch` 假設 CLI 在 PATH，但最主流 [farion1231/cc-switch](https://github.com/farion1231/cc-switch) 為 **Tauri GUI 桌面 app 不上 PATH**（使用者 2026-06-14 實裝後 `command -v cc-switch`=NOT FOUND 實證）→ 該閘永遠偵測不到；headless 自動 A/B 需 **CLI 變體**（[SaladDay/cc-switch-cli](https://github.com/saladday/cc-switch-cli)） | P3（可執行性/取證缺口，非阻擋；A 軌主鏈早於 improving_06 結案、`[5/5]` SKIP 非偽綠） | 整合層（AutoClaude 側）即修——共享 infra（`tools/integration_gate.ps1` + `AutoClaude/scripts/` 載具），**免 v0.05** | fixed@improving_10（證據：**(a)** 建 `AutoClaude/scripts/sdd_bridge_smoke.yaml`——自包含 2 步 `workflow_type=aisdlc_sdd` TDD 載具〔S01 先測 / S02 實作 + evaluator `pytest smoke_add_test.py -q`〕，`_validate_playbook_format`+`Playbook.model_validate` 載入 OK；`tests/integration/test_yaml_import.py` glob `scripts/*.yaml` 自動納入 → 新增 **6 參數化 case 全綠**〔T1 parse/T2 sha256/T3 detect=playbook/T4 process_single success/T5 roundtrip/T4+ build_diffs〕。**(b)** `integration_gate.ps1` [5/5] 改 `foreach($name in @("cc-switch","cc-switch-cli","ccs"))` 多 CLI 名偵測 + `Test-Path` 驗載具存在 + SKIP/PASS 訊息釐清「farion1231 為 GUI 不上 PATH、headless 需 CLI 變體」並給含正確載具路徑之 A/B 指令。`integration_gate.ps1 -SkipFull` 實跑 **exit 0「2 PASS / 1 SKIP」**，[5/5] 新訊息自證解析到 `scripts/sdd_bridge_smoke.yaml`〔已存在、無「載具缺失」警示〕。零退化：零 python 變更，full pytest 3069→**3075**〔+6 新載具參數化，0 failed〕、lint 8/0、LOC 0） |
| DEF-10-002 | 2026-06-14 | improving_10 收尾獨立 zero-trust 審查（SA-SD/QA 鏡，OVERALL PASS 後提出之流程/方法論建議） | AISDLC_SDD 開發流程兩項結構性改善（非本輪技術缺陷，本輪改動面零回歸全 PASS）：**(a) 外部工具依賴 invocation 形態未於規劃期偵察**——improving_01 §5.2 規劃 cc-switch A/B 驗收時假設其為 PATH 上 CLI，直到 improving_10 才發現主流版（farion1231）為 Tauri GUI 不上 PATH，導致 DEF-01-007 拖延 9 輪。**(b) Copy-on-Evolve「最新演化版反而無自動化閘門」屬結構性風險**——DEF-03-001 已於 improving_04 以雙軌 ci-gate 點修，但「演化版必納官方閘門」尚未上升為治理通則（fitness function），僅靠單輪缺陷修復 | P3（流程/方法論改善，非阻擋、非框架程式缺陷） | 下一輪 improving_11（按需）：**(a)** 迭代範本補檢核項；**(b)** 治理層固化 fitness function | **fixed@improving_11**（🔴 人工確認 scope「(a)+(b) 全做」）。**(a) fixed@範本v3**：`docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 階段一新增檢核項 (f)「外部工具依賴須先確認 invocation 形態（GUI app／PATH CLI／API），勿假設可 headless 自動化」（含 cc-switch 致 DEF-01-007 跨 9 輪實例），為 DEF-05-002/DEF-07-001「實作後回掃」系列首個落入範本之常設階段一檢核。**(b) fixed@v0.05**：Copy-on-Evolve v0.04→v0.05，`tools/arch_fitness/arch_fitness.py` 新增 **FF-17「演化版必納官方閘門」** 結構守門——靜態讀 `scripts/ci-gate.sh` 斷言四錨點動態最新版偵測（`ls -d AISDLC_SDD_v0.0*`／`sort -V`／`tail -1`／`FW_VERSIONS+=…LATEST`），退回靜態寫死＝structural fail（DEF-03-001 回歸防護），把點修固化為結構不變量；`test_arch_fitness.py` 5 新測試（真 repo PASS／合成雙軌 PASS／寫死單版 fail／漏 append fail／腳本缺 INFO）。**設計決策**：不另開 R-9.x（會連鎖 FF-8/10/12 且屬自演化 meta-loop 異類關注點），arch_fitness 本即治理 fitness-function 套件（Rule 2/3）。驗證：v0.05 1499 passed、arch_fitness 87 passed、雙軌 ci-gate exit 0「v0.01:1478 v0.05:1499」自證 FF-17（最新版 v0.05 自動入閘）。無 `_HAPPY_PATH`/`*.tla` 變更（Rule 9.18.1 不啟動） |
| DEF-11-001 | 2026-06-15 | improving_11 W-11-2 執行 Copy-on-Evolve v0.04→v0.05 時（缺口由收尾 zero-trust 複審揭露：runtime 產物將隨 commit 入庫，且初審潔淨度查證僅憑 .pyc） | 流程摩擦（Copy-on-Evolve 操作）：框架無官方 copy helper／SOP 步驟排除 runtime 產物——`cp -r` 整碗複製會夾帶 (1) `__pycache__/`／`*.pyc`（已 gitignored，無入庫風險）、(2) **`build/reports/` runtime 取證輸出 173 檔**（abort/drift/fsm/test-analysis…，部分 v0.04 逐字 stale 複製、部分 v0.05 dogfooding 重生）、(3) **根 `arch-fitness.json`**（每次 ci-gate/arch_fitness 重生）。(2)(3) 此前**未被任何 .gitignore 涵蓋**（既有規則僅選擇性排除 v0.01）→ `git add -A -n AISDLC_SDD/AISDLC_SDD_v0.05/` would-add **1013** 檔含此二類「輸出非輸入」runtime 產物，將污染 Copy-on-Evolve commit（潛在誤導 + 體積膨脹）。非阻擋（排除後零影響，雙軌 1499 不變），純潔淨度摩擦 | P3（流程摩擦，非框架程式缺陷、非阻擋） | **即清理子項**：本輪即以 .gitignore 排除 v0.05 `build/reports/` + `arch-fitness.json` + `chaos-report.json`。**系統性子項（routed v0.0Y）**：提供通用 `scripts/copy_on_evolve.sh <from> <to>` helper（rsync `--exclude __pycache__ --exclude '*.pyc' --exclude build/reports --exclude arch-fitness.json`），或在 `AISDLC_SDD_UPGRADE_SOP.md` 補「複製後／commit 前排除 runtime 產物」SOP 步驟；與 DEF-02-001/DEF-03-001 同屬 Copy-on-Evolve 版本治理家族 | **fixed@improving_11（即清理子項）/ routed v0.0Y（通用 helper+SOP 子項）**（證據：複審修復——於既有 `AISDLC_SDD/.gitignore` v0.01 區塊後新增 v0.05 區塊排除 `AISDLC_SDD_v0.05/build/reports/`+`arch-fitness.json`+`chaos-report.json`；`git add -A -n AISDLC_SDD/AISDLC_SDD_v0.05/` **1013→839**〔排除 174 = 173 build/reports + 1 arch-fitness.json〕，build/reports 命中 0、arch-fitness.json 0；真源碼保留：build/planning 52、build/logs/README.md、EVOLUTION_LOG/CHANGELOG/73 tests/FF-17 bridge 仍在。零退化：`bash scripts/ci-gate.sh` exit 0、雙軌「v0.01:1478 v0.05:1499」不變，跑後 runtime 產物重生仍被 gitignore〔would-add 維持 839〕——證實排除「輸出」不破壞測試收斂。詳見 `AutoSDD_ZeroTrust_Audit_11.md` §6。修正初稿「本輪已手動清理 v0.05 無殘留」之不精準字樣：實採 .gitignore 排除〔非 git rm／非手動 rm〕，且初稿僅憑 .pyc 的潔淨宣稱不完整，已補全為 .pyc+build/reports+arch-fitness.json 三類查證） |
| DEF-11-002 | 2026-06-15 | improving_11 結案複核（主 agent 派 zero-trust 複審 agent 揭露：Audit_11 初審把 commit 潔淨度查證窄化到 .pyc） | 審查方法論缺口（與 DEF-11-001 cp -r 操作缺口同題不同根）：zero-trust 審查「Copy-on-Evolve 無 stale 入 commit」之驗證僅憑 `git check-ignore *.pyc` + `git status` 數 pyc，**未跑全量 `git add -A -n` dry-run 審 would-add 清單**，致 227 個 build/reports + arch-fitness.json runtime 產物漏審、初審誤判 OVERALL PASS（複審以 `git add -n` dry-run 1013 檔當場揭露）。根因：審查紀律缺「新凍結版本/大批 untracked 入庫前，須以 `git add -A -n` 全量 dry-run 審查 would-add 清單有無 runtime/stale 產物」之常設檢核 | P3（流程/方法論改善，非阻擋、非框架程式缺陷） | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）階段四/結案前補檢核項「Copy-on-Evolve 或大批新檔入庫前跑 `git add -A -n <path>` 全量 dry-run，審 would-add 無 runtime/stale 產物（非僅 .pyc）」；與 DEF-05-002/DEF-07-001「實作後回掃」、DEF-11-001 同屬潔淨度/誠實性紀律家族 | open（即記即分流，留待下一輪範本落地） |

