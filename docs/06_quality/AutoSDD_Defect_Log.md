# AutoSDD 缺陷帳本（Defect Log）— 跨輪累積、只增不刪

> **用途**：AutoSDD 雙軌迭代（A 軌整合 / B 軌 Dogfooding）行進中發現的框架缺點/Bug/摩擦，
> 依「發現即記、絕不累積」紀律入帳，並依分流規則回流改進（見
> `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 的「🐶 自我迭代模式」）。
> **建立**：2026-06-12（首輪 N=01；首輪或帳本不存在時於階段一建立含表頭的帳本）。
>
> **格式定義**（每筆一列）：
> `ID`｜`發現日期`｜`發現情境`（FSM 狀態 / SCG 閘門 / hook / skill / nightly stage / 審計）｜
> `現象與證據`（file:line 或命令輸出）｜`嚴重度`｜`分流去向`｜`狀態`
> - **ID**：`DEF-{{N}}-{seq}`，N = 迭代輪編號（01/02…），seq 為該輪流水號。
> - **嚴重度**：依 v0.01 官方
>   `docs_template/sdd/testing/DEFECT-CLASSIFICATION-SPEC-TEMPLATE.md` 之 Priority 分級：
>   **P0**（Urgent — 必須立即修復，阻擋當前測試/發布）/ **P1**（High — 當前 Sprint 修復）/
>   **P2**（Normal — 下個 Sprint 修復）/ **P3**（Low — Backlog，擇機修復）。
> - **狀態**：`open`（未分流）/ `routed`（已分流待修）/ `fixed@<版本或載體>`（已修，附證據）/
>   `wontfix+理由`。
> - **分流去向**（對應官方回流機制）：規格/文檔缺陷 → Phase J SPEC-PATCH（併入 v0.0(X+1)）；
>   治理規則缺陷 → FPL→SLV→LEARNING_COMMIT；框架程式/模板/hook 缺陷 → RFC + v0.0(X+1)；
>   整合層（AutoClaude 側）缺陷 → 下輪 A 軌 W 項。

## 缺陷總表

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-01-007 | 2026-06-12 | W8 整合驗收 | `cc-switch` 未安裝（`Get-Command cc-switch` 不存在），計畫 §5.2 多模型後端 A/B 驗收無法執行；`integration_gate.ps1` [5/5] 段落以 SKIP + 指引處理（非偽綠：SKIP 明示於輸出） | P3 | 環境工具補裝後依 §5.2 手動執行 A/B（指標：一次通過率 / CORRECTION 次數 / SDD_CONTRACT_VIOLATION 次數 / token 峰值）；俟環境補裝 | open（2026-06-14 improving_06 結案盤點複驗：`which cc-switch`=NOT FOUND 仍重現；屬環境工具缺裝非純程式可修；`integration_gate.ps1 [5/5]` SKIP 非偽綠；**不阻擋 A 軌主鏈結案**——功能路徑齊備，僅缺多後端對比驗收；2026-06-14 improving_08 階段一複驗 `which cc-switch`／`command -v cc-switch` 皆 NOT FOUND 仍重現，屬環境工具缺裝非純程式可修，維持 open 俟補裝；2026-06-14 improving_09 階段一複驗 `command -v cc-switch`=NOT FOUND 仍重現，維持 open〔🔴 人工確認本輪選「輕量結案盤點」，未補裝〕；**2026-06-14 improving_10：🔴 人工選「補裝 cc-switch 關閉」**。使用者實裝 cc-switch，但裝的是主流 GUI 版（farion1231）→ `command -v cc-switch`=**NOT FOUND**（GUI app 不上 PATH，實證 **DEF-10-001(b)**）。本輪移除**所有倉內阻塞**（DEF-10-001 fixed：建 A/B 載具 + 硬化 gate），DEF-01-007 由此**縮窄**為純「環境/API 動作」：僅待使用者裝 cc-switch **CLI 變體**（SaladDay/cc-switch-cli）+ 配 2 個 model profile（含 API key）+ 授權 token 花費後，於 AutoClaude/ 一鍵跑 `cc-switch use <profile> && autoclaude scripts/sdd_bridge_smoke.yaml --fresh` 收 4 指標。維持 **open**（非純程式可修，倉內已零阻塞）） |
| DEF-01-009 | 2026-06-12 | 三專家審查（Architect） | sdd_governance_plugin.py raw 250 行恰貼 plugin_entry ≤250 上限（工具計非空行 217 過關）——後續任何擴充必須先拆 <feature>_plugin/ package | P3 | 下輪擴充前置作業（watch item） | open watch（2026-06-14 improving_06 結案盤點複驗：raw 仍 250、受控指標非空行 **224 < 250**、`check_loc_budget` violations=0＝已自癒；續 watch：對該 plugin 任何擴充前必先拆 package；本輪零擴充不觸發；2026-06-14 improving_08 階段一複驗 raw 仍 250、violations=0 已自癒持平，零擴充不觸發，維持 open watch；2026-06-14 improving_09 階段一複驗 raw 仍 250、`check_loc_budget` violations=0 已自癒持平，本輪零擴充不觸發，維持 open watch；**2026-06-15 improving_14：watch 首次真正觸發並處置**——W-14-2 拓樸橋接擴充 `sdd_governance_plugin.py`，count_loc 自 224→恰貼 250 上限（零餘量）。依本 watch「擴充前先拆」紀律，將拓樸載入邏輯抽出 `plugins/_sdd_topology_signoff.py`（27 行 helper，非 importlinter Rule 1 independence 清單成員 → plugin→helper 合法），plugin 降回 **243 < 250（餘 7）**、`check_loc_budget` violations=0、`lint-imports` 8 kept。watch 紀律有效運作，維持 open watch 續守後續擴充）。**improving_22 複驗：本輪零擴充 sdd_governance_plugin（W 項為 AISLDC_SDD 框架側），watch 不觸發，維持 open watch**；**improving_98 複驗（2026-06-30）：raw 277、`check_loc_budget` violations=0（count_loc 受控指標 < cap 250、自癒持平），本輪 W 為框架側 Copy-on-Evolve（零擴充該 plugin），watch 不觸發；正確處置＝維持 open watch、不投機拆 package（守工程紀律 Rule 2/3）** |
| DEF-13-002 | 2026-06-15 | improving_13 前一輪 zero-trust 稽核定案（split-brain 修復決策：觀察期斷檔處置） | monorepo 觀察期 #2/#3 因 DEF-13-001 split-brain 於 06-13~06-15 斷檔。**明確決策＝不 backfill**：舊副本在 branch `sprint/sd_09_phase9`，程式碼已與 monorepo main 分岔；把舊碼採集的 06-13~06-15 觀察 jsonl 注入 monorepo＝以「舊碼數據」充當「monorepo 現碼穩定證據」，違反 Nightly 取證紀律 #1（rc/數據須反映現碼真實執行）與 #3（PASS 須引現碼 RunId）。故**不複製／不覆寫／不注入任何 jsonl**；舊副本資料保留原處 | P1 | 修復決策（不 backfill）；排程修正後自今晚起從現碼重新連續採集，達標日順延 | **resolved@2026-06-15（PM 拍板）**：① backfill 決策＝**確認不 backfill、從現碼重新採集**（守取證紀律 #1/#3）；② 孤立舊副本 `d:\CursorProject\AutoClaude` 已**歸檔重新命名**為 `d:\CursorProject\AutoClaude_ARCHIVED_pre_monorepo_20260615`（杜絕未來再 split-brain；修前掃描確認無其他 schtasks 引用舊路徑；歸檔後複驗排程仍指 monorepo 不受影響）。觀察期 #2/#3 達標日順延、待現碼重新連續累積（追蹤點，非 open 缺陷） |
| DEF-17-001 | 2026-06-16 | improving_17 W-17-1 B 軌 dogfooding（接入鷹架代謝 GC、驗 flag ON 端到端時揭露） | 代謝閉環「半接」結構摩擦：W-17-1 把 `scaffold_gc.run_gc()` 接入 `enter_scaffold_gc` 主迴圈後，flag ON 確實自動產 SCAFFOLD-ROI 報告，**但 `compute_proposals()` 依 `rule_loader.propose_graduation()` 需 `fire_count ≥ GRADUATION_MIN_FIRES ∧ catch_count==0` 才提議**（`scaffold_gc.py:72-73`/`rule_loader.py:150-159`）。FF-9 實測 39 條 active 規則 aggregate `fire_count=0`（`record_fire` 在 FSM 主迴圈零自動呼叫——僅測試觸發），故**即使 flag ON，生產端 GC 恆產零提議**＝GAP-X2「代謝肌肉從未收縮」**未真正閉合**：接 `run_gc` 是必要但不充分，尚需把 `record_fire(rule_id, caught=…)` 遙測接入「規則實際 fire/catch」的 FSM 執行點，ROI 帳本才有非零資料驅動退役提議。非阻擋（W-17-1 機制正確、報告真實落盤、紅線守界皆達成；零資料零提議是誠實的下游依賴而非 bug） | P3（成熟度閉環一致性/遙測接線缺口，非阻擋；與 B 軌「機制齊備但遙測未接」家族同根） | routed 未來輪 B 軌：把 `record_fire` 規則命中記帳接入 FSM 規則執行/守門點（`rule_loader.load_for_state` 命中後記帳），使 scaffold_roi 累積真實 runtime 資料；屬獨立 W 項（遙測接線），非本輪 scope（本輪聚焦「GC 提議產出鏈」接入＝代謝收縮的決策側） | **routed（fire 側 fixed@improving_18 / catch 側 routed → DEF-18-001）**（即記即分流；本輪 W-17-1 收縮「決策側」已閉合並測試覆蓋。**2026-06-16 improving_18 W-18-1/W-18-2 閉合「遙測 fire 側」**：`record_state_fires` 接入 `transition()` 主迴圈，flag ON 時 `fire_count` 真實累積〔`test_rule_fire_telemetry_wiring.py::test_flag_on_fire_count_accumulates_persisted` 驗 3 次轉態累積至 3〕，GC 已有非零資料可驅動提議＝DEF-17-001 點名的「`fire_count=0` 根因」已閉合。**殘留 catch 側**〔`record_fire(caught=True)` 何時觸發之語意框架未定義〕轉記為獨立 **DEF-18-001**。**2026-06-25 improving_62（v0.24）：fire 側遙測翻預設 ON 活體化**——`_rule_fire_telemetry_enabled()` unset→True（鏡像 AUTO_RECOVERY/SLV 翻環），fire_count 由 opt-in 升為生產常態累積，DEF-17-001 點名根因「生產端 fire_count 恆 0」**實質閉合**（掌舵者 signoff「B 軸 telemetry 翻 ON」；保留顯式 opt-out=0；conftest autouse 隔離護凍結 governance）） |
| DEF-19-001 | 2026-06-16 | improving_19 W-19 B 軌 dogfooding（閉合 catch 側契約時，揭露覆蓋面缺口） | catch 歸因覆蓋面不足（DEF-18-001 閉合後之殘留面）：W-19 定義 catch 三要件契約並接入兩確定可歸因路徑（R-9.1 gate retry 耗盡 / R-9.21 monitor 破壞），但目前僅 **2/39** active 規則補了 `failure_mode` 並接線；其餘 37 規則 `failure_mode` 未定義 → 其守望的失敗模式即使觸發 escalation 也不參與 catch 歸因（`catch_count` 恆 0、fail-closed 不記）。後果：這 37 規則的 ROI 仍為單側信號，`propose_graduation` 對它們仍可能偏頗提議退役。本輪**刻意只接 2 條無歧義路徑**（Rule 2 簡單優先、寧缺勿濫不污染 ROI）；覆蓋率以 `rule_fire_telemetry_stats().safety_certificate.catch_attribution_coverage` 程式內誠實揭露。非阻擋（catch 機制正確、紅線守界、flag 預設 OFF＝生產零影響） | P3（成熟度閉環覆蓋面缺口，非阻擋；與 B 軌「機制齊備但覆蓋漸進」家族同根） | routed 未來輪 B 軌：逐步為其餘規則補 `failure_mode` 自描述 + 對應 escalation 呼叫點歸因（每條須有「無歧義映射」證據，不可為衝覆蓋率而臆測歸因——延續 DEF-18-001 紀律）；**生產全面啟用退役前須提升覆蓋率至可接受門檻**。屬漸進補強 W 項，非單輪可竟 | routed（漸進補強進行中：**improving_20 W-20-1 將 catch 歸因從 2/39 推進至 4/39**——新增 R-9.2〔auto_compact per-stage 超限→ESCALATION〕、R-9.22〔spec_patch per-AC 上限耗盡→ESCALATION〕兩條無歧義路徑，各補 `failure_mode` + 接對應 `record_escalation` 呼叫點顯式歸因〔fixed@v0.11，6 case 測試 test_w20_catch_wiring.py〕；`catch_attribution_coverage` 程式內誠實揭露。剩餘 35 規則續漸進補強，生產全面啟用退役前須提升覆蓋率至可接受門檻）。**improving_22 階段一複驗仍 4/39**；🔴 人工本輪 scope 選「DEF-12-002 + DEF-15-001 深層」未含 catch 補強，維持 routed 漸進，未推進。**improving_37 W-37-1 推進 4/39 → 5/39**（新增 R-9.7·9.7.2 HUMAN_PENDING 逾時 ≥168h ESCALATION 無歧義路徑，fixed@v0.15；明文僅 9.7.2 排除 9.7.3〔歸 R-9.2〕防雙重歸因，非重疊守門測試鎖定；R-9.9 親驗無唯一生產落點降級不接）。**improving_38 W-38-1/W-38-2 推進 5/39 → 7/39（+2）**（新增 **R-SELF-STRIDE**〔SANDBOX_HARDENING_GATE policy_violation → ESCALATION，唯一生產落點、與既有 5 條零交集〕、**R-9.3**〔record_spec_audit 的 SPEC_AUDIT 耗盡 → ESCALATION；failure_mode 明文排除 implementation-budget-exceeded 直接 escalate〔正交無規則〕與 R-9.1 gate-retry 落點，防雙重歸因〕兩條無歧義路徑，fixed@v0.16；`test_w38_catch_wiring.py` 8 case 含兩條非重疊守門〔R-SELF-STRIDE verdict=pass 不歸因 / R-9.3 implementation-budget 落點不歸因〕；runtime `catch_attribution_coverage` 實測 7/39）。**improving_39 揭露結構天花板**：grep `record_escalation(` v0.16 fsm_runtime.py＝9 生產 escalation 落點＝7 已接線 + 2 正交無規則（515 implementation-budget、2401 spec_patch-unable-to-draft），「1:1 無歧義落點接線」乾淨候選**已枯竭**；W-39-1 機械分類 39 條＝(A)FSM escalation 7 / (B)hook 3 / (C)lint·TLC 3 / (D)meta-loop guard 14 / (E)manual·advisory·憲法 12，證實 escalation-scoped 真實覆蓋＝**7/7=100%**（DEF-39-001 透明化）。**→ closed@improving_40（milestone，🔴 掌舵者 AskUserQuestion 拍板正式收尾）**：FSM-escalation catch 機制覆蓋已達結構天花板（7/7=100%）；其餘 32 條由 hook/lint/TLC/meta-loop/人工守門＝本質非 FSM-escalation catch-可歸因（`catch_count` 恆 0 為設計使然非缺口）。「其他守門機制覆蓋度量」另立新標的（B 軌未來輪，非本缺陷 scope） | 
| DEF-25-001 | 2026-06-17 | improving_25（B 軌 meta⁸ 視覺化飽和認定） | FSM-STATE 視覺化反射觸 R-9.37.4 邊界（視覺化模組不得篡改 meta⁸ 既有合約、反射狀態為唯讀邊界） | P2 | wontfix（by-design 邊界，非缺陷） | **wontfix**（R-9.37.4 設計邊界；歷史脈絡見 `AutoSDD_Defect_Log_archive_01.md` improving_25 段） |
| DEF-42-001 | 2026-06-21 | improving_42 階段一 Re-Audit（ci-gate v0.17 pytest） | 框架測試 flaky（Windows 並行檔案鎖）：`v0.17/tools/fsm_runtime/tests/test_file_lock.py::test_parallel_writes_do_not_lose_increments` full-suite 並行下間歇 PermissionError，ci-gate v0.17 偶報 1 failed；主 agent 隔離 3/3 全綠 → 確認**非回歸** | P3 | routed（flaky 隔離追蹤、非回歸；框架本體 RFC） | **routed**（archive_01 improving_42 段；非回歸 flaky） |
| DEF-42-003 | 2026-06-21 | improving_42 W-42-1 設計期（負向狀態碼跨路徑搶救被既有哨兵測試攔下而揭露） | 引號與負向狀態碼共存時負向被丟棄＝under-specify（引號路徑早 return、負向不評估）；屬 W-32-1 刻意設計決策 + 哨兵 `test_quoted_wins_over_negative_status` 鎖定 | P3 | wontfix（by-design，surface 供掌舵者未來輪決策） | **wontfix+by-design**（archive_01 improving_42 段；哨兵鎖定、非缺陷） |
| DEF-52-006 | 2026-06-23 | C 軌 improving_52（SA 鏡 FIND-4） | stderr 中文 UnicodeEncodeError（理論）：`_init_utf8_streams()` 僅 `__main__` 呼叫；ascii-strict console 直接 import 呼叫會拋。生產 wiring 經實證皆 subprocess→`__main__` 必走→風險為零 | P3 | wontfix（生產路徑必為 subprocess） | **wontfix+理由**（archive_01 improving_52 段；生產必走 subprocess、風險零） |
| DEF-53-001 | 2026-06-23 | C 軌 `.claude` 審查輪八（SA 鏡 P3-2） | `hub_merge.py` `_read_yaml`/remote→local copy 未 cap 讀 pull-cache yaml；`detect_conflict`/`resolve_or_record` 經查僅測試呼叫、未在任何 runtime 路徑 wire → 無 runtime 消費者、無活路徑可觸發（latent） | P3 | RFC（框架本體 `tools/fsm_runtime/`，`build/planning/active/SDD_improving_Automation_NN.md`） | **routed**（latent；archive_01 審查輪八段；**註**：與 DEF-CLDREV-030〔hub_sync yaml 大小上限 fixed@v0.20〕同家族，本項 hub_merge 路徑仍 latent） |
| DEF-100-002 | 2026-06-30 | improving_100 階段一 mutation backlog 分析（查 `.mutmut-cache` sqlite） | `autoclaude/plugins/token_guard/thresholds.py` `should_compact_decision` L49 `if in_correction_loop and correction_history_len <= 1: return token_pct >= threshold` 為**死分支**——到達 L49 時 `token_pct >= threshold` 已恆成立（L48 已 return False 攔掉 `<`），故該分支 ≡ `return True` ≡ 下方 `return True`；因此 `.mutmut-cache` #122-124（`and`→`or`、`<=`→`<`、`1`→`2`）皆為**等價變異**，補測試殺不掉、非真測試缺口（受控突變 EQUIV-CHECK 證實 GREEN） | P3 | routed 下輪評估重構移除冗餘分支（改源碼需驗證對齊 `PlaybookRunner._should_compact_now` 契約，本輪守 Rule 2/3 不動源碼） | routed（improving_100 已加等價性實證測試 RTM-100-5〔`TestShouldCompactL49DeadBranchEquivalence`，parametrize 證 token_pct≥threshold 時恆 True 不受 correction 參數影響〕pin 住死分支語意並保護未來重構；重構移除待下輪） |
| DEF-101-003 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 B zero-trust 盤點（凍結版 ps1 BOM 普查） | **凍結版 ps1 無 UTF-8 BOM 共 47 支（known-gap 豁免）**：`AISDLC_SDD_v0.01~v0.29/tools/fsm_runtime/formal/run_tlc.ps1`（29 支）+ `v0.12~v0.29/tools/install_hooks/install_post_commit.ps1`（18 支）含非 ASCII 且無 BOM，zh-TW Windows PowerShell 5.1 直跑會 parser 斷裂；v0.30 對應兩檔本輪已補 BOM，`scripts/ci-gate.ps1` 同輪補齊 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝改用 v0.30 對應檔；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-004 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D macOS 實測 | **凍結版 `verify_traceability.sh` 用 `declare -A`（bash 4+ 關聯陣列）→ macOS 內建 bash 3.2 必炸**（v0.01~v0.29 全數；v0.30 本輪已改 bash 3.2 相容） | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝用 v0.30 同檔，或 `brew install bash` 後以 bash 4+ 執行；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-005 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D 審視 `verify_traceability.sh` 相容化時發現既有缺陷 | **`verify_traceability.sh` 的 `set -euo pipefail` + `grep` 無匹配（rc=1）→ 腳本提前靜默退出**：任一 grep 掃描零命中即中止整支驗證、不報錯不輸出後續檢查結果（驗證鏡子靜默失真，違取證紀律 #4「驗證鏡子自身要被驗證」）；**所有 bash 版本皆然、v0.30 亦未修**（本輪 bash 3.2 相容化僅處理 `declare -A`，未動 pipefail/grep 行為） | P2 | 本輪修復（同 DEF-101-218，同一支腳本同一根因） | fixed@R16（詳見 DEF-101-218：`AISDLC_SDD/AISDLC_SDD_v0.30/tools/verify_traceability.sh` 9 處賦值敘述式補 `\|\| true`〔含本筆點名的 grep 零命中靜默 exit 1〕，「未找到」WARN 分支恢復可達；對照組／反例組／框架真實 `docs/` 三組皆驗證跑到底 exit 0；**R18 帳本一致性複核**：實測 `grep -n "\|\| true" verify_traceability.sh` 命中 9 處，確認與 DEF-101-218 記載一致，本列此前殘留 open + 軌道② RFC 分流敘述與同日 DEF-101-218 fixed@R16 自相矛盾，予以訂正） |
| DEF-101-018 | 2026-07-10 | 跨平台修復輪四方複審第三輪（自 DEF-101-006 帳目拆分） | **ruff 存量 baseline 1,339 筆待清理**：鎖版（DEF-101-006 fixed）已消除跨機器漂移，但 `ruff check .` 對全倉仍報 1,339 筆既有 issue（E501/I001/UP045 等，遍布 HEAD 既有檔案）——根 CLAUDE.md 教學指令在新環境滿版報錯的體感問題仍在 | P3 | 擇期分批清理存量或調整教學指令範圍（沿 DEF-101-006 原分流） | open（帳目拆分自 DEF-101-006：漂移根因已除、本列只追存量清理；1,339 筆為鎖版後穩定 baseline） |
| DEF-101-019 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點） | **凍結版 v0.01 subprocess 無 encoding 同病**：`AISDLC_SDD_v0.01/tools/fsm_runtime/sandbox_runner.py:252`、`tlc_runner.py:69` 與 DEF-101-015 同病（`text=True` 無 encoding），zh-TW Windows cp950 下可能 UnicodeDecodeError | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝ci-gate `.sh`/`.ps1` 已設 `PYTHONUTF8=1` 使凍結版子程序同獲 UTF-8；v0.30 對應處已 fixed@DEF-101-015） |
| DEF-101-020 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **v0.12~v0.29 中間凍結版 `test_closure_evidence.py` fixture 未清洗 env**：與 DEF-101-007 同源——中間凍結版測試的真 git fixture 未清 GIT_DIR/GIT_WORK_TREE；惟官方閘門路徑（ci-gate 雙軌＝v0.01＋LATEST）不執行中間版，hook 層 `env -u` 清洗亦已保護，僅剩「人工在中間版目錄帶敵意 env 手跑 pytest」邊角情境 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝閘門路徑不執行中間版＋hook 層 `env -u` 已清洗；v0.30 已 fixed@DEF-101-007） |
| DEF-101-021 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-010 殘餘） | **mutation artifact 累積鏈 90 天上限**：GitHub artifact retention 上限 90 天——連續 90 天無 token_guard 源碼變動觸發 `autoclaude-mutation-on-change.yml`，鏈上 `mutation-history` 過期 → 累積歸零重累；且 GitHub 側與 Windows 本機 nightly（`run_local_nightly.ps1`）為兩條互不同步的獨立累積點 | P3 | 平台上限如實揭露；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；緩解＝Windows 本機 nightly 為另一獨立累積點；限制已照實寫入 workflow 檔頭「已知限制」段） |
| DEF-101-022 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **`closure_evidence._run_git` 自身繼承呼叫端 env**：v0.30 `closure_evidence.py:89` `_run_git` 未自清 GIT_DIR/GIT_WORK_TREE——屬 read-only git 查詢（rev-parse 類），實害受限；現行防線為 hook 層 `env -u` 清洗 | P3 | 縱深防禦（函式內自清 env）留待後續輪；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；防線＝hook 層清洗已阻斷已知傳染路徑，函式內自清為縱深防禦補強項） |
| DEF-101-025 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-012 修復之語意註記） | **根層基建 bash -n leg 檢查「工作樹版本」而非 staged blob**：`tools/git-hooks/pre-commit` root-infra leg 以 `bash -n "$TOPLEVEL/$f"` 檢查工作樹檔——staged 壞＋工作樹好的罕見組合會本機假綠入庫 | P3 | 與既有 ruff-on-worktree 慣例一致故不改本機行為；known-gap 記入 `ONBOARDING.md` §9 | open（known-gap；緩解＝雲端 `root-infra-ci.yml` 對 push 後實際內容機械攔回） |
| DEF-101-040 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect 終裁 (e)，凍結版豁免家族；SA 終裁補充 cwd-wiring 面向） | **凍結版 v0.01~v0.29 settings.json 無 `PYTHONUTF8` env、hook command 仍為裸相對路徑**：直啟凍結版 session 時 ① hooks 的 cp950 stdin/stdout 風險不受 env 保護；② cwd 漂移時同樣有 DEF-101-028 的 exit-2 deny-lock 風險（僅 v0.30／根層已補 env＋shim） | P3 | 凍結版依紀律不回改；known-gap 記入 `ONBOARDING.md` §9（含兩面向） | wontfix+凍結版紀律（known-gap 已列；緩解＝根層 router 路由情境由根層/v0.30 覆蓋、直啟屬 dogfooding 邊角情境，操作時保持 cwd 於版本根） |
| DEF-101-051 | 2026-07-12 | 排程 nightly 紅燈追查（DEF-101-049 修後 pg-contract 真實狀態出土）；本輪深度根因 + 方向定案 | **三層 goal_task_id 為半成品功能（非單純約束矛盾）**：0010 migration 的 CHECK（`goal_task_id IS NOT NULL OR started_at < '2026-05-20'`）+ FK 欄 + validated + `test_alembic_0010` T8/T9/T10 契約測試皆齊，**但應用層從未接線**——全庫 `cp.goal_task_id` 零賦值、`playbook_runs.goal_task_id` 零寫入路徑、`PlaybookRun` ORM 連該欄都沒定義（run↔goal 現走另一張 `goal_progress` 表）。故 cutoff 後 **100%** checkpoint save 撞 CHECK；兩支 contract test 要求互斥（`test_alembic_0010`＝裸 run 必被拒 vs `test_pg_state_repository_contract` M4＝save_checkpoint 必成功建裸 run）。cutoff＝migration 撰寫日、PG production 未上線（ADR-SD09-001 gated） | P2 | **方向定案（使用者拍板）＝反向補完三層功能**（run 應有 goal_task_id 為正確設計意圖），**非放寬約束**（放寬＝放棄三層追蹤，已否決存證）；屬跨 plugin/DB/契約測試真功能 + 需 orphan-run 政策設計，SD_10 PG-track 落地。**使用者拍板 orphan-run 政策 (c)＝CHECK 加判別欄**（run_kind∈{standalone,three_tier}，CHECK 改 `run_kind<>'three_tier' OR goal_task_id IS NOT NULL`，無時間依賴→消除時間炸彈）。本輪落地並轉硬閘 | **fixed@nightly 追查輪·四方審查閉環**（證據：alembic **0017** run_kind 判別欄取代 0010 時間炸彈 CHECK〔fresh pg17 upgrade→head、downgrade 可逆〕；`PlaybookRun` ORM 補 goal_task_id+run_kind 欄；`_ensure_run_id` 依 cp.goal_task_id 標 three_tier；5 條 checkpoint 落地路徑〔_builder+_token_halt+_interrupt+_evolution+auto_resume〕接通 task.goal_task_id〔離線三層 playbook 端到端可達；runtime 動態分解 GoalDecomposer 未接為已知 gap〕；另修被 CheckViolation 遮蔽之 `schedule_resume` str→TIMESTAMPTZ 型別 bug；`_ensure_run_id` guard：非 UUID goal_task_id（fixture GT-xxx）退回 standalone+warn，不弄垮 checkpoint save。契約測試：`test_pg_state_repository_contract` 13 passed/1 skip〔含 three_tier 真實流程 seed projects→goal_tasks〕、`test_alembic_0010` T8/T10/T10b/T10c 更新至 0017 語意、builder threading 直測 3 case。移除 `autoclaude-ci.yml` pg-contract `continue-on-error`〔CI 模擬 13P/1S 綠〕。LOC=0、importlinter 8 kept、full suite 3632P/0F。四方獨立審查〔DBA/Dev/QA/Architect〕→全修〔P0 LOC 405>400 削行、P0 downgrade VALIDATE 回退陷阱改 NOT VALID、P1 5路徑接線、builder 直測〕→複審。canonical〔#4〕：`playbook_runs.goal_task_id`＝run 首 goal audit 標記〔UUID〕、`goal_progress` ledger＝per-goal 事件流〔TEXT，兩軌型別不同軌〕，見 NextAction。**衍生 follow-up：DEF-101-053／054**） |
| DEF-101-055 | 2026-07-12 | DEF-101-053 修復收緊 `match=` 時揭露 | **ORM `CheckConstraint(name=...)` 與 DB 實際 constraint 名分歧**：ORM `_pg_models.py` 宣告 `ck_playbook_runs_status`／`ck_kb_outcome`，但 DB（alembic 0001 inline CHECK）實際名為 `playbook_runs_status_check`／`knowledge_entries_outcome_check`（PG 匿名 inline CHECK 預設名）。因 production schema 由 `alembic upgrade` 建置（非 ORM `create_all`），這兩個 ORM CHECK name 對 DB **從未實現＝裝飾性**。影響：任何 migration 若 `DROP CONSTRAINT ck_playbook_runs_status` 會失敗；離線 DDL snapshot 測 ORM name 通過但不保證 DB 有該名。0017/0018 新 CHECK 用顯式 `ck_*` 名故 DB 一致，分歧僅限 0001 兩個 inline CHECK | P3 | 記事存證，不阻塞：CRUD 契約測試已改綁 **DB 實際名**（正確、真測 CHECK）；離線 DDL 測 ORM name（分層合理，二者測不同層）。未來若需對齊，另開 rename migration（屬 cosmetic，PG 未上線無急迫） | open（SD_10 PG-track 記事；本輪貢獻＝辨識 + CRUD 測試改綁真實名 + 探針取證真名） |
| DEF-101-056 | 2026-07-13 | Mac/Windows 相容性優化四方複審（SA 建議記帳，比照既有慣例） | **`install_post_commit.ps1` 用 `Out-File -Encoding ascii` 損毀非 ASCII 路徑，v0.01～v0.29 共 29 份歷史副本未修復**：bug 本體（heredoc 寫 hook 時非 ASCII 字元被靜默替換為 `?`）已在 LATEST（`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`）修復（改 `[System.IO.File]::WriteAllText` + `UTF8Encoding($false)` + LF 正規化）；v0.01～v0.29 依框架自身治理規則「凍結基線不可在原地修改、框架改動一律走 Copy-on-Evolve」不予回改。實務風險分層：v0.12～v0.29（18 份）因缺 UTF-8 BOM，zh-TW Windows PowerShell 5.1 會在 parser 階段先行斷裂、根本跑不到 ascii 編碼那行；v0.01～v0.11（11 份，有 BOM）若被直接執行仍會完整重現原始 P1（LATEST 動態解析設計＝實務上唯一會被安裝流程呼叫的版本〔現為 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`；R9 訂正原文誤植之 `AutoClaude/tools/install_hooks/...` 路徑——該路徑全 repo 歷史從未存在，見 DEF-101-107〕，故此為理論殘留風險而非已知會被觸發的路徑） | P3 | 記事存證，不修復：凍結基線治理規則優先於個別 bug 回補；若未來有文件/流程引導使用者手動 cd 進舊版目錄執行 hook 安裝，屆時需重新評估 | wontfix+凍結版紀律（known-gap；與 DEF-101-019/020/040 同款情境，本輪 Architect/SA 複審統一標籤：v0.01～v0.29 原地不修，記事存證；LATEST v0.30 已 fixed，見同輪 commit；**R15 事實訂正（SCAN-A-2＋SCAN-D-1 兩獨立 agent od 實測交叉）**：本列「v0.01～v0.11（11 份，有 BOM）」敘述失真——該 11 份實為**無 BOM 且純 ASCII**（首 3 bytes `23 20 49`、bytes>127=0，自初始 commit 未變），不炸 PS 5.1 parser 的真因是純 ASCII 而非有 BOM；風險結論不變（被直接執行仍會完整重現原始 P1），惟未來 BOM 普查勿以「有 BOM」為判準） |
| DEF-101-057 | 2026-07-13 | Mac/Windows 相容性優化 Wave 2（文件層級收尾複查，比照 DEF-101-056 既有慣例） | **`install_post_commit.{sh,ps1}` 在 git worktree 下硬編 `.git/hooks/post-commit`（`.ps1`：`.git\hooks\post-commit`）路徑，v0.01～v0.29 共 29 份歷史副本未回補**：bug 本體＝worktree checkout 下 `<worktree>/.git` 是指向主 repo 的純文字檔而非目錄，腳本硬編 `HOOK_TARGET="$REPO_ROOT/.git/hooks/post-commit"`（`.sh`）／`Join-Path $RepoRoot ".git\hooks\post-commit"`（`.ps1`）對其寫入會直接炸「Could not find a part of the path」；任何 Windows/macOS/Linux 開發者在 worktree 中對舊版目錄執行 install_post_commit 皆會重現。已在 LATEST（`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.sh` / `.ps1`）修復（改用 `git rev-parse --path-format=absolute --git-common-dir` 動態解析回主 repo 真正的 `.git`，不受 `core.hooksPath` 影響，worktree／一般 repo 皆正確；見 commit 7162959）；v0.01～v0.29 依框架自身治理規則「凍結基線不可在原地修改、框架改動一律走 Copy-on-Evolve」不予回改，29 份歷史副本仍為硬編寫法原樣。實務風險分層：`AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.{sh,ps1}` 為動態解析 LATEST 版設計＝正常安裝流程實務上唯一會被呼叫的版本，不受影響；僅當使用者手動 cd 進 v0.01～v0.29 任一舊版目錄、且該 checkout 恰為 git worktree（需同時滿足「舊版目錄」+「worktree」兩條件，均非官方安裝 SOP）時才會踩到，屬理論殘留風險而非已知會被觸發的路徑 | P3 | 記事存證，不修復：凍結基線治理規則優先於個別 bug 回補；若未來有文件/流程引導使用者手動 cd 進舊版目錄執行 hook 安裝，屆時需重新評估 | wontfix+凍結版紀律（known-gap；與 DEF-101-019/020/040 同款情境，本輪 Architect/SA 複審統一標籤：v0.01～v0.29 原地不修，記事存證；LATEST v0.30 已 fixed，見 commit 7162959） |
| DEF-101-058 | 2026-07-13 | Mac/Windows 相容性優化 Wave 1（`macos-compat-ci.yml` 新增 `macos-nightly-full` 在全新環境首次觸發） | **`AutoClaude/pyproject.toml:30` 的 `hypothesis>=6.0` 未鎖版本上限，全新環境 `pip install` 會解析到最新版並觸發連鎖測試失敗**：`macos-nightly-full` job 無既有 `.venv`、走 `pip install -e ".[dev]"` 全新依賴解析，裝到當時最新 `hypothesis==6.156.6`，其內部模組 `hypothesis.internal.conjecture.optimiser` 已改名/位移，導致約 19 個 property-based 測試連鎖失敗。**跨平台依賴版本漂移風險，非 macOS 特有**：`windows-compat-ci.yml` 亦有等價全新 `pip install -e ".[dev]"`／`".[dev,lint]"` 步驟（L124、L254），任何平台的全新安裝都會中招；目前多數開發者/CI 沿用既有 `.venv` 快取（`bootstrap.sh` 偵測到既有 `.venv` 即跳過重裝）掩蓋了此風險，故先前未被普遍注意到。**四方複審重要訂正（本輪，2026-07-14）**：SA/SD 兩方獨立以全新 venv 重新驗證，皆**無法重現**上述 19 個測試失敗——`hypothesis.internal.conjecture.optimiser` 模組現行仍存在且可正常 import，`hypothesis==6.156.6` 下全套測試（3,566 passed/196 skipped）與 hypothesis 相關測試皆綠燈；且 `macos-nightly-full` 這個「首次觸發」場景在本 repo 實際上**從未真正跑過一次**（僅綁 schedule/workflow_dispatch）。原始 19 個失敗的診斷缺乏可查證的 CI log 佐證，應視為未經證實的記事，不宜再被後續 audit 當既定事實引用 | P2 | 原建議「加 `<7` 上限」經 SA 獨立驗證證實**無效**——PyPI 從未發布過 hypothesis 7.x（現行最新即 6.156.6），`<7` 不改變任何解析結果。已改採本檔案 `dev` 區塊既有的 pytest/ruff 精確鎖版慣例（DEF-101-006），鎖定為經本機全新 venv 驗證綠燈的 `hypothesis==6.156.6` | **fixed@Mac/Windows 四方複審 2026-07-14**（`AutoClaude/pyproject.toml:30` 改精確鎖版 `hypothesis==6.156.6`；根因「19 個測試失敗」本輪無法重現，已如實訂正上一版記事，鎖版本身仍具備防止未來浮動安裝到未經驗證版本的預防價值） |
| DEF-101-060 | 2026-07-14 | Mac/Windows 相容性四方複審（QA 覆核 DEF-101-058 修復範圍時發現） | **`AutoClaude/pyproject.toml` 除 `hypothesis` 外，另有 ~18 條相依宣告同樣未鎖版本上限**：`pydantic>=2.0`、`pyyaml>=6.0`、`httpx>=0.27`、`wexpect>=4.0`、`keyboard>=0.13`、`pytest-mock>=3.14`、`cachetools>=5.3`（出現兩次）、`click>=8.0`、`import-linter>=2.0`、`plyer>=2.1`、`win10toast>=0.9`、`sqlalchemy>=2.0`、`asyncpg>=0.29`、`psycopg2-binary>=2.9`、`alembic>=1.13`、`tenacity>=8.2`、`pgvector>=0.3`、`claude-agent-sdk>=0.2.110`。DEF-101-058 只補了剛好在 CI 炸掉的那一個套件，屬頭痛醫頭；任一條在未來全新環境 `pip install` 時都有同樣的連鎖破壞風險 | P3 | 記事存證＋排入下一輪：候選處置為 (a) 對已知易破壞性升版的套件（如 pydantic/sqlalchemy/httpx）逐一升版驗證後精確鎖定；(b) 或新增機械檢查（比照 `tools/check_script_parity.py` 精神）強制「無上限宣告」需顯式列入白名單並附理由，而非預設放任。本輪範圍限 Mac/Windows CI paths／nightly 提醒／hypothesis 鎖版，全面依賴鎖版屬另一輪工作範圍，不逕行處理 | open（記事存證，待後續輪逐一評估鎖版；交叉參照 DEF-101-058） |
| DEF-101-068 | 2026-07-14 | R3 Mac/Windows 相容性複審（四方「架構最佳化建議」彙整，非本輪阻擋項，記事存證供下一輪參考） | **跨平台架構優化建議清單（本輪不逕行實作，範圍限於解決本輪 P0~P2 阻擋項）**：(a) Architect＝CI paths 白名單應由「條列法」改為「單一事實來源」（擴充 `test_ci_paths_cover_root_consumers.py` 涵蓋全部 4 個 CI workflow，而非只查自己）；(b) Architect/SD＝腳本雙軌維護（`.sh`+`.ps1` 各自完整實作）長期宜降為「薄 wrapper + 共用 Python 核心」（`tools/dev_start.py` 為既有示範模式），候選：`bootstrap`/`integration_gate`/`local_ci_gate`/`install_git_hooks`/`install-hooks`；(c) SD＝git hooks liveness 偵測邏輯在 `integration_gate.{sh,ps1}` 與 `local_ci_gate.{sh,ps1}` 重複 4 份、Git Bash/WSL 偵測樣板在至少 3 支 `.ps1` 重複，建議抽共用模組；(d) SD＝`check_script_parity.py` 目前只比對 step 標籤序列（docstring 自承不比語意），可選擇性加深至「雙邊實際執行 + 粗粒度輸出 diff」；(e) SA＝建議寫 `check_defect_log_crossref.py` 機械掃描文件內 `DEF-\d+-\d+` 引用，與帳本實際狀態比對揪跨文件同步落差（DEF-101-066 這類問題的治本解）；(f) QA＝「生產端接線」驗證應制度化為三段式檢核（觸發/執行/斷言），建議寫成可重用的 meta-lint 而非每輪人工複審碰運氣；(g) QA＝nightly-alert 類「等真實失敗才走到的分支」宜於設計時就內建可測試性（本輪已用 `simulate_nightly_failure` 落地此建議，其餘同類機制可比照）；(h) SA＝`gh issue create`（windows/macos-nightly-alert）缺 `--label`，與既有慣例（`aisdlc-sdd-fsm-chaos-nightly.yml` 有 `--label "p0,chaos,fsm-runtime"`）不一致 | P3（架構建議，非缺陷） | 記事存證，排入下一輪 B 軌/C 軌評估；(c)(g) 已落地 | open（記事存證；(g) 已於本輪 `simulate_nightly_failure` 落地；**(c) 已於 S11 落地**：liveness 偵測抽出 `tools/check_hooks_liveness.py`（四呼叫點 `local_ci_gate.{ps1,sh}`/`integration_gate.{ps1,sh}` 改一行呼叫）+ Git Bash 偵測抽出 `tools/lib/Find-GitBash.ps1`（三呼叫點 `ci-gate.ps1`/`install_git_hooks.ps1`/`install-hooks.ps1` dot-source 改一行呼叫），單元測試 `tools/tests/test_check_hooks_liveness.py`；其餘子項 **R14 補記（2026-07-20 Architect 檢視 ARCH-GAP-5，訂正帳本滯後實況）**：(a) **已落地**（`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py:10` S6 明文本子項、已擴至 4 份 CI workflow）；(e) **已落地**（`tools/check_defect_log_crossref.py`，`root-infra-ci.yml:44` 明文 DEF-101-068(e)）；(h) **已落地**（windows/macos-compat 兩 workflow `gh issue create` 已帶 `--label "p1,…,nightly"`）；(b) **部分落地**（local_ci_gate 已於 R12 收斂薄殼＋Python 單核心；bootstrap／integration_gate 仍雙原生實作，維持 defer）；(d) **維持 defer 並明文觸發判準**——下一個「同名 step 語意漂移」實證出現才投資「雙邊執行＋輸出 diff」重型基建；(f) **降級處置**——可機械子集（守門工具三處清單同步）已由 `tools/tests/test_root_infra_parity.py` 覆蓋，泛用 meta-lint 難寫準，殘餘改為迭代範本 checklist 條目、不硬寫泛用 lint。另註（守門侷限實證）：本列子項進度低於 `check_defect_log_crossref.py` 守門粒度（該工具只比對狀態字、無法偵測列內子項滯後），本次滯後即此侷限之實證 |
| DEF-101-081 | 2026-07-15 | 獨立重新複審（不預設上輪 APPROVE 為真）：以 `gh run list`／`gh api .../annotations` 親查 CI 實際執行紀錄，而非讀帳本 | **本輪「四方複審終裁全 APPROVE」的最終 commit `1175555` 及其前一輪 `a8e4b73`，在真實 GitHub Actions 上從未成功執行過一次——CI 因帳戶帳單問題全面停擺，且此狀態完全未被記錄或揭露**：`gh run list --workflow=windows-compat-ci.yml` 顯示 `a8e4b73`（push run `29335402759`、workflow_dispatch run `29335409369`）與 `1175555`（push run `29378155616`）的 Windows smoke job 皆在 4~7 秒內 failure 收場；`gh api repos/.../check-runs/87235788504/annotations` 證實 annotation 為 `"The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings"`（runner 從未被配置，非測試失敗）。交叉核對 `gh run list --branch main --limit 15`：同一時窗（`a8e4b73` push 當下 `2026-07-14T13:10:41Z` 起）macos-compat-ci／root-infra-ci／AutoClaude CI／aisdlc-sdd-ci 五個 workflow 同步失敗、同款帳單 annotation；本輪複審當下重跑仍為同狀態（`root-infra-ci`／`AutoClaude CI` push run `29378473172`／`29378473200`，`2026-07-15T00:14:10Z`，對應 HEAD `bda34da`），故障持續中、非單次偶發。之前最後一次真實成功執行是 `2026-07-14T13:03:47Z` 的 windows-compat-ci workflow_dispatch run `29334937643`（真實測試失敗 exit code 1，促成 `a8e4b73` 修復）。`grep -rni "billing\|帳單\|spending limit"` 對 `docs/06_quality/AutoSDD_Defect_Log.md`／`ONBOARDING.md`／`CLAUDE.md` 皆零命中——即這個涵蓋 2 個關鍵 commit、持續 11+ 小時的 CI 全面停擺，從未被四方複審任何一方發現或記錄 | P0 | **無法以程式碼變更修復**：`windows-compat-ci.yml`（及其餘 4 個 workflow）本身無 bug，annotation 顯示 job 在 workflow 任何 step 執行前就被 GitHub Actions 平台攔下，workflow 檔內沒有、也不可能有任何 step 能偵測或繞過帳戶帳單問題；根因修復需**帳號所有者**至 GitHub「Billing & plans」還清欠款或調高 spending limit，超出本 repo 程式碼與本次 agent 權限範圍。本行之補救動作僅限「補齊揭露缺口」——不留其未被記錄的狀態 | **open（需人工於 GitHub Billing & plans 處理，非本輪程式碼可解）**：本行本身即為「揭露」動作，之後任何一輪複審／nightly 若見 `windows-compat-ci.yml`／`macos-compat-ci.yml`／`root-infra-ci.yml`／`AutoClaude CI`／`aisdlc-sdd-ci` 五者同步在數秒內 failure，應先查 annotation 是否為本行同款帳單訊息，而非誤判為程式碼回歸；帳單問題解除後，須對 `1175555`（或其後續 HEAD）**重新觸發一次 workflow_dispatch 或等待下一次 push**以取得這五份 workflow 對本輪四方複審實際變更的第一次真實機械驗證結果，在那之前「四方複審 APPROVE」不應被視為已通過機械 CI 驗證。**R14 複核（2026-07-20，Architect 親查 `gh run list`＋`gh api .../runs/29688470830/jobs`）：停擺持續中**——7/19 R13 commit `44d7c25` push 觸發五 workflow 全部 2~3 秒 failure、job `steps=[]` 即殞落（runner 零配置，同款帳單 annotation）；R12／R13／R14 三輪變更均未獲任何一次雲端機械驗證，ONBOARDING §9 各「雲端 CI 機械攔回」型緩解句在停擺解除前均為紙上防線；帳務解除後須對 HEAD 補跑一次 workflow_dispatch 取得首次真實驗證 |
| DEF-101-089 | 2026-07-16 | R4 修復輪二審核准後，主 agent 親自補跑 AutoClaude 全套 pytest（QA 一審未跑完的覆蓋缺口）時實機發現 | **本機（裝有 `claude` CLI 於 PATH）執行 `AutoClaude` 全套 pytest 時，於 70% 進度處確定性卡死，CPU 5 秒取樣 delta=0（非慢，是真掛住）**：根因為 `tests/test_gap014_020.py`／`tests/test_gap039_049.py` 內多個 `@requires_claude_cli` 裝飾測試（`requires_claude_cli = pytest.mark.skipif(shutil.which("claude") is None, ...)`）——只要本機 PATH 上偵測得到 `claude` 執行檔就不會被跳過，會真的透過 `wexpect` 產生子行程執行 `claude.EXE --yes -p <prompt>`；在「本身就在一個存活的 Claude Code agent session 內執行測試」的巢狀情境下，該子行程永遠等不到可用回應而無限掛起（`wexpect` helper 行程與其父行程皆 CPU delta=0，非負載重而是真死鎖）。此為既有已知的 `test_batch_mutations_applied_via_mock_correction`（見本檔上方歷史記錄，2026-07-15 之前輪次已排除該單一測試）**同一根因類別的更廣版本**——先前只排除了撞見的第一個，`test_gap014_020.py`／`test_gap039_049.py` 內其餘至少 6 個同款 `@requires_claude_cli` 測試仍會撞上同一陷阱，代表任何本機裝有 `claude` CLI 且從巢狀 Claude Code session 執行全套本機 pytest 的開發者（本專案主要受眾）都會複現 | P2（本機開發驗證體驗缺口；正式 CI runner 未裝 `claude` CLI 故不受影響，非 CI 回歸） | 本輪未動程式碼修復（見右欄說明），改用驗證流程層級 workaround + 記事存證 | **workaround-applied，未改程式碼（本輪範圍判斷：可靠偵測「是否巢狀於存活 Claude Code session」需額外設計如何識別（環境變數？父行程樹掃描？），倉促加偵測邏輯風險蓋過收益，Rule 2 簡單優先，故不逕行改 `requires_claude_cli` 的 skip 條件）**：本輪驗證改為執行前暫時將 `claude` 執行檔目錄自 `PATH` 移除，使全部 `@requires_claude_cli` 測試比照正常 CI（未裝 `claude`）環境乾淨 skip，取得無隱藏回歸的完整跑；`python -m pytest tests/ -q` 兩次獨立重跑皆 **3557 passed, 206 skipped, 0 failed**（57~58 秒完成，無卡死）。記事存證供下一輪評估是否值得投入設計「巢狀 session 偵測」skip 條件（例如檢查特定環境變數是否存在），此刻不逕行猜測式加邏輯 |
| DEF-101-188 | 2026-07-20 | R14 全面掃描（Scan-Shell）：乾淨 PATH 唯讀實驗重現 | **`AISDLC_SDD/scripts/ci-gate.sh:39` LATEST 解析 `\|\| true` 把「python 缺席（127）」與「無版本目錄」合流靜默**：現代 macOS 乾淨 PATH 只有 `python3` 無 `python`，未啟 venv 直跑時 dry-run 假綠 exit 0（實測 `SDD_GATE_DRY_RUN=1 PATH=/usr/bin:/bin` 重現：輸出漏 LATEST 軌仍 rc=0）、非 dry-run 雙軌閘門靜默降為單軌 v0.01——與 DEF-101-005 同屬「驗證鏡子靜默縮面」家族但不同檔不同機制；15+ 裸 `python` 站點無指路守門（姊妹腳本 integration_gate.sh／local_ci_gate.sh／git_hooks_install_common.sh／macos_smoke_local.sh 皆有） | P3 | 本輪修復 | fixed@R14（ci-gate.sh 檔頭補 `command -v python` fail-loud 守門〔與姊妹腳本同款文案〕＋ LATEST 為空時 stderr 降軌警示可見化；實測無 python 時 rc=1 指路 venv、正常路徑雙軌解析不變；`test_ci_gate_version_resolution.py` 4 passed） |
| DEF-101-189 | 2026-07-20 | R14 全面掃描（Scan-Shell） | **LATEST `tools/install_hooks/install_post_commit.ps1` 以 `WriteAllText` 寫出 hook 後不設 exec bit**：pwsh-on-macOS/Linux 載體（R11 取證即實際走過）下 git 直呼印 hint 後忽略、根層 dispatcher `tools/git-hooks/post-commit:36` 的 `[ -x ]` 判 false 後零告警跳過（advisory 恆 exit 0）＝靜默失效；`.sh` 版第 74 行有 `chmod +x` 而 `.ps1` 無，R11 BYTE_IDENTICAL 取證比內容位元組不含 file mode 故未攔到 | P4 | 本輪修復（LATEST v0.30；凍結版依紀律不回改，與 DEF-101-019/020 同款 known-gap 家族） | fixed@R14（`.ps1` 寫檔後以 `OSVersion.Platform -eq 'Unix'` 判非 Windows 補 `chmod +x`〔PS 5.1 無 `$IsWindows`〕；pwsh Parser 驗證通過） |
| DEF-101-190 | 2026-07-20 | R14 全面掃描（Scan-Shell）；主控複核部分訂正 | LATEST `tools/fsm_runtime/formal/run_tlc.ps1` 三站點 `Tee-Object -FilePath` 寫 log——掃描原判「PS 5.1 下固定 UTF-16LE 與 .sh 版 UTF-8 log 不對稱」。**主控複核訂正：該檔檔頭（DEF-101-124）已有 pwsh 7+ fail-loud 守衛（PS 5.1 直接 exit 2 導流），UTF-16 情境實際不可達，原判屬部分誤報**；惟顯式編碼取代版本預設仍屬輕微硬化 | P4 | 本輪順手硬化（非必要修復，如實記載誤報成分） | fixed@R14（三站點 `Tee-Object \| Out-Null` 改 `Out-File -Encoding utf8`——行為等價〔原式即丟棄 console 流〕且不再依賴 pwsh 版本預設；pwsh Parser 驗證通過） |
| DEF-101-191 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs）：審 R14 降頻變更自身 | **`autoclaude-ci.yml` R14 降頻註記把每日兜底誤述為「本地 nightly 7-stage（run_local_nightly.sh/.ps1）」**：`.sh` 實為 R11 D1 拍板的 4-stage 薄聚合器（檔頭明文深度 stage 不移植），pg-e2e/perf/mutation 深度三軌兜底只有 Windows schtasks 一條腿——降頻決策權威紀錄自身失準會誤導未來稽核 | P3 | 本輪修復 | fixed@R14（註記改精確：深度三軌僅 Windows 7-stage 承擔、mac 4-stage 不覆蓋） |
| DEF-101-192 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs）：降頻連鎖 | `AutoClaude/docs/04_planning/ADR/ADR-SD08-003-perf-regression-policy.md` §2.1 表 (b) 宣稱 perf CI nightly「每天 02:00 UTC」——R14 降頻後頻率宣稱漂移（該 ADR 為現行 perf 政策權威非歷史快照） | P3 | 本輪修復 | fixed@R14（表格行補 R14 修訂註記：每週一 02:00 UTC；每日訊號改由 Windows 本地 nightly perf-baseline stage 承擔） |
| DEF-101-193 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs）：審 R14 變更自身 | `aisdlc-sdd-arch-fitness.yml:8` 檔頭「nightly 以 --trend 把**每日** score 累積回寫 TREND.yaml」——R14 已改 L13/L35 卻漏 L8，同檔自相矛盾 | P4 | 本輪修復 | fixed@R14（L8 改「每次排程（R14 起每週一）」） |
| DEF-101-194 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs）：降頻連鎖 | **arch-fitness「每日兜底＝本地 nightly sdd 軌」宣稱單腿＋零取證**：結構上僅 mac 4-stage（ci-gate.sh 內含 arch_fitness --strict 雙軌）承載、Windows 7-stage 無此軌；且 launchd R14 當日才真載入、心跳零實績（首跑 2026-07-21 02:00） | P3 | 本輪修復（文字精確化）＋取證補驗待首跑 | fixed@R14（註記精確化為「mac 單腿＋心跳哨兵取證」；**取證補驗**：2026-07-21 心跳落地後以 `bash tools/install_mac_nightly.sh --status` 驗新鮮並記錄，未落地則查 `AutoClaude/logs/nightly_mac_launchd.err`） |
| DEF-101-195 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs） | **`autoclaude-mutation-on-change.yml` 為全 repo 11 檔 workflow 中唯一 job 無 `timeout-minutes`**：mutmut/Docker 卡死一次即燒 GitHub 預設上限 360 分鐘 runner——帳務停擺＋額度緊縮下風險放大 | P3 | 本輪修復 | fixed@R14（補 `timeout-minutes: 60`＝帳本記載 45 分鐘上緣＋緩衝；YAML 驗證通過） |
| DEF-101-196 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs） | **`autoclaude-ci.yml` push 閘四 job（test/claude-md-budget/equivalence/pg-contract）無 `if:` 過濾，每次 schedule cron 都完整重跑**：降頻前每日 2 次、降頻後每週一 2 次＝純額度洩漏且無「刻意保留」註記 | P4 | 本輪修復（額度緊縮期使用者已裁決額度優先） | fixed@R14（四 job 補 `if: github.event_name != 'schedule'`＋WHY 註記；push/PR/dispatch 行為不變；YAML 驗證通過） |
| DEF-101-197 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs） | `ONBOARDING.md` §6 執行權限政策 755 rationale 句「plist ProgramArguments 以腳本路徑為執行檔」與實況矛盾——§8 範本與 `install_mac_nightly.sh` 產出的 plist 皆以 `/bin/bash` 為執行檔、腳本僅為引數，exec bit 非執行必要 | P4 | 本輪修復 | fixed@R14（rationale 訂正：R11 D6 決策延續＋容忍手動直呼的防禦，非 launchd 執行必要） |
| DEF-101-198 | 2026-07-20 | R14 全面掃描（Scan-CI-Docs） | 決策紀錄類頻率漂移二處：① `AISDLC_SDD/docs/02_architecture/adr/ADR-001-local-first-ci-parity.md:17`「drift-daily 與 arch-fitness 同 cron(02:30)」——降頻後僅週一重疊（`main-push-serialize` 緩解仍在且仍必要，無行為風險，屬歷史問題陳述段）；② `ADR-SD09-002` §2.3 active pilot「每日 02:00 UTC」——R14 前即與實況（03:00）不符的既有漂移，降頻再加深 | P4 | ② 本輪修復；① 歷史背景敘述不改、記事存證 | fixed@R14（② §2.3 補 R14 修訂註記載明現行實況＝TG 每週一 03:00、GS/Coord dormant；① 存證不改文） |
| DEF-101-199 | 2026-07-20 | R14 全面掃描（Scan-Python）：766 檔 AST+regex 全掃 | **subprocess-encoding AST 守門十樹清單的兩處理論覆蓋缺口**（現況零風險、防未來漏網）：① LATEST `tools/arch_fitness/` 為 fsm_runtime 外唯一 Python 樹，恆在清單外；② `AISDLC_SDD/conftest.py` 為十樹外唯一活躍漏網檔（目錄機制掃不到、又不能納整個 AISDLC_SDD/ 根——rglob 會誤掃凍結版）。掃描本體結論：**十樹內 open()/subprocess/getoutput 無 encoding＝全零站點，零筆新增 P0~P2** | P4 | 本輪修復 | fixed@R14（`test_subprocess_encoding_hygiene.py` 樹清單納入 arch_fitness〔實掃 2 檔下限 1，升版動態跟隨〕＋新增 `_scan_single_files` 單檔納管機制收 conftest.py，雙清單均由 pinning test 釘選；全套測試綠） |
| DEF-101-200 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-2） | **排程「初次安裝」能力反向不對等——mac 有一鍵安裝器、Windows 沒有**：`AutoClaude/tools/fix_nightly_catchup.ps1:16-25` 以 `$ErrorActionPreference='Stop'`＋`Get-ScheduledTask` 開場，任務不存在直接炸＝僅能校正既有任務；新 Windows 機器（或任務誤刪後）無一鍵重建路徑，亦無 `--status` 心跳三態對等物 | P2 | 排下輪 Windows 輪（需實機驗證，勿在 mac 輪盲改）：升級為 ensure 語意（不存在→`Register-ScheduledTask` 建立）＋加 `-Status`，或另立 `install_win_nightly.ps1` 並更新 parity 登記 | open（routed 下輪 Windows 輪；ARCH-OPT-5 設計已載於本列分流欄；**R15 補三筆 Windows 輪同捆項**：ARCH-R15-5〔`run_local_nightly.ps1` 每日 RunId log 無保留期 prune、mac 側 R15 已落 14 天，兩平台政策須對齊〕、ARCH-R15-1 Windows 側〔`nightly_latest.log` 為全量 log 非 3 行契約，FAIL 內容哨兵需另設計〕、SCAN-C-11〔windows-compat nightly-full 取消語意與 macos 側 R13 CI-5 不對齊，僅 dispatch 連發情境〕） |
| DEF-101-201 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-3） | **mac nightly 取證留存性系統性弱於 Windows**：①（已修）plist stdout/stderr 原導 `/tmp/autoclaude_nightly.{log,err}`——macOS 週期清理＋重開機清空，深夜失敗數日後排查 log 已散失；②（殘）心跳檔僅 3 行彙總、無 Windows 側 RunId log＋latest pointer 對等物，取證紀律 #3「PASS 聲稱必須引 RunId log:L」在 mac 結構上無法履行 | P2 | ① 本輪修復；② 排下輪（mac RunId log 對等） | fixed@R14（①：`install_mac_nightly.sh` heredoc 改導 `AutoClaude/logs/nightly_mac_launchd.{log,err}`〔gitignored、與心跳檔同目錄〕＋install 時 `mkdir -p` 防 launchd 缺目錄靜默丟 log＋ONBOARDING §8 範本同步＋已載入 plist 冪等重裝生效實證＋smoke 11 綠；**②殘項 open 記於本列**，下輪評估 RunId log；**② fixed@R15**：`run_local_nightly.sh` RunId log 落地——`AutoClaude/logs/nightly_mac_<ts>.log`（`exec >>` 改道＋BEGIN 首行 run_id、心跳檔第 4 行 `log=` 指標、`find -mtime +14` 14 天輪替；**心跳前 3 行契約與檔名零改動**）＋真機取證 `nightly_mac_20260720_182753.log` 四 stage PASS=4 FAIL=0（AutoClaude 3684/144、SDD 1475+1674；取證紀律 #3 mac 側自此可履行，`Nightly_Forensic_Discipline.md` v1.8 同步 mac 註記）；**ARCH-R14-REV-2（launchd log 無輪替）以需求物理消滅結案**——輸出改道後 launchd log 僅剩 exec 前啟動錯誤自限量，不建輪替機制（Rule 2）） |
| DEF-101-202 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-4） | **bash 3.2／BSD 工具紀律零機械靜態守門**：知識散落 8+ 檔案檔頭註解、無單一定義點；`bash -n` 攔不住 `declare -A`／`mapfile`（builtin 執行期才炸，DEF-101-004 即此家族）；smoke 動態網僅及被覆蓋腳本，新增未覆蓋 `.sh` 時純靠紀律；CI 停擺期間連 macos-smoke 動態網也不在 | P3 | 排下輪（ARCH-OPT-4）：新守門 `check_bash32_compat.py` regex 掃 active `.sh`（凍結版排除）之 bash4+ 家族＋`bash4-ok:` 行內豁免＋stale 自檢，掛 root-infra 第 12 道＋pre-push guard 清單 | **fixed@R15（搭載方式經 Architect 改判）**：依防線預算制搭載優先序**否決原案**「獨立 `check_bash32_compat.py` 掛 root-infra 第 12 道＋pre-push 第 6 支」（四站點連動、觸 DEF-101-209 警戒），改 **unittest 搭載** `tools/tests/test_bash32_compat.py`（六樹 28 檔 git ls-files 列舉＋LATEST 經 sdd_version.py SSOT 解析 fail-loud；A 組 9 執行期必炸 pattern＋B 組 3 BSD 紀律 pattern＝ONBOARDING §8 靜態自查清單機械化；字元級剝註解防字串內 `#` 誤剝；`bash4-ok:` 豁免 WHY 必填＋stale 自檢；per-tree 下限＋樹清單釘選；紅綠自證 fixture）——自動獲 root-infra 第 8 道＋pre-push＋mac nightly 三處執行零接線；實掃 active 六樹**零命中**（守回歸非守現況，與 R14/R15 兩輪人工掃描一致）。**退場條件（SA-R15-REV-2 補）**：若專案改採 bash 4+ 為最低支援版本（放棄 macOS 內建 3.2 相容），本守門即可拆除 |
| DEF-101-203 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-6）：本機當日即處於誤導窗實證 | **dev_start 心跳哨兵「已安裝、未首跑」窗誤導**：launchd 已載入但心跳檔尚未產生時提示「排程可能未啟用」；與 `install_mac_nightly.sh --status` 文案（已補「或尚未跑過第一輪」）實質分歧，且 mac 明明可廉價查 `launchctl` 消歧 | P3 | 文案版本輪修復；launchctl 查核版排下輪（ARCH-OPT-3 完整版） | fixed@R14（文案版：`dev_start.py` 缺席分支補「或已安裝但尚未跑過第一輪（mac 查證：install_mac_nightly.sh --status）」＋`test_dev_start.py` 補斷言鎖消歧文案，9 passed；**launchctl 精確查核殘項 open 記於本列**下輪評估；**launchctl 精確查核版 fixed@R15**：`dev_start.py` `_launchd_nightly_loaded()`——`launchctl list` 第 3 欄**精確等值** `com.autoclaude.nightly`（防 `nightly2` 前綴誤中，測試含干擾列 case）；非 darwin/rc≠0/例外回 None 維持原雙可能文案；缺席分支三態文案（已載入未首跑＝正常／未載入＝指路安裝／未知＝原文案），真機實跑取得 True 態實證；**label 與 8 天門檻雙站點字面互鎖**測試同輪落地（ARCH-R15-2/R15-3，regex 雙側抽取斷言相等）） |
| DEF-101-204 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-7） | ONBOARDING §6/§8 雙平台對照表（人工）與 `check_script_parity.py` `_SINGLE_SIDED_EXEMPT` 登記清單（機械）互相引用、零機械互鎖——單邊改動無訊號（pytest 基線已有站點鎖先例，此處尚無） | P4 | 排下輪（ARCH-OPT-6）：比照 `test_smoke_ci_sync` 抽取式互鎖手法 | **fixed@R15**：`tools/tests/test_onboarding_parity_interlock.py` 抽取式互鎖落地——範圍**刻意縮至兩條明文清單**防誤傷（Architect 親核 17 條豁免僅 9 條有 ONBOARDING 明文清單對應）：§6 bash-only 六支 ↔ `_SINGLE_SIDED_EXEMPT` rationale 含「bash-only」之鍵集合（「各版」措辭正規化 LATEST key）、§8 ops 排程家族三支 ↔ schtasks/§8 鍵之 `.ps1` 檔名集合；抽取下限 6/3 釘選＋錨定行恰一行 fail-loud＋紅綠自證三態；實跑現況兩側一致。**退場條件（SA-R15-REV-2 補）**：若 ONBOARDING §6/§8 兩份人工清單改為自 `_SINGLE_SIDED_EXEMPT` 機械生成（消滅雙寫），本互鎖測試即可拆除 |
| DEF-101-205 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-8） | exec-bit 政策（ONBOARDING §6 644/755 範圍句）無機械守門——R14 抽查現況合規（git index 755 集合＝6 hook＋run_local_nightly.sh 與政策句一致），漂移無訊號 | P4 | 低 ROI 單獨立項不值；可併入既有 NTFS/EOL 類掃描順手做，擇機 | open（記事存證） |
| DEF-101-206 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-9） | 深度 nightly 五軌（mutation/pg-e2e/perf/drift/obs）單押 Windows 一台實體機——R12 已如實揭露「心跳只能守本機」；CI 停擺＋R14 GitHub 深度軌降週頻雙重放大此單點（Windows 機斷載時五軌全球僅剩每週一 GitHub 窗口，且該窗口自身停擺中） | P4 | 記事存證；根本解＝帳務恢復（DEF-101-081）＋跨機心跳（超本輪範圍） | open（記事存證，與 DEF-101-081 交叉參照） |
| DEF-101-207 | 2026-07-20 | R14 Architect 演化壓力測試（Python 升版情境）實測發現 | **`tools/dev_start.py` venv 換手/重建判定只看「平台形狀＋依賴檔 hash」，`.python-version` 內容不在 hash 內**：Python 升版後既有 3.11 venv 被靜默沿用，僅 bootstrap「新建」路徑才警告版本不符 | P3 | 排下輪（ARCH-OPT-7）：`.python-version` 納入 `_deps_hash()` 或整備時比對 venv `python -V` major.minor | **fixed@R15（方案裁決：原案「納 `_deps_hash()`」經 Architect 因果論證否決**——hash 變動觸發 bootstrap 後，`bootstrap.sh` 對既有 venv 只驗平台形狀即沿用直譯器（版本警告僅在新建路徑），納 hash＝白付一次全量重裝、病灶原封）：落地整備後實測——`dev_start.py` `_python_version_target()`（讀 pin 截 major.minor，缺檔靜默）＋`_venv_python_minor()`（實測 venv 直譯器，失敗回 None）於 `step_venv` 成功收尾塊比對（涵蓋沿用/快取換回/hash 未變/bootstrap 完成全路徑），不一致 `_warn` 指路刪 .venv 重建（advisory 不阻擋，與 bootstrap「≥3.11 可用、pin 僅提醒」語意一致）；3 test case 鎖三態 |
| DEF-101-208 | 2026-07-20 | R14 Architect 架構檢視（ARCH-OPT-9） | CI 停擺期間「CI 活性」零本地訊號——五 workflow 同步秒級 failure 已持續 6 天無人機械通報（DEF-101-081 R14 複核佐證）；dev_start advisory 可加「CI 活性哨兵」（`gh run list` 最新 conclusion 檢查，read API 不耗 Actions 額度，離線/無 gh 靜默跳過） | P4 | 排下輪（ARCH-OPT-9(b)）；OPT-9(a)＝帳務解除後人工對 HEAD 補跑一次 workflow_dispatch（記於 DEF-101-081） | **fixed@R15**：`dev_start.py` `_check_ci_liveness()`——三道靜默跳過閘（無 gh／`SUMMARY["sync"]` 以「離線」「跳過」開頭＝重用 step_sync 判定不發第二次網路探測／非 git repo）；`gh run list --limit 1 --json`（read-only API **零 Actions 額度**，守 CI 額度鐵律）；rc≠0 或 JSON 壞→「CI 活性未知」一行不入 WARNINGS；非 success → `_warn` 引 DEF-101-081/208；全函式 try/except 兜底不改 exit（同心跳哨兵契約）；4 test case 鎖四態 |
| DEF-101-209 | 2026-07-20 | R14 Architect 複雜度審計（CPLX-1） | 防線塔遞迴深度已達四階（守門→守門之測試→數量下限釘選→清單交叉鎖），Architect 判定「不是過度工程但已達邊際」——第五階（互鎖的互鎖）邊際效益必為負，應治理級明文凍結；新防線須引實證缺陷編號、優先擴充既有掃描器樹清單而非新建掃描器 | P4 | 排下輪（ARCH-OPT-10）：「防線預算制」寫入 `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` | **fixed@R15**：防線預算制條文入範本「🔒 架構紅線（每輪不變）」節末——四階凍結（禁第五階互鎖之互鎖）＋新防線三要件（(1) 實證缺陷編號、(2) 搭載優先序：擴充既有掃描器＞`tools/tests/` unittest 搭載（零接線自獲三處執行）＞獨立 `check_*.py` 工具位（限非 Python 載具呼叫者、三站點同步）、(3) 退場條件載明）＋審查者對違反者一律 REJECT；R15 當輪 3 支新守門（bash32/ps1-bom/onboarding-interlock）全依此條文走 unittest 搭載＝條文可執行性自證 |
| DEF-101-210 | 2026-07-20 | R14 四方一審（Architect/SA/SD/QA 獨立背景 agent，全數 APPROVE 零阻擋） | **一審 17 發現（3 P3＋14 P4）同輪全修 15 項＋defer 2 項**。P3×3：①ci-gate.sh 新守門零測試鎖（ARCH-R14-REV-1＋QA-R14-REV-1 獨立交叉）→ `test_ci_gate_version_resolution.py` 補 3 case（python 缺席 rc=1／resolver 失敗 stderr 降軌警示＋stdout 純淨／SDD_FW_VERSION 覆寫時不印警示），7 passed；②workflows cron↔if 配對零機械鎖（QA-R14-REV-2，DEF-01-004 同家族）→ 新守門測試 `tools/tests/test_workflow_schedule_sync.py`（雙向集合相等，regex 抽取零依賴）；③（P3 計入 ①）。P4×14 擇要：ci-gate 降軌警示移至 FW_VERSIONS 定案後（SD-R14-REV-1）；install_post_commit.ps1 chmod 失敗補 fail-loud（SD-R14-REV-2）；單檔掃描清單經 `git ls-files` 全列舉補齊 3 檔＋LATEST 正規化 pinning（SD-R14-REV-3＋ARCH-R14-REV-4 獨立交叉；tmp_lint_check.py 為 tracked 暫存殘留，去留下輪裁決）；dev_start 查證指令依 flavor 分支（mac --status／windows schtasks，SD-R14-REV-4＋ARCH-R14-REV-5 獨立交叉）＋print 側斷言（QA-R14-REV-5）；smoke [6] 補 log 落點 grep 斷言（QA-R14-REV-4，plist 範本輕量互鎖）；pwsh 載體 exec bit 自動化回歸鎖 `test_install_post_commit_exec_bit.py`（QA-R14-REV-3，fake monorepo 骨架實跑安裝器）；文件 5 處（drift-daily 註解過時 SA-R14-REV-1、useMacWin 消歧未同步×2 SA-R14-REV-2、ADR-SD09-011 現況註記 SA-R14-REV-3、archive_04 表頭「狀態宣稱型引用」措辭精確化 SA-R14-REV-4、run_tlc.ps1 檔頭例子過時 ARCH 觀察②） | P3×2＋P4×14（一審彙整） | 本輪全修 15 項；**defer 2 項併入既有 open 列**：ARCH-R14-REV-2（launchd log append 無輪替，/tmp 時代隱性輪替消失）併 DEF-101-201 ② 下輪與 RunId log 一併設計；ARCH-R14-REV-3（plist 範本三方全面機械互鎖）併 DEF-101-204 抽取式互鎖（smoke [6] grep 已部分落地） | fixed@R14-一審修復（證據見左欄各站點；修復後複驗：ci-gate 版本測試 7 passed、heartbeat 10 passed、hygiene 9 passed、workflow-sync 1 passed、exec-bit 1 passed、smoke PASS=11 含新斷言） |
| DEF-101-211 | 2026-07-20 | R14 四方複審（Architect/SA/SD/QA opus 換模型繞開 Fable 5 限額，全數 APPROVE 零阻擋；bug-injection 交叉實證測試真能紅） | **複審再揪 6 發現（1 P3＋5 P4），主控就地修 3＋誤報 1＋defer 2**。**QA-R14-RR-1（P3，事實錯誤，必修）**：`autoclaude-mutation-on-change.yml` 註記「全 repo 11 檔唯一無 timeout 的 job」經機械掃描證實為假——另有 3 個輕量 job 無 timeout（`aisdlc-sdd-fsm-chaos-nightly.yml` track-streak-and-lock、macos/windows-compat-ci.yml 兩 nightly-alert）→ 三 job 各補 `timeout-minutes: 10`＋訂正錯誤註記＋新守門 `tools/tests/test_workflow_timeout_coverage.py`（機械鎖「所有 workflow 所有 job 皆有 timeout」＋自證判準紅綠，消滅人工目視「唯一/都有」誤判），3 passed。**ARCH-R14-RR-1（P4，必修）**：帳本輪替註記「R14 輪替後共 30 列」與主檔實際 53 列差 23 易誤讀 → 改為「保留 30 既有＋新增 23＝主檔現 53 列，30 僅指搬移當下既有留存子集」。**QA-R14-RR-3（P4，誤報）**：稱「compat paths 補 tmp_lint_check 無機械鎖」——Architect 複審以 bug-injection 實測反證（移除該 path 行 → `test_ci_paths_cover_root_consumers` 因 _SLASH_RE 匹配 hygiene 測試內字串而轉紅，指名 tmp_lint_check.py），機械鎖已存在，採 Architect 實測 > QA 推論。 | P3×1＋P4×2（複審彙整，就地修/裁決） | 就地修 3（QA-RR-1／ARCH-RR-1／含 meta 測試）；QA-RR-3 誤報結案；**defer 2**：QA-R14-RR-2（push 閘四 job `if != 'schedule'` 無機械鎖，P4 額度洩漏非正確性，降頻後洩漏極小，強鎖「哪些 job 該有」需語意判斷且觸 DEF-101-209 防線預算警戒——記事下輪評估）；ARCH-R14-RR-2（CI-2 降週頻放大 Windows nightly 斷載盲區，併既有 defer ARCH-R13-7 git 可見 liveness，交叉參照 DEF-101-206/208）；QA-R14-RR-4（mkdir 在 cmd_install 無 smoke 覆蓋＝真安裝屬使用者 ops 的已知取捨） | fixed@R14-複審修復（3 job timeout 補齊＋test_workflow_timeout_coverage.py 3 passed＋帳本數字訂正＋YAML 全驗；四方複審 4/4 APPROVE 為本列前提） |
| DEF-101-212 | 2026-07-20 | R15 全面掃描（4 獨立維度 agent：A Shell/PS、B Python、C CI/排程基建、D 文件帳本）＋Architect 架構檢視（第 5 agent，總評五層架構合理、裁決「新增內容掃描守門一律 unittest 搭載」＝防線預算制具體化） | **R15 掃描＋檢視共 21 發現（P3×6＋P4×15）＋6 筆架構缺口 [ARCH-R15-1~6]，零 P0~P2（五輪硬化實證）**。P3×6 全修：①SCAN-C-1 launchd 關機錯過不補跑（Windows 側 StartWhenAvailable 同型缺陷 R11 已修、mac 零對等）→ plist 加 `RunAtLoad`＋aggregator 心跳 mtime 當日去重（`stat -f %Sm` BSD）＋`--force` 繞過，等價 StartWhenAvailable 且冪等；②SCAN-C-2 .ps1 BOM 本地零守門（CI 停擺期人工編輯零訊號直推）→ `test_ps1_bom.py` unittest 搭載（19 支、半殘 BOM 反向斷言、紅綠自證；**退場條件〔SA-R15-REV-2 補〕**：CI 帳務恢復〔DEF-101-081〕且雲端 root-infra-ci 第 2 道穩定運作 N 輪後，本地對等可視為冗餘評估拆除）；③SCAN-C-3 timeout 守門解析器 fail-open（合成樣本實證 3 種合法 YAML 寫法靜默逸出）→ 容忍行尾註解＋每檔 ≥1 job fail-closed＋`*.yaml` 納掃＋11 檔下限＋自證測試；④SCAN-C-4 `aisdlc-sdd-arch-fitness.yml` 無 permissions → workflow 層 `contents: read`（nightly-strict job 層 write 覆寫保留，pyyaml 結構斷言驗證）；⑤SCAN-C-5 autoclaude-ci 唯一無 concurrency push 閘 → group 含 ref+event_name+event.schedule（兩 cron 各自成組互不取消、push 連發舊跑取消止額度洩漏）；⑥ARCH-R15-1（＝SCAN-C-8② 獨立交叉）心跳只驗活性不驗結果（nightly 連續全紅晨間仍 ✅）→ dev_start 心跳哨兵讀前 3 行 `FAIL=(\d+)`，>0 → `_warn`（mac 側；Windows 側路由 DEF-101-200）。P4×15：修 12——SCAN-A-1（v0.30 init_project.sh `local` 吞 mktemp rc，分離宣告；全檔同型僅此一處；凍結版不回改）、SCAN-A-2/D-1（DEF-101-056 BOM 敘述訂正入帳）、SCAN-A-3（g0_gate_check.ps1 `Add-Content` 補 `-Encoding utf8`，ASCII 純度保持）、SCAN-B-1（check_both_mode_metrics.py 補 stdout/stderr reconfigure，DEF-101-069/154 家族唯一漏網，鏡射 check_loc_budget 慣例）、SCAN-B-2（sync_exposed_skills.py `--write` 裸 rmtree → `_rmtree_windows_safe` onerror 清唯讀重試＋可讀錯誤；沙盒煙測抓到首版 fd-based rmtree TypeError 自缺陷已修）、SCAN-C-9（schedule-sync 守門剝整行註解防殘骸誤匹配＋自證）、SCAN-C-10（windows-compat 兩 pwsh 站點非末條 native 指令補 `$LASTEXITCODE` throw）、SCAN-D-2（輪替註記改方法引用消滅快照漂移面）、SCAN-D-3（ONBOARDING §8 消歧句唯一漏網站點補齊）、SCAN-D-4（Nightly_Forensic_Discipline v1.8 補 mac 聚合器與紀律 #3 mac 註記＋CLAUDE.md 摘要引用同步）、ARCH-R15-2（8 天門檻雙站點 regex 互鎖測試，計入 P4×15）、ARCH-R15-3（launchd label 雙站點互鎖測試，計入 P4×15，SA-R15-REV-5 訂正本句補標「計入」）；defer 3——**SCAN-C-6（chaos/drift/cleanup 三支 daily cron 未納 R14 降頻裁決，chaos 週最壞 3.5h 為最大單耗；Rule 9.9.4 nightly chaos 要求 vs 額度張力屬使用者決策，本輪送裁決勿自動回滿頻）**、SCAN-C-7（actions 版本雙家族並存 checkout@v4/v5 等，機械統一但帳務停擺無法雲端驗證——併 DEF-101-081 帳務解除後一次統一）、SCAN-C-8①（mac nightly stage 無 timeout 掛死→心跳停更→launchd 不重入之鏈式盲區；watchdog 需 bash 3.2 手工實作，複雜度 vs 8 天 advisory 兜底＋FAIL 哨兵已補，記事下輪評估）；另 SCAN-C-11/ARCH-R15-5 併 DEF-101-200（Windows 輪）、ARCH-R15-4 確認非缺口（--status 自身已印載入態）、ARCH-R15-6 隨 209 條文自然緩解（§6「十一道」數字站點下次觸及時改引用式）。 | P3×6＋P4×15（R15 掃描彙整） | 本輪修 P3×6＋P4×12；defer 3（C-6 送使用者、C-7 併 081、C-8① 記事）；2 併 200 | fixed@R15-掃描修復（證據見左欄各站點；修復後複驗：根 unittests 290 全綠、mac smoke PASS=13、nightly 真機四 stage PASS=4〔RunId log nightly_mac_20260720_182753.log，AutoClaude 3684/144、SDD 1475+1674〕、當日去重秒退實證、三支新守門紅綠自證、workflows YAML 結構斷言全過） |
| DEF-101-213 | 2026-07-21 | R15 四方一審（Architect/SA/SD/QA 四位獨立背景 agent；因 API session limit 中途重跑 Architect/SD/QA 三方，SA 一輪完成） | **四方一審共 4/4 APPROVE-with-conditions（零 REJECT、零 P0/P1）：SA（1 P3＋5 P4）、QA（1 P2＋4 P3）、Architect（3 P2＋4 P3＋1 P4 資訊性）、SD（1 P2＋3 P3＋1 低信心觀察）**。**P2×5 全修**：①ARCH-R15-REV-1（`run_local_nightly.sh` RunId log 建立在「當日去重」判斷之前——本機真實產生 `nightly_mac_20260720_183245.log`／`_184005.log` 兩份僅含 BEGIN+跳過訊息的殘留 log 為證）→ 去重判斷提前至 exec 改道之前，去重時直出 stdout（無 RunId log 副作用）；②ARCH-R15-REV-2（`exec >>` 對手動互動式執行也無條件生效，終端機零輸出——未被評估的 UX 回歸）→ `[ -t 1 ]` 偵測、互動終端機改走 `exec > >(tee -a log) 2>&1` 雙寫（process substitution 於真機系統 bash 3.2.57 實測驗證語法可用、雙寫正確、exit code 不受影響）；③ARCH-R15-REV-3（`_rmtree_windows_safe()` 零測試覆蓋——chmod-retry 與 TypeError fallback 兩條防禦路徑無自動化訊號）→ 新增 `AISDLC_SDD/scripts/tests/test_sync_exposed_skills.py`（4 case：正常刪除零副作用、chmod 重試成功、TypeError 回拋原始錯誤、最終失敗包裝可讀 RuntimeError；`scripts/tests/` 迴歸 169→173 passed）；④SD-R15-REV-1（`_check_ci_liveness()` 只 fetch `conclusion` 未取 `status`——GitHub Actions run 尚未跑完時 `conclusion` 恆為 null，剛 push 完就跑會被誤判為「帳務停擺/失敗」）→ `--json` 加 `status`，非 `completed` 一律視為「執行中」不落入 `_warn`；補 2 個新測試（in_progress／queued）＋訂正既有 2 個測試（payload 補 `status: completed`，否則會在新邏輯下失去覆蓋意義）。**P3×9 修 7、defer 2**：QA-R15-REV-1（smoke `[7/7]` 5 錨點中 3 個為整檔 `grep -qF` 純子字串比對，可被同名註解殘骸偽裝——QA 實測「功能碼 `-mtime +14`→`+30`、保留舊註解字樣」仍全綠）→ 比照 SCAN-C-9 剝除整行註解（`grep -v '^[[:space:]]*#'`，BSD/GNU 相容非 GNU 專屬 `\s`）後再比對；主控以 QA 的確切 repro 重新 bug-injection 驗證修復後**真轉紅**、還原乾淨。QA-R15-REV-3（DEF-101-207 `_python_version_target()`／`_venv_python_minor()` 全程被既有測試 mock 掉、零真身覆蓋）→ 新增 `TestVenvPythonVersionRealBody`（8 case：二/三段版號截斷、前後空白、缺檔、空白內容、`sys.executable` 真實 subprocess 驅動、不存在路徑、非零 exit）。QA-R15-REV-4（SCAN-C-4/C-5 permissions／concurrency 裸修無鎖）→ 新增 `tools/tests/test_workflow_permission_concurrency_lock.py`（3 case，零 pyyaml 依賴、行級 regex，比照既有 workflow 測試紀律）；bug-injection 驗證兩鎖真轉紅（**修復過程中主控自身一度誤用 `git checkout --` 還原單一測試檔案，連帶把 `autoclaude-ci.yml` 的 R15 全部異動〔含 concurrency 本身〕打回 git HEAD——與 QA-R15-REV-5／SD 併發污染觀察同根「git checkout/restore 對尚未 commit 異動的破壞性」，當場發現並用 Edit 精確重新套用＋YAML 結構複驗＋相鄰 workflow 守門測試迴歸確認無恙**）。SD-R15-REV-2（`test_workflow_schedule_sync.py` 模組 docstring 含未跳脫 `\s*#`〔單反斜線，同輪相鄰函式 docstring 卻正確用雙反斜線〕，`DeprecationWarning: invalid escape sequence`）→ 訂正為 `\\s*#`。ARCH-R15-REV-6（防線預算制條文「新防線三要件」第 (3) 退場條件，三支 unittest 掃描器〔202/204/212②〕皆補、三個 dev_start 內嵌 advisory sentinel〔207/208/203②〕皆無——條文對「永久性健檢」是否豁免退場要件未明文）→ 條文補澄清句：要件 (3) 僅適用掃描器/守門/互鎖，不適用隨依附機制存在而存在的永久性 advisory sentinel。ARCH-R15-REV-7（`useMacWin.md` 兩處「CI 活性哨兵僅當已同步成功時才會嘗試」措辭比程式碼實際閘門〔`sync.startswith("離線"/"跳過")` 才跳過〕更窄——`step_sync()` 的「未同步（髒工作樹/分叉/pull 失敗）」三態不以此開頭、實際仍會嘗試）→ 兩處措辭訂正為「非離線、非跳過同步判定」。**defer 2**：ARCH-R15-REV-4（無 per-stage timeout + 無 lock 疊跑風險，併入既有 DEF-101-206/SCAN-C-8① 記事，補充「並發競爭 `.pytest_cache` 等共用狀態」放大面向）；ARCH-R15-REV-5（當日去重純依賴心跳 mtime，雲端同步資料夾情境可能被外部程序碰觸 mtime 騙過——既有設計模式〔顯示用心跳新鮮度〕延伸出新用途〔功能性去重閘門〕使風險等級從「顯示失真」升級「靜默跳過驗證」，記事下輪評估改用心跳內容日期）。**P4×1 資訊性、不修復**：ARCH-R15-REV-8（起點盤點「20 modified」vs 實際「21 modified」——逐一核對 21 檔全部有據可查、非未授權檔案，純任務摘要人工計數落差，記錄存證）。**低信心觀察 1 筆記事**：SD-R15-REV-5（`g0_gate_check.ps1` 逐行 `Add-Content -Encoding utf8` 在 PS5.1 是否會重複吐 BOM——SD 自陳記憶不確定、macOS 沙盒無法驗證，記事待 Windows 輪真機以 `Format-Hex` 查證）。**裁決不修（附理由，非 defer）**：SD-R15-REV-3（`test_ps1_bom.py` 對 UTF-16 無 BOM 純 ASCII 檔案漏判——現實中主流編輯器極少預設存成此編碼，風險方向低）；SD-R15-REV-4（`test_bash32_compat.py` 註解分隔符集合不含 `)`，`func()# comment` 型態會誤判——風險方向安全〔只會多攔誤報、絕不會放過真違規〕，六樹實掃零真實觸發）。 | P2×5＋P3×9＋P4×1＋觀察×1（四方一審彙整） | 修 5 P2＋7 P3；defer 2 P3（併既有記事）；記事存證 1 P4＋1 低信心觀察；裁決不修 2（附理由） | fixed@R15-一審修復（證據見左欄各站點；修復後複驗：`scripts/tests/` 173 passed、`tools/tests/test_dev_start.py` 新增 14 test 全綠、`test_workflow_permission_concurrency_lock.py`／`test_ps1_bom.py`／`test_bash32_compat.py`／`test_onboarding_parity_interlock.py` 皆綠、smoke 重跑 PASS=13 FAIL=0〔含剝註解修復後正常路徑複驗＋QA 原始 repro bug-injection 真轉紅雙重確認〕；四方一審 4/4 APPROVE-with-conditions 為本列前提，待四方複審核准） |
| DEF-101-214 | 2026-07-21 | R15 四方一審（QA／SD 兩位獨立審查各自親歷；Architect 修復階段主控自身重演） | **方法論發現（非程式缺陷）：共享單一工作樹的多 agent 併發審查／修復存在真實交叉污染風險，本輪三度實例佐證**——①QA 親歷 `tools/dev_start.py` 在無自身寫入操作下短暫（約 2 分鐘）恢復成 git HEAD 版本又自行變回 R15 版本，事後對照 Architect 報告「過程中一度誤用 `git checkout` 把整份 R15 改動清空，已重建並還原」證實為同一事件；②SD 親歷 `run_local_nightly.sh.qa-backup` 殘留檔案與「上方註解 14 天／下方功能碼 30 天」暫態內部矛盾，係 QA 進行中的 bug-injection 尚未還原時被撞見（QA 隨後乾淨復原，三方交叉核對真正落地基線一致無誤）；③主控自身在修復 QA-R15-REV-4 時對 `autoclaude-ci.yml` 誤用 `git checkout --` 精確還原單一檔案，未料該檔案本身在 R15 輪已有正常增量修改（concurrency 區塊），指令把整份 R15 異動打回 HEAD（見 DEF-101-213 記載，當場發現並重新套用）。三起事件根因相同：**`git checkout`/`git restore` 對「尚未 commit 的異動」是全有全無的還原，無法選擇性只還原自己剛注入的 bug**，共享工作樹下極易誤傷其他審查者／自己稍早的合法異動。 | P2（方法論／流程風險，非本輪程式缺陷） | **建議下輪起四方複審／一審採 `Agent` 工具的 `isolation: "worktree"` 隔離每位審查者**（各自在獨立 git worktree 工作，互不干擾、bug-injection 還原失誤也不會波及他人或主控本身異動）；若仍需共享工作樹，還原紀律改為「`cp` 精確備份/回填單一檔案」而非 `git checkout`/`git restore`（後者是全域操作，即使目標只寫一個檔名，仍會抹除該檔案自己所有未提交的修改，不只抹除本次注入的部分） | open（記事存證＋流程建議送下輪／未來輪參考；本輪三起事件皆已確認未造成不可回復損害——最終工作樹經逐檔 diff --stat 核對與 git HEAD 基線一致） |
| DEF-101-215 | 2026-07-21 | R15 全套守門複驗（QA-R15-REV-4 修復落地後，主控重跑 `AISDLC_SDD/scripts/tests/` 全套時實測揪出） | **既有守門 `test_ci_paths_cover_root_consumers.py` 正確攔下真實回歸（非誤報）**：新增 `tools/tests/test_workflow_permission_concurrency_lock.py` 內以 `_REPO_ROOT / ".github" / "workflows" / "aisdlc-sdd-arch-fitness.yml"`／`"autoclaude-ci.yml"` 讀取這兩支 workflow 內容做結構斷言後，`tools/tests/`（被 `macos-compat-ci.yml`／`windows-compat-ci.yml` 消費執行）即真實依賴這兩支 workflow 檔案內容——但兩者當時未列入 macos/windows-compat-ci.yml 的 `paths:` 過濾清單，與 R10 對 `root-infra-ci.yml`／`aisdlc-sdd-ci.yml` 已修復的同型 DEF-101-042 盲區同構：「只改 aisdlc-sdd-arch-fitness.yml 或 autoclaude-ci.yml（例如未來又刪掉 permissions/concurrency）時，本輪剛新增的回歸鎖不會被觸發」。`test_all_root_consumers_covered_by_ci_paths[macos-compat-ci.yml]`／`[windows-compat-ci.yml]` 兩案例機械轉紅，非人工肉眼發現。 | P3 | 比照 R10 既有修法（同一守門、同一模式） | **fixed@R15**：兩支 workflow 的 push／pull_request paths 各補 `.github/workflows/aisdlc-sdd-arch-fitness.yml`／`autoclaude-ci.yml` 兩行（4 處編輯，含 WHY 註解引用本列與 test_ci_paths_cover_root_consumers 實測揪出事實）；複驗：YAML 解析 push==pull_request 對稱、`test_ci_paths_cover_root_consumers.py` 9 passed（原 7 passed 2 failed）、`scripts/tests/` 全套 173 passed、根層 unittest 303 passed |
| DEF-101-216 | 2026-07-21 | R15 四方複審（Architect/SA/SD/QA 四位獨立背景 agent 針對一審修復後狀態重新審查；SA/SD 因逾時/API 中斷各重跑 2~3 次方完成，過程詳見 DEF-101-217） | **四方複審 4/4 全數 APPROVE 或 APPROVE-with-conditions（零 REJECT、零 P0/P1）：QA（APPROVE-with-conditions，1 P2 新發現）、Architect（APPROVE-with-conditions，1 P4＋1 P3）、SA（APPROVE-with-conditions，2 P4）、SD（**無條件 APPROVE**，零發現）**。DEF-101-213 一審全部 12 項發現（5 P2＋7 P3）與 DEF-101-215 的修復，經複審逐項 bug-injection／獨立重跑複核，**全數確認確實落地且正確**（QA 以 cp 備份/回填〔非 git checkout〕重新注入 smoke `[7/7]`／`_python_version_target`／concurrency 鎖三項確切 repro，皆精準轉紅；Architect 對 REV-1~8 逐項複核並對 tty/tee 分支額外做 170 次真機壓力測試；SA 機械複核 crossref 58 筆與 3 項「N passed」數字宣稱皆精確相符；SD 確認 `_check_ci_liveness` 邏輯與 6 個測試全過）。**新發現與處置**：①**QA-R15-RR-6（P2，真實相容性 bug，非本輪引入但併入本輪修復）**：`tools/macos_smoke_local.sh` 5 處（原行號 186/209/243/282/388）裸 `$VAR` 緊鄰全形字元（如「rc=$sub_rc）」「$MIN_PASS——」），bash 3.2 + UTF-8 locale 下 `set -u` 誤判 unbound variable、彙總/失敗診斷段落崩潰——QA 以真機 `/bin/bash 3.2.57` 雙重驗證根因（`$VAR中文` 崩潰 vs `${VAR}中文` 正常）；`AutoClaude/tools/run_local_nightly.sh` 早有同款教訓的大括號慣例記載，本檔案未比照套用。主控依使用者「所有問題必須徹底修復」鐵律，判定此為本輪任務範圍內的真實 Mac 相容性 bug（雖由複審意外揭露、非四個掃描 agent 主動找到），5 處全補大括號並修復；驗證：QA 確切 repro（`$MIN_PASS` 裸變數版真機重現崩潰、`${MIN_PASS}` 版正常）＋端到端觸發真實失敗診斷分支（`MIN_PASS` 暫調 999 觸發 fail 分支，確認印出正確診斷文字而非崩潰，驗證後精確還原）。②**SA-R15-RR-1/2（P4，溯源 tag 缺漏，已修）**：`useMacWin.md`／DEF-101-212 兩處修復內容本身正確但缺乏本輪慣例的明確可 grep 溯源 tag（其餘同輪修復皆有）→ 各補「SA-R15-REV-3/5 訂正」字樣。③**ARCH-R15-RR-1（P4，文件補充，已修）**：`run_local_nightly.sh` tee 分支補一句「子行程無 wait 同步保證」已知限制記述（純文件、不改邏輯，170 次壓力測試 0 次重現，非阻擋）。④**ARCH-R15-RR-2（P3，方法論，記事）**：見 DEF-101-217。 | P2×1（真實相容性 bug）＋P4×3（溯源/文件補充） | 修 4（QA-RR-6／SA-RR-1／SA-RR-2／ARCH-RR-1） | fixed@R15-複審修復（證據見左欄各站點；修復後複驗：smoke 重跑 PASS=13 FAIL=0＋端到端崩潰repro消失、`run_local_nightly.sh` bash -n 過；四方複審 4/4 APPROVE/APPROVE-with-conditions 為本列前提） |
| DEF-101-217 | 2026-07-21 | R15 四方複審（QA／Architect 各自獨立親歷；SA/SD 兩次背景執行逾時/API中斷佐證同根風險） | **DEF-101-214 併發污染風險於複審輪再次真實重演，且新揭露「共享 scratchpad 目錄本身也有碰撞風險」這個 DEF-101-214 原文未提及的面向**：Architect 複審過程中以 `python3 -m unittest` 平行執行測試時，`test_workflow_permission_concurrency_lock.py::test_workflow_level_contents_read_present` 短暫轉紅（斷言找不到 `aisdlc-sdd-arch-fitness.yml` 的 permissions 區塊），經 `stat` 查 mtime 為複審進行中 2 分鐘前、Architect 自陳全程未編輯此檔，判定為「另一方（同時在跑的 SA/SD/QA 背景 agent）對同一檔案即時 bug-injection 造成的瞬間污染，已自行復原」；同時檢視共享 scratchpad 目錄，發現大量非 Architect 建立的檔案（`qa-backup/`、`imn_injected.sh`、`run_local_nightly.sh.orig_backup`、`repro_tee_exec.sh` 等，數個與其 mtime 幾乎同刻），證實該目錄確為本 session 四位審查者共用而非各自隔離。另主控自身於本輪帳本編輯過程中亦收到至少 2 次「檔案已在磁碟被修改」系統提示（`macos-compat-ci.yml`／`AutoSDD_Defect_Log.md`），逐一核實後確認皆為此同根污染的無害副作用（crossref 58 筆、diff --stat 增量皆與預期吻合，無實質內容遺失）。**複審階段另有 2 次背景 agent 執行逾時/API 中斷**（SA 第一次 stall 600s、SD 第一次 stall 600s＋第二次 API 連線中斷），時間點與內容（SD 中斷前訊息「Now let me inject a bug...」）高度提示同為共享工作樹資源競爭之外顯症狀，經主控立即查核工作樹皆確認未受損（zi 3 次重新派遣皆縮小範圍後於較短時間內成功完成）。 | P3（方法論／流程風險，非本輪程式缺陷；累計本輪已 4 起實例，風險評估由 DEF-101-214 的「觀察」提升為「確認模式」） | **Architect 建議：DEF-101-214 的「未來優先 isolation: worktree」應從「下輪參考」提升為「下輪起強制採用」**；且應一併檢討是否需要為每位審查者分配獨立 scratch 子目錄（本輪 4 起實例中至少 1 起〔SD 兩次逾時/中斷〕與共享 scratchpad 資源競爭時間點高度相關） | open（記事存證＋流程建議累加至 DEF-101-214 同一根因；本輪最終驗證確認全部 4 起實例皆無實損——工作樹與帳本內容經逐項核對與 crossref 機械複核，最終狀態正確完整） |
| DEF-101-218 | 2026-07-21 | R16 全面掃描（Shell/PowerShell 維度） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/verify_traceability.sh` `set -euo pipefail` + `grep \| sort -u \| wc -l` 管線靜默崩潰**：`grep` 找不到匹配時回傳 exit 1（正常「無匹配」語意），`pipefail` 讓整條管線視為失敗，`set -e` 因此在賦值敘述式上立即終止腳本；對空白 docs 樹與框架自己的真實 `docs/` 樹皆重現，在印出區段標題後、任何 PASS/WARN/FAIL 之前就中止 exit 1，作者原本設計的「找不到→印 ⚠️ 警告」分支完全不可達。此工具未接入 ci-gate.sh 或任何 pytest，無自動化閘門會執行到它 | P0 | 對每個受影響賦值敘述式補 `\|\| true` | fixed@R16：9 處（含複查追加發現的第 240 行 markdown-link-check 迴圈同款 bug）皆補 `\|\| true`；對照組（有匹配）、反例組（全新空白 docs）、框架真實 `docs/` 三組皆驗證跑到底且 exit 0 |
| DEF-101-219 | 2026-07-21 | R16 全面掃描（Python 維度） | **`PlaybookTask.step_id` 含 Windows 禁用檔名字元時 `RawStreamLogger.open()` 未捕捉崩潰**：`AutoClaude/autoclaude/utils/logger.py` 經 `pty_executor.py`/`pty_wrapper.py` 組出 `playbook_{step_id}.log` 路徑，`step_id` 含 `&lt; &gt; : " \| ? *`、結尾空白/句點、或保留裝置名（CON/PRN/AUX/NUL/COM0-9/LPT0-9）時，Windows 上 `open()` 拋未捕捉 `OSError`，整個 step 每次重試都用同一壞 step_id 再炸一次；macOS/Linux 因檔名規則寬鬆完全不觸發，只有真實 Windows 執行才會爆炸 | P1 | 在 `RawStreamLogger.__init__` 單一 chokepoint sanitize 檔名 + try/except fallback | fixed@R16：新增 `_sanitize_log_filename()`（禁用字元→`_`、去尾隨空白/句點、保留裝置名大小寫不敏感守衛），套用於 `open()` 前；另包 try/except OSError + hash fallback 檔名寫入 tempdir。`test_logger.py` 21 個測試（含原始案例 `step_id="Step 1: Setup"` 端到端測試）全過 |
| DEF-101-220 | 2026-07-21 | R16 全面掃描（Python 維度） | **`ConditionalStrategy` 白名單不含反斜線，Windows 路徑靜默拒絕**：`_SAFE_COND_PATTERN` 不接受 `C:\path\to\check.py` 這類路徑，`conditional.py` 拒絕時無任何 log（姊妹實作 `execution/mutation_applier/_conditional.py` 同情境有警告 log，兩者行為不一致）；且若白名單將來放寬接受反斜線，`_default_evaluator` 用 `shlex.split`（POSIX 模式）會把反斜線當跳脫字元吃掉、破壞路徑（已驗證 `shlex.split(r"C:\Users\dev\x.py")` → `['C:Usersdevx.py', ...]`），目前因白名單先擋下而被掩蓋，是潛在陷阱 | P2 | 補齊警告 log；不放寬白名單接受反斜線（避免曝露 shlex.split 陷阱），改在 docstring 記錄正斜線慣例 | fixed@R16：`conditional.py` 兩處拒絕點補齊與 `_conditional.py` 一致的警告 log；docstring 記錄正斜線慣例（比照 `evaluator.py` 既有慣例）；`_SAFE_COND_PATTERN` 留警告註解防未來誤放寬。`test_strategies.py` 新增 2 測試（反斜線拒絕+log、正斜線端到端接受），35 個全過 |
| DEF-101-221 | 2026-07-21 | R16 全面掃描（Python 維度） | **`PtyWrapper.close()` POSIX 孫行程孤兒防護與 Windows 不對稱**：Windows 側明確執行 `taskkill /T /F` 遞迴殺行程樹（先前輪次已修復的問題），POSIX 側 `_start_subprocess()` 未用 `start_new_session=True`（無獨立 process group），`close()` 只呼叫 `.terminate()`（只殺直接子行程），若底層 CLI 在 macOS/Linux 上 fork 出孫行程會變成孤兒不被回收 | P2 | POSIX 加 `start_new_session=True` + `close()` 用 `os.killpg` | fixed@R16：POSIX `_start_subprocess()` 加 `start_new_session=True`；`close()` 加 POSIX 分支 `os.killpg(pgid, SIGTERM)` 輪詢 2s 後升級 `SIGKILL`。新增孫行程存活測試，**先暫時撤回修復重跑確認測試會失敗**（證明抓到真 bug）再套用修復確認通過，驗證行程回收乾淨無殘留 |
| DEF-101-222 | 2026-07-21 | R16 全面掃描（Shell/PowerShell 維度） | **`install_post_commit.ps1` 唯一沒設 `$ErrorActionPreference = 'Stop'` 的同類腳本**：`$GitCommonDir = (git rev-parse ...).Trim()` 無 `$LASTEXITCODE`/null 檢查，非 git repo 目錄下執行會先印 git fatal 訊息，接著觸發 5 個串聯的非終止性 PowerShell 錯誤，才終於走到腳本自己設計的乾淨錯誤訊息 | P2 | 補 `$ErrorActionPreference = 'Stop'` + 明確 rc/null 檢查 | fixed@R16：pwsh 驗證非 git 目錄下不再串聯錯誤，單一乾淨 `Write-Error` exit 1；正常 git repo 內確認 hook 仍正確安裝 |
| DEF-101-223 | 2026-07-21 | R16 全面掃描（Shell/PowerShell 維度） | **`init_project.ps1` git clone 呼叫缺 DEF-101-124 同款 PS 5.1 版本守衛**：`2>&1 \| Out-Null` 在全域 `$ErrorActionPreference = "Stop"` 下執行、無 PS 版本守衛，姊妹腳本 `run_tlc.ps1`/`run_self_evolution.ps1` 皆有明確引用 DEF-101-124 的守衛，`init_project.ps1` 沒有；stock Windows PowerShell 5.1 下 SSH 首次連線 stderr 警告會被誤判為 clone 失敗 | P2 | 比照姊妹腳本補同款 PS 5.1 版本守衛 | fixed@R16（部分未測）：比照 `run_tlc.ps1`/`run_self_evolution.ps1` 補 `$PSVersionTable.PSVersion.Major -lt 6` 守衛；else 分支（pwsh 7+）確認與原邏輯位元組相同；**PS 5.1 分支本機無法實測**（本機僅 pwsh 7.6.3），留待下一輪真機驗證，程式碼內留註解說明 |
| DEF-101-224 | 2026-07-21 | R16 全面掃描（Shell/PowerShell 維度） | **`init_project.sh`/`.ps1` 「可用版本」fallback 清單固定過濾 `AISDLC_v*`**：但 `--sdd`/`-SDD` 模式實際目錄命名是 `AISDLC_SDD_v*`，導致 SDD 模式錯誤路徑印出空的或無用的版本清單；兩平台版本同一 bug，非跨平台分歧 | P4 | fallback 過濾依模式切換對應 pattern | fixed@R16：兩平台皆依 `--sdd`/`-SDD` 切換 `AISDLC_SDD_v*` vs `AISDLC_v*`；4 種組合（bash/pwsh × SDD/非SDD）皆驗證正確，正常成功路徑不受影響 |
| DEF-101-225 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`run_local_nightly.sh` RunId log 缺觸發來源記錄 + 去重 check-then-act 無鎖（TOCTOU）**：本 repo 真實 log 顯示 2026-07-21 同日出現兩輪完整 PASS=4 執行且無 skip 訊息，因記錄不完整無法判斷是合理手動重跑還是真正去重漏洞；且 launchd `RunAtLoad` 與 `StartCalendarInterval(02:00)` 兩觸發源時間重疊時，兩行程可能同時通過「今日尚未有心跳」檢查，導致重複執行整套 nightly gate | P2 | BEGIN 行加印觸發來源；去重檢查外包檔案鎖 | fixed@R16：`TRIGGER_SRC` 判定（--force/`XPC_SERVICE_NAME`/互動終端/未知四態）寫入 BEGIN 行；本機無 `flock`，改用 POSIX `mkdir` atomic lock，陳舊鎖以 PID 存活判斷清除（比照 `dev_start.py` `_acquire_bootstrap_lock()`），`trap EXIT` 保證釋放。驗證：兩行程近乎同時呼叫，第二個正確被擋＋印跳過訊息；陳舊死 PID 鎖正確清除重取；三種觸發來源分支獨立驗證正確 |
| DEF-101-226 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **全倉 8 條 GitHub Actions cron 排程全部落在整點/半點**，且三組「每日排程」與「每週一排程」精準相撞在同一分鐘（02:00/02:30/03:00 UTC），GitHub 官方建議避開整點/半點降低延遲風險，且相依 workflow 的相對執行順序假設依賴準時開始 | P2 | 錯開至非整點/半點分鐘，維持既有先後順序 | fixed@R16：8 條 cron 皆錯開（如 `0 2 * * *`→`3 2 * * *`），維持「每日先跑、每週接著跑」順序不變；**額外主動抓到並修復**：`autoclaude-ci.yml` 3 處 `github.event.schedule == '...'` 字串比對同步更新，避免 cron 時間改了但字串比對沒改導致 job 永不觸發的地雷；7 個 yaml 皆 `yaml.safe_load` 驗證語法通過 |
| DEF-101-227 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`tools/windows_smoke_local.ps1:255` 註解宣稱 `-c core.longpaths=true` 解決 Windows MAX_PATH=260 風險，實際只保護 git 內部路徑處理**：後續多處 `Test-Path`/`New-Item`/`[System.IO.File]::ReadAllText` 用 .NET/PS 5.1 API 操作同一批深路徑，在無系統級 `LongPathsEnabled` 登錄機碼時不會自動長路徑安全 | P2 | 訂正註解措辭，不誤導為已全面解決 | fixed@R16：註解改寫為明確說明保護範圍侷限與剩餘風險；pwsh AST parser 驗證語法 OK |
| DEF-101-228 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **Windows 側 nightly 完全無同構去重機制** + **`fix_nightly_catchup.ps1`/`reschedule_g0_gatecheck.ps1`/建立任務流程皆未顯式設定 `MultipleInstances`**：mac 側已於 DEF-101-225 補齊 mkdir lock，Windows 側排程觸發與手動重跑重疊時是否真的不會並行執行，完全依賴一個從未被本倉驗證過的 Task Scheduler 預設行為 | P2 | Windows 側補去重機制；顯式設定並印出 `MultipleInstances` 值 | open/routed：本輪未動 Windows 側去重與 `MultipleInstances`（需真實 Windows 機器驗證預設行為與新機制正確性，超出本輪可驗證範圍）；與 **DEF-101-200**（Windows nightly 一鍵安裝器，已排入下一輪）同批次一併處理 |
| DEF-101-229 | 2026-07-21 | R16 全面掃描（文件帳本一致性維度） | **`AutoSDD_Defect_Log_archive_01.md` 與 `archive_03.md` 各自完整搬移了 DEF-37-001／DEF-62-001 兩列**：內容一致、結案狀態相同（非矛盾），推測為兩次獨立搬遷批次涵蓋範圍重疊所致；`check_defect_log_crossref.py` 只掃描主檔與少數指定目標檔，不掃 archive 間互相重疊，此重複未被既有機械守門攔下 | P2 | 於歸檔政策註記加註已知重疊，不去重（archive 只增不刪政策） | fixed@R16：已於本檔「歷史複驗註記」歸檔政策段落加註說明（見上方 archive_04 條目後段） |
| DEF-101-230 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`ONBOARDING.md` §8 launchd 睡眠/關機 rationale 與官方文件不符**：文件稱「02:00 當下關機/睡眠則該日靜默跳過、不補跑」，但 `man launchd.plist` 明文「睡眠情境 launchd 會在喚醒時原生補跑並合併多次錯過的觸發」，只有「關機」才沒有原生補跑保障；R15 加的 `RunAtLoad` 修法對兩者皆有效不影響功能，僅 rationale 描述不準確 | P3 | 訂正文件區分睡眠 vs 關機兩種不同根因 | fixed@R16：`ONBOARDING.md` §8 該段已訂正，明確引用 `man launchd.plist` 原文區分睡眠（原生補跑）與關機（無原生保障）兩種情境 |
| DEF-101-231 | 2026-07-21 | R16 Architect 架構檢視 | **`_init_utf8_streams()` 被複製貼上到至少 8 個檔案，其中 6 份缺 Windows-only 平台守衛**：僅 `scaffold_sprint_section.py`/`snapshot_sync.py` 原本有 `sys.platform != "win32"` 守衛，其餘 6 份（`check_lang.py`/`loc_budget_check.py`/`check_ps1_encoding.py`/`check_sh_eol.py`/`enforce_docs_path.py`/`claude_md_freshness.py`）在所有平台無條件重新包裝 stdout/stderr——這是「散落平台判斷邏輯造成實際行為分歧」的已發生案例，且無任何守門在追蹤此漂移；全庫亦無集中式 `platform_utils.py` 模組，`tools/dev_start.py` 的 `_now_label()` 設計良好但未被其他檔案重用 | P2 | 建立 `tools/lib/platform_utils.py` 收斂 `is_windows`/`is_macos`/`os_label`/`init_utf8_streams`，8 檔統一改 import | fixed@R16（Architect 建議 A）：新增 `tools/lib/platform_utils.py`（stdlib-only），8 檔呼叫點替換為 import；6 份無守衛檔案行為改變（新增 Windows-only 守衛）為預期修正。既有測試 94 passed；8 支腳本手動 smoke 全部 exit 0；**新增止血測試** `tools/tests/test_platform_utils_dedup.py`（7 cases）機械掃描斷言 `init_utf8_streams` 只定義在單一檔案，防未來第 9 份複製貼上 |
| DEF-101-232 | 2026-07-21 | R16 Architect 架構檢視 | **`bootstrap`/`integration_gate`/`run_act` 三對「雙原生實作」腳本有 `check_script_parity.py` 守門，但只比對 step 標籤字面文字、不驗證行為對等**——會亮綠燈但不保證正確，比完全沒有守門更危險（誤導人以為已受保護）；三對業務邏輯各自平行維護，是雙重維護債的根源 | P2（架構） | 比照 R12 `local_ci_gate` 先例，收斂為「Python 單一核心＋雙殼薄封裝」 | fixed@R16（Architect 建議 B，經人工核准納入本輪範圍）：新增 `tools/bootstrap_core.py`（335 行，複審實測 345 行，R17 platform_utils 收斂後再變動，現 **348 行**）/`tools/integration_gate_core.py`（239 行，R17 現 **260 行**）/`AutoClaude/tools/run_act_core.py`（220 行，複審實測 225 行，R17 現 **232 行**），六份薄殼各 21～54 行；上列行數皆為對應輪次訂正時點快照，非恆定值——本檔案持續被後續輪次收斂編輯，之後每輪若再訂正請一併核對是否又已過期（`wc -l` 現查為準）。`check_wrapper_thinness.py`/`check_script_parity.py` 已同步更新（三對自 `_MARKER_PAIRS`/`_MIN_EXTRACT_COUNTS` 移至 `_THINNESS_ENROLLED` + hash 釘選，兩檔守門測試 43 個全過）。收斂過程額外發現並修復 stdout buffering 交錯問題，但四方一審 Architect 複核發現本欄原敘述不準：`integration_gate_core.py` 用逐 `print(..., flush=True)`（8 處，對齊 `local_ci_gate.py` 既有先例），`run_act_core.py` 實際用 `stream.reconfigure(encoding="utf-8", errors="replace", line_buffering=True)`（非 `flush=True`）；且 Architect 發現 `bootstrap_core.py` 當時**完全遺漏**此保護（三份核心中最早呼叫子行程、訊息最多的一份），已於一審複核當下補上同款 `stream.reconfigure(line_buffering=True)`（見 `main()` 頂部）。`bootstrap` 於 `/private/tmp/` 隔離副本完整驗證（未動真實 repo .venv）；三者皆用 pwsh 驗證薄殼語法與 exit code 轉發正確。**未能端到端驗證**：真實 Windows 原生 PowerShell（非 pwsh）、Windows `Scripts\python.exe` 實際檔案系統行為、`find_git_bash()` 真實三候選路徑探測、真 Docker daemon 下的 act 執行——皆留待下一輪真機驗證 |
| DEF-101-233 | 2026-07-21 | R16 Architect 架構檢視 | **排程機制無共用抽象層**：mac `install_mac_nightly.sh` 是成熟 ensure 語意安裝器，Windows 無對等安裝器；全庫無 `schedule_adapter` 抽象層，兩邊功能對等純靠人工在文件與 DEF 帳本互相校對 | P2（架構） | 建 `install_win_nightly.ps1`；補輕量「排程能力對照契約」機械測試（斷言 mac 支援子命令集合 ⊆ Windows 支援子命令集合），非重量級 ScheduleAdapter 介面 | open/routed（Architect 建議 C）：本輪未動，該不對稱已於 R14 記為 **DEF-101-200**（P2, open，已排入下一輪 Windows 輪）；本輪架構審查僅補充佐證與契約測試的具體設計建議，與 DEF-101-228 同批次一併處理 |
| DEF-101-234 | 2026-07-21 | R16 Architect 架構檢視 | **`init_project`/`run_self_evolution` 雙原生實作**（LATEST tools 下另兩對「雙原生實作」腳本，複雜度高：GitHub clone 精靈、FSE 有界驅動器），已有文件化豁免與人工審查覆蓋（`_EXEMPT_PAIRS` 附決策依據） | P4（架構，低優先） | 不建議現在主動重寫；等下次真的要改邏輯時「touch it, fix it」 | open watch（Architect 建議 D）：本輪不排入行動項，維持現狀觀察；下輪若有人異動其中任一對邏輯，應順手做 Python 核心化搬遷 |
| DEF-101-235 | 2026-07-21 | R16 全面掃描殘留 P3 觀察項（未修復，記事存證） | 本輪掃描發現但評估為低優先、暫不修復的殘留項：① `tools/dev_start.py` `_pid_alive`/`_list_pid_ppid_pairs_windows` 的 `ctypes.windll.kernel32.OpenProcess`/`CreateToolhelp32Snapshot` 未設定 `restype`（預設 32-bit，實際回傳 64-bit HANDLE），理論殘留無已知真實觸發；② `tools/windows_smoke_local.ps1:136` `Test-InstallRoundtrip` 假設 `.git` 是目錄，在 linked worktree 下會誤判，目前未觸發（呼叫點皆傳完整 clone）；③ `AutoClaude/tools/run_local_nightly.ps1:921` `Copy-Item` 更新 `nightly_latest.log` 指標無重試（不像同檔 `Add-LogLineSafe` 有退避重試），窄競態視窗；④ PS 5.1 版本守衛（DEF-101-124 家族）仍在 `run_tlc.ps1`/`run_self_evolution.ps1` 兩處逐字重複，未抽出共用模組 | P3 | 各項成本/風險評估後暫緩，留待下輪視是否觸發或有餘裕時處理 | open（記事存證，四項皆為理論性/低機率殘留，非本輪修復範圍） |
| DEF-101-236 | 2026-07-21 | R16 四方一審（Architect 獨立審查，親讀三份新核心全文＋bug-injection） | **`integration_gate_core.py::find_git_bash()` 是既有 `tools/lib/Find-GitBash.ps1`（供 3 個既有呼叫點共用的單一真相源）的手動 Python port**（docstring 自承）：本輪為收斂 `integration_gate` 進 Python 核心，新引入了第二套獨立維護的同語意實作，且兩者之間沒有任何機械等價鎖——與 R16 本身要消滅的「雙原生實作分歧風險」屬同一類問題。目前邏輯比對後一致，但未來 `Find-GitBash.ps1` 若修訂（如新增偵測路徑），Python 副本不會有任何紅燈提示需要同步 | P3（架構債，非阻斷；Architect 一審判定不足以打回整輪） | 補一道輕量等價鎖（比照 `check_script_parity.py` 模式），或下一輪評估把既有 3 個 `.ps1` 呼叫點也一併收斂進同一 Python 邏輯，終結兩套並存 | open（記事存證，排入下一輪 backlog；四方一審 Architect 明確判定非阻斷性缺陷，不影響本輪合併） |
| DEF-101-237 | 2026-07-21 | R16 四方一審（SD 獨立審查，親手構造邊角案例＋bug-injection） | **`AutoClaude/autoclaude/utils/logger.py:80` `_sanitize_log_filename` 用 `sanitized.rsplit(".", 1)[0]`（取最後一個點）切分副檔名再判斷 Windows 保留裝置名**，對 `nul.tar.gz` 這類多重副檔名輸入不會被淨化（`_sanitize_log_filename("nul.tar.gz")` 回傳原樣未變的 `'nul.tar.gz'`，而單一副檔名 `_sanitize_log_filename("nul.txt")` 正確變成 `'_nul.txt'`）。SD 實測確認**目前唯一呼叫路徑不受影響**：真實建構的檔名恆為 `f"playbook_{step_id}.log"`（`pty_executor.py:81`／`prompt_dispatcher.py:50`），`rsplit` 出的 stem 恆為 `"playbook_XXX"`，永遠不會等於裸保留名 | P3（一般工具函式正確性問題，非生產風險；目前唯一呼叫路徑因 `"playbook_"` 前綴天然免疫） | 若 `_sanitize_log_filename` 未來被其他呼叫點重用，改用 `sanitized.split(".", 1)[0]`（取第一個點）切分，使多重副檔名保留名也能正確淨化 | open（記事存證，非阻斷；SD 一審判定不影響本輪合併） |
| DEF-101-238 | 2026-07-21 | R16 四方一審（SD 獨立審查，全庫搜尋比對兩個姊妹實作） | **`conditional.py` 的 `_SAFE_COND_PATTERN`（不含 `!`）與姊妹實作 `execution/mutation_applier/_conditional.py` 的白名單（含 `!`）字元集不完全一致**：後者無 `_DENY_CHARS` 第二層黑名單，純靠白名單過濾，理論上會放行 `!`。SD 確認 POSIX/cmd.exe 非互動殼下 `!` 無特殊展開意義，非安全漏洞 | P4（資訊性，兩套白名單字元集不一致但無安全危害） | 下一輪一併收斂兩份白名單字元集，避免未來各自演化出更大分歧 | open（記事存證，非阻斷；SD 一審判定資訊性問題） |
| DEF-101-239 | 2026-07-21 | R16 四方一審過程中的自我發現（SA／QA 各自獨立發現同一問題，交叉印證） | **主控為解 ruff E501 過長行，把 `AutoClaude/tests/core/services/mutation/test_strategies.py:295` 的 `# platform-ok:` 豁免標記移到獨立一行，但 `tools/tests/test_platform_neutral_paths.py` 的豁免機制只認「同一行行尾」標記**，導致 `test_no_windows_drive_fake_paths` 持續紅燈，`tools/run_root_unittests.py`（310 tests）整體 FAILED——直接牴觸「根層測試全綠」的驗證聲稱。SA 與 QA 兩位審查者各自獨立在一審過程中發現此問題（互不知會），交叉印證非誤報 | P3（測試層級，修法簡單，不影響生產程式碼行為；但屬「驗證聲稱不實」需誠實記錄） | 把註解移回同一行行尾（縮短文字以符合 100 字元限制） | fixed@R16（一審發現當下立即修復）：改寫為 `condition_evaluator="C:/Users/dev/check.py --flag",  # platform-ok: 字面值`，`ruff check` 與 `test_platform_neutral_paths.py` 皆重新驗證通過，`tools/run_root_unittests.py` 重跑 310 tests exit 0 |
| DEF-101-240 | 2026-07-21 | R17 文件帳本掃描（Scan-D：文件↔程式碼一致性）發現的 5 項文件漂移 | 前置掃描找出 5 處文件文字落後實際程式碼/設定檔：①本表 DEF-101-232 敘述 `tools/bootstrap_core.py`「335 行」，但該列自身後段已述及 buffering 補丁 +10 行，git 實測早已是 **345 行**，帳本自身數字矛盾未同步；② `ADR-SD08-003-perf-regression-policy.md` §2.1 表格 (b) CI nightly 欄敘述「每週一 02:00 UTC」，但 `.github/workflows/autoclaude-ci.yml` 現行 cron 為 `7 2 * * 1`＝**02:07 UTC**（R16／DEF-101-226 為避開整點/半點錶開所致，ADR 未同步）；③ `ADR-SD09-002-mutation-full-module-expansion.md` §2.3 R14 修訂註記敘述 TokenGuard active pilot「每週一 03:00 UTC」，現行 cron 為 `7 3 * * 1`＝**03:07 UTC**，同一根因未同步；④ `Nightly_Forensic_Discipline.md`（v1.8）header 表與紀律 #3 mac 註記只描述 R15 版 mac 聚合器機制（心跳 mtime 判日期去重），未提及 R16／DEF-101-225 新增的 `run_local_nightly.sh` 兩項強化（BEGIN 行印 `TRIGGER_SRC` 四態＋心跳去重前置 POSIX `mkdir` atomic lock 修復 TOCTOU 競態）；⑤ `ONBOARDING.md` §8「R15 關機錯過補跑＋RunId log」段落同樣只描述心跳 mtime 去重，未提及④之鎖與 `TRIGGER_SRC`（與④同一缺口，權威站點） | P3～P4（純文件訂正；①為帳本自身數字矛盾 P4，②③為 cron 時間文件落後實況 P3～P4，④⑤為取證紀律文件落後一輪 P3） | 逐項訂正文字並反向核對（`git show`/`grep`）與實際程式碼/設定檔逐字一致 | fixed@R17：①已於本表 DEF-101-232 列原地訂正為「335 行，複審實測 345 行」；②③已改為「02:07 UTC」/「03:07 UTC」並補 R16／DEF-101-226 錶開理由（cron 逐字核對 `.github/workflows/autoclaude-ci.yml:44-45` `"7 2 * * 1"`/`"7 3 * * 1"`）；④已讀 `AutoClaude/tools/run_local_nightly.sh` 實際程式碼（L47-100）確認 `TRIGGER_SRC` 四態判定與 `mkdir`/`trap EXIT` atomic lock 行為後補入 header 表與紀律 #3，版號 v1.8→**v1.9**；⑤已依同一份程式碼核實後補入 ONBOARDING.md §8。五項訂正逐一以 `git show`/`grep` 反向核對新文字與程式碼/設定檔逐字相符；`grep -rn "Nightly_Forensic_Discipline\|ADR-SD08-003\|ADR-SD09-002" tools/ AutoClaude/tools/` 查無機械新鮮度守門引用本輪改動內容，未破壞任何既有機械檢查 |
| DEF-101-241 | 2026-07-21 | R17 四方一審（Architect／SA 交叉獨立發現同一缺口；SD 獨立發現另一真實缺陷） | **①（P1，Architect+SA 交叉發現）CI paths 消費盲區**：`tools/lib/platform_utils.py`（本輪收斂工程唯一真相源）／`tools/bootstrap_core.py`／`AutoClaude/tools/run_act_core.py` 三檔本輪皆有真實改動，但 `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 的 import 消費掃描器認不出 `tools/tests/test_platform_utils_dedup.py` 對這三檔採用的 `rglob`/`sys.path.insert(0, parents[1] / "lib")` 掃描慣用法，故三檔皆未被 `macos-compat-ci.yml`／`windows-compat-ci.yml` paths 涵蓋——若未來 commit 只改其中一份，兩份 compat-CI 皆不會觸發（DEF-101-042 同款假綠盲區，且恰好命中本輪核心 SSOT 檔案）。**②（P3，SD 獨立發現）`tools/windows_smoke_local.ps1` 兩處 `git rev-parse --path-format=absolute --git-common-dir` 呼叫（本檔既有 [5] 步驟 + 本輪新增 [4] `Test-InstallRoundtrip` 修復）的版本守衛實際失效**：git < 2.31 不認得 `--path-format` 時，`rev-parse` 不會以非零 rc 報錯，而是把未知旗標原樣 echo 成額外一行輸出（`rev-parse` 既有文件行為），令 `$LASTEXITCODE -ne 0`／`IsNullOrEmpty` 兩道守衛皆偵測不到，多行夾雜旗標字串的輸出會被誤當合法路徑餵進 `Join-Path`，用 bug-injection（`git rev-parse --bogus-unknown-flag-xyz --git-common-dir` 實測 exit=0 且原樣 echo）證實。**③（P3，SD+SA 交叉發現）`Copy-ItemWithRetry`（`AutoClaude/tools/run_local_nightly.ps1`）函式頭註解宣稱「比照 Add-LogLineSafe…上限 750ms 對等 wrapper」，但實測其因 `if ($i -lt $maxRetries-1)` 守衛跳過最後一次失敗後的 sleep，真實累計退避上限僅 500ms（Add-LogLineSafe 每次失敗皆 sleep，真實 750ms），兩者退避總時間不對等（僅重試次數/間隔公式相同）**——非功能缺陷（新函式反而更省時），純註解措辭誤導。**④（P4，Architect+SA 交叉發現）帳本自身數字再度漂移**：DEF-101-240 剛訂正的 `bootstrap_core.py`/`integration_gate_core.py`/`run_act_core.py` 行數，被本輪同批次 platform_utils 收斂編輯再打成過期（345/239/225 → 348/260/232） | P1×1／P3×2／P4×1 | ①在 macos-compat-ci.yml／windows-compat-ci.yml 對稱補上三個路徑；②改用 `@(...)` 強制陣列收集並斷言恰好一行（兩處同構修復）；③訂正註解措辭區分兩函式真實退避總時間；④於 DEF-101-232 列補「行數為訂正時點快照、非恆定值」提醒，訂正為現查數字 | fixed@R17-一審修復：①macos/windows-compat-ci.yml 皆已補 `tools/lib/platform_utils.py`／`tools/bootstrap_core.py`／`AutoClaude/tools/run_act_core.py`，`python3 -c` fnmatch 逐一核對三檔於兩份 workflow push+pull_request paths 皆命中，YAML `yaml.safe_load` 驗證通過；②`tools/windows_smoke_local.ps1` 兩處呼叫點改用 `$commonDirLines = @(git ... 2>$null)` + `.Count -ne 1` 判準，pwsh `Parser::ParseFile` 驗證零錯誤，QA 複審實測 `pwsh 7.6.3` 真跑 `windows_smoke_local.ps1` PASS=8 FAIL=0（含 [5]/[6] 兩步真實觸發）；③`run_local_nightly.ps1` 函式頭註解已訂正並明確區分 500ms/750ms 差異，pwsh parse 驗證零錯誤，QA 複審實測 `Copy-ItemWithRetry` 失敗案例耗時 531ms 與訂正後註解相符；④`docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-232 列已訂正為現查行數並補防再漂移提醒 |
| DEF-101-242 | 2026-07-21 | R17 四方一審（QA 獨立 bug-injection 反向驗證） | QA 複審 DEF-101-241 ①③ 兩項修復時，用 bug-injection 揪出兩個「測試提供的保護力比表面看起來弱」的殘留缺口（非本輪引入的新回歸，但本輪修復恰好觸及）：**①（P2）`tools/tests/test_find_git_bash_parity.py` 只做結構等價比對（regex 抽取兩份原始碼的候選路徑/排除字面片段），不驗證行為**——QA 把 `find_git_bash()` 呼叫點改回舊版 `"system32" not in found.lower()`（只改呼叫點、不動 `_has_system32_segment()` 定義），parity 測試仍全綠，因 regex 抽取到的仍是死代碼裡的同一段文字；對呼叫點本身的行為迴歸零防護力。**②（P2）DEF-101-241① 手動補上的三行 CI paths**（`tools/lib/platform_utils.py`／`tools/bootstrap_core.py`／`AutoClaude/tools/run_act_core.py`）**本身未受機械鎖保護**——QA 對稱移除 `windows-compat-ci.yml` 兩側的 `platform_utils.py` 一行，`test_ci_paths_cover_root_consumers.py` 9 個測試仍全部 PASS（根因：`test_platform_utils_dedup.py` 用 `rglob` 掃描消費這三檔，掃描器的 `_IMPORT_RE` BFS 只認得 `import X` 陳述式，看不到這種消費模式，故對稱誤刪無法被現有覆蓋率檢查抓到；先前單側移除是被另一支對稱性檢查意外攔到，非真被覆蓋率檢查抓到） | P2×2（QA 一審判定均非阻斷，本輪合入不受影響） | ①排下輪：補一支對 `find_git_bash()`/`_has_system32_segment()` 直接呼叫的行為測試（不依賴 regex 結構比對）；②排下輪：仿 `test_known_consumers_detected()`「已知消費檔必被掃出」手法，替這三檔補一條專屬機械存在性鎖 | open（QA 一審明確判定非阻斷，記事存證排入下一輪 backlog；四方一審 Architect/SA/SD/QA 對本輪其餘修復之獨立覆核皆確認有效） |
| DEF-101-243 | 2026-07-22 | R18 四方一審（QA 獨立 bug-injection 反向驗證，複審時合記三項非阻斷測試保護力缺口） | QA 複審 R18 修復（`windows_smoke_local.ps1` 新增 `[7/7]` 步驟、`ONBOARDING.md`/`AutoClaude/README.md`/`AutoClaude/CLAUDE.md` 文件同步）時，用 bug-injection 與靜態核查揪出三項非阻斷測試保護力縫隙：**①（P3）`$MinPass` 數值本身的語意正確性無機械鎖**——`tools/tests/test_smoke_ci_sync.py` 只交叉比對 `ONBOARDING.md` 宣稱數字與腳本 `$MinPass`/`MIN_PASS` 釘選值是否一致，不驗證釘選值是否等於實際步驟數；QA bug-injection 實測「整段刪除 `[7/7]` 步驟本體、保留 `$MinPass=10` 不變」會被 `test_scenario_groups_consistent_and_floored`（分組標籤與總數不符）攔到，但「只改錯 `$MinPass` 數值、步驟仍在」這種情境無專屬鎖。**②（P3）`AutoClaude/README.md` sprint-verified badge 與 `AutoClaude/CLAUDE.md` 檔尾「最後更新」日期皆為純人工欄位、零機械新鮮度檢查**——`grep -rn "sprint-verified\|sprint_verified"` 全 repo 零命中任何驗證腳本，不同於同檔已被 `tools/check_pytest_baseline_sites.py` 機械鎖住的 pytest 基線數字。**③（P3）`tools/tests/test_dev_start.py::TestLaunchdNightlyLoaded` 對 `_launchd_nightly_loaded()` 只測了 `"darwin"`/`"linux"` 兩態，缺 `"win32"` 專屬測試案例**（R18 本輪把該函式判斷式改用 `platform_utils.is_macos()`，行為等價、既有測試仍全過，但 Windows 分支從未被專屬案例直接驅動過） | P3×3（QA 二審判定均非阻斷，R18 本輪合入不受影響） | ①排下輪：仿 `test_known_consumers_detected()`手法，替 `$MinPass` 數值本身補一條「等於步驟數」的語意鎖；②排下輪：評估是否比照 pytest 基線模式（`check_pytest_baseline_sites.py`）替這兩個日期欄位加機械新鮮度告警；③排下輪：補一個 `mock.patch.object(sys, "platform", "win32")` 的獨立測試案例 | open（QA 二審明確判定非阻斷，記事存證排入下一輪 backlog；四方複審 Architect/SA/SD/QA 對 R18 其餘修復之獨立覆核皆確認有效） |
| DEF-101-244 | 2026-07-22 | R19 全面掃描（Scan-A/B/D）複核 R17/R18 五項 backlog（DEF-101-242①②、DEF-101-243①②③）＋新發現一項 | 五項 backlog 皆以 bug-injection／靜態核查重新驗證，**全數確認仍 open**：①`test_find_git_bash_parity.py` 僅結構比對、行為零防護（把 `find_git_bash()` 呼叫點改回舊版寬鬆判斷仍全綠）；②`test_ci_paths_cover_root_consumers.py` 對 `platform_utils.py`/`bootstrap_core.py`/`run_act_core.py` 三檔無機械存在性鎖（對稱移除 CI paths 仍全綠）；③`test_smoke_ci_sync.py` 未鎖 `$MinPass`/`MIN_PASS` 釘選值本身與腳本實際步驟數相符；④`AutoClaude/README.md` badge／`AutoClaude/CLAUDE.md` 日期無機械新鮮度鎖——Scan-D 實測 R18 同一 commit「已同步」的兩處日期，因本地日期／UTC 換算基準不一致，實際仍相差一日（2026-07-21 vs 2026-07-22），證明純人工同步不可靠；⑤`test_dev_start.py::TestLaunchdNightlyLoaded` 缺 win32 專屬案例。**新發現（P2）**：⑥`AutoClaude/tools/local_ci_gate.py:226` 用裸 `os.name == "nt"` 判斷平台、全檔零 `import platform_utils`，是第四處未收斂進 SSOT 的平台判斷邏輯，且未被既有重複掃描鎖（`test_platform_utils_dedup.py`）偵測到 | P2×2（①②）／P3×4（③④⑤⑥） | Architect 定位 DEF-101-242② 根因分屬兩種性質：`platform_utils.py` 是掃描器解析盲區（`sys.path.insert` 多層子目錄未列入候選路徑，可修的 bug）；`bootstrap_core.py`/`run_act_core.py` 是掃描器方法論邊界（執行期子行程消費，靜態 import-BFS 結構上看不到，改用顯式清單而非再加正則） | fixed@R19：①`test_find_git_bash_parity.py` 新增 `TestFindGitBashBehavior`（5 個直接呼叫 `_has_system32_segment()`/`find_git_bash()` 的行為測試，mock 驅動），bug-injection 健全性檢查確認有真實鑑別力；②`test_ci_paths_cover_root_consumers.py` 依 Architect 根因拆兩案：新增 `_resolve_sys_path_insert_dirs()` 動態解析 `sys.path.insert` 實際路徑（取代猜測 own_dir/parent(own_dir)）修復 `platform_utils.py` 盲區；新增 `_KNOWN_SUBPROCESS_ONLY_CONSUMERS` 顯式清單 + `test_known_subprocess_only_consumers_covered_by_ci_paths()` fail-loud 機械斷言修復 `bootstrap_core.py`/`run_act_core.py` 盲區；兩者皆以 bug-injection 重跑確認鎖真的生效（對稱移除後測試正確變紅）；③新增 `test_min_pass_equals_actual_step_count`，以顯式登記表（`_SH_EXCLUSIVE_PASS_GROUPS` 處理 .sh 互斥分支收斂、`_PS1_MULTI_CALL_FUNCS` 處理 .ps1 共用函式呼叫次數展開）機械核算實際步驟數；兩平台 bug-injection（改錯釘選值＋同步改錯 ONBOARDING 宣稱以排除既有交叉檢查訊號）皆證實唯有新測試能抓到「釘選值本身語意錯誤」；④`AutoClaude/CLAUDE.md:325` 訂正為 2026-07-22 對齊 README badge（機械新鮮度鎖本身仍記入 backlog，非本輪 scope）；⑤新增 `test_win32_returns_none_without_spawning`，bug-injection（放行 win32 分支呼叫 launchctl）確認測試變紅；⑥`local_ci_gate.py` 改 `import platform_utils` + `platform_utils.is_windows()`，同步修復 4 個既有測試（`test_local_ci_gate.py` 由 monkeypatch `os.name` 改為 mock `platform_utils.is_windows`）。全部修復皆通過對應測試套件＋`ruff`／`tools/run_root_unittests.py`（330 測試）驗證 | fixed@R19（待四方一審／複審） |
| DEF-101-245 | 2026-07-22 | R19 全面掃描（Scan-C：CI/排程基建維度）新發現 | Windows 側缺少與 mac `tools/install_mac_nightly.sh` 對等的 nightly 排程一鍵安裝腳本——既有 `AutoClaude/tools/fix_nightly_catchup.ps1` 假設 `AutoClaude_Nightly` 排程任務已存在（`Get-ScheduledTask` 找不到即直接拋錯），只能「校正既有任務設定」不能「從零建立」；`ONBOARDING.md:192` 明文稱該任務「既有、可用」，但 repo 內查無任何腳本可讓全新 Windows 機器一鍵建立此排程，須手動 `schtasks`/GUI 操作，與 mac 側「一鍵化＋機械驗證」的排程建置體驗形成結構性不對稱（非既有 `ONBOARDING.md` §8 已明示揭露的「ops 排程家族其餘三支 Windows-only 無 `.sh` 對等」缺口——那三支是方向相反的既有揭露項，本項是全新的一鍵安裝器缺口） | P2 | 新增 Windows 版一鍵安裝器，鏡射 `install_mac_nightly.sh` 定位（install/uninstall/status/`-WhatIf` 四模式），內建 `fix_nightly_catchup.ps1` 記載的補跑保護設定 | fixed@R19：新增 `tools/install_windows_nightly.ps1`（`[CmdletBinding(SupportsShouldProcess)]` 原生支援 `-WhatIf` 預覽、`Register`/`Unregister-ScheduledTask`、`-Status` 用 `Get-ScheduledTaskInfo`；建立時直接內建 `StartWhenAvailable`/`WakeToRun`/`DisallowStartIfOnBatteries:$false`/`StopIfGoingOnBatteries:$false`，新機器不必再另跑一次 fix 腳本）；新增 `tools/tests/test_install_windows_nightly.py`（8 測試：結構驗證＋pwsh Parser 語法解析，macOS/Linux 可跑不需真實 Windows 排程 API）；`tools/check_script_parity.py` 補 `_SINGLE_SIDED_EXEMPT` 雙向登記（`install_mac_nightly.sh`↔`install_windows_nightly.ps1`，stem 刻意不同，措辭刻意避開觸發 `test_onboarding_parity_interlock.py` 互鎖的「schtasks／§8」字面）；`ONBOARDING.md` §8 補充說明。`tools/run_root_unittests.py`（330 測試）、`check_script_parity.py`、`check_ntfs_paths.py`、`check_defect_log_crossref.py` 皆通過 | fixed@R19（待四方一審／複審） |
| DEF-101-246 | 2026-07-22 | R19 四方一審（Architect＋SA 交叉獨立發現同一真實 P1；SD 覆核＋2 項 P3；QA 對抗式 bug-injection＋4 項 P2） | **①（P1，Architect＋SA 交叉獨立發現，已修復）** DEF-101-245 新增的 `tools/install_windows_nightly.ps1`／`tools/tests/test_install_windows_nightly.py` 使 `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py::test_all_root_consumers_covered_by_ci_paths[macos-compat-ci.yml]` 變紅——新測試以字串引用 `AutoClaude/tools/fix_nightly_catchup.ps1`／`AutoClaude/tools/run_local_nightly.ps1`／`tools/install_windows_nightly.ps1` 三支 `.ps1`，`windows-compat-ci.yml` 因有 `**/*.ps1` 兜底 glob 未中招，`macos-compat-ci.yml` 只有 `**/*.sh`、缺這三支根層 `.ps1` 路徑（DEF-101-042 同構假綠盲區，且恰好是同一輪自己修過的測試檔又被自己新增的檔案打壞）。**②（P2，QA 對抗式 bug-injection，已修復）** `test_ci_paths_cover_root_consumers.py` 的 `_IMPORT_RE` 只認 `import X`／`import X as Y`，`from X import Y` 完全零防護——QA 建臨時根層模組＋以 `from X import Y` 消費的臨時測試，10 個測試仍全綠。**③（P2，QA 對抗式 bug-injection，已修復）** `TestFindGitBashBehavior`（`test_find_git_bash_parity.py`）只保護了 `find_git_bash()` 的 PATH 檢查分支，環境變數候選路徑的存在性檢查（`cand.is_file()`）完全無測試——QA 把該檢查拿掉，9 個測試仍全綠，函式會回傳磁碟上不存在的幽靈路徑。**④（P2，QA 對抗式 bug-injection，已修復）** `test_win32_returns_none_without_spawning` 只 mock `subprocess.run`，對 `Popen`/`os.system` 等其他子行程 API 無視野，「不 spawn 任何子行程」的意圖比實際斷言範圍寬鬆。**⑤（P2，QA 對抗式 bug-injection，判定為登記表方法論的既有侷限、非本輪迴歸，記入 backlog）** `test_min_pass_equals_actual_step_count` 的 `_SH_EXCLUSIVE_PASS_GROUPS` 顯式登記表完全信任人工登記——QA 在 `macos_smoke_local.sh` 插入兩個實際非互斥、但謊報登記進登記表的假互斥 `pass` 呼叫（連同同步竄改 `MIN_PASS` 與 `ONBOARDING.md` 排除交叉訊號），測試仍全綠；此為「顯式登記表換取正確性」設計本身固有的信任邊界（Architect／作者已在測試 docstring 自陳此取捨），非可用簡單修法補上，需要真正的 if/else 控制流解析才能杜絕，與該檔案已放棄的通用剖析器方向矛盾。**⑥（P3，SD 覆核，記入 backlog）** `_resolve_sys_path_insert_dirs()` 對 `sys.path.insert` 只認固定寫法（`str(Path(__file__).resolve().parents[N] / "seg")`），未來若多包一層括號或改用 `.parent.parent` 會靜默回退成猜測（與該檔案 S6→S12→S26 反覆盲區同構）。**⑦（P3，SD 覆核，記入 backlog）** `install_windows_nightly.ps1` 的 `-Status` 判斷式排在最前面且直接 `exit 0`，若使用者同時給 `-Status -Uninstall`，`-Uninstall` 會被靜默忽略、無警告訊息，與 repo「fail loud」慣例（Rule 12）略有落差 | P1×1（①，必修）／P2×4（②③④已修＋⑤登記表侷限入 backlog）／P3×2（⑥⑦入 backlog） | ①②③④ 本輪就地修復；⑤⑥⑦ 排下一輪評估是否值得投入真正的控制流解析或維持現狀 | fixed@R19：①`macos-compat-ci.yml` push＋pull_request 兩處 paths 補上三支 `.ps1`，`test_ci_paths_cover_root_consumers.py` 10 passed（含對稱移除重跑應變紅的迴歸驗證）；②`_IMPORT_RE` 旁新增 `_FROM_IMPORT_RE` 涵蓋 `from X import Y`，QA 原始注入場景重現後確認變紅；③`test_find_git_bash_parity.py` 新增 `test_find_git_bash_skips_env_var_candidate_that_does_not_exist_on_disk`，QA 原始注入（拿掉 `cand.is_file()`）重現後確認變紅；④`test_win32_returns_none_without_spawning` 同時 mock `subprocess.run`／`subprocess.Popen`／`os.system` 三者皆 `assert_not_called()`。全部修復皆以 bug-injection 逐一重跑確認有真實鑑別力，還原後 `cp`/`diff`/`md5` 核對逐位元組一致；`tools/run_root_unittests.py`（330 測試）、`AISDLC_SDD/scripts/tests/`（174 測試）、`check_script_parity.py`、`check_ntfs_paths.py`、`check_defect_log_crossref.py` 全綠 | fixed@R19，四方複審 Architect/SA/SD 皆 APPROVE、QA APPROVE-with-conditions（條件為記錄複審新發現，見 DEF-101-247，不阻斷本輪） |
| DEF-101-247 | 2026-07-22 | R19 四方複審（QA 二審，對 DEF-101-246②③④三項修復本身再做對抗式 bug-injection） | QA 複審刻意不重複一審手法、改用新角度戳修復本身：**①（P2，已修復）** `test_find_git_bash_skips_env_var_candidate_that_does_not_exist_on_disk` 只 mock 單一環境變數候選（`ProgramFiles`），無法區分「正確回 None」與「迴圈提前中止（第一候選不存在就直接放棄，不繼續檢查 `LocalAppData` 等後續候選）」——QA 把 `if cand.is_file(): return str(cand)` 改成該分支後再加一行無條件 `return None`（第一輪就放棄），該測試仍全綠；具實務風險：Git for Windows per-user 安裝預設落在 `LocalAppData`，若 `ProgramFiles` 剛好無 Git，此迴歸會誤判「找不到 Git Bash」。**②（P3，記入 backlog）** `_FROM_IMPORT_RE`（DEF-101-246②修復）沿用舊 `_IMPORT_RE` 同款行首錨點設計，只堵「行首 `from X import Y`」，未堵縮排的防禦性匯入（`try: from mod import X / except ImportError: X = None`）；SD 覆核確認現行 `tools/tests/`／`AISDLC_SDD/scripts/tests/` 皆無此慣用法實例，屬理論性、非現行盲區。**③（P3，記入 backlog）** `test_win32_returns_none_without_spawning`（DEF-101-246④修復）三重 mock（`subprocess.run`/`Popen`/`os.system`）未涵蓋 `os.spawnv`/`os.posix_spawn` 等不經 `Popen` 的行程建立 API；SD 覆核確認本專案風格全走 `subprocess`，發生機率低。**④（提案，記入 backlog，非本輪實作）** 針對 DEF-101-246⑤（`_SH_EXCLUSIVE_PASS_GROUPS` 顯式登記表可被謊報繞過）QA 提出輕量緩解：擷取登記表內兩則訊息在原始碼中的行號，斷言（a）兩者之間存在 `else`/`elif`/`;;` 其中之一的字面字串，且（b）行距不超過寬鬆上限（如 30 行）——不能杜絕蓄意造假，但能擋下「兩個無條件執行、彼此相鄰又無分支關鍵字」這種注入手法，成本遠低於完整控制流解析 | P2×1（①已修）／P3×2（②③記入 backlog）／提案×1（④記入 backlog） | ①本輪修復；②③④排下一輪評估 | fixed@R19：①`tools/tests/test_find_git_bash_parity.py` 新增 `test_find_git_bash_continues_to_next_env_var_candidate_when_earlier_one_missing`（兩候選情境：`ProgramFiles` 磁碟不存在、`LocalAppData` 磁碟存在，斷言正確回傳 `LocalAppData` 候選而非提前 `None`），QA 原始注入（`is_file()` 判斷後強制 `return None`）重現後確認變紅，還原後 `md5` 核對逐位元組一致；`python3 -m pytest tools/tests/test_find_git_bash_parity.py -q` 11 passed。②③④非本輪範圍，記入 backlog；四方複審 Architect/SA/SD/QA 皆已對 DEF-101-246 給出 APPROVE 或 APPROVE-with-conditions，本項修復完成後視為條件已滿足 | fixed@R19（待四方三審確認①即可，②③④為 backlog、非阻斷） |

## 四方一審裁決總結（R17，2026-07-21）

Architect／SA／SD／QA 四方獨立審查（皆直接於主工作樹操作，未用 git worktree 隔離）：

- **Architect**：APPROVE-with-conditions（F1 CI paths 盲區必修項已修復；F2/F3/F4 為非阻斷建議記事存證）
- **SA**：APPROVE-with-conditions（F1 帳本行數再漂移、F2 同 Architect F1、F3 註解精確度，三項皆已修復/訂正）
- **SD**：APPROVE-with-conditions（`windows_smoke_local.ps1` git 版本守衛失效、`Copy-ItemWithRetry` 註解不精確，兩項 P3 皆已修復）
- **QA**：APPROVE-with-conditions（親自重跑 314+3701+173+1475+1674 測試全綠＋144 skip 全數分類正常＋4 項回報修復逐一獨立覆核屬實；另揪出 2 項 P2 測試保護力缺口，QA 自判非阻斷、已記入 DEF-101-242 排下輪）

四方合計提出 4（Architect+SA 交叉/獨立）+2（SD）+2（QA）＝8 項發現，其中 6 項（DEF-101-241 ①②③④）於一審視窗內全數修復並經 QA 複審獨立驗證確認（含真實 pwsh 執行、非僅語法檢查）；剩餘 2 項（DEF-101-242）為 QA 自身判定非阻斷的測試覆蓋殘留缺口，記事存證排入下一輪。並行審查期間偵測到與 R16 同款的暫態污染（Architect/SD/QA 各自 bug-injection 時序重疊造成的檔案內容瞬態變化），依既有緩解方式（獨立 `git status`/`diff` 核查）確認皆已妥善還原、非真實缺陷。所有「必須修復」項目已於一審視窗內處理完畢，進入最終本機驗證與收尾。

## 四方一審／複審裁決總結（R18，2026-07-22）

前置全面掃描（Shell/PowerShell、Python、CI/排程基建、文件帳本 4 個獨立 agent ＋ Architect 架構檢視，皆背景並行、唯讀）零 P0，共 6 項需修（1 P1＋2 P3＋1 P2＋2 P4）；修復按檔案所有權切成 2 個不重疊修復包並行執行。Architect／SA／SD／QA 四方獨立一審（皆直接於主工作樹操作，未用 git worktree 隔離）：

- **Architect**：APPROVE-with-conditions（P1 必修：`ONBOARDING.md:203` `PASS=8` 未同步 `windows_smoke_local.ps1` 改成的 `$MinPass=10`，親跑 `tools/run_root_unittests.py` 實測 `test_smoke_ci_sync.py` 真的 FAIL；其餘 6 項修復逐一驗證通過）
- **SA**：APPROVE-with-conditions（獨立揪出 Architect 未提及的 P2：`.github/workflows/windows-compat-ci.yml` 第 674 行 `windows-nightly-full` job 的「安裝 AutoClaude 開發 + lint 依賴」步驟，有與本輪已修部分相同的 R15 SCAN-C-10 pip exit code 吞沒缺陷未修，屬本輪掃描漏網項目）
- **SD**：APPROVE（逐行/語法/全域回歸皆綠，但一審僅跑 `AutoClaude pytest` + `AISDLC_SDD ci-gate.sh`，未跑根層 `tools/run_root_unittests.py`，故未發現上述 Architect 點名的 P1）
- **QA**：APPROVE-with-conditions（bug-injection＋`git stash` 對照確認 P1 為本批異動自身造成、非既有缺陷；另合記 3 項非阻斷測試保護力缺口，已記入本表 DEF-101-243 排下一輪）

一審共 2 項必修（Architect 與 QA 各自獨立命中同一個 `ONBOARDING.md` PASS 數字問題，交叉印證；SA 獨立命中 CI workflow 漏網處），皆由主控直接就地修復（複審揪出的小缺陷、一行修規模，未另開修復包）：① `ONBOARDING.md:203` `PASS=8`→`PASS=10` 並補述 R18 新增 `[7/7]` 檢查說明；② `.github/workflows/windows-compat-ci.yml` 674 行區塊補 `$LASTEXITCODE` 檢查，逐字鏡射同檔既有「Install AutoClaude deps」步驟寫法。修復後以 `SendMessage` 派回四方原審查 agent（保留一審上下文）複審，四方**全數 APPROVE**（Architect/SA/QA 皆親自重跑 `test_smoke_ci_sync.py`／`run_root_unittests.py`／yaml lint 驗證修復生效且未引入新問題；SD 本輪複審把根層測試補進驗證範圍，記錄為流程教訓）。QA 複審時合記的 3 項非阻斷測試保護力缺口已記入 DEF-101-243 排入下一輪。並行一審期間未偵測到暫態污染。所有「必須修復」項目已於一審→複審視窗內處理完畢並全數 APPROVE，進入最終本機驗證與收尾。

## 四方一審裁決總結（R16，2026-07-21）

Architect／SA／SD／QA 四方獨立審查（皆直接於主工作樹操作，未用 git worktree 隔離——本輪實測 worktree 只會 checkout 至最後一次 commit、看不到任何未提交異動）：

- **Architect**：APPROVE-with-conditions（DEF-101-232 描述訂正 + `bootstrap_core.py` buffering 補齊，皆已修復；DEF-101-236 排入 backlog）
- **SA**：APPROVE-with-conditions（DEF-101-239 已修復並重新驗證）
- **SD**：APPROVE（DEF-101-237／DEF-101-238 為非阻斷 P3/P4，排入 backlog）
- **QA**：APPROVE（獨立重跑六項核心驗證數字與宣稱精確吻合；5 項 bug-injection 紅/綠驗證證實本輪新增測試皆非偽綠燈）

四方合計親手執行 bug-injection 實驗 15+ 項，全數證實對應測試/守門機制真實鎖住各自修復，未發現偽綠燈或誇大不實的驗證聲稱（除 DEF-101-239 一項已誠實記錄並即時修復）。所有「必須修復」項目已於一審視窗內處理完畢，待重新完整驗證後進入複審確認。

## 歷史複驗註記 — 已歸檔（improving_99 起）

> **歸檔政策（DEF-99-001 fixed@improving_99）**：本帳本「跨輪累積、只增不刪」，但中文長列使單檔逼近工具讀取上限（曾達 468KB > Read 256KB）。自 improving_99 起，**歷史敘事複驗/收尾註記**（其缺陷現況已被上方缺陷總表 live 狀態取代，或為已結歷史）搬遷至分檔，**原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）；主檔只保留標頭 + 完整缺陷總表（live SSOT）+ 本指標。**輪替規則：主檔與「每一個」 archive 檔皆以 < 256KB（Read 工具上限）為界**——主檔再逼近即開新 archive；單一 archive 逼近即拆下一個 archive（故本輪一次就把 619 行歷史拆成 archive_01 + archive_02 兩檔，而非單一超限檔）。
>
> **已歸檔內容**（四檔）：
> - **`AutoSDD_Defect_Log_archive_01.md`**（原主檔 93-543 行，≈170KB）：rounds 24-63 各輪複驗/收尾敘事註記 + 2026-06-22 agent/* 符規審查臨時塊（DEF-AGTREV-001~013 全 fixed@v0.18）+ 早期 `.claude` hooks/skills 審查輪（一~六）。
> - **`AutoSDD_Defect_Log_archive_02.md`**（原主檔 544-711 行，≈177KB）：improving_45 追記 + 2026-06-23 `.claude` hooks/skills 審查輪（三~九，DEF-CLDREV-* 系列收斂至零缺陷）。
> - **`AutoSDD_Defect_Log_archive_03.md`**（R9 跨平台複審 2026-07-16 建立，≈110KB）：缺陷總表中 **DEF-01～DEF-100 系列已結列（fixed / closed-by-decision）64 列**逐字搬移（主檔當時 272KB 已超 256KB 界線，DEF-101-123）。
> - **`AutoSDD_Defect_Log_archive_04.md`**（R14 跨平台輪 2026-07-20 建立，≈210KB）：缺陷總表**已結列（fixed / closed-by-decision）169 列**逐字搬移（主檔 258KB 進 240KB 預警區；含 R9 時因 crossref 引用留存、現已無引用的 DEF-20-001，及 DEF-101 系列已結列至 DEF-101-187）。
>
> **已知歷史重疊（R16 文件帳本一致性掃描發現，記事不刪除）**：`archive_01.md` 與 `archive_03.md` 各自完整搬移了 **DEF-37-001／DEF-62-001** 兩列（內容一致、結案狀態相同，非矛盾——推測為兩次獨立搬遷批次各自涵蓋範圍重疊所致）。因 `check_defect_log_crossref.py` 只掃描本主檔與少數指定目標檔，不掃 archive 間互相重疊，此重複未被既有機械守門攔下。依「archive 只增不刪、原文逐字保全」政策不予去重，僅此記事存證，供未來若真要整併 archive 時參考。
>
> **重要**：歸檔當下，所有**仍未結（open / routed / wontfix）缺陷與被 crossref 掃描目標引用的列均留在上方缺陷總表**（R14 輪替搬移當下保留 30 列既有，其後各輪陸續新增——**主檔現列數一律以 `grep -c '^| DEF-'` 實數為準，本註記不再快照具體列數/新增區間**：快照寫法已兩輪連續 stale（ARCH-R14-RR-1 訂正後 DEF-101-211 自身入帳又差 1，R15 SCAN-D-2 實證），改為方法引用消滅漂移面），故 live 追蹤**無須**讀 archive；archive 僅供已結缺陷之完整歷史佐證與審計回溯。
>
> **查詢某缺陷現況**：先看上方「缺陷總表」（完整 SSOT）；需歷史脈絡再查對應 archive 檔。
