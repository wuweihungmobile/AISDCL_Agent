# AutoSDD 缺陷帳本（Defect Log）— 跨輪累積、只增不刪

> **用途**：AutoSDD 雙軌迭代（A 軌整合 / B 軌 Dogfooding）行進中發現的框架缺點/Bug/摩擦，
> 依「發現即記、絕不累積」紀律入帳，並依分流規則回流改進（見
> `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 的「🐶 自我迭代模式」）。
> **建立**：2026-06-12（首輪 N=01；首輪或帳本不存在時於階段一建立含表頭的帳本）。
>
> **格式定義**（每筆一列）：
> `ID`｜`發現日期`｜`發現情境`（FSM 狀態 / SCG 閘門 / hook / skill / nightly stage / 審計）｜
> `現象與證據`（file:line 或命令輸出）｜`嚴重度`｜`分流去向`｜`狀態`
> - **ID**：`DEF-{{N}}-{seq}`，N = 迭代輪編號（01/02…），seq 為該輪流水號。
> - **嚴重度**：依 v0.01 官方
>   `docs_template/sdd/testing/DEFECT-CLASSIFICATION-SPEC-TEMPLATE.md` 之 Priority 分級：
>   **P0**（Urgent — 必須立即修復，阻擋當前測試/發布）/ **P1**（High — 當前 Sprint 修復）/
>   **P2**（Normal — 下個 Sprint 修復）/ **P3**（Low — Backlog，擇機修復）。
> - **狀態**：`open`（未分流）/ `routed`（已分流待修）/ `fixed@<版本或載體>`（已修，附證據）/
>   `wontfix+理由`。
> - **分流去向**（對應官方回流機制）：規格/文檔缺陷 → Phase J SPEC-PATCH（併入 v0.0(X+1)）；
>   治理規則缺陷 → FPL→SLV→LEARNING_COMMIT；框架程式/模板/hook 缺陷 → RFC + v0.0(X+1)；
>   整合層（AutoClaude 側）缺陷 → 下輪 A 軌 W 項。

## 缺陷總表

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-01-001 | 2026-06-12 | zero-trust 審計 | `AISDLC_SDD_v0.01/governance/RULES_INDEX.md:9` 表頭「共 35 檔」過期——`ls governance/rules/` 實數為 38 檔（R-9.x 37 條 + R-SELF-STRIDE）；2026-06-12 複驗仍存在 | P2 | v0.02（凍結本體文檔修正，Copy-on-Evolve） | fixed@v0.02（證據：`AISDLC_SDD_v0.02/governance/RULES_INDEX.md:8-11` 改記 39 檔〔R-9.1~9.38 + R-SELF-STRIDE〕+ next-act/next-rule 前緣同步 172/9.39；v0.01 凍結基線依紀律不回改） |
| DEF-01-002 | 2026-06-12 | zero-trust 審計（nightly/CI 腳本） | `AISDLC_SDD_v0.01/tools/fsm_runtime/formal/run_tlc.sh` 僅實裝 2 軌（SDD_FSM L73-77 + FLEET_FSM L119-137），與五軌真相源 `tlc_runner.py` 不對稱且全檔無說明註記；2026-06-12 複驗仍存在 | P3 | v0.02（補註記「五軌請走 tlc_runner.py」或補齊五軌） | fixed@v0.02（證據：`AISDLC_SDD_v0.02/tools/fsm_runtime/formal/run_tlc.sh:4-7` LEGACY 兩軌快驗註記 + 指向 tlc_runner 五軌） |
| DEF-01-003 | 2026-06-12 | zero-trust 審計（nightly/CI 腳本） | `AISDLC_SDD_v0.01/tools/` 缺顯式 `__init__.py`（`Test-Path` = False，2026-06-12 複驗），`python -m tools.…` 依賴 py3.3+ implicit namespace package | P3 | v0.02 | fixed@v0.02（證據：`AISDLC_SDD_v0.02/tools/__init__.py` 已建立，v0.02 pytest not-chaos 全綠驗證無回歸） |
| DEF-01-004 | 2026-06-12 | zero-trust 審計（AutoClaude CI） | AutoClaude `.github/workflows/autoclaude-ci.yml:189`（現名，已上移根層）pg-e2e-nightly 的 `if` 僅檢 `github.event_name == 'schedule'`，未以 `github.event.schedule` 過濾 cron——03:00 UTC 的 mutation cron（`autoclaude-ci.yml:11-13` 兩條 cron）也會觸發 pg-e2e，每晚雙跑（隱性成本，非錯誤）；2026-06-12 複驗仍存在；2026-06-13 improving_02 階段一複驗仍重現 | P3 | 下輪 A 軌 W 項（AutoSDD_improving_02.md W2） | fixed@improving_02（證據：`autoclaude-ci.yml:191` 改為 `(github.event_name == 'schedule' && github.event.schedule == '0 2 * * *') \|\| workflow_dispatch`，綁定 pg-e2e 自身 02:00 cron；對齊 mutation `0 3`(:281)/perf-baseline `0 2`(:444) 既有過濾〔行號含 W2 新增 2 行註解後之實際位置〕；YAML safe_load 通過、過濾字串計數=2） |
| DEF-01-005 | 2026-06-12 | 整合規劃流程（原始 Prompt vs 系統實況比對） | 原始整合 Prompt 假設與系統實況偏差：`GoalSynthesisPlugin` 已存在（`goal_synthesis_plugin.py:29`）卻被假設為待建；Port 實名為 `IBrain`/`IEvaluator`（`core/ports/brain.py:72`、`core/ports/evaluator.py:20`）卻寫作 BrainPort/EvaluatorPort——屬流程缺陷「規劃前未做 zero-trust 偵察」（證據：`AutoSDD_ZeroTrust_Audit_01.md` §2 比對表） | P2 | 已由迭代範本「階段一強制重偵察」修復 | fixed@範本v2（證據：`AutoSDD_Iteration_Prompt_Template.md` 階段一「Zero-Trust Re-Audit」條款 + 硬閘） |
| DEF-01-006 | 2026-06-12 | W6 實作（zero-trust 開檔複驗 kernel 派發實況） | 凍結計畫 §4.1/§4.2 錨定之 `POST_EVALUATE` / `ON_ESCALATION` 為合法 `KernelPhase` 枚舉（`hookspec.py:33,39`）但 **kernel 現況不派發**——全 codebase `phase=KernelPhase.X` emit 點僅 11 處（`kernel.py:87,120,136,153,174,176,204,222` + auto_resume/checkpoint 3 處），評估後實際派發為 `POST_ATTEMPT`/`ON_FAILURE`/`ON_SUCCESS`。計畫凍結前複驗只驗了「枚舉存在」未驗「實際派發」 | P2 | W6 已實作補償：`SddGovernancePlugin` 雙訂閱（計畫 phase 向前相容 + 實際派發 phase 真實載體，`sdd_governance_plugin.py` docstring 載明）；計畫文件已凍結不回改，偏差記入 `AutoSDD_ZeroTrust_Audit_01.md` §7；下輪迭代範本「階段一」應加「枚舉存在 ≠ 實際派發」檢核項 | fixed@W6（證據：`tests/plugins/test_sdd_governance.py` 22 case 以實際派發 phase 驗證全行為） |
| DEF-01-007 | 2026-06-12 | W8 整合驗收 | `cc-switch` 未安裝（`Get-Command cc-switch` 不存在），計畫 §5.2 多模型後端 A/B 驗收無法執行；`integration_gate.ps1` [5/5] 段落以 SKIP + 指引處理（非偽綠：SKIP 明示於輸出） | P3 | 環境工具補裝後依 §5.2 手動執行 A/B（指標：一次通過率 / CORRECTION 次數 / SDD_CONTRACT_VIOLATION 次數 / token 峰值）；俟環境補裝 | open（2026-06-14 improving_06 結案盤點複驗：`which cc-switch`=NOT FOUND 仍重現；屬環境工具缺裝非純程式可修；`integration_gate.ps1 [5/5]` SKIP 非偽綠；**不阻擋 A 軌主鏈結案**——功能路徑齊備，僅缺多後端對比驗收；2026-06-14 improving_08 階段一複驗 `which cc-switch`／`command -v cc-switch` 皆 NOT FOUND 仍重現，屬環境工具缺裝非純程式可修，維持 open 俟補裝；2026-06-14 improving_09 階段一複驗 `command -v cc-switch`=NOT FOUND 仍重現，維持 open〔🔴 人工確認本輪選「輕量結案盤點」，未補裝〕；**2026-06-14 improving_10：🔴 人工選「補裝 cc-switch 關閉」**。使用者實裝 cc-switch，但裝的是主流 GUI 版（farion1231）→ `command -v cc-switch`=**NOT FOUND**（GUI app 不上 PATH，實證 **DEF-10-001(b)**）。本輪移除**所有倉內阻塞**（DEF-10-001 fixed：建 A/B 載具 + 硬化 gate），DEF-01-007 由此**縮窄**為純「環境/API 動作」：僅待使用者裝 cc-switch **CLI 變體**（SaladDay/cc-switch-cli）+ 配 2 個 model profile（含 API key）+ 授權 token 花費後，於 AutoClaude/ 一鍵跑 `cc-switch use <profile> && autoclaude scripts/sdd_bridge_smoke.yaml --fresh` 收 4 指標。維持 **open**（非純程式可修，倉內已零阻塞）） |
| DEF-01-008 | 2026-06-12 | 三專家審查（Architect） | production 入口 main.py 呼叫 build_kernel 未傳 brain=（grep 證實非測試呼叫點僅 wiring 內部）→ SddGovernancePlugin._on_failure 的 IBrain.decide_escalation 升級諮詢於正式 CLI 路徑為死碼（plugin 已優雅降級 brain=None 直接 return，既有 EvolutionPlugin 升級鏈不受影響）。main.py 注入 MinimaxBrain 會同時啟用 kernel.decide_correction 路徑、改變既有修正行為，須獨立評估 | P1 | 下輪 A 軌 W 項（AutoSDD_improving_03 W1：main.py brain 注入影響評估 + e2e） | fixed@improving_03（證據：影響評估證實 `build_kernel` 把 brain 同時下發 `kernel`(wiring.py:315) 與 `SddGovernancePlugin`(wiring.py:307→202)，盲注入會改 production correction 語意〔kernel.py:198 區塊由跳過轉啟用 + API 故障 ESCALATE〕→ 採 **flag-gated** 落地：`MinimaxConfig.enable_kernel_brain` 預設 `False`＝brain=None 維持現況〔零退化〕，顯式啟用才注入。`main.py:~96` flag-gated 構造 `MinimaxBrainAdapter(minimax)`；`tests/integration/test_def_01_008_brain_injection.py` 9 case 鎖定〔預設 False / brain=None 雙消費者皆 None / flag-on 同一 brain 抵達 kernel+governance / correction 可達 / API 故障 ESCALATE / governance escalation 可達〕；full pytest 3060→3069 passed 0 failed、lint-imports 8 kept；死碼轉為 flag-gated 可選能力） |
| DEF-01-009 | 2026-06-12 | 三專家審查（Architect） | sdd_governance_plugin.py raw 250 行恰貼 plugin_entry ≤250 上限（工具計非空行 217 過關）——後續任何擴充必須先拆 <feature>_plugin/ package | P3 | 下輪擴充前置作業（watch item） | open watch（2026-06-14 improving_06 結案盤點複驗：raw 仍 250、受控指標非空行 **224 < 250**、`check_loc_budget` violations=0＝已自癒；續 watch：對該 plugin 任何擴充前必先拆 package；本輪零擴充不觸發；2026-06-14 improving_08 階段一複驗 raw 仍 250、violations=0 已自癒持平，零擴充不觸發，維持 open watch；2026-06-14 improving_09 階段一複驗 raw 仍 250、`check_loc_budget` violations=0 已自癒持平，本輪零擴充不觸發，維持 open watch；**2026-06-15 improving_14：watch 首次真正觸發並處置**——W-14-2 拓樸橋接擴充 `sdd_governance_plugin.py`，count_loc 自 224→恰貼 250 上限（零餘量）。依本 watch「擴充前先拆」紀律，將拓樸載入邏輯抽出 `plugins/_sdd_topology_signoff.py`（27 行 helper，非 importlinter Rule 1 independence 清單成員 → plugin→helper 合法），plugin 降回 **243 < 250（餘 7）**、`check_loc_budget` violations=0、`lint-imports` 8 kept。watch 紀律有效運作，維持 open watch 續守後續擴充）。**improving_22 複驗：本輪零擴充 sdd_governance_plugin（W 項為 AISLDC_SDD 框架側），watch 不觸發，維持 open watch**；**improving_98 複驗（2026-06-30）：raw 277、`check_loc_budget` violations=0（count_loc 受控指標 < cap 250、自癒持平），本輪 W 為框架側 Copy-on-Evolve（零擴充該 plugin），watch 不觸發；正確處置＝維持 open watch、不投機拆 package（守工程紀律 Rule 2/3）** |
| DEF-01-010 | 2026-06-12 | 三專家審查（SA-SD） | v0.02 新增 compiler agent 後 AISDLC_SDD_INIT.md 仍宣稱 25 Agents / 18 specialized（磁碟實數 26/19），arch_fitness FF-7 advisory 已偵測 | P2 | 本輪即修（v0.02 文件計數同步） | fixed@v0.02（證據：INIT.md 計數更正 + EVOLUTION_LOG delta 補記） |
| DEF-02-002 | 2026-06-14 | improving_02 收尾 zero-trust 主 agent 親跑複核（TLC 計數欄位誠實性） | `tlc_runner.py:82-83,111-112` 報 `TLC_DISTINCT=855 > TLC_GENERATED=706`，違反 TLC「generated ≥ distinct」恆等不變量（主 agent 2026-06-14 親跑 SDD_FSM 重現 855/706/depth14，與原審計 855/706/depth15 之 generated/distinct 完全吻合、僅 depth 差 1 屬 noise）。根因：`_grp` 用 `re.search` 取**首個**匹配，疑抓到中途 progress 行而非最終 summary，致計數標籤接反/不可靠（副審查 agent 另一次跑得 49/115 即不穩定佐證）。**權威停機判準為 `No error has been found`（五軌穩定、跨 agent 一致）**，raw 計數僅供參考 | P3 | 框架程式缺陷 RFC + v0.0Y（修 `tlc_runner` 改取 last-match 最終 summary + 斷言 `generated ≥ distinct`，否則 raise） | fixed@v0.04（證據：`AISDLC_SDD_v0.04/tools/fsm_runtime/tlc_runner.py` 抽出 module-level `parse_tlc_summary(out)`，`re.findall(...)[-1]` 取最終 summary 取代 `re.search` first-match + fail-closed 斷言 generated>=distinct 違反即 raise RuntimeError；`tests/test_tlc_runner_parsing.py` 4 case 全綠〔last-match/正常不誤報/畸形 raise/無匹配回 0〕；v0.04 五軌 TLC 重跑修正後 generated>=distinct〔見 EVOLUTION_LOG v0.03→v0.04〕。Copy-on-Evolve：v0.03 凍結） |
| DEF-02-001 | 2026-06-13 | B 軌 dogfooding（W1-d 跑 v0.03 一致性測試時） | Copy-on-Evolve 結構摩擦：v0.01/v0.02/v0.03 各保留**同名測試模組**（`tools.fsm_runtime.tests.test_md_python_sync` 等），從 v0.03 cwd 跑 pytest 時 traceback **顯示** `..\AISDLC_SDD_v0.02\...test_md_python_sync.py:85` 路徑（誤導為在測 v0.02），但實際受測為 v0.03（直接 import `transition_rules.__file__` = v0.03、collect-only 為 v0.03 相對路徑、斷言偵測到 v0.03-only 的 AUTOCLAUDE_DELEGATED 邊已證實）。功能正確、僅 traceback 路徑顯示誤導；潛在 sys.modules 同名模組 collision 風險（官方 ci-gate.sh 寫死 cd v0.01 + 各版獨立跑 → 不觸發跨版同跑，故官方閘門無此問題） | P3 | RFC：v0.0Y 評估每版測試 rootdir 隔離（或 conftest 注入版本前綴）；非阻塞，記為結構性 watch item | fixed@improving_05（**根因比原記更深**：improving_05 §2.2 沙箱實證——即便最新版改 importlib/移除 `__init__.py`，跨版同跑仍壞，因各版**生產套件 `tools.fsm_runtime.*` 同名**，單一 process sys.modules 只能裝一版 → 測試誤載別版生產模組〔情境3 `assert 'vA'=='vB'` 失敗〕。∴「單一 process 跨版同跑」**結構性不可支援**，唯一正解＝每版獨立 process〔官方 ci-gate.sh `cd vX` 本就正確隔離〕。採 **Rule 12 Fail-Loud guard**（非徒勞「讓跨版能跑」）：`AISDLC_SDD/conftest.py`〔repo 根、versioned 目錄外＝共享 CI infra，**免 Copy-on-Evolve**，同 DEF-03-001 精神〕以 `pytest_configure` 偵測本 session 觸及 >1 版 → raise `pytest.UsageError`，把 cryptic `ImportPathMismatchError` 轉為可行動訊息〔具名版本+正確用法〕。偵測邏輯抽 `scripts/cross_version_guard.py` 純函式 + `scripts/tests/test_cross_version_guard.py` 8 case 鎖定。驗證：bare `pytest` from root 實測 fire〔exit 4 + DEF-02-001〕；雙軌 ci-gate `cd vX` exit 0 證零干擾；AutoClaude 3069 持平、lint-imports 8 kept。**殘留**：顯式 `pytest vX/ vY/` 雙路徑因 version `pytest.ini` 捕獲 confcutdir、根 conftest 不載入而攔不到，列文件化已知限制〔原生錯誤已具名兩版可診斷〕。零觸碰任一 v0.0X 凍結本體） |
| DEF-05-002 | 2026-06-14 | improving_05 收尾 zero-trust 審計（SA-SD 鏡，三鏡親跑複核） | 流程缺口：DEF-05-001 把 hook 由 `pytest_load_initial_conftests` 改 `pytest_configure` 時，同輪 `AISDLC_SDD/scripts/tests/test_cross_version_guard.py:92` 引用該 hook 名的測試 docstring **未同步更新**（仍寫舊 hook 名），測試本體斷言/被測行為正確、無功能影響，但文件與實作不一致，直到收尾 zero-trust 審計才被抓到。根因：介面/hook 重命名時缺「同步掃描所有引用該名稱之註解/docstring」檢核步驟 | P3 | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）補檢核項「重命名/hook 變更後 grep 引用點同步註解/docstring」；屬流程改善非框架程式缺陷 | fixed@improving_05（證據：docstring 已改 `pytest_configure` 與 `conftest.py:26` 一致；獨立 QA agent 重跑 `test_cross_version_guard.py` 8 passed、grep 無殘留舊 hook 名〔`conftest.py:10` 對照說明為刻意保留〕） |
| DEF-05-001 | 2026-06-14 | B 軌 dogfooding（improving_05 W1-c 實作 guard 時） | pytest hook 語意摩擦：guard 原設計用 `pytest_load_initial_conftests` 攔截，沙箱偶然 fire 但**真 repo 不 fire**——根因 chicken-and-egg：rootdir conftest 是在該 hook 的 default 實作*之內*才被載入，故自身對該 hook 的實作不會被回呼（真 repo `pytest --co` 仍走到 collection 撞 `ImportPathMismatchError`）。改用 `pytest_configure`（configure 時 conftest 必已註冊，版本目錄碰撞在其後 collection → configure 先 raise）即正確 fire。屬 pytest 內部機制摩擦，非 AISDLC_SDD 框架缺陷 | P3 | 實作期即修（hook 改 `pytest_configure`）；記錄供後續 conftest-level guard 參考 | fixed@improving_05（證據：`AISDLC_SDD/conftest.py` 用 `pytest_configure`；`test_bare_pytest_from_root_fires_guard` subprocess 整合測試綠，退化即紅） |
| DEF-03-001 | 2026-06-14 | B 軌 dogfooding（improving_03 階段一重偵察跑 ci-gate 時） | Copy-on-Evolve 版本治理缺口：`AISDLC_SDD/scripts/ci-gate.sh:16-18` 寫死 `FW_DIR="${REPO_ROOT}/AISDLC_SDD_v0.01"` → 官方 CI 閘門（腳本 header 自述為 `.github/workflows/aisdlc-sdd-ci.yml`（現名，已上移根層）/docker ci-runner/pre-push/act 的單一真相源）**永遠只測 v0.01 凍結基線**；實際承載框架演化的 v0.02/v0.03/v0.04（EVOLUTION_LOG 自述「可修改版本」）**不在官方閘門/CI/pre-push 覆蓋內**，僅靠人工 `cd vX && pytest -m "not chaos"` 守護（improving_02/03 皆手動驗）。最新版＝實際改動版反而無自動化閘門，與「地端=CI=ubuntu-latest 同一組檢查」初衷相違。與 DEF-02-001 同根（Copy-on-Evolve 跨版治理）。2026-06-14 實測：`ci-gate.sh` 從 AISDLC_SDD 根跑通過 exit 0，但開檔證實受測為 v0.01 | P2 | 框架程式缺陷 RFC + v0.0Y（ci-gate.sh `FW_DIR` 參數化／環境變數選版，或新增「演化版測試軌」使最新版納入官方閘門；與 DEF-02-001 rootdir 隔離合併評估） | fixed@improving_04（證據：使用者凍結方案「雙軌：凍結基線 v0.01 + 自動偵測最新演化版」。`AISDLC_SDD/scripts/ci-gate.sh` 改 `FROZEN_BASELINE=AISDLC_SDD_v0.01` 恆測 + `LATEST=$(ls -d AISDLC_SDD_v0.0*｜sort -V｜tail -1)` 自動偵測最高版〔現 v0.04〕，包成 `run_gate_for_version()` 對 `FW_VERSIONS` 迴圈；新增 `SDD_FW_VERSION` 單版覆寫 + `SDD_GATE_DRY_RUN` dry-run。腳本在 `scripts/`〔versioned 目錄外＝共享 CI infra，**免 Copy-on-Evolve**〕。新增 `scripts/tests/test_ci_gate_version_resolution.py` 4 case 鎖定解析意圖〔雙軌含 v0.01+最新／最新=磁碟最高版非寫死／覆寫收斂單版〕。驗證：`bash scripts/ci-gate.sh` exit 0「版本：AISDLC_SDD_v0.01 AISDLC_SDD_v0.04」雙軌各 pytest not-chaos 全綠 + arch_fitness exit<2；4 測試全綠；獨立審計 agent 突變測試〔LATEST 改空→test_dual_track/test_latest_is_highest 立即 2 failed〕證非假測試。零退化：AutoClaude 3069 passed/0 failed 持平、lint-imports 8 kept。見 `AutoSDD_improving_04.md` / `AutoSDD_ZeroTrust_Audit_04.md`） |
| DEF-07-001 | 2026-06-14 | improving_07 收尾 zero-trust 三鏡審查（SA-SD 鏡） | 流程摩擦（同一資訊雙處維護而 drift，與 DEF-05-002 同根）：實作期把 ci-gate 收斂格式由 `${VER}:${PASSED}passed` 微調為無後綴 `${VER}:${PASSED}`（取證可讀性），但**計畫載體 `improving_07.md` §2.2/§2.3/§3.3/RTM 的介面描述未同步**，仍寫帶 `passed` 後綴字樣（落地碼 `ci-gate.sh` 與 Defect_Log/Audit_07 皆已正確，僅計畫文件殘留）。無功能影響，收尾審查才抓到。根因：介面/輸出格式於實作期調整後缺「回掃計畫文件介面 delta 描述」檢核 | P3 | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）補檢核項「實作期調整介面/輸出格式後 grep 計畫文件同步介面描述」（與 DEF-05-002「重命名後 grep 引用點」合併為同一類「實作後回掃文件引用」紀律）；屬流程改善非框架程式缺陷 | fixed@improving_07（證據：improving_07.md §2.2(a):63 / §2.3(b):75 / §3.3 / RTM 已全部對齊為無後綴 `v0.01:1478 v0.04:1494`，與 `ci-gate.sh` GATE_SUMMARY 落地碼一致；同輪修復、無 partial 殘留） |
| DEF-06-001 | 2026-06-14 | improving_06 結案盤點 zero-trust 三鏡複核（QA 鏡，獨立審查 agent 親跑 ci-gate 取證時） | 取證友善性缺口（共享 CI infra `AISDLC_SDD/scripts/ci-gate.sh`）：雙軌閘門尾段僅印總結「✅ 全數通過（版本：v0.01 v0.04）+ CI_GATE_EXIT=0」，**未在收斂彙總行印出各軌 `N passed` 計數**——審查 agent 因輸出截斷無法在 tail 視窗獨立複核到 v0.01:1478 / v0.04:1494 逐軌數字（須主 agent 另跑 `tee+grep` 補證，見 ZeroTrust_Audit_06 §4.4 觀察）。硬閘判據（exit 0 + 雙版本名 + arch_fitness exit<2）不受影響，**非阻擋、非偽綠**，純屬零信任取證時需多一道撈取的摩擦 | P3 | 框架/CI 流程改善（下一輪 improving_07 按需）：`ci-gate.sh` 收斂時為每軌補印 `==> <version>: N passed`（或彙總成單行 `v0.01:1478 v0.04:1494`），使單次輸出即自證逐軌結果，免審計捲動截斷輸出 | fixed@improving_07（證據：(1) `AISDLC_SDD/scripts/ci-gate.sh` `run_gate_for_version()` 改 `python -m pytest … \| tee "$PYTEST_LOG"` 保留串流的同時擷取該軌 passed 數，新增逐軌 `==> [1/3] <VER>: N passed（not chaos）` + 全域 `GATE_SUMMARY` accumulator，迴圈後於收斂行下印 `逐軌計數：AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494`〔對齊本缺陷原建議格式〕，單次輸出即自證逐軌結果。(2) 擷取邏輯抽純函式 helper `AISDLC_SDD/scripts/pytest_passed_count.sh`〔讀 stdin→印整數，取最終 `N passed`、fail-soft 無匹配回 0〕，共享 infra 免 Copy-on-Evolve〔同 DEF-03-001/DEF-02-001 精神〕。(3) `scripts/tests/test_pytest_passed_count.py` 7 case 鎖意圖〔plain / **subtests 邊界 1478 非 14** / 多行取最終 summary / **多 `N passed` 匹配取最後（釘 tail -1 防退化）** / fail-soft 回 0 / 含 failed 仍取 passed〕，`scripts/tests/` 19 passed〔12 既有 + 7 新〕。(4) 修復前 `/tmp/cigate_before_07.log:50` 收斂行無逐軌計數 → 修復後實跑雙軌 exit 0 + 收斂行自證 `v0.01:1478 v0.04:1494`〔`/tmp/cigate_final_07.log`〕。`set -o pipefail` 保留 → 任一軌紅燈彙總前即中止，硬閘語意不變。零退化：AutoClaude 3069 passed/0 failed 持平、lint-imports 8 kept；2026-06-14 improving_08 階段一複驗無回歸：ci-gate 收斂段自證印出「逐軌計數：AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494」〔`/tmp/cigate_08.log` 尾 3 行〕，取證友善性修復穩定生效；2026-06-14 improving_09 階段一複驗無回歸：ci-gate 收斂段自證逐軌計數行「AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.04:1494」〔`/tmp/cigate_09.log` 尾 3 行〕正常顯示，修復穩定生效） |
| DEF-10-001 | 2026-06-14 | improving_10 階段一 zero-trust 重偵察（為關閉 DEF-01-007 而盤點 A/B 關閉路徑可執行性） | A/B 關閉路徑**不可執行**（兩缺口）：**(a)** §5.2 / `tools/integration_gate.ps1:65-76` [5/5] 引用的 A/B 載具 `sdd_bridge_smoke.yaml` **全倉不存在**（`find . -iname "sdd_bridge_smoke*.yaml"` 零命中，bridge 僅有 pytest `tests/integration/test_sdd_bridge/test_bridge_smoke.py`）→ 即使裝 cc-switch，文件化指令 `autoclaude sdd_bridge_smoke.yaml --fresh` 必 `❌ 找不到 Playbook 檔案`（`main.py:42` SystemExit）。**(b)** `integration_gate.ps1:66` 用 `Get-Command cc-switch` 假設 CLI 在 PATH，但最主流 [farion1231/cc-switch](https://github.com/farion1231/cc-switch) 為 **Tauri GUI 桌面 app 不上 PATH**（使用者 2026-06-14 實裝後 `command -v cc-switch`=NOT FOUND 實證）→ 該閘永遠偵測不到；headless 自動 A/B 需 **CLI 變體**（[SaladDay/cc-switch-cli](https://github.com/saladday/cc-switch-cli)） | P3（可執行性/取證缺口，非阻擋；A 軌主鏈早於 improving_06 結案、`[5/5]` SKIP 非偽綠） | 整合層（AutoClaude 側）即修——共享 infra（`tools/integration_gate.ps1` + `AutoClaude/scripts/` 載具），**免 v0.05** | fixed@improving_10（證據：**(a)** 建 `AutoClaude/scripts/sdd_bridge_smoke.yaml`——自包含 2 步 `workflow_type=aisdlc_sdd` TDD 載具〔S01 先測 / S02 實作 + evaluator `pytest smoke_add_test.py -q`〕，`_validate_playbook_format`+`Playbook.model_validate` 載入 OK；`tests/integration/test_yaml_import.py` glob `scripts/*.yaml` 自動納入 → 新增 **6 參數化 case 全綠**〔T1 parse/T2 sha256/T3 detect=playbook/T4 process_single success/T5 roundtrip/T4+ build_diffs〕。**(b)** `integration_gate.ps1` [5/5] 改 `foreach($name in @("cc-switch","cc-switch-cli","ccs"))` 多 CLI 名偵測 + `Test-Path` 驗載具存在 + SKIP/PASS 訊息釐清「farion1231 為 GUI 不上 PATH、headless 需 CLI 變體」並給含正確載具路徑之 A/B 指令。`integration_gate.ps1 -SkipFull` 實跑 **exit 0「2 PASS / 1 SKIP」**，[5/5] 新訊息自證解析到 `scripts/sdd_bridge_smoke.yaml`〔已存在、無「載具缺失」警示〕。零退化：零 python 變更，full pytest 3069→**3075**〔+6 新載具參數化，0 failed〕、lint 8/0、LOC 0） |
| DEF-10-002 | 2026-06-14 | improving_10 收尾獨立 zero-trust 審查（SA-SD/QA 鏡，OVERALL PASS 後提出之流程/方法論建議） | AISDLC_SDD 開發流程兩項結構性改善（非本輪技術缺陷，本輪改動面零回歸全 PASS）：**(a) 外部工具依賴 invocation 形態未於規劃期偵察**——improving_01 §5.2 規劃 cc-switch A/B 驗收時假設其為 PATH 上 CLI，直到 improving_10 才發現主流版（farion1231）為 Tauri GUI 不上 PATH，導致 DEF-01-007 拖延 9 輪。**(b) Copy-on-Evolve「最新演化版反而無自動化閘門」屬結構性風險**——DEF-03-001 已於 improving_04 以雙軌 ci-gate 點修，但「演化版必納官方閘門」尚未上升為治理通則（fitness function），僅靠單輪缺陷修復 | P3（流程/方法論改善，非阻擋、非框架程式缺陷） | 下一輪 improving_11（按需）：**(a)** 迭代範本補檢核項；**(b)** 治理層固化 fitness function | **fixed@improving_11**（🔴 人工確認 scope「(a)+(b) 全做」）。**(a) fixed@範本v3**：`docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 階段一新增檢核項 (f)「外部工具依賴須先確認 invocation 形態（GUI app／PATH CLI／API），勿假設可 headless 自動化」（含 cc-switch 致 DEF-01-007 跨 9 輪實例），為 DEF-05-002/DEF-07-001「實作後回掃」系列首個落入範本之常設階段一檢核。**(b) fixed@v0.05**：Copy-on-Evolve v0.04→v0.05，`tools/arch_fitness/arch_fitness.py` 新增 **FF-17「演化版必納官方閘門」** 結構守門——靜態讀 `scripts/ci-gate.sh` 斷言四錨點動態最新版偵測（`ls -d AISDLC_SDD_v0.0*`／`sort -V`／`tail -1`／`FW_VERSIONS+=…LATEST`），退回靜態寫死＝structural fail（DEF-03-001 回歸防護），把點修固化為結構不變量；`test_arch_fitness.py` 5 新測試（真 repo PASS／合成雙軌 PASS／寫死單版 fail／漏 append fail／腳本缺 INFO）。**設計決策**：不另開 R-9.x（會連鎖 FF-8/10/12 且屬自演化 meta-loop 異類關注點），arch_fitness 本即治理 fitness-function 套件（Rule 2/3）。驗證：v0.05 1499 passed、arch_fitness 87 passed、雙軌 ci-gate exit 0「v0.01:1478 v0.05:1499」自證 FF-17（最新版 v0.05 自動入閘）。無 `_HAPPY_PATH`/`*.tla` 變更（Rule 9.18.1 不啟動） |
| DEF-11-001 | 2026-06-15 | improving_11 W-11-2 執行 Copy-on-Evolve v0.04→v0.05 時（缺口由收尾 zero-trust 複審揭露：runtime 產物將隨 commit 入庫，且初審潔淨度查證僅憑 .pyc） | 流程摩擦（Copy-on-Evolve 操作）：框架無官方 copy helper／SOP 步驟排除 runtime 產物——`cp -r` 整碗複製會夾帶 (1) `__pycache__/`／`*.pyc`（已 gitignored，無入庫風險）、(2) **`build/reports/` runtime 取證輸出 173 檔**（abort/drift/fsm/test-analysis…，部分 v0.04 逐字 stale 複製、部分 v0.05 dogfooding 重生）、(3) **根 `arch-fitness.json`**（每次 ci-gate/arch_fitness 重生）。(2)(3) 此前**未被任何 .gitignore 涵蓋**（既有規則僅選擇性排除 v0.01）→ `git add -A -n AISDLC_SDD/AISDLC_SDD_v0.05/` would-add **1013** 檔含此二類「輸出非輸入」runtime 產物，將污染 Copy-on-Evolve commit（潛在誤導 + 體積膨脹）。非阻擋（排除後零影響，雙軌 1499 不變），純潔淨度摩擦 | P3（流程摩擦，非框架程式缺陷、非阻擋） | **即清理子項**：本輪即以 .gitignore 排除 v0.05 `build/reports/` + `arch-fitness.json` + `chaos-report.json`。**系統性子項（routed v0.0Y）**：提供通用 `scripts/copy_on_evolve.sh <from> <to>` helper（rsync `--exclude __pycache__ --exclude '*.pyc' --exclude build/reports --exclude arch-fitness.json`），或在 `AISDLC_SDD_UPGRADE_SOP.md` 補「複製後／commit 前排除 runtime 產物」SOP 步驟；與 DEF-02-001/DEF-03-001 同屬 Copy-on-Evolve 版本治理家族 | **fixed@improving_11（即清理子項）/ routed v0.0Y（通用 helper+SOP 子項）**（證據：複審修復——於既有 `AISDLC_SDD/.gitignore` v0.01 區塊後新增 v0.05 區塊排除 `AISDLC_SDD_v0.05/build/reports/`+`arch-fitness.json`+`chaos-report.json`；`git add -A -n AISDLC_SDD/AISDLC_SDD_v0.05/` **1013→839**〔排除 174 = 173 build/reports + 1 arch-fitness.json〕，build/reports 命中 0、arch-fitness.json 0；真源碼保留：build/planning 52、build/logs/README.md、EVOLUTION_LOG/CHANGELOG/73 tests/FF-17 bridge 仍在。零退化：`bash scripts/ci-gate.sh` exit 0、雙軌「v0.01:1478 v0.05:1499」不變，跑後 runtime 產物重生仍被 gitignore〔would-add 維持 839〕——證實排除「輸出」不破壞測試收斂。詳見 `AutoSDD_ZeroTrust_Audit_11.md` §6。修正初稿「本輪已手動清理 v0.05 無殘留」之不精準字樣：實採 .gitignore 排除〔非 git rm／非手動 rm〕，且初稿僅憑 .pyc 的潔淨宣稱不完整，已補全為 .pyc+build/reports+arch-fitness.json 三類查證）。**v0.0Y 通用 helper 子項 fixed@improving_12（W-12-2）**：建 `AISDLC_SDD/scripts/copy_on_evolve.sh`（**共享 infra、免 Copy-on-Evolve**——Rule 7 修正本欄「routed v0.0Y」粗略標記，理由見 improving_12.md §1：家族前例〔ci-gate/conftest/pytest_passed_count/cross_version_guard〕一致、避免為改 2 行 SOP 整碗複製反觸本缺陷、routing「或」字單 helper 即足）。`tar --exclude` 串流複製排除 `__pycache__`/`*.pyc`/`*.pyo`+`build/reports`/`arch-fitness.json`/`chaos-report.json`（rsync 在 Win Git Bash 不可用故採 GNU tar）；`scripts/tests/test_copy_on_evolve.py` 5 case（排除 runtime∧保留源碼/拒覆蓋/來源不存在/參數錯）。驗證：scripts/tests 24 passed、真實 v0.05 實跑 build/reports+arch-fitness.json 排除而 build/planning+FF-17 源碼保留、突變移除 build/reports 排除即 test_excludes 轉紅（非假測試）。**SOP §2.1/§5（`cp -r`+`git add .` 根因）引用 helper 之餘項**留待下一次真實 v0.06 演化於可編輯副本同步（routing 餘項，非阻擋）。零退化：AutoClaude 3075 持平、ci-gate exit 0 |
| DEF-11-002 | 2026-06-15 | improving_11 結案複核（主 agent 派 zero-trust 複審 agent 揭露：Audit_11 初審把 commit 潔淨度查證窄化到 .pyc） | 審查方法論缺口（與 DEF-11-001 cp -r 操作缺口同題不同根）：zero-trust 審查「Copy-on-Evolve 無 stale 入 commit」之驗證僅憑 `git check-ignore *.pyc` + `git status` 數 pyc，**未跑全量 `git add -A -n` dry-run 審 would-add 清單**，致 227 個 build/reports + arch-fitness.json runtime 產物漏審、初審誤判 OVERALL PASS（複審以 `git add -n` dry-run 1013 檔當場揭露）。根因：審查紀律缺「新凍結版本/大批 untracked 入庫前，須以 `git add -A -n` 全量 dry-run 審查 would-add 清單有無 runtime/stale 產物」之常設檢核 | P3（流程/方法論改善，非阻擋、非框架程式缺陷） | 下一輪迭代範本（`AutoSDD_Iteration_Prompt_Template.md`）階段四/結案前補檢核項「Copy-on-Evolve 或大批新檔入庫前跑 `git add -A -n <path>` 全量 dry-run，審 would-add 無 runtime/stale 產物（非僅 .pyc）」；與 DEF-05-002/DEF-07-001「實作後回掃」、DEF-11-001 同屬潔淨度/誠實性紀律家族 | **fixed@範本v4（improving_12 W-12-1）**（證據：`AutoSDD_Iteration_Prompt_Template.md`「🔍 多專家 Zero-Trust 審查閉環」step 1 新增 note「Copy-on-Evolve / 大批新檔入庫潔淨度（DEF-11-002 紀律）」:164-170——審查涉新凍結版本或大批 untracked 入庫時**必跑 `git add -A -n <path>` 全量 dry-run** 審 would-add 無 runtime/stale，不可僅憑 .pyc 宣稱潔淨〔含 Audit_11 初審窄化 .pyc 漏審 227 build/reports 致誤判 OVERALL PASS、複審 dry-run 1013 檔當場揭露之實例〕，仿「並行派發隔離 #11」note 格式，與階段一 (f)、DEF-05-002/DEF-07-001 同列紀律家族。grep 證實落地） |
| DEF-12-001 | 2026-06-15 | improving_12 W-12-2 實作期 B 軌 dogfooding（為驗 copy_on_evolve 測試是否被閘門執行而查 CI 範圍） | CI gating 缺口：`.github/workflows/aisdlc-sdd-ci.yml:46`（現名，已上移根層）僅 `bash scripts/ci-gate.sh`，而 `ci-gate.sh:72` 各版只跑 `python -m pytest tools/fsm_runtime/tests/`——**`scripts/tests/`（共享 CI infra 回歸鎖：ci-gate 版本解析 test_ci_gate_version_resolution / pytest 計數 test_pytest_passed_count / 跨版 guard test_cross_version_guard / 本輪 test_copy_on_evolve）未被 ci-gate.sh 或任何 CI workflow 執行**（`grep scripts/tests .github/workflows/`=無）→ 這些「退化即紅」保護從未被實際強制。直接影響 W-12-2：若不修，test_copy_on_evolve.py 被任何 gate 都不跑＝Rule 9「不會被執行的測試＝無效」 | P3（CI 腳本缺口，非阻擋；但使 4 個共享 infra 測試檔的回歸保護失效） | 整合層/共享 CI infra 即修（`ci-gate.sh` 補 scripts/tests 軌，versioned 目錄外免 Copy-on-Evolve）——判為**完成 W-12-2 測試強制性之必要部分，非新增 scope** | **fixed@improving_12**（證據：`AISDLC_SDD/scripts/ci-gate.sh` 版本迴圈後新增「共享 infra scripts/tests/」軌——`cd REPO_ROOT && python -m pytest scripts/tests/ -q` 跑一次〔版本無關〕，`set -o pipefail`+`set -e` 任一紅燈即中止〔硬閘語意一致〕，`GATE_SUMMARY+=scripts/tests:N` 自證。`bash scripts/ci-gate.sh` exit 0 收斂行「AISDLC_SDD_v0.01:1478 AISDLC_SDD_v0.05:1499 **scripts/tests:24**」；FF-17 四錨點未動複驗 structural-pass〔fail=0〕；test_ci_gate_version_resolution 用 SDD_GATE_DRY_RUN 早退〔行 44 exit 0〕無遞迴。零退化：AutoClaude 3075 持平、lint 8/0） |
| DEF-12-002 | 2026-06-15 | improving_12 實作期（跑單一 `pytest scripts/tests/x.py::test_y` 取突變證據時觸發） | hook 誤攔：`AISDLC_SDD/scripts/cross_version_guard.py:44` `_is_path_arg` 用 `os.path.exists(token)` 判路徑，但 `pytest 檔案.py::test_y`（從 REPO_ROOT 跑單一測試的 nodeid）token 含 `::` → `os.path.exists("檔案.py::test_y")`=False ∧ 無版本片段 → 誤判「無路徑 arg」→ 走 bare 分支 `_versions_under_dir(cwd)` 展開全部 v0.01..v0.05 → 誤報「跨版 pytest 偵測」UsageError。實為單一非版本路徑、不該 fire。非阻擋（workaround：用目錄路徑或 `-k <名>`；官方 ci-gate.sh 用目錄路徑 `scripts/tests/` 不受影響） | P3（hook 誤攔/取證友善性，非阻擋、非阻塞主鏈） | 下一輪/v0.0Y 共享 infra 小修：`cross_version_guard._is_path_arg` 在 `os.path.exists` 前剝除 `::` 後綴（`token.split("::",1)[0]`）再判存在；補 `test_cross_version_guard` nodeid case。守本輪 scope 故即記不修 | **fixed@improving_22**（shared infra `AISDLC_SDD/scripts/cross_version_guard.py`，免 Copy-on-Evolve；證據：`_is_path_arg` 改 `path_part = token.split("::",1)[0]` 後再 `os.path.exists`；`scripts/tests/test_cross_version_guard.py` 新增 `test_nodeid_with_colons_not_false_positive`〔`smoke.py::test_x` → 空集不 fire〕+ `test_versioned_nodeid_still_detects_version`〔`v0.12/x.py::test_y` → 仍正確偵測 v0.12〕；scripts/tests 25→27 passed。**dogfooding 衍生 DEF-22-001**：寫第二 case 時揭露 VERSION_RE 對 v0.10+ 失效，同輪併修） |
| DEF-13-001 | 2026-06-15 | improving_13 前一輪 zero-trust 稽核定案（nightly stage / 排程定義 split-brain） | nightly 採集 split-brain 指錯 repo：Windows 排程工作 `AutoClaude_Nightly` 的 Action `-File` 路徑指向 monorepo 化前的孤立舊副本 `d:\CursorProject\AutoClaude\tools\run_local_nightly.ps1`（修前 `Get-ScheduledTask` 取證 Arguments 確認），致 2026-06-13 移除巢狀 .git 改 monorepo 後 nightly 一直寫進舊 repo。**證據**：舊副本 `d:\CursorProject\AutoClaude\.ac4_history.jsonl`／`.mutation_history.jsonl`／`.observability_history.jsonl` 於 06-13/14/15 02:04~02:05 持續更新；monorepo `AISDLC_SDD_Agent\AutoClaude\.ac4_history.jsonl` 末筆凍結於 `2026-06-12T20:05:41+00:00`（UTC，+0800=06-13 04:05），自此 monorepo 觀察期 #2/#3 斷檔未進帳 | P1 | 整合層/環境即修（修正排程 Action 路徑，免程式變更） | fixed@improving_13（證據：提權 PowerShell `New-ScheduledTaskAction -Execute powershell.exe -Argument '-NoProfile -ExecutionPolicy Bypass -File d:\CursorProject\AISDCL_Agent\AutoClaude\tools\run_local_nightly.ps1' -WorkingDirectory 'd:\CursorProject\AISDCL_Agent\AutoClaude'` + `Set-ScheduledTask`；非提權 session 獨立複驗 Action 已改 monorepo 路徑、WorkingDirectory=`...\AISDCL_Agent\AutoClaude`，觸發器〔StartBoundary 2026-05-20T02:00、DaysInterval=1〕／帳號〔wuwei/S4U〕／NextRunTime〔06/16 02:00〕全部不變；未手動觸發整輪 nightly〔mutation/Docker/perf 重且污染採樣〕，僅驗排程定義已正確指向 monorepo，自今晚 02:00 起從現碼採集） |
| DEF-13-002 | 2026-06-15 | improving_13 前一輪 zero-trust 稽核定案（split-brain 修復決策：觀察期斷檔處置） | monorepo 觀察期 #2/#3 因 DEF-13-001 split-brain 於 06-13~06-15 斷檔。**明確決策＝不 backfill**：舊副本在 branch `sprint/sd_09_phase9`，程式碼已與 monorepo main 分岔；把舊碼採集的 06-13~06-15 觀察 jsonl 注入 monorepo＝以「舊碼數據」充當「monorepo 現碼穩定證據」，違反 Nightly 取證紀律 #1（rc/數據須反映現碼真實執行）與 #3（PASS 須引現碼 RunId）。故**不複製／不覆寫／不注入任何 jsonl**；舊副本資料保留原處 | P1 | 修復決策（不 backfill）；排程修正後自今晚起從現碼重新連續採集，達標日順延 | **resolved@2026-06-15（PM 拍板）**：① backfill 決策＝**確認不 backfill、從現碼重新採集**（守取證紀律 #1/#3）；② 孤立舊副本 `d:\CursorProject\AutoClaude` 已**歸檔重新命名**為 `d:\CursorProject\AutoClaude_ARCHIVED_pre_monorepo_20260615`（杜絕未來再 split-brain；修前掃描確認無其他 schtasks 引用舊路徑；歸檔後複驗排程仍指 monorepo 不受影響）。觀察期 #2/#3 達標日順延、待現碼重新連續累積（追蹤點，非 open 缺陷） |
| DEF-13-003 | 2026-06-15 | improving_13 前一輪 zero-trust 稽核定案（範本 XAI/北極星段可讀性審查） | 範本 `docs/04_planning/AutoSDD_Iteration_Prompt_Template.md` 本輪新增「🌟 北極星」「🔭 XAI Turn」兩段後三項可讀性缺陷：**DOC-01** 北極星段用 L5/L10、`meta⁸`、「圖靈完備閉環」「具身接地」等成熟度術語卻缺分級定義出處；**DOC-02** XAI 段把具體 driver instance `ACT-159 / R-9.37` 硬寫進不變範本本體，下輪換 instance 易漏改；**DOC-03** XAI 段「三（四）源絕對一致」括號含糊，第四源未明 | P2 | 規格/文檔缺陷（範本即修，monorepo 整合層文件非框架凍結本體，免 Copy-on-Evolve） | fixed@improving_13（證據：**DOC-01** 北極星段末新增一行「成熟度分級判準出處：L5↔L10／meta 階數以當輪 active planning `_NN.md` 的 maturity ladder 為準，禁各輪自行詮釋」；**DOC-02** 標題與輸出行的 `ACT-159 / R-9.37` 改 `{{本輪 driver instance（範例：ACT-159 / R-9.37）}}` 佔位符化（保留 ACT-159/R-9.37 為括號內範例），底部設計說明表對應列同步改「以 `{{本輪 driver instance}}` 佔位，當前範例＝ACT-159/R-9.37」；**DOC-03** 改寫為明確四源清單「(1) TLA+ 證明邏輯、(2) Python 執行邏輯、(3) 儀表板渲染邏輯、(4) `FSM-STATE.yaml` 狀態真相」；`git diff` 自檢三處未動其他段落、未破壞編號/表格/zero-trust 收斂語句）。**2026-06-15 後續補強**：DOC-01 原指向的「active planning maturity ladder」實際未被定義（潛在缺口），已起草正式 SSOT `docs/04_planning/AutoSDD_Maturity_Rubric.md`（bespoke、錨定 SAE/CMMI/AGI；同一把 L0–10 尺量 C 引擎/B 流程/A 協作三軸，上捲 `L_合體=min(A,B,C)`），範本判準出處改指該檔；揭露實測位粗估 L3–L4≠宣稱 L5 |
| DEF-15-001 | 2026-06-15 | improving_15 W-15-1 B 軌 dogfooding（首次真實 v0.06 Copy-on-Evolve 後跑 FSM 測試時當場揭露） | Copy-on-Evolve helper 過度排除 + 跨版 gitignore 潛在缺陷（兩面同根）：**(面1 helper)** `scripts/copy_on_evolve.sh` 的 `tar --exclude './build/reports'` 整樹排除，誤殺 `build/reports/fsm/FSM-STATE-TEMPLATE.yaml`——它是 `state_loader._load_template()`（`tools/fsm_runtime/state_loader.py:247` `TEMPLATE_PATH.open`）必需的 **FSM 種子模板＝真輸入、非運行時輸出**，漏帶致演化版整個 FSM runtime 無法 bootstrap（FileNotFoundError，`pytest test_phase_h/test_transitions/test_auto_recovery` **46+ 全紅**）。**(面2 gitignore 潛在缺陷)** 進一步查 `git ls-files`：v0.01/v0.04 模板 **tracked**，但 **v0.05 模板 untracked（被 ignore）**——improving_11 DEF-11-001 加 `AISDLC_SDD_v0.05/build/reports/` 整樹 ignore 時**未比照 v0.01:57 `!…FSM-STATE-TEMPLATE.yaml` negate**，致 v0.05 模板自此 git-untracked＝**fresh checkout / GitHub CI 跑 v0.05/v0.06 會缺模板而 FSM 無法 bootstrap 的潛在缺陷**（本地一直靠物理檔過關未顯露）。fresh-checkout 模擬（只 tracked 檔 + 模板）**1507 passed**〔唯 1 FF-17 失敗為 temp-dir 隔離 artifact〕證模板為唯一 build/reports 源碼依賴 | P2（潛在 fresh-checkout/CI 破壞；本地非阻擋但 fresh checkout 阻擋；與 DEF-11-001/12-002 同屬 Copy-on-Evolve 版本治理家族） | 共享 infra 即修（copy_on_evolve.sh / .gitignore，versioned 目錄外免 Copy-on-Evolve）——判為完成 W-15-1 v0.06 演化之必要部分，非新 scope | **fixed@improving_15（三層修復）**（證據：**(1) helper 物理保留**：`scripts/copy_on_evolve.sh` `tar` 後新增「排除後補回模板」步驟〔來源存在時 `cp $FROM/build/reports/fsm/FSM-STATE-TEMPLATE.yaml $TO/...`〕。**(2) .gitignore negate 追蹤**：比照 v0.01 逐層 re-include idiom〔`build/reports/*` + `!…/fsm/` + `…/fsm/*` + `!…/FSM-STATE-TEMPLATE.yaml`，因 git 無法在父目錄已排除時直接 negate 子檔〕，**同時補 v0.05 + v0.06**——`git check-ignore` 驗模板「已可追蹤」、abort 等 runtime 檔「仍 ignored」；v0.05 模板 would-add 補追蹤〔修潛在缺陷〕。**(3) 測試鎖**：`scripts/tests/test_copy_on_evolve.py` `_make_source` 加 FSM-STATE-TEMPLATE.yaml + 改既有斷言〔runtime FSM-STATE-x 仍排除〕+ 新 `test_preserves_fsm_state_template`〔退化即紅〕，scripts/tests 24→**25 passed**。**深層 routed**：模板寄居 runtime 目錄 build/reports/ 本身是結構異味，理想應移至 tracked 源碼位〔如 tools/fsm_runtime/templates/〕，惟需改 state_loader.TEMPLATE_PATH 並動凍結版＝較大重構，**routed 未來輪/真實大版重構**，非本輪 scope。零退化：v0.06 1508 passed、ci-gate 雙軌 exit 0）。**深層 fixed@improving_22（v0.13）**：🔴 人工 signoff 選「DEF-12-002 + DEF-15-001 深層重構」。Copy-on-Evolve v0.12→v0.13，模板 `FSM-STATE-TEMPLATE.yaml`（真輸入）由 `build/reports/fsm/` 移至與 loader 同層的 tracked 源碼位 `tools/fsm_runtime/templates/`；`state_loader.TEMPLATE_PATH` 改 `Path(__file__).resolve().parent/"templates"/...`、docstring 同步、`DEFAULT_STATE_DIR`（runtime 輸出位 build/reports/fsm）不變＝**輸入/輸出分離**。同步 3 處文件連結（SDD_FSM_ENGINE.md/UPGRADE_SOP.md/test-failure-analyzer SKILL.md）；`.gitignore` v0.13 區塊 build/reports/ **整樹排除無需 negate idiom**（消除 DEF-11-001/15-001 反覆打補丁的結構異味根因），`copy_on_evolve.sh` 補回模板特例對 v0.13+ 自然成 no-op（保留供 ≤v0.12 相容）；`tools/fsm_runtime/tests/test_template_location.py` 3 case 回歸鎖（TEMPLATE_PATH 在 templates 非 build/reports / 模板可載入非空 / DEFAULT_STATE_DIR 仍 build/reports/fsm）。`save_state` `target.parent.mkdir(parents=True,exist_ok=True)` 證 fresh checkout 無 build/reports/fsm 也按需建、不破 bootstrap。零退化：v0.13 1580 passed（1577+3）、ci-gate 雙軌 exit 0 v0.01:1478/v0.13:1580/scripts:27、TLC 6 檔逐位元零差異免五軌、潔淨度 853 無 runtime 殘留。結構異味根因消除（深層 fully addressed）） |
| DEF-13-004 | 2026-06-15 | improving_13 階段一三軸成熟度實測（C 引擎自治評級，zero-trust Explore 親跑） | C 軸 L5 一致性缺口：成熟度量表 L5 要求自演化「範圍·預算·終止由硬閘＋**人工 signoff** 守界」。Gap-012-D 自動重載（`AutoClaude/autoclaude/execution/playbook_runner.py:351`）已具預算硬閘（`max_evolutions` 預設 3）+ 終止（耗盡即 escalation），但**演化版直接 auto-reload 無人工 signoff**（`playbook_runner.py:368` 直接 `continue`），與 `goal_decomposer.py:138-145` 之 🔴 signoff 硬閘不對稱 → C 軸卡在 L4→L5 邊界（萌 L5 但 signoff 守界不一致）。屬成熟度一致性缺口，非功能 bug | P3（成熟度一致性/治理守界，非阻擋；預設行為已有預算/終止硬閘） | 本輪即修（W-13-2，flag-gated signoff 硬閘，預設 off 零退化）—— 🔴 人工 scope 選「兩項都做」 | **fixed@improving_13（W-13-2）**（證據：`PlaybookConfig.require_evolution_signoff: bool=False`〔`config.py:54-60`〕＝預設維持自動重載零退化；為 True 時 `playbook_runner.py` run() 重載 gate 前插 `_evolution_signoff_granted(count, path)` helper〔fail-closed：approver 缺失/拒絕/例外一律 deny 停機不重載+審計痕〕，對齊 goal_decomposer signoff + `enable_kernel_brain` flag-gate 雙前例；`tests/test_def_13_004_evolution_signoff.py` 9 case〔flag-off 自動重載/無 approver fail-closed/拒絕阻重載/核可放行/例外 fail-closed + helper 四分支〕全綠，既有 `test_gap012` 35 重載測試零退化。facade 437<450 行、LOC violations=0、lint 8/0。零五軌 TLC 觸發〔無 .tla/_HAPPY_PATH 變更〕） |
| DEF-17-001 | 2026-06-16 | improving_17 W-17-1 B 軌 dogfooding（接入鷹架代謝 GC、驗 flag ON 端到端時揭露） | 代謝閉環「半接」結構摩擦：W-17-1 把 `scaffold_gc.run_gc()` 接入 `enter_scaffold_gc` 主迴圈後，flag ON 確實自動產 SCAFFOLD-ROI 報告，**但 `compute_proposals()` 依 `rule_loader.propose_graduation()` 需 `fire_count ≥ GRADUATION_MIN_FIRES ∧ catch_count==0` 才提議**（`scaffold_gc.py:72-73`/`rule_loader.py:150-159`）。FF-9 實測 39 條 active 規則 aggregate `fire_count=0`（`record_fire` 在 FSM 主迴圈零自動呼叫——僅測試觸發），故**即使 flag ON，生產端 GC 恆產零提議**＝GAP-X2「代謝肌肉從未收縮」**未真正閉合**：接 `run_gc` 是必要但不充分，尚需把 `record_fire(rule_id, caught=…)` 遙測接入「規則實際 fire/catch」的 FSM 執行點，ROI 帳本才有非零資料驅動退役提議。非阻擋（W-17-1 機制正確、報告真實落盤、紅線守界皆達成；零資料零提議是誠實的下游依賴而非 bug） | P3（成熟度閉環一致性/遙測接線缺口，非阻擋；與 B 軌「機制齊備但遙測未接」家族同根） | routed 未來輪 B 軌：把 `record_fire` 規則命中記帳接入 FSM 規則執行/守門點（`rule_loader.load_for_state` 命中後記帳），使 scaffold_roi 累積真實 runtime 資料；屬獨立 W 項（遙測接線），非本輪 scope（本輪聚焦「GC 提議產出鏈」接入＝代謝收縮的決策側） | **routed（fire 側 fixed@improving_18 / catch 側 routed → DEF-18-001）**（即記即分流；本輪 W-17-1 收縮「決策側」已閉合並測試覆蓋。**2026-06-16 improving_18 W-18-1/W-18-2 閉合「遙測 fire 側」**：`record_state_fires` 接入 `transition()` 主迴圈，flag ON 時 `fire_count` 真實累積〔`test_rule_fire_telemetry_wiring.py::test_flag_on_fire_count_accumulates_persisted` 驗 3 次轉態累積至 3〕，GC 已有非零資料可驅動提議＝DEF-17-001 點名的「`fire_count=0` 根因」已閉合。**殘留 catch 側**〔`record_fire(caught=True)` 何時觸發之語意框架未定義〕轉記為獨立 **DEF-18-001**。**2026-06-25 improving_62（v0.24）：fire 側遙測翻預設 ON 活體化**——`_rule_fire_telemetry_enabled()` unset→True（鏡像 AUTO_RECOVERY/SLV 翻環），fire_count 由 opt-in 升為生產常態累積，DEF-17-001 點名根因「生產端 fire_count 恆 0」**實質閉合**（掌舵者 signoff「B 軸 telemetry 翻 ON」；保留顯式 opt-out=0；conftest autouse 隔離護凍結 governance）） |
| DEF-16-001 | 2026-06-15 | improving_16 W-16-1 B 軌 dogfooding（接入 SLV 自動提議、讀 learn 採納鏈時揭露） | FSM 採納鏈結構性斷裂：`exit_production_behavioral_signal("learn")`（`tools/fsm_runtime/fsm_runtime.py:2204`）轉 LEARNING_COMMIT 但**不填 `learning_commit_tracking`**（不同於 `enter_learning_commit` 會填 fpl_id/proposed_slv_id/proposed_rule_path）；致經 learn 路徑進 LEARNING_COMMIT 後呼叫 `exit_learning_commit("approved")` 會因 `proposed_rule_path` 未設在 `fsm_runtime.py:874-878` raise ValueError「no rule YAML to audit」＝learn→approve 鏈本身走不通。非阻擋（v0.06 本即如此、且該鏈在無 auto-draft 時罕用），純 dogfooding 揭露之結構摩擦 | P2（潛在採納鏈斷裂；flag OFF 時與 v0.06 一致非退化；同 B 軌規則自演化鏈家族） | 框架程式即修：W-16-1 接入 auto-draft 時於 learn 分支順帶填 tracking（判為完成 W-16-1 必要部分，非新 scope）；versioned 目錄內 → 隨 v0.07 Copy-on-Evolve | **fixed@improving_16（v0.07）**（證據：`AISDLC_SDD_v0.07/tools/fsm_runtime/fsm_runtime.py` learn 分支 flag ON 時填 `learning_commit_tracking`{fpl_id,proposed_slv_id,proposed_rule_path,review_status:pending,origin:auto}；`test_slv_auto_propose_wiring.py::test_learn_to_approved_chain_closes_after_human_verify` 驗 learn→人 verify→approve 鏈閉合到 RELEASE。flag OFF 時行為同 v0.06〔斷裂留存且對齊 v0.06，零退化〕。v0.07 1517 passed、ci-gate 雙軌 exit 0 v0.01:1478/v0.07:1517/scripts/tests:25） |
| DEF-18-001 | 2026-06-16 | improving_18 W-18-1 B 軌 dogfooding（閉合 fire 側遙測時，揭露 catch 側語意缺口） | catch 側遙測語意未定義（與 DEF-17-001 同根之殘留面）：W-18-1 把 on-watch fire 記帳接入 `transition()` 主迴圈後 `fire_count` 真實累積，但 `record_state_fires` 預設 `caught=False`——`record_fire(rule_id, caught=True)` **何時應被觸發**（規則「實際攔到問題」之 FSM 執行點）框架從未定義（偵察證據：`rule_loader.record_fire` docstring 僅「caught=True 代表真攔到問題」、無觸發契約；FSM gate FAIL/MONITOR_VIOLATION/ESCALATION 路徑皆無 rule_id→catch 的乾淨映射）。後果：`catch_count` 恆 0 → `propose_graduation`（`rule_loader.py:158` fires≥1000 ∧ catches==0）會把**所有**達門檻規則一律提議退役（含實有價值者），ROI 為單側信號。**本輪刻意不接 catch 側**：擅自映射（如「每次 gate fail 給所有 on-watch 規則記 catch」）會污染 ROI 信號、比不做更糟（Rule 1 不臆測、Rule 2 簡單優先）。非阻擋（fire 側機制正確、紅線守界——退役仍 🔴 人工 set_maturity，stats 證書誠實標 `catch_side_wired=False`；flag 預設 OFF＝生產零影響） | P3（成熟度閉環一致性/遙測 catch 側語意缺口，非阻擋；與 B 軌「機制齊備但遙測未接」家族同根） | routed 未來輪 B 軌：**先定義「規則 catch（實際攔到問題）」之 FSM 觸發契約**（哪個執行點、如何映射 rule_id），再把 `record_state_fires(caught=True)` / `record_fire(caught=True)` 接入該點，使 ROI 為雙側信號；**生產啟用退役（flag ON + 達門檻自動提議消費）前須先閉合此項**，否則退役提議偏頗。屬獨立 W 項（catch 語意契約 + 接線），非本輪 scope（本輪聚焦 fire 側累積） | **fixed@v0.10（catch 側契約定義並接入；覆蓋面殘留 → DEF-19-001）**（2026-06-16 improving_19 W-19-1/2/3 閉合：定義 catch **三要件契約**〔規則自描述 `failure_mode` ∧ 攔截事件真實發生 ∧ 呼叫端結構化歸因 rule_id，**顯式可歸因、非時序鄰近猜測**——正面回應本缺陷「擅自映射比不做更糟」的警告〕；`rule_loader.record_state_catches` + `FSMRuntime._record_escalation_catches` 接入**兩個 `record_escalation` 呼叫點**〔gate retry 耗盡→R-9.1、monitor 破壞→R-9.21〕；`rule_fire_telemetry_stats` 翻 `catch_side_wired=True`、ROI 成雙側信號；`test_rule_catch_telemetry_wiring.py` 11 case 覆蓋〔含持久化陷阱回歸鎖、monitor violation 整合真接〕。**殘留覆蓋面**：catch 歸因目前僅 2/39 規則接線 → 衍生 **DEF-19-001** 漸進補強。**2026-06-25 improving_62（v0.24）：catch 側遙測翻預設 ON 活體化**——`_rule_catch_telemetry_enabled()` unset→True，escalation 歸因即記 on-attribution catch 成生產常態，DEF-18-001「catch 側 opt-in 懸置」**實質閉合**（保留顯式 opt-out=0；紅線只增 catch_count 永不 set_maturity；覆蓋面 DEF-19-001 仍 routed）） |
| DEF-19-001 | 2026-06-16 | improving_19 W-19 B 軌 dogfooding（閉合 catch 側契約時，揭露覆蓋面缺口） | catch 歸因覆蓋面不足（DEF-18-001 閉合後之殘留面）：W-19 定義 catch 三要件契約並接入兩確定可歸因路徑（R-9.1 gate retry 耗盡 / R-9.21 monitor 破壞），但目前僅 **2/39** active 規則補了 `failure_mode` 並接線；其餘 37 規則 `failure_mode` 未定義 → 其守望的失敗模式即使觸發 escalation 也不參與 catch 歸因（`catch_count` 恆 0、fail-closed 不記）。後果：這 37 規則的 ROI 仍為單側信號，`propose_graduation` 對它們仍可能偏頗提議退役。本輪**刻意只接 2 條無歧義路徑**（Rule 2 簡單優先、寧缺勿濫不污染 ROI）；覆蓋率以 `rule_fire_telemetry_stats().safety_certificate.catch_attribution_coverage` 程式內誠實揭露。非阻擋（catch 機制正確、紅線守界、flag 預設 OFF＝生產零影響） | P3（成熟度閉環覆蓋面缺口，非阻擋；與 B 軌「機制齊備但覆蓋漸進」家族同根） | routed 未來輪 B 軌：逐步為其餘規則補 `failure_mode` 自描述 + 對應 escalation 呼叫點歸因（每條須有「無歧義映射」證據，不可為衝覆蓋率而臆測歸因——延續 DEF-18-001 紀律）；**生產全面啟用退役前須提升覆蓋率至可接受門檻**。屬漸進補強 W 項，非單輪可竟 | routed（漸進補強進行中：**improving_20 W-20-1 將 catch 歸因從 2/39 推進至 4/39**——新增 R-9.2〔auto_compact per-stage 超限→ESCALATION〕、R-9.22〔spec_patch per-AC 上限耗盡→ESCALATION〕兩條無歧義路徑，各補 `failure_mode` + 接對應 `record_escalation` 呼叫點顯式歸因〔fixed@v0.11，6 case 測試 test_w20_catch_wiring.py〕；`catch_attribution_coverage` 程式內誠實揭露。剩餘 35 規則續漸進補強，生產全面啟用退役前須提升覆蓋率至可接受門檻）。**improving_22 階段一複驗仍 4/39**；🔴 人工本輪 scope 選「DEF-12-002 + DEF-15-001 深層」未含 catch 補強，維持 routed 漸進，未推進。**improving_37 W-37-1 推進 4/39 → 5/39**（新增 R-9.7·9.7.2 HUMAN_PENDING 逾時 ≥168h ESCALATION 無歧義路徑，fixed@v0.15；明文僅 9.7.2 排除 9.7.3〔歸 R-9.2〕防雙重歸因，非重疊守門測試鎖定；R-9.9 親驗無唯一生產落點降級不接）。**improving_38 W-38-1/W-38-2 推進 5/39 → 7/39（+2）**（新增 **R-SELF-STRIDE**〔SANDBOX_HARDENING_GATE policy_violation → ESCALATION，唯一生產落點、與既有 5 條零交集〕、**R-9.3**〔record_spec_audit 的 SPEC_AUDIT 耗盡 → ESCALATION；failure_mode 明文排除 implementation-budget-exceeded 直接 escalate〔正交無規則〕與 R-9.1 gate-retry 落點，防雙重歸因〕兩條無歧義路徑，fixed@v0.16；`test_w38_catch_wiring.py` 8 case 含兩條非重疊守門〔R-SELF-STRIDE verdict=pass 不歸因 / R-9.3 implementation-budget 落點不歸因〕；runtime `catch_attribution_coverage` 實測 7/39）。**improving_39 揭露結構天花板**：grep `record_escalation(` v0.16 fsm_runtime.py＝9 生產 escalation 落點＝7 已接線 + 2 正交無規則（515 implementation-budget、2401 spec_patch-unable-to-draft），「1:1 無歧義落點接線」乾淨候選**已枯竭**；W-39-1 機械分類 39 條＝(A)FSM escalation 7 / (B)hook 3 / (C)lint·TLC 3 / (D)meta-loop guard 14 / (E)manual·advisory·憲法 12，證實 escalation-scoped 真實覆蓋＝**7/7=100%**（DEF-39-001 透明化）。**→ closed@improving_40（milestone，🔴 掌舵者 AskUserQuestion 拍板正式收尾）**：FSM-escalation catch 機制覆蓋已達結構天花板（7/7=100%）；其餘 32 條由 hook/lint/TLC/meta-loop/人工守門＝本質非 FSM-escalation catch-可歸因（`catch_count` 恆 0 為設計使然非缺口）。「其他守門機制覆蓋度量」另立新標的（B 軌未來輪，非本缺陷 scope） | 
| DEF-19-002 | 2026-06-16 | improving_19 階段四 B 軌 dogfooding（Copy-on-Evolve v0.10 後跑雙軌 ci-gate 當場揭露） | 版本偵測 glob 在十位數跨越失效（P2 真 bug）：官方閘門與多處 helper 用 glob `AISDLC_SDD_v0.0*` 偵測最新演化版（`scripts/ci-gate.sh:29`、`AISDLC_SDD_v0.09/tools/arch_fitness/arch_fitness.py:1553` `_latest_version_dir`、`scripts/tests/test_ci_gate_version_resolution.py:60` `_disk_versions`）。**v0.09→v0.10 開頭由 `v0.0` 變 `v0.1`**，`v0.0*` 不再匹配 → ci-gate 仍只測 v0.09、最新演化版 v0.10 **不入官方閘門**（FF-17「演化版恆入閘」初衷反被自身 glob 邊界破功）。實測：`ls -d AISDLC_SDD_v0.0* \| sort -V \| tail -1` = v0.09（漏 v0.10）；首跑 ci-gate「版本：v0.01 v0.09」 | P2（阻擋本輪零退化閘門正確涵蓋 v0.10；影響所有未來 v0.1X+） | 框架程式缺陷 → 共享 infra（versioned 目錄外，免 Copy-on-Evolve）+ v0.10 本體：①`scripts/ci-gate.sh` 改雙 glob `v0.0* v0.[1-9]*`（保 `v0.0*` 子串向後相容 v0.01 凍結 FF-17 regex）；②`scripts/tests/test_ci_gate_version_resolution.py` `_disk_versions` glob→`v0.*`；③`AISDLC_SDD_v0.10/tools/arch_fitness/arch_fitness.py` `_latest_version_dir` glob→`v0.*` | **fixed@v0.10 + 共享 infra**（2026-06-16：三處 glob 修復；dry-run 實證 `SDD_GATE_VERSIONS=AISDLC_SDD_v0.01 AISDLC_SDD_v0.10`；雙軌 ci-gate exit 0「版本：v0.01 v0.10」、v0.01:1478/v0.10:1545/scripts:25 全綠；v0.01 凍結基線 FF-17 因保留 `v0.0*` 子串不破。**未通則化** → **通則化完成@v0.11**：improving_20 W-20-2 把 arch_fitness FF-17 驗證正則 `_CI_GATE_LATEST_GLOB_RE` 由寫死 `v0.0*` 放寬為通則 `v0.[0-9\[*]`〔接受 v0.0* / v0.[0-9]* / v0.[1-9]* / v0.* 等動態偵測慣用語〕，解除「ci-gate.sh 被迫保留 `v0.0*` 子串才不被 FF-17 假紅擋下」的結構耦合異味（improving_19 雙 glob 修復正是被此耦合逼著保留子串）；新增 4 case 測試〔test_arch_fitness.py FF-17 接受 4 種通則 glob，含向後相容 v0.0*〕。子串耦合風險閉合（fully addressed）） |
| DEF-20-001 | 2026-06-16 | improving_20 結案後 zero-trust 複核（三鏡 Architect/SA-SD/QA + 主迴圈，全 PASS；流程面觀察非技術缺陷） | **AISDLC_SDD 閉環結案缺少「結案證據強制重推導」之可執行閘門/hook**（流程缺陷）：improving_20 之所以發生「幻覺工具輸出」事故（前 session 編造整套四件套——測試 passed、commit hash、push 成功、三鏡 PASS、潔淨度數字皆虛構），根因之一是框架**無任何機制要求結案數字（pytest passed、ci-gate、commit/push）必須由獨立驗證者就當前 repo 真實狀態重新推導後才接受**。現行反幻覺紀律僅存於 agent 跨 session 記憶（`no-fabricated-tool-output`）與 `AutoSDD_ZeroTrust_Audit_NN.md` 敘事，**未落為 AISDLC_SDD_v0.01 的 hook/SCG 閘門**——下次換 session 仍可能重演。佐證：`.claude/hooks/` 現有 `session_start.py`/`context_ledger_*`/`post_commit_drift.py` 皆無「結案宣稱數字 vs repo 真實狀態」交叉核對；SCG-4/5（PR/RTM 關卡）依賴人工填寫，無機械重推導門。本輪複核能抓出事故純靠人工派三鏡，非框架保證 | P2（治理閉環誠信缺口，影響所有跨 session 結案之可信度；非阻擋本輪——本輪已人工複核全 PASS） | routed 下一輪 B 軌（框架程式/hook 缺陷）：設計「結案證據重推導」hook——例如 `post_commit_drift.py` 擴充或新增 `closure_evidence_verify` hook，於結案 commit 時自動就 repo 真實狀態重跑/比對 improving_NN 文件宣稱的關鍵數字（至少 pytest passed 計數 + HEAD hash + 演化版 floor），落差即 fail-closed 擋下；使反幻覺紀律由「agent 自律」升級為「框架機械強制」。先入根層 Defect_Log，待 SDD_improving_Automation_NN（軌道②）開 RFC 帳本 | **fixed@improving_21（v0.12）**（2026-06-16：reactive 紀律落為框架機械閘門。**W-21-1 廉價層 fail-closed**：`AISDLC_SDD_v0.12/tools/fsm_runtime/closure_evidence.py:verify_git_facts` 對 improving_NN.md 末尾 `closure-evidence` 契約宣稱的 `claimed_commits`/`claimed_tag` 以 `git cat-file -e`+`merge-base --is-ancestor`+`rev-parse --verify` 就 monorepo 根真實狀態重推導〔白名單正則消毒、list-form argv shell=False〕，任一無法重推導→FAIL〔直擊「編造 commit/push/tag」事故核心〕。**W-21-2 昂貴層 inconclusive fail-closed**：pytest passed/ci-gate floors **不重跑**〔守 hook <2s budget〕，改驗綁定當前 HEAD 的 rederive 證書〔`write_rederive_cert` stamp HEAD〕，契約 base_sha≠HEAD 或證書缺失/數字不符→INCONCLUSIVE〔絕不假綠，比照 embodied_grounding 零觀測語意〕。thin hook `.claude/hooks/closure_evidence_verify.py`〔git toplevel 定位、exit 0、fail-soft、advisory 不阻擋 commit，verdict 寫 `.git/CLOSURE_EVIDENCE_VERDICT`〕；安裝 `tools/install_hooks/install_post_commit.{sh,ps1}` 串接 drift+closure。測試 `test_closure_evidence.py` **19 case**〔tmp 真實 git repo 驗 cat-file/merge-base/rev-parse 真實行為、突變證非假測試〕。**決策不新增 R-9.x**〔advisory hook 不需規則承載，同 DEF-10-002 + Rule 2；治理規則承載+catch 覆蓋面+SCG-4/5 機械閘門 routed 未來輪〕；**不動 FSM/`*.tla`→免五軌 TLC**〔transition_rules.py+5 tla 對 v0.11 逐位元零差異〕。RFC `SDD_improving_Automation_27.md`。三鏡 zero-trust PASS〔修復回合 1：文件路徑落差〕、零退化 AutoClaude 3112/v0.12 1574/lint 8/ci-gate exit 0/潔淨 852。見 `AutoSDD_improving_21.md` / `AutoSDD_ZeroTrust_Audit_21.md`） |
| DEF-21-001 | 2026-06-16 | improving_21 W-21-2 B 軌 dogfooding（smoke test closure hook 當場揭露） | 契約解析歧義（本輪即修）：`improving_NN.md` 可同時含「§4 schema 範例」與「文件末尾真實結案契約」兩個 ```yaml ``closure-evidence`` fenced 區塊；`closure_evidence.parse_closure_evidence` 初版取**首個**匹配 → 誤抓 schema 範例（placeholder `<hash>`）→ hook smoke 輸出 `.git/CLOSURE_EVIDENCE_VERDICT` 假 FAIL「hash 不合法」。根因：解析未採 last-match 慣例（真實契約慣例放文件末尾）。非阻擋（本輪即修，dogfooding 揭露） | P3（解析歧義/取證友善，非阻擋；本輪 W-21-2 必要部分非新 scope） | 框架程式即修（v0.12 內 → 隨本輪 Copy-on-Evolve）：parse 改 last-match + §4 schema fence 改 ```text 雙保險 | **fixed@improving_21（v0.12）**（證據：`closure_evidence.py:parse_closure_evidence` 改持續覆蓋留 last-match〔對齊 DEF-02-002 `tlc_runner.parse_tlc_summary` findall[-1] 紀律〕；`test_closure_evidence.py::test_parse_evidence_last_match_wins` 鎖定 schema 範例在前+真實契約在後取末尾；improving_21.md §4 schema 範例 fence 改 ```text 不被 parser 匹配；hook 重 smoke 不再誤抓範例〔INCONCLUSIVE「未找到契約」非假 FAIL〕） |
| DEF-21-002 | 2026-06-16 | improving_21 結案 commit 前 git status 潔淨度盤點 | 潔淨度（本輪即修，同 DEF-11-001 家族）：closure hook `write_verdict_report`/`write_rederive_cert` 以 `git rev-parse --show-toplevel`（monorepo 根）為 repo_root，在**根** `build/reports/closure/` 產生 `VERDICT-<sha>.yaml`/`REDERIVE-<sha>.yaml` runtime 產物（「輸出非輸入」），但**monorepo 根 `.gitignore` 未涵蓋**（既有規則僅 `__pycache__`/`.venv`/`.env` 等）→ `git status` 顯示 untracked `build/`，將污染 improving_21 結案 commit。非阻擋（排除後零影響） | P3（潔淨度摩擦，非阻擋、非框架程式缺陷） | 共享 infra 即修（monorepo 根 `.gitignore`，versioned 目錄外）——判為完成本輪結案潔淨之必要部分，非新 scope | **fixed@improving_21**（證據：monorepo 根 `.gitignore` 新增 `/build/reports/closure/` 排除〔註明 closure hook 以 git toplevel 為 repo_root 之 runtime 產物，同 AISDLC_SDD 各版 build/reports 排除精神〕；`git check-ignore build/reports/closure/VERDICT-38de1e786a2d.yaml` 命中、`git status` 不再顯示 build/。closure 證書屬本機 rederive runtime 產物，fresh checkout 無證書→hook INCONCLUSIVE 為誠實 fail-closed 行為〔同 ci-gate log 本機性〕） |
| DEF-21-003 | 2026-06-16 | improving_21 dogfooding 自驗（準備跑 rederive 證書時揭露） | 訊息 vs 實況不符（本輪即修）：closure hook INCONCLUSIVE flag 訊息承諾「跑 `python -m tools.fsm_runtime.closure_evidence --rederive`」，但 `closure_evidence.py` 初版**僅有 `write_rederive_cert` 函式、無 CLI `__main__` 入口** → 該 `python -m` 指令 import 模組即結束、無任何動作（rederive 證書無法經承諾入口產生），W-21-2 昂貴層「綁定 HEAD 證書」實務上無便捷產生途徑。非阻擋（dogfooding 揭露，本輪即補） | P3（訊息/實況不符 + 機制完整性缺口，非阻擋；本輪 W-21-2 rederive 機制完整之必要部分） | 框架程式即修（v0.12 內 → 隨本輪 Copy-on-Evolve）：補 `_main(argv, repo_root=None)` CLI 入口（`--rederive --observed '<json>'` stamp HEAD 落盤證書 / 無參數 evaluate 印 verdict）+ `if __name__` 派發 | **fixed@improving_21（v0.12）**（證據：`closure_evidence.py` 補 `_main` + `__main__`〔repo_root 可注入供測試〕；`test_closure_evidence.py` +3 case〔rederive 寫證書 base_sha 綁 HEAD / bad-json 回 2 / 無參數 evaluate 回 0〕共 **22 passed**；實跑 `python -m tools.fsm_runtime.closure_evidence --rederive --observed '{…3112/1574/floors…}'` 證書落盤 + 無參數印 `verdict=VERIFIED`〔commit fact PASS + 3 昂貴項 VERIFIED〕——**本輪 dogfooding 自驗成功：新 hook 對本輪自身結案 commit `5f8b633` 判 VERIFIED，反幻覺迴圈閉合**） |
| DEF-22-001 | 2026-06-17 | improving_22 W-22-1 B 軌 dogfooding（寫 DEF-12-002 版本化 nodeid 回歸 case 時當場揭露） | 版本偵測 regex 十位數跨越失效（P2 真 bug，DEF-19-002 同根之漏網處）：`AISDLC_SDD/scripts/cross_version_guard.py:18` `VERSION_RE=re.compile(r"AISDLC_SDD_v0\.0\d+")` 僅匹配 v0.00–v0.09（要求 `v0.0` 後接數字），對**現役 v0.10/v0.11/v0.12/v0.13 完全不匹配**。後果：cross-version pytest guard 對 v0.10+ 失能——`_is_path_arg`/`_versions_in_path` 無法將 v0.10+ 路徑歸因到版本、`_versions_under_dir` bare 展開漏算 v0.10+ 目錄（guard 核心防誤用功能對當前主力版本部分失效）。DEF-19-002 當時只修 ci-gate.sh/arch_fitness/test_ci_gate_version_resolution 三處 glob，**漏掉此處 conftest guard 的 VERSION_RE**。實證：`versions_touched(["AISDLC_SDD_v0.12/x.py::test"])` 修前回空集（應得 {v0.12}）。非阻擋（既有 v0.01–v0.09 仍在故 bare guard 仍會 fire，僅版本集合漏 v0.10+；官方 ci-gate `cd vX` 每版獨立跑不依賴此偵測）| P2（guard 對現役版本偵測失能；DEF-19-002 同根十位數跨越家族）| 共享 infra 即修（`AISDLC_SDD/scripts/`，versioned 目錄外免 Copy-on-Evolve）——判為完成 W-22-1 cross_version_guard 正確性之必要部分（同檔同函式族）、非新 scope | **fixed@improving_22**（證據：`cross_version_guard.py:VERSION_RE` 放寬 `r"AISDLC_SDD_v0\.0\d+"` → `r"AISDLC_SDD_v0\.\d+"`〔對齊 DEF-19-002 通則化哲學，match v0.X 任意 X〕；`scripts/tests/test_cross_version_guard.py::test_versioned_nodeid_still_detects_version` 鎖定 `v0.12/x.py::test_y`→{v0.12}〔退化即紅〕，既有 v0.03/v0.04 雙版 case 不受影響；scripts/tests 27 passed。同檔 W-22-1 一併修，免額外版本演化） |
| DEF-23-001 | 2026-06-17 | improving_23 階段一 zero-trust 偵察（構件重現查核 agent，查 cross_version_guard 測試數時） | 文檔 vs 實況：v0.13 `releases/CHANGELOG.md` 宣稱 `test_cross_version_guard.py`「+2 case（25→27）」，但該檔單檔實測僅 **10 個 test 函式 / 10 passed、無 parametrize**（`scripts/tests/` 全套 27 passed 為跨檔合計）。測試本身真實全綠、DEF-12-002/DEF-22-001 回歸鎖確在檔內，僅「25→27」計數口徑與單檔函式數不一致 | P3（文檔計數口徑，無功能影響） | 規格/文檔缺陷 → v0.14 CHANGELOG 釐清「27＝scripts/tests 全套合計，非單檔函式數」（Copy-on-Evolve 隨 W-23-1 落版） | fixed@improving_23（證據：見 §結案；v0.14 CHANGELOG 補註計數口徑） |
| DEF-23-004 | 2026-06-17 | improving_23 階段五 SA-SD 鏡 zero-trust 複審（文件 vs 實況比對） | 文件 drift（DEF-05-002/07-001 同家族復發）：計畫書 `docs/04_planning/AutoSDD_improving_23.md §6 RTM` 列的 9 個測試名中 7 個與實作落地名不符（實作期改名後未回掃 RTM），如 `test_fold_aware_verify_passes_for_honest_fold`→實作 `test_fold_honest_render_passes_verify_and_guard`、`test_fold_off_is_v013_behavior`→`test_fold_off_is_default_v013_behavior` 等。功能全存在且覆蓋更全（實作 11 個 `test_fold_*` + 2 chaos fold ≥ RTM 9 項，實跑 50 passed），僅 RTM 追溯鏈表面命名不對應 | P3（文件追溯鏈命名，無功能影響） | 規格/文檔即修（本輪 RTM 對齊實作落地名 + 加註命名 drift 校正說明） | fixed@improving_23（證據：§6 RTM 已改為實作落地名並加 SA-SD 校正註；改名後回掃 RTM 之紀律補入結案說明） |
| DEF-23-003 | 2026-06-17 | improving_23 階段四 潔淨度 dry-run（DEF-11-002 紀律：Copy-on-Evolve 後跑 `git add -A -n`） | Copy-on-Evolve 結構摩擦：`AISDLC_SDD/.gitignore` 為**逐版本**手寫 runtime block（v0.01/v0.05~v0.13 各一段），新版 v0.14 建立後**尚無對應 block** → `git add -A -n AISDLC_SDD_v0.14` 當場揭露 11 個 runtime 產物漏網（`arch-fitness.json` + `build/reports/abort,fsm,test-analysis/*`，皆跑 pytest/ci-gate 時 FSM bootstrap/arch_fitness 生成）。根因：v0.13+ 模板已移出 build/reports（DEF-15-001）後本可用通則 glob，但 gitignore 仍逐版手補；漏補即漏網（DEF-11-001/11-002/15-001 同家族復發）| P3（潔淨度，dry-run 即攔，未實際入庫；但每輪 Copy-on-Evolve 復發） | 本輪即修（`AISDLC_SDD/.gitignore` 補 v0.14 整樹排除 block，shared infra 免 Copy-on-Evolve）；通則 glob（v0.13+ 免 negate 後 `AISDLC_SDD_v0.1[3-9]/build/reports/` 類）評估 routed 未來輪 | fixed@improving_23（證據：`.gitignore` v0.14 block〔`AISDLC_SDD_v0.14/build/reports/` + arch-fitness.json + chaos-report.json〕；修後 `git add -A -n` v0.14 = **853 would-add 零 runtime 漏網**、FSM 模板仍 tracked） |
| DEF-23-005 | 2026-06-17 | improving_23 結案後 zero-trust 複審（中斷重啟後三鏡整體複驗，DEF-23-002 系統性根因抽離） | 框架流程自動化缺口（DEF-23-002 之系統性根因）：框架明定「active=待決 / archive=已決」RFC 生命週期，但**無任何 hook/lint/CI 自動強制**——已完成的 `_26/_27` 滯留 active/ 直到人工 zero-trust 盤點才揪出（DEF-23-002 已手動 `git mv` 修症狀，但「下次再漏」的結構性風險未除）。同類「逐版本手寫 gitignore block」（DEF-23-003）亦屬「框架治理靠人工紀律、缺機械強制」家族。本輪複審：三鏡全 OVERALL PASS、零新功能缺陷，此為唯一可行動之**流程改善候選** | P3（流程衛生自動化，無功能影響；每輪 Copy-on-Evolve/RFC 收官皆潛在復發） | 框架程式/hook 缺陷 → 下一輪 RFC + v0.0(X+1)：評估 `.claude/hooks/` 或 ci-gate 增「RFC 生命週期 lint」（active/ 內含已標收官/已決 RFC 即 warn/fail）+「Copy-on-Evolve 新版 gitignore block 缺漏偵測」 | **fixed@improving_30**（RFC 生命週期 lint 落 shared infra `scripts/rfc_lifecycle_lint.py`〔version-agnostic、**免 Copy-on-Evolve**，DEF-02-001/DEF-03-001 先例，非走 `.claude/hooks` per-version〕，接入 `ci-gate.sh` 硬閘掃**最新版** active/。「最低誤報」雙信號：(a) 落地版本欄指向**磁碟已存在**之凍結版〔已決〕、(b) 顯式結案狀態行〔狀態：已決/結案/archived/closed〕，皆**錨定行首 header 欄位式**避免 meta 文件誤報。證據：`scripts/tests/test_rfc_lifecycle_lint.py` 11 passed〔正例 fire/待決不誤報/meta 防誤報/只掃最新版/語意版本/CLI/真實 v0.14 active 乾淨鎖〕；真實資料 lint 對 archive RFC 27〔落地 v0.12 存在〕正確 fire、對 RFC 26〔無標記〕正確不 fire；`ci-gate.sh` exit 0 scripts/tests 27→**38** + RFC lint PASS；dogfooding RFC `SDD_improving_Automation_28.md` 經 active→archive 完整生命週期、lint 自我驗證 proposal 態不誤報。**gitignore block 缺漏偵測**部分未納本輪 scope，留 DEF-30-001 家族/未來輪） |
| DEF-30-001 | 2026-06-18 | improving_30 W-30-1 B 軌 dogfooding（建 RFC 生命週期 lint 時偵測標記慣例） | 框架文檔慣例缺口：RFC「已決」結案標記**無標準化欄位**——37 個 archive RFC 中 `落地版本` 僅 1 檔、`結案` 2 檔、`狀態：已決` 0 檔、`決策` 區塊 25 檔（proposal 模板本有，非已決標記）。致機械 lint 只能以「最低誤報」雙信號鎖現有最強標記，無法覆蓋所有已決 RFC（如 RFC 26 Phase Y 藍圖無任何已決標記、lint 偵測不到）。非阻擋（lint 對有標記者正確強制；無標記者本就靠人工） | P3（文檔慣例標準化，無功能影響） | 規格/文檔缺陷 → 未來輪評估於 RFC 模板（`docs_template/` 或 RFC 撰寫指引）補標準 `**狀態**：proposed/decided` 欄並回填既有 archive RFC；屆時 lint 可升級為「active/ RFC 缺 狀態 欄即 warn」強制慣例 | **fixed@improving_33**（標準慣例 `**狀態**：proposed\|decided` 落於 shared infra lint module docstring〔即 SSOT，免 Copy-on-Evolve〕；`scripts/rfc_lifecycle_lint.py` 三項升級：(a) `_CLOSED_STATUS_RE` 值集補標準英文 token `decided`〔補既有 `已決`〕使 `**狀態**：decided` 被識別為已決；(b) 新增 `_STATUS_FIELD_RE`+`find_active_rfcs_missing_status()`+`missing_status()`，active/ RFC 缺行首 `**狀態**` 欄 → `main()` 印 `::warning::`〔**不影響 exit code**，與「已決滯留 active/」exit 1 硬違規嚴格分級〕；(c) docstring 文件化慣例。**閉環邏輯**：強制每個 active/ RFC 帶狀態欄，則已決滯留 active/ 必帶 `**狀態**：decided`→被既有 decided 偵測攔下，覆蓋缺口閉合。`test_rfc_lifecycle_lint.py` 11→15 case〔+decided token fire/缺欄 advisory 非硬違規/proposed 全乾淨/CLI 缺欄 warn 但 exit 0〕，M2a〔移除 decided〕+M2b〔反轉缺欄邏輯〕突變實證非假。**dogfooding 自驗**：RFC `SDD_improving_Automation_29.md`〔driver improving_33〕由 `**狀態**：proposed` 起→翻 `decided` 仍留 active/ 時 lint 以新標準 token 攔下〔exit 1 實證〕→`mv` 入 archive/→active/ 復乾淨 lint exit 0。**scope（Rule 2）**：不回填 archive RFC〔lint 只掃最新版 active/，archive 不在掃描範圍〕、不擴充 `已歸檔`/`EXECUTED` 等舊詞彙〔收斂至 proposed/decided 標準，非 vocab whack-a-mole〕。ci-gate scripts 38→42 exit 0 不退化） |
| DEF-23-002 | 2026-06-17 | improving_23 階段一 zero-trust 偵察（active planning 盤點） | 流程漂移：`AISDLC_SDD_v0.13/build/planning/active/SDD_improving_Automation_26.md`（Phase Y，§D 已載「✅ 已執行收官 2026-06-06」）與 `_27.md`（closure_evidence，§6 標「決策後 archive」、已落 v0.12）**兩份已完成 RFC 仍滯留 `build/planning/active/`**，違反框架自身「active=待決 / archive=已決」生命週期（archive/ 已有 _01~_23+，獨缺已完成的 _26/_27）| P3（流程衛生，無功能影響；但使 active 列表失真，誤導後續輪判斷待辦） | 框架程式/流程缺陷 → v0.14 `git mv` _26/_27 入 `build/planning/archive/`（隨 W-23-1 落版） | fixed@improving_23（證據：v0.14 archive/ 含 _26/_27、active/ 清空已完成項；**SA-SD 鏡複審連帶修**：`ID_REGISTRY.yaml:116` Phase Y `ref` 由 `active/SDD_improving_Automation_26.md` 同步改 `archive/...`，消除歸檔後 stale ref） |
| DEF-59-001 | 2026-06-24 | improving_59 階段四 ci-gate（`test_gitignore_coverage_lint.py::test_real_repo_latest_covered` 當場揭露） | Copy-on-Evolve 結構摩擦（**DEF-58-002 / DEF-23-003 同根因家族**「人工後步驟未釘進腳本」復發）：建 v0.23 後 `AISDLC_SDD/.gitignore` 無 v0.23 runtime 產物 block → ci-gate 之 gitignore 覆蓋 lint 對 LATEST 報紅（missing `AISDLC_SDD_v0.23/build/reports`/`arch-fitness.json`/`chaos-report.json`）。improving_58 已把**戳記同步**釘進 `copy_on_evolve.sh`，但 **.gitignore block 補寫仍為人工後步驟**、漏補即帶紅（dogfooding 當場踩到） | P2（潔淨度/閘門帶紅；每輪 Copy-on-Evolve 復發，同 DEF-58-002 家族） | 共享 infra 即修（`AISDLC_SDD/scripts/copy_on_evolve.sh` 根因硬化，versioned 目錄外免 Copy-on-Evolve）+ `.gitignore` 補 v0.23 block | **fixed@improving_59**（證據：① 立即修 `.gitignore` 補 v0.23 block → ci-gate exit 0；② 根因硬化 `copy_on_evolve.sh` 建版後自動 append 新版 `.gitignore` block〔idempotent、grep-skip 首行 path〕；③ 回歸鎖 `test_copy_on_evolve.py::test_auto_appends_gitignore_block_on_evolve_def_59_001`〔scripts/tests 128→129〕，受控突變 **M-W593**〔`if false &&` 停用自動補 → 測試轉紅、還原 9 passed〕） |
| DEF-75-001 | 2026-06-26 | improving_75 階段二增量設計（讀 A/B 載具 parse 邏輯時揭露） | 載具 dead-parse（本輪即修）：`AutoClaude/tools/ab_compare_backends.py` 的 `_RE_FIELD_BOOL` 含 `halted` regex（:50），`parse_run_metrics` 把它解析進區域 `bools` 字典（:101-104）卻**僅取 `success`/`escalated` 寫回 RunMetrics、丟棄 `halted`**（:105-106）——RunMetrics 根本無 `halted` 欄。後果：A/B 載具無法區分「撞 ≥90% halt 門檻（token 失控需 checkpoint 暫停）」的後端，compaction 成本圖譜缺孿生指標。非阻擋（既有四指標仍正確；halted 純漏記） | P3（載具 dead-parse / 觀測完整性，非 autoclaude 生產碼、非阻擋） | 整合層（AutoClaude 載具）即修 → 本輪 W-75-3 閉環內修復（RunMetrics 加 `halted` 欄 + 寫回 + `aggregate_runs` halted_count，對稱 success/escalated_count） | **fixed@improving_75**（證據：`parse_run_metrics` 加 `m.halted = bools.get("halted", False)`；`RunMetrics.halted` + `AggregateMetrics.halted_count` additive 欄；回歸測 `test_halted_parsed_from_kernel_result` + `test_perfect_run_not_halted` + `test_aggregate_halted_count`；受控突變 **MUT-75-3**〔`m.halted = bools.get(...)` → `= False` 回退〕致兩測轉紅〔`0==1` / halted is False〕，Edit 還原後 26 passed 復綠） |
| DEF-76-001 | 2026-06-26 | improving_76 三鏡審查（SA-SD P1 揪 CORRECTION 雙 emit site，parent zero-trust 複核連帶揭露 token marker production-blind） | **載具 token marker production-blind（重大觀測缺口，跨輪根因）**：`AutoClaude/tools/ab_compare_backends.py` 的 `peak_token_pct`/`compact_count` 自 improving_71 起只解析 `=== STATE: TOKEN_COMPACT ===`，但**該 marker 僅在已棄用 PlaybookRunner 直連路徑 `_impl.py:233` 印**；production **唯一正式路徑＝Kernel**（`main.py:123`「SD_Improving_05 W6：雙路徑已移除；Kernel 路徑為唯一正式路徑」、`autoclaude/core/` 全域 grep 零 `TOKEN_COMPACT`）**不印 TOKEN_COMPACT** → improving_71/75 的 peak/compact **在 production 真跑恆為 0**（candidate (a) 真跑會直接踩到）。Kernel 端 token 壓力以 `=== STATE: TOKEN_HALT \| [Sxx] context NN% ===`（`plugins/checkpoint/_token_halt.py:46`，≥90% halt）表達，但**載具完全未解析**；另注：該 TOKEN_HALT marker 由 deprecated `handle_token_halt`（`plugin.py:191`，docstring 載明自 `_runner_internals` 搬移）印，production phase handler `save_token_halt`（`_phase_handlers.py:77`，ON_TOKEN_USAGE）**只存 checkpoint 不 log marker** → production 端 token-% 觀測是否落地仍待補完 trace。**附帶**：CORRECTION 兩 emit site——Kernel `kernel.py:224` 帶 `step=`（W-71-2 補、per-step correction production 有效）；已棄用 `_impl.py:437`「諮詢 Minimax」不帶 step= → per-step correction 對該行為**下界**（非全稱「皆帶 step=」，原計畫書 overclaim 已訂正） | P2（觀測完整性 / 跨輪根因；非阻擋〔halted 布林、CORRECTION/✓ step id 在 production 仍正確〕，但使 token 成本維度在 production 真跑失真） | **分兩段**：①載具側可離網即修（本輪 W-76-2）——載具納入 TOKEN_HALT 解析（peak/per-step peak「哪路徑印就抓哪個」）+ 訂正 overclaim 為「下界」；②**production 端 Kernel observability marker 補強需動 autoclaude 生產碼**（仿 W-71-2 為 Kernel 補 CORRECTION 之前例，於 Kernel/checkpoint 正式路徑補可被載具計數的 token-壓力 marker + SSRF/攻防無關但需回歸/攻防測試）→ **routed 下一輪 A 軌 W 項**（improving_77，需生產碼非純載具，justified 延後） | **partially-fixed@improving_76（載具側）+ routed（production marker）**：①證據——`parse_run_metrics` 掃描迴圈納入 `is_halt = "TOKEN_HALT" in line` 餵 peak/per-step peak（halt≠compact churn 故 compact_count 不計入）；回歸測 `test_token_halt_marker_feeds_peak_and_per_step`（peak 92 from TOKEN_HALT、compact_count 0、halted True）+ `test_per_step_correction_is_lower_bound_for_untagged`（whole 2 / per-step sum 1 下界）；受控突變 **MUT-76-4**〔`is_halt = "TOKEN_HALT" in line` → `= False`〕致 production peak 歸 0 轉紅〔`0.0==92.0`〕、Edit 還原後 38 passed 復綠。②production Kernel marker 補強 routed improving_77（需生產碼）。**▸ improving_77 更新（2026-06-26）**：本輪掌舵者選「輕量真跑」，真跑啟動意外揪出 DEF-77-001（載具 real-run fail-loud），改修該載具缺陷；DEF-76-001 production marker **本輪未做、續 routed improving_78**；但本輪真跑（pty/sdk N=1）**再次實證** token 峰值/compact 恆 0（production blind + smoke 過短雙因），補強證據鏈。**▸ improving_78 framing 澄清 + halt 維度修復（2026-06-26）**：DEF-78-001 揭露「marker 缺」根因＝production Kernel 路徑根本沒接 token-guard 編排（非單純漏印），故原「補靜態 marker」前提不成立。improving_78 W-78-1 改為**真接線 halt 路徑**：production Kernel ≥90% 真實 token% 觸發真誠 TOKEN_HALT marker → **本載具 peak/halt 維度此後在 production 真跑為真值**（DEF-76-001 的 halt 觀測缺口隨之消解）。compact_count 維度仍待 DEF-78-001 的 W-78-2（compact 動作接線）→ 續 routed。**▸ improving_79 全閉合（2026-06-26）**：DEF-78-001 W-78-2 接 production Kernel compact 子路徑（≥80% 真送 /compact + 真誠 TOKEN_COMPACT marker），**compact_count / peak 維度此後 production 真跑為真值** → DEF-76-001 halt + compact 兩維度皆 production 真值，由 partially-fixed 升 **fixed@improving_79**（載具側 + halt + compact 全閉合）。詳見 improving_79 §4 / ZeroTrust_Audit_79 |
| DEF-77-001 | 2026-06-26 | improving_77 階段一（掌舵者選「輕量真跑」，parent 親跑 real-run 模式啟動真跑時揪出） | **A/B 載具 real-run 模式靜默 0/0 + fail-loud 違反**：`AutoClaude/tools/ab_compare_backends.py` real-run 以子目錄為 cwd 起 autoclaude subprocess（`run_backend:350-353` `cmd=[python, -m, autoclaude, playbook, --fresh]` + `cwd=str(workdir)`），但 **(1) playbook 路徑（`args.run`）未 resolve 成絕對**——傳相對路徑 `scripts/sdd_bridge_smoke.yaml` 時 subprocess 在子目錄找不到 → autoclaude 啟動即失敗未建 log（N=1 分支 `main:405-406` 連 config 也未 resolve，只有 N>1 `run_backend_n:367` resolve config，**兩路徑皆未 resolve playbook**）；**(2) fail-loud 違反**——`subprocess.run(...)` 的 returncode/stderr 整個丟棄（`:353`），log 不存在時 `:355` 回空字串 → `parse_run_metrics("")` 全 0 → `format_comparison` 印「成功的平淡 A/B」+ `main` 回 **exit 0**。**證據（parent 親跑）**：`--run scripts/sdd_bridge_smoke.yaml`（相對）→ pty/sdk 皆 0/0 步驟、run 成功 False、全 0、exit=0、workdir 子目錄全空；改絕對路徑 `$(pwd)/scripts/sdd_bridge_smoke.yaml` → pty/sdk 皆 100% 一次通過、2/2、run 成功 True（真起 `claude -p` wexpect、真建檔、pytest 評估 exit=0）。違反工程紀律第 12 條 Fail Loud（completed 是錯的若有東西被靜默跳過） | P2（載具觀測誠實性；使用者從非專案根 cwd 或用相對路徑跑會拿到假全 0 報告而不知真跑未跑；非生產碼、不影響 autoclaude 引擎本身） | 載具層缺陷、可離網即修：本輪 **W-77-1** 即修——(1) 新 helper `_resolve_invocation_path` 把 playbook + config resolve 成絕對（main 解析 args 後即做，cwd 仍為使用者 cwd）；(2) 新 helper `_load_log_or_raise` log 不存在即 `raise RuntimeError`（含 backend/returncode/stderr），不再回空字串假裝 0/0。log 存在時行為與舊版完全一致（escalated/halted 照常解析） | **fixed@improving_77**（W-77-1；證據見 §4.1 / ZeroTrust_Audit_77：兩 helper + 單測 + 受控突變 MUT-77-x 轉紅還原；修復後相對路徑真跑亦成功 2/2 不再 0/0） |
| DEF-78-001 | 2026-06-26 | improving_78 階段二（掌舵者核准 DEF-76-001 production marker 後，parent 零信任直讀生產碼坐實 marker 補點時揭露） | **production Kernel 路徑 token-guard 編排（≥80% /compact、≥90% halt+checkpoint）從未接線（跨輪潛伏缺陷，與 DEF-71-001/DEF-72-001「Kernel 遷移長期無覆蓋」同根因家族）**：production 唯一正式路徑＝`main.py:138 service.run()` → Kernel.run（`core/kernel.py:69`）→ `_run_step`（:144）→ `self._exec.execute(...)`（:177）**不傳 on_event**；POST_ATTEMPT payload（:238-242）無 token_pct → `token_guard/policy.py:158` 取 `token_pct=0.0` → should_halt/should_compact 恆 False；Kernel 全檔只在 :248 消費 `request_halt`、**零消費 `request_compact`**；全 codebase **零 ON_TOKEN_USAGE emit 端**（僅 4 subscriber 含 `checkpoint/_phase_handlers.py:77 save_token_halt` → 死碼）。真正會印 `=== STATE: TOKEN_COMPACT ===`（`_impl.py:233`）/`TOKEN_HALT`（`_token_halt.py:46`）與 compact/halt 決策（`prompt_dispatcher.py` 取 `runner: PlaybookRunner`）全在**棄用 PlaybookRunner 路徑**。故 production token 壓力實際全由 executor 內 act-first 檢查（`sdk_executor_adapter.py:233`）+ Claude Code CLI 自身 autocompact 處理，AutoClaude 編排層 compact/halt 在 Kernel 路徑是 dead code。**對 DEF-76-001 的連帶釐清**：其 routed improving_78 的「production 端補可被載具計數的 token 壓力 marker」前提**部分不成立**——production 無 live compact/halt 決策點可掛 marker；硬掛靜態 marker＝為死碼偽造訊號（違 fail-loud / [[no-fabricated-tool-output]]）。**真值出處**：production 真實 token% 其實存在於 executor（SDK `get_context_usage().percentage`、PTY `extract_context_pct`），只是 Kernel 未把 on_event 接出來記入 log，故載具讀不到。 | P2（觀測完整性 + 編排潛伏缺口；**非阻擋**——CLI autocompact + act-first 仍提供 token 安全，且 smoke 不觸發門檻；但使「AutoClaude 自身 compact/halt 觀測」在 production 真跑結構性恆 0、且 DEF-76-001 的 production marker 無處可掛） | **處置待掌舵者裁（improving_78 階段二）**：候選 (A) 最小誠實觀測——Kernel `_run_step` 傳 on_event callback 把 executor 真實 token% 記入 log（+門檻跨越時印 marker），載具即讀到 production 真實峰值（additive、observability-only、零控制流變更，仿 W-71-2 前例，touches core/kernel.py）；(B) 完整接線——把 token-guard compact/halt 編排真正接進 Kernel（emit ON_TOKEN_USAGE → token_guard 決策 → Kernel 執行 compact/halt），恢復文件宣稱行為（大、架構級、跨輪、零退化風險高）；(C) 本輪零生產碼——只記本缺陷 + 訂正 DEF-76-001 framing + 載具誠實標 token 維度結構性 N/A、route 真修 | **fixed@improving_79（DEF-78-001 halt+compact 雙子路徑全接線、全閉合）**〔improving_78 partially-fixed halt 子路徑 → improving_79 補 compact 子路徑〕：掌舵者裁 B/路徑層+純Kernel/halt先行。**halt 接線（W-78-1）**：Kernel `_run_step` 經新 `core/_token_observer.py` 觀測 executor 真實 token%（SDK TOKEN_PCT / PTY PARTIAL_OUTPUT 解析）→ 僅 peak>0 時 emit ON_TOKEN_USAGE（`_consult_token_guard`）→ 真 TokenGuardPlugin ≥90% 回 request_halt → Kernel 印真誠 `=== STATE: TOKEN_HALT \| [Sxx] context NN% >= halt 門檻 ===` + 回 HALT（StepOutcome/KernelResult additive 帶 halt_step_idx/peak_token_pct）；AutoResumeService 握 path 存 path-aware halt checkpoint（`_persist_halt_checkpoint`，resume 點=halt_step_idx）。新測 13（observer 6 + kernel halt 4 + 持久化 3）+ executor fake 補 on_event 協定一致性。零行為變更鐵證＝peak=0（無 token 訊號）不 emit ON_TOKEN_USAGE。**compact 子路徑（≥80% /compact 動作）routed W-78-2**（屬執行層業務邏輯、需 helper 送 /compact，架構張力 + 零退化餘量考量本輪不塞，誠實延後）。**誠實限制**：最小 halt checkpoint 不持久化跨 session 計數器（預設空 dict）。詳見 improving_78 §4 / ZeroTrust_Audit_78。**▸ improving_79 W-78-2 全閉合（2026-06-26）**：compact 子路徑接線——新 `core/_token_compactor.perform_compact`（送 /compact + 印真誠 `=== STATE: TOKEN_COMPACT \| [Sxx] context NN% >= compact 門檻 ===`）；Kernel `_consult_token_guard` 加 `request_compact` 分支 → 新 `_handle_compact` → emit `POST_COMPACT`；TokenGuardPlugin 訂閱 POST_COMPACT + 新 `_evaluate_post_compact` **完整移植 Gap-008-E**（掌舵者裁定：連續 compact 失敗 2 次 → request_halt → Kernel HALT；CompactFailureState SSOT 留 plugin、走 EventBus 不外洩 Kernel）；Gap-008-E HALT checkpoint 複用 W-78-1 `_persist_halt_checkpoint`（零新持久化碼）。**載具 compact_count/peak 維度此後 production 真值**（DEF-76-001 compact 維度連帶閉合）。新測 13（compactor 4 + kernel compact 4 + plugin POST_COMPACT 5）；受控突變 MUT-79-1/2/3 轉紅還原無殘留；全套 **3434/122/0**、lint 8 kept、LOC 0、snapshot OK；零碰 AISDLC_SDD。memory-anchor enrichment 未移植（justified：避免破壞 core-purity/DRY，非接線必要）。詳見 improving_79 §4 / ZeroTrust_Audit_79 |
| DEF-77-002 | 2026-06-26 | improving_77 結案後對話（掌舵者追問「Minimax key/URL 填哪、是否走 .env」，parent zero-trust 核對 config/env 載入鏈時揭露） | **文件缺陷：`.env` 不會被自動載入卻無任何警示，且 config.yaml 註解不完整**。(1) `AutoClaude/.env.example` 引導使用者 `cp .env.example .env` 填憑證，但全 codebase **無 `load_dotenv`、pyproject 未依賴 `python-dotenv`**（`grep -rnE "load_dotenv|dotenv" autoclaude/` 僅 docstring 提及 `.env.example`）——程式只直接讀 `os.environ`（如 `main.py:103-107` 讀 `MINIMAX_API_KEY`/`MINIMAX_BASE_URL`/`MINIMAX_MODEL` 覆寫 config）。故使用者把值填進 `.env` 後若啟動方式未自行 source/export，變數**靜默不生效**、key 仍空。(2) `config.yaml:9` 原註解「建議改用環境變數 MINIMAX_API_KEY」雖技術正確（main.py 確讀），但未提醒「.env 不自動載入」之前提 → 易誤導。**附帶釐清（parent 自我訂正）**：對話中 parent 一度因 `grep ... \| head` 輸出被截斷而誤稱「MinimaxClient 不讀任何 env、config.yaml 註解誤導」，經直讀 `main.py:103-107` 訂正——env 覆寫對 brain 有效（截斷輸出妄下結論之教訓，呼應 [[no-fabricated-tool-output]]） | P3（純文件/DX 缺陷；功能正確，僅缺使用指引致易踩坑；非程式行為錯誤） | 文件即修（非 autoclaude/ 生產碼、非 .md 故 enforce_docs_path hook 不涉）：補 `config.yaml` minimax 區塊註解（env 覆寫欄位 + ⚠ 不自動載入 .env）+ `.env.example` 頂部「⚠️ 重要：AutoClaude 不自動載入 .env，需啟動時自行注入」段（附 `set -a; source .env; set +a` 範例） | **fixed@2026-06-26**（證據：`config.yaml:8-12` 註解補完整三 env 欄 + 不自動載入警示；`.env.example:8-16` 新增載入警示段 + bash source 範例；commit 隨本次對話收尾） |
| DEF-81-001 | 2026-06-26 | improving_81 階段一真跑探測（掌舵者選「真跑取真實 token 差異」，parent 親跑 smoke 雙 backend 真跑時揭露） | **真跑 token% 訊號源盲區（DEF-76-001 家族最終根因）**：PTY/SDK 雙 backend 真跑 `KernelResult.peak_token_pct` 皆恆 0、compact(≥80%)/halt(≥90%) 在真實負載**從未觸發**。根因分兩支：(1) **PTY**——`pty_executor.py` 跑 `claude -p`（print/非互動），其純文字輸出不含 context%（探針鐵證：`logs/playbook_S01.log` 僅 `Created ... [TEST_READY]`），`TokenObserver` 靠 `extract_context_pct` 6 regex（`utils/token_tracker.py:20-34`）無從抓 → 恆 0；(2) **SDK**——`sdk_executor_adapter.py:264-282` `_emit_token_pct` 在 `get_context_usage().percentage` 取不到時**靜默跳過**（:276 else 不 emit），真跑全程無 `TOKEN_PCT` log。後果：improving_76~80 共 5 輪宣稱接好的 production token-guard 編排（marker emit + decision），其**最上游 token% 訊號在真跑從未流動**——上輪「production 真跑都是真值了」宣稱經本輪真跑實證不成立。**證據（parent 親跑）**：scratchpad 臨時目錄真跑 `scripts/sdd_bridge_smoke.yaml`（2 步），pty/sdk 各一次皆 `KernelResult(success=True, completed_steps=2, ..., peak_token_pct=0.0)`、真建檔、evaluator pytest exit=0 | **P2**（核心編排機制真跑空轉；**非阻擋**——(a) 每 step `claude -p` 為獨立短上下文、單次爆 context 機率低；(b) SDK 模式有原生 autocompact 兜底，`_verify_act_first` act-first 守門確保排序；屬長期潛伏非本輪引入退化） | **分兩段**：①**載具 fail-loud 護欄本輪即修（W-81-1）**——載具報告層區分「訊號源未產出」vs「真值 0」，杜絕下輪再誤宣稱取到真值；②**根因修復（訊號源接線）routed 下輪**——PTY `claude -p --output-format json` 取 usage 算 context% / SDK `get_context_usage` schema 診斷後接通 percentage（需動 `autoclaude/` infra 生產碼，justified 延後） | **partially-fixed@improving_81（載具護欄）+ routed（訊號源根因）**：證據——`ab_compare_backends.py` 加 `RunMetrics.observer_peak_token_pct`（解析 KernelResult 真值）+ `token_signal_observed` property + `_fmt_token_peak` fail-loud 渲染 +「token 訊號源」狀態列（單輪/多輪 format）+ `AggregateMetrics.token_signal_observed_count`；新測 9（RTM-81-1~4）；受控突變 MUT-81-1/2/3 轉紅還原無殘留；全套 **3449/122/0**、lint 8 kept、LOC 0、snapshot OK；零碰 `autoclaude/` 與 `AISDLC_SDD/`。詳見 improving_81 §4 / ZeroTrust_Audit_81。**▸ improving_82 PTY 支 fixed（訊號源根因端到端閉合，掌舵者裁「修根因」）**：(1) W-82-1 `token_tracker.context_pct_from_claude_json` 從 `claude -p --output-format json` 的 usage÷modelUsage.contextWindow 推算近似 context%（親跑實證 JSON **無** percentage 欄，偵察推測被推翻）；(2) W-82-2 `pty_executor.py` 接 `--output-format json`（parse→`result` 還原答案文字保下游 regex 零退化、emit TOKEN_PCT；fail-loud fallback）+ `ClaudeConfig.output_format="json"`；(3) **W-82-4 真跑揭露的端到端最後一哩**——直接探測 PtyExecutor 已 emit 5.9% 但端到端 `KernelResult.peak_token_pct` 仍 0，查出 Kernel **成功路徑（StepAction.ADVANCE）丟棄 observer.peak_pct**、`KernelResult.success_` 無 peak 參數 → `kernel.py` run() 累積 run_peak + 成功 StepOutcome 帶 observer.peak_pct + success_ 加 peak 參數（additive 零退化）。**真跑鐵證**：上輪/本輪修前恆 0.0 → 修後 **6.2128%**（success=True, 2/2, halted=False）。新測 19、MUT-82-1/2/3 全轉紅 Edit 還原、全套 **3468/122/0**、lint 8 kept、LOC 0、snapshot OK、ci-gate PASS、零碰 `AISDLC_SDD/`。**SDK 支**：W-82-3 `_emit_token_pct` fail-loud 化（percentage 缺失/例外 → warn 不再靜默，盲區可見），**真值接線續 routed improving_83**（需真跑取 SDK `get_context_usage` 真實 schema 後無推測接通）。詳見 improving_82 §4 / ZeroTrust_Audit_82。**▸ improving_83 SDK 支 fixed（real-run-verified，零生產碼改動）**：parent 親跑揭露**原假設被推翻**——(1) SDK `claude_agent_sdk` 0.2.110 `types.py:778` `ContextUsageResponse` schema **本就有 `percentage: float`**，probe 真跑（`scratchpad/probe83_sdk_ctx.py`）回 `percentage=5`/totalTokens=50622/maxTokens=1000000；與 PTY `claude -p --output-format json`「無 percentage 欄」是**兩條不同管道**（SDK 走 control request）；(2) 既有 `sdk_executor_adapter._emit_token_pct:281 usage.get("percentage")` **本就正確**，真跑會 emit TOKEN_PCT 不走 warning 分支；(3) W-82-4 的 Kernel 成功路徑接線已讓 SDK 真值端到端流動——**端到端真跑 `KernelResult.peak_token_pct=2.0`（SDK）/ 6.2006（PTY）**（log 鐵證）。故 improving_81/82 的「SDK 盲區/真跑 0」係從沒真跑過的靜態推測。契約鎖於既有單測 `test_emit_token_pct_emitted_when_percentage_present`。**DEF-81-001 PTY+SDK 雙支至此全閉合**。詳見 improving_83 §2.2/2.3/§6 / ZeroTrust_Audit_83 |
| DEF-82-001 | 2026-06-26 | improving_82 階段三真跑（載具 ab_compare_backends `--run` 兩 backend 跑完、parent 親跑 print 報表時揭露） | **載具報表 fail-loud `⚠` 在 Windows cp950 console print 炸（W-81-1 引入、本輪 dogfooding 真跑揭露）**：`AutoClaude/tools/ab_compare_backends.py` `main()` 真跑兩 backend **已成功跑完並建 log**，但 `print(format_comparison(...))` 渲染 W-81-1 的 fail-loud `"0%（⚠ 訊號源未產出，非真值）"` 含 `⚠`，Windows console 預設 cp950 編碼器無法編 → `UnicodeEncodeError: 'cp950' codec can't encode character '⚠'` 中斷、報表全失。載具 docstring 雖載「cp950 會 mangle 中文/✓ 故讀 log 用 utf-8」，但**未涵蓋 print 報表到 stdout**這條（W-81-1 新引入 ⚠ 非 ASCII）。違反工程紀律第 12 條（真跑「完成」卻在輸出階段靜默炸）| P3（載具觀測層、Windows console-only、非生產碼、非阻擋；CI utf-8 環境不複現） | 載具層即修（可離網）：本輪 **DEF-82-001 fix**——`main()` 開頭 best-effort `sys.stdout.reconfigure(encoding="utf-8")`（非 TextIOWrapper/不支援時靜默略過） | **fixed@improving_82**（證據：`ab_compare_backends.py:main` 開頭 reconfigure utf-8；回歸測 `test_ab_compare_backends.py::test_main_parse_mode_renders_failloud_on_cp950_stdout` 以 `io.TextIOWrapper(BytesIO, encoding="cp950")` fake stdout 主動複現——修前 print ⚠ 即 UnicodeEncodeError、修後 reconfigure→utf-8 正常輸出 rc=0、斷言 rendered 含「訊號源未產出」；scripts/tools 全套綠） |
| DEF-83-001 | 2026-06-27 | improving_83 階段一真跑（端到端 A/B 真跑兩 backend 跑完，parent 親跑載具報表時揭露） | **載具 `ab_compare_backends.py` 把 observer 真實峰值盲報為 0%（[[ab-carrier-production-blind-marker]] 家族再現）**：端到端真跑 `KernelResult.peak_token_pct` PTY=6.2006 / SDK=2.0（log 鐵證），但載具「token 峰值」列兩後端皆印「0%」、同時標「token 訊號源：已觀測」（自相矛盾）。根因（`tools/ab_compare_backends.py:227-234` `_fmt_token_peak`）：訊號已觀測分支印 `m.peak_token_pct`（決策層 marker peak，掃 TOKEN_COMPACT/HALT 行得；smoke 太短未撞 80/90% 門檻 → 無 marker → 恆 0）而非 `m.observer_peak_token_pct`（訊號層真值 6.2/2.0）；`_fmt_agg_token_peak:368` 同病。W-81-1 fail-loud 只分「訊號未產出」vs「真值」兩態，**漏掉第三態**「訊號已產出但未撞門檻（marker=0）」→ 應報 observer 卻誤報 marker 0。驗證腳本 `scratchpad/verify83_carrier.py` 實證 `observer_peak=6.2006 marker_peak=0.0 signal_observed=True ->顯示='0%'` | P3（觀測載具報告層；非生產碼、非阻擋；但會讓下輪誤判「真跑 peak=0」、藏掉真實 A/B token 差異） | 載具層即修（可離網）：`RunMetrics.effective_peak_token_pct=max(observer,marker)` SSOT property + `_fmt_token_peak`/`_fmt_agg_token_peak` 改用 + aggregate 加 effective mean/max | **fixed@improving_83**（W-83-1；證據：6 新測 RTM-83-1~6〔observer-only 報真值 / marker-only 零退化 / 兩者取 max / 真跑 KernelResult peak=2.0 渲染 2% / aggregate effective〕；MUT-83-1（只回 marker）→4 紅、MUT-83-2（max→min）→7 紅、MUT-83-3（_fmt 印 marker）→2 紅，全 Edit 還原；修後對同組真跑 log 顯示 6%/2%〔修前 0%/0%〕；全套 3474/0/122、lint 8 kept、LOC 0、ci-gate PASS。詳見 improving_83 §2.4/§4 / ZeroTrust_Audit_83） |
| DEF-83-002 | 2026-06-27 | improving_83 階段一真跑（核對上輪 §8.2 SDK 宣稱時揭露） | **誠實性缺陷（上輪文件）**：improving_82 §8.2 第 2 點稱「本輪 SDK 端到端真跑仍 peak=0」，但 audit_81/82 **從未做過真正的 SDK 端到端真跑**（audit_82 僅驗 PTY 真跑；SDK「peak=0」係靜態讀碼推測「mock dict 無 percentage → 真跑亦無」）。本輪真跑實證 SDK 端到端 `KernelResult.peak_token_pct=2.0`、`get_context_usage()` 真跑回 `percentage=5`——原宣稱不成立。屬「用未跑的真跑結果當事實」（違 [[no-fabricated-tool-output]] / zero-trust） | P3（文件誠實性；無程式行為錯誤；但誤導後輪認知） | 文件即修：improving_83 §2.5 訂正 + 帳本載明 + 以 §2.2/2.3 真跑鐵證取代推測 | **fixed@improving_83（訂正）**（證據：improving_83 §2.5 + 本列；DEF-81-001 SDK 支以真跑驗證閉合，終結 5 輪推測） |
| DEF-84-001 | 2026-06-27 | improving_84 階段三 W-84-3 真跑 dogfooding（compact 調低門檻真跑 compact=1% 竟不觸發，parent 親跑查 log + policy 根因） | **compact 動態門檻 decay floor=65 硬寫值默默夾掉 config 低門檻（config 契約違反）**：`autoclaude/plugins/token_guard/thresholds.py:get_dynamic_compact_threshold` 的 `floor=65.0` 為函式預設值、`policy.py:83-86` 呼叫時**未從 config 傳入**，故 `compact_threshold_pct` 設定**低於 65 時被 `max(base - decay, 65)` 默默夾到 65**。真跑鐵證：config `compact_threshold_pct=1.0`、`halt_threshold_pct=99.0`、真跑觀測 peak 6.2% ≥ 1% 卻 **compact_count=0、TOKEN_COMPACT 從未出現**（`completed 2/2 success、halted=False`）；halt 路徑（`should_halt_decision` 直接 `token_pct >= halt_threshold`）無此 floor → 同組調低門檻（halt=1.0）halt 真跑正常觸發（TOKEN_HALT ×11、halted=True）。此即 compact 編排無法以「調低門檻真跑」cheap 驗證之根因，亦為 production 端 `compact_threshold_pct` config 契約被硬寫值部分忽略之缺陷（影響任何設 compact 門檻 < 65 的部署；production 預設 80 不受影響） | P3（僅影響 base < 65 的低門檻 config；production 預設 base=80 行為正確、無退化；非阻擋） | 整合層（AutoClaude 生產碼）缺陷 → 本輪 C 軌**當場 surgical 修**（非 SDD 本體，免 Copy-on-Evolve） | **fixed@improving_84（W-84-4）**：`thresholds.py` `effective_floor = min(floor, base_threshold)`——decay floor 不得高於 base，honor config 低門檻；`base ≥ floor`（production 預設 80）為 no-op（min(65,80)=65，回傳值逐位相同＝零退化）。證據：`test_dynamic_threshold_low_base_honored_not_clamped_to_floor`（base=1→1、base=50→50、base=80 仍 65 迴歸守衛）+ MUT-84-1（還原 min→floor）轉紅；修後同組 compact 真跑 **TOKEN_COMPACT ×2、completed 2/2 success**（端到端解除）；既有 base=80 測試 `test_token_guard_plugin.py:58-66` 全綠不變；全套 3488/0/122、lint 8 kept、LOC 0、ci-gate PASS。詳見 improving_84 §4.2/§6 / ZeroTrust_Audit_84 |
| DEF-87-001 | 2026-06-27 | improving_87 階段三 W-87-1 首次真跑（mock brain × 真 Claude correction 閉環，parent 親跑查 log 根因） | **self-correction 與 `expected_output_regex` 閘的交互缺陷（regex-gated step 修正後無法收斂）**：Kernel 套用 Brain 修正時把 `task.prompt` **整個取代**為 `correction_prompt`（`core/kernel.py:259-260`），原 prompt 內「請輸出 keyword `[IMPL_DONE]`」要求隨之消失；若該 step 同時以 `expected_output_regex` 把關，修正後 claude 改好 code 卻不再複述 keyword → regex 永遠失敗 → 每次重試都再觸發 CORRECTION → 耗盡 max_retries → ESCALATION。真跑鐵證（首跑）：S02 同設 regex `[IMPL_DONE]`+evaluator，CORRECTION ×3（attempt 1/2/3）後 `KernelResult(success=False, reason="[S02] 輸出未符合期望 regex", escalated=True)`，雖 calc.py 早已被修對（evaluator 本會過）。mock 修正 prompt 為泛用模板（不複述 keyword）；真 Minimax `build_correction_message` 雖收到 `expected_regex` 為輸入，是否複述 keyword 取決於模型、無保證 | P2（影響任何「regex + evaluator 雙閘且 maintain_context」step 在 Brain correction 下的收斂性；可致誤判 escalate；smoke 端本輪已緩解，production 行為需決策） | **smoke 端即修**（W-87-1：S02 移除 `expected_output_regex`，以 evaluator_command＝pytest 為唯一權威閘，閉環即收斂）；**production 端 routed 待決策**——是否由 Kernel 在套用 correction 時自動保留/重附 `expected_output_regex` 要求（vs 視為 Brain 職責），牽涉 correction 語意廣泛改動，列下輪 C 軌 W 項候選供掌舵者裁 | **fixed@improving_88（production Kernel 修復）**：掌舵者 2026-06-27 裁示選項 A「Kernel 自動保留 regex 約束」。改 `core/kernel.py`：新增 `_preserve_output_contract(task, correction_prompt)`——套用 Brain CORRECTION 時若 step 有 `expected_output_regex` 則確定性把該約束回填修正後 prompt（無 regex/已含則原樣回傳，零退化+冪等）；line 260 改走此 helper。證據——`tests/core/test_kernel.py::TestKernelCorrectionPreservesRegex` 5 測試（RTM-88-1 收斂/88-2 prompt 含 regex/88-3 無 regex 零退化/88-4 冪等/88-5 不累積）全綠；MUT-88-1 受控突變（還原賦值）使 RTM-88-1/88-2 轉紅證有牙；全套 3519 passed/0 failed（詳見 improving_88 §4）。**真模型品質驗證@improving_90（W-90-1）**：以真 Minimax M2.7 × 真 Claude 對「S02 同掛 `expected_output_regex`+`evaluator_command`」的 `scripts/correction_regex_smoke.yaml` 端到端真跑，**首跑即綠**——log 鐵證真 Minimax 修正主體只談「改 `return a*b`」、由 Kernel `_preserve_output_contract` 補回 regex 約束（emit `=== REGEX CONTRACT PRESERVED | step=S02 ===`，W-90-2 新增 marker），attempt 1 雙閘並存收斂 `KernelResult(success=True, escalated=False, peak_token_pct=9.49)`；88 輪 FakeBrain 單元測修復取得真模型品質佐證（載具退碼 0 / CORRECTION=1 / REGEX CONTRACT PRESERVED=1）。〔smoke 端緩解史料：improving_87 S02 移除 regex 改以 pytest 為唯一閘〕 |
| DEF-87-002 | 2026-06-27 | improving_87 階段三 W-87-1 真跑（parent 親跑見 autoclaude 子程序末段 stderr「Logging error」） | **autoclaude 生產 logger console handler 在 Windows cp950 撞非 ASCII 崩潰（非致命、噪音、遮蔽該行 console 輸出）**：`autoclaude/utils/logger.py` `setup_logger` 的 console `StreamHandler(sys.stdout)` 於 Windows 預設 cp950 編碼；`main.py:140` `logger.info("Playbook 結束 | %s", result)` 的 `result` repr 含 `step_log` 的 `✓`(U+2713) → `UnicodeEncodeError: 'cp950' codec can't encode character '✓'`。**非致命**（logging 模組自捕、autoclaude 仍 exit 0；utf-8 file handler `autoclaude.log` 完整記錄該行，故 carrier 讀檔解析不受影響）。屬 DEF-82-001 同 cp950 家族，但 DEF-82-001 只修了載具 `ab_compare_backends`、**未修生產 logger** | P3（Windows console-only、非致命、不影響檔案 log 與下游解析；CI utf-8 環境不複現） | 整合層（AutoClaude 生產碼）缺陷 → **routed 下輪 C 軌候選**（justified defer：非致命 + pre-existing + 修法 `sys.stdout.reconfigure(errors="backslashreplace")` 牽涉全域 stdout 行為改動，需獨立測試設計與決策，不宜夾帶 correction-loop 輪擴大 blast radius；沿 DEF-82-001「載具修、生產 routed」前例） | **fixed@improving_89（C 軌生產修復）**：掌舵者 2026-06-27 裁示鎖定本缺陷為 89 輪主 W 項。改 `autoclaude/utils/logger.py`：**否決 routed 原案的 `sys.stdout.reconfigure`**（全域副作用 + pytest capture 無 `.buffer`/不可 reconfigure 環境會炸），改採更外科手術的**自訂 `_EncodingSafeStreamHandler`**（StreamHandler 子類，`emit` 依目標 stream 編碼做 `backslashreplace` sanitize）——僅隔離在 autoclaude logger 自己的 console handler、不動全域 stdout；utf-8 環境 sanitize 無損（零退化）、純函式冪等。`setup_logger` 改用該 handler（1 行）。證據——`tests/test_logger.py` 7 測（**原無任何 logger 測試**）：RTM-89-1 cp950 不丟例外不觸發 handleError / 89-2 不可編碼字元 backslash escape 整行不遺失 / 89-3 utf-8 位元級無損 / 89-4 cp950 可編碼中文不誤 escape / 89-5 setup_logger 用安全 handler 且去重註冊冪等 / +2 except 兜底補測（broken stream 走 handleError 不上拋、RecursionError 須上拋不被吞）；MUT-89-1 受控突變（`safe=msg` 移除 sanitize）使 RTM-89-1/89-2/89-4 轉紅、stderr 印真實 cp950 `UnicodeEncodeError`（病灶重現）證有牙，Edit 還原（非 git checkout）。三鏡審查當場修 Architect P2（emit 補 `except RecursionError: raise` 鏡像 CPython 語意）。全套 3526 passed/0 failed（+7）、lint 8 kept、LOC 0、ci-gate PASS。詳見 improving_89 §3.2/§4 / ZeroTrust_Audit_89 |
| DEF-87-003 | 2026-06-27 | improving_87 階段三 W-87-1 首次真跑（本輪新建載具 correction_loop_verify 自身 dogfooding） | **新建載具 cp950 stdout print 崩潰（DEF-82-001 同家族、本輪自犯）**：`tools/correction_loop_verify.py` 報表 print 含 `≥`(U+2265)/`✅`，經 Bash `| tail` pipe 時 stdout 為 cp950 → `UnicodeEncodeError` 中斷報表（真跑機制已成功、僅輸出階段炸，違工程紀律第 12 條） | P3（載具觀測層、Windows console-only、非生產碼、非阻擋；本輪新建即犯即修） | 載具層即修 | **fixed@improving_87**：`main()` 開頭 best-effort `sys.stdout/stderr.reconfigure(encoding="utf-8")`（不支援時靜默略過）+ 報表改 ASCII（`>=`/`OK`/`WARN`）雙保險。複跑報表正常輸出 rc 0/1 |
| DEF-87-004 | 2026-06-27 | improving_87 階段三 W-87-1 第二次真跑（mock /stats.post_count 回 None，parent 親跑 netstat 揪殘留 server） | **新建載具 subprocess mock server 在 Windows `terminate()` 殺不乾淨 → 殘留 server 佔埠 9100 → 後續真跑 /stats 對到殘留/失敗 server 致 post_count 失準（None）**：carrier 原以 `subprocess.Popen` 起 `mock_brain_server.py --port 9100`；首跑 crash（DEF-87-003）後 finally 的 `terminate()` 未殺死（netstat 證 PID 23324 仍 LISTENING），次跑新 Popen bind 失敗、health 卻連到殘留 server，`urllib /stats` 對埠失準回 {} → post_count=None。新鮮 server（9123）/stats 邏輯本身正確（post_count=1 實證） | P3（載具觀測層、Windows-only、非生產碼、非阻擋；本輪新建即犯即修） | 載具層即修 | **fixed@improving_87**：carrier 改用**同程序 threaded server**（`ThreadingHTTPServer("127.0.0.1", 0)` 自動選埠 + daemon thread），免 subprocess 殺不掉、免埠衝突；post_count 直接讀模組 `_STATS`（同程序）免 HTTP 對埠。修後 mock 連 3 跑 post_count=1 穩定 |
| DEF-90-001 | 2026-06-27 | improving_90 階段四零退化取證（parent 親跑全套，W-90-2 後 3529 vs fresh 3535 差 6 揭露） | **stale `.pytest_cache` 致全套少報 6 個 collect（驗證數字不可信）**：MUT-90-1 受控突變留下 `.pytest_cache`（lastfailed/nodeids）異常狀態，W-90-2 後全套報 `3529 passed`；清 `.pytest_cache/.hypothesis/checkpoints/logs` 後 fresh 全套穩定 `3535 passed`（`--collect-only` 3651→3657）。屬「舊 cache 騙過驗證」（對應 AutoClaude CLAUDE.md Nightly 紀律 #7「cache 路徑強制 fresh」家族）。權威 fresh 取證：同 session `git stash` HEAD=**3532 passed**、本輪=**3535 passed**、`--collect-only` diff 精確 3 行（皆本輪 RTM-90-1/2/3 新測）→ 本輪嚴格 **+3 零退化** | P3（驗證流程/誠實性；非生產碼、非阻擋；但若採信 stale 瞬時數會誤判退化或數字漂移） | 流程紀律即正（本輪取證即發現即正，非框架/生產碼缺陷） | **fixed@improving_90**：階段四改以 fresh cache（跑前清 `.pytest_cache/.hypothesis/checkpoints/logs`）重跑確立權威數字；引用 pytest 數字前須清 cache 重跑、禁採信 stale 瞬時數。實證再現既有 Nightly #7 並入帳，供後輪 audit 鏡引以為戒 |
| DEF-91-001 | 2026-06-27 | improving_91 階段一 zero-trust 重偵察（embedder 設定治理偵察時旁及 chat config dataclass） | **MinimaxConfig dataclass 預設值滯後 config.yaml**：`autoclaude/utils/config.py:14-15` 預設 `base_url="https://api.minimax.chat/v1/text/chatcompletion_v2"`、`model="MiniMax-Text-01"`，但 `config.yaml` 當前值為 `https://api.minimax.io/.../chatcompletion_v2`、`MiniMax-M2.7`（improving_90 commit 6daa540 只改 config.yaml、未同步 dataclass 預設）。實證：`AppConfig.model_validate({"minimax": {}})` → `base_url=...minimax.chat...` / `model=MiniMax-Text-01`（舊端點/舊 model）。後果：config.yaml 缺 minimax 欄位時 fallback 到舊端點 → 設定漂移（90 輪 base_url 漂移的 dataclass 端殘留） | P3（漂移風險；config.yaml 有覆寫時不爆，缺欄位才現） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-91-3 修 | **fixed@improving_91**（W-91-3，見計畫書 §3.3） |
| DEF-91-002 | 2026-06-27 | improving_91 階段一 zero-trust 重偵察（embedder adapter 設定讀取面） | **`.env.example` 宣告 `MINIMAX_EMBED_MODEL` 但 adapter 從未讀取**：`.env.example:54` 列 `MINIMAX_EMBED_MODEL=embo-01`，但 `autoclaude/infra/adapters/minimax_embedder.py:38,51` `model_id` 為簽章硬編預設 `"embo-01"`、`__init__` 內**無 `os.environ.get("MINIMAX_EMBED_MODEL")`**——文件宣稱可配置、實則不可（功能上 embo-01 恰等於 .env 範例值故未爆）。對比 base_url/dimension 皆有 env 讀取，model 漏接 | P3（文件 vs 實況不符；宣稱可配置實則硬編） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-91-2 修（補 env + config 兜底） | **fixed@improving_91**（W-91-2，見計畫書 §3.3） |
| DEF-91-003 | 2026-06-27 | improving_91 階段一 zero-trust 重偵察（ConfigResolver RBAC vs AppConfig schema 對照） | **ConfigResolver RBAC 保護幽靈欄位**：`autoclaude/utils/config_resolver.py:49`（`_PROTECTED_FIELDS` frozenset 起 :47）含 `"embedder.api_key"`（設計者預期 AppConfig 有 embedder 區塊），但 `AppConfig`（config.py）**無 EmbedderConfig**。實證：`AppConfig.model_validate({"embedder": {...}})` → `hasattr(c, "embedder")=False`、`extra=ignore`。後果：(a) `embedder.api_key` RBAC 保護無效（保護不存在欄位）；(b) 使用者在 config.yaml 寫 `embedder:` 區塊被 Pydantic 靜默丟棄 | P3（設計意圖 vs 實作缺口；RBAC 形同虛設 + 設定靜默丟棄） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-91-1 補齊 EmbedderConfig 使 RBAC 槽位生效 | **fixed@improving_91**（W-91-1，見計畫書 §3.3） |
| DEF-92-001 | 2026-06-27 | improving_92 階段一 zero-trust 重偵察（bge-m3 TEI adapter 設定讀取面；與 DEF-91-002 同類） | **`.env.example` 宣告 `TEI_MODEL_ID` 但 adapter 從未讀取**：`.env.example:73`（HEAD）列 `TEI_MODEL_ID=BAAI/bge-m3`，但 `autoclaude/infra/adapters/bgem3_local.py:37`（HEAD）`model_id` 為簽章硬編預設 `"BAAI/bge-m3"`、`__init__` 內**無 `os.environ.get("TEI_MODEL_ID")`**——文件宣稱可配置、實則不可（功能上 BAAI/bge-m3 恰等於範例值故未爆）。對比 base_url 有 env(TEI_URL) 讀取，model 漏接。git 鐵證見 ZeroTrust_Audit_92（SA-SD `git show HEAD`） | P3（文件 vs 實況不符；宣稱可配置實則硬編） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-92-2 修（補 TEI_MODEL_ID env + config 兜底） | **fixed@improving_92**（W-92-2 + MUT-92-1 驗牙，見計畫書 §3.2/§4.2） |
| DEF-92-002 | 2026-06-27 | improving_92 階段一 zero-trust 重偵察（bge-m3 TEI adapter 設定讀取面） | **`.env.example` 宣告 `TEI_EMBED_DIMENSIONS` 但 adapter 從未讀取**：`.env.example:75`（HEAD）列 `TEI_EMBED_DIMENSIONS=1024`，但 `autoclaude/infra/adapters/bgem3_local.py:38`（HEAD）`dimension` 為簽章硬編預設 `_DEFAULT_DIM`(1024)、`__init__` 內**無 `os.environ.get("TEI_EMBED_DIMENSIONS")`**——同 DEF-92-001 類文件 vs 實況不符 | P3（文件 vs 實況不符；宣稱可配置實則硬編） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-92-2 修（補 TEI_EMBED_DIMENSIONS env〔`.isdigit()` 檢核〕+ config 兜底） | **fixed@improving_92**（W-92-2 + MUT-92-2 驗牙，見計畫書 §3.2/§4.2） |
| DEF-92-003 | 2026-06-27 | improving_92 階段一 zero-trust 重偵察（bge-m3 adapter 兜底鏈 vs config.yaml 治理一致性） | **bge-m3 TEI 非機密設定游離 config.yaml 之外**：`bgem3_local.py:44-48`（HEAD）兜底鏈僅 2 層（建構參數 > env(TEI_URL) > 硬編），**無 config 層**；config.yaml 的 `embedder:` 區塊（91 輪建立）只含 Minimax 欄位、無 bge-m3 欄位——與 91 輪「embedder 非機密設定收進 config.yaml」治理不一致（方案 B 未涵蓋 bge-m3）。TEI 為本地容器、全非機密（無 api_key/帳號識別），故無 RBAC 需求 | P3（治理一致性缺口；非機密設定無集中權威源） | 整合層（AutoClaude 側）→ 本輪 C 軌 W-92-1 補 EmbedderConfig bge_m3_* 欄位 + config.yaml + adapter config 層 | **fixed@improving_92**（W-92-1/W-92-2，見計畫書 §3.2/§4） |
| DEF-25-001 | 2026-06-17 | improving_25（B 軌 meta⁸ 視覺化飽和認定） | FSM-STATE 視覺化反射觸 R-9.37.4 邊界（視覺化模組不得篡改 meta⁸ 既有合約、反射狀態為唯讀邊界） | P2 | wontfix（by-design 邊界，非缺陷） | **wontfix**（R-9.37.4 設計邊界；歷史脈絡見 `AutoSDD_Defect_Log_archive_01.md` improving_25 段） |
| DEF-37-001 | 2026-06-18 | improving_37 階段四 CI 平價（Copy-on-Evolve v0.15 潔淨度 `git add -A -n` 查證） | Copy-on-Evolve 新版 `.gitignore` block 缺漏**無自動偵測**：建新版後 ci-gate 生成 runtime 產物（arch-fitness.json + build/reports/*），per-version 排除 block 未涵蓋新版 → would-add 夾帶 cruft，須手動補 block | P3 | RFC + v0.0(X+1)（DEF-23-005 結案時明文 deferred 之 Copy-on-Evolve 後置自動化家族） | **fixed@improving_43**（B 軌·結構性自動偵測落地：improving_43 新增 shared-infra `AISDLC_SDD/scripts/gitignore_coverage_lint.py` + 12 case + ci-gate 接線，結構性結案；歷史脈絡見 archive_01 improving_37 段。與 DEF-59-001〔.gitignore block 自動化〕同家族。**本輪審計校正**：初遷誤判 routed，Architect/SA-SD 雙鏡核源原 L290 權威狀態更正為 fixed@improving_43） |
| DEF-42-001 | 2026-06-21 | improving_42 階段一 Re-Audit（ci-gate v0.17 pytest） | 框架測試 flaky（Windows 並行檔案鎖）：`v0.17/tools/fsm_runtime/tests/test_file_lock.py::test_parallel_writes_do_not_lose_increments` full-suite 並行下間歇 PermissionError，ci-gate v0.17 偶報 1 failed；主 agent 隔離 3/3 全綠 → 確認**非回歸** | P3 | routed（flaky 隔離追蹤、非回歸；框架本體 RFC） | **routed**（archive_01 improving_42 段；非回歸 flaky） |
| DEF-42-003 | 2026-06-21 | improving_42 W-42-1 設計期（負向狀態碼跨路徑搶救被既有哨兵測試攔下而揭露） | 引號與負向狀態碼共存時負向被丟棄＝under-specify（引號路徑早 return、負向不評估）；屬 W-32-1 刻意設計決策 + 哨兵 `test_quoted_wins_over_negative_status` 鎖定 | P3 | wontfix（by-design，surface 供掌舵者未來輪決策） | **wontfix+by-design**（archive_01 improving_42 段；哨兵鎖定、非缺陷） |
| DEF-52-006 | 2026-06-23 | C 軌 improving_52（SA 鏡 FIND-4） | stderr 中文 UnicodeEncodeError（理論）：`_init_utf8_streams()` 僅 `__main__` 呼叫；ascii-strict console 直接 import 呼叫會拋。生產 wiring 經實證皆 subprocess→`__main__` 必走→風險為零 | P3 | wontfix（生產路徑必為 subprocess） | **wontfix+理由**（archive_01 improving_52 段；生產必走 subprocess、風險零） |
| DEF-53-001 | 2026-06-23 | C 軌 `.claude` 審查輪八（SA 鏡 P3-2） | `hub_merge.py` `_read_yaml`/remote→local copy 未 cap 讀 pull-cache yaml；`detect_conflict`/`resolve_or_record` 經查僅測試呼叫、未在任何 runtime 路徑 wire → 無 runtime 消費者、無活路徑可觸發（latent） | P3 | RFC（框架本體 `tools/fsm_runtime/`，`build/planning/active/SDD_improving_Automation_NN.md`） | **routed**（latent；archive_01 審查輪八段；**註**：與 DEF-CLDREV-030〔hub_sync yaml 大小上限 fixed@v0.20〕同家族，本項 hub_merge 路徑仍 latent） |
| DEF-62-001 | 2026-06-25 | improving_62 起多輪（auto_recovery call-site 註解 doc-lag） | `fsm_runtime.py` auto_recovery call-site 註解「預設 OFF」滯後（feature 自 v0.22/improving_57 翻預設 ON，call-site 註解未同步）——純註解 doc-lag、零執行語意問題 | P3 | Copy-on-Evolve v0.0(X+1) 校正註解 | **fixed@improving_98**（v0.30 校正 `fsm_runtime.py:420-421` 註解極性為「v0.22 起預設 ON」、與 L39/L51 一致；v0.29 凍結本體留 stale 為歷史快照。**本列校正帳本 stale**：archive_01 improving_63 段仍記「維持 open」係 improving_98 修復前之過時敘事） |
| DEF-99-001 | 2026-06-30 | improving_99 階段一/二（缺陷帳本讀取摩擦，B 軌 dogfooding） | `AutoSDD_Defect_Log.md` 累積至 711 行 / 468KB（中文長列）超過 Read 工具 256KB 上限 → zero-trust 審計無法一次讀完；且 open/routed 追蹤散落各輪敘事、主表非完整 SSOT（DEF-31~58 等孤兒無主表列） | P3 | ops 治理層（本輪 B 軌就地修） | **fixed@improving_99**（歷史敘事歸檔至 `AutoSDD_Defect_Log_archive_01.md` + `_archive_02.md`〔原 93-711 行逐字拆兩檔、各 < 256KB、只增不刪〕+ 主表補 7 個孤兒列〔5 未結：DEF-25-001/42-001/42-003/52-006/53-001；2 已結補全/校正：DEF-37-001 fixed@43、DEF-62-001 fixed@98〕成完整 live SSOT + 歸檔輪替政策〔主檔與單一 archive 皆以 < 256KB 為界〕；主檔 468KB→124KB 可一次讀。DEF-37-001 誤遷經 Architect/SA-SD 雙鏡 audit 當輪校正） |
| DEF-100-001 | 2026-06-30 | improving_100 階段一 zero-trust（C 軌 SD_09 W1 觀察期偵察） | SD_09 觀察期文件 stale：`AutoClaude/CLAUDE.md` SD_Improving_09 摘要停在 R51（2026-06-01），`G0 最遲 2026-06-26` 已逾期未更新；觀察期狀態過期（`.mutation_history.jsonl` tail7 已 **3 unique sha**〔20940e1b/4af78567/55013d0a〕非舊記 2、kill_rate 因 improving_79/80/84 改 `token_guard/` 源碼自 0.7651 降至 **0.6956**〔survived 35→42〕逼近 0.68 effective threshold〔僅餘 1.56pp〕）；偵察期 Explore agent 誤判「~6/29 到期」，實為**源碼演進閘門、無日曆到期日**（ADR-SD09-009 §11.6 PM 拍板 v1.2） | P3 | C 軌本輪即修（doc 校準） | fixed@improving_100（證據：`AutoClaude/CLAUDE.md` SD_Improving_09 段校正到 2026-06-30 R-100 現況〔tail7=3 unique sha、kill_rate 0.6956≥0.68、source-sha 為源碼演進閘門無日曆到期、G0 已逾期待 PM 後續決策〕；`AutoSDD_improving_100.md` §2.2 五點真相鐵證） |
| DEF-100-002 | 2026-06-30 | improving_100 階段一 mutation backlog 分析（查 `.mutmut-cache` sqlite） | `autoclaude/plugins/token_guard/thresholds.py` `should_compact_decision` L49 `if in_correction_loop and correction_history_len <= 1: return token_pct >= threshold` 為**死分支**——到達 L49 時 `token_pct >= threshold` 已恆成立（L48 已 return False 攔掉 `<`），故該分支 ≡ `return True` ≡ 下方 `return True`；因此 `.mutmut-cache` #122-124（`and`→`or`、`<=`→`<`、`1`→`2`）皆為**等價變異**，補測試殺不掉、非真測試缺口（受控突變 EQUIV-CHECK 證實 GREEN） | P3 | routed 下輪評估重構移除冗餘分支（改源碼需驗證對齊 `PlaybookRunner._should_compact_now` 契約，本輪守 Rule 2/3 不動源碼） | routed（improving_100 已加等價性實證測試 RTM-100-5〔`TestShouldCompactL49DeadBranchEquivalence`，parametrize 證 token_pct≥threshold 時恆 True 不受 correction 參數影響〕pin 住死分支語意並保護未來重構；重構移除待下輪） |
| DEF-101-001 | 2026-06-30 | improving_101 階段一/設計（C 軌 SD_09 W1 鎖定機制 zero-trust，掌舵者質疑「空轉一個月是好設計嗎」） | **SD_09 W1 mutation 鎖定判準把「源碼演進證據」綁死成「日曆天數」**：`mutation_baseline_lock.py:197-203` M-05 去重鍵＝同 UTC 日期（不看 sha）+ `run_local_nightly.ps1:25` schtasks `/SC DAILY` + `autoclaude-ci.yml` cron 每日一次 → unique sha 每日最多 +1、7 個 unique sha 至少需 ≥7 日曆天、idle 日重複 sha 稀釋 tail7 → 鎖定空轉數週至一月。mutation test 確定性使「每日重測同 sha」零信心增益；原防單日抖動目的（ADR-SD08-002 §2.4）已被 unique sha + ±2pp tolerance（ADR-SD09-009 §5.5）+ `compute_consistency_warning`(L265) 三機制取代 → 日曆綁定為無防護目標的遺留懲罰 | P2 | 框架程式缺陷 RFC → **ADR-SD09-011**（PM signoff 2026-06-30）→ improving_101 落地 | fixed@improving_101（證據：`mutation_baseline_lock.py` 去重鍵 UTC日期→`source_sha256`〔`_dedup_key` sha 優先/date fallback〕+ `compact_history_by_sha` 方案A壓縮〔真實 30筆→6筆/4 unique sha 不假鎖定〕+ `--migrate-compact-sha` 子命令；觸發解耦＝新 `autoclaude-mutation-on-change.yml`〔現名；源碼變動觸發〕+ pre-push hook opt-in 偵測 + nightly 角色轉監控；受控突變 MUT-101-1 驗紅；反作弊強度不減〔ADR §3 對照〕） |
| DEF-101-002 | 2026-06-30 | improving_101 結案後掌舵者維運指示（「所有 Windows Schedule task Jobs 都需喚醒啟動才作用；避免排程失敗」） | **schtasks 排程缺「喚醒啟動」→ 機器睡眠/休眠時 nightly 漏跑**：`tools/fix_nightly_catchup.ps1` 設了 `StartWhenAvailable`（關機補跑）但**只印不設 `WakeToRun`**（原 L27 僅 Write-Output 檢查值）→ 機器睡眠/休眠時 02:00 排程到點不主動喚醒執行 → 觀察期 idle 漏跑（與「schtasks 漏跑日高度敏感」同源；schtasks.exe `/create` 預設 WakeToRun=false + StartWhenAvailable=false） | P2 | ops 加固（C 軌，improving_101 維運跟進，非完整 improving 輪） | fixed@v2026.06.30-53（ops hotfix；證據：`fix_nightly_catchup.ps1` 補 `$t.Settings.WakeToRun=$true` + 成功判定納入 WakeToRun + powercfg 喚醒計時器提示〔四面完整覆蓋：StartWhenAvailable 關機補跑/WakeToRun 睡眠喚醒/DisallowStartIfOnBatteries=false 電池不擋啟動/StopIfGoingOnBatteries=false 電池中途不殺——後者為掌舵者實機跑出 StopIfGoingOnBatteries=True 揪出之第 4 中斷風險〕；`reschedule_g0_gatecheck.ps1` 同補 WakeToRun + StopIfGoingOnBatteries；`run_local_nightly.ps1` .NOTES 補「建後務必 elevated 跑 fix_nightly_catchup.ps1」提示；3 檔 PowerShell parse 0 errors、BOM 正確〔ASCII 檔無 BOM、中文檔有 BOM〕。⚠ 實際套用需**實機 elevated** 跑 `fix_nightly_catchup.ps1`，本 session 僅驗腳本語法/設定值正確、無法驗 schtasks 實機喚醒行為） |
| DEF-101-003 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 B zero-trust 盤點（凍結版 ps1 BOM 普查） | **凍結版 ps1 無 UTF-8 BOM 共 47 支（known-gap 豁免）**：`AISDLC_SDD_v0.01~v0.29/tools/fsm_runtime/formal/run_tlc.ps1`（29 支）+ `v0.12~v0.29/tools/install_hooks/install_post_commit.ps1`（18 支）含非 ASCII 且無 BOM，zh-TW Windows PowerShell 5.1 直跑會 parser 斷裂；v0.30 對應兩檔本輪已補 BOM，`scripts/ci-gate.ps1` 同輪補齊 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝改用 v0.30 對應檔；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-004 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D macOS 實測 | **凍結版 `verify_traceability.sh` 用 `declare -A`（bash 4+ 關聯陣列）→ macOS 內建 bash 3.2 必炸**（v0.01~v0.29 全數；v0.30 本輪已改 bash 3.2 相容） | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（緩解＝用 v0.30 同檔，或 `brew install bash` 後以 bash 4+ 執行；v0.30 已 fixed@跨平台修復輪） |
| DEF-101-005 | 2026-07-09 | 跨平台修復輪（macOS ⇄ Windows）修復 D 審視 `verify_traceability.sh` 相容化時發現既有缺陷 | **`verify_traceability.sh` 的 `set -euo pipefail` + `grep` 無匹配（rc=1）→ 腳本提前靜默退出**：任一 grep 掃描零命中即中止整支驗證、不報錯不輸出後續檢查結果（驗證鏡子靜默失真，違取證紀律 #4「驗證鏡子自身要被驗證」）；**所有 bash 版本皆然、v0.30 亦未修**（本輪 bash 3.2 相容化僅處理 `declare -A`，未動 pipefail/grep 行為） | P2 | 框架程式缺陷 RFC + v0.0(X+1)（grep 補 `\|\| true` 或顯式處理無匹配分支，並補「零命中場景」單元測試） | open（第五輪 Architect 複核：處置路徑〔軌道② RFC〕站得住，但入帳三日 RFC 回流尚未啟動——下輪 B 軌點名確認落地） |
| DEF-101-006 | 2026-07-10 | 跨平台修復輪四方複審（SA 複審新發現 P2-N2） | **ruff 未鎖版 → lint 結果跨機器漂移**：`AutoClaude/pyproject.toml` dev extras 的 ruff 無版本上限，fresh bootstrap 裝到 ruff 0.15.20 對全倉 `ruff check .` 報 1,339 筆既有 issue（E501/I001/UP045 等，遍布 HEAD 既有檔案）；pre-commit hook 只 lint staged 檔、CI 無 ruff step 故不紅燈，但根 CLAUDE.md 教學指令 `ruff check .` 在新環境滿版報錯，且與「地端=CI 同版」精神不符（本輪已將被觸碰 6 檔修至全綠） | P2 | dev extras 鎖定 ruff 版本（如 `ruff==0.x.y`）+ 擇期分批清理存量或調整教學指令範圍 | fixed@四方複審第三輪修復（2026-07-10；證據：`AutoClaude/pyproject.toml:25,27` dev extras 鎖 `pytest==9.0.3`＋`ruff==0.15.21`〔pytest 與 `AISDLC_SDD/AISDLC_SDD_v0.01/requirements-ci.txt` 同版，一併消除 fresh bootstrap「先裝 9.1.1 再降版」的安裝順序依賴〕；重裝後實測 full pytest 基線 3,529/181 不變、lint-imports 8 kept、ruff baseline 1,339 筆不變＝跨機器漂移根因〔未鎖版〕已除；升版規則比照 requirements-ci.txt 本機先升跑全綠再回寫釘選。**殘餘帳目拆分**：存量 1,339 筆 ruff baseline 待分批清理 → **DEF-101-018** 續追） |
| DEF-101-007 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；隔離 victim repo PoC 實證） | **linked worktree push 時 GIT_DIR/GIT_WORK_TREE 洩漏進 pytest 子行程 → 真 git fixture 改操作「使用者真 repo」**：git 對 pre-push hook 注入絕對路徑 `GIT_DIR`（hook 又補 `GIT_WORK_TREE`），傳染進閘門 pytest 的真 git fixture（tmp repo 內 git init/add/commit）後 cwd=tmp 被 env 覆寫——髒工作樹時把使用者 WIP 偷 commit 上真分支、乾淨樹時 fixture 炸掉致閘門假紅；QA 複審終裁輪另實證**破壞面更廣**：敵意 env 下 fixture 的 `git init` 經 commondir 把 `core.worktree=<worktree路徑>` 寫入主庫 `.git/config`，主 checkout 自此所有 git 操作被重定向 | P0 | 本輪即修（雙層防禦：hook 呼叫點 env 清洗＋fixture 層自清） | fixed@四方複審第三輪修復（證據：hook 層 `AISDLC_SDD/.githooks/pre-push:25`〔DEF-101-026 移除死碼後行號〕、`AutoClaude/tools/git-hooks/pre-push:72,85`〔pytest 與 mutation 呼叫點〕加 `env -u GIT_DIR -u GIT_WORK_TREE`；fixture 層 v0.30 `tools/fsm_runtime/tests/test_closure_evidence.py:24` 與 `AISDLC_SDD/scripts/tests/test_copy_on_evolve.py:42` 增 `_clean_git_env()`〔清 GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE〕；隔離 victim repo PoC 前紅後綠實證。殘餘邊角＝中間凍結版 fixture 與 `_run_git` 繼承 env，帳列 DEF-101-020／DEF-101-022） |
| DEF-101-008 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **根層 dispatcher 對「rename 移出子專案」fail-open**：git rename 偵測使 `diff --name-only` 只列新路徑一條，`git mv AutoClaude/x docs/x` 這類移出子專案的變更舊路徑消失 → 來源子專案閘門靜默漏跑（`ONBOARDING.md` §6「已知縫隙」原僅承認 merge/rebase 繞過） | P1 | 本輪即修（dispatcher diff 停用 rename 偵測） | fixed@四方複審第三輪修復（證據：`tools/git-hooks/pre-commit:40` 與 `tools/git-hooks/pre-push:65` diff 加 `--no-renames`〔rename 拆回 A+D 兩條路徑，舊路徑重新可見照常分流〕；隔離 repo `git mv` 前紅後綠） |
| DEF-101-009 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B1） | **根層 `aisdlc-sdd-artifact-cleanup.yml` 7 天清理會殲滅 AutoClaude 30/90 天取證 artifact**：workflows 上移 monorepo 根層後 artifact 命名空間合一，原邏輯對「整個 repo」刪超齡 artifact——AutoClaude 取證產物（`mutation-reports-*`/`ac4-*` retention 30 天、`perf-baseline-*`/`mutation-history` 90 天）全數 > 7 天門檻，Nightly 取證紀律證據鏈會被排程性斬斷；檔頭註解「對齊各 workflow 的 retention-days」失實 | P1 | 本輪即修（刪除 allowlist 化） | fixed@四方複審第三輪修復（證據：`.github/workflows/aisdlc-sdd-artifact-cleanup.yml:58` `ALLOWLIST_PREFIXES`——僅 `ci-arch-fitness-`/`arch-fitness-findings`/`fsm-chaos-report-` aisdlc-sdd 家族前綴可刪，未命中一律保留〔fail-safe 寧留勿刪〕；檔頭補前綴↔來源 workflow 對照表、失實註解照實改寫） |
| DEF-101-010 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B2） | **`autoclaude-mutation-on-change.yml` 宣稱 unique-sha 累積但 fresh checkout 永不累積**：`.mutation_history.jsonl` 已出庫（`AutoClaude/.gitignore`），CI fresh checkout 每 run 從空 history 起、run 完即蒸發 → ADR-SD09-011 的 7 unique-sha 鎖定閘門在 GitHub Actions 側永不推進 | P1 | 本輪即修（artifact 三段式持久化） | fixed@四方複審第三輪修復（證據：`.github/workflows/autoclaude-mutation-on-change.yml:55` restore〔gh api 下載最新未過期 `mutation-history` artifact，失敗印告警從空 history 起累不 fail；行號含 DEF-101-023 concurrency 增行後〕→ accumulate → `:109` upload〔`if: always()`，history retention 90 天＋本次報告 30 天〕；permissions 補 `actions: read`。殘餘＝90 天 retention 上限，帳列 DEF-101-021） |
| DEF-101-011 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 A3） | **NTFS 敵意檔名零防護**：macOS(APFS) 允許的檔名在 Windows(NTFS) checkout 直接炸掉或靜默碰撞——路徑段含 `< > : " \| ? *`/控制字元、以空白/句點結尾、保留裝置名 CON/PRN/AUX/NUL/COM1~9/LPT1~9、大小寫碰撞（NTFS 不分大小寫互相覆蓋）皆無閘 | P2 | 本輪即修（根層 pre-commit 檔名閘） | fixed@四方複審第三輪修復（證據：`tools/git-hooks/pre-commit:56-135` `_ntfs_seg_bad` 對本次新增（A/C）路徑逐段檢查＋`git ls-files` 大小寫碰撞比對，命中 fail-loud rc=1、取不到新增清單亦擋下；bash 3.2 相容；5 場景實測〔非法字元/保留名/尾隨空白/尾隨句點/大小寫碰撞〕全數攔下） |
| DEF-101-012 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 A4＋B4） | **根層基建自身變更零閘門**：根層 `tools/`（dispatcher hooks／bootstrap／整合閘門）與 `.gitattributes`/`.editorconfig`/`.github/` 過去無任何 pre-commit 檢查與 CI 覆蓋，改壞 dispatcher/bootstrap 只能事後在 hook 爆炸時發現 | P2 | 本輪即修（本機 leg＋CI 雙層） | fixed@四方複審第三輪修復（證據：本機層 `tools/git-hooks/pre-commit:45-48,138-166` 新增根層基建 leg〔變更命中根層基建路徑即對變更到的 `.sh`／無副檔名 hook 檔跑 `bash -n`，失敗 rc=1〕；CI 層新增 `.github/workflows/root-infra-ci.yml`〔bash -n＋pwsh `Parser::ParseFile`＋`git ls-files --eol` EOL 守門，paths 觸發、`contents: read` 最小權限、零外部 action〕） |
| DEF-101-013 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B3） | **dependabot pip 生態漏掃 AutoClaude**：根層 `.github/dependabot.yml` pip 條目原僅涵蓋 AISDLC_SDD，`AutoClaude/pyproject.toml` 相依無自動更新掃描 | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`.github/dependabot.yml:34` 補 `directory: "/AutoClaude"` pip 條目〔weekly，與既有條目同格式〕） |
| DEF-101-014 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **`AutoClaude/tools/local_ci_gate.ps1` 缺 python 前置守門＋`Invoke-Gate` 殘值 fail-open**：未啟 venv 時各 gate 逐一噴錯難診斷；scriptblock 拋例外（如 CommandNotFoundException）不更新 `$LASTEXITCODE`，前一 gate 殘值 0 令本 gate 假 PASS | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`local_ci_gate.ps1:37` 補 `Get-Command python` 前置守門〔與 `.sh` 版 `command -v python` 對稱〕；`:85` `Invoke-Gate` 每次執行前 `$global:LASTEXITCODE=0` 重置＋try/catch 例外明確判 FAIL〔PS 5.1 相容〕） |
| DEF-101-015 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **fsm_runtime v0.30 四處 subprocess `text=True` 無 encoding**：zh-TW Windows 預設 cp950，子程序輸出含中文即 UnicodeDecodeError——`closure_evidence.py` `_run_git`、`sandbox_runner.py` DockerBackend、`tlc_runner.py` TLC 呼叫、`tests/test_closure_evidence.py` `_git` helper 四處 | P2 | 本輪即修（v0.30＋閘門雙層） | fixed@四方複審第三輪修復（證據：v0.30 `closure_evidence.py:96`、`sandbox_runner.py:254`、`tlc_runner.py:94`、`tests/test_closure_evidence.py:39` 補 `encoding="utf-8", errors="replace"`；`AISDLC_SDD/scripts/ci-gate.sh:26`／`ci-gate.ps1:11` 補 `PYTHONUTF8=1`〔對齊 AutoClaude local_ci_gate 同名設定〕。凍結版同病依紀律不回改，帳列 DEF-101-019） |
| DEF-101-016 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **根層 `.gitattributes` `.githooks/*` 為死規則**：含斜線 pattern 錨定根層，而根層無 `.githooks/` 目錄 → 規則永不命中（`AISDLC_SDD/.githooks/` 實際靠子專案自帶 `.gitattributes` 兜底） | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`.gitattributes:24` 改 `**/.githooks/*`＋註解照實〔明示子專案自帶 `*` 兜底規則仍會遮蔽本行、非可靠兜底，各子專案 `.githooks` 行尾政策仍須自身完備〕） |
| DEF-101-017 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **AutoClaude pre-push 失敗提示只印 Windows 指令＋header 單平台語意**：閘門失敗指引僅給 `run_act.ps1`，header 稱「在 Windows 跑本機 Python」，macOS 開發者無從對照 | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`AutoClaude/tools/git-hooks/pre-push:7-8` header 雙平台語意；`:94-95` 失敗提示補 macOS/Linux `bash tools/run_act.sh --job test`＋Windows `run_act.ps1` 雙行） |
| DEF-101-018 | 2026-07-10 | 跨平台修復輪四方複審第三輪（自 DEF-101-006 帳目拆分） | **ruff 存量 baseline 1,339 筆待清理**：鎖版（DEF-101-006 fixed）已消除跨機器漂移，但 `ruff check .` 對全倉仍報 1,339 筆既有 issue（E501/I001/UP045 等，遍布 HEAD 既有檔案）——根 CLAUDE.md 教學指令在新環境滿版報錯的體感問題仍在 | P3 | 擇期分批清理存量或調整教學指令範圍（沿 DEF-101-006 原分流） | open（帳目拆分自 DEF-101-006：漂移根因已除、本列只追存量清理；1,339 筆為鎖版後穩定 baseline） |
| DEF-101-019 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點） | **凍結版 v0.01 subprocess 無 encoding 同病**：`AISDLC_SDD_v0.01/tools/fsm_runtime/sandbox_runner.py:252`、`tlc_runner.py:69` 與 DEF-101-015 同病（`text=True` 無 encoding），zh-TW Windows cp950 下可能 UnicodeDecodeError | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝ci-gate `.sh`/`.ps1` 已設 `PYTHONUTF8=1` 使凍結版子程序同獲 UTF-8；v0.30 對應處已 fixed@DEF-101-015） |
| DEF-101-020 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **v0.12~v0.29 中間凍結版 `test_closure_evidence.py` fixture 未清洗 env**：與 DEF-101-007 同源——中間凍結版測試的真 git fixture 未清 GIT_DIR/GIT_WORK_TREE；惟官方閘門路徑（ci-gate 雙軌＝v0.01＋LATEST）不執行中間版，hook 層 `env -u` 清洗亦已保護，僅剩「人工在中間版目錄帶敵意 env 手跑 pytest」邊角情境 | P3 | 凍結版依紀律不回改；known-gap 記入根層 `ONBOARDING.md` §9 | wontfix+凍結版紀律（known-gap；緩解＝閘門路徑不執行中間版＋hook 層 `env -u` 已清洗；v0.30 已 fixed@DEF-101-007） |
| DEF-101-021 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-010 殘餘） | **mutation artifact 累積鏈 90 天上限**：GitHub artifact retention 上限 90 天——連續 90 天無 token_guard 源碼變動觸發 `autoclaude-mutation-on-change.yml`，鏈上 `mutation-history` 過期 → 累積歸零重累；且 GitHub 側與 Windows 本機 nightly（`run_local_nightly.ps1`）為兩條互不同步的獨立累積點 | P3 | 平台上限如實揭露；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；緩解＝Windows 本機 nightly 為另一獨立累積點；限制已照實寫入 workflow 檔頭「已知限制」段） |
| DEF-101-022 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-007 殘餘） | **`closure_evidence._run_git` 自身繼承呼叫端 env**：v0.30 `closure_evidence.py:89` `_run_git` 未自清 GIT_DIR/GIT_WORK_TREE——屬 read-only git 查詢（rev-parse 類），實害受限；現行防線為 hook 層 `env -u` 清洗 | P3 | 縱深防禦（函式內自清 env）留待後續輪；known-gap 記入根層 `ONBOARDING.md` §9 | open（known-gap；防線＝hook 層清洗已阻斷已知傳染路徑，函式內自清為縱深防禦補強項） |
| DEF-101-023 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect 複審終裁輪，DEF-101-010 修復之衍生） | **`autoclaude-mutation-on-change.yml` restore→upload 非原子、無 concurrency 序列化**：mutation run 長 20~45 分鐘，連續兩次 push token_guard 時後開始先結束的 run 讀不到另一 run 的 sha，較晚 `created_at` 的 artifact 蓋掉對方 → 該筆 unique sha 永久丟失（方向安全：少計不多計、不會假鎖定，僅 7-sha 閘門推進變慢） | P2 | 複審終裁輪即修（一行 concurrency 序列化） | fixed@四方複審第三輪修復（證據：workflow 增 `concurrency: {group: mutation-history-accrual, cancel-in-progress: false}`——排隊不取消，每筆 sha 都入鏈；yml 解析驗證通過） |
| DEF-101-024 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-011 修復之覆蓋缺口） | **NTFS 檔名閘漏擋反斜線 `\`**：`tools/git-hooks/pre-commit` 非法字元 case 僅列 `< > : " | ? *`——macOS 端誤建含 `\` 檔名並 commit 後，Windows checkout 直接 `error: invalid path` 整庫癱瘓，正是此閘要擋的類別（SD bash 3.2 單元實測 `back\slash.txt` 判 CLEAN 放行實證） | P2 | 複審終裁輪即修（case pattern 補一項） | fixed@四方複審第三輪修復（證據：case 補 `*'\'*`、錯誤訊息同步列 `\`；檔內實際引號寫法實測 HIT；`bash -n` 過） |
| DEF-101-025 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-012 修復之語意註記） | **根層基建 bash -n leg 檢查「工作樹版本」而非 staged blob**：`tools/git-hooks/pre-commit` root-infra leg 以 `bash -n "$TOPLEVEL/$f"` 檢查工作樹檔——staged 壞＋工作樹好的罕見組合會本機假綠入庫 | P3 | 與既有 ruff-on-worktree 慣例一致故不改本機行為；known-gap 記入 `ONBOARDING.md` §9 | open（known-gap；緩解＝雲端 `root-infra-ci.yml` 對 push 後實際內容機械攔回） |
| DEF-101-026 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-007 修復之殘留死碼） | **AISDLC pre-push 的 `export GIT_WORK_TREE` 成死碼**：DEF-101-007 修復後其唯一下游消費者（ci-gate.sh）已被下一行 `env -u` 清洗，export 無作用且註解易誤導後人「此 env 有人在用」 | P3 | 複審終裁輪即修（移除死碼區塊） | fixed@四方複審第三輪修復（證據：`AISDLC_SDD/.githooks/pre-push` 移除 guarded export 四行，僅剩 `env -u` 清洗呼叫；`bash -n` 過） |
| DEF-101-027 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-015 清掃之同檔漏網） | **`test_copy_on_evolve.py:241` `text=True` 無 encoding**：`command -v python` 探測 subprocess 漏補 encoding（輸出為路徑，實害極低，但與本輪清掃標準不一致） | P3 | 複審終裁輪即修（一行補齊） | fixed@四方複審第三輪修復（證據：補 `encoding="utf-8", errors="replace"`；`scripts/tests/` 130 passed 複驗） |
| DEF-101-028 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD live 實證，終裁 (f) 核准修法） | **hook wiring cwd 相對路徑 → PreToolUse 全工具硬鎖死**：三份 settings.json 的 hook command 為裸 `python <相對路徑>`，hook 子行程 cwd≠專案根（Agent SDK 子 session／Bash cd 殘留）時 python 缺檔退出碼恰為 2＝PreToolUse deny 語意 → matcher 涵蓋的 Write/Edit/Read/Bash/Task 全滅（審查管線被鎖死 live 實證；雙平台同構） | P0 | 本輪即修 | fixed@四方複審第四輪（證據：根 4＋AutoClaude 6＋v0.30 3 條 command 全改 python -c shim——python 自讀 `CLAUDE_PROJECT_DIR` 錨定+os.chdir、缺檔 fail-open exit 0、argv/stdin/exit code 原樣傳遞〔目標腳本 exit 2 阻斷語意保留〕；contract test `AISDLC_SDD/scripts/tests/test_hook_wiring_cwd_safety.py` 三道鎖＋錨定行為鎖擴及三份 settings；ONBOARDING §4 同步） |
| DEF-101-029 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **根層 `sdd_hook_router.py` stdin 未設 UTF-8**：只 reconfigure stdout/stderr，zh-TW Windows pipe 預設 cp950，payload 含中文即 `sys.stdin.read()` UnicodeDecodeError → router 崩潰 fail-open（DEF-43-001 只修了 child 端沒修自身入口） | P1 | 本輪即修 | fixed@四方複審第四輪（證據：`.claude/hooks/sdd_hook_router.py:42-49` reconfigure 迴圈擴為 stdin/stdout/stderr＋防非 TextIOWrapper；cp950 模擬修前重現、修後全過） |
| DEF-101-030 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **AutoClaude 五支 hook＋v0.30 兩支 context_ledger 裸 `sys.stdin.read()`**：同 cp950 病灶，共 8 個使用點；阻斷級 enforce_docs_path 於「Write 內容含中文」即靜默失效、check_ps1_encoding 恰在其存在目的場景失效 | P1 | 本輪即修 | fixed@四方複審第四輪（證據：8 處統一改 `sys.stdin.buffer.read().decode("utf-8","replace")`＋無 buffer 回退；新增 `AutoClaude/tests/tools/hooks/test_hooks_stdin_utf8.py` 6 case 含阻斷保真；凍結版 v0.01~v0.29 依紀律未動） |
| DEF-101-031 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SA） | **macOS 桌面通知靜默降級 log-only**：`notifications` extra 在 darwin 只裝 plyer，plyer darwin 後端需未宣告的 `pyobjus` → `notify()` 必炸，三層 fallback 無聲落 log；ESCALATION 桌面通知為引擎核心行為 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：`notifier.py` 插入 darwin `osascript` fallback〔`on run argv` 參數化、注入免疫、timeout=10〕＋`tests/utils/test_notifier.py` 10 case；macOS 實機發出通知） |
| DEF-101-032 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **v0.30／根層／AutoClaude settings.json 無 `PYTHONUTF8` env**：直啟子專案 session 時 hooks 無 UTF-8 防護（v0.30 hooks 原無 reconfigure、🔴 emoji 輸出 cp950 UnicodeEncodeError） | P2 | 本輪即修 | fixed@四方複審第四輪（證據：三份 settings.json env 皆補 `"PYTHONUTF8": "1"`；凍結版 v0.01~v0.29 不回改，見 DEF-101-040） |
| DEF-101-033 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **子專案 .ps1 零 CI 語法覆蓋**：root-infra-ci pwsh parse 與 paths 僅根層 tools/，改壞 `local_ci_gate.ps1`／`run_local_nightly.ps1`（42KB）只能在 Windows 本機爆 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：root-infra-ci pwsh parse 擴至 AutoClaude/tools/＋AISDLC_SDD/scripts/＋動態 LATEST 版〔regex+version 排序鏡射 ci-gate.sh〕，凍結版排除；15 支 active .ps1 容器實跑全過） |
| DEF-101-034 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **.ps1 UTF-8 BOM 政策無 CI 機械複核**：唯一強制為 Claude Code session 內 hook，人工編輯器／GitHub web 可讓無 BOM 含中文 .ps1 入庫 → zh-TW PS5.1（cp950）誤讀 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：root-infra-ci 併入「含非 ASCII 的 active .ps1 必須有 EF BB BF」檢查，範圍與 pwsh parse 同源；負向容器實測攔截/放行正確） |
| DEF-101-035 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **NTFS 檔名閘僅本機 pre-commit，CI 無對等**：`--no-verify`／GitHub web／未裝 hooks 的 clone 可繞過，一旦入庫全部 Windows checkout 癱瘓 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：新增 `tools/check_ntfs_paths.py`〔忠實移植 `_ntfs_seg_bad`＋大小寫碰撞，git ls-files 全量〕入 root-infra-ci；實跑 27,328 路徑 0 違規＋9 敵意/6 正常單測；root-infra-ci 觸發改全變更〔終裁 (d) 准〕） |
| DEF-101-036 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **雙腳本 .sh/.ps1 對等無機械守護**：bootstrap／integration_gate／local_ci_gate 三對閘門清單純靠人工雙改，單點漂移風險 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：新增 `tools/check_script_parity.py`〔step 標籤序列比對＋pytest 釘選雙處同版〕入 root-infra-ci，現況 3+5+9 標籤一致＋負向測試正確判不等；其餘三對無標籤錨點明文豁免於 ONBOARDING §6） |
| DEF-101-037 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **跨專案 bridge 測試 CI paths 盲區**：AutoClaude bridge 測試實際消費 AISDLC_SDD v0.01/v0.02/v0.05 檔案，但 autoclaude-ci paths 只有 `AutoClaude/**`——AISDLC_SDD-only 變更改壞凍結版要等下次 AutoClaude push 才暴露 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：autoclaude-ci push＋pull_request paths 補 `AISDLC_SDD/AISDLC_SDD_v0.01/**`、`v0.02/**`、`v0.05/**`，消費點以 grep 實證） |
| DEF-101-038 | 2026-07-11 | 跨平台修復輪四方複審第四輪（四方 P3 彙總） | **P3 × 17 全數修復**：根層缺 README／PTY macOS 差異未文件化／`Thumbs.db` 忽略不對稱／根 CLAUDE.md 單平台路徑／GUI commit venv PATH 雷區未文件化／`integration_gate.ps1` 缺 python 守門＋LASTEXITCODE 重置／`ci-gate.ps1` 覆蓋差距單向擴大／`run_act.sh` pipefail+grep -q 違規／bootstrap 沿用 .venv 無平台形狀檢查／`copy_on_evolve.sh` 檔頭直執行示範／`download_bge_m3.py` docstring Windows 路徑／aisdlc-sdd-ci artifact 只傳 v0.01／pytest 釘選雙處無一致性檢查／equivalence 測試裸 python PATH／v0.01 post_commit_drift docstring 不符（known-gap 化）／contract test 錨定鎖擴及三份 settings／parity 未覆蓋對明文侷限 | P3 | 本輪即修 | fixed@四方複審第四輪（證據：README.md 新增、.gitignore 補 Thumbs.db/desktop.ini、CLAUDE.md:11 中性化、ONBOARDING §2/§4/§5/§6/§6.1/§9 多處、ci-gate.ps1 薄委派 bash、run_act.sh herestring、bootstrap×2 形狀 fail-fast、equivalence conftest、aisdlc-sdd-ci glob、parity 腳本含釘選比對；統一驗證 pytest 3543/181＋scripts/tests 全綠） |
| DEF-101-039 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect 終裁 (c) 另立） | **NTFS 檔名閘（hook＋CI）皆無 MAX_PATH 長度檢查**：Windows 未開 `core.longpaths` 時 >260 字元路徑 checkout 失敗；現況最長 tracked 路徑 142 字元、含 clone 前綴仍餘裕充足，急迫性低 | P3 | 後續輪 hook＋CI 兩側同步加「≤180 warn / ≤200 fail」保守長度閘（預留 clone 前綴） | fixed@跨平台第五輪（證據：`tools/git-hooks/pre-commit` A3 閘第 4 項 >200 擋下/>180 預警〔code point 計數＝刪 UTF-8 連續位元組，locale 無關〕；`tools/check_ntfs_paths.py::_length_level` 同門檻同語意；contract test `AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py` 13 case 六道鎖〔純函式邊界 180/181/200/201、hook 實跑同四點釘死、存量 >fail 零違規、CI main() 接線鎖、`_ntfs_seg_bad` 跨語言 parity、CJK C-locale 無關；cacheinfo 暫存不落地建檔〕；實跑 27,333 tracked 路徑 0 違規、最長 142） |
| DEF-101-040 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect 終裁 (e)，凍結版豁免家族；SA 終裁補充 cwd-wiring 面向） | **凍結版 v0.01~v0.29 settings.json 無 `PYTHONUTF8` env、hook command 仍為裸相對路徑**：直啟凍結版 session 時 ① hooks 的 cp950 stdin/stdout 風險不受 env 保護；② cwd 漂移時同樣有 DEF-101-028 的 exit-2 deny-lock 風險（僅 v0.30／根層已補 env＋shim） | P3 | 凍結版依紀律不回改；known-gap 記入 `ONBOARDING.md` §9（含兩面向） | wontfix+凍結版紀律（known-gap 已列；緩解＝根層 router 路由情境由根層/v0.30 覆蓋、直啟屬 dogfooding 邊角情境，操作時保持 cwd 於版本根） |
| DEF-101-041 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SA 發現，Architect 終裁 (a)(b) 准） | **v0.02~v0.04 arch-fitness.json＋findings.json 誤入庫殘留**：政策（DEF-11-001，v0.05 起）建立前複製的三版 SENSE 輸出被追蹤，evidence 欄嵌機器絕對路徑（`D:\` 實錘）→ 雙平台輪替每次重生即 diff 抖動（已實證發生） | P3 | 本輪即修 | fixed@四方複審第四輪（證據：六檔 `git rm --cached` 出庫〔磁碟保留〕＋`AISDLC_SDD/.gitignore` 補兩個排除 block；零消費點以 grep 實證；僅改追蹤狀態不改凍結版位元組） |
| DEF-101-042 | 2026-07-12 | 跨平台修復輪四方複審第五輪（Architect/SA/SD/QA 四方一致；SA 擴及既有案例） | **contract test 落 CI 觸發盲區（DEF-101-037 同構復發）**：`test_ntfs_length_gate.py`（消費根層 `tools/check_ntfs_paths.py`＋`tools/git-hooks/pre-commit`）與既有 `test_hook_wiring_cwd_safety.py`（消費根層＋AutoClaude 的 `.claude/settings.json`）唯一 CI 載體為 aisdlc-sdd-ci（經 ci-gate scripts leg），但其 paths 僅 `AISDLC_SDD/**`；root-infra-ci 雖全變更觸發卻無 pytest step——**只改根層消費檔的變更（如把 `_LEN_FAIL` 改 2000）三道鎖全數沉默**，要等下次 AISDLC_SDD 變更才暴露 | P2 | 本輪即修 | fixed@第五輪（證據：aisdlc-sdd-ci push＋pull_request paths 各補 **5** 條根層消費檔〔tools 兩支＋兩份 settings.json＋複審輪 Architect/SD 交叉抓漏之 `.claude/hooks/sdd_hook_router.py`——兩支 router contract test＋ci-gate router lint 硬閘消費〕；檔頭補「根層消費檔 paths」設計說明；不在 root-infra-ci 裝 pytest——保其「零依賴安裝 <1 分鐘」契約。**治本**：新增 meta contract test `test_ci_paths_cover_root_consumers.py`——正則掃描 scripts/tests 全部 `_monorepo_root()` 消費點、機械斷言 ∈ workflow paths＋push/PR 對稱鎖＋已知 5 條防正則退化鎖；負向驗證＝抽掉 router 行即紅——037→042→router 三度同構打地鼠自此機械終結） |
| DEF-101-043 | 2026-07-12 | 跨平台修復輪四方複審第五輪（Architect F-2＋QA P2-2 同源） | **長度閘存量鎖與 warn 帶語意矛盾**：防線 3 硬斷言全量 tracked ≤180，但閘門政策明定 181~200＝warn 可入庫（hook exit 0、CI exit 0）——合法 warn 路徑一旦在 `AISDLC_SDD/**` 外入庫（該處 aisdlc-sdd-ci 不觸發），下一個無辜的 AISDLC_SDD 變更在防線 3 爆紅（延遲、錯誤歸因），三閘對同一問題給兩種答案 | P2 | 本輪即修 | fixed@第五輪（證據：防線 3 門檻改 `>_LEN_FAIL` 常數引用〔閘門本應攔下卻在庫＝自我矛盾，語意收斂單一故事〕；warn 帶政策維持第四輪終裁 (c) 規格原文；docstring 明文「warn 帶依政策可入庫，不以 180 斷言」防回退） |
| DEF-101-044 | 2026-07-12 | 跨平台修復輪四方複審第五輪（QA mutation 實證 M1/M6 假綠） | **CI 版 `main()` 長度接線零測試鎖**：防線 1 只鎖 `_length_level` 純函式——整段集成刪除（M1）或 fail 降級寫入 warnings（M6）後測試照綠；root-infra-ci 實掃因存量全合規也照綠＝閘門靜默失效的假綠窗口 | P2 | 本輪即修 | fixed@第五輪（證據：防線 4 兩 case——工具複製至 sandbox（`_REPO_ROOT=parents[1]` 自然錨定）實跑 main()：201→exit 1＋「路徑過長」、190→exit 0＋⚠ stderr〔同時鎖「warn 不影響退出碼」〕；QA M1/M6 mutation 重放已被殺，見第五輪複審驗證） |
| DEF-101-045 | 2026-07-12 | 跨平台修復輪四方複審第五輪（SA） | **pytest 基線三文件矛盾（交叉引用斷裂）**：ONBOARDING §7 宣稱「與根層 CLAUDE.md 記載的 2026-07-11 實測基線 3,543/181 一致」，但根 CLAUDE.md:76 實記 3,529@2026-07-09、AutoClaude/CLAUDE.md 標頭＋文末元數據同滯留 3,529——被引用的內容不存在（第四輪 +14 case 未同步兩份 CLAUDE.md） | P2 | 本輪即修 | fixed@第五輪（證據：根 CLAUDE.md:76、AutoClaude/CLAUDE.md 標頭與元數據三處同步 3,543/181@2026-07-11＋「第四輪 +14 case」註記；本機實測 3,543/181 佐證） |
| DEF-101-046 | 2026-07-12 | 跨平台修復輪四方複審第五輪（SA） | **CLAUDE.md 計數漂移家族**：根 CLAUDE.md「16 Plugin／12 ports」vs 機械生成 Snapshot「18 active/19 靜態、Port 18 個」（漂移 2 plugin＋6 port）；AutoClaude/CLAUDE.md:111 手寫目錄樹「14 active/15 靜態」與**同檔** Snapshot 自我矛盾——根檔對 AISDLC_SDD 數字有「指向 FRAMEWORK_STATUS」政策，對 AutoClaude 數字卻裸寫零守護 | P2 | 本輪即修 | fixed@第五輪（證據：根 CLAUDE.md 兩處數字中性化＋明文「Plugin/Port 計數一律見 [Architecture Snapshot]，與 FRAMEWORK_STATUS 同政策」；AutoClaude/CLAUDE.md:111 去數字改指 Snapshot——手寫數字歸零，漂移面拆除） |
| DEF-101-047 | 2026-07-12 | 跨平台修復輪四方複審第五輪（P3 彙總：Architect F-3~F-7＋SA×3＋SD×2＋QA×2） | **P3 × 11 全數處置**：①註解算術差一（MAX_PATH=260 含 NUL，260−60=200 邊界超限）②hook 邊界釘定精度（QA M5 warn 門檻漂移存活；190 取樣容許 [190,200] 漂移帶）③C locale 下 `${#f}` 退化 byte 計數→CJK 67 字元誤擋且與 CI 分歧（SD/QA 實證）④`_ntfs_seg_bad` 第 1~3 項跨語言雙實作零 parity 鎖⑤換行檔名 hook 逐行讀 fail-open 疑點（Architect F-5）⑥merge/rebase/cherry-pick 繞過 pre-commit 未註記⑦Windows+WSL 環境 `shutil.which("bash")` 誤選 System32 佔位 bash→四檔 bash 類測試紅燈而非 skip⑧ONBOARDING §6 攔截原因清單漏列長度閘⑨ONBOARDING §2「30 個凍結版 SDD hooks」措辭不精確⑩`wiring.py` docstring 漏列 rtm_writeback/translation_learner⑪ONBOARDING §9 尾註漏 DEF-101-040 對應 | P3 | 本輪即修 | fixed@第五輪（證據：①三處註解改「260 含 NUL 可用 259、259−200＝59」②防線 2 四點 180/181/200/201 hook 實跑釘死③hook 改「刪 UTF-8 連續位元組」code point 計數（locale 無關）＋防線 6 LC_ALL=C CJK case④防線 5 Python 表驅動 11 case＋hook 實跑保留名/禁字⑤**查證不成立**——SD/QA 雙實證 git 對含控制字元路徑恆 C-quote（不受 quotepath=false 影響），引號化表徵含 `"`/`\` 觸發第 1 項攔截，兩側封閉，結論記入 CI 版 docstring⑥hook 檔頭補註（root-infra-ci 兜底）⑦新增 `scripts/bash_probe.py` 共用 helper（git 相鄰 Git Bash 優先＋排除 System32），四檔 skipif/argv 升級⑧⑨⑪ONBOARDING 三處補正⑩docstring 補列兩 key；scripts/tests 135→148 全綠） |
| DEF-101-048 | 2026-07-12 | 跨平台修復輪四方複審第五輪之**複審輪**（Architect REJECT 阻塞 1＋SD/QA APPROVE 附帶殘餘） | **複審輪殘餘 × 5**：①（P2，Architect 阻塞項/SD 交叉印證）DEF-101-042 修法掃漏根層 `.claude/hooks/sdd_hook_router.py`——`test_router_version_path_safety.py:31`＋`test_session_start_hub_timeout_budget.py:40`＋ci-gate `router_hook_coverage_lint` 硬閘皆消費該檔，單改 router 時三道鎖本機＋雲端雙盲（037→042→本筆三度同構）②（P3，Architect/SD）`check_ntfs_paths.py:46` 常數區塊殘留舊算術「260−60」與同檔 docstring 矛盾（047① 宣稱三處實漏第四處）③（P3，SD）`test_ntfs_length_gate.py:172` 新引入 E501（AutoClaude convention line-length=100；「ruff 7 檔綠」原聲明僅預設規則集成立）④（P3，QA 探針 M10/M11 存活）CI `main()` 的 seg_bad 接線與大小寫碰撞段零行為鎖——大小寫碰撞（A3 第 3 項）兩側皆無行為測試⑤（P3，QA 實測）flen 管線每檔 3 spawn，863 檔 Copy-on-Evolve commit 於 Windows Git Bash 估 25~75s 可感延遲 | P2×1＋P3×4 | 本輪即修（複審輪內閉合） | fixed@第五輪複審輪（證據：①paths 補第 5 條＋meta contract test 治本〔詳 DEF-101-042 欄〕②:46 改「可用 259−59＝200」四處歸一③172 行斷言拆行；另 I001（import 分組隨 cwd 而異之歧義）於終裁輪由 Architect 抓出、四檔 `# isort: skip` 註記補修，:46 修正註解自身 E501 同輪拆行——終態以釘選 ruff 0.15.21 `--isolated --line-length 100 --select E,F,I,UP` 於 monorepo 根與 AISDLC_SDD 雙 cwd 實測全綠（sister 三檔 I001 同驗）④防線 4 擴充 `test_ci_main_seg_bad_and_case_collision_wiring`——staged CON.txt＋Readme.md/readme.md 斷 exit 1＋兩訊息；M10/M11 重放各 1 failed 被殺⑤hook 前濾 `[ "${#f}" -gt 180 ]` 短路——任一 locale 下 ${#f}≥code point 數故 ≤180 必然合規，現況最長 142 → 全量零 spawn，超標者才進精確管線；scripts/tests 148→152 全綠、還原後 mutation 基線 14 passed。**另 SD 未驗殘餘觀察**：WindowsApps WSL bash 別名理論上繞過 System32 字串排除——macOS 無法實證，`bash_probe` 的 `echo ok` 驗活對此無鑑別力，known-observation 待 Windows 實機輪驗證） |
| DEF-101-049 | 2026-07-12 | 排程 nightly 紅燈追查（push 後兩封失敗通知；本機 CI 對等容器沙箱實證） | **`alembic/env.py` 交易語意缺陷 → `alembic upgrade head` 靜默整包 rollback**：R56 P0-1e 的預建 alembic_version（`exec_driver_sql`）觸發 SQLAlchemy 2.0 autobegin，alembic 偵測「呼叫端已有進行中交易」切換 caller-managed 模式不再自行 commit，而 env.py 從未 commit → `with connect()` 結束 close() 把 16 個 migration＋版本戳＋CREATE TABLE 整包 rollback——**exit 0、零輸出（env.py 無 fileConfig 本就無 INFO log）、零資料表**；pg-contract／pg-e2e-nightly 兩 job UndefinedTable 之根因，所有呼叫端（2 CI job＋labeled-PR workflow＋local_ci_gate 雙腳本＋Windows nightly）同受害 | P0 | 本輪即修 | fixed@nightly 追查輪（證據：env.py 預建表後補 `connection.commit()` 結清 autobegin、alembic 回自管交易；CI 對等 pg17 fresh 容器實證修前 0 表／修後 46 表＋`alembic_version=0016 (head)`；三處 workflow migration 步驟補持久化守門 `alembic current \| grep -q "(head)"`——同類靜默回滾未來就地現形；沙箱換 alembic 1.16.5 同病、排除新版 regression。**雲端取證**：labeled PG E2E run 29177170726〔最新 main merge ref〕migration＋守門步驟綠） |
| DEF-101-050 | 2026-07-12 | 排程 nightly 紅燈追查（DEF-101-049 修後 recall 仍 0.000 續查） | **pg-e2e 的 X1 seed 步驟自 monorepo 匯入（0cda8aa，2026-06-12）即遺失**：recall 測試前置明載「`tools/seed_kb.py` 已 seed 100 列」，但新舊 workflow 皆無 seed 步驟（git 考古實證 0 命中）；fixture JSON 在庫使測試不走 skip 分支 → 查詢空 `knowledge_entries` → recall 恆 0.000（p95 對空表反而假綠通過） | P1 | 本輪即修 | fixed@nightly 追查輪（證據：autoclaude-ci pg-e2e-nightly＋autoclaude-pg-e2e-on-label 兩 workflow 於 migration 後補 `seed_kb.py --mock-pg-seed --pg-dsn $AUTOCLAUDE_TEST_PG_DSN` 步驟〔seed 同時重生 fixture 與當次資料嚴格配對；工具自帶 +asyncpg strip〕；本機 fresh 容器完整鏈 migrate→seed→pytest 實證 recall/p95 **2 passed**〔修前 recall=0.000 紅〕。**雲端取證**：labeled PG E2E run 29177170726〔空 commit 取證 PR #8，最新 main〕Seed 步驟「Inserted 100 mock entries」＋pytest 2 passed/1 skipped——與本機一致；首次取證誤用 dependabot PR #7 為載具因 merge ref 過期（未含 02:21Z 修復）而無效，已作廢） |
| DEF-101-051 | 2026-07-12 | 排程 nightly 紅燈追查（DEF-101-049 修後 pg-contract 真實狀態出土） | **`ck_runs_post_cutoff_has_goal` 日期分界約束 vs repository 寫入路徑矛盾**：0010 migration 的 CHECK（`goal_task_id IS NOT NULL OR started_at < '2026-05-20'`）過 cutoff 後生效，而 `PgStateRepository` 對無 goal_task_id 的 run 仍裸 INSERT → 契約測試 12 案中 7 案 CheckViolation（先前被 DEF-101-049 的 UndefinedTable 遮蔽，修後真實狀態現形）；workflow 早有明文「此 job 暫標 continue-on-error，SD_10 PG-track 處理」 | P2 | 既有 SD_10 PG-track 範疇（workflow 檔頭明文既知紅燈；本輪不重設計 repository/約束，維持 continue-on-error 非阻塞） | open（SD_10 PG-track；本輪貢獻＝揭開遮蔽使真實失敗面可見：5 passed/7 CheckViolation/1 skipped，非 setup 全滅） |
| DEF-101-052 | 2026-07-12 | 雲端取證輪（兩次 labeled run「Comment AC4 progress on PR」步驟同紅） | **labeled PG E2E workflow 缺 permissions 宣告**：預設 GITHUB_TOKEN 唯讀 → github-script 對 PR 留言恆 `HttpError: Resource not accessible by integration`，每次 labeled run 帶一顆無關紅 X 干擾取證判讀 | P3 | 本輪即修 | fixed@雲端取證輪（證據：job 補 `permissions: contents:read + pull-requests:write`；dependabot 觸發情境依 GitHub 政策仍強制唯讀屬平台限制，已於註解明文「取證勿用 dependabot PR 當載具」） |

## 歷史複驗註記 — 已歸檔（improving_99 起）

> **歸檔政策（DEF-99-001 fixed@improving_99）**：本帳本「跨輪累積、只增不刪」，但中文長列使單檔逼近工具讀取上限（曾達 468KB > Read 256KB）。自 improving_99 起，**歷史敘事複驗/收尾註記**（其缺陷現況已被上方缺陷總表 live 狀態取代，或為已結歷史）搬遷至分檔，**原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）；主檔只保留標頭 + 完整缺陷總表（live SSOT）+ 本指標。**輪替規則：主檔與「每一個」 archive 檔皆以 < 256KB（Read 工具上限）為界**——主檔再逼近即開新 archive；單一 archive 逼近即拆下一個 archive（故本輪一次就把 619 行歷史拆成 archive_01 + archive_02 兩檔，而非單一超限檔）。
>
> **已歸檔內容**（兩檔，原主檔 93-711 行逐字拆分）：
> - **`AutoSDD_Defect_Log_archive_01.md`**（原主檔 93-543 行，≈170KB）：rounds 24-63 各輪複驗/收尾敘事註記 + 2026-06-22 agent/* 符規審查臨時塊（DEF-AGTREV-001~013 全 fixed@v0.18）+ 早期 `.claude` hooks/skills 審查輪（一~六）。
> - **`AutoSDD_Defect_Log_archive_02.md`**（原主檔 544-711 行，≈177KB）：improving_45 追記 + 2026-06-23 `.claude` hooks/skills 審查輪（三~九，DEF-CLDREV-* 系列收斂至零缺陷）。
>
> **重要**：歸檔當下，所有**仍未結（open / routed / wontfix）缺陷已全數上移為上方缺陷總表列**（DEF-25-001 / 42-001 / 42-003 / 52-006 / 53-001；DEF-37-001 經本輪 audit 校正為 fixed@improving_43），故 live 追蹤**無須**讀 archive；archive 僅供已結缺陷之完整歷史佐證與審計回溯。
>
> **查詢某缺陷現況**：先看上方「缺陷總表」（完整 SSOT）；需歷史脈絡再查對應 archive 檔。
