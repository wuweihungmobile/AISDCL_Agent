# AutoSDD Defect Log — Archive 45

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **3 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-081`、`DEF-101-707`、`DEF-101-709`
>
> **本次操作備註**：R68 入帳後容量輪替：本輪 9 列入帳後主檔 260777 bytes、距 262144 硬線僅 1367。判準④ 具名承認 3 筆 false positive（皆本輪自撰、逐字複核無待接工作）：DEF-101-705（marker 解鎖條件 出現在引述 DEF-101-702 的解鎖條件）、DEF-101-708（marker deferred 出現在引述硬規則② 散文原文）、DEF-101-709（marker 留待 出現在引述殘留字樣範例）。刻意不 ack DEF-101-710（明載 DEF-101-432 首詞未改、改派未指派＝真實待辦）與既有 521/524/541/554/652/663/689（前輪已判有真實待辦）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-081 | 2026-07-15 | 獨立重新複審（不預設上輪 APPROVE 為真）：以 `gh run list`／`gh api .../annotations` 親查 CI 實際執行紀錄，而非讀帳本 | **本輪「四方複審終裁全 APPROVE」的最終 commit `1175555` 及其前一輪 `a8e4b73`，在真實 GitHub Actions 上從未成功執行過一次——CI 因帳戶帳單問題全面停擺，且此狀態完全未被記錄或揭露**：`gh run list --workflow=windows-compat-ci.yml` 顯示 `a8e4b73`（push run `29335402759`、workflow_dispatch run `29335409369`）與 `1175555`（push run `29378155616`）的 Windows smoke job 皆在 4~7 秒內 failure 收場；`gh api repos/.../check-runs/87235788504/annotations` 證實 annotation 為 `"The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings"`（runner 從未被配置，非測試失敗）。交叉核對 `gh run list --branch main --limit 15`：同一時窗（`a8e4b73` push 當下 `2026-07-14T13:10:41Z` 起）macos-compat-ci／root-infra-ci／AutoClaude CI／aisdlc-sdd-ci 五個 workflow 同步失敗、同款帳單 annotation；本輪複審當下重跑仍為同狀態（`root-infra-ci`／`AutoClaude CI` push run `29378473172`／`29378473200`，`2026-07-15T00:14:10Z`，對應 HEAD `bda34da`），故障持續中、非單次偶發。之前最後一次真實成功執行是 `2026-07-14T13:03:47Z` 的 windows-compat-ci workflow_dispatch run `29334937643`（真實測試失敗 exit code 1，促成 `a8e4b73` 修復）。`grep -rni "billing\|帳單\|spending limit"` 對 `docs/06_quality/AutoSDD_Defect_Log.md`／`ONBOARDING.md`／`CLAUDE.md` 皆零命中——即這個涵蓋 2 個關鍵 commit、持續 11+ 小時的 CI 全面停擺，從未被四方複審任何一方發現或記錄 | P0 | **無法以程式碼變更修復**：`windows-compat-ci.yml`（及其餘 4 個 workflow）本身無 bug，annotation 顯示 job 在 workflow 任何 step 執行前就被 GitHub Actions 平台攔下，workflow 檔內沒有、也不可能有任何 step 能偵測或繞過帳戶帳單問題；根因修復需**帳號所有者**至 GitHub「Billing & plans」還清欠款或調高 spending limit，超出本 repo 程式碼與本次 agent 權限範圍。本行之補救動作僅限「補齊揭露缺口」——不留其未被記錄的狀態 | **closed-by-decision@R68（外部條件已解除，回執見本欄末段）**：原首詞為 `open（需人工於 GitHub Billing & plans 處理，非本輪程式碼可解）`。本行本身即為「揭露」動作，之後任何一輪複審／nightly 若見 `windows-compat-ci.yml`／`macos-compat-ci.yml`／`root-infra-ci.yml`／`AutoClaude CI`／`aisdlc-sdd-ci` 五者同步在數秒內 failure，應先查 annotation 是否為本行同款帳單訊息，而非誤判為程式碼回歸；帳單問題解除後，須對 `1175555`（或其後續 HEAD）**重新觸發一次 workflow_dispatch 或等待下一次 push**以取得這五份 workflow 對本輪四方複審實際變更的第一次真實機械驗證結果，在那之前「四方複審 APPROVE」不應被視為已通過機械 CI 驗證。**R14 複核（2026-07-20，Architect 親查 `gh run list`＋`gh api .../runs/29688470830/jobs`）：停擺持續中**——7/19 R13 commit `44d7c25` push 觸發五 workflow 全部 2~3 秒 failure、job `steps=[]` 即殞落（runner 零配置，同款帳單 annotation）；R12／R13／R14 三輪變更均未獲任何一次雲端機械驗證，ONBOARDING §9 各「雲端 CI 機械攔回」型緩解句在停擺解除前均為紙上防線；帳務解除後須對 HEAD 補跑一次 workflow_dispatch 取得首次真實驗證。**R42 複核（2026-07-24，SA／QA 一審各自獨立以 `gh run list --branch main --limit 8`＋`gh api .../jobs`＋`gh api .../check-runs/{id}/annotations` 重新即時驗證）：停擺持續中，未解除**——本輪最終 commit `33733a0`（R41 收尾）push 觸發之 `macos-compat-ci`／`windows-compat-ci`／`aisdlc-sdd-ci`／`root-infra-ci` 四 workflow（run `30088637281`／`30088637246`／`30088637224`／`30088637250`，`2026-07-24T11:10:08Z`）與前一 commit（R40 收尾）push（run `30085411368`／`30085411445`／`30085411530`／`30085411371`，`2026-07-24T10:13:26Z`）皆在 3~6 秒內 failure；`gh api repos/.../check-runs/89466619843/annotations` 證實 annotation 仍為同款「recent account payments have failed or your spending limit needs to be increased」訊息，job `steps=[]`（runner 未配置，非測試回歸）。距 DEF-101-081 首次記錄（`2026-07-14T13:10:41Z`）已持續 **9 天 22 小時+**，橫跨 R14~R42 共 20+ 輪皆未獲雲端機械驗證。**本輪起建議**：往後每輪四方複審至少執行一次 `gh run list --branch main --limit 8` 即時重新驗證（而非僅沿用舊帳本敘述），避免「停擺已解除但帳本未更新」或「仍停擺但誤判已解除」兩類過期風險。🔴 **R68 回執（2026-08-02，Pkg-8 以 `gh` 親查，非讀帳本）：停擺已解除，本列結案**——鑑別力取證刻意不只貼「四支 success」，而是貼 **runner 首次真被配置**那一刻：`gh run view 30695971130 --json jobs` → `steps=[26,0,0]`（`461fd3b`，`2026-08-01T10:34:25Z`，本次為真實測試失敗而非 `steps=[]` 的帳單攔截）；此前 `30624747906`／`30576918926` 等歷次 run 皆 `steps_total=0`。首次四支全綠＝`24c5f34`（`2026-08-01T14:03:42Z`，windows／macos-compat-ci、aisdlc-sdd-ci、root-infra-ci 皆 `success`；同日 `9672d01` 的 windows-compat-ci 為 `failure` ⇒ success 具鑑別力、非全綠假象）。本列所要求的「帳單解除後須對 HEAD 補跑一次以取得首次真實機械驗證」至此達成，故轉已結。**未一併宣稱的事**：`ONBOARDING.md` §9「R60（及近輪）全部實機驗證平台＝Windows 11」那一列仍過期（R67 已是首個 macOS 真機輪），該檔本輪由他包獨佔，已列跨包請求 |
| DEF-101-707 | 2026-08-02 | R68 收斂半套工作樹時的 Scan-H 同型復發自查 | **本輪新落地的三處程式碼在加進閘門路徑時零測試**：`tools/lib/ci_liveness.py`（100 行新模組，`grep` 全 `tools/tests/` 零命中）、`check_defect_log_crossref.py` 的 `unpinned_handover_problems()` 與 `stale_grandfather_problems()`（硬規則② 後半句與其存量豁免 stale 自檢）。三者皆已被接進每次都跑的閘門，卻沒有任何東西保證它們還有鑑別力——**正是它們自己要消滅的形狀**（用來偵測哨兵已死的哨兵自己沒人看） | P2 | 根層 `tools/tests/`（護欄層） | fixed@R68：補 16 支雙向注入鎖——`test_dev_start.TestStaleScheduleTracks`（8 支：陳舊必報／新鮮不報／週頻容忍一次跳過／無訊號不報／從未成功必報／dormant cron 不列期望軌／預算耗盡即止／多 cron 取最嚴）＋ `test_check_defect_log_crossref.TestUnpinnedHandoverAndStaleGrandfather`（8 支，含「綁定不得退化成靜默關掉」的結構鎖）。根層 unittest 1400 → 1438 |
| DEF-101-709 | 2026-08-02 | R68 Scan-G（同上普查） | **已結列的殘留待辦在結構上永遠進不了承接稽核**：`_UNRESOLVED_CLASSES` 明確排除 `fixed`，故一列只要首詞是 `fixed`，其狀態／分流欄寫的「承接輪次：未指派」「留待下一輪」就不會被任何機械物看到。實測主檔 17 筆已結列帶這類殘留字樣。`archive_defect_log.py --plan` 的 needs_ack 區塊雖列出其中一部分，但那張清單**只在有人要歸檔時才被看到**——「只在有人主動去看時才被看到」的清單等於沒有 | P3 | 根層 `tools/check_defect_log_crossref.py` | fixed@R68：接進每次都跑的閘門輸出（warning 而非 fail——已結列的殘留待辦是提示不是矛盾），現實跑印出 18 筆逐列座標。真待辦仍應拆出獨立 DEF 列承接，本道只負責讓它們**可見** |
