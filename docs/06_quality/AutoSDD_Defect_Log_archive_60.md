# AutoSDD Defect Log — Archive 60

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **33 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-811`、`DEF-101-846`、`DEF-101-845`、`DEF-101-840`、`DEF-101-862`、`DEF-101-837`、`DEF-101-843`、`DEF-101-852`、`DEF-101-868`、`DEF-101-864`、`DEF-101-844`、`DEF-101-860`、`DEF-101-842`、`DEF-101-838`、`DEF-101-839`、`DEF-101-841`、`DEF-101-865`、`DEF-101-853`、`DEF-101-854`、`DEF-101-857`、`DEF-101-836`、`DEF-101-859`、`DEF-101-861`、`DEF-101-858`、`DEF-101-849`、`DEF-101-855`、`DEF-101-850`、`DEF-101-832`、`DEF-101-847`、`DEF-101-833`、`DEF-101-835`、`DEF-101-834`、`DEF-101-851`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：R77 開場歸檔：搬 R76 已結列 33 筆，降主檔體積（未結列數不受影響）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-811 | 2026-08-04 | R75 帳本容量包執行 `--apply` 時當場撞到 | `tools/archive_defect_log.py --apply` 對可搬清單全有全無、無排除入口：`:1391` 搬整份 `movable`，`--ack-handoff` 只能加不能排除，`:1486`~`:1492` 無 `--keep`／`--only` ⇒ 下令保留特定列時無合法路徑可遵守（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-811` 節） | P2 | `tools/archive_defect_log.py`（加「本輪列預設不可搬」判準或 `--keep <ID>`） | fixed@R76｜🔴 R76 訂正首詞（原文逐字接於後）：open（承接輪次：**R76**）｜附帶查證：禁令所附機械理由（搬走本輪列會讓時鐘退回）實測不成立，仍印「當前輪 R75」；有效理由只剩政策面｜🔴 R76 回執：`--only`／`--keep` 已落地（`archive_defect_log.py` CLI ＋ `defect_ledger_index.select_move_subset()`）。三條防後門約束皆實測：判準跑完才做集合運算故只會變小、ID 打錯即 fail-loud 且零寫入（實測 `--only DEF-999-999` rc=1）、排除留痕於 archive 標頭與索引 bullet。鎖＝`TestMoveSubsetSelectionIsNamedAndTraceable`（7 測） |
| DEF-101-846 | 2026-08-05 | R76 PKG-E（Scan-C／R76-03）＋PKG-F 判準面 | 兩支 compat-CI 的 nightly-full 帶 job 層 `continue-on-error` ⇒ 紅被吸收、run 層 conclusion 仍 success，§7 表③ 照實填 success 也不算填錯；唯一顯形通道是 GitHub issue #10（2026-07-14 OPEN、橫跨四輪「雲端全綠」宣稱、零讀者）。當時寫下「現查 3 個 fail-open job」 | P1 | 文件面補表③-b ＋ 機械面補判準⑧ | fixed@R76 ｜表③-b 逐列記排程軌 job 結論（實查 windows-nightly-full=failure、macos=success，run 30803941764／30807193487）＋SOP 第 6 步＋錨帶 `nightly-red`／`nightly-run`／`nightly-checked-at` 三欄；判準⑧＝`cloud_nightly_red_problems()`＋`cloud_fail_open_jobs()`。🔴 複審 SA-02 訂正：本列上一欄那個「3 個」是假數字（正則要求行尾故漏掉帶行尾註解的第 4 個），已改 SSOT 正則，複審後現查 **4 個**（配方見 DEF-101-857） |
| DEF-101-845 | 2026-08-05 | R76 PKG-E（Scan-N／R76-09） | 「文字讀寫漏 `encoding=`」這一整類 mac→Windows 缺陷**零靜態掃描器**（mac 恆綠／zh-TW Windows 的 cp950 即炸），且其執行期可見度被根 `.claude/settings.json` 的 PYTHONUTF8 設定關掉 ⇒ agent 開發迴圈裡結構上不可見 | P1 | 併入既有鎖檔新增第七道 AST 判準 | fixed@R76 ｜`test_platform_neutral_paths.py` 增 AST 判準（`read_text`／`write_text`／`open` 缺 encoding；二進位與 `os.open` 出射程；行尾標記＋stale 自檢），810 檔實掃；存量 3 檔 9 筆以**雙邊 shrink-only 棘輪**凍結、續修責任具名於該常數 docstring；注入紅綠皆實測。R76 複審 SA-03：本欄兩個裸識別字原本被搬遷判準② 當成活躍狀態字 `open` ⇒ 本列永久搬不走，已加反引號（零語意變更，同 archive_55 對 7 筆歷史 token 的既有體例） |
| DEF-101-840 | 2026-08-05 | R76 PKG-D（Scan-M，R76-06）＋PKG-F 家族層 | 🔴 R75 頭號教訓第三次復發：`windows_smoke_local.ps1` 退場判準 E3 要求「移除後 `check_scheduled_task_drift.py` 回 rc=0」，而期望值 SSOT 同時列兩支任務 ⇒ 執行 E3 自己授權的動作必然讓它回 `task_missing`／rc=1。R75 的鎖只圈 `cloud_` 家族 Python 碼，.ps1 散文零訊號 | P1 | E3 改逐任務量測 ＋ 不限語言的家族層鎖 | fixed@R76 ｜E3 只看 `.tasks.AutoClaude_Nightly`，並就地寫下規則「判準的量測對象不得隨被它所判的動作而改變」。兩鎖各附注入紅綠（家族層＝`TestR76ExitCriteriaSurviveTheirOwnAction`） |
| DEF-101-862 | 2026-08-05 | R76 複審 QA-01（根 CLAUDE.md 鐵律三對照表） | `CLAUDE.md:178` 把 `$env:*` 讀取記成「無機械物、沒有東西會紅」，而 `DEF-101-766` 的 PATHEXT 那一半早有掃描器；`git log -S` 實查：掃描器與這句假話是同一個 commit（R74 `a371068`）落地的 | P1 | 訂正 :170／:178（沿用 `Get-Command` 列的既有體例） | fixed@R76 ｜保留「無機械物」字樣（棘輪鎖需要）並就地揭露唯一例外；:179 經實測確無掃描器故不動。三向注入皆紅（拿掉字樣／具名不存在的檔／具名不存在的符號），現況 10 tests rc=0。取證 `CrossPlatform_R76_Scan_Findings.md` §R76-FIX-4 |
| DEF-101-837 | 2026-08-05 | R76 PKG-C 擴大 pg-contract 射程時撞紅 | **Mako 1.4.0 誤打包頂層 `tools/` 套件**（RECORD 列 `tools/__init__.py`／`toxnox.py`／`warn_tox.py`），而 `AutoClaude/tools/` 無 `__init__.py` ⇒ regular package 恆勝 namespace、與 sys.path 順序無關 ⇒ 凡裝 `[postgres]` 者 `tools.*` 全被遮蔽。實測 5 模組 ImportError rc=2 ＋1 子行程 ModuleNotFoundError | P1 | CI 就地釘 ＋ pyproject 根治 | fixed@R76 ｜`autoclaude-ci.yml` 就地釘 `mako<1.4`（第二道）；🔴 收斂包補上**根治位置**＝`AutoClaude/pyproject.toml` postgres extra 亦帶同一約束，本機 `uv pip install -e '.[dev,postgres]'` 不再踩到 |
| DEF-101-843 | 2026-08-05 | R76 PKG-E（Scan-F／R76-01） | 開箱無 clone 步驟：`ONBOARDING.md` §2 從 bootstrap 起手、`useMacWin.md:98` 誤導。實測未帶 `-c core.longpaths=true` clone 到 168 字元目錄 → rc=128、27,523 支 tracked 只落地 301 支、`bootstrap.ps1` 不在磁碟上（半套 checkout）；旗標只在已 clone 成功的 `--local`，`--system`／`--global` 皆 rc=1 ⇒ fresh clone 無保護 | P0 | 文件補第 0 步 ＋ 長度閘假設改成輸出 | fixed@R76 ｜§2 補 clone 旗標＋`--global` 兜底、useMacWin 就地訂正、`check_ntfs_paths.py` 每次執行印算式；rc=0：檔案面 259−142=117／目錄面 247−124=123／門檻預留 59 |
| DEF-101-852 | 2026-08-05 | R76 PKG-G（R76-17；R68-67 open 跨 8 輪） | `AutoClaude/pyproject.toml:24` 的 `keyboard>=0.13` 無平台條件，metadata 逐字 `Requires-Dist: pyobjc ; sys_platform == darwin` ⇒ 即 macOS 側 pyobjc 傘包成因；而它在 mac 需 euid==0，非 root 者付了安裝面拿不到功能 | P1 | 移出核心相依、新開 `[hotkey]` | fixed@R76 ｜刻意不寫死 win32（Linux root 仍可用）。缺席實測：阻斷 import 後三模組仍 import OK、`register()` 不拋例外。🔴 三處消費面已同步：`bootstrap_core.py` 記下刻意不含 hotkey 及其代價、UserGuide §4.2、ONBOARDING 雷區表（順帶訂正 mac 失效實因是 euid） |
| DEF-101-868 | 2026-08-06 | R76 收尾包首次對含交棒書的樹跑根層 unittest | 交棒書自己讓兩道根層鎖轉紅：`test_defect_id_reference_integrity` 的 ① 斷鏈引用（現查配方寫了不完整的 ID 字面 ⇒ 判指向空號）與 ② 占位形站點上限 5>4。它在 `5993f09` 時 untracked，寫它的包自陳「零改動」且未重跑閘門 | P1 | 兩處改述（不動判準） | fixed@R76 ｜🔴 訂正文第一版**自己又踩一次**（逐字示範占位形 ⇒ 同支測試再紅），改成只描述不示範，11 tests OK rc=0。通則同 `DEF-101-858`。**.md 也在閘門掃描面內，untracked 檔更危險**——寫它的人多半不跑閘門 |
| DEF-101-864 | 2026-08-05 | R76 收斂包複驗（Fixer 逐道跑 push 閘門） | `ruff check tools/`（＝pre-push leg ④／`root-infra-ci.yml` 第 16 道）**rc=1**：`sync_onboarding_baselines.py:136/137/144` 三筆 E501。成因是同輪為新 headroom 鎖騰餘裕而合併 docstring 折行；HEAD 版同檔 rc=0 ⇒ **本輪造的紅，整輪無人跑過這一道** | P1 | 縮短該三行（不得加行——加行會讓餘裕由 6 掉到 3、當場破新鎖） | fixed@R76 ｜只改字不改行數：`ruff check tools/` rc=0、raw lines 1493 不變。🔴 教訓：CJK 檔的 E501 量的是**顯示寬度**不是字元數（`len()` 96 vs ruff 107），拿 `len()` 自檢會漏 |
| DEF-101-844 | 2026-08-05 | R76 PKG-E（Scan-N／R76-10） | `tools/git-hooks/pre-commit` 兩條 NTFS 大小寫碰撞閘用 `grep -iFx` 並以 or-true 尾綴吞 rc ⇒ **locale 相依 fail-open**：GNU grep 3.0 的 `-i`＋`-F` 在無 UTF-8 locale 時 SIGABRT（rc=134），崩潰被吞成「沒有碰撞」；照鐵律一用 Find-GitBash 複驗這道閘的人拿到的正是那個假綠 | P1 | 換 bash 3.2 折疊慣例 ＋ rc 分級 fail-loud | fixed@R76 ｜改 `tr` 折疊＋不帶 `-i` 的 `grep -nFx`（行號回查原拼法）、rc≥2 擋下。注入實測：真碰撞 rc=1；注入 grep rc=2 → rc=1 且兩處印「無法驗證，擋下」；舊 idiom 同條件 rc=0 放行 |
| DEF-101-860 | 2026-08-05 | R76 追加射程（`pg_real` 真機 e2e） | ground truth 記的是列 UUID，而 `seed_kb.py --mock-pg-seed` 每次重新隨機產生 ⇒ committed 那份與任何其他 DB 交集結構上恆為 0（實測 0／100），recall 以 0.000 轉紅並指向錯的地方；同檔 p95 在空 DB 上則是假綠 | P1 | 把檔頭第 4 條啟用條件寫成 code，不動 `>= 0.95` | fixed@R76 ｜新增 `_require_seeded_corpus()`（`[PG-CORPUS-MISSING]`／`[PG-CORPUS-STALE]`，理由帶可複製指令）。四情境實測含**注入壞檢索→rc=1 不被吃成 skip**；同次 seed 時 recall 實測 0.999。取證 `Skipped_Test_Inventory_R76.md` §4.7.3 |
| DEF-101-842 | 2026-08-05 | R76 PKG-D（Scan-M，R76-13） | GA 判準量的是**紀錄筆數**不是日曆天，對「整段沒跑」零偵測：`observability_ga_check.py` 當回合實測以「30 筆橫跨 58 個日曆天、含一段 12 天全黑」印出 `green_streak=44 >= window=30 → GA 取證通過`（rc=0）；drift 同型 span 65 天 | P1 | 兩支各補 `staleness_days` 與窗內連續性 | fixed@R76 ｜>30 天即 `stale`；跨度 ≤ window×4÷3（＝覆蓋率下限 3÷4），語意抄 `ac4_progress_check.py`。🔴 obs 因此由達標翻回未達標（`sparse`／rc=1），方向正確**不得放寬**；雙向注入：連續 30 天綠、挖 12 天洞紅 |
| DEF-101-838 | 2026-08-05 | R76 PKG-C（R76-15） | **為看見覆蓋損失而建的機制與看著它的鎖一起沉默**：`AutoClaude/tests` 6 個 posix-only 站點 0/6 帶標籤 ⇒ conftest 的 `POSIX/MAC-NATIVE-ONLY SKIPS` 區塊在 Windows 上結構性零輸出；`_POSIX_TAG_RATCHET` 又把 6 凍結成可見欠債使棘輪恆綠；該區塊本身零回歸鎖 | P1 | `tools/lib/skip_tag_policy.py` ＋ `AutoClaude/tests/` 5 檔 | fixed@R76 ｜6 處補標、棘輪 6→0、加 shrink-only 天花板封住「把基線改大」舊出口、既有鎖檔補正負兩支。區塊由 0 行變 17 行；五筆注入紅綠見 `Skipped_Test_Inventory_R76.md` §4.6.1 |
| DEF-101-839 | 2026-08-05 | R76 PKG-D（Scan-D＋H＋M 命中 R76-05） | R75 的具名豁免（`status=drift` 只印 WARN、不計 nightly 失敗）活過了它自己寫的解除條件：`tools/check_scheduled_task_drift.py` 當回合實測 `status=ok`／rc=0（兩支任務各 7 項全符），而 `run_local_nightly.ps1` 白名單仍含 `drift` ⇒ 真回歸被歸成「已知存量」 | P1 | 白名單只留 `ok`／`skip`，新增標籤 `schedule_drift_regression` | fixed@R76 ｜退場改由鎖盯不靠人讀 WARN：`test_install_windows_nightly.py::TestNamedExemptionRetiresWhenItsUnlockConditionHolds`；注入實測 3 紅 → 還原 50 tests OK |
| DEF-101-841 | 2026-08-05 | R76 PKG-D（Scan-M，R76-12） | 五本觀察期帳本裡 `AutoClaude/.drift_log_history.jsonl` 是**唯一被 git 追蹤**的（`git ls-files --error-unmatch` 實測 drift rc=0、obs／ac4／mutation／perf 皆 rc=1）⇒ 進帳會被 `git checkout -- .`／`stash`／`reset --hard`／worktree 切換靜默回捲，已實測損失 UTC 2026-07-27 一整天 | P1 | 與另四本對齊：`AutoClaude/.gitignore` ＋ `git rm --cached` | fixed@R76 ｜理由寫進 `drift_log_snapshot.py` 檔頭；實測 `check-ignore` rc=0、`ls-files` rc=1、磁碟 37 筆完好。`run_local_nightly.sh:10` 的舊敘述已由收斂包同步訂正 |
| DEF-101-865 | 2026-08-05 | R76 技術債收尾（孤兒腳本） | `reschedule_g0_gatecheck.ps1` 唯一用途是重排 R71 已移除的 `AutoClaude_SD09_G0_GateCheck`；本機實查僅存 Nightly／WindowsSmoke ⇒ 每條路徑停在 Task not found exit 1 | P2 | 刪檔＋同步全部登記站點 | fixed@R76 ｜parity 活體表移除（`_TIER_BASELINE` 依規則留為永久記憶，tier3/4 課責 10/10 不變）、ONBOARDING §8、macos-compat-ci 兩處 paths、3 檔註記；4 個實測型下限同步下修（sched_family 3→2、AutoClaude/tools 7→6 兩站、SSOT 35→34、AC 48→47）。parity／LOC／根層 unittest 1979 OK 皆 rc=0 |
| DEF-101-853 | 2026-08-05 | R76 PKG-G（R76-47，DEF-101-018 引用面） | `AutoClaude/pyproject.toml:32-34` 的 ruff 存量「1,147 筆一致」「原 1,339 筆」八輪無人重量；同版 ruff 0.15.21 今日實測 824（cwd=AutoClaude）與 3,711（cwd=repo 根）⇒ 任一讀法下都失實 | P2 | 刪數字、改記量測配方 | fixed@R76 ｜只留鎖版理由，配方＝`ruff check . --statistics`，並明寫必須同時記 cwd（AutoClaude 與 repo 根是兩個不同的量）。實測數字只進驗證紀錄、不寫進檔案。`tools/_cli_flags.py:54` 那處寫死的「1618／1617／餘 1 行」亦由收斂包改為現查配方 |
| DEF-101-854 | 2026-08-05 | R76 PKG-G（`Scheduled_Jobs_Lifecycle_Review_R75.md:446,539`） | `AutoClaude/tools/reschedule_g0_gatecheck.ps1` 唯一用途是重排 R71 已從本機移除的排程工作，`g0_gate_check.ps1` 亦無排程消費者 ⇒ 樹裡兩支看起來還在用 | P3 | 兩支加檔頭 DEPRECATED 註記 | fixed@R76 ｜ASCII-only 慣例未破（實測 nonAscii=0、parse errors=0）。當回合實查 `Get-ScheduledTask` 只剩 `AutoClaude_Nightly` 與 `AutoClaude_WindowsSmoke`；G0 現由 `run_local_nightly.ps1:1647` 四軌判定＋`:1772` 的 `.g0_readiness.json` 承載。刪除成本（跨 5 檔 7 處）見 `DEF-101-856` |
| DEF-101-857 | 2026-08-05 | R76 複審 SA-02（判準⑧ 掃描面） | `continue-on-error` 正則要求行尾 ⇒ 帶行尾註解的 job 整個漏掉，`autoclaude-pg-e2e-on-label.yml:35` 是活體逃逸案例；同一份正則另有第二份複本於 `tools/lib/ci_liveness.py`，兩處同向失明 | P1 | 收斂為單一 SSOT 正則＋注入鎖；訂正帳本假數字 | fixed@R76 ｜正則收斂到 `ci_liveness.JOB_FAIL_OPEN_RE`（容行尾註解）、判準⑧ 改 re-export；複審後現查 **4** 個 fail-open job（`DEF-101-846` 那句「現查 3 個」已就地訂正）。取證 `CrossPlatform_R76_Scan_Findings.md` §R76-FIX-1 |
| DEF-101-836 | 2026-08-05 | R76 PKG-C（掌舵者問 224 skipped 怎麼測到） | **192 支 skip（86%）零通道**：`pg-contract` 早備妥 PG service＋extras＋DSN＋alembic 整套 recipe，最後一步卻只跑 1 支檔（`autoclaude-ci.yml:255`）；`sdk` extra 從未被任何 workflow 裝過 ⇒ alembic 0007~0012 契約、三層 schema、9 表 DDL 漂移鎖皆從未執行過 | P1 | `.github/workflows/autoclaude-ci.yml` | fixed@R76 ｜射程擴為 contract＋integration＋infra、`test` job 加 sdk。實測 skip 195→40 rc=0 ＋5.87s；全樹 3902/224→3905/221 rc=0。詳 `Skipped_Test_Inventory_R76.md` §4.4／§4.5 |
| DEF-101-859 | 2026-08-05 | R76 追加射程（掌舵者糾正「本機沒有 PG」假前提） | 整棵樹接上本機 PG 後 157 支由 skip 轉真跑，曝出 4 支從未被執行過的紅：3 支 `backfill_legacy_fk() 不存在` ＋ 1 支 `recall@10 = 0.000` | P1 | 逐筆分因處置＋盤點文件補 §4.7 | fixed@R76 ｜3 支＝環境（開發 DB 停在 head 卻沒真跑過 0010；同容器新建 DB 跑 `alembic upgrade head` 後該檔 16 passed rc=0）⇒ 斷言不動、只讓訊息指得到成因；1 支見 `DEF-101-860`。乾淨環境全樹 4108 passed／69 skipped rc=0。取證 `Skipped_Test_Inventory_R76.md` §4.7 |
| DEF-101-861 | 2026-08-05 | R76 複審 SA-03（歸檔留痕射程） | `archive_59` 標頭與索引 bullet 兩句與磁碟不符（對餘裕做定性宣稱、寫死本輪新列筆數）。根因＝`DEF-101-811` 的排除留痕只覆蓋 `--only`，「被搬遷判準擋下」的列結構上無處可記 | P1 | 訂正兩檔散文＋標頭新增「被判準擋下」欄 | fixed@R76 ｜兩檔同步訂正（不逐字重述被推翻的話）、改為現查配方、新增「被搬遷判準擋下」欄並逐筆記名。複驗 crossref rc=0、`archive_defect_log.py --check` rc=0。取證 `CrossPlatform_R76_Scan_Findings.md` §R76-FIX-3 |
| DEF-101-858 | 2026-08-05 | R76 複審 ARCH-01 修法落地後的二次紅 | 改名解了標記碰撞，但同一包在 `#` 註解裡逐字寫出姊妹檔標記 ⇒ 對方判它是真標記而報 stale。`run_root_unittests.py` 實跑 rc=1，逐字 `test_platform_neutral_paths.py:2050／:2342 encoding-ok 標記 stale` | P1 | 改註解措辭（不動判準）＋就地寫下通則 | fixed@R76 ｜兩處改為不逐字引述任何編碼家族標記，並寫下通則「要引述就寫進 docstring／字串字面值」。複驗 SEH 36 tests、PNP 73 tests 皆 OK rc=0。取證 `CrossPlatform_R76_Scan_Findings.md` §R76-FIX-2 |
| DEF-101-849 | 2026-08-05 | R76 Scan-H 維度自檢（PKG-F） | Scan-H 既有五條必跑項**全部是單鎖視角** ⇒ 兩形態零必跑項：①「A 鎖要求往某檔加一行、B 鎖（零餘裕棘輪）禁止加那一行」；②「同一支工具內先跑的檢查早退，把後面訊號整批遮蔽，方向是看起來變乾淨」。本輪 P0 兩者兼具 | P2 | 維度檔補必跑項⑥⑦ 並各附立案實證 | fixed@R76 ｜⑥＝治理文件登記 vs LOC 棘輪互鎖；⑦＝crossref 早退吞掉孤兒警告。🔴 誠實劃界：⑥⑦ 是**流程必跑項**不是機械鎖，故無 bug-injection 證明 |
| DEF-101-855 | 2026-08-05 | R76 收斂包跑根層全套閘門時當場觸發 | **A 鎖要求登記、B 鎖因此轉紅**（Scan-H 新必跑項⑥ 的第三個實例）：`CrossPlatform_R76_Scan_Findings.md` 依 `unregistered_governance_docs()` 登記後即進入 SC-7 使用面，而該檔為說明長名截斷偽陽性**逐字複製**了維度表的兩個單字母代號 ⇒ `test_adr_xplat001_c1c2_lock.py` 11 支紅（1 支本體＋10 支串音） | P1 | 邊界宣告只留一個家，複本改為指路 | fixed@R76 ｜§6.3 與 §7 兩處改寫並指向 `CrossPlatform_Scan_Dimensions.md:369-373`。實測 93 tests OK |
| DEF-101-850 | 2026-08-05 | R76 PKG-G（R76-16） | `SPECIAL_FILES` raw-line 棘輪與根層 tools tier 兩層皆無預警帶，第一個訊號就是紅（實測六格餘裕 0~2） | P1 | `check_loc_budget.py` 判準上移為共用 `warn_band()` | fixed@R76 ｜三層一份實作、新增 `SPECIAL_WARN_MARGIN=5`、JSON 加 `special_warn_band` 與 `root_tools_warn_band`、文字加兩段非阻塞警示且 rc 不變。🔴 該包自陳「零回歸鎖」已由收斂包補上：`test_check_loc_budget_tier_headroom_warn.py` 併入 6 支（含三層共用實作的行為綁定），三筆注入紅綠皆實測、15 passed |
| DEF-101-832 | 2026-08-05 | R76 PKG-A 帳本工具 | 搬遷判準② 對帳本慣用語「訂正首詞（原文逐字接於後）：」之後的**舊狀態引文**誤命中，把現況已結的列永久釘死在主檔（`DEF-101-676` 第三種逸出面）。`--plan` 實測：修前被判準② 擋下 20 筆／69113 bytes（主檔 29.3%） | P1 | `tools/lib/defect_ledger_index.py` 加第三種遮罩 | fixed@R76 ｜`VERBATIM_QUOTE_TAIL_RE`（界停在全形豎線，引文之後的現況敘述不遮），釋出 14 筆／51044 bytes。鎖＝`TestCriterion2VerbatimQuoteTailMask`（7 測，雙向注入） |
| DEF-101-847 | 2026-08-05 | R76 Scan-H（PKG-F 護欄鑑別力） | `tools/tests/test_doc_loc_baseline_freshness_r60.py:2816` `hook_claim_problems()` 用**整檔 substring** 判「已註冊」⇒ `.claude/settings.json` 一個 `_comment` 提到某 hook 名就買到整支免檢，拔掉真 wiring 只留註解仍全綠＝R75 才拆掉的 OR 型通行證換皮復活 | P1 | 改走既有 SSOT `hook_command_scripts()` | fixed@R76 ｜不自寫第二份 JSON 解析。注入：只在 `_comment` 出現的 hook ＋ 無射程註記的文件行，修前零違規、修後點名該 hook；該類 9 tests rc=0 |
| DEF-101-833 | 2026-08-05 | R76 PKG-A 帳本工具 | 未結列數是唯一沒有洩壓閥的天花板：未結列結構上不可搬，而歸檔會讓 bytes 大幅下降 ⇒ 剛歸檔的人看到的訊號方向相反。實測未結 82 筆、距 fail 98 僅 16 筆，而 warn 86 之下**零訊號** | P2 | `defect_ledger_index.unresolved_advisory_notes()` | fixed@R76 ｜新增 warn 線**之下**的非阻斷預警帶（`UNRESOLVED_ROWS_ADVISORY_MARGIN=20`），訊息明載「歸檔不會降低此數」；warn 86／fail 98 兩線未動，rc 不變 |
| DEF-101-835 | 2026-08-05 | R76 PKG-B（R76-11） | 兩支 compat-CI 的 smoke job 從不跑 AutoClaude 測試樹：`AutoClaude/tests/**` 100% 在 paths 內（實測 340/340）卻只執行 `tests/test_perception.py` 一支 ⇒ push 閘門對 AutoClaude 生產碼在雙平台零行為證據 | P1 | 兩支 smoke 各加一步平台敏感子集 | fixed@R76 ｜`-k 'path or encoding or platform or shell or subprocess'`；Windows 真機 277 passed／7 skipped／13.54s；選 0 支時 pytest rc=5 故失效方向是紅（實測）；載具登記已補 |
| DEF-101-834 | 2026-08-05 | R76 PKG-B（R76-02） | `shell: powershell` 的 run 本體含中文 ⇒ runner 寫成無 BOM 暫存 .ps1、PS 5.1 以 ANSI codepage 誤讀 ⇒ ParserError、整支零執行。`windows-compat-ci.yml` nightly-full step[2]/[3] 自 R48 起四輪未真跑（雲端 run 30803941764 逐字 ParserError） | P1 | 兩處 run 改全 ASCII ＋新鎖 | fixed@R76 ｜鎖＝`test_gha_action_versions.py::TestPowershellRunBodyIsAscii`（只鎖 powershell 不鎖 pwsh，pwsh 7 預設 UTF-8）；注入 rc=1 → 還原 rc=0 |
| DEF-101-851 | 2026-08-05 | R76 PKG-G（R75 交棒書 5-4） | `tools/check_script_parity.py` 卡 1618 行 shrink-only 棘輪、實測 1617 只餘 1 行 | P2 | 收斂逐字重複的交叉鎖函式體 | fixed@R76 ｜兩支收斂為唯一實作 `_cross_lock()`（訊息字面不變），另壓實兩張純資料常數；1617→1595、餘裕 23 行。棘輪維持 1618：禁調高，也刻意不下調到新實測值（下調會把剛騰出的餘裕當場歸零）。工具輸出與改前逐字相同、101 支測試 OK |
