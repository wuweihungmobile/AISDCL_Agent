# AutoSDD Defect Log — Archive 32

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **12 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 5 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-580`、`DEF-101-563`、`DEF-101-577`、`DEF-101-562`、`DEF-101-571`、`DEF-101-579`、`DEF-101-568`、`DEF-101-578`、`DEF-101-574`、`DEF-101-569`、`DEF-101-572`、`DEF-101-576`
>
> **本次操作備註**：R60 round 3 收輪前第三次歸檔（Pkg-P11）：主檔實測 245,600 bytes、距 warn 線 245,760 僅 160 bytes，而本包尚須寫入 5 筆新條目＋2 筆補述＋指路段。可搬清單 12 筆全屬 R60 本輪已結列（judge 判準①②③⑤ 全過、判準④ 未命中）——刻意不做人工二次篩選：程式即判準，另設散文篩選正是本工具立帳要消滅的病。R60 已踩過 archive_31「未登記索引」的坑（DEF-101-569），故本次同時倚賴判準⑤ 自動註冊索引 bullet，並由本包在主檔 R60 條目區塊開頭加醒目指路。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-580 | 2026-07-29 | R60 r3：Pkg-P4 於測試時挖到、**Pkg-P6 獨立重驗後修復**（`P4→P6` 交棒） | **閘門自己的欄位切分假綠**：`check_defect_log_crossref.py` 切欄時**把空欄整個濾掉**（`if c.strip()`）且全程無欄數（arity）檢查 ⇒ **狀態欄留空時 `cells[-1]` 靜默位移到左鄰的「分流去向」欄**。構造輸入實測（未動主檔）：空狀態欄＋去向含 `fixed` ⇒ `_load_ledger_status()` 回 `fixed`；去向以合法關鍵字開頭 ⇒ `status_first_word_problems()` 回 `[]`——**兩道檢查同時零訊號**；且修前訊息會誤植成「狀態欄原文開頭：'去向'」＝**指向錯的欄位，比沒擋更危險** | P2（潛伏非現行：Pkg-P5 實查主檔 **0 列含空欄、0 列 arity 不符**；但這是「抓跨文件假綠」的工具自己的同型假綠面） | 根層 `tools/check_defect_log_crossref.py` | fixed@R60：根因＝欄位索引隨空欄數浮動。改「**保留空欄**（`_row_cells()`）＋**表頭欄名**定位狀態欄（`_table_layout()`，不再取 `cells[-1]`）＋`row_arity_problems()` arity 硬閘」，訊息改逐欄傾印。測試 41→51 支全綠、真實 repo rc=0。🔴 **方法論界線（本筆最有價值處，亦是主控任務書事實錯誤被下游正確駁回的紀錄，不美化）**：任務書要求「修復後須紅並指出『欄數不符』」，該期望**實測為錯**——那兩列的欄數本來就對（9 切片＝表頭欄數），**只加 arity 斷言完全抓不到**，承重的是「保留空欄＋表頭定位」；arity 治的是**另一種**列形（欄內未轉義字面豎線 ⇒ 欄數變多，`DEF-101-560`）。兩機制**互補、不可互相冒充**。修復者拒絕照做並釘成專門測試（Pkg-P5 實查該測試與 docstring 界線段皆存在）。見證據檔 `## DEF-101-580` 節 |
| DEF-101-563 | 2026-07-29 | R60 round 2 四方複審 **全部四位獨立命中同一根因**（ARCH-R60R2-02／SA-R60R2-02／SD-R60-R2-02／QA2-R60-01） | **ONBOARDING §7 表②（dated snapshot）四格靠「收輪紀律」＝純記憶機制，而表頭同時宣稱「四格皆另經 R60 SA 複審者獨立序列重跑覆核相符」⇒ 該句在寫下時即為假。** 機制上的復發路徑：round 1 填了 ci-gate v0.30 當時值 → round 2 動了該測試樹使實測改變 → 沒人回填。round 2 有三方各自實跑 ci-gate 皆測得同一個新值、而文件仍是舊值差 11。ARCH 原話：「一個宣稱『四格皆經獨立覆核』而其中一格與現場實測差 11 的表，比沒有 provenance 宣稱更糟」 | P2（活文件假宣稱） | 根層治理工具 + ONBOARDING §7 | fixed@R60 round 3：①v0.30 格訂正為實測值（三方獨立覆核一致）；②表頭假宣稱刪除，改為**逐格 provenance 表**（哪一格、什麼時點、誰實測過）；③新增 `--write --with-slow`＝**一條指令回填四格**（解析 ci-gate 自己的 `逐軌計數：vX:N` 自證行 ＋ plain 形態 pytest，形態與該格宣告逐字一致）；④新增 `--check-snapshot`＝**因果式 presumed-stale 觸發器**（比對四套測試樹內容指紋，毫秒級），接 pre-push 第 8 支守門與 root-infra-ci 第 14 道，**刻意不接根層 unittest 閘門**（那支每輪跑數十次，會養成忽略紅燈的習慣）；⑤四格的當輪值一併從歸因散文清除（同 DEF-101-562 紀律）。誠實劃界：指紋只覆蓋測試樹 `*.py`，生產碼改 `parametrize` 來源／docker daemon／平台差異皆能改變計數而指紋不動 ⇒ **充分觸發器、非必要條件（會漏、不會冤）**。完整證據見 `CrossPlatform_R60_Fix_Evidence.md` |
| DEF-101-577 | 2026-07-29 | R60 r3 主控自查（收斂 Pkg-P1 未完成品時發現；**非四方回報**） | **歸檔工具 docstring 措辭與實作不符——本輪自己犯的同型錯**：`archive_defect_log.check()` 的 docstring 在 r3 收斂後仍寫「**七項**都是實作」「r2 又抓到 **(5)(6)(7)** 三個」，而作廢的第(7)項（方案乙反向鎖）已被刪除、實際只有六項；判準④ 的例外也已由兩種增為四種而散文沒跟上。**這正是本工具立帳要消滅的病（宣稱一道檢查存在而它不在）在工具自己的 docstring 上復發**，且是同一輪內 | P3（文件與實作不符，不影響 rc；但屬本輪核心紀律的自我違反） | 根層 `tools/archive_defect_log.py` | fixed@R60 r3：`check()` docstring 的項數／四種例外／`governance` 分流先由主控訂正；🔴 **Pkg-P5 續揪出同檔兩處「第七項」殘留**（同輪內第二次復發）——①`apply()` 寫進 archive 標頭的散文仍手寫**七項**、第(7)項即被推翻的方案乙反向鎖 ⇒ **每跑一次 `--apply` 就把失實宣稱複製成一份永久史料**（archive 零刪除）；②`_fenced_line_numbers()` 仍寫「判準⑦ 用」。**兩處皆已由 Pkg-P7（P7-4）於同輪修復，且是根因級**：新增 `CHECK_CRITERIA`／`MOVE_CRITERIA` 判準清單 SSOT，`check()` 成功訊息與 `apply()` 標頭散文**全部由它生成、不得手寫第二份** ⇒「兩份說法」結構上不可能再出現，並由 `TestCriteriaListIsASingleSsot` 三項斷言把守（逐項見證據節）。Pkg-P5 交件前複驗兩處修復皆在位。見證據檔 `## DEF-101-577` 節 |
| DEF-101-562 | 2026-07-29 | R60 round 2 四方複審 **全部四位獨立命中同一根因**（ARCH-R60R2-03／SA-R60R2-02／SD-R60-R2-03／QA2-R60-02）＝本輪收斂度最高的一筆 | **「機械鎖已落地」的認定門檻本身有缺口：產生器 ＋ `--check` 只保證「被抽取的那個 token」新鮮，完全不保證同一行的散文新鮮。** round 1 為 ONBOARDING §7 表① 落地 `sync_onboarding_baselines.py` 後，受鎖行的 token 已回填為當輪實測值，而**同一行的散文仍留著同輪的較舊宣稱**（形態：`R<當輪>=<舊值>`），該行下一句恰好正在講「同一份 repo 對同一個數字只准有一種說法」 | P2（活文件自我矛盾；照該行驗證的人會把正常狀態誤判為退化） | 根層治理工具 + ONBOARDING §7 | fixed@R60 round 3：①受鎖行散文改為不含當輪值的形態（「見本格 live 值」）；②`Spec` 新增 `historical` 白名單 ＋ `prose_problems()` 兩道判準——(1) 受管值不得在受鎖行出現第二次（≥3 位數才判，位數門檻的 WHY 寫在程式常數旁）、(2) `R<輪號>=<數字>` 同量宣稱值 ≠ live 者必須具名登記附 WHY；③判準以**真實缺陷語料**當正樣本（`R60=756` 逐字）。🔴 **落地當場抓到主控自己**：我在同一行寫「已是 845 而散文仍寫 R60=756」解釋缺陷，兩道判準同時紅——說明它抓的不是特例而是形態。零新增鎖檔（落在既有 `_SPECS` 機制與既有測試檔內）。完整證據見 `CrossPlatform_R60_Fix_Evidence.md` |
| DEF-101-571 | 2026-07-29 | R60 r2 SA 複審（`SA-R60R2-03`） | **`見主檔 DEF-x` 變體逸出**：`archive_26.md:7` 原寫該形態而失實（實居 archive_27），而 `POINTER_RE` 硬要求字面「立帳見」，少兩個字就完全逸出 ⇒ 稽核照印「19/19 皆…一致」——**在它自己界定的範圍內為真，卻讀起來像整個類別已閉合** | P2（`DEF-101-570` 的姊妹逸出面，且已產生一處實際失實宣稱） | 根層 `tools/archive_defect_log.py` ＋ `AutoSDD_Defect_Log_archive_26.md` | fixed@R60 r3：根因＝判準綁死單一字面。新增判準⑥ 涵蓋 `見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 兩方言、並訂正 `archive_26.md:7`；Pkg-P5 實測 **4 個**此類指針全數驗過居所一致、rc=0。🔴 **Pkg-P5 另發現稽核面不對稱，已由 Pkg-P7 P7-5 於同輪修復**：原判準④ 已擴為 `_pointer_audit_files()` 而**判準⑥ 仍只掃 `_family_files()`** ⇒ 治理文件內該形態零檢查（本包以自己的證據節初稿實測坐實：`NONVERB_RESIDENCE_RE` 命中、非 code span 非圍籬，而 `--check` 仍 rc=0）；P7-5 已把判準⑥ 改用同一份 `_pointer_audit_files()` SSOT、例外沿用 (甲) code span ＋ (丙) 圍籬，並**刻意不設 (丁) 對等例外**（沒有 ID 就根本不命中、無物可豁免）。Pkg-P5 交件前複驗該修復已在位。見證據檔 `## DEF-101-571` 節 |
| DEF-101-579 | 2026-07-29 | R60 r2 Architect 複審（`ARCH-R60R2-06`）的 r3 續量——判定與處置已在 `DEF-101-565`／`561`，本列只補量測 | **護欄層規模趨勢續量**。Pkg-P5 r3 親跑實測：`tools/tests/*.py` **56 支**（r1＝52、r2＝56）、行數 **23,599 → 23,999**（r1＝20,188、r2＝22,524）；`check_script_parity.py` ＝**13 對 + 18 支單邊**（與 r2 相同、**未下降**）rc=0；`run_root_unittests.py` 獨占跑兩次為 **906** 與 **916** 支（皆 OK／skipped=11／rc=0） | P3（量測義務，非新缺陷） | 根層 `docs/06_quality/`（趨勢帳） | fixed@R60 r3（量測義務已履行）。🔴 **本列數字是移動標的、非凍結值**：兩次量測之間 Pkg-P4 仍在落地，故 906→916；**權威值依 `MIN_TESTS` 自己訂的重釘紀律辦**（主控在所有並行包停工後實跑填實測值、不做加減推算）。🔴 誠實揭露：r3 的修復**再次讓護欄行數上升**（22,524→23,999），緩解是**零新增鎖檔**（全部擴充既有檔、支數維持 56）＋把 ONBOARDING §7 表② 那 4 格「零機制」收進既有機制 ⇒ 未受檢面淨減少；但**行數仍在漲，ARCH 的警告第三次成立**。另訂正任務書轉述：「23,329 行」「總數 845」實為 **23,599~23,999 行**與 **906~916 支**（845 是下限常數、非總數）。見證據檔 `## DEF-101-579` 節 |
| DEF-101-568 | 2026-07-29 | R60 r3 四方複審（`SD-R60-R2-05` ①②③／`SA-R60R2-04` ③／`ARCH-R60R2-04`＋`SD-R60-R2-06` ⑤） | **round 2 才落地的 ADR C1/C2 機械鎖自身五缺**（`tools/tests/test_adr_xplat001_c1c2_lock.py`）：①日界用嚴格 `>` 而本輪新列同日 ⇒「日界後新列塞不進基線」對自己產出不成立；②`shrink-only` 只看「筆數 ≤ 上限」，上限改大不紅＝人審慣例非機制；③掃描面只有主檔，而 §4.3.1 典型狀態 `wontfix` 是歸檔判準① 允許搬遷的 ⇒「新增違規列＋同輪歸檔」整條繞過；④`_MIN_LEDGER_ROWS=60` 餘裕見底；⑤宣告「筆數不寫在散文裡」後自己寫死三處 | P1 ×2（①②宣稱有鎖實為慣例）／P2（③繞道面，實查尚未被利用）／P3 ×2（④⑤） | 根層 `tools/tests/` ＋ ADR-XPLAT-001 §4.3.4 | fixed@R60 r3（Pkg-P2 修、Pkg-P5 重驗）：根因＝判準綁日曆＋掃描面過窄。改 `_BASELINE_ID_CEILING` 字典序上界（與 ADR §4.3.4 雙向綁定）、`ratchet_problems()` 真棘輪（`git show HEAD:`）、掃描面枚舉 import `_family_files()`、`_MIN_FAMILY_ROWS=665`、`_BARE_COUNT_RE` 散文自檢。Pkg-P5 重驗：家族 32 檔、§4.3.1 共 13 筆、archive 側 unmet 2 筆皆早於上界 ⇒ **零假紅**、`hard_block_offenders()` 回 `{}`。見證據檔 `## DEF-101-568` 節 |
| DEF-101-578 | 2026-07-29 | R60 r3 主控自查（**流程缺陷，非程式缺陷**；非四方回報） | **Pkg-P1／Pkg-P3 因 API session 上限中途死亡，留下半套修改在磁碟**：r3 派出三包並行修復，其中兩包被 session 上限中途砍斷，**但它們已寫入磁碟的修改仍在**——`archive_defect_log.py` 留下未定義的 `FAMILY_ONLY_SYNTAX_DOCS`（NameError，11 支 ERROR）、`check_defect_log_crossref.py` 的新斷言缺主檔權威散文（4 支 FAIL），且兩包都沒來得及交 `cross_package_requests` | P2（流程／協作缺陷；工作樹處於不可交付的半套狀態而無人知） | 迭代流程紀律（根層 `docs/06_quality/`） | fixed@R60 r3。🔴 **根因與教訓**：本 repo 已記載「agent 回傳 null 不等於沒動磁碟」，**本次是同一形態的第二次實例**（死因是 API 上限而非 API 錯誤）。處置紀律：agent 異常結束後**一律先獨立盤點磁碟真實狀態**（`git status --short` ＋ `git diff --numstat` 逐檔 ＋ 實跑相關測試取錯因），**不採信任何「它應該沒做完所以沒影響」的推測**——本次即靠實跑 root 全套逐支歸因，才發現兩包各自完成了大部分工作、只差收斂。Pkg-P5 交件時實查 `FAMILY_ONLY_SYNTAX_DOCS` 已全庫零命中。見證據檔 `## DEF-101-578` 節 |
| DEF-101-574 | 2026-07-29 | R60 r3 SD 複審（`SD-R60-R2-09`） | **`.ps1` 守門鎖的寬化窄縫（假綠）**：`test_local_ci_gate_shell_arg_parity.py` 判準中的 `[^)]*` 容許任意前置，於是**寬化守門** `if ($Act -or $PytestArgs)` 實測 `total=7 red=0`＝假綠，而實跑時 `-Act` 為真且未給 pytest 參數就會把空字串送進核心＝同原缺陷後果。正控確認 `if ($true)` 注入會 5 red，故是**窄縫而非整體失效** | P2（鎖存在但對真正會發生的寬化方向無鑑別力） | AutoClaude 側 `tests/tools/` | fixed@R60 r3：根因＝判準只排除窄化方向、未排除寬化。收緊為「條件必須**恰好**是 `$PytestArgs` 真值判斷」（`_PS1_PYTESTARGS_GUARD_RE`，Pkg-P5 實查逐字），對抗式自測 total 17 項、`widened_or` 由 red=0 轉 **5 failed／rc=1**。🔴 **誠實劃界（Pkg-P5 實查、非轉述）**：本鎖自此是「符合單一寫法」型守門，**窄化守門也會誤紅**——`_PS1_NARROWED_FORMS` 明列兩種**語意更嚴且更正確**的寫法會被擋下，檔頭已寫成明文邊界（非隱藏缺口）；改用它們須同步放寬判準。見證據檔 `## DEF-101-574` 節 |
| DEF-101-569 | 2026-07-29 | R60 r2 四方**全數獨立命中**（`ARCH-R60R2-01`／`SA-R60R2-01`／`SD-R60-R2-01`／`QA2-R60-03`） | **歸檔索引涵蓋性零判準**：磁碟 31 支 archive、主檔索引只 30 條 bullet、`archive_31` 零登記、標題寫死「三十檔」，而當時四項判準完全不看索引 ⇒ `--check` 照印 rc=0；且**同一支閘門在同一 session 印的是「32 檔」**（家族檔數），兩個矛盾數字並存於同一份輸出而無人被擋 | P2（閘門對自己輸出的內部矛盾無感知） | 根層 `tools/archive_defect_log.py` | fixed@R60 r3：根因＝建立 archive 與登記索引不是同一個動作。判準⑤ 上線（bullet 數 == 實查檔數，**雙向**）＋`apply()` 自己寫 bullet 並把新增位元組列入守恆算式（`新主檔 + 釋出 == 舊主檔 + 新增`）⇒ 漏登記在結構上不可能；補登 `archive_31`；份數改**現場核算不寫死**（主控採 SD「別再寫死」而非三方的「三十一檔」）；四支注入測試。Pkg-P5 實測 `--check` rc=0，判準⑤ 斷言為「歸檔索引 31 條 bullet 對 31 支 archive」。見證據檔 `## DEF-101-569` 節 |
| DEF-101-572 | 2026-07-29 | R60 r2 SA 複審（`SA-R60R2-06`） | **帳本狀態欄首詞合法值硬斷言未裝**：r1 已證 `_classify()` 的寬鬆前綴比對讓 `partially-fixed` 靜默命中 `fixed`（「只修了一部分」在閘門眼中等於「已修」）；r2 雖清乾淨 4 筆存量非法首詞（`DEF-101-556`）**但產生它們的機制沒變**，且該斷言在程式與測試皆零命中 ⇒「把樣本清乾淨＝把鎖裝好」的錯覺 | P2（把樣本清乾淨誤當機制落地） | 根層 `tools/check_defect_log_crossref.py` ＋ 主檔《格式定義》 | fixed@R60 r3：根因＝合法值從未有機械來源。新增 `status_first_word_problems()`，合法值與主檔《格式定義》**權威散文雙向綁定**且刻意**仍逐列比對**（否則改壞散文即為關鎖捷徑）；**零白名單上線**。Pkg-P5 實測閘門 rc=0，訊息載明「狀態欄首詞皆落在《格式定義》宣告的 7 個合法值內」。測試側正負樣本由 Pkg-P4 補齊——兩次量測皆全綠，但支數在量測期間由 41 → 51（Pkg-P4 仍在落地），**故本列不寫死支數**。見證據檔 `## DEF-101-572` 節 |
| DEF-101-576 | 2026-07-29 | R60 r3 主控自查（**非四方回報，如實標明無 finding id**） | **pre-push dispatcher 測試 fixture 未隨守門迴圈擴充**：主控把 `sync_onboarding_baselines.py --check-snapshot` 接為 pre-push leg ③ 第 8 支守門後，`tools/tests/test_pre_push_dispatcher.py` 建的暫存 fake repo 沒有那支檔 ⇒ 3 支測試紅 | P2（新接守門與其 fixture 不同步，落地即紅） | 根層 `tools/tests/` ＋ `tools/git-hooks/pre-push` | fixed@R60 r3：根因＝守門清單與 fixture 無機械連動。fixture 補 stub 並**照既有慣例檢查 argv**，使該測試同時成為「守門迴圈項可帶子指令、分詞機制正確」的端到端驗證——Pkg-P5 實查 `pre-push:223~228` 迴圈確有 8 項、其中**兩項各帶不同子指令**，兩項併看即證明分詞對多項皆成立。實測 `Ran 10 tests OK` rc=0。見證據檔 `## DEF-101-576` 節 |
