# AutoSDD Defect Log — Archive 48

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **19 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-716`、`DEF-101-719`、`DEF-101-713`、`DEF-101-711`、`DEF-101-729`、`DEF-101-715`、`DEF-101-731`、`DEF-101-717`、`DEF-101-720`、`DEF-101-724`、`DEF-101-727`、`DEF-101-723`、`DEF-101-725`、`DEF-101-728`、`DEF-101-722`、`DEF-101-730`、`DEF-101-718`、`DEF-101-732`、`DEF-101-712`
>
> **本次操作備註**：R69 第三波帳本統一配號包收尾：本輪新立 DEF-101-711~733 共 23 列，主檔加入後達 287,732 bytes、超過 262,144 硬線（check_defect_log_crossref 由警告轉 FAIL）。依判準①②③⑤⑥ 搬遷可搬的 19 筆已結列（皆為 R69 本輪新立之 fixed/wontfix 列，與 R60 archive_30/31 同形態——動工前的可搬清單已無舊列可搬，程式即判準、不另做人工二次篩選）。留在主檔的 R69 列＝DEF-101-714（watch 護欄成長）／721（open 跨包）／726（watch LOC 容量）／733（partial 本機綠≠對面綠），皆為活躍列。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-716 | 2026-08-02 | R69 第三波追問「`DEF-101-628` 有鎖，為什麼 `DEF-101-715` 還能發生」 | **看守 `DEF-101-628` 的測試是「造假直譯器」，結構上不可能抓到 import-time 失敗（測試有效性缺陷，P1）**：`tools/tests/test_dev_start.py::_FAKE_39_SHIM` 的作法是「用**真的 3.11** 直譯器開跑，再於行程內改寫 `sys.version_info=(3,9,6,...)`」——底下跑的仍是 3.11，`from datetime import UTC` 在它身上**永遠成功**。⇒ 它只驗得了「版本**判斷分支**走不走得對」，驗不了「prelude 在真的舊直譯器上**載不載得動**」。**實測憑證（最刺眼的一項）**：在 `DEF-101-715` 那個缺陷**活著**的時候，`pytest -k TestPickPythonGeMin` 仍 **6 passed** | P1 | 根層 `tools/tests/test_dev_start.py`（**依「不新增 tools/tests/ .py 檔」棘輪，全部加進既有檔**） | fixed@R69（第三波）：補**兩層真鎖**。① `TestRealSubMinInterpreterPrelude` — 以**真的** < `_MIN_PY` 直譯器（`/usr/bin/python3` → `python3.9`／`3.10`／`3.8`／`3.7` 候選鏈）subprocess 實跑，斷言 `rc==2`、stderr 含友善訊息與 `brew install python@3.11`、且**不含 `Traceback`**；找不到舊直譯器時 skip 訊息帶 `[TOOL-MISSING]` 前綴（fail loud，不靜默綠）；另一支斷言「版本閘訊息指名的補救路徑 `bootstrap_core.py`」在真 3.9 上 `import` rc==0——否則那句指引是**死路**。② `TestPy39PreludeStaticScan` — **恆跑、零環境依賴**：射程由 AST **推導**（dev_start 版本閘以前的切片 ＋ `bootstrap_core` 整支 ＋ 遞迴納入其 import 到的 `tools/`／`tools/lib/` 本地模組整支檔），對射程做 (a) 3.10/3.11+ stdlib 名字黑名單掃描、(b) `ast.parse(feature_version=(3,9))` **零策展文法鎖**（實測會擋 `match` 陳述式）、(c) **射程推導自檢**（必含 `_stdio_utf8`、必**不**含帶 `datetime.UTC` 的 `ci_liveness`，防射程悄悄崩塌）、(d) 黑名單棘輪（必留 `UTC` 與 `tomllib` 兩個真兇）。憑證＝修前紅 2 筆、修後 `6 passed` |
| DEF-101-719 | 2026-08-02 | R69 四方複審 R2（Architect／SD 交叉）追問「新落地的 `tools/ruff.toml` 是誰在跑」 | **R69 新增的 `tools/ruff.toml` 全 repo 零執行者**——沒有任何 CI step、git hook 或工具跑它。⇒ 它自陳要治的病（`R68-38`：對根層護欄層跑 ruff 得到假綠）**原樣留著**，只是把假綠從「ruff 出廠預設」換成「**根本沒人跑 ruff**」，帳面上卻可被讀成已修。**另一處錯的指路**：`tools/ruff.toml:15` 把對齊性棘輪鎖指向 `tools/tests/test_check_defect_log_crossref.py::TestRootToolsLintPolicy`，而該檔內**沒有**這個類別——錯的指路比沒有指路更糟，它讓下一個人以為查過了 | P2 | 根層 `.github/workflows/root-infra-ci.yml`（新增第 16 道）＋ `tools/git-hooks/pre-push`（root-infra 快層第 ④ 段）＋ `tools/ruff.toml:15`（訂正指路） | fixed@R69（第三波）。憑證＝**修前** `git show HEAD:tools/git-hooks/pre-push \|\| grep -c ruff` = **0**、`root-infra-ci.yml` 同為 **0**；接上兩側執行者後，注入 `tools/dev_start.py` 一行 120 字元 → `ruff check tools/` 印 `E501 Line too long (161 > 100) --> tools/dev_start.py:2013:101 / Found 1 error / rc=1`，還原 → `All checks passed! / rc=0`。快層耗時實測 **0.04s**（冷 cache 0.02s）＝可放進「任何 push 皆跑」的快層。指路已改為 `tools/tests/test_subprocess_encoding_hygiene.py::TestRootToolsLintPolicy` 並附訂正說明。**防回流鎖**＝`tools/tests/test_root_infra_parity.py::TestRootToolsRuffHasExecutors`（三支：CI 側有執行者／pre-push 側有執行者／兩者皆不得改用 `--config`，理由見 `DEF-101-720`）。🔴 **本筆只修根層 `tools/` 半邊**；`R68-38` 原點名的 `AISDLC_SDD` 全樹仍零 ruff 設定與零執行者，見 `CrossPlatform_R68_Scan_Findings.md` R68-38 的 partial 狀態欄 |
| DEF-101-713 | 2026-08-02 | R69 四方複審 R2（Architect）判定「同型第三次復發」後獨立立案 | **「文件寫死機器當場可算出的數字」已成本 repo 的復發家族，且本輪首度擴散到 ADR 正文**。譜系：`ONBOARDING.md` §7（`DEF-101-289`／`DEF-101-515`）→ `tools/run_root_unittests.py::MIN_TESTS` 一輪三釘（`DEF-101-701`）→ `ADR-XPLAT-002` §4.3.1 成長率常數（R67 round 4）→ **R69 命中 `ADR-XPLAT-003` §3**（`DEF-101-711`）。每一次的修法都只治「那一份檔」，家族本身零機械物看守 ⇒ 下一份文件照樣可以寫死 | P2 | 根層 `tools/tests/test_doc_loc_baseline_freshness_r60.py`（掃描面擴及 `docs/04_planning/ADR/*.md`） | fixed@R69（第三波，**機械化**）：新增 `TestR69AdrMeasurementTokensAreLive`（12 支，含 9 支紅綠自證；**未新增 .py 檔**，加進既有檔）。鎖的四件事＝(1) `ADR-*.md` 內出現的 `total=`／`baseline=`／`cap=` 必須逐字等於 `check_loc_budget.py --json` **現查**值；(2) `violations=` 與 `<3 位數以上> passed` 一律禁載（那是每次執行都會變的量測 token，寫進 ADR 即製造無看守 stale 站點）；(3) 豁免必須具名理由且**逐行**生效（如 `adr-measurement-historical`），不得整檔開後門；(4) **掃描面崩塌 fail-loud**（`ADR-*.md` glob 打空即紅，防「射程悄悄變零仍全綠」的空砲形態）。已由 `tools/run_root_unittests.py` 消費（rc=0，實測計數以該工具當次輸出與 `ONBOARDING.md` §7 表② 為準，本列刻意不寫死） |
| DEF-101-711 | 2026-08-02 | R69 四方複審 R2（Architect）逐字複現 `ADR-XPLAT-003` §3 的量測宣稱時 | **ADR 正文寫死的三個量測值在交付樹上一個都複現不出來**：§3 原載 (a) `total=20415`（實測 `total=20436`）、(b)「改後餘裕 23 行」（實測餘裕 2 行、`autoclaude/` 生產碼凍結**完全未解除**）、(c) `3923 passed`（實測 3929 passed / 146 skipped）。根因＝**射程偷換 ＋ 量測時點漂移**：−21 LOC 是 §3 表列**那四個檔**的淨值（為真），但同輪其他修復包已把該額度全數消耗，全樹淨值為 0；ADR 卻把「本包四檔的 Δ」講成「全樹的餘裕」。ADR 是**架構決策的權威文件**，其量測宣稱被下游 `DEF-101-706`／`ADR-XPLAT-002` 二次引用，錯一次即擴散 | P2 | 根層 `docs/04_planning/ADR/ADR-XPLAT-003-*.md` §3／§8 ＋ `docs/06_quality/AutoSDD_Defect_Log.md` `DEF-101-706` | fixed@R69（第三波）：§3 三處已改為現查值並在 §8 保留「R69 訂正前原文逐字」歷史段（標 `adr-measurement-historical` 豁免註記，依 repo 紀律歷史訂正段不得改寫）；`DEF-101-706` 狀態欄同步訂正並改為指向 live 來源。憑證＝`python -m unittest tools.tests.test_doc_loc_baseline_freshness_r60.TestR69AdrMeasurementTokensAreLive`：修前 FAILED（逐筆點名上述 token）／修後 `Ran 12 tests ... OK` |
| DEF-101-729 | 2026-08-02 | R69 四方複審 R2（SD）循 `DEF-101-728` 同型普查時 | **同型的隱蔽版：用 `try/except ImportError` ＋ `skipIf` 包住跨樹 import ⇒ CI 上永遠 skip、永遠不報錯、也永遠沒在測**：`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_state_component_sanitizer_parity.py` 以此形態包住 `import autoclaude...`。它比 `DEF-101-728` **更危險**——後者至少會紅給你看，前者是**綠著爛掉**：這支「交叉一致性鎖」自落地起在 CI 上**從未真正執行過**一次 | P1 | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/`（LATEST 版；凍結版依 Copy-on-Evolve 不回改） | fixed@R69（修復波）：該檔已移除（`git status` 實測 `D  AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_state_component_sanitizer_parity.py`）。憑證＝`git show HEAD:<該檔>` 首行為「交叉一致性鎖：state_loader._sanitize_component 與 AutoClaude 側…」（修前存在且確為跨樹鎖）；交付樹該目錄下同名檔不存在。🔴 **誠實揭露的代價**：移除等於**承認這道跨樹一致性鎖從來沒有在 CI 上生效過**，並不等於「該一致性已被別處保障」——若確實需要這道鎖，正解是改以**契約檔／黃金樣本**（兩側各自讀同一份資料，不互相 import）重建。承接輪次：**未指派** |
| DEF-101-715 | 2026-08-02 | R69 第三波以 macOS **系統** python3（`/usr/bin/python3`，3.9.6）實跑 `tools/dev_start.py` 入門路徑時 | **版本前置閘失效復發，`DEF-101-628` 修好的友善訊息被整個打回原形（P1，直接擋住 macOS 新人入門）**：R69 期間 prelude 第 53 行被加入 `from datetime import UTC`（`UTC` 別名 **3.11 才有**；疑為 UP017/UP035 lint 自動修正之副作用——同一 hunk 併同把 `typing.Callable` 改成 `collections.abc.Callable`），而該行位在 `_MIN_PY` 版本閘**之前** ⇒ 直譯器在 **import 期**就炸，使用者看到的是 `ImportError` traceback、`rc=1`，而非閘門契約定義的友善最低版本訊息＋`rc=2`。**教訓**：版本閘只保護「閘之後」的程式碼，**閘之前的 prelude 是它自己的盲區**，而 lint 自動修正會**無差別地**動 prelude | P1 | 根層 `tools/dev_start.py`（prelude／版本閘） | fixed@R69（第三波）：`UTC` import **下移至版本閘之後**（帶 `# noqa: E402` 並附逐字理由），並在閘旁釘下**結構性約束註解**「本閘之前的 prelude 必須在 < `_MIN_PY` 的直譯器上可載入」。憑證＝修前 `/usr/bin/python3 tools/dev_start.py --help` → `ImportError` traceback ＋ `RC=1`；修後同一指令 → 友善訊息 ＋ `RC=2`、**零 Traceback**。防回流鎖見 `DEF-101-716` |
| DEF-101-731 | 2026-08-02 | R69 四方複審 R2（QA）對 `check_defect_log_crossref.py` 做變異測試 | **自稱 shrink-only 棘輪，但全檔零比較、零強制**：`_UNPINNED_HANDOVER_CEILING` 自 R68 上線起，註解宣稱「只准變小」，實際上**只被讀進成功訊息裡印出來**。實測（修復前）：往帳本插一列散文式延後的未結列、再把該 ID 加進白名單，工具印的是「存量豁免 58 筆／棘輪上限 57」而 **`rc=0`**——**天花板被超過的事實原樣印在成功訊息裡，卻不擋**。⇒ 白名單可隨每輪「順手加一筆」無聲膨脹，最終退化成只進不出的死名單，正是 `stale_grandfather_problems()` 想防、卻只防住另外半邊的形態 | P2 | 根層 `tools/check_defect_log_crossref.py`（新增 `grandfather_ceiling_problems()`） | fixed@R69（修復波）：新增純函式 `grandfather_ceiling_problems()` 擋**膨脹**（`len > ceiling` 即紅），與既有 `stale_grandfather_problems()` 擋**不縮**構成完整棘輪（兩者缺一都不成棘輪，該函式 docstring 逐字記載分工）。刻意**不吃參數、不看帳本**（對原始碼常數本身的斷言），並在 `main()` 裡**無條件**執行、不受 `_DEFECT_LOG == _DEFAULT_DEFECT_LOG` 守衛約束——否則換一本帳本就能繞過棘輪，那又是一個假鎖 |
| DEF-101-717 | 2026-08-02 | R69 四方複審 R2（QA）對本輪**新造**的守門鎖做變異測試 | **`assertNotRegex` 漏 `re.MULTILINE` 造成的假鎖——而且是本輪自己新造的**：`tools/tests/test_workflow_permission_concurrency_lock.py::TestRootInfraNightlyStalenessSentinel::test_sentinel_is_blocking_not_advisory` 寫 `assertNotRegex(step, r"^        if:")`，但 `assertNotRegex` 走 `re.search` 且**預設不含 MULTILINE**，而 `step` 是**多行**文字 ⇒ `^` 只錨到整段第一個字元，**任何後續行的 step 層 `if:` 都匹配不到**。後果：本輪頭號 P1 哨兵（root-infra-ci 第 15 道 nightly 陳舊度）可被 `if: false` 整道停用，而該檔 23 支**全綠** | P3 | 根層 `tools/tests/test_workflow_permission_concurrency_lock.py:454` 附近 | fixed@R69（第三波）：改為 `r"(?m)^        if:"` 並在該行上方釘下「`(?m)` 不可省」的逐字理由註解。憑證＝注入 `        if: false` → **23 passed（假綠，缺陷實證）**；改用 `(?m)` 後同一注入 → **1 failed（`AssertionError: Regex matched: '        if:'`）**；還原注入 → 23 passed。**附帶普查**：以 AST 掃全 repo 得 **64 個** anchored `assertRegex`／`assertNotRegex` 呼叫點，逐一核對確認**唯此一處**為真踩雷（其餘或為單行輸入、或已帶 `(?m)`） |
| DEF-101-720 | 2026-08-02 | R69 第三波接 ruff 執行者時，第一版寫 `--config tools/ruff.toml` 當場踩雷 | **判準記載（供未來勿再踩）：對根層 `tools/` 樹跑 ruff **不得**帶 `--config tools/ruff.toml`**。原因＝帶 `--config` 時 ruff 的 **project root 變成 cwd** 而非設定檔所在目錄，於是 `[lint.per-file-ignores]` 的**相對 pattern** 與 isort 的 src 判定**全部錯位**。同機實測：`ruff check tools/ --config tools/ruff.toml --statistics` → **136 E501 ＋ 4 I001 = Found 140 errors**；`ruff check tools/`（走 ruff 自己的「由檔往上尋找」，`--show-settings` 證實 `Settings path` 即 `tools/ruff.toml`）→ **All checks passed!**。同一份設定、同一棵樹，兩種叫法差 140 筆 | P3 | 根層 `tools/ruff.toml` 檔頭 ＋ `.github/workflows/root-infra-ci.yml` 第 16 道註解 ＋ `tools/git-hooks/pre-push` 第 ④ 段註解（三處皆已逐字記載） | wontfix/by-design（**這是 ruff 的既定語意，不是本 repo 的缺陷；立帳目的是把踩雷經驗釘死，避免下一個人「好心」加上 `--config`**）。已由 `tools/tests/test_root_infra_parity.py::TestRootToolsRuffHasExecutors` 第三支機械看守（兩側執行者皆不得出現 `--config`） |
| DEF-101-724 | 2026-08-02 | R69 四方複審 R2（Architect）對本輪新落地的 waiver 機制做時間軸推演 | **`WAIVER_UNTIL` 到期後本機零示警——鎖只擋「太長」、不擋「已過期」**：`.github/workflows/root-infra-ci.yml` 的 `WAIVER_UNTIL: "2026-08-10"`，既有三條鎖只驗「不得超過 `MAX_WAIVER_DAYS` 上限」，而過期後 `(until - today).days` 變**負數**、恆滿足上限 ⇒ **過期 51 天本機仍全綠**，要 push 到 CI 才炸。這是典型的「單向邊界只驗一邊」 | P3 | 根層 `tools/tests/test_workflow_permission_concurrency_lock.py`（新增 `waiver_expiry_verdict()` 三態純函式） | fixed@R69（第二／第三波）。憑證＝**修前**模擬今天＝`2026-09-30`（過期 51 天），現行三條鎖仍 **PASS**；**修後** `waiver_expiry_verdict()` 三態實測：`08-06` → **ok**／`08-07`~`08-10` → **warn**（≤3 天，印 ⚠️ **不擋**）／`08-11` 起 → **expired**（`self.fail`）。訊息逐字指明處置＝**唯讀** `gh run list` 確認排程已成功後，把 `WAIVER_UNTIL` **清成空字串**（🔴 **不是往後推日期**——推日期是把過期時點往後挪一輪，不是修復，同 `DEF-101-569` 判過的形態）。該檔 **23 → 25 passed** |
| DEF-101-727 | 2026-08-02 | R69 四方複審 R1／R2 交叉（`375f291` 押上 main 後 `windows-compat-ci` 由綠轉紅，run 30720156050） | **測試以 POSIX 絕對路徑字面值斷言 Path 內插後的訊息 ⇒ `windows-compat-ci` 由綠轉紅**：斷言寫死 `/Users/...`／`/tmp/...` 之類的 POSIX 字面值，而受測程式是把 `Path` 內插進訊息——同一份程式在 Windows 上內插出 `\` 分隔的字串，字面值比對必不中。**這是 R68 交付的回歸**：`24c5f34` 四支 CI 全綠，`375f291` 後 `windows-compat-ci` 轉紅 | P2 | 根層 `tools/tests/`（多站點）＋ `tools/tests/test_platform_neutral_paths.py`（守門面） | fixed@R69（修復波）：各站點改為以 `Path`／`os.sep` 組出期望值再比對（或改斷言路徑**片段**），並以 `test_platform_neutral_paths.py` 靜態守門防回流。🔴 **零真機驗證的誠實揭露**：本 repo **無 Windows 真機**，本筆的 Windows 側只能靠 `windows-compat-ci` 雲端 job 證實；本輪依「GitHub CI 只准唯讀」紀律**未 push 驗證**，故 Windows 側現況為「本機模擬綠、對面未複驗」。承接輪次：**未指派**（觸發點＝下一次合法 push 後唯讀 `gh run list` 複核 `windows-compat-ci`） |
| DEF-101-723 | 2026-08-02 | R69 四方複審 R2（SA）追查「A1 只修一處，姊妹站點呢」 | **`str(mock.call(...))` 的 repr 拼裝輸出再斷言路徑＝Windows 必假紅家族，且第一波只修了一處**：repr 會**轉義路徑分隔符**（`\` → `\\`），使針對 Windows 路徑的 `assertIn` 恆為 False。R69 第一波（A1）只修 `tools/tests/test_dev_start.py` 一處，**姊妹站點 4 筆仍是舊寫法且零守門**：`tools/tests/test_dev_start.py:2656`、`AutoClaude/tests/test_perception.py:779/805/847` | P2 | 根層 `tools/tests/test_platform_neutral_paths.py`（新增靜態守門）＋ `AutoClaude/tests/test_perception.py`（三處改寫） | fixed@R69（第二／第三波）。憑證＝`str(mock.call(r'…\elsewhere\AutoClaude\…'))` 得 `call('… \\elsewhere\\…')` ⇒ 路徑 `assertIn` 為 **False**，改取 `.args` 後為 **True**。AST 全掃四棵測試樹＋生產碼樹：**修前 4 筆、修後 0 筆**。防回流鎖＝`tools/tests/test_platform_neutral_paths.py::TestNoMockCallObjectRepr`（含豁免標記 `call-repr-ok:` ＋ **stale 偵測**〔豁免已不需要即紅〕＋ **對真檔修復前形態自證**：`test_dev_start` 修前 2 筆／`test_perception` 修前 3 筆，修後皆 0） |
| DEF-101-725 | 2026-08-02 | R69 第三波實跑 `python tools/run_root_unittests.py` 檢視 skip 明細時 | **安全網樣本被逐筆歸檔，會讓看守它的鎖無聲縮到零樣本**：`tools/tests/test_archive_defect_log.py::TestCriterion2Narrowing::test_handoff_net_is_untouched_by_the_narrowing` 在樣本被歸檔時直接 `self.skipTest`；B3 包的 `archive_47` 搬走 `DEF-101-524` 後，它成為根層**唯一未標籤的「樣本流失型」skip**（明細印 `[未標籤] …(def_id='DEF-101-524')`）。⇒ 樣本一筆筆被合法歸檔，這道安全網就一格格失效，而且**是靜默的** | P3 | 根層 `tools/tests/test_archive_defect_log.py` | fixed@R69（第二／第三波，**刻意選「修掉讓它真的跑」而非「補個標籤讓 skip 合規」**——後者只是讓失效變得好看）。改法＝對**仍在主檔**的樣本照跑，並加 `_MIN_LIVE_HANDOFF_SAMPLES=2` 的 fail-loud **下限**（存活樣本掉到下限以下即紅，逼人補新樣本而非放它縮到零）。憑證＝`python tools/run_root_unittests.py` 修前 skip 明細共 16 支、含 **1 支未標籤**；修後 `OK (skipped=15)`、明細 **15 支全數 `[已標籤]`** |
| DEF-101-728 | 2026-08-02 | R69 四方複審 R2（SD）追查 `aisdlc-sdd-ci` 於 `375f291` 轉紅的根因 | **子專案測試跨樹 `import autoclaude.*`，使 `aisdlc-sdd-ci` 直接 `ModuleNotFoundError`（pydantic）**：`AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py` 內含 `import autoclaude...`，但 `aisdlc-sdd-ci` 的環境**刻意只裝** `AISDLC_SDD/AISDLC_SDD_v0.01/requirements-ci.txt`（`pyyaml`＋`pytest`），不裝 AutoClaude 的相依 ⇒ collection 期即炸。**根本問題是架構的**：AISDLC_SDD 與 AutoClaude 是兩個獨立子專案，子專案測試跨樹 import 等於偷偷把對方的相依變成自己的硬相依 | P1 | `AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py` | fixed@R69（修復波）：移除跨樹 import，改以**不依賴 AutoClaude runtime** 的方式取得受測資料（該檔現行僅在**註解**中提及 `from autoclaude.utils.logger import ...` 作為反例說明）。憑證＝`git show HEAD:AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py \|\| grep -c autoclaude` = **2**（修前）；交付樹同一 grep 對**程式碼行** = **0**（僅存 1 處註解引述）。同型隱蔽版另見 `DEF-101-729` |
| DEF-101-722 | 2026-08-02 | R69 四方複審 R2（QA）對「註解宣稱 vs 實作射程」做第二輪普查 | **註解代言了未實作的射程，造成 100% 放行的恆綠空砲**：`AISDLC_SDD/scripts/tests/test_cross_subproject_import_isolation.py` 的 `_SDD_PACKAGE_PREFIXES` 註解宣稱「動態自磁碟取模組名」，實作卻是**寫死的 2 元組** ⇒ `AISDLC_SDD/scripts/*.py` 那 **14 支共享 infra 的反方向越界 import 全部放行**。實測憑證（修前）：`foreign_imports('from component_sanitizer import x', _SDD_PACKAGE_PREFIXES)` 回 **`[]`** | P2 | `AISDLC_SDD/scripts/tests/test_cross_subproject_import_isolation.py` | fixed@R69（第二／第三波）：改為自磁碟 glob 推導，前綴集合由 2 枚增至 **16 枚**；同一輸入修後回 `[(1,'component_sanitizer'),(2,'sdd_version')]`。另加 `_MIN_SDD_SCRIPT_MODULES=10` 的 **import 期 fail-loud**（glob 打空即 `AssertionError`，防「射程悄悄變零仍全綠」——即本列在治的病自己復發），並以 `test_empty_glob_is_fail_loud_not_silently_empty` 實測綠。該檔 **12 → 24 passed** |
| DEF-101-730 | 2026-08-02 | R69 四方複審 R2（QA）照著哨兵印出的處置指令實際操作時 | **哨兵的查詢條件與它自己印的處置指令互斥 ⇒ 照做永遠解不開**：`root-infra-ci.yml` 第 15 道 nightly 陳舊度哨兵原本寫死 `gh run list --event schedule`，而它印給使用者的處置指令 `gh workflow run <wf>.yml` 產生的是 `event=workflow_dispatch` ⇒ **兩個集合互斥**：使用者照指令跑一次 nightly、CI 仍宣稱「查無成功紀錄」。這是「指路本身是死路」的第二例（第一例見 `DEF-101-716` 的 `bootstrap_core` 補救路徑） | P2 | 根層 `.github/workflows/root-infra-ci.yml`（第 15 道） | fixed@R69（修復波）：改為 **`schedule` 與 `workflow_dispatch` 兩事件都查、取較新者**（實作見該檔 L487-506 的 `for ev in schedule workflow_dispatch` 迴圈），並在註解逐字記載互斥根因（L427-432）。理由正當性＝目標 workflow 的 `if:` 條件本來就寫 `github.event_name == 'schedule' \|\| github.event_name == 'workflow_dispatch'`，兩事件跑的是**同一段**完整測試樹 |
| DEF-101-718 | 2026-08-02 | R69 四方複審 R2（QA）逐條核對「docstring 宣稱 vs 實作」 | **宣稱與實作不符的空轉鎖**：`tools/tests/test_subprocess_encoding_hygiene.py::TestRootToolsLintPolicy::test_the_config_actually_covers_the_root_tools_tree` 的 docstring 逐字宣稱「以 `ruff check --show-settings` 驗證設定確實覆蓋根層 tools/ 樹」，實作卻只有 `self.assertTrue(_overlong_line_count(_TESTS_DIR) >= 0)`——**恆真**，`ruff` 根本沒被呼叫。⇒ 讀 docstring 的人以為「設定覆蓋面已被機械看守」，實際上零看守 | P3 | 根層 `tools/tests/test_subprocess_encoding_hygiene.py`（`TestRootToolsLintPolicy`） | fixed@R69（第三波，採方案 (a)：**真的去跑 ruff**）。憑證＝注入①（把 `tools/ruff.toml` 移離 `tools/`）→ **1 failed**（`--show-settings` 輸出無 `Settings path:` 行）；注入②（把 `select` 改成 `["F"]`）→ **1 failed**（輸出中找不到 `line-too-long (E501)`）；還原 → **1 passed**；`ruff` 不在 PATH 時 → **SKIPPED 並標 `[TOOL-MISSING]`**（不靜默綠） |
| DEF-101-732 | 2026-08-02 | R69 四方複審 R2（QA）對 `AutoClaude` 側跨平台鎖做變異測試 | **對 `start_new_session` 的防護是字串比對，常數 import 行即可滿足**：`AutoClaude/tests/test_evaluator_kill_tree.py` 原本唯一的防護是在原始碼裡 grep 字串 `start_new_session` ⇒ **只要檔案任一處出現該字串**（哪怕只是註解或常數定義）即算通過，真正的 `Popen(..., start_new_session=True)` 被拿掉也照樣綠。實害＝少了它，子行程與 AutoClaude **同 pgid**，逾時回收的 `killpg` 會**打在 AutoClaude 自己身上** | P2 | `AutoClaude/tests/test_evaluator_kill_tree.py` | fixed@R69（修復波）：改為**行為斷言**——直接檢查 `Popen` 實際收到的 kwargs，且平台分流寫成 `expected = {} if sys.platform == "win32" else {"start_new_session": True}`（Windows 上該參數不適用，改走 job object 路徑），失敗訊息逐字寫明「逾時回收的 killpg 會打在 AutoClaude 自己」。檔頭 L17 已逐字記載本假鎖的修正沿革 |
| DEF-101-712 | 2026-08-02 | R69 四方複審 R2（SD）追查 `DEF-101-706` 的 −21 LOC 憑證鏈 | **`ADR-XPLAT-002:32`「淨 −21 LOC」原句無射程宣告**，讀者只能讀成「全樹淨 −21」。實況：該值只對 `ADR-XPLAT-003` §3 表列的**四個檔**成立；`autoclaude/` 全樹淨值為 **0**（`check_loc_budget` 動工前後 total 相同、餘裕不變、生產碼凍結未解除）。**無射程的量化宣稱＝可被下游合法誤引的地雷**，`DEF-101-706` 正是踩到它才寫出「餘裕 2 行 → 23 行」 | P3 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:32` 附近 | fixed@R69（第三波）：該處補「🔴 上句的射程必須逐字讀成『那四個檔』，不是全樹」訂正段，並明載全樹淨值 0、護欄層另計。憑證＝`ADR-XPLAT-002` 現行 L32-38 訂正段 ＋ 兩側 `count_loc` 語意雙側量測（指令見 `ADR-XPLAT-002` §4.3.1 R69 段），與 `DEF-101-711` 同鎖看守 |
