# AutoSDD Defect Log — Archive 63

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **41 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-628`、`DEF-101-358`、`DEF-101-680`、`DEF-101-714`、`DEF-101-893`、`DEF-101-913`、`DEF-101-895`、`DEF-101-892`、`DEF-101-891`、`DEF-101-894`、`DEF-101-905`、`DEF-101-934`、`DEF-101-920`、`DEF-101-929`、`DEF-101-915`、`DEF-101-924`、`DEF-101-901`、`DEF-101-910`、`DEF-101-928`、`DEF-101-908`、`DEF-101-909`、`DEF-101-890`、`DEF-101-933`、`DEF-101-922`、`DEF-101-900`、`DEF-101-921`、`DEF-101-930`、`DEF-101-902`、`DEF-101-904`、`DEF-101-931`、`DEF-101-899`、`DEF-101-916`、`DEF-101-896`、`DEF-101-903`、`DEF-101-911`、`DEF-101-914`、`DEF-101-906`、`DEF-101-907`、`DEF-101-932`、`DEF-101-897`、`DEF-101-898`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：R80 開場歸檔：主檔逼近 262144 硬閘（餘 3718 bytes）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-628 | 2026-07-31 | R66 修復 `DEF-101-627` 時依任務指示雙載具（Bash 與 PowerShell）覆跑驗證，副作用發現 | **`tools/tests/test_bash_probe_spec_contract.py::_probe_a_real_usable_bash_for_fixture()` 挑 `_BASH` fixture 時，驗活探測不帶任何 `env=` 覆寫、直接沿用呼叫端當下繼承的環境變數**：PATH 不含 `Git\usr\bin` 時（原生 powershell.exe 常見），候選第一名（不自我注入 PATH 的誠實版）連探測都失敗（`dirname: command not found`），於是落到第二名（會自我注入內部 PATH 的版本）並誤選為 `_BASH`；而兩支專門要驗「PATH 缺 dirname 時應拒絕該候選」的測試，在那種環境裡因此反向失效 | P2（測試鑑別力隨執行環境的 PATH 組成而不確定性翻轉，非 `DEF-101-627` 修復引入） | 根層 `tools/tests/test_bash_probe_spec_contract.py` 的候選驗活判準，需 Architect 設計裁決 | fixed@R71｜🔴 R75 訂正首詞、🔴 **R79 瘦身**（欄內長文逐字搬出證據檔，判定與狀態分類一個字沒改）：R71 修畢；原文另載當時雙載具實測（Bash 工具 12 passed 對上 PowerShell 工具 2 failed 10 passed）與兩條候選修法。原四欄全文逐字保全於 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-628` 節 |
| DEF-101-358 | 2026-07-25 | DEF-101-357 例外回補時同步核實的殘留落差（FrozenPatch 五批次一致觀察），R44 QA 一審獨立複驗確認記載屬實 | **v0.01～v0.29 的 `_sanitize_component()` 仍是較弱版本**：只把斜線與反斜線剝除或替換為底線，未比照 v0.30 做 Windows 保留裝置名（CON／PRN／AUX／NUL／COM 數字／LPT 數字）、控制字元、長度上限等強化。與過去同類凍結版判例（`DEF-101-019`／`020`／`040` 皆判 P3 wontfix）的關鍵差異：這裡的較弱淨化函式**現在真的在擋一個已證實可利用的路徑穿越攻擊面**，故明文記載需重新評估優先度 | P3（暫沿用既有凍結版慣例分級，但本列明文記載需重新評估、非直接沿用不查） | 評估是否把 v0.30 強化版 backport 至 v0.01～v0.29，或維持現狀 | fixed@R45｜🔴 R60 與 R75 兩度訂正首詞、🔴 **R79 瘦身**（欄內長文逐字搬出證據檔，判定與狀態分類一個字沒改）：R45 經使用者明確核准「抽共享層」方案後修畢；QA 一審逐版比對 20 版 `_sanitize_component()` 的 return 陳述式，佐證本列所稱涵蓋範圍無誇大。原四欄全文逐字保全於 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-358` 節 |
| DEF-101-680 | 2026-08-01 | R67 round 2 四方複審（SA-R67-03；ADRDOC 包**未根治**） | **本輪三項頭號架構異動（Phase 3 解封／平台前提中立化／§8 交棒表機制化）的唯一防回流機制 SC-1~SC-4 全 repo 零可執行消費者**——複審員注入違規形態後全套測試仍全綠；EVIDENCE 包於 round 2 收尾再次實查 `grep -rlnE 'SC-[1-7]' --include='*.py' --include='*.sh' --include='*.yml' --include='*.ps1' .`（zsh 下 `--include=` 已加引號，避免 nomatch abort）仍為 rc=1 零輸出。依 Scan-H 判準⑤「可重跑但沒有閘門看 rc ＝ 不可重跑」，它們不是活體守門 | P2 | 承接容器已具名＝`tools/tests/test_adr_xplat001_c1c2_lock.py`（照抄同檔 `TestCriterionIsBoundToAdrProse`） | fixed@R79 ｜🔴 R79 收尾訂正首詞（原狀態欄全文逐字保全於 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-680` 節）：具名承接容器 `tools/tests/test_adr_xplat001_c1c2_lock.py` 已載入 SC-1~SC-10 全部判準並逐條附單點注入，由 `tools/run_root_unittests.py` discover 收進 ⇒ 本列「零可執行消費者」已失實 |
| DEF-101-714 | 2026-08-02 | R69 第三波帳本包對「護欄層／生產碼成長比」雙側量測時 | **R69 護欄層成長報表**（`CrossPlatform_Scan_Dimensions.md` §8 item 7「新增護欄無成本訊號」的**第二筆實證**）：`375f291` → 交付樹，root `tools/` **+1795**、`AutoClaude/tests` **+333**、`AutoClaude/tools` **+19**、`AISDLC_SDD/scripts` **+167**、**生產碼 Δ=0** ⇒ 本輪增長 100% 落在護欄層。🔴 量測面本身髒（多包並行＋收尾複量已漂至 **+3079**），故**刻意不把成長率常數登進 ADR**（否則即 `DEF-101-713` 家族復發） | P3 | `CrossPlatform_Scan_Dimensions.md` §8 item 7＋`ADR-XPLAT-002` §4.3.1（只登量測指令、不登常數） | fixed@R79 ｜🔴 R79 收尾訂正首詞（原狀態欄全文逐字保全於 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-714` 節）：所要的機械訊號已存在＝`TestGuardLayerRatchet`（逐檔行數表，淨增一行即紅），比原文舉例的 warn band 更緊；殘留的重釘方向零約束由 `DEF-101-925` 承接 |
| DEF-101-893 | 2026-08-07 | R79 V-rest 掃描（收斂包修） | 幽靈符號鎖 R78 只擴了 token 形狀、引用面自始至終只有 `.py`：引發整個 R78 C 包的那個常數當時仍活在 10 支 docs 檔共 14 處，其中一處把它當**現行**約束陳述（語意早被推翻），照著讀的人會把新鎖放到別的樹去。另查定義面不含 `.claude/hooks/` ⇒ 整個 hook 層的符號在索引裡等於不存在（偽陽性方向） | P2 | 本輪修復：引用面加 docs 活文件白名單（凍結史料刻意排除）、定義面補三棵樹、豁免表加 shrink-only 天花板；並訂正該處失實語意 | fixed@R79：注入紅綠見 `R79_HANDOFF.md` 的 §1 |
| DEF-101-913 | 2026-08-07 | R79 CTX 包（context 水位與排程機制） | archive_16 的 DEF-101-089 原結論**只在 `claude -p` subprocess 這條路被推翻**（rc=0／4.0s、3.6s），連帶「排程重啟路徑無法從 session 內部驗證」亦不再成立；11 支 skip 實走的 wexpect pty 路不在該反證射程內 | P3 | planner 新增三支 schtasks 旗標並真跑取證；收斂輪補測 pty 路、把結論收窄到 subprocess 並同步 CLAUDE.md 與兩支測試檔 reason | fixed@R79：喚醒四項實測全對＋probe 已移除；pty 路 `start()` 三次皆未回返（180/180/45s）故 skip 續留，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-913` 節 |
| DEF-101-895 | 2026-08-07 | R79 收斂包停工窗口實跑全套閘門時顯形 | `.ps1` 掃描面去重（CI 第 2 道改呼叫 Python SSOT）落地後，三道既有一致性鎖同時轉紅：新守門工具沒接進 pre-push root-infra leg、workflow 檔頭守門清單漏列、SSOT 自身成為入口點卻無 UTF-8 stdio 保護。三者都是「改了 A 而 B 沒跟上」而非新缺陷，但**只有在單人窗口跑全套時才看得到**（並行期間量到的紅無法歸因） | P2 | 本輪修復：補 pre-push 第 9 支守門（含 argv 檢查的 fixture stub）、補檔頭條目、`__main__` 內補 stdio 保護 | fixed@R79：rc 實測見 `R79_HANDOFF.md` 的 §1 |
| DEF-101-892 | 2026-08-07 | R79 V-rest 掃描（收斂包修） | 逐檔凍結表在**乾淨 HEAD** 上已有 3 筆與磁碟不符（−11／+7／+4，淨額 0），棘輪與 `--print-guard-lines` 雙雙印綠——判準本體的「誠實劃界」段逐字寫過這個盲區，而它在鎖落地的同一輪就已被踩進去且入庫。後果：逐檔數字不能再當基線，且總量守恆下任一護欄檔可無限膨脹只要別支等量縮水 | P2 | 本輪修復：新增 `[逐檔漂移]` 款（共同鍵值不符筆數 > 容忍值即紅，容忍值釘 0），並讓重釘入口在淨額為 0 時仍印逐檔 DIFF | fixed@R79：注入紅綠見 `R79_HANDOFF.md` 的 §1 |
| DEF-101-891 | 2026-08-07 | R79 V-rest 掃描（收斂包修） | 護欄層重釘稽核痕跡 `_GUARD_LINES_REPIN_LOG` 檔頭自稱 append-only，**零機械強制**：實測把 R77＋R78 兩列壓成一列、起點總量改成任意值（54188→90000），五款判準全部沉默、rc=0。該表存在的唯一理由是讓淨額結構上不可能缺席，而壓平歷史比不補一列更難看見（表上永遠有一列） | P2 | 本輪修復：新增 `[歷史變短]`（列數下限）與 `[歷史被改寫]`（除最後一列外的 sha256 前綴指紋）兩款；追加一列時指紋不變、改寫既有列即紅 | fixed@R79：注入紅綠見 `R79_HANDOFF.md` 的 §1 |
| DEF-101-894 | 2026-08-07 | R79 V-879-hook 掃描（收斂包修） | 根 CLAUDE.md 到 R78 為止**從未提及** `lint_powershell_command.py`（鐵律二與「讀 rc 不接管線」的唯一機械物，R77 起就在根層攔），兩節因此讀起來像純自律 ⇒ 後續輪次很可能為同一件事再蓋一支攔截器。守文件的那道鎖只檢查「文件裡有被點名的」hook，對「已註冊但沒被提到」結構上失明 | P2 | 本輪修復：兩節補上機械物欄與行內豁免出口，並補第三向判準（分母＝settings.json 現查的註冊集合） | fixed@R79：注入紅綠見 `R79_HANDOFF.md` 的 §1 |
| DEF-101-905 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | `AutoClaude/tests/perf/test_pgvector_recall_perf.py` 自 2026-06-12 起零通道零執行且壞掉：全 repo 無處設 `PG_REAL_ENABLED`，首次真跑即 TypeError（建構子與 search 兩處簽章皆錯），修好後再揭一層單位錯誤（每樣本 100 次查詢對上每次 50ms 的 SLA，實測 p95=4403.8ms） | P2 | 改 `from_dsn` 與具名參數、每樣本 1 次查詢 runs=100、加語料前置檢查；通道補進 pg-e2e-nightly | fixed@R79：修後本機 1 passed in 4.87s，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-905` 節 |
| DEF-101-934 | 2026-08-07 | R79 複審後修復包（單人窗口跑 ci-gate 回填基線時顯形） | 同形態**第四次**：`_WORKTREE_EOL_POLICY` 本輪改成從 `.gitattributes` 現查（除掉第二個家），該檔因此成為判準輸入，但兩支 compat-CI 的 `paths:` 沒補 ⇒ 只改它時讀它的鎖不會被觸發。唯一偵測者住 SDD scripts 軌、不在根層 unittest 閘門內 | P2 | 兩支 workflow 的 push 與 PR 兩段各補 `.gitattributes` 一條 | fixed@R79：修前 `test_ci_paths_cover_root_consumers` 逐字點名 2 failed（macos／windows 各一），修後 47 passed rc=0 |
| DEF-101-920 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | 一支鎖因別包的合法動作而靜默失去鑑別力：`test_ratchet_flags_both_directions_of_drift` 用第一格基線減一施測，而 `_POSIX_TAG_RATCHET` 的 `tools/tests` 被正確下修為 0 之後，扣完等於沒扣、該半題恆綠 | P2 | 改為挑基線大於 0 的樹施測，並在全格為 0 時 fail-loud；判準不得依賴「哪一格排第一」這種偶然事實 | fixed@R79：當回合實測該半題確實恆綠、修後轉紅，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-920` 節 |
| DEF-101-929 | 2026-08-07 | R79 CONV 收斂包（單人窗口實跑全套時顯形） | 目錄項原語普查的掃描面是檔案系統 rglob，排除清單只列 `.venv`／`venv` 兩個名字 ⇒ 該普查的雙向精確比對基準是「這台機器上剛好有哪幾個 venv」的函數；實測建一個叫 `cleanvenv` 的環境使實測值由 41 跳到 58、閘門轉紅 | P2 | 改以 PEP 405 的 `pyvenv.cfg` 標記偵測 venv 根，與命名無關 | fixed@R79：收尾當回合實查該判準已改讀 `pyvenv.cfg`，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-929` 節 |
| DEF-101-915 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | `.ps1` 行尾寫入端零強制，寫入者已溯源為 Claude Code 的 Write 工具（新建與覆寫既有 CRLF 檔都吐 LF），而同一支 PostToolUse hook 只補 BOM 不碰行尾 | P2 | `check_ps1_encoding.py` 擴為位元組正規化器（BOM＋CRLF、非合法 UTF-8 完全不動、冪等、射程不含 `.sh`） | fixed@R79：端到端修前 crlf=0 lonely_lf=3、修後 crlf=3 lonely_lf=0；LF→CRLF 對 `.ps1` blob-neutral 已親驗，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-915` 節 |
| DEF-101-924 | 2026-08-07 | R79 ARCH 包（架構減法） | `check_pytest_baseline_sites` 是 8 檔人工白名單、無前瞻發現鎖：擋得住刪清單一行，擋不住在新文件裡多開一個家；實測掃描面外 1430 支 tracked 檔、4495 行命中同一判準 | P3 | 發現面改全庫 tracked 扣具名日期性文物樹，未納管檔數 shrink-only 雙向棘輪＝114，並加發現面下限 900 與活文件錨 | fixed@R79：新增 tracked 文件寫下基線數字 rc 由 0 轉 1、移除後回 0，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-924` 節 |
| DEF-101-901 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | R77 的失誤歸因百分比無任何可重跑產物，使根 CLAUDE.md 的 Windows 失誤根因成為不可稽核常數（同 R71 n=8 模型被沿用五輪的形態） | P2 | 新建 `tools/probe/misstep_attribution.py`：檔內 SSOT、桶具名附判準、平手一律 OTHER、逐筆附歸屬理由、語料塌了 rc=1 | fixed@R79：四道注入全紅；重跑結果與「百分比不得引為常數」的判準性質由腳本自印，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-901` 節 |
| DEF-101-910 | 2026-08-07 | R79 CTX 包（context 水位與排程機制） | context guard 的立案 docstring 逐字寫「實查三處」而獨漏 harness 自己：`claude --help` 有 `--autocompact`、二進位內開關預設 true，花一輪做偵測器卻沒查內建解 | P2 | docstring 補第四處並收斂角色；planner 新增 `--check-autocompact`（關閉時 rc=1）；另補 PreToolUse 阻斷模式 | fixed@R79：本機實測 autocompact 開啟、window=auto，阻斷注入見 `PreToolUseBlockTest`，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-910` 節 |
| DEF-101-928 | 2026-08-07 | R79 CONV 收斂包（單人窗口實跑全套時顯形） | 兩支 compat-CI 的 `paths:` 未涵蓋新增判準所消費的根層檔（`.editorconfig` 與成熟度 SSOT）——只改被讀的檔時，讀它們的鎖不會被觸發；同形態自 R78 起第三次復發，唯一偵測者不在根層 unittest 閘門內 | P2 | 兩支 workflow 的 push 與 PR 兩段各補兩條 path | fixed@R79：收尾當回合實查兩支 workflow 皆已含 `.editorconfig` 與成熟度 SSOT 兩條，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-928` 節 |
| DEF-101-908 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | 甲類 skip 靠人記得設環境變數：本機一件缺件都沒有（PG 容器長駐 healthy、相依全裝、DB 已在 head 0018），缺的只有三個環境變數 | P1 | `local_ci_gate` 新增 `pg_autodetect()`，四條剎車＝使用者已顯式設過／CI 有值／本行程是 pytest／DB 未 migrate | fixed@R79：4069 passed 135 skipped → 4160 passed 44 skipped，91 支轉 passed、耗時零增加、零新增 failed，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-908` 節 |
| DEF-101-909 | 2026-08-07 | R79 CTX 包（context 水位與排程機制） | context 水位守衛在 1M 模型上結構性保證在真 90% 靜默：window 判定僅三階，而本機 settings 的 model 欄是 opus[1m]，真 15% 與 18% 各誤喊一次燒掉兩個閂鎖，之後到 99.9% 全靜默 | P1 | window 改五階＋model 標記交叉否決；閂鎖鍵改為含 window | fixed@R79：真機 `--check` 由 96.4% 修正為 19.3%，注入紅綠見 `WindowSourceOrderTest` 與 `LatchRearmTest`，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-909` 節 |
| DEF-101-890 | 2026-08-07 | R79 Scan-G（清債包動工前實查） | 帳本「單列 ≤700 bytes」連三份交棒書列為禁令，**全 repo 零機械物**：實測 120 列有 110 列違反、平均 2089 bytes，主檔距硬閘僅 5224 bytes。唯一在看體積的觀測者觸發當下是早退，會連帶關掉同工具其後全部判準 | P1 | 本輪修復（逐列位元組判準＋具名存量豁免＋雙棘輪；體積判準改累積不早退） | fixed@R79：注入紅綠與瘦身帳見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-890` 節 |
| DEF-101-933 | 2026-08-07 | R79 複審後修復包（前一包注入時量到、登記未修） | 守交棒書的 `_handoff_claim_blocks()` 在任何 `##` 以上標題都重設 `in_section`（含 `###`）⇒ 受管大節底下、小標題不含觸發字的整區條目靜默退出射程；前一包的處置是把觸發字寫進每一個小標題＝繞過 | P2 | 改成巢狀標題繼承父節射程，只有同級或更高級標題才重設，並補三向掃描器自檢 | fixed@R79：注入見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-933` 節 |
| DEF-101-922 | 2026-08-07 | R79 ARCH 包（架構減法） | `check_wrapper_thinness` 的並聯第三訊號 `_FORBIDDEN` 只有 14 鍵而 `_PINNED_SHA256` 有 16 鍵，兩支 LATEST 薄殼靜默回空集合＝零訊號，只剩 hash 一道，而更新 pin 正是它的合法維護動作 | P3 | 補登記缺的那一族，並加鍵集合相等鎖（缺鍵與孤兒鍵雙向紅）＋非空自錨＋真跑鑑別力 | fixed@R79：注入 4 形態全紅，含真檔加 jq 後工具 rc=1，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-922` 節 |
| DEF-101-900 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | 三筆觀測者失真合記：`--latest` 窗被同期 agent 逐字稿汙染（同指令一小時三組數字）、鐵律四佐證條件近乎恆真、`inline-loop` 三分之二命中其實是慣用管線 | P2 | 必印量測窗清單＋`--exclude`；`EVIDENCE_RE` 去掉裝飾字元且窗 12→3 附敏感度表；迴圈欄拆兩欄並標明無攔截端 | fixed@R79：四道注入各自轉紅，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-900` 節 |
| DEF-101-921 | 2026-08-07 | R79 ARCH 包（架構減法） | 同一份 `.ps1` 掃描面有 3 份獨立實作，為了偵測三份不同步養了 866 行對抗式正則錨，而該錨自承 3 種已實測抓不到的逃逸，軍備競賽已翻車兩次 | P2 | 兩個非 Python 站點改呼叫 `tools/_script_scan_surface.py --list` SSOT，複本消失即結構上不可能不同步；刪 866 行 | fixed@R79：注入 4 形態全紅、還原全綠，smoke rc=0、parity rc=0，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-921` 節 |
| DEF-101-930 | 2026-08-07 | R79 四方複審（ARCH nonblocking） | schtasks 腳本產生器把 plan_path／task_name／python 路徑直接內插進 PowerShell 單引號字串而未跳脫：含撇號的**合法** Windows 路徑讓整段註冊腳本語法錯，惡意 `--task-name` 可讓 payload 逃出字串成為獨立指令 | P3 | 加 `_ps_single_quote`，五個內插點各套一次 | fixed@R79：回歸鎖併進既有 `EnduranceWiringTest`；注入與真 tokenizer 對照見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-930` 節 |
| DEF-101-902 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | pg-contract 硬閘吞掉 pytest 的 rc：GHA 未宣告 shell 走 `bash -e`（無 pipefail）、`tee` 讓管線 rc 恆 0，census 只數 passed 與 skipped 不看 failed；重放 tail「3 failed, 1238 passed」判定零問題且 rc=0 | P1 | run 首行 `set -o pipefail`、真 rc 以 OR list 顯式接出、census 由 2 道增為 4 道 | fixed@R79：同一份 tail 修前 rc=0、修後 rc=1，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-902` 節 |
| DEF-101-904 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | 標籤詞彙鎖的輸入面只吃字面 reason，唯一已知違規 `[TOOL-MISSING]` 住在函式體的 f-string 裡故隱形三輪 | P1 | `windows_skip_tags` 新增 `nonliteral_skip_reason_prefixes`（取 JoinedStr 開頭常數片段），接進已消費 rc 的靜態閘門 | fixed@R79：注入 `[BOGUS-TAG]` 修前 0 筆、修後 1 筆；加寬後當場多看見 3 個未登記標籤，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-904` 節 |
| DEF-101-931 | 2026-08-07 | R79 四方複審（SA blocking，QA 同源） | 交棒書 §4 指示 R80 補寫的 34 列帳本列在寫下該指示時已全部在磁碟上：照做會超出主檔 bytes 硬閘並產生 34 筆重複 ID。成因＝交棒書由收斂包時點寫成，其後 ledger 包才落地而無人回填 | P1 | §4 改寫成「已在磁碟上、不要再補寫」，§0 加成書時點聲明 | fixed@R79：連同 §2-Q5 的未結列數一併訂正，詳見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-931` 節 |
| DEF-101-899 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | 量測器自寫扁平正則，兩個相反方向同時失準：視窗只跨一個換行使多行指令整類隱形（低報），不切語句不比位置使根 CLAUDE.md 教的正解被算成違規（高報、符號相反） | P1 | 量測端改借攔截端的 `_rc_after_pipe`，扁平正則整條消失 | fixed@R79：probe 162→150、兩端分歧 15→0，注入 failures=8，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-899` 節 |
| DEF-101-916 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | 工作樹 `.ps1` 行尾漂移在本機結構上不可見（`eol=crlf` 讓 index 恆為 LF、status 兩側套同一份規則），hook 也繞得過 | P2 | 新增事後閘讀 `git ls-files --eol` 的 w 欄，`.ps1` 必 CRLF、`.sh` 必 LF 雙向對稱，掃描面下限 240 | fixed@R79：把一支 tracked `.ps1` 轉 LF 即轉紅、原樣還原後 sha256 相同且轉綠，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-916` 節 |
| DEF-101-896 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | 攔截器 `_RC_RESET_RE` 把「提到」當成「執行」：`2>&1` 的 `&` 與任意位置的 `.exe` 都被判成 rc 已重設，實測三種語句一個都沒重設，端到端重現真 rc=7 被讀成 0 | P1 | 左邊界加 `>`、副檔名收到語句開頭、抽出 `_statement_resets_rc` | fixed@R79：注入 7 failures，913 條真實指令對照 149→150，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-896` 節 |
| DEF-101-903 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | 全 repo 對 runtime skipped 數零管轄：唯一天花板只覆蓋一個 job、`local_ci_gate` 對 skipped 零字樣、根層 runner 只印不判 | P1 | 新建 `tools/lib/skip_group_policy.py`（7 標籤 6 群、逐群天花板加 shrink-only 守護），消費者 `check_skip_census` 併進 rc | fixed@R79：六向注入全部轉紅，誠實登記新群不轉紅，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-903` 節 |
| DEF-101-911 | 2026-08-07 | R79 CTX 包（context 水位與排程機制） | M5 可用「加十幾題現行判準已經攔得到的語料」刷過門檻而三支現行鎖全綠：門檻是比率、棘輪釘的卻是絕對攔截數，分母不受任何約束 | P2 | 門檻由比率改成「未攔到題數」，並鎖住「已知攔不到的題不得被刪掉」 | fixed@R79：注入紅綠見 `tools/tests/test_maturity_criteria_r79.py`，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-911` 節 |
| DEF-101-914 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | 單平台專屬 API 掃描器的守衛特赦是檔案級＋純文字比對：同一組 5 筆注入只抓到 1 筆，守衛字樣寫在字串常數即可特赦全檔 | P1 | 改為站點級 AST 判定，守衛述詞由文字比對改結構化符號比對；存量 4 筆登記於 `_FOREIGN_API_SCOPE_DEBT` | fixed@R79：注入實測 A 1→5、C 0→1、D 0→1，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-914` 節 |
| DEF-101-906 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | 「alembic head 不等於整條鏈跑過」的唯一偵測器只跑在它結構上不可能失敗的環境（雲端每次全新 container） | P2 | oracle 由一個 function 擴為四支 revision 六個產物，通道改由本機 PG 自動偵測對長壽 DB 跑 | fixed@R79：把 0012 的 `try_acquire_import_lock` 改名即 1 failed、還原即 1 passed，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-906` 節 |
| DEF-101-907 | 2026-08-07 | R79 SKIP 包（pytest skipped 徹底解決） | runner 把 6 筆結構性 POSIX-only 印成「未標籤」，與真環境性 skip 同桶，害分流把可救回的工作量高估一倍 | P3 | 類別層 skipIf reason 補 `[POSIX-NATIVE-ONLY]`，`_POSIX_TAG_RATCHET` 與其 CEILING 一起由 1 下修為 0 | fixed@R79：拿掉標籤即 1 筆紅；基線改回 1 而天花板留 0 亦 1 筆紅，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-907` 節 |
| DEF-101-932 | 2026-08-07 | R79 四方複審（ARCH blocking） | `.ps1` 掃描面三合一的立論是「掃描面靜默縮小必須 rc=1」，但新鎖必要字串漏 `--with-latest`，且 CLI 對「有 `--check-floors` 卻無 `--with-latest`」回 rc=0 ⇒ LATEST 版樹整棵連同下限靜默退出掃描面而毫無訊號 | P2 | 測試側補該旗標；CLI 側 `_main` 對該組合 rc=2 拒跑 | fixed@R79：實測見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-932` 節 |
| DEF-101-897 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | `naked-cd` 尾巴硬性要求一個參數，不帶參數的 `cd`／`sl`／`chdir`／`Set-Location` 整條放行，而它切到 `$HOME` 破壞性更大 | P2 | 尾巴改可選，hook 與 probe 兩份逐字同步（字面相等鎖覆蓋） | fixed@R79：兩側注入各紅（failures=3 與 2），MUST_BLOCK 新增 4 列，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-897` 節 |
| DEF-101-898 | 2026-08-07 | R79 OBS 包（攔截器 hook 與量測器 probe） | 行內豁免比對原文，任何在字串裡引述 `ps-lint-ok` 標記的指令會一次關掉三條檢查且無痕跡 | P3 | `mask_regions` 加 `keep_comments`，只認住在真註解裡的標記（舊版防撇號誤擋的理由已保住） | fixed@R79：注入還原成比對原文 rc=1 failures=1，證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-898` 節 |
