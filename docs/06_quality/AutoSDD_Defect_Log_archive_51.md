# AutoSDD Defect Log — Archive 51

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **7 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-766`、`DEF-101-762`、`DEF-101-763`、`DEF-101-760`、`DEF-101-759`、`DEF-101-761`、`DEF-101-767`
>
> **本次操作備註**：R72 開場歸檔：R71 已收官（1e5214b）之 fixed 列 759~767，主檔距硬線僅 179 bytes
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-766 | 2026-08-03 | R71 四方複審 QA blocking C-1：`DEF-101-759` 的修復落地後，複審追問「這段在沒有 PATHEXT 的平台上會怎樣」 | **`tools/lib/WindowsAppsGuard.ps1::Resolve-NativeExecutable`（`DEF-101-759` 為擋 pyenv-win 無副檔名 shim 而生）無條件照 `$env:PATHEXT` 過濾候選，而 PATHEXT 是 Windows-only 概念**：PS Core 跑在 macOS/Linux 時該變數不存在，且 POSIX 執行檔本來就不帶副檔名 ⇒ 每個候選都被淘汰 ⇒ `Get-PythonGeMin` 恆回 `$null` ⇒ `macos-compat-ci` 與 `root-infra-ci`(ubuntu) 必紅。與 `DEF-101-759` 是同一個病、只是換平台發作。**為何既有防線沒抓到**：`tools/tests/test_dev_start.py::TestGetPythonGeMinPowerShell` 是行為鎖，但本機（Windows 11）只有 PS 5.1、`$PSVersionTable.PSVersion.Major` 恆為 5 ⇒ 非 Windows 那條分支在本機**結構上跑不到**；而 `$PSVersionTable` 又是 read-only（本包實測 `$PSVersionTable = …`／`$local:PSVersionTable = …`／`New-Variable -Force` 三種寫法皆回 `Cannot overwrite variable PSVersionTable because it is read-only or constant.`，連子作用域都蓋不掉）⇒ 唯一觀測載具是真 pwsh 7 on POSIX，而那正是本機沒有的東西 | P0 | 生產修法主控本輪已落地；回歸鎖由本包補（不得依賴真 macOS） | fixed@R71：生產修法＝`$isWindowsHost = ($PSVersionTable.PSVersion.Major -lt 6) -or $IsWindows`，非 Windows 直接退回「第一個 `CommandType='Application'`」＝本函式導入前的原行為（判定式順序不可調換：`$IsWindows` 在 PS 5.1 未定義，必須先由版本比較短路擋掉）。**回歸鎖（本包補，兩道，皆在 `tools/tests/test_dev_start.py`；`DEF-101-561`③ 禁新增鎖檔故併入既有檔）**：① `::TestResolveNativeExecutableNonWindowsBranch`＝行為鎖，把生產函式原始碼原封搬進 harness、只把蓋不掉的 `$PSVersionTable.PSVersion.Major` 換成可設定的 `$FakePsMajor`（替換恰 1 處，數目不對即 fail-loud），同一份 fixture 只換平台兩輸入做四組單變因實測——PS5.1／無副檔名候選 ⇒ `$null`、PS5.1／`.cmd` 候選 ⇒ 命中（正控，證明 Windows 分支與 PATHEXT 都是活的）、Core＋非 Windows／無副檔名 ⇒ 命中（本體）、Core＋Windows／無副檔名 ⇒ `$null`（防過度修復，否則裝了 pwsh 7 的 Windows 機器會重新踩回 `DEF-101-759` 的 ShellExecute 對話框）；② `::TestResolveNativeExecutableShortCircuitOrder`＝順序鎖，任何平台皆跑。**兩道鎖射程實測不重疊，不是例行的「行為＋字面」搭配**：本包把短路整段搬到 PATHEXT 過濾之後，①仍回 `RESULT_NULL=False`＝全綠，只有②看得見；刪掉整段短路則①當場紅（`RESULT_NULL=True`）。**注入退化雙向取證**（退化只落在 tempdir 複本，生產檔全程唯讀、事後逐字比對確認未變）：D1 短路整段刪除 ⇒ 4 紅、D2 搬到過濾之後 ⇒ 2 紅、D3 `-or` 兩側對調 ⇒ 2 紅；還原 ⇒ 7/7 綠。🔴 **被否決的第三種鎖法，誠實記錄以免下一個人再走一遍**：「在 PS 5.1 下清空 `$env:PATHEXT` 再跑生產函式、斷言它不回 `$null`」**零鑑別力**——本包實測（原生 5.1、子行程內 `$env:PATHEXT = ''`）修好之後對無副檔名候選回 `FAKEPY_NULL=True`、對真 `.exe` 候選 `git` 也回 `GIT_NULL=True`，因為 Major=5 一律短路進 Windows 分支，清 PATHEXT 只是讓該分支自己把候選濾光，永遠碰不到本次修的那條路，修好修壞都綠 🔴 **殘留紅燈一筆，本包無權修（`tools/lib/WindowsAppsGuard.ps1` 為射程外唯讀）**：生產修法用到的 `$IsWindows` 被 `tools/tests/test_ps51_compat.py::test_active_ps1_trees_have_no_ps7_only_usage` 判為 PS 6.0+ 專屬自動變數 ⇒ 該鎖現為紅。**機械歸因（本包當回合實跑）**：拿該檔的純函式 `scan_source()` 分別餵 `git show HEAD:tools/lib/WindowsAppsGuard.ps1` 與工作樹版——前者 `offenders=[]`、後者恰 1 筆 `WindowsAppsGuard.ps1:162` ⇒ 紅燈由**未提交的生產修法**帶入，非本包造成。**正解是掛豁免、不是改碼**：本處正屬該掃描器自陳「確有必要」那一類（左側版本比較先短路，5.1 上根本求值不到 `$IsWindows`），應於 `tools/lib/WindowsAppsGuard.ps1:162` **行尾**加 `# ps7-ok: <WHY>`（WHY 須載明短路順序，而該順序此刻已由上述②順序鎖機械看守——豁免的理由不再只是一句宣稱）。本包已**前瞻驗證**：把該豁免掛到複本上後，①②兩道鎖仍 7/7 綠（`ps_code_only()` 之所以連行尾註解一併剝，就是為了不讓「別人補了正確的豁免」變成本鎖翻紅的理由）。🔴 **本包當回合複核：該殘留紅燈已消**——`tools/lib/WindowsAppsGuard.ps1:165` 行尾已掛 `# ps7-ok:`（WHY 逐字載明短路順序並指名順序鎖），實跑 `python -m unittest tools.tests.test_ps51_compat -v` → `Ran 10 tests in 0.632s` `OK`、rc=0。承接輪次：R71 主控 |
| DEF-101-762 | 2026-08-03 | R71 由 `DEF-101-761` 補上的取證層換來的單變因實驗鏈鎖定（排程環境 CP950、人手環境 CP65001） | **PowerShell 以 `[Console]::OutputEncoding` 解碼原生指令 stdout，而 git 與 `tools/_stdio_utf8.py` 一律吐 UTF-8 ⇒ CP950 下含非 ASCII 的路徑被解成 mojibake**，下游 `assert-hooks-present` 找不到 hook 檔 → exit 1 ⇒ **中文路徑的 repo 根本裝不了 hooks**；引數方向不受影響（PS→argv 走 UTF-16 `CreateProcessW`，實測 round-trip 相同），故只需修解碼這一側。**本包當回合雙向實測**（`cmd /c chcp 950` 起原生 PS 5.1，於 `...\Temp\probe762_b68e3586\煙霧測試` 假 repo，逐字元印 codepoint）：未釘編碼直呼 python CLI → `U+003F U+EA57 U+EBEC U+769C U+7948 U+5CAB`（6 個字元，`Test-Path=False`）；走修好的 `Get-DispatcherHooksDir`（內部走 `Invoke-CommonPy`）→ `U+7159 U+9727 U+6E2C U+8A66`（煙霧測試，`Test-Path=True`）。另以純解碼探針獨立複現同一機制：同一段 UTF-8 輸出在 CP950 下 4 字元被解成 6 個 mojibake codepoint，釘 UTF-8 後逐字元 round-trip 相同。**為何既有防線沒抓到**：**所有人工驗證都在 CP65001 跑**（Claude Code 的 PowerShell 工具、Windows Terminal 皆 65001），而缺陷只在 CP950 顯形＝**驗證載具系統性繞開了缺陷條件**；雲端 windows runner 同樣不是繁中 CP950；`windows-compat-ci.yml` 雖有 install_post_commit 的非 ASCII 編碼斷言，但那個 step 也跑在 UTF-8 主控台下 ⇒ 三層驗證（人工／CI／smoke 手動跑）**共用同一個被繞開的前提**，唯一會踩到的是每日 01:00 的排程，而它剛好沒有 log（`DEF-101-761`） | P0 | `tools/lib/GitHooksInstallCommon.ps1::Invoke-CommonPy`（一處修好兩個安裝器）＋ `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1:16` 起 ＋ `tools/windows_smoke_local.ps1::Read-NativeUtf8`（載具側）＋ **第 4 站點** `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/formal/run_tlc.ps1:47`（同「`git rev-parse` 輸出反推路徑」形態，同一解法；R71 補列——此前本欄 grep `run_tlc` 命中 0，而該檔在地註解又只寫「見帳本」，指標雙向懸空）＋ 回歸鎖 `tools/tests/test_dev_start.py::TestGetDispatcherHooksDirUnderCp950`（cp950 真跑行為鎖＋負控，[WINDOWS-NATIVE-ONLY]）與同檔 `::TestNativeStdoutDecodingRoutingLock`（路由／釘選順序靜態鎖；其 `_LATEST_REV_PARSE_SITES` 表 R71 由「只鎖 install_post_commit.ps1」參數化為**兩個** LATEST 站點，run_tlc.ps1 自此由 hash 釘選＋順序鎖雙層看守，`DEF-101-757`「已知鎖射程缺口不得只以劃界結案」入規後的第一次落實）。🔴 **回歸鎖檔名 R71 已變更**：原 `tools/tests/test_native_stdout_utf8_decoding.py` 已併入 `tools/tests/test_dev_start.py` 並刪除（`DEF-101-561`③ 禁止新增鎖檔、只准併入，該檔曾使 `tools/tests` 鎖檔數 53→54 讓三支機械棘輪翻紅）——查回歸請走上列兩個類別，勿再找該檔 | fixed@R71（**端到端 smoke 綠燈受 clone 隔離盲區阻擋，誠實劃界**）：每處在呼叫期間把 `[Console]::OutputEncoding` 釘成 `UTF8Encoding($false)`、`finally` 立刻還原；沒有主控台可設時 `catch` 後維持既有行為（下游斷言仍 fail-loud，不退化成假成功）。🔴 **刻意不整支釘 UTF-8**：受測腳本是以 `& $installer` **同行程**呼叫的、會直接繼承主控台編碼，整支釘等於讓 smoke 的 [5][6] 在任何載具下都拿到 UTF-8 主控台，那個「只在 CP950 顯形」的生產缺陷就再也重現不出來——那是**拆掉 tripwire，不是修好它**。🔴 **本包複核當場擋下一個可能的假鎖**：修復落盤後（`tools/lib/GitHooksInstallCommon.ps1` mtime `2026-08-03 01:43:54`）於 CP950 再跑的那一輪 smoke（transcript `開始時間: 20260803015012`）**仍是 `PASS=11 FAIL=2`、[6] 中文路徑仍紅**。經查非修復失效：smoke [6] 的受測 installer 取自 `Join-Path $TargetRepo $InstallerRel`，而 `$TargetRepo` 是 `git clone` 自本 repo **HEAD** 的 fake repo（該限制逐字寫在 `tools/windows_smoke_local.ps1:60-61`）⇒ **未提交的修復結構上不在 smoke 視野內**，與 `DEF-101-751`／`752` 同一個未追蹤／未提交盲區家族。故本列的 fixed 憑證是上述 `Invoke-CommonPy` 層雙向實測，**不是** smoke 綠燈；smoke [6] 在 CP950 下轉綠須待本輪 commit 後重跑，**在那之前不得引 smoke 為本列已修的證據** |
| DEF-101-763 | 2026-08-03 | R71 本包做「程式碼引用了帳本沒有的 DEF 編號」全樹反查時揭露（本缺陷此前無人立帳，且與 `DEF-101-759` 撞號） | **剛落地、用來讓平台覆蓋不再靠人記憶的那道機械守，自己一天都沒真的量到過東西**：`tools/lib/baseline_origin.py::nightly_evidence()` 隨 `fbc9bb5`（`DEF-101-756`／`757`／`758`）落地，首版彙總行解析是 `read_text().splitlines()[:3]` 找 `PASS=`——那是 **mac 心跳**的形狀。win32 讀的卻是 `AutoClaude/tools/run_local_nightly.ps1` 的**全量 log 複本**（實測 494 行，含完整 pytest／mutmut 輸出時可達數 MB），彙總行在**檔尾**（實測第 491 行）、字面是 `END nightly summary: ...`。「讀前 3 行」與「找 `PASS=`」兩個致命點**各自獨立、任一個都足以讓它恆不命中** ⇒ `--check-snapshot` 的 Windows 欄每天印「（心跳無彙總行）」。**最難發現的壞法＝fallback 文案把自己的失效偽裝成資料現況**：沒有紅燈、沒有 traceback，只有一句看似合理的話。**為何既有防線沒抓到**：①這個逐平台差異**早就寫在本 repo 裡**——`tools/dev_start.py::_windows_heartbeat_fail_note` 檔頭逐字記著「`nightly_latest.log` 是全量 log（可能數 MB）…故不能沿用讀前 3 行的作法」，新程式碼沒有重用這份既有知識；②探針落地當輪的驗證全部餵 mac 形狀的樣本，win32 分支從未拿一份真實 Windows nightly log 餵過；③`nightly_evidence()` 是純資訊輸出、**不進 rc**，故不論對錯都不會讓任何閘門說話——這正是 `DEF-101-756` 那筆「rc=0 的工具輸出把主控導向與事實相反的結論」的同型復發 | P1 | `tools/lib/baseline_origin.py`（`NIGHTLY_SUMMARY_SPECS` 逐平台解析契約 ＋ 檔尾位元組窗格）＋ 回歸鎖 `tools/tests/test_doc_loc_baseline_freshness_r60.py::TestR71NightlyProbeActuallyParsesEachPlatformsOwnFormat`／`TestR71StaleFingerprintMustNotSwallowTheCoverageDetail` | fixed@R71（**程式碼側編號尚未落地，見下**）：改為逐平台 `_SummarySpec`——win32＝檔尾 16384 bytes 窗格找 `END nightly summary:`（**絕不整檔載入記憶體**）、darwin＝心跳前 3 行找 `===== nightly 彙總：PASS=n FAIL=n =====`；並把 `strict`／`loose` 兩層分開：命中 loose 不命中 strict＝格式漂移（處置是改探針），兩層皆不命中＝那一輪根本沒跑到收尾（處置是去看那台機器）——**兩者處置相反，故不可共用一句話**。回歸鎖含反「一個 pattern 硬套兩平台」的鑑別鎖（斷言兩邊錨點字面互不出現，防未來有人為了少幾行又把兩個 spec 併回一個——而併回去的當下不會有任何東西轉紅）。🔴 **本列的 7 處程式碼站點目前逐字寫的是 `DEF-101-759`**：`tools/lib/baseline_origin.py:124`／`:318`、`tools/tests/test_doc_loc_baseline_freshness_r60.py:76`／`:2579`／`:2617`／`:2664`／`:2700`。與主控指派給 pyenv-win 案的 `DEF-101-759` 撞號；本帳本依主控任務書把 759 定為 pyenv-win 案、本案另立 763，那 7 處待改號——**本包射程限帳本，不動程式碼**，改號動作與判準登記於 `DEF-101-764` |
| DEF-101-760 | 2026-08-03 | R71 Windows 11 真機開箱路徑實跑；與 `DEF-101-755`（唯一有行為鑑別力的鎖被 skip 在門外）互為因果 | **Windows PowerShell 5.1 把參數交給原生執行檔時會自己重組一次命令列字串，過程中吃掉字串內嵌的一個雙引號 ⇒ 版本探測碼的 `else ""` 變成未閉合字串 ⇒ `Get-PythonGeMin` 恆回 `$null`**。**本包當回合真機重現（雙向）**：原生 Windows PowerShell 5.1.26100.8875、CP=950，直呼真 3.11 exe——`... else ""` → rc=1 ＋ `SyntaxError: unterminated string literal (detected at line 1)`（argv 尾巴只剩一個 `"`）；`... else str()` → rc=0 ＋ 印出 `C:\Users\wuwei\.pyenv\pyenv-win\versions\3.11.9\python3.11.exe`。淨效果＝迴圈把**每一個**候選都判不合格，`tools/dev_start.ps1` 的「無 `.venv` 開箱」路徑在全新 Windows 機器上整條是死的。**為何既有防線沒抓到**：①唯一真的會執行候選鏈的行為鎖 `tools/tests/test_dev_start.py::TestGetPythonGeMinPowerShell::test_skips_sub_311_candidate` 當時掛著 `@unittest.skipIf(os.name == "nt")` ⇒ 這支 `.ps1` **唯一出貨的平台**上鑑別力為零，而 macOS／pwsh 上跑同一類全綠（`DEF-101-755` 已逐字記載這件事，本筆是那筆「殭屍鎖」放行出去的第一個真缺陷）；②同類其餘鎖是字面／正則比對，`tools/tests/test_ps51_compat.py` 是**行級掃描器**、掃的是已被剝除字串的程式碼，對「字串**內容**在傳遞途中被吃掉一個字元」架構上無能為力（該檔 `:104` 現已逐字把這一整類缺陷寫成誠實劃界）；③bash 側同一段探測碼寫 `else ""` 完全正常 ⇒ 「兩側逐字相同」的 parity 鎖是綠的，parity 綠不等於兩側都能跑 | P0 | `tools/lib/WindowsAppsGuard.ps1:135`（`else str()`）＋ `tools/lib/windowsapps_guard.sh`（維持逐字相同契約）＋ 回歸鎖 `tools/tests/test_ps51_compat.py::TestPs51NativeArgvRoundTrip`（真起 powershell.exe 做 argv round-trip） | fixed@R71：探測碼空字串改寫為 `str()`——語意完全相同但**不含任何引號字元**，故對 PS 5.1 的命令列重組免疫；bash 側同步採同一寫法以維持「兩側逐字相同」。行為側改由 `TestPs51NativeArgvRoundTrip` 真的起一支 powershell.exe 做 argv round-trip 看守，不再只靠行級掃描。**本包當回合意外實證這條缺陷會反覆咬人、不是一次性事件**：主控為驗 `DEF-101-762` 而現寫的第一版探針裡有 `sys.stdout.reconfigure(encoding="utf-8")`，在同一台機器上被同一個機制吃掉引號，python 回 `NameError: name 'utf' is not defined` ⇒ **任何新寫的 PowerShell 載具只要在 `-c` 字串裡放雙引號就會再中一次**（`tools/tests/test_dev_start.py:5420` 起的正控測試檔頭已逐字記下同款教訓：探測片段刻意不含雙引號） |
| DEF-101-759 | 2026-08-03 | R71 Windows 11 真機開箱路徑實跑（主控親修）；雲端 windows runner 不裝 pyenv-win，此形態結構上只有真機跑得出來 | **pyenv-win 在 shims 同時放兩支同名檔，PowerShell 的 `&` 選中無副檔名的 POSIX sh wrapper ⇒ 回退 ShellExecute ⇒ 彈「你要如何開啟這個檔案？」對話框**：`Get-Command <裸名> -All` 的第一筆就是無副檔名那支（PowerShell 命令探索**不受 PATHEXT 約束**）。本包當回合真機實查：`Get-Command python3.11 -All` 依序回 `C:\Users\wuwei\.pyenv\pyenv-win\shims\python3.11`（無副檔名）與同名 `.bat`；前者內容逐字為 `#!/bin/sh` ＋ `pyenv exec $(basename "$0") "$@"`（CRLF），後者為 `@echo off` ＋ `chcp 1250` ＋ `call pyenv exec %~n0 %*`；本機 `PATHEXT` 為 `.COM;.EXE;.BAT;.CMD;.VBS;.VBE;.JS;.JSE;.WSF;.WSH;.MSC`（不含「無副檔名」）。`& $exe` 對無副檔名那支 CreateProcess 失敗 → ShellExecute → 查不到副檔名關聯 ⇒ 互動式每次探測騷擾使用者一次且該候選判定停在對話框上、無人值守（schtasks／CI）則直接失敗或掛住。落點＝`tools/lib/WindowsAppsGuard.ps1::Get-PythonGeMin` 的候選鏈（`:173` 起）。**為何既有防線沒抓到**：①雲端 windows runner 不裝 pyenv-win ⇒ CI 上根本不存在「同名兩支」這個前提，缺陷條件在 CI 側**結構上不可構造**；②本機日常開工已有 `.venv`，而候選鏈只在「無 .venv 開箱」路徑才會被走到 ⇒ 人工驗證天然繞開；③`Get-Command` 的回傳順序、以及「PowerShell 命令探索不吃 PATHEXT」這兩件事全庫零機械物在看（既有的 `Test-IsRealPython` 守的是 WindowsApps 空殼，不是副檔名解析） | P0 | `tools/lib/WindowsAppsGuard.ps1`（新增 `Resolve-NativeExecutable`） | fixed@R71：新增 `Resolve-NativeExecutable`（`tools/lib/WindowsAppsGuard.ps1:137-165`）——列舉 `Get-Command -All`、**只接受副檔名落在 `PATHEXT` 內的 `Application`**（與 cmd.exe 的解析規則一致），找不到回 `$null`；`Get-PythonGeMin` 的裸 python／python3 分支改為先解析再 `&`（`:183-187`），`py` launcher 分支維持既有存在性檢查（不會被 Store 註冊成空殼）。**本包當回合真機複驗**（原生 Windows PowerShell 5.1.26100.8875、CP=950）：`Resolve-NativeExecutable -CandidateName 'python3.11'` → `C:\Users\wuwei\.pyenv\pyenv-win\shims\python3.11.bat`（跳過無副檔名那支）；`Get-PythonGeMin` → `C:\Users\wuwei\.pyenv\pyenv-win\versions\3.11.9\python3.11.exe`，全程零對話框。🔴 **編號注意**：本 ID 在程式碼裡只有 `tools/lib/WindowsAppsGuard.ps1:140`／`:183` 兩處；另有 7 處程式碼站點也逐字寫 `DEF-101-759` 但講的是完全不同的缺陷（已另立 `DEF-101-763`），撞號處置登記於 `DEF-101-764` |
| DEF-101-761 | 2026-08-03 | R71 追查 `AutoClaude_WindowsSmoke` 排程 2026-08-01 與 08-03 兩次 `LastTaskResult=1` 的真因時，發現無從追起 | **`tools/windows_smoke_local.ps1` 全檔只有 `Write-Host`、零 log 落點 ⇒ schtasks 起的 console 輸出直接進虛空，排程紅燈事後永久不可考**。同一支腳本手動跑是 rc=0（PASS=12 FAIL=0）——「只在排程載具下紅」正是最需要取證的一類，卻剛好完全沒有紀錄，兩次紅燈的真因因此佚失。**為何既有防線沒抓到**：nightly（`AutoClaude/tools/run_local_nightly.ps1`）早就有 RunId log ＋ `nightly_latest.log` 指標 ＋ 14 天輪替，取證紀律 #3 也逐字要求「PASS 聲稱必須引 log 行號」——但那套政策**綁在 nightly 那一支腳本上，不是綁在「被排程器呼叫的腳本」這個類別上**；於是同一台機器、同一個 Task Scheduler、只差一小時的第二支排程腳本（`AutoClaude_WindowsSmoke`，01:00）完全不受涵蓋，而且沒有任何機械物會因為「一支被 schtasks 呼叫的腳本沒有 log 落點」而說話（`tools/tests/test_install_windows_nightly.py` 管的是排程四項設定，不管被呼叫者有沒有取證） | P1 | `tools/windows_smoke_local.ps1:99-137`（transcript 取證層 ＋ `[smoke-env]` 診斷欄） | fixed@R71：`Start-Transcript` 落 `AutoClaude/logs/windows_smoke_latest.log`，形狀對齊 nightly（同目錄＋`_latest` 指標＋14 天輪替），差別在**歸檔時機**改為「下一次啟動時把上一輪的 latest 依它自己的寫入時刻歸檔成 dated」——PowerShell 的 `exit` 不保證跑 `finally`、本腳本有 3 個前置守門 exit 點 ＋ 2 個結尾 exit 點，逐點加收尾必漏；改成前置歸檔後，連行程被 `ExecutionTimeLimit` 強制中止都不會丟掉上一輪內容。transcript 起在三道前置守門**之前**，故「守門就 exit」也有紀錄。另補 `[smoke-env]` 排程載具診斷欄（cwd／whoami／UserInteractive／MSYSTEM／codepage／PSVersion／python 與 git 的解析結果）。**本包當回合實查落地產物**：`AutoClaude/logs/` 下 8 支 `windows_smoke_*.log`；`windows_smoke_latest.log` 標頭逐字含 `PSVersion: 5.1.26100.8875` 與 `主應用程式: ...powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\CursorProject\AISDCL_Agent\tools\windows_smoke_local.ps1`。**取證層落地後第一次就換來根因**——四輪單變因實驗的 `[smoke-env]` 行皆可逐輪回查：cwd=repo 根／cp=65001／`.venv` → `PASS=12 FAIL=0`；cwd=System32／cp=950／pyenv shim → `PASS=11 FAIL=2`；cwd=System32／cp=65001／`.venv` → `PASS=12 FAIL=0`（排除 cwd）；cp=950 ＋ 強制 `.venv` → `PASS=11 FAIL=2`（排除直譯器）⇒ 單一變因為 codepage 950 ⇒ `DEF-101-762` |
| DEF-101-767 | 2026-08-03 | R71 收官前實查「windows-nightly-full 那 2 步 PS 5.1 驗證多久沒跑」，答案與問題本身的預設不同 | **PS 5.1 行為驗證通道不是「近三週沒跑」，是自加入以來零次執行**：`windows-compat-ci.yml` 檔頭 ③（`:163-168`，本包逐字複核）——nightly-full 最後獲配 runner 是 2026-07-14 的 run 29317698994（**僅 9 步、不含這兩步**），首見 `shell: powershell` 的 commit 是隔天的 `1175555`；其後 6 次 schedule 全部 steps=0（帳務平面攔阻，`DEF-101-081`），且該 job 的 `if:` 使 push 一律 skipped ⇒ 5.1 的**行為**面在 PR 閘門零覆蓋，`DEF-101-760` 即這條空窗放行的 P0。**為何既有防線沒抓到（四層各自獨立）**：① 通道死在 **GitHub 帳務平面**，repo 內無機械物觀測得到「job 被配額擋下」；② `tools/lib/ci_liveness.py` 哨兵判準＝「距上次成功 > `STALE_PERIOD_FACTOR × period`」（`:32`＝2.0、`:165` 逐字「週頻軌的門檻是 14 天」）⇒ 週頻軌最快 14 天才響；③ `test_ps51_compat.py` 是**行級**掃描器，比對前已把字串字面值抹成空白（`:104-113` 誠實劃界）⇒ 本族缺陷就住在那字串裡；④ 檔頭原文自陳「已由這兩步在 nightly 層級部分覆蓋」——**該宣稱在寫下那一刻就已失實**（commit 時序如上），同 `DEF-101-741` 判例 | P1 | `.github/workflows/windows-compat-ci.yml`（windows-smoke 新增 1 步 ＋ 檔頭 ③ 訂正） | fixed@R71：挪到**每次 push 都跑**的 `windows-smoke`（`:547` 起，`shell: powershell`），位置貼近「全新機器開箱」；斷言**症狀本身**（`Get-PythonGeMin` 真的挑得到直譯器）而非代理指標 ⇒ `759`／`760`／`766` 三族任一回歸皆由同一步逮到（主控回報 0.53s，未親測） |
