# AutoSDD Defect Log — Archive 37

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **3 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-627`、`DEF-101-631`、`DEF-101-625`
>
> **本次操作備註**：R66 收輪前歸檔：3 筆本輪已結列（DEF-101-625/627/631）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-627 | 2026-07-31 | R66 Review round 1（QA，独立審查 `DEF-101-624` 新增模組 `tools/lib/sdd_latest.py` 的測試覆蓋） | **原始發現**：`tools/lib/sdd_latest.py`（R66 新增，`DEF-101-624`）落地時只做手動 bug-injection 驗證，未落成任何測試檔的永久斷言——`FROZEN_VERSION_DIR_RE.fullmatch()` 邊界行為、`resolve_latest_name`/`resolve_latest_root` 正常路徑與 fail-loud 路徑、`exclude_frozen_sdd_versions` 過濾語意皆無回歸鎖，日後若被還原或改壞不會有任何測試轉紅。本應新增專屬 `tools/tests/test_sdd_latest.py`，但 `DEF-101-561③` 棘輪（`test_adr_xplat001_c1c2_lock.py::TestGuardFileCountShrinkOnlyRatchet`）自 R61 起禁止 `tools/tests/` 新增鎖檔、只准擴充既有檔——故改將測試併入本模組原始兩個肇事呼叫端所在的既有鎖檔：`tools/tests/test_component_sanitizer_shared_layer_lock.py`（`TestFrozenVersionDirRegexFullmatchLock`／`TestOwnCallSiteStaysOnFullmatch`／`TestResolveLatestAgainstRealRepo`／`TestResolveLatestFailLoud`，均標註「R66 追加（DEF-101-627）」）與 `tools/tests/test_sanitize_component_frozen_sdd_versions_lock.py`（`TestOwnCallSiteStaysOnFullmatch`／`TestExcludeFrozenSddVersions`，同款標註）。**round 2 覆審追加發現（Architect，独立複審）**：上述兩檔的 docstring 與 class docstring 共 8 處逐字引用「DEF-101-627」，彷彿該號已立帳，但撰寫測試當下漏了同步寫入本帳本——`grep -rn "DEF-101-627" docs/06_quality/` 實查零命中（主檔與全部 36 份 archive 皆無此列），導致 `python -m pytest tools/tests/test_defect_id_reference_integrity.py -q` 確定性 `1 failed, 10 passed`（`test_every_referenced_defect_id_exists_in_ledger_family` 逐行列出上述 8 處斷鏈引用），連帶 `python tools/run_root_unittests.py` 由全綠轉為 `FAILED (failures=1, skipped=10)`。本列即為補寫的帳本條目，回填後追溯鏈閉合 | P2（新模組上線時缺乏永久回歸覆蓋，若未來被還原或改壞無測試能抓到；且帳本斷鏈曾讓本輪自己的守門測試確定性失敗，波及全套 `run_root_unittests.py` 判讀） | 根層 `tools/tests/`（測試併入既有兩檔）＋ `docs/06_quality/AutoSDD_Defect_Log.md`（本列補寫） | fixed@R66：測試覆蓋部分已於 round 1 隨 `DEF-101-624` 同輪落地（見上）；帳本回填部分為本列本身。**驗證**（本列判定所需的鑑別力鎖 `test_defect_id_reference_integrity.py` 已 Bash 與 PowerShell 各跑一次、結果一致；其餘兩項先於 Bash 跑通，PowerShell 覆跑時另揭露一筆與本列無關的既有缺陷，已獨立記為 `DEF-101-628`、不動搖本列判定）：`python -m pytest tools/tests/test_defect_id_reference_integrity.py -v` Bash＝PowerShell 皆 `11 passed`（回填前 `1 failed, 10 passed`）；`python -m pytest tools/tests/ -q`（Bash）→ `1128 passed, 10 skipped`（回填前 `1 failed, 1127 passed, 10 skipped`；PowerShell 覆跑為 `2 failed, 1126 passed, 10 skipped`——該 2 筆失敗屬 `DEF-101-628`，與本列引用完整性無關，見該列）；`python tools/run_root_unittests.py`（Bash）→ `Ran 1138 tests ... OK (skipped=10)`，rc=0（回填前 `FAILED (failures=1, skipped=10)`；PowerShell 覆跑同樣因 `DEF-101-628` 額外 2 筆而回報 `FAILED (failures=2, skipped=10)`，非本列判定範圍） |
| DEF-101-631 | 2026-07-31 | R66 收尾複審（掌舵者親自 zero-trust 覆核 ADR／帳本內「現查驗證證據」時發現，交由 Fix-applier 修復） | **`ADR-XPLAT-002-platform-surface-reduction.md:5`（§3 決策段 R66 二次訂正句）／`:743`（§5 Phase 2-D 列 R66 現查逐條覆驗①）與 `AutoSDD_Defect_Log.md:136`（DEF-101-626 列，SD 複審引述句＋其自身 fixed@R66 覆驗句，同列 2 處）共 4 處，把同一支指令 `grep -rn "AISDLC_SDD_v\d" --include=*.py tools/` 當作「Phase 2-C／2-D 收斂後 0 筆殘留」的現查驗證證據反覆引用**，但標準 `grep`（無 `-P`/`-E`、雙引號傳入）的 BRE 語意不支援 Perl 風格數字類 `\d`，該指令**不論檔案內容為何恆回空結果、exit 1**——三輪四方複審（含引用者 SD 自己）皆未察覺指令本身失效，逐字抄錄擴散 4 次。本輪親自重跑實測：舊指令 `grep -rn "AISDLC_SDD_v\d" --include=*.py tools/`（Git Bash）→ 空輸出、exit 1（工作樹內容不變的情況下必為此結果，非殘留判準）；改用精準錨定「正則常數定義式」的 `grep -rn -E "_?FROZEN_(SDD_VERSION\|VERSION_DIR)_RE\s*=" --include=*.py tools/` → 含 `tools/lib/sdd_latest.py` 本體時 **1** 筆（該檔 37 行 `FROZEN_VERSION_DIR_RE = re.compile(...)` 定義本尊）、排除本體後 **0** 筆殘留，確認底層事實（5 份定義收斂為 1 份）本身為真，只是原驗證指令是死的。另覆核同段引用的姊妹指令 `grep -rln '"sdd_version.py"' tools/tests/*.py`（不含 `\d`）本輪重跑仍正常回 0（原基線 10），無同型缺陷 | P2（不影響任何程式碼行為或測試結果，純驗證指令語法缺陷；但該指令被複審鏈當作「已現查證實」的證據反覆引用，若不修正會讓後續讀者誤信某段驗證確曾被真實執行，屬證據鏈完整性缺陷） | 本檔自身（`docs/06_quality/AutoSDD_Defect_Log.md:136`）＋ `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md`（`:5`／`:743`） | fixed@R66：4 處引用皆已換成 `grep -rn -E "_?FROZEN_(SDD_VERSION\|VERSION_DIR)_RE\s*=" --include=*.py tools/` 並重跑取得真實輸出（實質結論不變，仍 0 筆殘留），逐處附註原指令恆空的原因。🔴 **附帶更正**：交辦時原引用「5 處」，本輪逐一 `grep` 覆核後確認實際為 **4 處**；另 2 處疑似候選 `re.match(r'^AISDLC_SDD_v\d+\.\d+$', 'AISDLC_SDD_v0.30\n')`（ADR:743 內 2 處）經核對為合法 Python raw-string regex 程式碼示例、非 shell 驗證指令，不受 BRE `\d` 限制、非同型缺陷，不列入本次修復範圍 |
| DEF-101-625 | 2026-07-31 | R66 Review round 1（Architect，独立審查 `DEF-101-619` 修復包新增的鎖測試） | **`test_uninstall_branch_does_not_depend_on_carrier_script_existence`（`tools/tests/test_install_windows_nightly.py:291-334`）docstring 宣稱「以 foreach 為錨點、排除 `-Status` 區塊內那個巢狀 `if ($Uninstall)`」，但 305 行的 `re.search` 無 `^`／`re.MULTILINE` 錨定，實際是「找第一個出現的 `if ($Uninstall) {`」**：對真實檔案重跑同一段 regex，`m.start()=5355`（對應第 130 行、即 `-Status` 區塊內的巢狀 `if`），而非文件宣稱要錨定的第 138 行真正 `-Uninstall` 區塊；捕獲範圍（`m.span()=(5355, 6196)`）雖然過寬（從巢狀 `if` 一路跨過真正 `-Uninstall` 開頭直到 `\n}`），但因真正回歸模式（`Test-Path` 檢查被搬到「介於 -Status 與真正 -Uninstall 之間」）恰好落在此過寬範圍內，對 `DEF-101-619` 這個特定回歸仍有紅綠鑑別力——文件宣稱的機制與程式碼實際機制不符，是品質缺口而非鑑別力缺口，故 Architect 定性為 major（非 blocking） | P3（文件與機制不符，非阻擋；同輪另有黑盒測試 `test_uninstall_whatif_succeeds_when_carrier_scripts_are_unreachable` 提供無此瑕疵的真實覆蓋） | 根層 `tools/tests/test_install_windows_nightly.py` | fixed@R66：正則加 `^` 起點錨點 + `re.MULTILINE`（`^if \(\$Uninstall\) \{`），使其只匹配行首無縮排的真正區塊（第 138 行，2 空格縮排的巢狀 if 在第 130 行不再命中），docstring 同步訂正並補「錨點修訂記錄」段落誠實記載原始缺陷。**bug-injection 紅綠實測**（scratchpad 隔離，不動生產檔）：對修復後現況（控制組）套用新錨定 regex → `PASS（綠燈）`；在 scratchpad 複本上重演 `DEF-101-619` 回歸手法（把兩個 `Test-Path` 檢查搬到 `-Status` 區塊結束、真正 `if ($Uninstall) {` 之前）→ 新錨定 regex 轉紅：`RED: FAIL: $NightlyPs1 存在性檢查未排在 -Uninstall 區塊之後（回歸 DEF-101-619）`。實跑 `python -m pytest tools/tests/test_install_windows_nightly.py -v`（Bash 與 PowerShell 各跑一次，同一 `.venv` python）：`19 passed`，含本測試與語法解析測試（`TestInstallWindowsNightlySyntax::test_parses_with_zero_errors` 未被跳過，證實原生 PowerShell 引擎在場） |
