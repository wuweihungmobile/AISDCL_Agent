# AutoSDD Defect Log — Archive 38

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **34 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-634`、`DEF-101-636`、`DEF-101-632`、`DEF-101-639`、`DEF-101-667`、`DEF-101-635`、`DEF-101-638`、`DEF-101-651`、`DEF-101-648`、`DEF-101-642`、`DEF-101-633`、`DEF-101-654`、`DEF-101-671`、`DEF-101-645`、`DEF-101-660`、`DEF-101-666`、`DEF-101-640`、`DEF-101-668`、`DEF-101-655`、`DEF-101-656`、`DEF-101-637`、`DEF-101-665`、`DEF-101-641`、`DEF-101-644`、`DEF-101-659`、`DEF-101-653`、`DEF-101-672`、`DEF-101-650`、`DEF-101-669`、`DEF-101-657`、`DEF-101-670`、`DEF-101-658`、`DEF-101-647`、`DEF-101-673`
>
> **本次操作備註**：R67 收輪：40 筆存活缺陷（DEF-101-632～671）＋4 筆修復包落地時自曝（672～675）入帳後，主檔 298365 bytes 逾 262144 硬線，依 DEF-99-001 政策輪替；本次只搬本輪已結（fixed）且不帶交棒字樣者，R67 中仍有殘留工作的列（652／661／662／663 等）一律留主檔
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-634 | 2026-08-01 | R67 Scan-B（GUARD 包；pwsh 7.6.3 -NoProfile 行為表實跑，11 列樣本 × 四份實作） | **`tools/lib/WindowsAppsGuard.ps1:55::Test-IsRealPython` 用 `-notlike '*\WindowsApps\*'`，是 real_python_candidate 家族四份實作中唯一未做路徑分隔符正規化者**：`(Get-Command python).Source` ＝「PATH 條目逐字＋分隔符＋檔名」，PATH 條目寫正斜線時 Source 即帶正斜線（同機制已有 R60 P10-2 真 Windows 實測），該 pattern 因缺一個反斜線不命中 ⇒ guard 回 `$true` ⇒ 13 個 `.ps1` 呼叫端全部改去執行 Microsoft Store 空殼，對 `GitHooksInstallCommon.ps1`／`ci-gate.ps1` 這類阻斷式閘門即為掛起。行為表實測 11 列樣本中 5 列 PS 判「真 Python」而 bash／bootstrap_core／pre_run_validator 三份一致判「空殼」＝1 對 3 相反裁決。姊妹 capability（System32／`Find-GitBash.ps1`）同型缺陷 R60 P10-2 已修並補了行為表 parity 鎖，WindowsApps 這半漏修 7 輪（R60→R66）——因為那半有行為表鎖、這半沒有 | P2（原 P1，四方複審降級） | 根層 `tools/lib/WindowsAppsGuard.ps1` ＋ 行為表 parity 鎖 | fixed@R67：改為逐段比對 `Test-HasWindowsAppsSegment`（`-split '[\\/]+'` ＋ `ToLowerInvariant() -eq 'windowsapps'`），比照 `Find-GitBash.ps1::Test-HasSystem32Segment`；補四方行為表 parity 鎖（11 列樣本 × PS/bash/2×Python 真實作，PS 與 bash 讀同一暫存樣本檔）。缺陷注入三向：退回 `-notlike` rc=1『16 failed』／退化為子字串 `-like` rc=1『7 failed』（誘餌列 MyWindowsAppsBackup 翻紅）／拔掉分隔符正規化 rc=1『16 failed』；還原 rc=0『64 passed, 1 skipped, 93 subtests』 |
| DEF-101-636 | 2026-08-01 | R67 Scan-B（GUARD 包；沙箱 clone 內以真實第 4 個 hooks 目錄注入實測） | **WindowsApps bash guard 的 repo-wide 防增生掃描，其「無副檔名 git hook」那條腿（`tools/tests/test_windowsapps_guard_bash_parity.py:219-243::_tracked_extensionless_hook_files`）寫死 3 個 hooks 目錄名冊、無完整性自檢**：在第 4 個 hooks 目錄（`NewSub/.githooks/pre-push`）放一支裸 `command -v python` 的 hook 並 `git add -f`，該測試檔 rc=0『29 passed』全綠零訊號；控制組（同內容改 `.sh`／同內容放名冊內目錄）皆紅 ⇒ 盲區精確落在「無副檔名 ∧ 名冊外目錄」交集。今日 6 支非 `.sh` shell 腳本全在名冊內（預防型），但 hooks 目錄數已從 1 長到 3、`tools/git-hooks/pre-push:161-174` 逐一 `if [ -f ]` 扇出子專案 hook 即為既有標準作法 ⇒ 新目錄的 hook 會真的在阻斷式 pre-push 路徑上被執行 | P2（is_real_bug=false，預防型） | 根層 `tools/tests/test_windowsapps_guard_bash_parity.py` | fixed@R67：掃描面改為**全庫推導**（`git ls-files` 逐檔讀前 128 bytes 判 shell shebang，lru_cache；27474 檔 0.77s），硬編名冊消滅；`_KNOWN_HOOK_DIRS` 降格為只供自檢的快照，新增 `TestHookDirRosterIntegrity` 三支 fail-loud（名冊外目錄／名冊內目錄消失／推導結果為空）。涵蓋面不縮（推導 6 支與舊名冊逐字相同）。沙箱五向注入實測：HEAD 版+注入 rc=0『29 passed』（重現）→ 修法版+同注入 rc=1『2 failed』→ 乾淨樹 rc=0『32 passed』→ 抽掉既有 hook shebang rc=1『1 failed』→ 推導器回傳空 rc=1『3 failed』 |
| DEF-101-632 | 2026-08-01 | R67 Scan-A（CASE 包；macOS 真機 ＋ hdiutil case-sensitive APFS 卷對照實測） | **git index 內存在兩對「僅大小寫不同」的目錄**：`docs/04_planning/{Archive,archive}`（32+18 檔）與 `docs/06_quality/{Archive,archive}`（32+17 檔），在 macOS/Windows 上塌縮成單一磁碟目錄且 `git status` 全綠＝本機零訊號，Linux CI／github.com 上卻是兩個獨立目錄。成因：`f81ad94` 用大寫收 improving_01–31、`22782fe` 改小寫收 32–50，此後靜默共存 6 週。守門缺口同源於兩處：`tools/check_ntfs_paths.py:166` 以整條路徑 `lower()` 當分群鍵、`tools/git-hooks/pre-commit:158` 以 `grep -iFx` 整行比對，對「目錄段拼法不同、basename 完全不重複」結構性失明。hdiutil case-sensitive 卷實測坐實同一 commit 兩平台拓撲不同（1 目錄 50 檔 vs 2 目錄 32+18），且該漂移現在仍可原樣重演 | P2（原 P1，四方複審降級） | 根層基建（`tools/` 守門）＋ 根層 docs/ 資料面 | fixed@R67：資料面收斂為大寫 `Archive/`（35 筆 git rename，plumbing `update-index` 逐條改、工作樹零變動）；機制面 `check_ntfs_paths.py` 新增第 5 軸「目錄段層級碰撞」、pre-commit A3 以 bash 3.2 相容寫法鏡射。回歸鎖 `tools/tests/test_ntfs_trailing_space_device_name.py::TestDirSegmentCollisionAxis`（8 支）＋真 hook 端到端 3 支＋index 現況 3 支；缺陷注入 CI 側／hook 側／資料層三向紅綠實測皆通過 |
| DEF-101-639 | 2026-08-01 | R67 Scan-C（GATE 包；全庫普查 226 行/86 檔命中後逐行檢視真實執行點） | **`tools/integration_gate.{sh,ps1,_core.py}`（monorepo 整合層閘門，5 stage）在整個自動化層零呼叫端**：真實執行點只有 2 行——`macos-compat-ci.yml:635` 與 `windows-compat-ci.yml:640`，而兩支 compat-CI 因 CI 帳務停擺（DEF-101-081）多輪未執行 ⇒「雲端是唯一執行者的東西＝實質已死」。git hooks／nightly／smoke／launchd 四類本機載具 grep 皆掛零；`grep -rn 'integration_gate_core\.' tools/tests/ AutoClaude/tests/` 空輸出＝編排層全庫零測試。閘門本體實跑 rc=0（未壞，壞的是載具） | P3（同構判例 DEF-101-519 判 P3） | 根層 `tools/git-hooks/pre-push` ＋ `tools/tests/test_find_git_bash_parity.py` | fixed@R67：接成雙層本機載具——① pre-push 新增「整合閘門 leg」，路徑範圍觸發 ＋ fail-safe 納入，實跑 `bash tools/integration_gate.sh --skip-full`（偵測迴圈 0.01s/500 檔、命中時 2.24s×3，對照同 leg 既有 root-infra unittest 111.89s ⇒ 日常 push 成本 0、命中時 +2.0%）；② `test_find_git_bash_parity.py` 併入 12 支測試（編排語意／stage 目標存在性雙平台／薄殼→核心委派實跑）。缺陷注入五向＋端到端全數紅/綠對稱，其中 INJ-2（編排吞掉失敗）unittest rc=1 **而真閘門 rc=0** ⇒ 證明第二層不可省 |
| DEF-101-667 | 2026-08-01 | R67 Scan-H（CIPATH 包；以形態依賴注入定位根因，C 維以獨立根因探針交叉確認） | **專守「根層消費檔須列入 compat-CI paths」的機械鎖 `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 對 `tools/lib/sdd_latest.py` 的缺口回報假綠**（19 passed / rc=0、零訊號）：根因為第五個盲區——`_SYSPATH_PARENTS_RE`（`:215-218`）要求整串 `Path(__file__).resolve().parents[N]` 逐字內嵌於 `sys.path.insert(...)` 引數，而 R66 全部 sdd_latest 消費者採具名變數形態，結構性零命中；且該檔亦不在 `:355-375` `expected` 硬編碼防呆表內，兩道防線同時無訊號。判別條件竟是寫法變體而非語意，且對看不懂的形態靜默略過 | P2 | AISDLC_SDD `scripts/tests/test_ci_paths_cover_root_consumers.py` | fixed@R67：依檔頭 R54 既訂收斂門檻（第 4 個盲區起改程式化反向驗證），刪除該正則改用 AST 解析＋同檔名稱綁定常數傳播，並補 fail-loud 讓解析失敗從靜默降級升為顯式失敗。差集實測 NEW=+1（僅 sdd_latest.py）／LOST=0／unresolved=0，零既有偵測退化。新增回歸鎖 3 類共 5 支；缺陷注入四向實測，其中反事實退化回舊正則後覆蓋鎖 rc=0（假綠精確複製，證明 AST 改造為必要且充分）而新回歸鎖 rc=1。鎖由 19 passed 增為 24 passed |
| DEF-101-635 | 2026-08-01 | R67 Scan-B（CASE 包；以 `git update-index --index-info` 注入 NFD 檔名實測） | **NTFS 鎖家族（pre-commit A3 ＋ `tools/check_ntfs_paths.py:109-129` ＋ 4 份 sanitizer）設計標的清一色是「Windows 簽出會不會炸」，對 macOS Unicode NFD/NFC 正規化軸零覆蓋**：注入 NFD 檔名後 macOS 工作樹產生 phantom（同一檔同時 staged 為 NFD、untracked 為 NFC，`git status` 永遠不乾淨；`git clean -fd` 清掉 phantom 即變成 tracked 檔遺失，兩種不乾淨狀態互斥且無常規手段回復），而 `check_ntfs_paths.py` 仍回報 ✅ 0 違規。今日 repo 內非 ASCII tracked 檔名為 0 故非活缺陷，屬預防鎖的結構性缺席 | P3（原 P2，四方複審降級；is_real_bug=false，預防型） | 根層基建 `tools/` | fixed@R67：`check_ntfs_paths.py` 新增第 6 軸（`is_normalized('NFC')` 為 False 即違規）＋碰撞分群鍵改為 NFC→lower 使正規化與大小寫歸為同一類碰撞。**刻意不鏡射進 pre-commit**（實測 `core.precomposeunicode=true` 下 mac 端 `git add` 走 argv 或目錄走訪皆 precompose，NFD 只能由 Linux/GitHub web/plumbing 進來而三者都不經過 pre-commit），此不對稱決策由 `TestNfcAxisScopeIsCiOnlyByDesign` 反向鎖釘住；兩向缺陷注入（NFC 軸恆回 None／分群鍵退回不含正規化）皆轉紅 |
| DEF-101-638 | 2026-08-01 | R67 Scan-C（GATE 包；全庫 grep 普查載具 ＋ tmp fake repo 內真 zsh／真 bash 實跑） | **`tools/dev_start.sh` 的 zsh 分支全 repo 零活體驗證**：macOS 自 Catalina 起預設 shell 即 zsh、ONBOARDING §2.1 教使用者 `source tools/dev_start.sh` 開工；該檔有真正的 zsh 專屬碼（`:13-15,28-30` ZSH_EVAL_CONTEXT sourced 偵測、`:77` `${(%):-%x}` 路徑解析），但全庫 grep 顯示其唯一自動化執行者是 `.github/workflows/macos-compat-ci.yml:381-384` 一個 step，CI 帳務停擺（DEF-101-081）後從未執行；`bash -n`／`zsh -n` 只做語法解析（本機實測皆 rc=0），執行不到該分支 | P3（原 P2，四方複審降級：HEAD 上 zsh 分支實測健康；同位置 DEF-101-104 已於 R9 判 P3） | 根層 `tools/tests/test_dev_start_ps1_lastexitcode.py`（本機載具補位） | fixed@R67：併入 7 支測試（zsh/bash 各一組共用斷言主體），在 tmp fake repo 內以真 zsh／真 bash 跑真 `dev_start.sh`（stub 核心＋stub activate，不建 venv/不連網）；`tools/dev_start.sh` 本體零異動（`check_wrapper_thinness` rc=0 佐證）。注入三向皆紅/綠對稱：破壞 sourced 偵測／`${(%):-%x}`→`$0`／移除 `eval unset` 殘留清理 → 分別 rc=1（2/3/2 支 FAIL），還原後皆 rc=0（Ran 9 OK）且 `git diff` 空 |
| DEF-101-651 | 2026-08-01 | R67 Scan-F（CLI 包；沙箱全新 clone 實跑 `--help` 觀測副作用） | **`tools/bootstrap` 三支完全不解析命令列參數**：`bash tools/bootstrap.sh --help` rc=0 卻**跑完整套 bootstrap**（沙箱實測：`ls -d .venv` 由「不存在」變存在、146M／409 個 site-packages 項），`--forse-bootstrap` 等 typo 同樣 rc=0 靜默走預設路徑；根因＝兩支薄殼（`tools/bootstrap.sh:27`／`tools/bootstrap.ps1:51`）原樣透傳 `$@`／`@args` 到一個零 argv 讀取的核心（`tools/bootstrap_core.py:336`），`bootstrap.ps1` 檔頭還把此透傳寫成刻意設計 | P2（原 P1，四方複審降級） | 根層 `tools/bootstrap_core.py` ＋ `tools/bootstrap.ps1` 檔頭 | fixed@R67：`bootstrap_core.main()` 於任何副作用前 argparse（`--help` rc=0 且零副作用、未知旗標 rc=2），`.ps1` 檔頭訂正；回歸鎖 `tools/tests/test_bootstrap_core.py::TestCliContractNoSilentBootstrap` ＋ `TestWrapperPassThroughKeepsCoreReachable` ＋ `TestUsageEpilogFlagAttributionIsNotStale`（10 支）；缺陷注入雙向實測 rc=1→rc=0。殘餘：CLI 契約只做在核心一處，殼若被改成不傳 `$@` 缺陷會整個復活（已加兩支靜態鎖並注入實測會紅，但仍是結構性單點） |
| DEF-101-648 | 2026-08-01 | R67 Scan-E ＋ Scan-H（RATCHET 包；記憶體注入 `reason="x"` 實測） | **Tier 分類模型 34.8%（8/23）落在 `unpinned`＝「不符任一 Tier 定義」**（`tools/check_script_parity.py:503`），而 unpinned 唯一門檻是 reason 非空（記憶體注入實測 `reason="x"` 回 True，`:915-960`），棘輪只偵測 tier1~4→unpinned、unpinned→unpinned 不在偵測面內（`:964-978`）⇒ 一筆進了 unpinned 即永久合法、無退場義務。掃描員自評 is_real_bug=false（`ONBOARDING.md:139` 逐字全列且有互鎖、零使用者面平台影響），惟「分類法有 1/3 落在以上皆非且無退場義務」之結構性論點未被駁倒 | P3（原 P2，四方複審降級；is_real_bug=false，就判準完整性修補） | 根層 `tools/check_script_parity.py` | fixed@R67：加雙門檻——(a) unpinned 的 reason 須含退場錨點 `退場：未指派`／`退場：R<輪號>`（8 筆逐一補齊）；(b) `_UNPINNED_CEILING=8` 只准下修且刻意不由 baseline 導出（防自我對齊）。回歸鎖 `TestR67UnpinnedExitObligation` 4 支 ＋ `test_unpinned_ceiling_is_not_derived_from_the_baseline`。誠實邊界：不驗證輪號是否仍在未來（該做法會永紅，§191 已警告） |
| DEF-101-642 | 2026-08-01 | R67 Scan-D（BASELINE 包；沙箱把 macOS 欄三格灌成 9999 實測） | **§7 表② 只有一條全域 `snapshot-fingerprints:` 錨（`ONBOARDING.md:257`），而回填在結構上只寫得到一欄 ⇒ 另一欄的 stale 永遠測不到**：沙箱實測把 macOS 欄三格灌成 9999，`--check-snapshot` 照樣 ✅ rc=0。另以 `git log -S` 實查訂正掃描員 evidence：Windows 欄是混世代（v0.30 於 R65 `58a829f` 回填 1747→1750，其餘三格為 R60 世代，R66 `1cbe9fd` 只洗指紋未回填計數） | P2（原 P1，四方複審降級） | 根層 `tools/sync_onboarding_baselines.py` ＋ `ONBOARDING.md` §7 表② | fixed@R67：改為每平台一條錨（四棵樹指紋 ＋ `measured-at`/`host`/`docker`/`pgextras` provenance），判準改逐欄、別平台欄只印單行 ⚠️ 不計 rc；無欄平台（Linux CI）退化為「全欄皆 stale 才紅」並誠實劃界。🔴 Windows 欄因混世代整欄記 `unrecorded`（恆判 presumed stale，不填猜的指紋）＝下一位在 Windows 收輪者必須先跑 `--write --with-slow` 才能過 pre-push leg③，此為刻意設計。回歸鎖 `TestR67PerPlatformFingerprints`（8 支）；注入（忽略平台／刪 provenance 欄）rc=1／還原 rc=0 |
| DEF-101-633 | 2026-08-01 | R67 Scan-A（CASE 包；以 `git cat-file` 當 case-sensitive oracle） | **3 處文件交叉引用指向 `docs/04_planning/Archive/AutoSDD_improving_39.md`，而該檔當時實住小寫 `archive/`**（`docs/04_planning/AutoSDD_improving_54.md:56`、`docs/06_quality/AutoSDD_ZeroTrust_Audit_54.md:36`、`docs/06_quality/AutoSDD_Defect_Log_archive_02.md:127`）：mac/Win 因 FS case-insensitive 可解析，Linux 與 github.com 上 404。oracle 實證：大寫 `git cat-file` rc=128「exists on disk, but not in HEAD」、小寫 rc=0。自查另發現第 4 處：`docs/04_planning/Archive/AutoSDD_improving_31.md` 以相對路徑引用 `AutoSDD_improving_32.md`，31 住大寫 32 住小寫故相對解析亦錯 | P3（原 P2，四方複審降級） | 根層 docs/ 整合層 | fixed@R67：不改散文、改搬檔——本輪收斂為大寫後 4 處引用原地全部變正確（`cat-file` rc 皆 0），因而不必改寫逐字保全的歷史歸檔帳本。機械鎖 `tools/tests/test_ntfs_trailing_space_device_name.py::TestRootDocsPathRefsAreCaseExact`（掃根層 docs/ 260 支 md，以 git index 為真相源而非 `Path.exists()`；現況 259 exact-ok／0 wrong-case）；散文層與資料層雙向注入實測皆轉紅 |
| DEF-101-654 | 2026-08-01 | R67 Scan-F（CLI 包；官方閘門形態 `cd vX && pytest` 實跑 ＋ AST 前瞻掃描） | **AISDLC_SDD 的 `[WINDOWS-NATIVE-ONLY]` skip 可見度機制只覆蓋 `scripts/tests`，對官方閘門的 v0.01／v0.30 兩軌結構上不可達**：共用層 `AISDLC_SDD/conftest.py:78` 在 `cd vX && pytest`（`scripts/ci-gate.sh:126`）的 confcutdir 之上，其 docstring 自陳「不載入/不干擾」；v0.30 已有一支未被彙整的真 Windows-only skip（`AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_file_lock.py:129`） | P3（原 P2，四方複審降級：實際曝險面為 1 支測試而非 93% 測試面） | AISDLC_SDD 共用層 ＋ `AISDLC_SDD_v0.30/conftest.py` | fixed@R67：新增 `AISDLC_SDD_v0.30/conftest.py` 薄殼借用共用層函式（不改 ci-gate.sh、不碰凍結版 v0.01，後者零 Windows 條件式 skip 已機械斷言）＋該 skip 補標籤；回歸鎖 `AISDLC_SDD/scripts/tests/test_conftest_windows_native_skip_report.py` 新增 6 支（含官方閘門形態實跑＋常駐無 conftest 對照組＋LATEST 樹 AST 前瞻掃描）；缺陷注入雙向 rc=1→rc=0，ci-gate log 彙整區塊由「只有 scripts/tests 軌」變成「v0.30 軌 L110 ＋ scripts/tests 軌 L182」 |
| DEF-101-671 | 2026-08-01 | R67 Scan-M（MACNIGHTLY 包；10/10 必然重現的確定性分歧，非 flaky） | **恰好 8.0 天整秒處 bash 與 python 兩份心跳實作判定確定性分歧**（`tools/install_mac_nightly.sh:103-108` BSD `stat -f %m`／`date +%s` 整數截斷 vs `tools/dev_start.py:1665,:1686` `os.stat().st_mtime` 浮點），而既有等價測試（`tools/tests/test_dev_start.py:3446-3471`）以 7.9／8.1 取樣**刻意避開**此點。🔴 訂正掃描員裁決：其主張「任何對齊只是平移分歧點」不成立——載體精度是整數秒為兩側共同下界，dev_start 單側 `int(time.time()) - int(mtime)` 即結構性消除該邊界 | P3 | 根層 `tools/dev_start.py` ＋ `tools/install_mac_nightly.sh` ＋ `tools/tests/test_dev_start.py` | fixed@R67：測試同步納入 8.0 天整與逾期 1 秒兩取樣點，並以 `IMN_NOW`（bash）＋ `time.time` patch（python，刻意帶 0.5 秒次秒偏移模擬真實觀測）做確定性比對；回歸鎖 `test_fresh_boundary_and_far_stale_ages_classify_identically` 之邊界 subTest；注入浮點年齡 rc=1 且僅邊界點翻紅／還原 rc=0。誠實邊界：有限取樣非形式化證明，任一側改變取整方式分歧會回來 |
| DEF-101-645 | 2026-08-01 | R67 Scan-E（MACNIGHTLY 包；mac 真機同一顆心跳檔 `PASS=2 FAIL=2` 修前/修後對照） | **`tools/install_mac_nightly.sh --status` 對 nightly FAIL>0 假綠**：該檔 `:23-24` 自陳「心跳三態語意對齊 `dev_start._check_nightly_heartbeat`」，但 `dev_start.py:1673-1679` 於 R15（ARCH-R15-1）新增第 4 維度 FAIL 計數後 bash 側從未跟上，而跨站等價鎖被寫死在 R12 的「三態」語意（`tools/tests/test_dev_start.py:3423-3430` `_make_heartbeat()` 寫 "heartbeat\n" 永無 `FAIL=` 行、`_classify()` 只回三態）故零訊號。真機實測：修前 `✅ 心跳：新鮮（距今 0 天）` rc=0 | P3（原 P1，四方複審降級） | 根層 `tools/install_mac_nightly.sh` ＋ `tools/tests/test_dev_start.py` | fixed@R67：修後印 `⚠️ 最近一輪 FAIL=2——排程在跑但驗證未全綠…（ARCH-R15-1）`。根因治理：檔頭宣稱改為機讀 `（DIMENSIONS: 三態, FAIL 計數）` 清單，等價鎖 `_classify()` 改回傳 dict 且其鍵集合須與清單逐字相等（雙向翻紅）。回歸鎖 `test_fail_count_is_reported_identically`／`test_lock_covers_every_dimension_claimed_by_installer`；三種注入 rc=1／還原 rc=0 |
| DEF-101-660 | 2026-08-01 | R67 Scan-G ＋ Scan-H（DOCRULE 包；macOS 真機 zsh 實跑，含 stderr 觀測） | **`docs/06_quality/CrossPlatform_Scan_Dimensions.md:195,:200` 硬規則③ 用來示範判準的兩條「R60 實測」指令寫成 `grep -rn … --include=*.md .`**，macOS 預設 zsh 的 nomatch 會在 **grep 被執行之前**中止整條命令列（實測 `zsh:1: no matches found: --include=*.md`、rc=1、連 `2>` 重導向都沒被建立）；而該規則正是以「零命中」推論「容器不存在」⇒ **判準的示範指令在 mac 上恆答『不存在』** | P3（原 P2，四方複審降級） | 根層治理文件 `docs/06_quality/CrossPlatform_Scan_Dimensions.md` | fixed@R67：兩條指令改 `--include='*.md'`（zsh 與 bash 3.2 皆 6 行 rc=0，R60 當年對既有四筆缺陷的裁定不受影響），並就地新增 shell 邊界註記（未加引號的零輸出不是零命中、真零命中 stderr 亦空）。機械鎖 `tools/tests/test_check_defect_log_crossref.py::TestSpecDocShellCommandsAreZshSafe` 6 支（判定面＝反引號內含 grep 的可執行指令，放行純引述壞形態的散文）；注入去引號 rc=1／還原 rc=0。涵蓋面刻意只收規範性規格檔 |
| DEF-101-666 | 2026-08-01 | R67 Scan-H（RATCHET 包；記憶體實測 tampered 與原檔 hash 相同） | **薄殼 hash 釘選的 `tools/check_wrapper_thinness.py:324-334::_normalize()` 把首行 shebang 當註解剝掉**：8 支釘選 `.sh` 的 `#!/usr/bin/env bash` 可改成 `#!/bin/sh` 而 hash 紋風不動（記憶體實測：舊演算法 tampered 與原檔 hash 相同＝True）——而「選直譯器」正是薄殼三職責的第一項，該 hash 鎖對自己宣稱守護的頭號職責天生零訊號。掃描員 R1~R6 已駁倒實際平台危害（8 支皆 100644、14 個呼叫點 100% 為 `bash X.sh`，shebang 目前 inert） | P3（原 P2，四方複審降級；判準載體完整性修補） | 根層護欄 `tools/check_wrapper_thinness.py` ＋ `tools/check_script_parity.py` | fixed@R67：`_normalize()` 首行 `#!` 無條件保留進 hash；重釘 8 支（本表 7 支 `.sh` ＋ LATEST `run_tlc.sh`），重釘前完成三段內容完整性取證（舊演算法重算 16 支全部逐字重現舊 pin；新文字≡shebang+舊文字 16 支皆 True；hash 變動面與 shebang 有無完全重合）；回歸鎖 `TestR67ShebangIsNotAComment` 6 支 ＋ `TestR67LatestPinnedShebangCoverage` 1 支 |
| DEF-101-640 | 2026-08-01 | R67 Scan-C（BASELINE 包；逐 step 歸屬盤點 ＋ 注入全新 CI step 實測） | **`ONBOARDING.md:167-168` 宣稱兩支 smoke 是 compat-CI 的「本地補償**對等**」，實測 macOS 側 19 個實質 CI step 有 5 步零本地承載**（bootstrap 全新建立／bootstrap 重跑／dev_start 實跑／zsh source dev_start／integration_gate 實跑）＋1 步僅部分承載（真實 git commit 經 `core.hooksPath` 觸發 dispatcher；`grep -n "git commit" tools/macos_smoke_local.sh` 空輸出），且該覆蓋差集**零機械鎖**——注入一個全新 CI step 後 8 支根層守門全 rc=0、根層 1139 支測試全綠 | P3 | 根層 `tools/tests/test_smoke_ci_sync.py` | fixed@R67：在既有鎖檔內落 `_CI_STEP_LOCAL_CARRIER` 登記表（macos-smoke 22 步 ＋ windows-smoke 22 步逐步歸屬）＋5 支斷言（新增 step 未登記即紅、登記表 stale 自檢、`NO-LOCAL-CARRIER`/`PARTIAL` 必附理由、指名載具須真存在、§6.1 不得再宣稱「對等」）。刻意不新建掃描器檔案（DEF-101-519 折中），文件改為指向登記表而非重抄對照表。注入（新增 CI step／還原「對等」措辭）rc=1／還原 rc=0 |
| DEF-101-668 | 2026-08-01 | R67 Scan-M（MACNIGHTLY 包；mac 真機植入一份死排程 plist 後對照） | **`tools/install_mac_nightly.sh:145-163::cmd_status` 對已安裝 plist 內容零檢查**：僅 `[ -f ]` 存在性判斷，一份缺 RunAtLoad、ProgramArguments 指向 `/nonexistent/OLD_PATH`、StandardOutPath 導 `/tmp` 的死排程回報三行全綠 rc=0；護欄側 `tools/macos_smoke_local.sh:474-477` 鎖的是安裝器 heredoc **原始碼**而非機器上的**產物**。量化不對稱：`grep -c expected` windows=4／mac=1（且該 1 筆在註解，對照 `tools/install_windows_nightly.ps1:116-120`） | P3（原 P2，四方複審降級） | 根層 `tools/install_mac_nightly.sh` ＋ `tools/tests/test_dev_start.py` | fixed@R67：修後真機 `--status` 逐項印「實際值 (expected 期望值)」四列＋整檔 diff＋三列 launchd 無對應鍵之誠實標註；回歸鎖 `TestMacNightlyPlistCapabilityTable` 4 支 ＋ `TestMacNightlyStatusWiring` 接線鎖；注入未接線 rc=1／還原 rc=0。誠實邊界：`WakeToRun 對等`／`電池策略對等`／`NextRunTime 對等` 三列是散文（launchd 結構上無對應鍵可查），不像上面四列有實際值比對 |
| DEF-101-655 | 2026-08-01 | R67 Scan-F（CLI 包；以真實測試檔＋生產守門 `run_with_floor` 缺陷注入實測） | **`tools/run_root_unittests.py:73,393` 的「N 個 Windows 專屬測試未在原生 Windows 驗證」標題低報 33%**：今日 15 支 skip 全為 Windows 專屬，標題只印 10；未帶標籤的 5 支分別由 R65（`01fd8c3`）與 R66（`8654975`）落地時漏標（`tools/tests/test_bash_probe_spec_contract.py:241,290`、`tools/tests/test_dev_start.py:3599` 等），而全 repo 零個標籤完整性檢查（實測：把帶滿 Windows 關鍵詞的未標籤 skip 附加進既有鎖檔，1140 支測試無一轉紅） | P2 | 根層 `tools/run_root_unittests.py` | fixed@R67：5 支補標籤（標題 10→15、未標籤 0）＋新增前瞻鎖 `untagged_windows_like_skips()`（關鍵詞掃描＋具名豁免表，非 Windows 平台 fail-closed 讓整輪 rc=1；Windows 上刻意閉嘴以免 POSIX-only skip 假紅）；回歸鎖 `tools/tests/test_run_root_unittests.py::UntaggedWindowsLikeSkipsTest` 9 支；缺陷注入 rc=0→1→0。誠實邊界：關鍵詞掃描是啟發式，抓不到 reason 完全不提 windows/win32/pathext 等字眼的 Windows-only skip |
| DEF-101-656 | 2026-08-01 | R67 Scan-F（BASELINE 包；乾淨 venv（`psycopg2`/`sqlalchemy` 皆 ABSENT）真機回填） | **`ONBOARDING.md:216` §7 表① 根層那格寫死 `616（skipped=4；R57 macOS 量測）` 而 macOS 實測已達四位數**（落後約九輪），該格結構上不在 `rootunit-baseline-live` 鎖的取值範圍內 ⇒ 任何機械物都抓不到，卻與受鎖格同處一張標榜「有機械鎖」的表；表② macOS 欄（`:261,:263,:264`）多格亦與真機不符 | P3（原 P2，四方複審降級） | 根層 `ONBOARDING.md` §7 ＋ `tools/sync_onboarding_baselines.py` | fixed@R67：表② macOS 欄以**乾淨 venv** 真機回填 3779/210、1475、1748、253 ＋ 逐欄 provenance；🔴 共用 `.venv`（含 PG extras）量得的 3863/146 依警告**未寫入任何地方**，並把該陷阱升為守門（帶 PG extras 的 venv 回填 rc=2 拒填）。表① macOS 欄改寫成「指向 live 來源 ＋ 自陳不受鎖管轄」而非再填一個會過期的數字。同批訂正三句因回填而失實的散文。回歸鎖 `test_table1_macos_cell_declares_it_is_not_lock_covered`；注入 rc=1／還原 rc=0 |
| DEF-101-637 | 2026-08-01 | R67 Scan-C ＋ Scan-H 交叉命中（CIPATH 包；yaml 解析 paths ＋ fnmatch ＋ 直呼機械鎖內部掃描器三路複核） | **R66 收斂出的 SSOT `tools/lib/sdd_latest.py` 不在兩支 compat-CI 任一 paths 白名單**（`.github/workflows/macos-compat-ci.yml:74,189`、`windows-compat-ci.yml:197,297` 四區塊全缺）：逐檔 grep `sdd_latest.py mac=0 win=0`，姊妹檔 `platform_utils.py`／`bash_probe_spec.py` 皆 mac=2 win=2 為正控制；fnmatch 現查 push_hits=[]。後果＝「只改該 SSOT」的 commit 不觸發兩支 compat-CI，該變更永遠得不到 macOS/Windows 平台維度驗證。減損證據：`root-infra-ci.yml` 無 paths 過濾必跑、pre-push 亦跑 `run_root_unittests`，故遺失的僅平台維度而非全部覆蓋 | P2 | 根層 `.github/workflows/{macos,windows}-compat-ci.yml` | fixed@R67：四個 paths 區塊各補 `- "tools/lib/sdd_latest.py"` ＋成因註解；缺陷注入雙向實測 macOS 側 rc=1→rc=0、Windows 側 rc=1→rc=0；`tools/lib` 全 8 檔 ×2 workflow ×push/PR 稽核零 GAP；YAML 語法有效、push==pr 對稱（macos 63/63、windows 60/60） |
| DEF-101-665 | 2026-08-01 | R67 Scan-H（RATCHET 包；新增第 8 張模組層登記表後觀測 AC 與 tools/tests 反應） | **§4.2 反位移判準 AC 完全無機械鎖**：新增第 8 張模組層登記表（3 筆）後 `tools/check_script_parity.py:1128-1172::_print_collapse` 的 AC 紋風不動、`tools/tests` 零紅，整張表可從判準逃逸；且號稱「獨立重算」的 `tools/tests/test_check_script_parity.py:691-714::test_ac_matches_sum_of_seven_registries` 逐字複製 production 的七項 `len()` 算式＝對該逃逸天生零訊號。AC 值本身亦無鎖（48→49 全綠） | P3（原 P2，四方複審降級） | 根層護欄 `tools/check_script_parity.py` | fixed@R67：AC 涵蓋面資料化（`_AC_REGISTRY_NAMES` 7 張納入 ＋ `_AC_EXCLUDED_REGISTRIES` 6 筆具名排除），`_print_collapse()` 改對 `ac_registries()` 求值；回歸鎖 `TestR67AcCoverage` 6 支以 ast 掃描兩支工具的模組層登記表交叉比對（與 production 不同實作路徑，非恆真）＋ AC 活體值釘選 48。掃描器上線即抓到 `_AC_EXCLUDED_REGISTRIES` 自身未表態，為其非橡皮圖章之活體證據 |
| DEF-101-641 | 2026-08-01 | R67 Scan-D（BASELINE 包；沙箱實跑 `measure_slow()+render_slow()` 於 macOS） | **`tools/sync_onboarding_baselines.py` 全檔零平台偵測**（`grep -E 'platform\|sys.platform\|os.name\|darwin\|win32'` 零命中），而 §7 表② 四格的欄位正則（`:467-500` `_SLOW_SPECS`）一律以 `**…**` 粗體錨定 Windows 欄（原註解自陳「以 `**` 包裝限定在 Windows 欄」）；`ONBOARDING.md:245` 與 `:586` 的 `--check-snapshot` 紅燈訊息**都指路**同一條回填指令。沙箱實跑於 macOS：四格全部落在 Windows 欄、macOS 欄一字未動（rc=0，靜默）⇒ 在 mac 上照文件回填即摧毀該表存在的唯一理由並產生假 provenance | P1（本輪唯一 P1） | 根層 `tools/sync_onboarding_baselines.py` ＋ `ONBOARDING.md` | fixed@R67：改以表頭推導欄座標＋切格後只在該格 `sub`；回填端加「無欄平台 rc=2」「`--platform`+`--write` rc=2」兩道守門。回歸鎖 `tools/tests/test_doc_loc_baseline_freshness_r60.py::TestR67PlatformColumnIsFirstClass`（5 支）；缺陷注入（硬綁 win32）rc=1／還原 rc=0 |
| DEF-101-644 | 2026-08-01 | R67 Scan-D（BASELINE 包；乾淨 clone 內注入真實過期後對照實測） | **`tools/sync_onboarding_baselines.py:772-851::main()` 以 `"--flag" in argv` 手搓解析、未知旗標靜默掉進 default 分支並 rc=0**：於乾淨 clone 注入真實過期後實測「`--check-snapshot` rc=1 而少打一字 rc=0」＝同一棵工作樹同一時刻該紅的守門回綠燈；且三份文件（`ONBOARDING.md:210-211`、`docs/06_quality/CrossPlatform_Scan_Dimensions.md:45` 等）引用的 `--check` 根本不是實存旗標 | P3 | 根層 `tools/sync_onboarding_baselines.py` ＋ 文件旗標反查鎖 | fixed@R67：改 argparse（`allow_abbrev=False`），`--check` 實作為真旗標，未知旗標／打錯字／前綴縮寫一律 rc=2、`--help` rc=0、模式旗標互斥 rc=2；另落**文件旗標反查鎖**（凡提及工具名之行上的 `--flag` 都必須是 parser 實存選項，實測覆蓋 7 個旗標）。刻意不採掃描員「只加 `_KNOWN_FLAGS` 集合」的建議。注入（靜默放行／`allow_abbrev=True`／文件假旗標）rc=1／還原 rc=0 |
| DEF-101-659 | 2026-08-01 | R67 Scan-G（DOCADR 包；macOS BSD grep 2.6.0-FreeBSD ＋ python 獨立 ground truth 對照） | **假驗證同型復發**：R66 為修 DEF-101-631（`\d` 假驗證）而換上的替代指令，其 `--include=` glob 未加引號（`ADR-XPLAT-002-platform-surface-reduction.md:5`、`:743`），在 macOS 預設 zsh（nomatch）下整條命令於呼叫 grep 前即被跳過（`zsh:1: no matches found`、rc=1、零 stdout）——修假驗證的指令自己是假驗證。另 R66 記載的根因「標準 grep BRE 不支援 Perl 風格 `\d`，恆為空／exit 1」為過度一般化：macOS BSD grep 實測 `\d` 在 BRE 與 ERE 皆為數字類，回 55 行 rc=0，與 python ground truth 逐字相同 | P2（原 P1，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` 狀態欄與 §5 Phase 2-D 列 ＋ §9.1 SC-1 | fixed@R67：兩處補單引號＋追加 R67 訂正，並把原根因限定為 GNU grep／Git Bash 側現象。誠實邊界：GNU grep 側本輪無法重測（本機 `command -v ggrep` 無結果），依 §6 邊界 1 標為推論 |
| DEF-101-653 | 2026-08-01 | R67 Scan-F（CLI 包；`/bin/bash -c 'echo ${XPC_SERVICE_NAME}'` 真機實測 ＋ 7 份真排程 log 回讀） | **macOS nightly 觸發來源歸因永遠誤判為 launchd**：`AutoClaude/tools/run_local_nightly.sh:50-61` 用 `[ -n "$XPC_SERVICE_NAME" ]` 測存在性，而 `XPC_SERVICE_NAME=0` 是 macOS 對一般使用者行程注入的常態值（真機實測回 `0`），使 manual-interactive／non-interactive-unknown 兩態成為死碼；此欄唯一被設計服務的取證情境（同日兩輪 PASS 歸因）會得到反向結論——去重漏洞被記成排程觸發 | P3（原 P2，四方複審降級：生產 log 10 天內誤標次數為 0） | AutoClaude nightly 載體 `tools/run_local_nightly.sh` | fixed@R67：改為與 launchd job Label 值比對（`NIGHTLY_LAUNCHD_LABEL`，與 `tools/install_mac_nightly.sh` 的 `LABEL` 跨檔字面鎖），註解中被真機證偽的宣稱一併訂正；回歸鎖 6 支（4 支抽真判定區塊實跑＋跨檔一致性＋靜態判準）；缺陷注入雙向 rc=1→rc=0（含 label 漂移注入） |
| DEF-101-672 | 2026-08-01 | R67 GUARD 包落地時自曝（`_INLINE_NOTLIKE_RE` 對整檔文字 vs 剝註解後對照） | **靜態鎖靠註解裡的字面值維持全綠**：修法把舊寫法 `-notlike '*\WindowsApps\*'` 原文引述進 `tools/lib/WindowsAppsGuard.ps1` 的 `<# #>` 檔頭沿革後，既有靜態鎖 `test_shared_guard_file_exists_and_defines_the_function` 的 `assertRegex(整檔文字, _INLINE_NOTLIKE_RE)` 仍然通過——一個宣稱「鎖住 guard 邏輯本體」、實際只鎖住「字串曾出現過」的鎖（與同檔 R46 在 bash 側治過的 `_has_ssot_guard` 同病）。實證：`_INLINE_NOTLIKE_RE` 對整檔文字命中＝True、對剝掉註解後的程式碼命中＝False | P2 | 根層 `tools/tests/test_windowsapps_guard_cross_consistency.py` | fixed@R67：新增 `_ps_code_only()`（剝 `<# #>` 區塊與 `#` 行註解），斷言改為「新形狀必須在、舊形狀不得在」，並由真跑 PowerShell 的行為鎖承擔主要鑑別力 |
| DEF-101-650 | 2026-08-01 | R67 Scan-E（DOCADR 包；對兩支 compat-ci 全部 6 個 job 的既有守門逐一回讀） | **ADR-XPLAT-002 §8 item 9（`:870`）的延後理由因果反了**（「帳單停擺無 CI 回饋通道，改完無法實跑驗證」——正因雲端停擺、本機才是唯一活體通道，該斷言本就該在本機跑），且該列隱含「workflow 層零守門」實測不成立：`tools/tests/test_workflow_permission_concurrency_lock.py` 已逐一具名釘選兩支 compat-ci 全部 6 個 job、`test_gha_action_versions.py::TestWindowsCiHeaderSnapshotLock` 蓋住 Windows 側新增方向 | P3（原 P2，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §8 item 9 | fixed@R67：刪除該理由、訂正射程、把殘餘缺口收斂為「macOS 側單邊新增 job 零訊號」單一方向；明文排除 step 級對稱與 paths glob 正規化（理由＝§6 邊界 6 實測顯示 step 層重複多為合理平台差異） |
| DEF-101-669 | 2026-08-01 | R67 Scan-M（MACNIGHTLY 包；注入硬編 8 ＋ 生產側 8→10 三向對照） | **`tools/tests/test_dev_start.py:3408` 跨站等價鎖硬編第 4 份 `HEARTBEAT_MAX_AGE_DAYS=8` 且不受任何跨檔鎖保護**：兩生產站點（`tools/install_mac_nightly.sh:57`、`tools/dev_start.py:1519`）合法同步演進（8→10）時字面鎖仍綠、只有行為等價鎖假紅，且失敗訊息指控生產程式碼「兩份獨立實作分歧」把維護者導向不存在的問題（macOS-only，因該 class 有 `skipUnless darwin`） | P3（原 P2，四方複審降級） | 根層 `tools/tests/test_dev_start.py` | fixed@R67：改為自 `dev_start._HEARTBEAT_MAX_AGE_DAYS` 動態注入，四份字面值收斂為兩份（生產各一、測試零份）；回歸鎖 `test_bash_wrapper_threshold_follows_dev_start_constant`；注入硬編 8 rc=1、硬編 8＋生產 8→10 rc=1 三紅完全重現掃描員宣稱、同狀態換上修法 rc=0 |
| DEF-101-657 | 2026-08-01 | R67 Scan-F（MACNIGHTLY 包；本機 07-28/29/30 三天真實空窗對照） | **`tools/install_mac_nightly.sh --status` 心跳哨兵無法偵測「中間漏跑」**：本機 07-28/29/30 三天零 nightly（整段關機），修前 `--status` 仍印「✅ 心跳：新鮮（距今 0 天）」rc=0，因 07-31 開機後 RunAtLoad 補跑一輪把計數歸零、空窗永久不可見（`:57`、`:97-111`；對照 `tools/install_windows_nightly.ps1:113-115`） | P3（原 P2，四方複審降級） | 根層 `tools/install_mac_nightly.sh` ＋ `tools/tests/test_dev_start.py` | fixed@R67：修後印「⚠️ 覆蓋連續性：近 7 天有 3 天無 nightly 紀錄（20260730, 20260729, 20260728）」，逐日日期與磁碟實況吻合；回看窗口 7 天刻意短於載體 14 天輪替窗口，並自「昨天」起算避免 00:00~02:00 假缺口。回歸鎖 `TestMacNightlyCoverageContinuity` 3 支；注入未接線 rc=1／還原 rc=0 |
| DEF-101-670 | 2026-08-01 | R67 Scan-M（MACNIGHTLY 包；(8,9) 窗口 10 點修前/修後逐字對照） | **(8,9) 天窗口內心跳過期文案自我矛盾**：SD-R13-1 只改判定（`tools/install_mac_nightly.sh:108-109` 秒級比較）沒改顯示（`:105` `_age_days` 整數天除法截斷），691201~777599 秒整整 24 小時印出「距今 8 天 > 8 天」這種數學上為偽的句子；且 691199 秒（7.9999 天）被低報為「距今 7 天」。`tools/dev_start.py:1687-1690` 同型窗口 [8.0, 8.05) 天約 72 分鐘 | P3 | 根層 `tools/install_mac_nightly.sh` ＋ `tools/dev_start.py` ＋ `tools/tests/test_dev_start.py` | fixed@R67：兩站點同步改——bash 以「先乘 10 再整除」合成一位小數（bash 3.2 無浮點，不引入 bc／awk），訊息改為不含不等式的敘述句；回歸鎖 `test_stale_message_carries_no_false_inequality`；注入整數天除法+不等式文案 rc=1／還原 rc=0 |
| DEF-101-658 | 2026-08-01 | R67 Scan-G（DOCADR 包；zsh 與 bash 對照組實跑） | **ADR-XPLAT-001 §4 步驟 0 第 1 項「可觸達性」的閘門指令 `grep -rn "<入口符號>" --include=*.py AISDLC_SDD/`（`ADR-XPLAT-001-copy-on-evolve-frozen-baseline-backport.md:118`）在 macOS 預設 zsh 下零 stdout／rc=1**，而 ADR 明文把零輸出定義為「可觸達性＝否」且「任一項拿不出來就不得進入步驟 1」⇒ 該不可跳過的閘門在 macOS 上結構性恆答否，每次 Copy-on-Evolve 回補評估被自動路由到 §4.3「只動 LATEST」 | P3（原 P1，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-001-*.md:118` ＋ ADR-XPLAT-002 §9.1 SC-1 | fixed@R67：加單引號（實測 7 行 rc=0，bash 對照組同）並加嚴判準——零消費者結論必須同時出示控制組非零命中（硬規則① 反「零輸出＝好消息」） |
| DEF-101-647 | 2026-08-01 | R67 Scan-E（DOCADR 包；以 `git cat-file` 自物件重建該 commit 的樹並重算） | **ADR-XPLAT-002 §8 item 7（`:866`）寫死的護欄層基線在其自己指名的落地 commit `796c7a6` 上複現不出來**：自物件重建得 74 檔／34,035 行，檔數對得上、行數差 1,327；同一把量尺對 `tools/tests` 重建得 56／26,286 與 §4.3 Pkg-E 值逐字吻合＝量尺有效 | P3（原 P2，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §8 item 7 ＋ §8.2 ＋ §9.1 SC-3 | fixed@R67：依 12-Rule Rule 7 移除全部量測常數、改指向新增的 §8.2 M-1~M-3 現查指令，並落 §9.1 SC-3 禁止常數回流。另訂正該列隱含的「零預算⇒膨脹」因果：`AutoClaude/tools` 與 `AutoClaude/tests` 兩棵零預算樹同期幾乎持平，相關性來自工作面而非預算有無 |
| DEF-101-673 | 2026-08-01 | R67 MACNIGHTLY 包自查（承接上一輪夭折 session 留下的半套修改） | **上一輪夭折的半套修改把 `report_plist_capabilities()` 與 `report_coverage()` 兩支函式寫好卻未接進 `cmd_status`**，`bash -n`／既有全部測試皆綠而 `--status` 行為與修前一模一樣；另 `_cap_line` 用裸 `$4`（本檔 `set -u`，省略第四參數即 unbound 中止整支 `--status`）、檔頭引用杜撰識別碼 `ARCH-R67-1`（全 repo 零 referent）、檔頭宣稱「等價鎖會解析 DIMENSIONS 清單」但測試側完全未動（該句在繼承狀態下為假） | P3 | 根層 `tools/install_mac_nightly.sh` ＋ `tools/tests/test_dev_start.py` | fixed@R67：四項均已修正並補靜態接線鎖 `TestMacNightlyStatusWiring::test_cmd_status_invokes_both_reports`；注入拿掉任一呼叫 rc=1／還原 rc=0 |
