# AutoSDD Defect Log — Archive 53

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **6 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-779`、`DEF-101-775`、`DEF-101-776`、`DEF-101-773`、`DEF-101-778`、`DEF-101-780`
>
> **本次操作備註**：R73 收官歸檔：搬本輪已結 5 列 + R72 遺留 DEF-101-773（本輪修畢）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-779 | 2026-08-04 | R73 Scan-C 逐項稽核兩支 schtasks job，交叉比對安裝器原始碼與 `Get-ScheduledTaskInfo` 線上實值 | 🔴 **「修 A 的唯一途徑會靜默破壞 B」＝沒有人敢執行的修復指令**。`ADR-SD09-012` 早已列出本機五項排程設定從未套上（Nightly/Smoke 的 `ExecutionTimeLimit=PT72H` 應為 PT4H、`MultipleInstances=IgnoreNew` 應為 StopExisting ×2、Smoke `LogonType=Interactive` 應為 S4U），R73 實測值與該 ADR §2.7 的量測**逐字相同**——經 R71、R72 兩輪**一項都沒套上**。機械原因在此被找到：要套上就得跑 `tools/install_windows_nightly.ps1`，而該檔 install 路徑是 Unregister→Register **且把觸發時間寫死** nightly `'02:00'`／smoke `'01:00'`，與實況（實測 nightly **22:30**、smoke **23:30**）不符 ⇒ 跑下去會把時間靜默改掉。連帶 **7 處文件漂移**（安裝器檔頭／`run_local_nightly.ps1` 檔頭 3 處／`ONBOARDING.md` 3 處）仍寫 02:00／01:00，且 `run_local_nightly.ps1` 那段「smoke 刻意排在 nightly 之前一小時」的立論與現況**相反**（現行 smoke 晚於 nightly 一小時）| P0 | `tools/install_windows_nightly.ps1` 參數化 ＋ 3 檔 7 處文件 ＋ `tools/tests/test_install_windows_nightly.py` 兩條鎖改判準 | **fixed@R73（阻塞解除，套用需舵手提權）** — 時間改為 `-NightlyAt`／`-SmokeAt` 參數並帶 `ValidatePattern`。預設值取法在兩個約束交會處定案：`NightlyAt='22:30'`＝現行實況（不帶參數跑不動 nightly）；`SmokeAt='21:30'`＝**回復被 active 鎖守住的不變量**「smoke 早於 nightly」（`test_smoke_task_shares_catchup_settings_and_runs_before_nightly` 斷言 smoke_at < nightly_at）。🔴 衝突時挑**被鎖住的那一邊**（Rule 7 不折中）：鎖是經論證並機械強制的產物，現行 23:30 則是全庫查無理由的手動漂移；若把兩個預設都設成現況，等於讓預設值主動違反一條 active 鎖、把技術債固化進安裝器。文件 7 處**不改寫成新常數**（那只是換一個會再過期的數字），一律改為指向現查 `Get-ScheduledTask ... \| Get-ScheduledTaskInfo`。兩條鎖改讀 param 預設值而非 trigger 字面值，順序不變量**未弱化**。實測：預設 `-WhatIf` rc=0 且機器未被改動（NextRunTime 仍 22:30／23:30）、顯式傳參 rc=0、非法時間 `25:99` 被 ValidatePattern 擋且 **rc=1**（首測讀到 rc=0 是 `\| Select-Object` 覆寫 `$LASTEXITCODE` 的假數字，去掉管線重測才拿到真值——本 repo 既有坑，本輪再次踩到並自查）、23 passed。**未做（需提權，交棒舵手）**：五項設定的實際套用；建議 elevated 跑 `tools/install_windows_nightly.ps1`（欲維持現行 smoke 時刻則傳 `-SmokeAt 23:30`），事後以 `-Status` 或 `Get-ScheduledTaskInfo` 貼出憑證 |
| DEF-101-775 | 2026-08-04 | R73 舵手複現 Scan-C 報告的 nightly obs 軌 `TOOL-ERROR`，於真檔 `AutoClaude/.observability_history.jsonl` 上端到端重跑 | 🔴 **本輪唯一活體 P0：閘門把「已達標」報成「量不出來」**。`run_local_nightly.ps1` 四支閘門 helper（`Get-ObsGaPass`／`Get-DriftGaPass`／`Get-Ac4Gate`／`Get-MutationLockGate`）＋ F2 稽核區塊共 **5 處**共用同一個 JSON 濾法「見到第一個 `{` 之後**後面全收**」，隱含假設 stderr 一律排在 JSON 之前（原註解逐字這樣寫）。實測不成立：`observability_ga_check.py` 的 legacy-record WARN 走 stderr 且排在 JSON **之後**，`2>&1` 合流後被接到尾巴 ⇒ `ConvertFrom-Json` 拋 `Additional text encountered after finished reading JSON` ⇒ 走 catch ⇒ `Ok=$false`。**舵手當回合逐字複現**：工具 rc=0、真實答案 `status=ready, green_streak=43, window=30`（obs GA 早已達標），現行濾法 `collected=3` 且 parse FAILED。回歸點可定位：8/3 02:00 那輪 log `obs_ga=True`，8/3 22:30（R71 commit `1e5214b` 落地後）起變 `TOOL-ERROR`。**方向性是本筆算 P0 的理由**：R71 那包自陳「存在的理由就是消滅假達標數字」，卻在同一個閘門種了一個**假未達標**——假未達標讓人白等，與假達標同為假訊號。既有 66 支測試全綠卻抓不到，因為所有 payload 都是「乾淨 JSON」或「完全沒有 JSON」，真實世界的第三種形狀（JSON ＋ 尾隨雜訊）零覆蓋 | P0 | `AutoClaude/tools/run_local_nightly.ps1` 5 處濾法 ＋ `AutoClaude/tests/tools/test_run_local_nightly_static.py` 補鎖 | **fixed@R73** — 修法＝「收到第一個能成功 parse 的 JSON 就停」（`ConvertFrom-Json -ErrorAction Stop` 試 parse ＋ 成功即 `break`）。**刻意逐支內聯不抽共用層**：行為測試會用 regex 抽出單一函式本體丟給 `powershell.exe` 獨立真跑，抽成共用函式那支探針會找不到函式而失去鑑別力（此設計意圖原就記載於 `test_gate_helpers_share_three_state_contract` docstring）。六情境於 **PS 5.1 生產引擎**實測全正確（stderr 在後／在前／兩側夾殺／pretty-print／無 JSON／JSON 截斷後兩者 fail-closed）。補三條鎖並以**注入舊 bug 實證有牙**：靜態鎖 `test_gate_helpers_stop_at_first_parseable_json`（綁四支，涵蓋沒有行為測試的 `Get-Ac4Gate`／`Get-MutationLockGate`）＋行為鎖 `test_stderr_after_json_*` 2 條 → 注入後 3 紅、對照組不假紅；還原後 66 passed rc=0、注入痕跡 grep 0、`-ErrorAction Stop` 5 處齊備；端到端複驗 obs 閘門量到 `status=ready streak=43/30` |
| DEF-101-776 | 2026-08-04 | R73 因本機新裝 PowerShell 7.6.4 而首次可達的路徑；Scan-M 逐筆比對 CI 與本機鏡射的斷言差異 | **本機 smoke 缺 CI 早已有的引擎斷言，pwsh 7 進場後會靜默失去鑑別力**。`tools/windows_smoke_local.ps1` 的 `[1/9] Parser::ParseFile` 用的是**執行本腳本那個引擎**的文法；而 `.github/workflows/windows-compat-ci.yml` 的 `windows-smoke` job 帶 `if ($PSVersionTable.PSVersion.Major -ne 5) { throw "ENGINE-MISMATCH..." }`，本機鏡射卻只 `Write-Host` 印版本、**不擋**（既有前置守門只擋 MSYS，不擋 pwsh）。**為何 R73 才成為缺陷**：在 pwsh 7 裝上這台機器之前，`powershell` 就是唯一引擎、本守門不可能被觸發；2026-08-04 起這條路第一次可達。失效方向**靜默且偏危險側**——5.1 與 7 的 `Encoding::Default` 不同（zh-TW：big5 vs utf-8），「UTF-8 無 BOM ＋ 中文註解」的 `.ps1` 在 7 解析全綠、在 5.1 被 Big5 誤讀到吃掉引號而 parse 死。Scan-M 全庫 137 支 `.ps1` 雙引擎實測：**5.1 → OK=108/ERR=29；7.6.4 → OK=137/ERR=0**，且以「同一份 bytes 前面補 `EF BB BF` 後 5.1 errors 由 1 → 0」隔離證明根因是缺 BOM（非語法）。生產（schtasks 兩支 job 的 Action）跑的是 `powershell.exe`＝5.1，用 7 驗等於用寬鬆文法驗受 5.1 約束的對象。🔴 舵手本輪**親身踩到同一機制**：以 pwsh 7.6.4 的 `Set-Content -Encoding utf8`（不寫 BOM）寫一支含中文的原型腳本，交給 5.1 執行即 `Missing '=' operator in hash literal` parse 死 | P1 | `tools/windows_smoke_local.ps1` 前置守門區 | **fixed@R73** — 補上與 CI 同語意的 ENGINE-MISMATCH fail-loud 守門，刻意不提供 `-Force` 旁路（旁路會被習慣性使用，而它豁免掉的正是這支腳本唯一的鑑別力來源）。兩向實測：pwsh 7.6.4 執行 → 擋下 **rc=1** 並印 ENGINE-MISMATCH；`powershell.exe` 5.1 執行 → 放行 **rc=0**、`PASS=12 FAIL=0`、ENGINE-MISMATCH 命中數 0（零假擋）|
| DEF-101-773 | 2026-08-03 | R72 依根 `CLAUDE.md` 鐵律一指定做法（「`.sh` 腳本＝PowerShell 內呼叫 `bash <script>`」）實跑 `install_mac_nightly.sh` 語法檢查時 | **鐵律一自己指定的載具做法在本機是壞的**：`Get-Command bash` 解析到 `C:\WINDOWS\system32\bash.exe`（**WSL，非 Git Bash**），且反斜線路徑被吃掉，逐字輸出＝`/bin/bash: D:CursorProjectAISDCL_Agenttoolsinstall_mac_nightly.sh: No such file or directory`、**rc=127**。改用 `C:\Program Files\Git\bin\bash.exe` ＋**正斜線**路徑後 rc=0（控制組 `dev_start.sh` 同樣 rc=0，證明不是被測腳本的問題）。**同型舊帳**：`DEF-101-617/618(b)` 已記載「本機 `shutil.which("bash")` 誤解析 WSL 佔位版」，本輪是同一根因換到 PowerShell 載具再犯一次 | P1 | 根 `CLAUDE.md`〈🔴 Windows 側單一載具原則〉鐵律一表格「`.sh` 腳本」列 | **fixed@R73** — 已修，且修法**優於本列原處方**：原處方寫「一律指名 `C:\Program Files\Git\bin\bash.exe`」，那只是把「裸 bash 解析錯」換成「把一台機器的安裝路徑寫成治理文件的常數」（Git 裝在別處的 checkout 照著一律失敗）。R73 改走 repo 既有 SSOT `tools/lib/Find-GitBash.ps1`（含 system32/WSL 逐段排除），雙引擎實測皆回 `C:\Program Files\Git\bin\bash.exe`、`-n` 語法檢查 rc=0，對照 `Get-Command bash` 兩引擎皆誤命中 system32。另見 `DEF-101-778`（同輪把 `block_bash_on_windows.py` hook 訊息裡的同一句壞指引一併修掉——**執行規則的機械物不該教壞掉的作法**）。原處方殘留＝該列改寫為「呼叫 `.sh` 一律指名 `C:\Program Files\Git\bin\bash.exe` 並用正斜線路徑，**禁裸 `bash`**」。🔴 本條形態正是本 repo 反覆在治的「已知缺口只以劃界結案」（`DEF-101-757`）：鐵律一是 R71 為消滅載具決策負荷而立的，卻在它自己指定的替代路徑上留了一個會靜默 rc=127 的洞 |
| DEF-101-778 | 2026-08-04 | R73 Scan-M 比對 hook 訊息與根 `CLAUDE.md` R72 訂正的一致性 | **執行規則的機械物自己教了壞掉的作法**。`.claude/hooks/block_bash_on_windows.py` 的指引訊息與 docstring 教人寫裸 `bash <script>`，而根 `CLAUDE.md` 鐵律一（R72）已明載裸 `bash` 會走 WSL 佔位版且吃掉反斜線（`DEF-101-773`）。實測 `Get-Command bash` 在 **5.1 與 7.6.4 兩個引擎都**回 `C:\WINDOWS\system32\bash.exe`。同一則訊息另寫「PowerShell 5.1 沒有 && 與 \|\|」——互動載具升到 7.6.4 後該句對讀者為假（7.x 支援這兩個運算子），於是整條建議看起來已過期而會被忽略，**真正的理由與互動載具是哪一版無關**：生產路徑跑的是 `powershell.exe`＝5.1。hook 比純文件更權威（讀者會照它做），教錯的殺傷力更大 | P1 | `.claude/hooks/block_bash_on_windows.py` ＋ 根 `CLAUDE.md`〈鐵律一〉 | **fixed@R73** — 兩處都改走 repo 既有 SSOT `tools/lib/Find-GitBash.ps1`（含 system32/WSL 逐段排除），且**不寫死任何磁碟機路徑**（該 hook 會被 commit，寫死對其他 checkout 一律是錯的指引，且 `tools/tests/test_platform_neutral_paths.py` 會逐行掃描判紅）。`&&` 那句改為綁**生產引擎**而非綁當下 session 的版本。雙引擎實測 SSOT 版：`Find-GitBash` → `C:\Program Files\Git\bin\bash.exe`（5.1 與 7.6.4 皆同）、`-n` 語法檢查 rc=0；對照 `Get-Command bash` 兩引擎皆誤 |
| DEF-101-780 | 2026-08-04 | R73 開場探測 agent 讀 `CrossPlatform_Scan_Dimensions.md` 時順手命中 | **把會成長的清單寫進標題，注定過期**：該檔標題逐字寫「跨平台複審 Scan-A~H 掃描維度慣例」，而表格早已擴充到 Scan-**M／N／T**（M 為 R67、N 與 T 為 R68 新增）；§維度定義 的小節標題亦只提「M 為 R67 新增」，漏 N 與 T。與本檔緣起段自陳「**刻意不寫死輪號**」（R56 教訓：原寫『累積至今 R47』時實況已 R55）是**同一個病，只是這次寫死的是字母範圍** | P2 | `docs/06_quality/CrossPlatform_Scan_Dimensions.md` 標題 ＋ §維度定義 小節標題 | **fixed@R73** — 標題改為不含字母清單（維度清單只有下方表格一個權威源，與該檔既有的「不寫死輪號」政策一致）；小節標題補上 N 與 T |
