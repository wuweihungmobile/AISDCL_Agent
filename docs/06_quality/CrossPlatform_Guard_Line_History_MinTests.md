# 護欄層行數棘輪 — MIN_TESTS 重釘沿革姊妹檔

- **建立日期**：2026-09-09
- **建立者**：DEF-200-274 第五輪四方獨立複審收尾單人窗口

本檔是 [CrossPlatform_Guard_Line_History.md](CrossPlatform_Guard_Line_History.md) 的姊妹檔（DEF-101-587 拆分慣例：入口檔逼近 Read 工具單次讀取上限時，新增姊妹檔承載較新的節，入口檔留一行指標，不改名、不刪內容）。

---

## MIN_TESTS 逐輪重釘沿革（R15~R136）

WHY 搬遷理由：`tools/run_root_unittests.py` 的 `MIN_TESTS` 常數採「每次重釘把新理由
prepend 在舊理由前面、整段留在同一個 trailing comment」的記帳慣例，經 R15 至 R136
數十輪累積後，這一行的長度已達 23,000+ 字元／1 個實體行——第五輪四方獨立複審收尾
時，這個長度觸發了 pre-push 閘門某個 encoding-sniffing 路徑的 `SyntaxError: Non-UTF-8
code`（單行過長導致讀取緩衝在多位元組 UTF-8 字元中途截斷）。修法：把完整沿革原文
逐字搬到本節（零內容流失），該行本身改回只留**最新一輪**的重釘理由＋指向本節的
指標，比照本檔既有慣例（`_scan_roots` 三處修正 WHY／R60 round 3 tmpdir 判準 WHY
等節皆為同一種「原地過長→搬遷至本檔」的處置）。

原文逐字如下（R15 為最早、R136 為最新，中途每一次「前值 N 為 XX 值，以下為 XX
沿革」皆為當時重釘者自己所寫，本次搬遷未改一字）：

🔴 **重釘 4042 → 4045（方向＝收緊；2026-09-09，DEF-200-274 第四輪第二次對抗式複審收斂批，Developer 角色收尾）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算；成長來源＝本輪新增回歸測試類別 `ParallelShardWorkerThreadBaseExceptionDoesNotEscapeTest`（覆蓋 Architect finding 1(a)：worker thread 內部拋出非 `Exception` 子類的 `BaseException`（`SystemExit`）時 `run_parallel()` 不得讓它穿透，改由新抽出的 `_crash_fallback()` 直接收尾、不依賴外層 `except Exception` 篩選型別，逐項見 `docs/06_quality/CrossPlatform_DEF200274_Parallel_Tests_Evidence.md`〈第四輪〉節）。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格已由 `python tools/sync_onboarding_baselines.py --write` 同步回填。前值 4042 為 DEF-200-274 第三輪對抗式複審收斂批值。以下為 4042 沿革：🔴 **重釘 4034 → 4042（方向＝收緊；2026-09-08，DEF-200-274 第三輪對抗式複審收斂批，收尾單人窗口）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算；成長來源＝本輪新增回歸測試類別 `ParallelShardStderrBackpressureRegressionTest`／`ParallelShardMergeResultsMissingKeyStillRaisesTest`／`ParallelShardRunParallelExceptionSafetyTest`／`ParallelShardMergeExceptionLeakFenceIntegrationTest`／`ParallelShardPopenFailureKillsAlreadyStartedProcsTest`／`ParallelShardRealSubprocessProtocolIntegrationTest`（覆蓋 stderr backpressure 修復、`run_parallel()` 例外安全網、Popen 失敗孤兒行程清理與不 mock `subprocess.Popen` 的真實協定通道整合測試，逐項見 `docs/06_quality/CrossPlatform_R131_Scan_Findings.md` §8）。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格已由 `python tools/sync_onboarding_baselines.py --write` 同步回填。前值 4034 為 DEF-200-274 本機平行執行落地批值。以下為 4034 沿革：🔴 **重釘 4033 → 4034（方向＝收緊；2026-09-08，DEF-200-274 本機平行執行落地批）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算；成長來源＝本輪新增 `ParallelShardMergeSmokeTest` 一支純函式冒煙測試（AC8：驗證 2 分片情境 JSON 彙總協定能正確重建 testsRun 加總與 skip/error 清單，不真的 spawn subprocess）。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格已由 `python tools/sync_onboarding_baselines.py --write` 同步回填。前值 4033 為收尾單人窗口 guard-line 記帳收尾批值。以下為 4033 沿革：🔴 **重釘 4032 → 4033（方向＝收緊；2026-09-08，收尾單人窗口 guard-line 記帳收尾批）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算——本輪只做 guard-line 記帳（`_FROZEN_GUARD_LINES`／`_GUARD_LINES_REPIN_LOG`）與 `_EXPECTED_MIN_TEST_COUNTS` 下限重釘，未新增任何測試方法（實測前後皆 4033），絕對餘裕軸容忍度內（`min_tests_margin.absolute_gap_message(4033, 4032)` 回 `None`，未觸發）；純粹補齊上一輪「中途值」自陳的收尾重釘義務（M3「作者自證不計分」，複審收斂後仍須再釘一次）。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格已由 `python tools/sync_onboarding_baselines.py --write` 同步回填。前值 4032 為 M-04 leak_fence 判死面落地批值。以下為 4032 沿革：🔴 **重釘 3895 → 4032（方向＝收緊；2026-09-07，M-04 leak_fence 判死面落地批）**：由餘裕軸判準（`tools/lib/min_tests_margin.py`）的紅線層在本輪新增回歸鎖後**逐字指示**「請把 tools/run_root_unittests.py 的 MIN_TESTS 重釘為 4032」⇒ 本行照填、零加減推算（值取自唯讀 discovery 探針，不執行任何測試）。成長來源＝本輪 `LeakFenceTest` 判死面三支（種下且收尾仍在＝紅／註冊完又登出＝不紅／列舉量不到＝不判死）＋`RatchetDriftWarningTest` 的 `MIN_TESTS` 絕對餘裕軸兩支（`min_tests_margin.absolute_gap_message`：分母是實跑數自己，`collapse_loss` 歸零時餘裕軸整條靜音而它不會）。🔴 **本值是中途值、且量測當下工作樹帶有同工作樹並行包尚未提交的異動**（該包新增的測試也計入本數）⇒ 收尾單人窗口須在所有並行包停工後再釘一次；本下限是**單邊**的（釘太高＝零相依收集數低於下限＝硬紅），若那些新增被回退，必須同輪把本值下修，不得放著。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。前值 3895 為 DEF-200-170 收尾值。以下為 3895 沿革：🔴 **收尾單人窗口重釘 3767 → 3895（方向＝收緊）**：由 `DEF-200-170` 本次落地的餘裕軸判準（`tools/lib/min_tests_margin.py`）在三個並行實作包全部停工後的全套實跑中**逐字指示**「請把 tools/run_root_unittests.py 的 MIN_TESTS 重釘為 3895」⇒ 本行照填、零加減推算。🔴 **這是本行歷來第一次由「該重釘」自己的判準先開口**：前五次同型復發（R82／R83／R84／R96／R96-D2）先炸的都是 `ZeroDepEnvironmentDiscriminationTest`，而它的紅字把讀者指往「相依沒裝齊」——方向錯的歸因正是 `DEF-200-170` 的缺陷本體；本次餘裕軸在環境判準失效**之前**說話並直接帶出目標值，那就是該缺陷的驗收核心（行為層證明，非公式替換）。成長來源＝本次三個並行實作包各自新增的回歸鎖（`DEF-200-169` 的扇出視窗剩餘秒數／`DEF-200-170` 的餘裕軸判準本身／`DEF-200-222` 的併發鎖與縮窄阻斷面）。🔴 本檔 raw-line 餘裕為 0 ⇒ 本次重釘刻意**不新增任何一行**，全部併進本行。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。前值 3767 為 v2.1.13 G3+G4 值。以下為 G3+G4 沿革：🔴 **v2.1.13 G3+G4 實作批 (c)+(d) 重釘 3747 → 3767（方向＝收緊；2026-08-31，施工圖＝PRD_Amendment_R113_WakeChain_LastMile.md §3(c)/(d)，落款批）**：以唯讀 discovery 探針（discover_suite(_TESTS_DIR).countTestCases()，不執行任何測試）實測直接填入、零加減推算。成長來源＝RelayStateMachineTruthTableTest 三支＋RelayProgressAndCapTest 九支＋RelaySettleWindowTest 五支＋RelayFailurePathsTest 兩支＋SentinelArmingCriterionTest 新增一支（合計 20 支）。比例 3767/3747≈1.005，未跨 RATCHET_WARN_RATIO（1.10）⇒ 兩層保鮮棘輪照舊沒先響，照實記、不動比例常數。🔴 本檔 raw-line 餘裕為 0 ⇒ 本次重釘刻意不新增任何一行，全部併進本行。同步站點：ONBOARDING.md §7 表① 的 rootunit-baseline-live: 格由 python tools/sync_onboarding_baselines.py --write 於同一次變更內回填。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。前值 3747 為 G2 批 (b) 值。以下為 G2 沿革：🔴 **v2.1.13 G2 實作批 (b) 重釘 3741 → 3747（方向＝收緊；2026-08-31，施工圖＝PRD_Amendment_R113_WakeChain_LastMile.md §3(b)，落款批）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算。成長來源＝HandbackVisibilityTest 三支＋HandbackSessionStartAnnounceTest 三支（V-b1~V-b3）。比例 3747/3741≈1.002，未跨 `RATCHET_WARN_RATIO`（1.10）⇒ 兩層保鮮棘輪照舊沒先響，照實記、不動比例常數。🔴 本檔 raw-line 餘裕為 0 ⇒ 本次重釘刻意**不新增任何一行**，全部併進本行。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。前值 3741 為 G1 批 (a) 值。以下為 G1 沿革：🔴 **v2.1.13 G1 實作批 (a) 重釘 3735 → 3741（方向＝收緊；2026-08-31，施工圖＝PRD_Amendment_R113_WakeChain_LastMile.md §3(a)，落款批）**：以唯讀 discovery 探針（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算。成長來源＝UnattendedPermissionPostureTest 六支（V-a1／V-a2 三支／V-a4／V-a3 靜態半格）。比例 3741/3735≈1.002，未跨 `RATCHET_WARN_RATIO`（1.10）⇒ 兩層保鮮棘輪照舊沒先響，照實記、不動比例常數。🔴 本檔 raw-line 餘裕為 0 ⇒ 本次重釘刻意**不新增任何一行**，全部併進本行。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。🔴 本值依本行自己的判準仍屬中途值（本批修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。前值 3735 為結構性長債分軌輪收尾值。以下為該輪沿革：🔴 **結構性長債分軌輪收尾單人窗口重釘 3631 → 3735（方向＝收緊；2026-08-30，掌舵者分軌裁決存證＝AutoSDD_TechDebt_Paydown_Playbook.md §6 第 3 條）**：於收尾單人窗口以**唯讀 discovery 探針**（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測直接填入、零加減推算。成長來源＝本輪 TestStructuralDebtLog 九支＋分軌前數輪已落地而未及重釘的鎖檔成長（前值 3631 釘於 R100 收尾）。比例 3735/3631≈1.029，仍未跨 `RATCHET_WARN_RATIO`（1.10）⇒ 兩層保鮮棘輪照舊一次都沒先響，照實記於此、不動那兩個比例常數（判準改動需複審）。🔴 本檔 raw-line 餘裕為 0 ⇒ 本次重釘刻意**不新增任何一行**，全部併進本行。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。🔴 本值依本行自己的判準仍屬中途值（本包修復未經第三方複審，依 M3「作者自證不計分」），複審收斂後必須再釘一次。前值 3631 為 R100 收尾值。以下為 R100 沿革：🔴 **R100 收尾單人窗口重釘 3464 → 3631（方向＝收緊）**：所有並行包停工後，於最終工作樹以**唯讀 discovery 探針**（`discover_suite(_TESTS_DIR).countTestCases()`，不執行任何測試）實測 3631，直接填入、零加減推算（全套實跑的當下計數見本 runner 終端輸出，刻意不抄一份）。成長來源＝本輪並行包新增鎖檔，非任一單包。比例 3631/3464＝1.048，仍**未**跨 `RATCHET_WARN_RATIO`（1.10）⇒ `DEF-200-170` 記的「兩層保鮮棘輪一次都沒先響」本輪第六度復現，照實記於此、不動那兩個比例常數（判準改動需複審）。🔴 本檔 raw-line 餘裕實測為 0（`SPECIAL_FILES` budget 759＝現值）⇒ 本次重釘刻意**不新增任何一行**，全部併進本行。同步站點：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 於同一次變更內回填。前值 3464 為 R96 D2 值。以下為 R96 沿革：🔴 **R96 修復包 D2 重釘 3462 → 3464（方向＝收緊）**：本包（四方複審三輪之後的收斂波）只改註解與散文、不新增也不刪除任何測試，故實收數與前次僅差 2——重釘的價值不在這 2 支，而在把差額歸零、不把「該重釘」這件事當成待辦承接到下一輪。🔴 **本句原文在此寫了一個超前輪號（帳本當前輪 ＋1 的字面），由 R96 第 ⑩ 波收尾窗口就地改為「下一輪」；🔴 訂正註記刻意不逐字引述那個字面——引述它就是把違規再造一次（本窗口實測犯過一次）**：`tools/tests/test_check_defect_log_crossref.py::TestR71CodeRoundLabelsNeverExceedLedgerCurrentRound` 禁止程式碼散文出現超前帳本 `current_round()` 的輪號，而 D2 交件時未跑全套（D1 同時在改帳本，並行會互踩）故沒看見這一紅。**刻意不掛該鎖的具名豁免**（常數＝該檔的 `_ROUND_LABEL_EXEMPT`，字面刻意不寫在這裡——寫下去會讓本行**當場被自己豁免掉**，那是「看起來變乾淨」而不是修好）：該豁免的具名用途是「測試用的合成帳本語料」，本行不是；把它借來用等於把一個窄豁免磨成通用後門。**輪號要往前指就寫「下一輪」**，別在 `.py` 裡開第二個輪次時鐘。🔴 **這是同型復發的第五次**：R82／R83／R84 逐字同型，第四次就是本行下方那則收尾單人窗口的 3284 → 3462。🔴 **本次順帶訂正一個假因果，逐字記下以免下輪再被它擋住**：R96 收尾窗口當時不重釘的理由是「重釘會連動 `ONBOARDING.md` 的 live 格，而那又要求再跑一次全套」——QA 複審實查 `tools/sync_onboarding_baselines.py` 的 `measure_rootunit()` 證偽：那一格讀的是**本常數本身**（`import run_root_unittests` 取 `MIN_TESTS`，該函式 docstring 逐字寫「刻意用 import 而非重跑整套 unittest」）⇒ `--write`／`--check` 皆為秒級，**重釘的成本是零**。⇒ 「同步站點很貴」自此不得再被當成不重釘的理由。🔴 **值的取得**：本包在**只做 discovery、不執行任何測試**的唯讀探針內取 `discover_suite(_TESTS_DIR).countTestCases()`，直接填入、零加減推算（刻意不採信交辦書轉述的數字，自行量過）。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本包是四方複審**之後**的收斂波，其修復本身尚未被第三方看過 ⇒ 依 M3「作者自證不計分」，與 R74／R77／R78／R81～R85 那幾次同型，複審再收斂後必須再釘一次。🔴 **另記一筆本行守不住的事（`DEF-200-170` 承接）**：本行下方那兩層保鮮棘輪（`RATCHET_WARN_RATIO` 1.10／`RATCHET_STALE_RATIO` 1.25）在這五次同型復發裡**一次都沒有先響**；每次真正先響的是零相依沙箱那支鑑別力測試。原因是兩層棘輪量錯了分母——真正的安全餘裕是「**零相依沙箱**收集數 vs 本下限」（QA 複審量到 3288，本包未複驗），而不是相依齊備環境下的 `discovered − MIN_TESTS`（本次只有 2）。本包刻意不動那兩個比例常數（那是判準改動、需複審），只把事實記在這裡。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 在同一次變更內回填。前值 3462 為 R96 收尾單人窗口值。以下為 R96 收尾單人窗口沿革：🔴 **R96 收尾單人窗口重釘 3284 → 3462（方向＝收緊）**：本輪兩批修復（Windows 真機切換輪八筆跨平台修復＋四方複審全數 REJECT 後的修復包 A／包 B）新增了判準本體，最終工作樹的實收數升到本值，而舊下限已低於**零相依沙箱**的收集數 ⇒ `tools/tests/test_run_root_unittests.py::ZeroDepEnvironmentDiscriminationTest::test_zero_dep_message_says_environment_not_disappearance` 當回合 FAIL，訊息逐字是「`環境問題` not found in `❌ discovery 佔位測試 4 筆…`」——閘門走「下限通過」那一支、印佔位測試清單並把整棵樹再跑一次，而不是走 `report_floor_failure` 印出環境歸因。⇒ 這是本行下方那則預告的**第四次**應驗（R82／R83／R84 逐字同型），也再次證明本下限的鑑別力是**會隨樹長大而失效**的，不是一次釘死就算了。值取本 runner 當回合印出的計數直接填入、零加減推算；重釘由**收尾單人窗口在所有並行包停工後**做一次（同 `_FROZEN_GUARD_LINES` 的重釘紀律）。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 在同一次變更內回填。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本包是四方複審**之後**的收斂波，其修復本身尚未被第三方看過 ⇒ 依 M3「作者自證不計分」，與 R74／R77／R78／R81／R82／R83／R84／R85 那幾次同型，複審再收斂後必須再釘一次。前值 3284 為 R85 F1 值。以下為 R85 F1 沿革：🔴 **R85 F1（四方複審收斂包）重釘 3268 → 3284——方向是收緊，且它訂正的是 P2 那次下修的正當性**：SD 複審實測指出，P2 把下限由 3279 下修到 3268 時，**最終樹的實收數高於舊下限 3279** ⇒ 舊下限根本不會轉紅，那次下修沒有換到任何東西，只把「靜默蒸發若干支測試仍然全綠」的窗口從 0 拉開到 16。**併表本身是正當的**（SD 逐族核對：三族的注入樣本數 10→10、3→3、2→2，逐字零損失；生產側 `_FORBIDDEN` 16 鍵 AST 零差異），不正當的是**併完之後把下限跟著調低**——表驅動化真正需要的補償是**樣本數鎖**而不是下修下限，而 P2 只補了族①（`test_check_wrapper_thinness._FORBIDDEN_CASES`），族②（`test_dev_start._FALSE_NEGATIVE_CASES`）與族③（`._FALSE_POSITIVE_CASES`）漏補 ⇒ 本輪補齊那兩族，並把下限重釘回**當回合實測值**（零加減推算）。⇒ 補了樣本數鎖之後，表驅動版對「刪掉一列樣本」的鑑別力**強於**原本的 N 支獨立方法：刪一列當場轉紅，而刪一支方法只能靠 MIN_TESTS 這個粗聚合察覺。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本行明訂重釘應綁在四方複審全數 APPROVE 之後，而本包是**複審後的收斂波**、其修復本身尚未被第三方看過 ⇒ 依 M3「作者自證不計分」，與 R74／R77／R78／R81／R82／R83／R84 那幾次同型，複審收斂後必須再釘一次。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 在同一次變更內回填。前值 3268 為 R85 P2 值。以下為 R85 P2 沿革：🔴 **R85 P2 重釘 3279 → 3268（本輪唯一一次「下限往下」，理由必須看得懂才算數）**：本輪把數族「同鷹架、只有注入字串不同」的測試收斂成表驅動判準（`test_check_wrapper_thinness` 的 forbidden 注入族、`test_dev_start` R23 假陰性族與R25 假陽性族）⇒ **unittest 的「測試方法數」下降，但注入樣本、期望值與斷言一個都沒有少**——它們改由 `subTest` 逐案報出，失敗訊息裡看得到是哪一列紅的。這與本行歷來「往上釘＝收緊」的方向相反，故照實寫成**放寬**並列入四方複審：判斷本次下修是否正當的唯一方法是去讀那三族的 diff，看樣本數有沒有真的守住（本行不代為擔保）。值取本 runner 當回合印出的計數 3268，零加減推算。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格由 `python tools/sync_onboarding_baselines.py --write` 在同一次變更內回填。前值 3279 為 R84 值。以下為 R84 沿革：🔴 **R84 收輪重釘（收尾單人窗口親自，於本輪全部並行包停工後在最終工作樹實跑本 runner，取其當場印出的計數直接填入、零加減推算）**：🔴 **本次重釘的觸發與 R82／R83 那兩次逐字同型，本行下方的預告第三次應驗**：`tools/tests/test_run_root_unittests.py::ZeroDepEnvironmentDiscriminationTest::test_zero_dep_message_says_environment_not_disappearance` 當回合 FAIL，訊息逐字是「`環境問題` not found in `❌ discovery 佔位測試 4 筆…`」——舊下限已低於零相依沙箱的收集數，於是走「下限通過」那一支、印佔位測試清單並把整棵樹再跑一次，而不是走 `report_floor_failure` 印出環境歸因。⇒ 這支鎖量的是「本下限對零相依環境還有沒有鑑別力」，它一紅就代表下限舊到讓自己的看守者失效。**方向是收緊**（下限往上釘＝讓「靜默蒸發若干支測試仍然全綠」的窗口變小），與「不准為了讓數字好看而調高門檻／棘輪」那條禁令方向相反——那條禁的是**放寬**。🔴 **本值依本行自己的判準屬中途值，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」，而 **R84 的四方複審未執行**（九包並行後直接進收尾單人窗口）⇒ 依成熟度判準 M3「作者自證不計分」，本值與 R74／R77／R78／R81／R82／R83 那幾次同型，複審收斂後必須再釘一次。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 3095 為 R83 值。以下為 R83 沿革：🔴 **R83 收輪二次重釘（舵手親自，於全部 13 個 workflow 停工後的單人窗口實跑本 runner，取其印出的計數直接填入、零加減推算）**：前一次 R83 值 3052 是本輪**中段**（四方複審之前）量的，其後四方複審 55 筆 findings 的收斂波（W10~W12）與 mac 喚醒鏈 P0（W11）又淨增測試 ⇒ 3052 對收輪實況的鑑別力已衰減。**方向是收緊**（下限往上釘＝讓「靜默蒸發若干支測試仍然全綠」的窗口變小）。🔴 **本輪這一次終於滿足了本行自己的判準的大半**：四方複審**已執行**（Architect／SA／SD／QA 全數 APPROVE_WITH_CONDITIONS、無人 REJECT），其 blocking 由 W10~W12 逐筆收斂 ⇒ 本值不再是 R74／R77／R78／R81／R82 那種「複審根本沒跑」的中途值。**仍未滿足的那一半照實寫**：複審後的收斂包（W10~W12）與本次重釘**沒有再被第三方看過** ⇒ 依成熟度判準 M3「作者自證不計分」，那一段仍是自證。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 3052 為 R83 中段值。以下為 R83 中段沿革：🔴 **R83（mac 真機首輪）收尾重釘（舵手親自，於全部 agent 停工後的單人窗口實跑本 runner，取其印出的計數直接填入、零加減推算）**：量測前後各取一次含內容的工作樹指紋並確認相同。🔴 **本次重釘的觸發與 R82 那次逐字同型，且本行下方 R74／R69 的預告再次應驗**：`tools/tests/test_run_root_unittests.py::ZeroDepEnvironmentDiscriminationTest::test_zero_dep_message_says_environment_not_disappearance` 當回合 FAIL，訊息逐字是「`環境問題` not found in `❌ discovery 佔位測試 4 筆…`」——舊下限已低於零相依沙箱的收集數，於是走「下限通過」那一支、印佔位測試清單並把整棵樹再跑一次，而不是走 `report_floor_failure` 印出環境歸因。⇒ 這支鎖量的是「本下限對零相依環境還有沒有鑑別力」，它一紅就代表下限舊到讓自己的看守者失效。**方向是收緊**（下限往上釘＝讓「靜默蒸發若干支測試仍然全綠」的窗口變小），與「不准為了讓數字好看而調高門檻」那條禁令方向相反。🔴 **本值依本行自己的判準屬中途值，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」，而**四方複審在本值落地時尚未執行**——配額五小時桶觸及收斂帶，依 `quota_policy` 的節流演算法延到 reset 後（外部資源限制，不是疏漏）⇒ 依成熟度判準 M3「作者自證不計分」，本值與 R74／R77／R78／R81／R82 那幾次同型，複審收斂後必須再釘一次。🔴 **本輪另記一筆與本行相關的實測**：本輪成長主要來自 mac 真機第一次看得見的判準（逐檔清單見 `docs/06_quality/CrossPlatform_R83_Scan_Findings.md` §B），其中三支新鎖檔曾有 3 個類別繼承本地共用夾具而非直接繼承 `TestCase`，被 `test_schedule_capability_parity` 判為「unittest 不收」——實測那三類**其實收得到**（貢獻 16 支真的在跑的測試），故該判準本輪已補上「同檔內基底鏈解析」以消掉那個假陽性；若當時照假紅去攤平類別階層，會換來三份手抄夾具。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 2795 為 R82 值。以下為 R82 沿革：🔴 **R82 收尾重釘（本輪最後一個修復者親自，取 runner 當場印出的計數直接填入、零加減推算）**：量測於本輪所有並行包停工後的**單人窗口**內取得——本包動工到量測之間沒有其他 agent 在改樹，且本包自己的改動只碰註解／散文與本常數，不新增也不刪除任何測試（收集面盤存的檔數與指紋在改動前後相同，值本身不寫在這裡，見本 runner 當場輸出）。🔴 **本次重釘不是例行保養，是為了修一支已經在紅的鎖**：`tools/tests/test_run_root_unittests.py::ZeroDepEnvironmentDiscriminationTest::test_zero_dep_message_says_environment_not_disappearance` 當回合 FAIL——舊下限已經**低於零相依沙箱的收集數**（宣告相依被擋掉時只有 4 支模組塌成佔位測試，其餘照收），於是 `run_with_floor` 走的是「下限通過」那一支、印出 discovery 佔位測試的清單並**把整棵樹再跑一次**，而不是走 `report_floor_failure` 印出「環境問題」的歸因。⇒ 這支鎖量的其實是「本下限對零相依環境還有沒有鑑別力」，它一紅就代表下限已經舊到讓自己的看守者失效。🔴 **方向與連鎖代價的釐清（免得下一輪誤讀本行下方 R74／R69 的警語而不敢重釘）**：那兩段警語講的失效模式正是**下限太舊**——探針不再提前判紅、改為實跑整棵樹，牆鐘因此暴衝甚至撞逾時。把下限往上釘＝**收緊**，恰好把那條放大路徑關掉，**不是**觸發它；本次重釘後零相依探針在下限層即判紅，不再進入整棵樹的執行面。與「不准為了讓數字好看而調高門檻／棘輪」那條禁令方向相反——那條禁的是**放寬**。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」，而**針對本包這幾筆修復的第三方複審並未執行**（本包在額度節流下不派任何子 agent）⇒ 依成熟度判準 M3「作者自證不計分」，本值與 R74／R77／R78／R81 那幾次同型，下一輪複審收斂後必須再釘一次。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 2574 為 R81 值。以下為 R81 沿革：🔴 **R81 收尾重釘（收尾者親自，取 runner 當場印出的計數直接填入、零加減推算）**：量測在 11 個並行包全部停工後的**單人窗口**內取得，量測前後工作樹未再變動。🔴 **本次另加一項前幾輪沒做的交叉驗證**：同一棵樹分別以本機 `.venv`（裝了 PG extras 與 sdk extra）與**出廠乾淨 venv**（兩者皆無、與 ONBOARDING §7 表② Windows 欄同一支直譯器）各跑一次，兩邊收集數**相同**且皆 rc=0 ⇒ 這個下限不是某一種環境的偶然值。做這件事的理由是本行下方已記載的失效模式：下限一旦低於**零相依環境**的收集數，零相依探針就不再提前判紅而是實跑整棵樹並撞逾時（`DEF-101-803`）——只在裝滿 extras 的環境量一次，恰好看不見那個環境。🔴 **方向是收緊、不是放寬**（本常數是下限，往上釘＝讓「靜默蒸發若干支測試仍然全綠」的窗口變小），與「不准為了讓數字好看而調高門檻／棘輪」那條禁令方向相反。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」，而 R81 的四方複審**尚未執行** ⇒ 依成熟度判準 M3「作者自證不計分」，本輪 11 包與本收尾包的改動全屬作者自證，下一輪複審收斂後必須再釘一次（承接載體＝帳本 `DEF-101-876`，輪號寫在帳本那一列、不寫在本行）。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 2466 為 R80 值。以下為 R80 沿革：🔴 **R80 收尾重釘（收尾包親自，取 runner 當場印出的計數直接填入、零加減推算）**：本值在 R80 push 前的**單人窗口**內量得——量測時無任何並行 agent，量測前後工作樹未再變動。🔴 **方向是收緊、不是放寬**：本常數是**下限**，往上釘＝讓「靜默蒸發若干支測試仍然全綠」的窗口變小；前值對當時實況的鑑別力已明顯衰減（差額就是可靜默蒸發的支數，runner 每次執行都會把當下的兩個值印在終端，故此處不寫死）。與「不准為了讓數字好看而調高門檻／棘輪」那條禁令**方向相反**——那條禁的是**放寬**。🔴 **本行自己的判準仍有一半未滿足，照實記**：R80 四方複審跑了兩審、blocking 全收斂，但二審收斂包與本收尾包其後落地的改動**沒有再被第三方看過**（含本包為解開 ONBOARDING §7 表② 回填所修掉的 ci-gate 真紅：五支 `tools/lib/` 共用層未列入三支 workflow 的 paths，被 `test_ci_paths_cover_root_consumers.py` 當場攔下）⇒ 依成熟度判準 M3「作者自證不計分」，那一段仍是自證。**同步站點**：`ONBOARDING.md` §7 表① 的 `rootunit-baseline-live:` 格與本常數由 `test_root_unittest_cell_agrees_with_min_tests_ssot` 綁死，本次以 `python tools/sync_onboarding_baselines.py --write` 在**同一次變更內**回填。前值 2341 為 R79 值。以下為 R79 沿革：🔴 **R79 複審後重釘（複審後修復包親自，取 runner 當場印出的計數直接填入、零加減推算）**：重釘在四個複審後修復包全部停工後的**單人窗口**內執行——沿用 R78 已取證的機械事實：多包共用一棵工作樹時本 runner 的 rc 是「別人鍵盤的函數」，故量測窗口必須是單人的。🔴 **本行的判準第一次真的被滿足，但只滿足了大半，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」——R79 的四方（Architect／SA／SD／QA）**已跑，且四方全部 APPROVE_WITH_CONDITIONS**，其 blocking 條件由複審後修復包逐筆收斂，故本值不再是 R74／R77／R78／R79 前次那種「複審根本沒跑」的中途值。**仍未滿足的那一半照實寫**：複審審的是**被送審的那批修復**，而為了收斂那些條件在其後新落地的判準（含本包自己的改動）**沒有再被第三方看過** ⇒ 依成熟度判準 M3「作者自證不計分」，那一段仍是自證，其強度上限是「作者自己跑的注入紅綠」。🔴 **方向說明（免得下輪誤讀）**：本常數是**下限**，往上釘＝**收緊**（讓「靜默蒸發若干支測試仍然全綠」的窗口變小），與「不准為了讓數字好看而調高門檻／棘輪」那條禁令方向相反——那條禁的是**放寬**。前值 2284 為 R79 複審**前**的中途值、2201 為 R78 中途值。以下為 R78 沿革：🔴 **R78 收尾重釘（收尾者親自，取 runner 當場印出的計數直接填入、零加減推算）**：重釘在所有修復包與所有獨立 agent 停工後的窗口內執行，量測前後各取一次含內容的工作樹指紋（`git status --porcelain` ＋ `git diff HEAD`）並確認相同。🔴 **本值依本行自己的判準仍屬中途值，照實記**：本行明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」，而 R78 的四方複審審的是**上一輪 R77**（30 findings／9 blocking，逐筆見 `docs/06_quality/CrossPlatform_R78_Review.md`）——**針對本輪五個修復包本身的第三方複審並未執行** ⇒ 依成熟度判準 M3「作者自證不計分」，本值與 R74／R77 那兩次同型，下一輪對本輪修復複審收斂後必須再釘一次。🔴 **R78 另記一筆本行未涵蓋的失效面**：本輪實測到「多 agent 共用單一工作樹」會讓本 runner 的 rc 成為**別人鍵盤的函數**（同指令相隔數分鐘一紅一綠，成因見 `DEF-101-886`）⇒ 本行要求的「量測窗口前後工作樹指紋相同」在該作業型態下**結構上不成立**，本輪的處置是紀律（收輪閘門一律在所有 agent 停工後取得）而非機械物，缺口仍在。前值 2105 為 R77 中途值。以下為 R77 沿革：**本輪收尾重釘（收尾者親自）**：所有修復包停工後於最終工作樹實跑本 runner，取其印出的計數直接填入、零加減推算；量測窗口前後各取一次含內容的工作樹指紋並確認相同。🔴 **本值的取得條件有一項未滿足，照實記**：重釘時修復包確已停工，但**四方複審本輪未執行**——被月度支出上限擋住 agent 派工（外部資源限制，不是疏漏也不是判斷失誤），而本行判準明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」⇒ **本值依本行自己的判準屬中途值**，下一輪四方複審收斂後必須再釘一次。這與 R74 那次同型（該次亦因額度而未跑複審），逐字記錄以免下輪誤認為新發現。前值 1979 為 R76 值。以下為 R76 沿革：（R76 收尾包重釘，該輪四方複審已全跑並收斂 ⇒ 該值不再是 R74 那種「中途值」）：由收尾包在所有修復包與四方複審 agent 全部停工後，於最終工作樹實跑本 runner、取其印出的計數**直接填入、零加減推算**；量測窗口前後各取一次**含內容**的工作樹指紋並確認相同（`git status --porcelain` ＋ `git diff HEAD` ＋ untracked 檔內容雜湊）。🔴 **方向說明（免得下一輪誤以為這是「調高門檻」）**：本常數是**下限**，往上釘＝**收緊**——它讓「靜默蒸發 N 支測試仍然全綠」的窗口變小，與「不准為了讓數字好看而調高門檻／棘輪」那條禁令方向相反（那條禁的是**放寬**）；真正「只准變小」的是 shrink-only 棘輪那一族（`SPECIAL_FILES` raw-line 上限），兩者不是同一種東西，別混為一談。🔴 **不重釘的代價不只是鑑別力衰減**：本值同時是零相依探針的**鑑別門檻**，一旦低於零相依環境的收集數，探針就不再提前判紅而是實跑整棵樹並撞逾時（`DEF-101-803` 實測事故），所以「維持現狀」在這裡**不是中立選項**。🔴 **仍未機械化的那一半，照實記**：指紋比對只能證明「量測期間沒有人在動樹」，證明不了「之後不會再有一波」——後者由「四方複審已收斂 ＋ 本包是本輪最後一個工作者」承擔，**零機械物看守**（＝`DEF-101-701` 所述結構衝突仍未解）。前值 1819 為 R74 中途值。以下為 R74 沿革：R74 **收尾者於最終工作樹重釘，取 runner 當場印出的計數直接填入、未做任何加減推算**（🔴 **本次重釘的觸發正是本行下方 R69 已逐字預告過的失效模式**：陳舊下限低於零相依環境的收集數 ⇒ 零相依探針不再提前判紅而是實跑整棵樹，兩支測試由秒級轉為撞逾時；本輪實測到的是它的**第二形態**——探針還會遞迴 spawn 孫探針，見 `DEF-101-803`。⇒ 該段預告在本輪應驗，此處逐字回指以免下一輪又當成新發現。🔴 **本值的取得條件有一項未滿足，照實記**：重釘時所有修復包確已停工，但**四方複審本輪未執行**（`DEF-101-801`），而本行判準明訂「真正的解法是把重釘綁到四方複審全數 APPROVE 之後」⇒ **本值依本行自己的判準屬中途值**，下一輪四方複審收斂後必須再釘一次（承接載體＝`DEF-101-801`，輪號寫在帳本那一列、不寫在本行——本行寫死未來輪號會被輪號鎖判紅，本輪實際踩過一次）。前值 1663 為 R71 值）。以下為 R71 沿革：**收官前最後一包停工後重釘（該值＝R71 最終值：四方複審 R1 18 筆→R2 6 筆全數修畢、收官聚焦複審抓出的第三代缺陷 2 筆亦修畢、環境相依紅燈 DEF-101-618(a) 殘留修畢之後才量；量測前後各取一次含內容的工作樹指紋並確認相同）**（🔴 **前綴自己就復發過兩次，逐字記錄**：本前綴在 R69 第二次重釘時仍寫著 R67 的說法、被四方終審 R3 點名為「值是 R69、標籤寫 R67」；訂正後又在第三次重釘時寫成「第三波修復後」而內文已是第四波，被終審 R4 再度點名。**同一行同時裝「值」與「這個值是哪一波量的」兩件會各自變動的事，就是本註記通篇在治的那個病的最小實例**——真正的解是把波次沿革移出本行，本輪未做，見 `DEF-101-701`。**前綴沿革**：本行在 R67 round 3 曾釘 1362 並在此寫「本輪第三次；動工前為 R60 round 3 釘的 1069」，R69 兩度重釘時未同步改寫這個前綴，被四方終審點名為「值是 R69 的、標籤卻寫 R67」的自我矛盾——同一行同時裝兩輪的說法，正是本註記通篇在治的病，故就地訂正）。🔴 **「一輪三釘」本身是本 repo 的結構性缺陷，逐字記錄比修好它更要緊（`DEF-101-701`）**：本輪序列＝1069 → round 1 釘 1318 → round 2 釘 1343 → round 3 本次。1318 那次的取證被四方三向交叉命中為不實（SA-R67-02／SD-R67-01／QA-R67-01：註記寫死的收集面指紋與兩處計數，在最終工作樹一個都複現不出來；同一 commit 內 DEF-101-677 自己記的又是第三個數字，正是 R60 SA-R60-01／ARCH-R60-03 判過的「同一份 repo 對同一個數字兩種說法」原型復發）。1343 那次同樣逐字宣稱「全部修復包停工後」，其後 SC 鎖包（`DEF-101-699`）又淨增測試 ⇒ 同一句話**再度**在一天內轉假。**根因不是粗心，是兩條規則的結構衝突**：重釘判準要「取當下實測」，而本 repo 的收輪是**多波次**（修復包 → 四方複審 → 再一波修復包 → 收尾包），任何一包寫下「已停工」時都只對**當下**為真；round 2 為此加訂的 (a) 仍靠**人自己判斷自己是不是最後一個**，零機械物看守 ⇒ 同型復發已可列舉：R60 留下 756／845／994／1063 四個中途值，R67 留下 1318／1343。**故 R67 round 2 加訂、round 3 沿用並補一小步**：(a) 重釘必須發生在**最後一波**修復收斂之後、由當輪唯一仍在工作的包執行，且該包必須先實查工作樹已停止變動——round 3 的實查手法＝**重釘量測前後各取一次工作樹內容指紋並確認相同**，指紋須**含內容**——`{ git status --porcelain; git diff HEAD; git ls-files -o --exclude-standard -z | xargs -0 shasum; } | shasum`；🔴 **只雜湊 `git status --porcelain` 是不夠的**（本包第一版就是這樣寫的，當場自查抓到）：該輸出只有狀態字母與路徑、**不含內容**，於是「對一支已在清單內的檔再改一次」對它完全不可見，正是本檔多處在治的 fail-open。這是唯一被機械化的一小步；「誰是最後一個工作者」本身仍無判準，未修的那一半見 `DEF-101-701`；(b) 🔴 **本註記內一律不得寫死「當場即可現查」的量測 token**（收集面指紋、`Ran <數字> tests`、`發現 <數字> 個測試`）——本 runner 每次執行都會把當下的值印在終端，寫進註記等於憑空製造一個沒有任何機械物看守的 stale 站點（同 `ADR-XPLAT-002` §8 表頭規則 3「完成判準欄禁止寫死量測常數」）。(b) 已由 `min_tests_note_stale_tokens()` 機械強制：違反即 rc=1，訊息直接指路。本值＝在 R69 收輪 push **成功後、雲端 windows-compat-ci 抓到 WSL bash 劫持（`DEF-101-753`）的修復包**停工後，由收尾者於最終工作樹實跑（🔴 **1588 那次的教訓再進一階**：pre-push 阻斷事件補上了 untracked 盲區之後，push 成功、本機十道閘門全綠、四方四輪複審全過——然後**雲端真 Windows runner** 抓到一支本機結構上不可能重現的紅：裸名 `bash` 在 Windows 上因 `CreateProcess` 把 System32 排在 PATH 之前而必定命中 WSL 啟動器。⇒ 「本機全綠」與「掃描面無盲區」都還不夠，**「只在某平台才成立」的判斷必須有本機可重現該平台語意的回歸路徑**，否則唯一的發現通道就是雲端真機）（🔴 **1581 那次的教訓比前幾次都重要**：當時四方終審 R4 已判 0 筆擋收輪，收尾者據此宣稱「不會再有下一波」這件事**首次有外部憑證**——結果 `git add` 的那一刻就冒出一道真紅。原因不是四方失職，而是那道鎖的掃描面用 `git ls-files`、對 untracked 檔天然隱形，而本輪新增的 `AutoClaude/autoclaude/utils/platform_caps.py` 全程未被 add ⇒ **四輪四方複審與收尾者歷次全套實跑，全都在一個看不見該檔的掃描面上取得「全綠」**。`DEF-101-751`／`DEF-101-752`。⇒ 「不會再有下一波」連外部憑證都還不夠，還要求**該憑證的取得面本身沒有盲區**） `python tools/run_root_unittests.py`，取其印出的計數直接填入，未做任何加減推算；重釘前後各取一次含內容的工作樹指紋並確認相同。🔴 **R69 本身貢獻了兩個中途值 1526 與 1557，逐字記錄**：收尾者在「修復波九包停工後」釘了 1526 並逐字寫下「九包全部停工後」——其後四方複審 R2 判出 12 筆確認發現，觸發第三波修復五包再度淨增測試，該句在同一輪內轉假；收尾者接著在「第三波五包停工後」釘了 1557 並寫下同型句子，其後四方**終審 R3** 又判出兩筆擋收輪 P1，觸發第四波修復四包，**同一句話在同一輪內第二次轉假**。這是 `DEF-101-701` 所述結構衝突的**第三次**實例（R60 留下 756／845／994／1063，R67 留下 1318／1343，R69 留下 1526／1557／1580／1581／1588，R70 留下 1592／1594），根因不變：判準要「取當下實測」，而收輪是**多波次且波數事前未知**（修復波 → 四方複審 → 再修復波 → …），任何一包寫下「已停工」時都只對當下為真。工作樹指紋實查只能證明「量測前後沒人在動」，**證明不了「之後不會再有一波」**——後者要等四方複審判 APPROVE 才成立，故真正的解法是把重釘綁到**四方複審全數 APPROVE 之後**而非「修復包停工後」。🔴 **R69 新增的第三個失效證據（本輪實際事故，比前兩次更嚴重）**：R67 釘的 1362 落後測試樹後，並非只是「鑑別力衰減」這種慢性病——`tools/tests/test_run_root_unittests.py::ZeroDepEnvironmentDiscriminationTest` 的零相依探針以 `run_with_floor` 判紅，下限一旦低於零相依環境的收集數，探針就不再提前判紅而是**實跑整棵樹**，於是那兩支測試由秒級轉為撞 300s `TimeoutExpired` ⇒ **陳舊的下限會讓看守它的那道鎖自己壞掉**（R69 實測：全套耗時由分鐘級膨脹到十二分鐘級並 rc=1）。換言之 `MIN_TESTS` 不只是下限，它同時是零相依探針的**鑑別門檻**，兩個角色共用一個常數＝本檔尚未修掉的耦合，見 `DEF-101-701`。歷史值序列：R15 為 290、R57 為 616、R59 為 661、R60 round 3 為 1069、R67 round 1 為 1318、R67 round 2 為 1343（後兩者皆中途值，見上）、R67 round 3 為 1362、R69 修復波後為 1526、R69 第三波後為 1557、R69 第四波後為 1580、R69 收輪前最後一包後為 1581、R69 push 阻斷修復後為 1588、R70 WSL bash 劫持修復後為 1592、R70 stub 可執行性修復後為 1594（七者皆中途值，見上）；R60 中途曾釘 756、845、994、1063——994 是 round 3 修復波開跑前的值，1063 是 Pkg-F 交件時的值而 Pkg-H 之後又淨增 6 支，兩者都在「還有包在跑」的時點量測，故皆非最終值。R58 整輪作廢故無 R58 值；R57 收尾重釘時，動工前的 R15 值 290 對當時實況 530 已鑑別力失效 45%＝可靜默蒸發 240 支仍綠——與本輪同款腐化，故重釘節奏訂為「每輪收尾必做」。**重釘判準（R57 訂立，R67 round 2 補上 (a)(b)，round 3 補上工作樹指紋實查）**：本值一律由收尾者在**所有並行修復包與四方複審 agent 全部停工後**，於最終工作樹實跑 `python tools/run_root_unittests.py`，取其印出的計數直接填入，不做任何加減推算——R57 過程中兩度用算式推得 552／558，兩次都當場就與實況不符（SD-R57-01／QA-R57-07 抓出），故本行的重釘判準明定為「填實測值」
