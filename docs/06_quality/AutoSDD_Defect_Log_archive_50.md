# AutoSDD Defect Log — Archive 50

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **10 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-757`、`DEF-101-756`、`DEF-101-643`、`DEF-101-734`、`DEF-101-521`、`DEF-101-704`、`DEF-101-541`、`DEF-101-735`、`DEF-101-689`、`DEF-101-663`
>
> **本次操作備註**：R71 入帳前輪替：主檔 249885 bytes 已越過 warn=245760。8 筆 ack 逐列人工複核有承接者或無殘留待辦（521→DEF-101-500③／541→DEF-101-559／663 為改派載體其兩筆主體留主檔／689 建議已於 R68 落地為 --reconcile／704 解鎖條件逐字履行／734→DEF-101-739／735 三支回歸鎖齊備／756 fixed@R70 無殘留），刻意不 ack 的三筆＝DEF-101-554（狀態欄含內嵌管線、未逐字複核）與 DEF-101-652／710（各自帶「改派為：未指派」的真殘留待辦、主檔無獨立承接列，搬走即靜默埋葬——R45 誤歸檔事故判例）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-757 | 2026-08-02 | R70 修 `DEF-101-756` 時回溯「為何這句話能寫進這麼多檔而零告警」，發現**這是同一形態的第三次復發，且防它的鎖射程缺口早已被逐字寫下、處置卻是「劃界」而非「補鎖」** | **(a) 鎖射程缺口被寫成政策**：`ADR-XPLAT-002` §9.1 邊界 (d) 逐字寫「SC-4 只認上列動詞…同義寫法（『零真機』『這台機器』…）**抓不到**」，`DEF-101-643` 結案敘述逐字重複同一句。⇒ R69/R70 得以把「Windows 零真機」寫得橫跨 docs 與 `.py`（`platform_caps.py:20`、`test_evaluator_kill_tree.py:487`、`ADR-XPLAT-003:38,83,200`、`improving_102/103` 檔頭與 §10.5 標題、`AutoSDD_Defect_Log.md`、四支 archive、`CrossPlatform_Scan_Dimensions.md`、`CrossPlatform_R68_Scan_Findings.md`）且**零告警**，並被主控逐字複製進約 30 份 agent 任務書、四方複審提示詞與三個 commit 訊息（`edd5388`／`d2758b5`／`390149a`）。病史：①R67 前 `ADR-XPLAT-002` §6「macOS 零真機」＝`DEF-101-643`（P2，**partial**）；②R67 `CrossPlatform_Scan_Dimensions.md`「macOS 欄至今零真機量測」＝`DEF-101-682`；③本次。**(b) SC-4 掃描面只有兩份 ADR** ⇒ 同一句話換一個檔案就整個逸出。**(c) nightly 執行史零通道**：每日 02:00 的完整回歸跑在 **Windows 11 真機**（Task Scheduler `AutoClaude_Nightly`＋01:00 `AutoClaude_WindowsSmoke`，`tools/install_windows_nightly.ps1`；mac 側 launchd `com.autoclaude.nightly`），是本 repo **最密集的平台真機證據源**，而**沒有任何工具或文件讀過它**——心跳／log 落 `AutoClaude/logs/`、`.gitignore` 排除、14 天輪替；`dev_start` 心跳哨兵只讀**本機平台那一支**、`install_mac_nightly.sh --status` 的缺跑 glob 是 `nightly_mac_*`（連檔名都對不上 Windows 家族）、`sync_onboarding_baselines.py` 全檔零 nightly 提及。**(d) 逐輪覆蓋表停在 R67**：`ADR-XPLAT-002` §6 那張**專為回答「哪一輪在哪個平台跑過」而建**的表，R68~R70 三輪沒有新列——正是出事的三輪 | P1 | 根層 `tools/tests/test_adr_xplat001_c1c2_lock.py`（SC-9）＋ `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §9.1／§6 ＋ `tools/lib/baseline_origin.py` | fixed@R70：(1) **新增 SC-9**（`sc9_no_unscoped_zero_real_machine_claim`）＝SC-4 已知射程缺口的補完。判準核心是**輪次界定**而非「不准提平台」（對齊 §6 邊界 1「平台覆蓋是輪次屬性」）：「本輪無 Windows 真機」合法、「Windows 零真機」違規；三段式條件＝命中詞 ∧ 平台名落在相鄰視窗 ∧ 同行無輪次界定 ∧ 無同行具名豁免 `stale-premise-ok:`。**掃描面擴為**兩份 ADR＋ADR-XPLAT-003＋維度表＋具名治理文件＋帳本家族＋`AutoSDD_improving_*.md`＋`AutoClaude/autoclaude`／`AutoClaude/tests`／`tools` 全部 `*.py`（枚舉全走既有 SSOT／glob）。**射程是實測收斂的，兩個方向都貼過輸出**：只用「否定詞＋真機」＝52 命中且多為規則句與跨標點誤配（噪音鎖）；加上三段式條件後＝9 命中零誤報。**刻意不納入**「這台機器」（實測命中多數為誤報，且 ADR 內該管的已由 SC-4 覆蓋）與「把 SC-4 舊樣式擴到寬掃描面」（實測多出一整批誤報）——誤報的鎖最後一定被加豁免繞過。注入實測雙向：修復前 SC-9 對真實文件逐處點名轉紅（含**本輪自己新寫的 `tools/lib/baseline_origin.py` 與回歸鎖 docstring**，兩者改掛具名豁免）、逐處訂正後 rc=0；`_SC9_INJECT`（逐字取自 `ADR-XPLAT-003:83` 修復前實況）注入 ADR-XPLAT-003 後**只有 SC-9 轉紅、零串音**。(2) **逐處訂正**：活躍宣稱改為輪次界定或指 live 來源；史料／規格樣本逐字保全並掛同行具名豁免。(3) **nightly 進入判定**：`--check-snapshot` 逐欄印該平台 nightly 心跳現況，**兩個平台家族都讀、不看 `sys.platform`**，且訊息逐字劃界「本機看不到只代表本機不是那台機器，**不得**據此推論該平台沒有 nightly 或沒有真機」；`ADR-XPLAT-002` §6 補上 nightly 證據軌說明。(4) **逐輪覆蓋表補列** R9~R19／R20~R59／R68~R70 並註明「逐輪補列是收輪必做項（缺列不會有任何東西轉紅）」。🔴 **入規（本筆最重要的產物）**：**此後本 repo 對「已知的鎖射程缺口」不得只以劃界結案**——要嘛補上判準，要嘛在同一處寫明「為何補不上」與承接輪次；已寫進 §9.1 SC-9 宣告與邊界 (d) 訂正段。殘餘（誠實劃界）：「目前只有一台」「唯一一台」全庫零命中無樣本可驗鑑別力，故未納入 |
| DEF-101-756 | 2026-08-02 | **R70 主控據工具輸出向使用者做出錯誤平台建議，被使用者當場以開發史駁回**（逐字記載：主控讀 `python tools/sync_onboarding_baselines.py --check-snapshot` 印出的「Windows 欄**尚未建立基線**（provenance 四項全為 unrecorded）」，據此在「下一輪該去哪個平台」的建議中宣稱「**Windows 側從未有真機輪**」；使用者回覆「這個專案本來就是在 Windows 開發的，每次都跑相關測試，還有每日凌晨 02:00 的 nightly testing」。**這是本 repo 少見的「文件缺陷直接造成決策錯誤」實例**——不是測試紅、不是 CI 紅，是一份**全綠 rc=0** 的工具輸出把主控導向與事實相反的結論） | **工具用同一個 `unrecorded` 同時表達兩件相反的事**：①「該平台從未量測」②「量測過、但早於 R67 引入的 provenance 機制」，而輸出一律用①的措辭。**自證矛盾**：同一份 rc=0 輸出的下三行就印著 Windows 欄的 `3767 passed / 208 skipped`（`ONBOARDING.md` §7 表②），而該格歸因欄逐字記著「`[WINDOWS-NATIVE-ONLY]` 3 支由 skip 轉 pass、反向新增 2 支 `[WinError 1314]` skip」——**只有在真 Windows 上跑才寫得出來**。史料複驗（`git log` 全史）：R20 逐字「本輪首次在真實 Windows 11 機器（非 mac）上執行本 repo」、R20~R66 為 Windows 真機期（R42「在本機真實 Windows 11 上失敗」、R59「真 Windows 開機執行」、R64／R66 皆有 Windows-only 實測憑證），R9~R19 與 R67~R70 為 macOS 真機期 ⇒ **兩平台都有真機輪**。程式座標：`tools/sync_onboarding_baselines.py::_stale_summary`（修前分岔判準＝`all(prov[f] == unrecorded)`、措辭＝「尚未建立基線」）＋ 其回歸鎖 `test_doc_loc_baseline_freshness_r60.py::test_never_baselined_column_says_so_instead_of_faking_a_drift`（**該鎖把錯誤措辭釘死了**）＋ `ONBOARDING.md` §9 末列（逐字「`snapshot-fingerprints-win32` 錨整欄 `unrecorded` ⇒ 今日缺的是 Windows 真機量測」——同一列另有一句「其 rc 反映的是指紋有無漂移，不是有沒有真機量測過，兩者別混」，**同列自我矛盾**） | P1 | 根層 `tools/sync_onboarding_baselines.py` ＋ `tools/lib/baseline_origin.py`（新）＋ `ONBOARDING.md` §7／§9 ＋ `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §6 | fixed@R70：(1) **語意三態分離**——`baseline-origin` 升為必備 provenance 欄位（缺席即 fail-loud），三值 `self-recorded`／`pre-provenance-mechanism`／`never-measured`；判準**不新增人手旗標**，「有沒有量過」由「表② 該欄有沒有數字」機械決定（`column_has_measurements()`），`baseline-origin` 只答「哪一種來源」並與前者**交叉驗證**（`baseline_state()`）⇒ **宣告「從未量測」而表上有數字在結構上 fail-loud**，那正是本次那句錯話。(2) **回溯補齊（不捏造）**：win32 錨依 git 史料填 `baseline-origin=pre-provenance-mechanism`，四個 env 欄位**維持 `unrecorded`**（混世代，量測環境真的不可考——**刻意不猜一個日期讓工具閉嘴**）。(3) **輸出訂正**：`--check-snapshot` 逐欄先印三態人話再印原始 dict，`pre-provenance` 態的訊息**自己說出**「這是 provenance 缺口、不是平台覆蓋缺口，不得據本行宣稱該平台從未驗證過」並指路權威來源；末尾另加「**本工具不是平台覆蓋的權威**」。(4) 回歸鎖改鎖**意圖**：`test_pre_mechanism_column_is_never_described_as_never_measured`（禁止「尚未建立基線／從未量測／沒量過」三種措辭出現在有數字的欄）＋ `test_a_truly_never_measured_column_still_says_so`（對照組，防退化成「永遠不准說沒量過」）＋ `test_declaring_never_measured_while_the_table_shows_numbers_fails_loud`。**教訓入規**：一則會被拿去做平台決策的訊息，不得在只知道「provenance 沒記」時說出「沒量過」——**缺 provenance ≠ 缺量測**，兩者處置相反（前者補記錄、後者要真的去跑一輪） |
| DEF-101-643 | 2026-08-01 | R67 Scan-D ＋ Scan-E（DOCADR 包；macOS 真機 checkout 後逐項複驗） | **ADR-XPLAT-002 §6 邊界 1 把撰寫當下那台機器寫成永久事實**（原文逐字「❌ macOS 零真機。本機是 Windows 11 / PowerShell 5.1（無 pwsh 7）/ Git Bash」，`docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:757-761`），R67 於 macOS 真機 checkout 後三項全不成立，而所有以它為前提的排期決策（尤其 §5 Phase 3「不排期」）跟著失效卻零機械訊號。同型硬編平台前提另散落 4 處：`docs/06_quality/CrossPlatform_Scan_Dimensions.md:45`、`tools/sync_onboarding_baselines.py:513`、`tools/tests/test_doc_loc_baseline_freshness_r60.py:903`、AISDLC_SDD ADR-001:24 | P2（原 P1，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §6 邊界 1 ＋ §9.1 SC-4 | fixed@R70（原 partial@R67，R70 依 `DEF-101-757` 補完 SC-4 射程後收斂）：ADR-XPLAT-002 那一處已改寫為平台中立的逐輪覆蓋表＋現查指令 `uname -sr`，並落 §9.1 SC-4 不變式（注入硬編前提 rc=0 轉紅／還原 rc=1 回綠，zsh -f 與 bash 3.2.57 雙側實測）。**未修 4 處**：其中 `sync_onboarding_baselines.py:513` 與 `test_doc_loc_baseline_freshness_r60.py:903` 經覆核為 `_normalize_eol` 的 🔴 WHY docstring（陳述未正規化版本的反事實後果），判定**不應改**；`CrossPlatform_Scan_Dimensions.md:45` 與 AISDLC_SDD ADR-001:24 不在該包授權面。殘餘：SC-4 只認 `本機(是\|沒有\|為)` 句式，同義寫法（「零真機」「這台機器」）抓不到，已於 §9.1 邊界 (d) 劃界 🔴 **R70 重新評估並收斂為 `fixed@R70`（`DEF-101-757`）**：本列的「殘餘：SC-4 只認 `本機(是\|沒有\|為)` 句式，同義寫法（「零真機」「這台機器」）抓不到，已於 §9.1 邊界 (d) 劃界」**這句結案敘述本身，就是本次事故的根因之一**——它把一個**已知的鎖射程缺口**寫成了可接受的政策，於是同一句錯話得以在 R69/R70 橫跨 docs 與 `.py` 寫下而零告警，主控再據以向使用者做出錯誤平台建議（`DEF-101-756`）。R70 已補上 **SC-9**（判準＝無輪次界定的「某平台零／無／從未真機」宣稱，掃描面擴至帳本家族＋治理文件＋improving 系列＋三樹 `*.py`，注入實測雙向、零誤報），該殘餘不再是劃界。**仍未補的殘餘（誠實逐條）**：①「這台機器」——實測命中多數為誤報，補上即成噪音鎖，故**明文不補**並在 §9.1 邊界 (d) 具名記載；②「目前只有一台」「唯一一台」——全庫零命中，無樣本可驗鑑別力；③ SC-5 的「配額／服務中斷」同義詞與 SC-2 的非粗體形態，屬其他條的射程，未動。**此後對「已知的鎖射程缺口」不得只以劃界結案**（已入 §9.1） |
| DEF-101-734 | 2026-08-02 | R69 終審 R3（Architect／QA 各自對帳本主檔實跑 `wc -c` 與 `archive_defect_log.py --plan`） | 🔴 **P1 結構性死鎖：帳本主檔距 262,144 bytes 硬線僅約 250 bytes，而 `--plan` 可搬 0 筆 ⇒ 下一個「加一列」動作即 rc=1、且無機械解法**。逐條查明「為何 0 筆」：95 筆真為未結（`open`／`routed`，依政策本就留主檔）、8 筆已結但被判準④ 攔下待具名承認 ⇒ **判準沒有過嚴，是主檔裡有一段永遠搬不走的東西**。根因＝**歸檔索引段（`## 歷史複驗註記` 全段）是單調增長且無法被歸檔回收的一段**：每跑一次 `--apply` 就往主檔多寫一條約 0.9KB 的 bullet，近幾輪每輪建 3~5 支 archive ⇒ 每輪 3~5KB 只增不減，而缺陷列還能靠結案＋歸檔釋出。把單調增長項放進有硬上限的檔，數學上保證撞牆——歷輪「一輪歸檔兩三次」只是在買下一輪的餘裕 | P1 | 根層護欄層（`tools/archive_defect_log.py`／`tools/check_defect_log_crossref.py`／`tools/lib/defect_ledger_index.py`） | fixed@R69：**職責重新切分**——索引段逐字外移至 `docs/06_quality/AutoSDD_Defect_Log_archive_INDEX.md`（主檔留指針 stanza）。⚠️ **不是**調高 262144 硬線（該線＝Read 工具實測上限，調高等於砸溫度計），也**未**放鬆 crossref 任何檢查。檔名刻意落在既有 `AutoSDD_Defect_Log_archive_*.md` glob 內 ⇒ 帳本家族／判準④⑥ 指針稽核／體積守門／兩支 compat-CI `paths:`／測試沙箱複製面**五道零改動即涵蓋**（若改名成 glob 外的形態，五道會同時靜默漏掉——即 `DEF-101-587` 的形狀）。判準⑤ 的 bullet 解析點與 `--apply` 的寫入點改指索引檔，索引檔本身以**檔名常數**具名排除（非「零表格列就跳過」的模糊判準）；主檔守恆式因此回到最嚴的「新主檔 + 釋出 == 舊主檔」（`added=0`），索引檔側另立同型守恆式。實測：主檔 261,894 → 217,655 bytes（釋出 44,239）；`--check` rc=0（48 bullet 對 48 archive）、`check_defect_log_crossref.py` rc=0。回歸鎖 `tools/tests/test_archive_defect_log.py::TestArchiveIndexDocIsExternalized`（3 支：家族成員身分／具名排除非靠空表／主檔零殘留 bullet ＋ `apply()` 寫入落點）。🔴 **本輪淨效果誠實話**：外移只移除了**單調增長項**，缺陷列本身的增長沒有變——本輪入帳後主檔仍會回到 230KB 級，下一輪若再加 20 列仍會進 240KB warn 帶。真正的下一步是壓縮**每列體積**（R60 已定的「帳本列只寫摘要、證據落 Evidence 檔」兩層化政策至今無機械守門），見 `DEF-101-739` 同族的容量政策討論 |
| DEF-101-521 | 2026-07-28 | R59 四方一審 SA-R59-02（Scan-G 維度的首例，且該維度即因本筆而立） | **R58 整輪作廢後，四處 `deferred` 指向一個不存在的輪次，成為無主孤兒**：`DEF-101-500`③⑤ 寫「列 R58 backlog」「R58 收斂時一併納入」「R58 若仍無消費端則刪除」、`DEF-101-496` B3 同款，另 `docs/06_quality/CrossPlatform_Scan_Dimensions.md`（1 處）與 `tools/tests/_platform_helpers.py`（**3 處**——L10／L127／L251）這兩份**活文件／生產測試碼**內也有。**R59 二審 SA-R59-P2-2 訂正**：本列初稿寫「各有一處」，普查不完整；L127／L251 兩處是二審 SA 以 `grep -rn "R58" --include=*.py` 揪出的，**修「孤兒 backlog」的人自己在同一個檔案裡漏掉兩處孤兒**——與 SA 二審點出的整體形狀（修某類缺陷的人站在那類缺陷的盲區裡）同型，如實留痕。三處均已改派。R58 已由使用者判定失控作廢（`reset --hard 75aab89`），指向它等於沒有承接者，而下一輪讀到會以為有人在做。**本輪還讓同一家族的複本數靜默 +1**：DEF-101-519 的新前瞻鎖複製了第 5 份 `_FROZEN_SDD_VERSION_RE`（該家族正是 DEF-101-500③ 登記的缺陷——`\d+\.\d+` 抓不到三段版號時N 份會同時靜默誤分類），而帳本原本沒記這件事。**結構性根因（同輪一併處理）**：既有五維掃描的 Scan-D 管「基線新鮮度／歸檔／crossref 一致」，**不管「上一輪 deferred 是否有承接者」**，故這類孤兒可以安然存活 | P2（上一輪已登記未結案的項目失去承接者；與使用者要求「所有問題必須徹底全部修復」直接衝突） | 本輪修復（四處改派 + 新鎖就地標註 + 新增 Scan-G 維度） | fixed@R59：① 兩份活文件／生產碼的「列 R58 backlog」改寫為「列 backlog——R59 改派為 R60 起**未指派** backlog」並註明 R58 作廢的事實；② `test_windows_forbidden_filename_parity.py` 的第 5 份複本就地加註（點名另四份位置、點名它屬 DEF-101-500③ 家族、說明本輪刻意複製而非 import 的理由——`tools/tests/` 無 `__init__.py`，`-m unittest <module>` 與 discover 兩種模式模組名不同，**主控實跑 `-m unittest tools.tests.test_dev_start` 當場撞到 `ModuleNotFoundError: No module named '_platform_helpers'`，坐實此限制為真**——並明寫「收斂時五份應一併處理」；③ **新增 Scan-G（backlog 接續稽核）維度**至 `CrossPlatform_Scan_Dimensions.md`，並在 §使用方式 訂下硬規則「任何 deferred／backlog 必須指向一個存在的輪次或明確標為未指派，且解鎖條件要具體到下一輪能直接執行」 |
| DEF-101-704 | 2026-08-02 | R68 Scan-F（macOS 26.5.2 真機實跑） | **P1**：`tools/dev_start.py:51` 頂層 `import tomllib`（3.11+ 才有），而薄殼 `tools/dev_start.sh:42-52` 的直譯器候選無版本閘、無 `.venv` 時退回系統 `python3`——macOS 系統 python3 為 3.9 ⇒ `ONBOARDING.md` §2.1 明文承諾的「全新機器一鍵開工」在 mac 上必炸。同包另一筆：第⑤步自動裝 git hooks 在 macOS 預設情境 100% 失敗（子行程 PATH 無 `.venv/bin`，mac 又無裸 `python`）而 `dev_start` 仍 rc=0＝靜默失敗 | P1 | 根層 `tools/dev_start.{py,sh}` ＋ `tools/lib/git_hooks_install_common.sh` | fixed@R69（🔴 R69 P3 批次訂正首詞：原首詞為 `partial@R68`，而同欄後段早已載明 `→ **fixed@R69 P2**`；`_classify()` 取最早關鍵字 ⇒ 本列被計入未結存量，`check_defect_log_crossref.py` 的「狀態首詞待更新」warning 逐字指名本列。體例同 `DEF-101-433`：首詞改為已結 token、原文逐字保留於後，語意零變更。**本輪另行實測複核**版本閘確已落地且行為正確：`/usr/bin/python3 tools/dev_start.py --help` → rc=**2** ＋「❌ dev_start 需要 Python >= 3.11，當前為 3.9.6」；`env -i PATH=/usr/bin:/bin bash tools/dev_start.sh --help` → rc=**0**（薄殼經 `pick_python_ge_min` 的絕對路徑候選撿到 3.11）。逐字保全原首詞段落如下）：`partial@R68：hooks 那半已修（把本行程直譯器所在目錄前置進子行程 PATH，兩平台同款）並經真機驗證；`_safe_is_file()` 兜底補上（既有鎖 `TestStepHooksIsFileOSError` 攔下裸 `.is_file()`）。🔴 **tomllib 版本閘未落地**，改派為：未指派。解鎖條件＝以 `/usr/bin/python3`（3.9）直呼 `tools/dev_start.sh` 實跑重現、修復後同法複驗，兩次 rc 皆記錄於當輪帳本列` → **fixed@R69 P2**（解鎖條件已逐字履行）：**修前**（HEAD=375f291，`env -i PATH=/opt/homebrew/bin:/usr/bin:/bin bash tools/dev_start.sh`，機上 `python3.11 → /opt/homebrew/bin/python3.11` 明明可用）rc=**2** ＋「dev_start 需要 Python >= 3.11，當前為 3.9.6」；**修後**同指令 rc=**0**（`--help` 走完核心）。修法＝候選鏈上移 SSOT：`tools/lib/windowsapps_guard.sh::pick_python_ge_min`（bash）／`tools/lib/WindowsAppsGuard.ps1::Get-PythonGeMin`（PowerShell），兩側同一套候選順序（3.11→3.12→3.13→python3→python，bash 另含 Homebrew／pyenv 絕對路徑落點；Windows 含 `py -3.x`）與**同一段**版本探測碼，找不到才 fail-loud 並印逐字可執行的補救指令。R68 那次只把 traceback 換成友善訊息、未動選擇邏輯，故本列到 R69 才真的關閉 |
| DEF-101-541 | 2026-07-28 | R60 Scan-C C-03（掃描者提出、反駁者收窄範圍並訂正兩處失實：承接者其實部分存在＝DEF-101-490，且 md5 分群是本機 CRLF 假象） | **`tools/check_gha_action_versions.py` 檔頭的到期風險揭露自陳「需…記入缺陷帳本」，而帳本零記載 ⇒ 承接者不存在（懸空的孤兒揭露）**。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P4（採納反駁者降級：暴露面嚴格小於已結案的 DEF-101-434〔那是根層真會跑的 CI job 仍只給 P3〕——這 30 份在 GitHub 永不觸發〔巢狀路徑〕、v0.01~v0.29 依 Copy-on-Evolve 禁改、v0.30 檔頭自述是「給尚未存在的 Hub Registry repo 的 sample」。掃描者自陳本筆與 Mac/Windows 相容性無關、應標排除範圍外，評級跟著它走） | 本輪修復「懸空自陳＋無承接者」這一半（Pkg-4 Scan-C）；**LATEST 升版決策本身 routed → AISDLC_SDD 凍結/LATEST 政策擁有者**（見狀態欄的解鎖條件與期限）。🔴 **R60 round 2（round 1 SA-R60-08）**：「政策擁有者」是角色不是輪次，違反 `CrossPlatform_Scan_Dimensions.md:149` R59 硬規則②（backlog 必須指向存在的輪次或明確標「未指派」）→ 該半邊已拆為 `DEF-101-559`，承接輪次明標 **未指派** | fixed@R60（僅涵蓋「自陳不再懸空」這一半；升版決策半邊已拆出 `DEF-101-559` 承接）：🔴 **R60 round 2 訂正（round 1 SA-R60-08）**——本欄原首詞為 `partially-fixed@R60（自陳不再懸空）／routed（升版決策）`，兩者皆非本帳本《格式定義》（本檔 :16-17）的合法值；且 `partially-fixed` 的 `fixed` 前綴是 `-`（非 ASCII 英數邊界）⇒ `_classify` 仍命中 `fixed`，把 `routed` 那半邊的活躍語意**靜默吞掉**（閘門印「零含糊」為真，但語意與分類不一致）。訂正手法＝一列一狀態：本列收斂為單一 `fixed@R60`，LATEST 升版決策另立 `DEF-101-559`（`routed`，承接輪次：未指派）。① `check_gha_action_versions.py`〈掃描面邊界〉⚠️ 段改寫為完整分流結論：凍結版依 Copy-on-Evolve 不動；**LATEST 是否升版刻意不由 CI 工具鏈側代決**（該檔自述是 sample、要不要升屬框架散佈品質命題，且升 LATEST 會讓「各版此檔為同一 git blob」這個目前可機械核對的不變量首次分裂，代價需由該側評估）；依 R57 政策把寫死的「30 份」改為執行期實查後印出；另補「複核者陷阱」段記載 md5 假象與正確載具。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-735 | 2026-08-02 | R69 終審 R3（P2 #17；SD 覆核 ADR 與帳本一致性） | **ADR 對缺陷狀態的宣稱與帳本直接互斥，且機械上完全盲**：`ADR-XPLAT-003` 表頭〈關係〉欄與 `ADR-XPLAT-002` §1 各有一處寫「`DEF-101-706` 隨之結案」，而同輪帳本該列狀態欄是 `partial`（同欄明寫「解鎖條件①未達標故不結案」）。兩層缺口：(a) `docs/04_planning/ADR/` **不在** `check_defect_log_crossref.py` 的 `_CROSSREF_TARGETS` 內；(b) 即使納入也看不見——`_scan_target()` 的 `_CLAIM_RE` 只認「DEF-ID 緊接**括號**」這一種宣稱形態，而 ADR 的宣稱長在**散文**裡。**納入掃描面 ≠ 看得見**，這是本筆的重點 | P2 | 根層護欄層（`tools/check_defect_log_crossref.py`／`tools/lib/defect_ledger_index.py`） | fixed@R69：(a) `_CROSSREF_TARGETS` 以 **glob**（`docs/04_planning/ADR/ADR-*.md`）納入根層 ADR——刻意不逐支具名，具名清單必漏掉下一支新 ADR 而漏掉零訊號（本輪就新增了 XPLAT-003）；誠實劃界＝只掃根層，子專案 ADR 不在內。(b) 新增純函式 `closure_claim_problems()`：同行、往前 30 字內的最近 DEF-ID ＋「結案」術語，帳本判為未結即 rc=1；否定形態（`不／未／待／可／難／能／該／就／否` 緊接）與超出視窗者刻意不報。只認「結案」二字、不認「已修復／已解決」等近義詞——近義詞在活文件裡大量描述**動作**而非狀態宣稱，擴進來即噪音淹沒訊號。**紅綠實測**：接上後 rc=1、逐字指名 `ADR-XPLAT-002-platform-surface-reduction.md:32`（唯一命中，零誤報）；兩處 ADR 依「以帳本為準」改寫為「收斂標的落地、該筆不結案」後 rc=0。回歸鎖 `tools/tests/test_check_defect_log_crossref.py::TestAdrClosureClaimsAreMechanicallyChecked`（3 支：掃描面涵蓋／注入必紅且已結時放行／否定與遠距不誤報） |
| DEF-101-689 | 2026-08-01 | R67 round 2 四方複審（SA-R67-08 收輪流程；LOCK 包自查） | **修復包自報 status 與帳本狀態欄不一致，本輪是靠人工比對才發現**：`DEF-101-664` 狀態欄寫 `partial@R67`（自陳兩支同病棘輪不在該包授權面、改派未指派），而 round 2 LOCK 包已全數修畢，兩邊說法在收輪當下並存 | P3 | 收輪流程（下一輪加一道機械對帳） | fixed@R67R2：本輪已就地訂正 `DEF-101-664` 狀態欄為 `fixed@R67R2` 並附三項取證（(a) `TestShrinkOnlyRatchet` 基準由 `git show HEAD:<本檔>` 改為簽入的 `_FROZEN_MAX_BASELINE_ENTRIES`／`_FROZEN_BASELINE_ID_CEILING`；(b) `TestGuardFileCountShrinkOnlyRatchet` 由 `git ls-tree -r HEAD` 改為 `_FROZEN_GUARD_FILE_COUNT`；(c) 新增結構鎖 `test_ratchet_is_independent_of_git_state`，禁用 subprocess）。**建議**：收輪流程加一道「修復包自報 status ↔ 帳本狀態欄」機械對帳，本輪僅人工完成🔴 **R68 落地（Pkg-8）**：本列建議的「修復包自報 status ↔ 帳本狀態欄機械對帳」已落地為 `tools/check_defect_log_crossref.py --reconcile <報表.json>`（純函式 `reconcile_problems()`＋rc=1 硬閘；查找面＝帳本家族主檔 ∪ archive）。收輪用法：各修復包交件時附 `{DEF-ID: 自報狀態}` JSON，主控入帳後跑一次即得集合差。 |
| DEF-101-663 | 2026-08-01 | R67 Scan-G（帳本收尾包；`tools/check_defect_log_crossref.py` 硬閘上線後的第一批真實命中） | **`DEF-101-610`（帳本 :133）與 `DEF-101-596`（:132）是活孤兒**：兩列皆明文指派 R61 承接、R61 未結案，R62 之後五輪無任何改派；R62 對 610 只補了交叉引用並明示「不變更其 open 狀態」。本輪 R67 新落地的硬規則② 機械鎖上線後立即以 rc=1 具名攔下這兩列，證明該鎖非橡皮圖章 | P2 | 根層 `docs/06_quality/AutoSDD_Defect_Log.md`（帳本收尾包） | fixed@R67：依硬規則② 合法出口①，於兩列**狀態欄末尾就地追加**改派附記（原文逐字保留、零改寫），改派為：未指派（各附具體解鎖條件）；本列即該兩筆改派的具名載體。落地後 `python tools/check_defect_log_crossref.py` 由 rc=1（2 筆孤兒）轉 rc=0 |
