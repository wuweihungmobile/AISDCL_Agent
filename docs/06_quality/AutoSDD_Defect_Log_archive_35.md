# AutoSDD Defect Log — Archive 35

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **3 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 5 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-615`、`DEF-101-573`、`DEF-101-614`
>
> **本次操作備註**：R63 動工前歸檔（缺陷帳本容量警戒：237,068 bytes，距 WARN 8.7KB）；DEF-101-573 最終狀態為 fixed@R61(closed-by-verification)，其「下輪」字樣為敘事引述歷史候選成因（已排除），非真實未結交棒，經讀原文核實後具名承認
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-615 | 2026-07-30 | R62 Architect（承接 ADR-XPLAT-002 §8 交棒表項目 3／5，補齊 R61 遺留驗證缺口＋複核治理文件現況） | **兩項發現，皆屬治理文件正確性而非現存程式缺陷**：①ADR-XPLAT-002 §5 Phase 2-E／§8 交棒表項目 5 誤將 `check_wrapper_thinness._normalize` 的 BOM 缺陷列為「R62 待辦」——親讀 `tools/check_wrapper_thinness.py` 現況＋`git log -S"utf-8-sig"` 證實該修復已由 R60 round 3 平行修復包（commit `796c7a6`，該 commit 引入的程式碼註解／docstring 自述「P10-1」，非 commit message 本身——`git log -1 --format=%B 796c7a6` 逐字核對訊息全文不含「P10-1」）落地——**非「Phase 2-E 先修好、ADR 後定案時漏追」這種乾淨先後順序**：`git log --follow --diff-filter=A` 顯示本 ADR 檔案本身也是首次入庫於同一 commit `796c7a6`（同輪 checkpoint 一次彙整多個並行修復包），是同輪內兩個互不知情的並行產出、彼此未同步，而非時間上一先一後：`_read_source()` 已用 `utf-8-sig`、5 支 `.ps1` hash 已重釘、且既有 `tools/tests/test_check_wrapper_thinness.py:382-504` 的 `TestBomIsNotContent` 五項回歸鎖（含首行非 BOM／hash 不因 BOM 而異／讀檔口單一入口 AST 鎖／反恆真檢查）已完整存在——本 ADR 撰寫時未追蹤到同輪其他平行包的產出，屬過期宣稱（Scan-D／Scan-E 型缺陷，非程式缺陷）。②R61 證據文件 `CrossPlatform_R61_Architect_Evidence.md` 就 Phase 1-B 明文記載「兩平台 smoke 的 install/uninstall 往返…**未跑**（本輪只驗證登記層／hash 層，未起 windows_smoke_local.ps1 真實安裝）」——此為 R61 誠實自陳的驗證缺口，非新發現的缺陷，本列記錄其於 R62 補齊 | P3（①治理文件正確性；②驗證覆蓋率補強，皆非阻擋現存閘門的程式缺陷） | 無需分流，本輪（R62）當場訂正與驗證 | **fixed@R62**：①已改寫 ADR-XPLAT-002 §1 狀態列、§5 Phase 2-E 列、§8 交棒表項目 5，訂正為「已於 R60 round 3 結案」並補上正確 commit／行號依據；R62 複驗 `python tools/check_wrapper_thinness.py` → `✅ wrapper 薄殼守門通過（14 支殼 hash 釘選 + 行數上限皆正常）` rc=0，`python -m pytest tools/tests/test_check_wrapper_thinness.py -q -k "Bom or bom"` → `5 passed, 27 deselected, 2 subtests passed`。②以原生 PowerShell（`$env:MSYSTEM` 為空，未觸發 DEF-101-511 拒跑守門）真跑 `tools/windows_smoke_local.ps1`：`===== 彙總：PASS=12 FAIL=0 =====`，涵蓋 install_git_hooks.ps1／install-hooks.ps1 安裝解除往返 [2][4a] 與三種 linked-worktree 拒絕情境 [3][4b][7]，rc=0。**回歸複驗（Scan-F，本輪零生產邏輯變更；唯一程式碼異動＝`tools/check_defect_log_crossref.py` 新增 3 行純登記表項目，登記本檔進 `_GOVERNANCE_DOCS`，不改動任何判準/邏輯分支）**：`tools/tests/` 1065 passed/10 skipped/**368** subtests（較 R61 基線「367」多 1；`git stash` 前後對照〔A/B 測試〕逐檔二分定位，證實該 +1 由本列自身新增造成——`DEF-101-615` 狀態 `fixed@R62`、未被其他列宣告承接，落入 `archive_defect_log.plan()` 的可搬遷集合，使 `test_archive_defect_log.py::TestPlanNeverProposesActiveRows::test_movable_rows_are_all_closed_and_unclaimed` 內 `for v in p["movable"]: with self.subTest(...)` 迴圈多跑一輪，非環境雜訊，詳見 `CrossPlatform_R62_Architect_Evidence.md` §3）；`AutoClaude/tests` 3767 passed/208 skipped（與 R61 基線逐字相同）；`tools/run_root_unittests.py` rc=0（既有 POSIX-only skip 清單不變）；`AISDLC_SDD/scripts/ci-gate.sh` 三軌 1478／1747／249 全 rc=0（與 R60 round 3 基線逐字相同）；`lint-imports` 8 kept/0 broken；`check_loc_budget` total=20361/cap=20438/violations=0；`check_ntfs_paths.py` 27469 路徑/0 違規；`check_defect_log_crossref.py`／`sync_onboarding_baselines.py --check` 皆 rc=0。**全專案 Scan-A~H 複掃：零新缺陷**（Scan-A：`subprocess.*bash` 硬編、`os.system(`、`/tmp/` 字面量三類 grep 掃描，除既有測試 fixture／spec 常數外無新命中；Scan-B：WindowsApps guard／sanitizer 家族測試隨全套通過；Scan-C：CI workflow runs-on 計數不對稱為既有已知劃界〔ADR §6 邊界 6〕非新發現；Scan-D：crossref／baseline 皆 rc=0，帳本體積本輪淨增本列後仍在硬閘內，見帳本末行體積記錄）。Phase 1-C 全量（(a)(b)(d)）評估後仍判斷延後，理由與具體解除判準見 `ADR-XPLAT-002` §5 Phase 1-C 列與 §8 項目 4（承接者改派 R63）。詳見 `docs/06_quality/CrossPlatform_R62_Architect_Evidence.md` |
| DEF-101-573 | 2026-07-29 | R60 r2 QA 複審（`QA2-R60-04`）——QA 第一次全跑即中招 | **機器全域具名 mutex ⇒ 並行必假紅**：`AutoClaude/tests/tools/test_run_local_nightly_static.py` 的 `_TEST_MUTEX_NAME` 是**機器級命名空間 ＋ 固定名**，而其中一支測試持有它 6 秒 ⇒ 任何同時進行的第二個 pytest 行程跑到「鎖是空的」那支必然假紅（QA 實測 rc=1，隔離與全套重跑皆 rc=0）。**非本輪引入**（最新 commit 為 R44 `68c159d`），但本輪「四方各自重跑全套」的協定與此設計**結構性衝突** | P2（測試設計缺陷；症狀 flaky，成因確定性） | AutoClaude 側 `tests/tools/` | fixed@R60：根因＝鎖名不含行程身分。改帶 `os.getpid()` + `uuid4().hex[:8]` 後綴＋靜態斷言「不得回退為固定名」（實查 4 條斷言）。🔴 **並行跑全套的假紅成因累積為三個**：①`__pycache__` 位元碼寫入競態（`DEF-101-268`）②就地突變 tracked 生產碼互踩 ③**本列 mutex**——下輪見「另一個 nightly 行程持有去重鎖」應立刻歸因。🔴 **R60 r3 訂正（`QA-R60R3-01`／`ARCH-R60R3-05`）**：本列原記為第四個候選成因的那一半——**測試互踩**——已定位並根治（樹內固定共用暫存目錄，見 `DEF-101-595`）；**另一半「收集數少 12 支」是獨立問題，本筆不涵蓋、狀態不變，仍交 R61**。原文如下——🔴 **Pkg-P5 另發現第四個候選成因、未修**：與 6 個 python 行程並行時 `run_root_unittests.py` 只收集 **894** 支且 `TestMain` 4 支翻紅，獨占重跑皆 **906** 支 rc=0 ⇒ 假紅；**收集數少 12 支三成因皆無法解釋**，交 R61。見證據檔 `## DEF-101-573` 節。🔴 **R61 訂正（本輪 SA/QA 親自複驗，非引用歷史宣稱）**：「收集數少 12 支」**排除為活躍缺陷**——本輪以具鑑別力載具重現原始情境「6 個 python 行程並行跑 `run_root_unittests.py`」：先清空 `tools/`／`AutoClaude/` 全部 `__pycache__`（排除位元碼快取殘留變因），6 份完整 `python tools/run_root_unittests.py` 同時起跑，**逐份皆收集 1075 支／指紋 `7c1aaffef133`／`OK (skipped=10)`，零差異**；另以輕量探針（`discover_suite`+`inventory_fingerprint`，跳過實際執行）分別在「8 行程並行、無額外負載」與「8 行程並行＋`AutoClaude` 全套 pytest（3767 passed）同時起跑」兩種情境各再取 8 筆樣本，合計 22 筆樣本、指紋與收集數**逐筆相同、零違反**——與 R60 round 3 checkpoint（`796c7a6`）自身做過的 115 次量測（並行度 0~8、零違反）結論一致：collection 機制本身**不是**非決定性來源。原始「894/906/916」的成因是**同一份磁碟狀態在不同時間點被量到**（另一個並行修復包正在改寫 `test_check_defect_log_crossref.py`，該檔測試數由 29→41→51 逐步擴充），不是 race。獨立核對「`TestMain` 4 支翻紅」那一半：該檔 `_ledger_text()` fixture docstring（L46-51）已載明成因——R60 為 `status_first_word_problems()` 新增「必須抽到權威散文」的 fail-loud 判準時，4 支 `TestMain` 舊 fixture 尚未跟上而觸發**與並行完全無關**的 fail-loud；現況 `python -m unittest tools.tests.test_check_defect_log_crossref.TestMain -v` 實測 **11/11 ok**。**結論：非本輪待修的程式缺陷，是量測方法論缺口（跨時間點比較未跨磁碟狀態校準）疊加一次性 fixture 落後，兩者皆已在 R60 收輪前消失**。🔴 **主控裁示（R61 收輪）**：round 1 QA 複審**獨立**另跑 2 輪×3 行程（共 6 份樣本），逐份仍為 1075／`7c1aaffef133`／`OK(skipped=10)`，與 SA/QA 的 22 筆樣本合計 **28 筆樣本、零反例**，兩個獨立單位交叉驗證一致 ⇒ **狀態改判 fixed@R61（closed-by-verification）**：非本輪程式缺陷，量測方法論缺口已消失，無需排入下一輪 |
| DEF-101-614 | 2026-07-30 | R61 Architect（本輪架構優化任務，承接 `DEF-101-561`／ADR-XPLAT-002 Phase 1-B） | `AutoClaude/tools/install_git_hooks`／`AISDLC_SDD/scripts/install-hooks` 兩對腳本自 R11（DEF-101-088）起掛在 `check_script_parity._EXEMPT_PAIRS`（零守門的決策豁免），業務邏輯雖已下沉 `tools/git_hooks_install_common.py` 單一真相源，殼層本身卻無任何機械鎖防止日後長回業務邏輯——ADR-XPLAT-002 §4.1 定義的 UEP（未受檢等價平面）因此恆為 8，R59→R60 前後零下降 | P3（架構級收斂，非現存缺陷） | `tools/check_script_parity.py`、`tools/check_wrapper_thinness.py` | **fixed@R61**：四支殼 raw 行數（50/65/40/42）皆 ≤ `check_wrapper_thinness.MAX_LINES=100`，符合 ADR §3.1 納編前置條件；兩對由 `_EXEMPT_PAIRS` 遷入 `_THINNESS_ENROLLED`＋`_PINNED_SHA256` 補 4 支 hash＋`_FORBIDDEN` 補 4 組並聯關鍵字（沿用既有薄殼同款清單，非新判準）。實測：`python tools/check_wrapper_thinness.py`／`python tools/check_script_parity.py` 皆 rc=0，交叉鎖印「7 對薄殼登記與 14 支 hash 釘選鍵集合一致」；UEP 8→**6**、AC 42→**46**（`_EXEMPT_PAIRS` −2／`_THINNESS_ENROLLED` +2／`_PINNED_SHA256` +4，逐項對應 ADR §4.2 rule 2）。bug-injection dominance test：在 `install-hooks.ps1` 注入 `foreach (...)` 迴圈，hash 釘選與並聯關鍵字兩訊號皆正確轉紅（rc=1），revert 後 `git diff` 確認四支腳本零位元變動、rc 復綠。零新增 `tools/tests/test_*.py`（`tools/tests/` 仍 56 支，但行數 28,118→**28,194**（+76，來自擴充既有測試檔 `test_check_script_parity.py`／`test_check_wrapper_thinness.py`，round 1 SA/Architect 複審訂正原「不變」誤寫），改動全落在 `tools/` 下兩支既有登記表檔＋上述兩支既有測試檔，零新檔）。另附加 Phase 1-C 最小可行切片：`check_script_parity.py` 新增 `--print-collapse`（純附加 CLI 分支，不改動既有 `_EXEMPT_PAIRS`／`_SINGLE_SIDED_EXEMPT` 的字串型別，故不需連動修改 `test_onboarding_parity_interlock.py`／`test_schedule_capability_parity.py` 等既有消費端），印出 UEP/AC/六張登記表長度供未來棘輪化沿用。完整 tier 分類重構（ADR Phase 1-C 全量）留待 R62：該重構須把 `_EXEMPT_PAIRS`／`_SINGLE_SIDED_EXEMPT` 的值由字串換成 `(tier, reason)` tuple，經 grep 確認至少 3 支既有測試檔（`test_check_script_parity.py:248` 的 `.strip()`、`test_onboarding_parity_interlock.py:105/114` 的 `for key, why in ...items()` 字串比對）直接依賴字串型別，貿然全量重構風險與驗證負擔皆超出本輪判斷應承擔的範圍，故本輪僅取安全子集。詳見 `docs/06_quality/CrossPlatform_R61_Architect_Evidence.md` 與 `ADR-XPLAT-002` R61 裁決段 |
