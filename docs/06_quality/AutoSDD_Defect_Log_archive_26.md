# AutoSDD Defect Log — Archive 26

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 R56 殘留的最後 5 列（DEF-101-460／462／471／476／477），於 R57 **收尾**（2026-07-27）搬遷。至此 R56 全輪條目已完全退出主檔。
>
> **為何是這 5 列、為何現在才搬**：R57 動工前的 `archive_25` 已搬走 R56 的 15 筆已結列，這 5 列當時**刻意留在主檔**，各有明確理由（見 archive_25 標頭）：`460`／`476` 是 archive_23／24 的歸檔立帳條目、`462`／`471` 是含交接資訊的流程紀錄、`477` 是 R56 round 7 四方收斂確認——其 ④（`_CI_TREE_RE` 三份複本未收斂）與 ⑤（聯集鎖未鎖呼叫端）正是**交接給 R57 的 backlog**，當時搬走會使 R57 無從對帳。
>
> **搬遷條件現已成立**：R57 已完成該兩項 backlog——④ 收斂為 SSOT `tools/tests/_ci_scan_anchors.py` 並補呼叫端鎖（見主檔 DEF-101-481）、⑤ 補上 `test_scan_loop_goes_through_matches_stub_anchor`（見 DEF-101-483）。對帳鏈閉合，5 列同時失去「必須留在主檔」的理由，故一併搬遷。
>
> **位元組變化**：主檔 245,075 → 235,494 bytes（釋出 9,581）。此次歸檔的直接動因是體積：R57 寫入 15 條新條目後主檔距 240KB（245,760）warn 線僅剩 685 bytes，而 round 2／3 的複審紀錄尚未寫入。立帳見主檔 DEF-101-493。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-460 | 2026-07-27 | R56 Scan-D 預警＋主控收尾執行（本輪修復量達往常 2~3 倍） | 缺陷帳本主檔 229,068 bytes，距 `tools/check_defect_log_crossref.py` 的 `_LEDGER_WARN_BYTES`（240KB）僅 16,692 bytes。Scan-D 依近五輪成長率（R51 179,738 → R55 229,068，均值約 +12,300/輪）推估本輪結束後約 241k~243.6k、R57 幾乎必然踩進 warn 帶。但本輪實際修復量達 32 項（往常 2~3 倍），若先寫入條目再歸檔會在寫入當下就衝破 256KB fail 硬閘 | P4（帳本治理；預防性） | 本輪處理（主控） | fixed@R56：改為「**先歸檔騰空間、再寫入本輪條目**」。搬遷 16 筆已結列（fixed@／wontfix，且經程式化比對確認未被四份 crossref 掃描目標以 DEF-ID 樣式引用）至新建 `AutoSDD_Defect_Log_archive_23.md`，原文逐字保全、零刪除。主檔 229,068 → 204,278 bytes（釋出 24,790），`check_defect_log_crossref.py` 重跑 rc=0、106 筆有效狀態、四份掃描目標無矛盾 |
| DEF-101-462 | 2026-07-27 | R56 流程紀實（非程式/文件缺陷，記事存證） | R56 以 Workflow 執行五維掃描（Scan-A~E，26 項候選）＋四方（Architect/SA/SD/QA）複審迴圈，round 1/2/3 四方**全數 REJECT**（issues 24／20／26 項）並完成三輪分域修復（root/autoclaude/aisdlc_sdd 共 32 項）。**round 3 的 root 領域修復包與 round 4 的四方複核（共 5 個 agent）撞上 session 用量上限夭折**（訊息：resets 4am Asia/Taipei） | —（流程紀錄） | — | no_action_needed（流程紀錄，非缺陷，無待辦動作）。主控處理：①以 `journal.jsonl` 逐 agent 讀完整 result 物件重建三輪完整狀態（非只看 verdict/計數）；②發現 round 3 root 的 16 項發現去重後約 10 項未執行，於上限重置後另派**兩個檔案不重疊的分域修復包**（A：CI workflow ⇄ 本機 smoke 對稱性；B：WindowsApps guard 鎖鑑別力＋架構判準落地）補完；③**兩包各自獨立揭露一項任務書失準**——round 3 修復包並非「完全未執行」，而是**檔案已寫入（mtime 03:13~03:33）但死在驗證之前**，故兩包重心正確地轉為「獨立複核既有改動＋補做從未執行的 bug-injection 鑑別力證明」，包 A 另補完唯一真缺口（DEF-101-453）；④主控親自處理保留檔案（帳本、ONBOARDING.md）與 ADR/判例引用等跨域項目；⑤主控親自複驗 DEF-101-433 證偽、於全新乾淨 venv 重跑全套 pytest、重跑三個本地 CI 閘門 |
| DEF-101-471 | 2026-07-27 | R56 round 4/5 流程紀實（非程式/文件缺陷，記事存證） | R56 最終共執行**五輪四方複審**：round 1/2/3/4 四方**全數 REJECT**（issues 24／20／26／9），round 5.1 四方再全數 REJECT（9 項，全部是「本輪自身修復留下的無鎖硬編」），round 5.2 收斂為 **SD／Architect APPROVE ＋ SA／QA 各餘 1~2 項**（皆為 P3/P4 文件揭露或掃描面縮面），由主控修畢。**過程中兩次撞上 session 用量上限**：round 3 的 root 修復包（16 項發現未執行）與 round 4 原班四方 agent（5 個 agent 全掛） | —（流程紀錄） | — | no_action_needed（流程紀錄，無待辦動作）。主控處理：①以 `journal.jsonl` 逐 agent 讀完整 result 物件重建狀態，發現 round 3 root 修復包**並非「完全未執行」而是檔案已寫入（mtime 03:13~03:33）但死在驗證之前**——此事實由補派的兩個修復包**各自獨立**揭露，兩包因而正確地把重心從「重做」轉為「獨立複核既有改動＋補做從未執行的 bug-injection 證明」；②上限重置後補派分域修復包與四方重新複核；③親自處理保留檔案（帳本、ONBOARDING.md）與跨域項目（ADR §6.3、判例改錨、`.gitattributes`、掃描面縮面）；④於所有並行 agent 停工後在最終工作樹上一次性重測三套回歸。**帳本體積處置（round 6 SA 質疑 → 主控複驗 → round 7 收尾完成，敘事已改完成式）**：本輪帳本毛增約 47KB，**共執行兩次歸檔**。①動工前搬 16 筆至 `archive_23`（229,068 → 204,278，見 DEF-101-460）。②round 6 收尾寫入後主檔達 256,829 bytes、距 256KB fail 硬閘僅剩 5,315 bytes，已不能留待 R57，故**本輪收尾再建 `archive_24` 搬 19 筆 R56 round 1~3 已結列**（256,829 → 238,308，釋出 18,521，**已退出 240KB warn 帶**，見 DEF-101-476）。**判準爭點的裁定**：SA round 6 主張依 `archive_23` 判準尚有 4 筆可搬，經主控逐列複驗，其多算的 DEF-101-242／243 狀態欄開頭實為 `open`（「記事存證排入下一輪 backlog」，243 更明文「維持 open」），不符「已結」要件——**該項回報不成立，SA 於 round 7 已自行複驗並書面認錯**；依原判準合格者確實僅 DEF-101-428／431 兩筆 1.2KB。但主控**採納 SA 同時舉出的有效反例**：`archive_23` 內 DEF-101-420 仍被 `tools/check_gha_action_versions.py` 等三處**程式碼**引用，可見「被程式碼引用」不阻止搬遷（crossref 只掃四份指定文件、不掃程式碼），據此擴大判準至「本輪 round 1~3 已完全收斂的自身條目」完成②。**交接給 R57 的實況**：主檔 243,795 bytes（含 round 7 追加搬遷 2 筆後）、距 fail 硬閘餘 18,349 bytes（大於本輪單輪淨增 ~20.9KB），**非「必撞硬閘」狀態**；但 R57 若同樣是長輪，仍應在寫入前先評估歸檔（可搬來源＝本輪 round 4~7 條目 DEF-101-457~476，屆時已成歷史脈絡） |
| DEF-101-476 | 2026-07-27 | R56 round 7 SA 複核（指出本輪最大單一動作未立帳、且歸檔索引漏登記兩檔） | **歸檔動作本身未被記錄**：本輪收尾新建 `docs/06_quality/AutoSDD_Defect_Log_archive_24.md`（19 列、主檔 −18,521 bytes）是 R56 最大的單一帳本動作，卻**無任何對應條目**（對照 `archive_23` 有 DEF-101-460 明文立帳）；同時帳本自身在檔頭宣告「主檔只保留標頭＋完整缺陷總表＋本指標」的**歸檔索引未登記 `archive_23`／`archive_24`**，標題「二十一檔」在 HEAD 時點即已 stale 1 檔（R50 新增 archive_22 未同步）、本輪再加兩檔後 stale 3 檔。SA 程式化比對證實**引用鏈實際斷裂**：主檔內文提及但已搬至 archive_24 的 ID 有 5 個（438／442／444／451／453）、搬至 archive_23 的有 5 個（397／403／410／420／427），讀者照索引找不到這兩檔。archive_01~22 每一檔建立當輪都補了索引條目（含 R39／R40／R44 同輪雙歸檔先例），**R56 是唯一漏補的一輪且漏兩次** | P3（帳本指定的唯一導航機制失效；任何 R57 審查員查上述 10 個 ID 的歷史脈絡時必然踩到） | 本輪修復（主控） | fixed@R56 round 7：①歸檔索引補上 `archive_23`／`archive_24` 兩條完整條目（含建立時機、搬遷判準、位元組變化、立帳指向），標題「二十一檔」訂正為「二十四檔」並註明原數字自 HEAD 即 stale；實測索引條目數 24 == 磁碟實際檔案數 24。②本列即 archive_24 的正式立帳（比照 DEF-101-460 體例）。③DEF-101-471 的帳本敘事由「退不出 warn 帶、留給 R57」改寫為完成式，並訂正其已失效的推論（原寫「餘約 12KB、小於單輪增量、不先歸檔必撞硬閘」，實況為餘 23,836 bytes 大於單輪淨增 ~20.9KB）。**誠實揭露**：這三處都是主控自身的疏漏——本輪反覆抓「宣稱 vs 實況落差」，卻在收尾動作上再犯同型錯誤 |
| DEF-101-477 | 2026-07-27 | R56 round 7 四方收斂確認（QA APPROVE；Architect／SD／SA 各持 REJECT，發現全部經主控親自複驗成立） | round 7 四方共提 5 項，全屬 P3/P4 文件/測試層、無功能性缺陷：①**三元清單第二次寫錯**（`$c ? 'a':'b'` 實為已涵蓋而非假陰性，Architect／QA／SD 三方交叉，見 DEF-101-472 訂正）；②**CI 抽取的 fail-open 只縮小未關閉**（引號界定與 Join-Path 計算式路徑仍全盲，Architect F2／QA ② 交叉，見 DEF-101-475 訂正）；③**歸檔索引未登記 archive_23／24 且 DEF-101-471 敘事停在未完成式**（SA，見 DEF-101-476）；④`_CI_TREE_RE` 被我描述為「共用常數」實為三份逐字複本（Architect F3，用詞不實）；⑤QA backlog：2 支聯集鎖只鎖純函式、未鎖「掃描端有呼叫它」（繞過 helper ＋ 翻運算子的複合動作仍可逃逸） | P3/P4（四方一致認定本輪修復品質整體很高、無功能性缺陷；SA 另書面承認其 round 6 第 3 項回報有誤） | 本輪修復（主控） | fixed@R56 round 7：①②③ 已修並各自 bug-injection 複驗（詳見 DEF-101-472／475／476）；④`_CI_TREE_RE` 的用詞已於本列據實訂正為「三份逐字複本」——三份收窄任一份都會被自身 `len==3` 斷言擋下（SD 複核確認無 fail-open），故**刻意不收斂為單一共用常數**（收進 `_platform_helpers.py` 屬跨檔重構，逾越本輪『純文字/最小侵入』界線，列 R57 backlog）；⑤QA 自評「逃逸需繞過 helper ＋ 翻運算子兩個獨立動作複合，而 round 6 指名的單步觸發情境已被擋下，且同檔姊妹 helper 暴露面相同＝全檔一致慣例而非本次退步」，**判為 backlog 不修**。**流程紀實**：R56 共執行**七輪**四方複審，缺陷嚴重度單調下降（P0/P1 → P2 → P3/P4 → 純文字一致性），round 7 首度出現 APPROVE |
