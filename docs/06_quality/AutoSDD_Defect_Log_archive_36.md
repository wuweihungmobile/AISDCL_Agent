# AutoSDD Defect Log — Archive 36

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **9 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-618`、`DEF-101-619`、`DEF-101-617`、`DEF-101-623`、`DEF-101-620`、`DEF-101-622`、`DEF-101-621`、`DEF-101-624`、`DEF-101-612`
>
> **本次操作備註**：R66 Triage 收斂前歸檔：帳本 261,361 bytes 距硬閘 262144 僅 783 bytes，先騰餘裕再落地 DEF-101-596 座標訂正／ADR §6 邊界過期敘述訂正
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-618 | 2026-07-30 | R64 修復 `DEF-101-617` 後依任務指示分別在 Bash 工具（Git Bash）與 pwsh 兩種 shell 驗證同一測試檔，發現結果不一致（8/8 vs 6/8）而深挖出的新根因；同一批 pwsh 驗證中另實測到姊妹檔同型缺陷 | (a) Git for Windows 同一安裝下 `Git\bin\bash.exe`（47008 bytes）與 `Git\usr\bin\bash.exe`（2553064 bytes）是兩個不同二進位檔、行為不同：直接呼叫 `usr\bin\bash.exe`（不經 `bin\bash.exe`、呼叫端環境未預先由 Git Bash 自身設好 MSYSTEM 等變數，如原始 pwsh 直接起）時，其內部 PATH 不含 `/usr/bin`，`dirname` 找不到（實測 rc=127："dirname: command not found"）；反之直接呼叫 `bin\bash.exe` 時，無論外部傳入 PATH 是否限縮（甚至指向單一空目錄）都會在啟動時把 `/mingw64/bin:/usr/bin`（相對自身安裝根目錄）無條件注入到內部 PATH 最前面（實測：`env={"PATH": <單一空目錄>}` 呼叫後，bash 內部 `echo $PATH` 印出 `/mingw64/bin:/usr/bin:/c/Users/wuwei/bin:<空目錄>`），使「限縮外部 PATH 模擬缺 dirname」這個手法對它完全失效。`tools/tests/test_bash_probe_spec_contract.py`（`DEF-101-617` 修復後）與生產端 `AISDLC_SDD/scripts/bash_probe.py::usable_bash()` 用同一套候選蒐集順序，第一個「驗活成功」的候選才會被選中——選到哪一個取決於呼叫當下環境是否已有 Git Bash 自身設定好的 PATH／MSYSTEM，造成同一份測試在不同 shell 下結果不同（Bash 工具環境 8/8、pwsh 環境 6/8）。**pwsh 正是 `windows-compat-ci.yml` 對本檔的實際執行 shell**，非純理論風險。(b) 同批 pwsh 驗證另實測到 `tools/tests/test_windowsapps_guard_bash_parity.py:393-394` 的 `_bash_exe()` 仍是與 `DEF-101-617` 修復前完全相同形狀的天真 `shutil.which("bash")`（未排除 WSL system32 佔位版、未走 git.exe 相對路徑候選），pwsh 下同樣解析到 `C:\WINDOWS\system32\bash.exe`，致 `TestSharedGuardShellFunctionBehavior.test_real_candidate_accepted`／`test_legit_dir_merely_containing_substring_is_accepted` 兩支失敗——與 `DEF-101-617` 根因完全同型，只是換了一個檔案 | P2（非生產正確性缺陷：production `usable_bash()` 兩種情況下最終選到的都是真實可用的完整 Git Bash，不會誤選壞掉的候選；純粹是「限縮 PATH 模擬缺 coreutils」這個測試模擬手法對 `bin/bash.exe` 這類會自我注入 PATH 的啟動器失去鑑別力，以及另一檔案尚未套用 `DEF-101-617` 已驗證有效的修復手法） | (a) `tools/tests/test_bash_probe_spec_contract.py` 的兩支「拒絕」測試需要人工設計決策（限縮 PATH 對 `bin/bash.exe` 這類自我注入的啟動器無效，需另一種模擬手法或接受該情境在此候選下無法以真實 subprocess 驗證）；(b) `tools/tests/test_windowsapps_guard_bash_parity.py` 可機械套用與 `DEF-101-617` 完全相同的修復手法 | fixed@R65（(a) fixed@R65：新增手法 A／B 兩組測試補齊 `bin/bash.exe` 候選類型鑑別力，詳見下方；既有兩支「拒絕」測試依指示保留不修改）／(b) fixed@R64：`tools/tests/test_windowsapps_guard_bash_parity.py::_bash_exe()` 套用與 `DEF-101-617` 完全相同的修復手法——獨立重寫候選蒐集（`shutil.which("git")` 往上最多 4 層找 `usr/bin/bash.exe`／`bin/bash.exe`；再收非 system32 段的裸 `shutil.which("bash")`），共用 `tools/lib/bash_probe_spec.py` 的 `SYSTEM32_SEGMENT`／`PROBE_CMD`／期望輸出資料規格（比照既有消費者慣例新增 `import bash_probe_spec as _spec`，不 import 生產碼或姊妹測試檔），對每候選實跑 `PROBE_CMD` 驗活，第一個驗活成功者才接受為回傳值；以 `functools.lru_cache` 快取結果（函式簽名／回傳型別／呼叫慣例不變）。驗證：`python -m unittest tools.tests.test_windowsapps_guard_bash_parity -v` 29/29 全綠、rc=0（含先前在 pwsh 下失敗的 `TestSharedGuardShellFunctionBehavior.test_real_candidate_accepted`／`test_legit_dir_merely_containing_substring_is_accepted` 兩支）。(a) fixed@R65：`tools/tests/test_bash_probe_spec_contract.py` 新增 `_find_real_git_bash_install_root()`／`_build_coreutils_less_bash_clone()` 兩個獨立輔助函式（手法 B：複製一份刻意缺 coreutils 的 `bin/bash.exe` 複製品——啟動器＋解譯器＋`msys-2.0.dll`＋空 `etc/`／`mingw64/bin/`），並新增 `TestBinBashLauncherSelfInjectsPathContract`（手法 A：直接鎖住『限縮外部 PATH 對真實 `bin/bash.exe` 無效、但 bash 內部 `export PATH= ; ` 前綴可讓其確定性失敗』現象本身，2 case）與 `TestUsableBashRejectsCoreutilsLessBinBashClone`（手法 B：(i) 直接呼叫複製品確定性 rc≠0、(ii) 透過生產端 `usable_bash()` wiring 正確回傳 `None`，2 case）兩個測試類；`usable_bash_with_probe_spy()` 加一個可選參數 `candidate_bash`（預設沿用既有 `_BASH`，對既有呼叫端零行為變化）以支援 (ii)。既有兩支「拒絕」測試依指示保留不修改、不弱化——它們對原本針對的 `usr/bin/bash.exe` 候選類型仍有鑑別力，在本機 pwsh 下候選落到 `bin/bash.exe` 時繼續紅，非新增回歸（成因見 `DEF-101-617`）。實測：Bash 工具（Git Bash）`python -m unittest tools.tests.test_bash_probe_spec_contract -v` 12/12 全綠、rc=0；pwsh（`windows-compat-ci.yml` 實際執行 shell）重跑同指令 10/12（FAIL 恰為上述既有兩支、新增 4 支全綠）、rc=1；`python tools/run_root_unittests.py`：Bash 工具下 1103 測試（較 R64 的 1099 淨增 4）、rc=0；pwsh 下同為 1103 測試、FAILED(failures=2)（FAIL 明細與上同）、rc=1。承接：兩支既有「拒絕」測試本身在 pwsh 下仍非綠，作為已知殘留特徵保留；是否修改/替換其模擬手法讓整檔在 pwsh 下也全綠，留給 maintainer 決定（非本輪範圍） |
| DEF-101-619 | 2026-07-31 | R66 Triage 修復清單 P1 項；舵手在真實 Windows 11 開發機以 `Get-ScheduledTask` 實測驗證（立帳見 `DEF-101-529`（現居 `archive_33`）：該列曾記載 fixed@R60「新增 AutoClaude_WindowsSmoke」，但**這台**機器上實測只見 `AutoClaude_Nightly`／`AutoClaude_SD09_G0_GateCheck` 兩筆，成因不明——可能是該輪驗證發生在另一台機器／後續有人動過本機排程，本列不對成因做無證據推測，只記載實測現況） | (a) **`AutoClaude_WindowsSmoke` 排程任務在本機不存在**：`install_windows_nightly.ps1 -Status` 自行印出「❌ 排程任務不存在：AutoClaude_WindowsSmoke」且 `REAL_EXITCODE=1`。(b) **`-Uninstall` 被與 install 共用的 `Test-Path -LiteralPath $NightlyPs1` 存在性守門誤擋**：修復前該檢查放在 `if ($Uninstall)` 判斷之前，install／-Uninstall 兩路共用；scratchpad 隔離重現（複製腳本到 temp、`AutoClaude/tools/run_local_nightly.ps1` 不存在後跑 `-Uninstall -WhatIf`）：`RED_EXITCODE=1`、錯誤訊息「找不到 nightly 載體」，與 mac 側 `install_mac_nightly.sh` 的 `cmd_uninstall()`（完全不檢查底層腳本是否存在）行為不對稱 | P1（本輪 Triage 修復清單最高嚴重度：CI 帳單停擺〔DEF-101-081〕期間 Windows 側唯一執行級補償控制在本機實質未啟用，且 -Uninstall 邊界缺口影響冪等語意） | `tools/install_windows_nightly.ps1`、`tools/tests/test_install_windows_nightly.py` | fixed@R66：(a) 本機自動化 session 無法取得互動式 UAC 提權（`Test-IsAdmin()` 回傳 `False`；`whoami /groups` 顯示 `BUILTIN\Administrators` 為 `Group used for deny only`），改直接以 `Register-ScheduledTask`／`New-ScheduledTaskAction`／`New-ScheduledTaskTrigger`／`New-ScheduledTaskSettingsSet` 呼叫（參數逐字取自腳本安裝分支對 `$SmokeTaskName` 的呼叫，非另行改寫）補齊註冊；`Get-ScheduledTask`／`Get-ScheduledTaskInfo` 實測確認：`AutoClaude_WindowsSmoke` 現存在、`State=Ready`、`NextRunTime=2026/8/1 上午 01:00:00`、`StartWhenAvailable=True`／`WakeToRun=True`／`DisallowStartIfOnBatteries=False`／`StopIfGoingOnBatteries=False`；既有 `AutoClaude_Nightly`（`LastRunTime=2026/7/31 上午 02:00:01`／`LastTaskResult=0`）與 `AutoClaude_SD09_G0_GateCheck` 兩支任務前後未變（未被誤觸）。重跑 `install_windows_nightly.ps1 -Status`：`STATUS_EXITCODE=0`。(b) 把 `$NightlyPs1`／`$SmokePs1` 兩道 `Test-Path` 存在性檢查一起移入 install-only 段落（`if ($Uninstall) { ... exit 0 }` 區塊之後），`-Uninstall` 分支不再觸碰 `Test-Path`。Bug-injection 紅綠對照（隔離複本，見上）：修復前（HEAD 版）`-Uninstall -WhatIf`＝`RED_EXITCODE=1`；修復後同指令＝`GREEN_EXITCODE=0`（正確預覽兩個 `Unregister-ScheduledTask`）。控制組確認未破壞既有語意：install 路徑載體缺席仍 `CONTROL_A_EXITCODE=1`（fail-loud 不變）、install 路徑載體齊全時 `-WhatIf` 仍正確預覽兩個 `Register-ScheduledTask`（`CONTROL_B_EXITCODE=0`）、`-Uninstall`（非 `-WhatIf`、非管理員）仍正確要求提權（`CONTROL_C_EXITCODE=1`）。(c) 新增測試覆蓋：`TestInstallWindowsNightlyStructure::test_uninstall_branch_does_not_depend_on_carrier_script_existence`（結構鎖：以 `foreach ($name in @($TaskName, $SmokeTaskName))` 為錨點取出真正的 `-Uninstall` 區塊，斷言其不含任一 `Test-Path` 呼叫、且兩個存在性檢查必須排在該區塊之後）＋ `TestStatusExitCodeRuntime::test_uninstall_whatif_succeeds_when_carrier_scripts_are_unreachable`（沿用既有 `_script_with_absent_task_names` 複本手法，真實執行 `-Uninstall -WhatIf`，斷言 rc=0 且預覽輸出涵蓋兩個改名後的任務）。驗證：`python -m unittest tools.tests.test_install_windows_nightly -v` 於 Bash 工具（Git Bash）與原生 PowerShell 各跑一次皆 19/19 全綠、rc=0；`python tools/run_root_unittests.py`：本列補齊帳本引用後應回綠（帳本引用完整性檢查是修復當下唯一的失敗項）。🔴 **殘留發現（非本列缺陷本體，觀察但不阻擋，不在本輪處理）**：探測到**非提權**的 PowerShell session 對「不帶 `-Principal`／`RunLevel Highest`」的標準排程任務，`Register-ScheduledTask` 實際上也能成功註冊（同一台機器上以 `-RunLevel Highest` 探測則正確地 `Access is denied`）——代表腳本既有的 `Test-IsAdmin()` 全域守門，在「本機帳戶屬 Administrators 但目前處於標準權杖（split token）」這類常見開發機設定下，可能比 Task Scheduler 對這組具體 cmdlet 呼叫的實際 ACL 要求更保守。本列**不**變更 `Test-IsAdmin()` 邏輯（超出本項指派範圍；且不確定其他機器設定下是否真的需要提權，貿然放寬有回歸風險），僅如實記載觀察供後續輪次評估是否值得放寬 |
| DEF-101-617 | 2026-07-30 | R64 舵手在 Windows 11 機器親跑 `python -m unittest tools.tests.test_bash_probe_spec_contract -v` 真實重現（`git stash` 證實乾淨 HEAD 即可重現，與本輪 ADR-XPLAT-002 §8 item 12 UEP 棘輪化工作無關） | `tools/tests/test_bash_probe_spec_contract.py:26`（修復前）`_BASH = shutil.which("bash")` 是模組載入時算好、全檔共用的天真固定值。在本機這種「Git Bash 未直接掛在系統 PATH、只能透過 git.exe 相對路徑找到，PATH 上 `bash` 卻解析到 WSL System32 佔位啟動器」的常見 Windows 開發機設定下，`_BASH` 會錯誤等於 `C:\WINDOWS\system32\bash.exe`（非真正 Git Bash）。該 WSL 佔位版透過 RPC 呼叫 wslservice，呼叫時 `env=` 缺 `SystemRoot` 會確定性失敗（rc=1，stdout 為亂碼 UTF-16 RPC 錯誤），而 `_run_probe()`／`usable_bash_with_probe_spy()` 皆只放 `PATH` 未放 `SystemRoot`；更根本的是 `usable_bash_with_probe_spy()` 把 `shutil.which` 整個 mock 掉（查 "git" 一律回 `None`），使生產端 `bash_probe.py::usable_bash()`（`AISDLC_SDD/scripts/bash_probe.py:48-63`）的「git.exe 相對路徑找 Git Bash」候選蒐集失效，只能靠已被排除的 WSL 佔位版本身，candidates 為空 → 一次 subprocess 都沒呼叫就回 `None`。連鎖造成 6/8 測試確定性失敗：`TestProbeCmdRealSubprocessBehavior.test_succeeds_with_real_path`、`TestUsableBashEndToEndWithRestrictedPath` 兩支、`TestNoneSourceIsDistinguishable` 三支 | P2（讓既有回歸鎖整組失去鑑別力／在此類機器設定下假紅，不影響生產端 `bash_probe.py`／`bash_probe_spec.py` 正確性，它們本身無問題） | `tools/tests/test_bash_probe_spec_contract.py`（測試檔自身的 `_BASH` fixture，非生產碼） | fixed@R64：把 `_BASH = shutil.which("bash")` 改為獨立重寫的模組層級函式 `_probe_a_real_usable_bash_for_fixture()`（比照生產端 `usable_bash()` 的候選蒐集邏輯獨立重新實作、刻意不 import 生產碼，維持本檔案頭「三份消費者各自獨立重寫」架構慣例，避免測試因共用生產端邏輯而失去對生產端共同盲點的鑑別力）：先蒐集 git.exe 相對路徑候選（`shutil.which("git")` 往上最多 4 層找 `usr/bin/bash.exe`／`bin/bash.exe`），再蒐集非 system32 段的裸 `shutil.which("bash")`，對每個候選實際跑一次 `PROBE_CMD`（`_spec.PROBE_CMD`，`subprocess.run(..., timeout=15)`）驗活，第一個驗活成功者才接受為 `_BASH`；全部候選皆失敗（或無候選）才回傳 `None`，維持既有 `@unittest.skipUnless(_BASH, ...)` 語意。驗證：在 Bash 工具（Git Bash）環境下 `_BASH` 正確解析到 `C:\Program Files\Git\usr\bin\bash.exe`，`python -m unittest tools.tests.test_bash_probe_spec_contract -v` 8/8 全綠（重跑兩次皆穩定綠）；`python tools/run_root_unittests.py`（同環境）僅剩本列自身的帳本引用完整性檢查一項待補（本列補齊後即綠，1099 測試、rc=0）。🔴 **殘留發現（非本列缺陷本體、已另立 `DEF-101-618`，不佔用本列 fixed 狀態）**：改用**實際 CI 執行本檔的 pwsh shell**（`.github/workflows/windows-compat-ci.yml` 明文 `shell: pwsh`）重跑，`_BASH` 落在候選蒐集第二順位 `C:\Program Files\Git\bin\bash.exe`（47008 bytes 精簡啟動器，非 `usr\bin\bash.exe` 2553064 bytes 完整版）；該啟動器啟動時會無條件把 `/mingw64/bin:/usr/bin` 注入到自身內部 PATH 最前面、不受外部傳入 PATH 限縮影響，使 `test_fails_when_path_lacks_dirname`／`test_usable_bash_rejects_candidate_when_path_lacks_dirname` 兩支「限縮 PATH 模擬缺 dirname」的測試在 pwsh 下仍為紅（該 shell 下 6/8，非 8/8）。此非本次修復造成的回歸——這兩支測試先前是靠 WSL 佔位版因無關原因（缺 `SystemRoot`）巧合式通過，並未真正驗證到位；本次修復診斷正確後才使這個更深一層的既有缺口浮現，詳見 `DEF-101-618` |
| DEF-101-623 | 2026-07-31 | R66 Triage 修復清單 P2 項（component_sanitizer 呼叫點掃描器：凍結風險名單新鮮度檢查掃描範圍擴大至與 offender 掃描一致） | `AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_sanitize_component_call_site_lock.py::test_live_bootstrap_is_subset_of_frozen_list`（修復前第 88 行）只掃 `_FSM_RUNTIME_DIR`（`tools/fsm_runtime/`），姊妹 offender 掃描（`AISDLC_SDD/scripts/tests/test_sanitize_component_callsite_frozen_versions.py::test_all_versions_have_no_unsanitized_callsite_offenders`，R57 DEF-101-489 起）已涵蓋 `tools/fsm_runtime`／`.claude/hooks`／`tools/arch_fitness` 三個子目錄——凍結風險名單新鮮度檢查與 offender 掃描範圍不一致，構成偵測空洞：若 `.claude/hooks` 或 `tools/arch_fitness` 風格檔案首次呼叫 `_sanitize_component()` 引入全新識別字，新鮮度檢查看不到、不會提示回填 `_FROZEN_RISKY_NAMES`，之後任何子目錄對同一識別字的裸用即可逃過 `find_offenders()`。**bug-injection 實測證實可觸發**：於 `.claude/hooks/_r66_def101623_probe.py`（臨時檔，驗證後已刪除、未留痕）注入 `def emit(zzz_r66_probe_id): return _sanitize_component(zzz_r66_probe_id)`——用修復前掃描範圍（僅 `tools/fsm_runtime`）重跑，`unknown` 集合為空、測試綠燈（實測輸出：`OLD scope (tools/fsm_runtime only) unknown = frozenset()` → `PASS (green) -- 漏偵測，即修復前的偵測空洞`），證實偵測空洞真實存在；改用修復後範圍（3 子目錄）重跑同一注入，`test_live_bootstrap_is_subset_of_frozen_list` 正確轉紅（`AssertionError: frozenset({'zzz_r66_probe_id'}) != set()`）。修復前另以 `grep -rn "_sanitize_component" .claude/hooks tools/arch_fitness`（LATEST）確認 rc=1 零命中，故現況無真實 offender 逃逸，屬前瞻性但已證實可觸發的機械鎖缺口，非阻擋 | P2 | 本輪修復（`_SCAN_SUBDIRS` 常數自 `test_sanitize_component_callsite_frozen_versions.py` 抽到 `AISDLC_SDD/scripts/component_sanitizer_callsite_scan.py` 本體當單一真相源，方法論⑦新增第 7 條；`test_sanitize_component_call_site_lock.py` 新增 `_iter_scan_subdir_files()` helper，`test_live_bootstrap_is_subset_of_frozen_list` 改用其掃描 3 個子目錄，`test_all_filename_fstrings_sanitize_known_risky_identifiers`〔offender 本體鎖〕維持原掃描範圍不變，仍由 frozen_versions 測試涵蓋 LATEST 其餘 2 子目錄） | fixed@R66（三檔改動：`component_sanitizer_callsite_scan.py` 新增 `_SCAN_SUBDIRS` SSOT＋方法論⑦說明；`test_sanitize_component_callsite_frozen_versions.py` 改 import 不再自行定義；`test_sanitize_component_call_site_lock.py` 新增 `_VERSION_ROOT`／`_iter_scan_subdir_files()`、更新 `test_live_bootstrap_is_subset_of_frozen_list`＋類別 docstring。**回歸驗證**：`test_component_sanitizer_callsite_scan.py`／`test_sanitize_component_callsite_frozen_versions.py`／`test_component_sanitizer_reserved_trailing_space.py`／`test_sanitize_component_call_site_lock.py`／`test_state_component_sanitizer_parity.py`／`test_component_sanitizer_shared_layer_lock.py`／`test_sanitize_component_frozen_sdd_versions_lock.py` 共 `58 passed, 45 subtests passed`（零回歸）；`AISDLC_SDD_v0.30/tools/fsm_runtime/tests/`〔不含 chaos〕全套 `1750 passed, 6 skipped, 34 deselected, 14 subtests passed`；`AISDLC_SDD/scripts/tests/` 全套 `249 passed, 1 skipped, 31 subtests passed`；PowerShell 原生載具另跑一次 `test_sanitize_component_call_site_lock.py` 同樣 `4 passed`） |
| DEF-101-620 | 2026-07-31 | R66 Triage 修復清單 P2 項；bug-injection 實測（含 1 個唯讀檔的假 `.venv` 目錄）發現 `tools/dev_start.py` 的 `.venv` 自我修復路徑用裸 `shutil.rmtree()`，未採用同 repo 已在 `hub_sync.py`（`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/hub_sync.py:148-197 _rmtree_windows_safe`）與 `sync_exposed_skills.py` 建立的唯讀位元安全處理技巧 | 本機 Windows 11 Pro 26200 真機驗證：對含唯讀檔的假 `.venv` 目錄呼叫裸 `shutil.rmtree()` → `RAISED PermissionError: [WinError 5] 存取被拒。: '...\.venv\bin\python'`，且事後 `venv.exists()` 仍為 True（半殘目錄未清除，非只失敗一次）。影響三處呼叫點：`tools/dev_start.py::_ensure_venv_shape()` 內兩處（換手保留失敗殘留的 `.venv-cache-<other>/` 清理 `dev_start.py:808`、兩平台直譯器皆缺的壞損 `.venv/` 移除 `dev_start.py:825`）與 `step_venv()` 一處（跨 OS 同 flavor 切換清除舊 `.venv/` `dev_start.py:1272`；行號為修復前基準）。三處皆已被 `except OSError` 包住不會裸崩潰，但「自動修復」本身在此情境下靜默降級為要求人工手動介入 | P2（三處呼叫已有外層 `except OSError` 兜底不崩潰，但工具自稱的核心賣點——免動手自動修復——在 Windows 唯讀檔情境下實質失效） | `tools/dev_start.py`、`tools/tests/test_dev_start.py` | fixed@R66：新增 `_rmtree_windows_safe()` helper（技巧同款移植自 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/hub_sync.py::_rmtree_windows_safe`／`AISDLC_SDD/scripts/sync_exposed_skills.py::_rmtree_windows_safe`，R15 SCAN-B-2 首次建立、Rule 11 conformance over taste——依 Python 版本二擇一走 `onexc=`〔3.12+〕或 `onerror=`〔3.11〕，錯誤時先 `os.chmod(p, stat.S_IWRITE)` 再重試，仍失敗則重拋「原始」例外），並把三處呼叫點（`_ensure_venv_shape()` 內兩處、`step_venv()` 一處）改為呼叫該 helper。Bug-injection 紅綠對照（本機 Windows 11 Pro 26200 真機驗證，非模擬）：RED（修復前，對含唯讀檔的 fixture 直接呼叫裸 `shutil.rmtree`）＝`RAISED PermissionError: [WinError 5] 存取被拒。` 且 `venv.exists()` 仍 True；GREEN（修復後，改呼叫 `_rmtree_windows_safe`）＝無例外拋出、`venv.exists()` 為 False（目錄確實整個移除）。另以 monkeypatch 把 `_rmtree_windows_safe` 換回裸 `shutil.rmtree` 重跑同一組 fixture，確認會重新觸發 `PermissionError`——證明本測試 fixture 具鑑別力、非假綠。新增測試：`tools/tests/test_dev_start.py::TestRmtreeWindowsSafe`（green 對照組 `test_removes_directory_with_readonly_file` ＋ Windows-only red 對照組 `test_bare_rmtree_would_have_raised_permission_error`，驗證 fixture 本身確實會踩雷、green 測試不是因為沒踩到雷才綠）與 `TestVenvSelfHealCallSitesUseSafeRmtree`（同 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_hub_sync.py::TestMirrorLocalWindowsResilience::test_mirror_local_does_not_call_bare_rmtree` 鎖法的平台中立 call-site 鎖，`inspect.getsource` 斷言 `_ensure_venv_shape`／`step_venv` 原始碼含 `_rmtree_windows_safe(` 且不再含裸 `shutil.rmtree(`，防止未來被順手改回）。驗證：`cd tools/tests && python -m unittest test_dev_start -v` 168 tests OK（skipped=10，皆既有 POSIX-only/macOS-only 略過項，與本次修復無關）；`python tools/run_root_unittests.py` 1114 tests OK（skipped=10）——本列補齊帳本引用後應回綠（帳本引用完整性檢查是修復當下唯一失敗項） |
| DEF-101-622 | 2026-07-31 | R66 Triage 修復清單 P2 項；`tools/check_script_parity.py` R65 新增的 `_LATEST_THINNESS_ENROLLED`／`_LATEST_PINNED_SHA256` 兩張表與既有姊妹表 `_THINNESS_ENROLLED`／`check_wrapper_thinness._PINNED_SHA256`（已有 `_check_thinness_cross_lock`，R12 QA-1 教訓）同型，但未比照建立交叉鎖 | Bug-injection 實測（真 repo 現況，隔離腳本注入非落地修改）：清空 `_LATEST_PINNED_SHA256`、不動 `_LATEST_THINNESS_ENROLLED`，`_check_latest_thinness()` 迴圈次數歸零、`ok` 從未被設為 `False`，回傳 `True` 並印出自相矛盾訊息「✅ LATEST 薄殼釘選：0 支 hash 釘選皆正常（1 對）」——兩份獨立字面清單各自腐化即雙工具全綠，與 R12 QA-1 修過的病灶同型，只是這次發生在 R65 新造的表上 | P2（不影響生產正確性；納管完整性的「反向腐化」訊號缺口，屬 Scan-H 機械鎖覆蓋率缺陷） | `tools/check_script_parity.py`、`tools/tests/test_check_script_parity.py` | fixed@R66：新增 `_check_latest_thinness_cross_lock()`（比照既有 `_check_thinness_cross_lock()` 形狀：斷言 `_LATEST_THINNESS_ENROLLED` 每個 stem 的 `.sh`／`.ps1` 皆在 `_LATEST_PINNED_SHA256`，反向多餘 pin 亦紅），已接入 `main()` 無參數呼叫路徑（`ok = _check_latest_thinness_cross_lock() and ok`）與成功訊息列舉；未改動任何既有生產邏輯或既有測試斷言。Bug-injection 紅綠對照（隔離 `python -c` 腳本操作記憶體內模組物件，未落地修改任何檔案）：RED（清空 `_LATEST_PINNED_SHA256`、跑既有 `_check_latest_thinness()`）＝回傳 `True` 且印「✅ LATEST 薄殼釘選：0 支 hash 釘選皆正常（1 對）」（缺陷本體重現）；GREEN（同一份被清空狀態改跑本輪新增的 `_check_latest_thinness_cross_lock()`）＝回傳 `False` 並印兩行「❌ LATEST thinness 交叉鎖：LATEST/tools/fsm_runtime/formal/run_tlc.sh／.ps1 已登記 _LATEST_THINNESS_ENROLLED 但不在 _LATEST_PINNED_SHA256」。控制組（真實現況，未注入）：`python tools/check_script_parity.py` 印「✅ LATEST thinness 交叉鎖：1 對薄殼登記與 2 支 hash 釘選鍵集合一致」、整體 rc=0。新增測試類 `TestLatestThinnessCrossLock`（`tools/tests/test_check_script_parity.py`）：`test_current_tables_consistent_green`（現況綠）、`test_bug_injection_cleared_pins_still_enrolled_is_red`（原樣重現缺陷情境，紅）、`test_missing_pin_key_is_red`（單刪一支 pin，紅並點名）、`test_extra_pin_without_enrollment_is_red`（塞入未登記的多餘 pin，紅並點名）。驗證：`cd tools/tests && python -m unittest test_check_script_parity -v` 於 Bash 工具（Git Bash）與原生 PowerShell 各跑一次皆 `Ran 67 tests` `OK`（含既有 63 支 + 本輪新增 4 支）；`python tools/check_script_parity.py` 兩種載具皆 rc=0。`python tools/run_root_unittests.py`（Bash 工具，160s）：`Ran 1118 tests`，僅 1 個既有失敗 `test_defect_id_reference_integrity.py::test_every_referenced_defect_id_exists_in_ledger_family`（正是本列補齊帳本引用前的預期過渡態：程式碼內 3 處 `DEF-101-622` 引用在補上本列前於帳本查無主鍵，本列補上後應回綠，未另行複驗——與 `DEF-101-619`／`620`／`621` 同款收尾模式），與本次修改的邏輯本身無關；`skipped=10` 皆既有 POSIX-only/Windows 權限受限略過項，與本次修復無關 |
| DEF-101-621 | 2026-07-31 | R66 Triage 修復清單 P2 項；實測驗證（Bash 工具與 PowerShell 兩種載具皆同一結果）重現：`SDD_FW_VERSION=AISDLC_SDD_v0.30 bash scripts/ci-gate.sh` 於通過版本迴圈後、進入「共享 infra scripts/tests/」pytest 階段（修復前 `ci-gate.sh:193`）出現 2 筆假紅，整支腳本以 rc=1 收尾——而該用法正是 `ci-gate.sh` 檔頭第 11 行明文記載的官方單版 debug 用法 | `SDD_FW_VERSION` 只在呼叫當下的行程樹內設定、版本迴圈結束後從未清除，原樣外溢進下方共享 infra 的 `python -m pytest scripts/tests/` 子行程；而 `scripts/tests/test_ci_gate_version_resolution.py` 內 4 個直接呼叫 `ci-gate.sh` 的 `subprocess.run`（`_dry_run` 輔助函式、`test_missing_python_fails_loud_not_silent_downgrade` 第二呼叫、`test_resolver_failure_downgrades_with_stderr_warning`、`test_override_with_failed_resolver_suppresses_downgrade_warning`）皆未帶 `env=` 覆寫、原樣繼承外層行程環境，使外溢值滲透進巢狀組出的 dry-run 呼叫，把測試期待的雙軌/降軌情境覆寫成單版。本機重現（Bash 工具）：`SDD_FW_VERSION=AISDLC_SDD_v0.30 python -m pytest scripts/tests/test_ci_gate_version_resolution.py -q` → `2 failed, 8 passed`（`test_dual_track_includes_frozen_baseline_and_latest` 斷言「凍結基線 v0.01 必恆測」失敗，實得 `['AISDLC_SDD_v0.30']`；`test_resolver_failure_downgrades_with_stderr_warning` 斷言「resolver 失敗應僅測凍結基線」失敗，實得 `AISDLC_SDD_v0.30`） | P2（不影響生產正確性；官方文件記載的合法單版 debug 用法會讓共享 infra 測試假紅，易誤導後續除錯方向） | `AISDLC_SDD/scripts/ci-gate.sh`、`AISDLC_SDD/scripts/tests/test_ci_gate_version_resolution.py` | fixed@R66：雙重防禦——①`ci-gate.sh` 於版本迴圈結束、進入共享 infra pytest 階段前新增 `unset SDD_FW_VERSION`；②`test_ci_gate_version_resolution.py` 新增 `_env_without_fw_version()` helper（複製 `os.environ` 後 `pop` 掉 `SDD_FW_VERSION`），套用於上述 4 個直接呼叫 `ci-gate.sh` 的 `subprocess.run` 呼叫點（第 5 個僅檢測 python 存在性的 `command -v python` probe 呼叫與此缺陷無關，未變動）。Bug-injection 紅綠對照：RED（修復前，`SDD_FW_VERSION=AISDLC_SDD_v0.30 python -m pytest scripts/tests/test_ci_gate_version_resolution.py -q`）＝`2 failed, 8 passed`；GREEN（同指令，修復後）＝`10 passed in 1.05s`。控制組（無汙染環境變數）確認未破壞既有語意：`python -m pytest scripts/tests/test_ci_gate_version_resolution.py -q` 修復前後皆 `10 passed`。完整重現原指令兩種載具皆重跑確認 rc=0：Bash 工具（Git Bash）`time SDD_FW_VERSION=AISDLC_SDD_v0.30 bash scripts/ci-gate.sh` → `EXIT_CODE=0`（`real 1m14.070s`），逐軌計數 `AISDLC_SDD_v0.30:1750 scripts/tests:249`；PowerShell（經 `& "C:\Program Files\Git\bin\bash.exe" scripts/ci-gate.sh`——純 `bash scripts/ci-gate.sh` 在此機器的 PowerShell 下會先解析到 WSL 佔位版 `C:\WINDOWS\system32\bash.exe` 而非本缺陷範疇，改直呼 Git Bash 執行檔繞過）→ `RC=0`（`ELAPSED=00:01:11.83`），`1750 passed, 6 skipped...`／`249 passed, 1 skipped...`／`✅ 本機 CI 閘門全數通過（版本：AISDLC_SDD_v0.30）` |
| DEF-101-624 | 2026-07-31 | R66 Triage 修復清單 P2 項（ADR-XPLAT-002 Phase 2-C/2-D 落地）；Architect（Scan-E）評估 10 支測試檔逐字核對後 GO | `_FROZEN_VERSION_DIR_RE`（`test_component_sanitizer_shared_layer_lock.py`／`test_sanitize_component_frozen_sdd_versions_lock.py`）誤用 `.match()`：`re.match(r'^AISDLC_SDD_v\d+\.\d+$', 'AISDLC_SDD_v0.30\n')` 因 `$` 允許結尾前恰一個換行字元而誤判命中，`.fullmatch()` 正確不命中（bug-injection 實測見下）；另 LATEST 版本解析 subprocess 樣板複製 10 份、`_FROZEN_SDD_VERSION_RE`/`_exclude_frozen_sdd_versions()` 複製 5 份 | P3（真實正確性缺口，但被 `.fullmatch()` 排除的畸形輸入〔目錄名帶尾隨換行〕在現行檔案系統下難以自然產生，非阻擋） | `tools/lib/sdd_latest.py`（新增）；`tools/tests/test_bash32_compat.py`／`test_component_sanitizer_shared_layer_lock.py`／`test_platform_neutral_paths.py`／`test_ps1_bom.py`／`test_ps51_compat.py`／`test_sanitize_component_frozen_sdd_versions_lock.py`／`test_subprocess_encoding_hygiene.py`／`test_windows_forbidden_filename_parity.py`／`test_windowsapps_guard_bash_parity.py`／`test_windowsapps_guard_cross_consistency.py` | fixed@R66：新增 `tools/lib/sdd_latest.py`（`resolve_latest_name`/`resolve_latest_root` raise 版入口＋`FROZEN_VERSION_DIR_RE`/`FROZEN_SDD_PATH_PREFIX_RE`/`exclude_frozen_sdd_versions`），10 檔改為委派、移除各自複本，2 處 `.match()`→`.fullmatch()` 一併修正；未加 check_script_parity.py 既有的 Optional 版契約（該檔不在本輪範圍，且無呼叫端會是死碼）。Bug-injection：RED（`re.match` 對帶尾隨換行輸入）＝`True`（誤判）；GREEN（`sdd_latest.FROZEN_VERSION_DIR_RE.fullmatch`）＝`False`；控制組（真實版本目錄名）兩者皆 `True`（無回歸）。驗證：`cd tools/tests && python -m unittest test_bash32_compat test_component_sanitizer_shared_layer_lock test_platform_neutral_paths test_ps1_bom test_ps51_compat test_sanitize_component_frozen_sdd_versions_lock test_subprocess_encoding_hygiene test_windows_forbidden_filename_parity test_windowsapps_guard_bash_parity test_windowsapps_guard_cross_consistency -v` 163 tests OK；`python tools/run_root_unittests.py` 1118 tests（測試數與收斂前一致，純抽取無行為增減）、僅本列補齊前的帳本引用完整性檢查 1 項待補（同 DEF-101-619~623 收尾模式） |
| DEF-101-612 | 2026-07-30 | R60 r3 收尾包執行 `--apply` 時當場撞到（**非四方回報／Pkg-F 自查**） | **`archive_defect_log.py` 的搬遷判準不含「指針反向依賴」檢查**：判準①②③④ 只看該列自身狀態與 crossref 狀態宣稱，不看**有沒有別的檔指著這一列**。`--apply` 搬走 `DEF-101-529`／`555`／`558` 後共 **11 處**居所指針同時失實 | P3 | 根層 `tools/archive_defect_log.py` | fixed@R66：抽出 `check()` 判準(4)(6) 既有的指針掃描基元（`POINTER_RE`／`NONVERB_RESIDENCE_RE`／`_quotation_kind`／`_fenced_line_numbers`／`_CODE_SPAN_RE`，`check()` 本體零改動），新增 `_external_residence_claims_for(def_id, texts)`（回傳「有沒有別的檔宣稱該 ID 現居主檔」）＋ `_read_pointer_audit_texts()`（讀 `_pointer_audit_files()` 全文、缺檔 fail-loud）；`plan()` 新增判準⑥（`MOVE_CRITERIA` 第 6 項）：命中即硬擋、**不接受 `--ack` 繞過**（與判準④ 的交棒偵測刻意不同——指針居所是可驗證的事實陳述，不是需要人具名承認的主觀判斷）。範疇確如 Scan-E 評估比帳本原文重：不是局部小補丁，而是把 `check()` 內建的指針掃描邏輯抽出供 `plan()` 共用。新增 `tools/tests/test_archive_defect_log.py::TestPlanRejectsRowsWithExternalResidencePointers`（5 個 bug-injection 測試，比照既有 `TestPlanNeverProposesActiveRows`／`TestCheckModeBugInjection` 手法）：控制組（合成列 `DEF-999-994` 無外部指針時可搬）、注入 `立帳見主檔`／`見主檔` 兩種方言各自使其轉為不可搬且理由具名指向本缺陷、宣稱已現居某 archive 與 code span 引述兩種反向坐實（證明範圍未過寬、豁免確實沿用 `check()` 同一組基元）。驗證：`python -m unittest tools.tests.test_archive_defect_log -v` → 101 tests OK（含新增 5 支，較修復前 96 支淨增）；`python tools/run_root_unittests.py` → 1123 tests、rc=0；`ruff check tools/archive_defect_log.py tools/tests/test_archive_defect_log.py` → all checks passed；對真實帳本重跑 `python tools/archive_defect_log.py --plan` → 8 筆可搬（`①②③⑤⑥ 判準全過`，ID 與修復前相同）、`--check` 仍 `REAL_RC=0`——本缺口在現行帳本上**潛伏未觸發**（現有可搬候選查無外部居所指針引用），本輪修的是判準本身的缺口，不是既有活躍缺陷 |
