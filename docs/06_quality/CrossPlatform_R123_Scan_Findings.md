# R123 精準修復輪第二棒 — 護欄層淨額承認

> **性質**：本棒不是掃描輪。本檔承擔 `repin_log_problems()` 款(9) 強制的**護欄層累積淨額
> 承認**（下節的 `guard-total` 標記行，與 `_GUARD_LINES_REPIN_LOG` 表尾雙向對帳，寫錯即紅）。
> 逐筆結案取證在 `CrossPlatform_R123_Debt_Closure.md`。
> **體例**：不使用「延後到R／交給R／留給R／承接輪次：R」等前瞻輪號句型。

---

## §1 護欄層累積淨額承認

<!-- guard-total:R123 --> 本棒護欄層行數 `91668→91990`（淨額 +322）。

| 列 | 起 → 後 | 淨額 | 性質 |
|---|---|---|---|
| 1 | 91668 → 91987 | +319 | 全額功能軌：三筆修復的回歸鎖落地 |
| 2 | 91987 → 91990 | +3 | 同輪追加：指紋鏈列＋prefix_len 同步 |

款(10) 上限 559 未撞。上一棒（R122）淨額 −125 已使款(11) 的連續上升 streak 歸零，本棒為
其後**第一個**正淨額輪。

🔴 **為何另立輪號而非併入上一棒**：上一棒已 commit／push／雲端全綠＝已收輪，本棒是其後的
新一批。併列會讓上一棒的 −125 被本棒抵銷回正、款(11) 的 streak 歸零判定失實——而判準本身
把「拆輪次」列為合法出口（逐字：「拆輪次不是拆列，同輪多列會被合併計算」）。

**逐檔漂移**（`--print-guard-lines` 實測，最終收斂為零漂移）：

| 檔 | 前 → 後 | 來源 |
|---|---|---|
| `test_quota_policy.py` | 3198 → 3316 | `DEF-200-200` 的六支回歸鎖 |
| `test_skip_ceiling_ratchet_direction.py` | 508 → 700 | `DEF-200-183` 的九支回歸鎖 |
| `test_adr_xplat001_c1c2_lock.py` | 7246 → 7258 | 本棒的重釘儀式自身 |

`DEF-200-205` 的二十一支回歸鎖落在 `AutoClaude/tests/`，不計入本層。

**三包皆未超預算**：每份任務書給的是「`tools/tests/` 淨增 ≤200」，實測 +118／+192／0。
這是上一棒的教訓所換來的——上一棒沒有事先預算，867 行的回歸鎖同時撞款(10)(11)，必須靠
散文搬遷抵銷才過得去。

---

## §2 誠實劃界

- 本棒三筆修復皆未經四方定點複審，依成熟度判準 M3「作者自證不計分」屬自證。
- 上一棒記錄的途中發現（Stop 稽核器把 hook 的 non-blocking 提醒誤判為「本平台載具失敗」）
  在本棒**再次復現**，仍未入帳本。全文＝`CrossPlatform_R122_Scan_Findings.md` §2。
