# AutoSDD Defect Log — Archive 34

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **24 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 5 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-613`、`DEF-101-567`、`DEF-101-566`、`DEF-101-589`、`DEF-101-595`、`DEF-101-609`、`DEF-101-601`、`DEF-101-591`、`DEF-101-597`、`DEF-101-593`、`DEF-101-599`、`DEF-101-611`、`DEF-101-598`、`DEF-101-603`、`DEF-101-592`、`DEF-101-600`、`DEF-101-590`、`DEF-101-605`、`DEF-101-606`、`DEF-101-607`、`DEF-101-602`、`DEF-101-604`、`DEF-101-594`、`DEF-101-608`
>
> **本次操作備註**：R61 開輪前歸檔（R60 收輪標記主檔距 warn 僅 1,189 bytes）；566/567 的「下一輪」字樣係敘事引述非真實交棒，經讀原文核實後具名承認；573 因含真實未解交棒（收集數少 12 支問題）故不歸檔，留待本輪處理
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-613 | 2026-07-30 | R60 r3 收尾包交件前逐檔驗行尾時撞到（**非四方回報／Pkg-F 自查**） | 🔴 **表② 的內容指紋以磁碟 bytes 計算，而磁碟行尾是機器本地狀態 ⇒ 指紋不可跨機器重現**：`tree_fingerprint()` 直接 `digest.update(path.read_bytes())`，而 `git ls-files --eol` 實查四棵指紋樹內有 **212 支** tracked 檔處於「索引 LF、本機工作樹 CRLF」。`.gitattributes` 宣告 `* text=auto eol=lf` ⇒ **全新 clone／CI runner／macOS 機器 checkout 出來都是 LF**，四格指紋與本機記錄值必然全部不同 ⇒ `--check-snapshot` 在別台機器開箱即紅。今日零活體後果純粹因為**本 repo 至今只有這一台 Windows 機器在跑**，與 §9④「只在 Windows 驗過」同根 | P2（守門在單機以外恆假紅；且它正是 `DEF-101-563` 用來偵測表② 過期的唯一觸發器） | 根層 `tools/sync_onboarding_baselines.py` | fixed@R60（Pkg-H；改判：本輪主題就是跨平台，不交棒）：`tree_fingerprint()` 於 hash 前先過新增的 `_normalize_eol()`（bytes 層 `\r\n`→`\n`、孤立 `\r`→`\n`）——刻意不 decode 成文字，測試樹存在非 UTF-8／BOM 檔，decode 會引進與行尾無關的新失敗模式。回歸測試 6 支加在既有 `tools/tests/test_doc_loc_baseline_freshness_r60.py`（`TestFingerprintIsLineEndingAgnostic`，**零新增檔**）；鑑別力＝把正規化器 monkeypatch 成 identity（＝修復前行為）後 **5 紅 1 綠**、修復後 **6 綠**，唯一兩態皆綠的那支正是「未縮面」鎖（改內容／增行／改檔名仍須轉紅），本就該兩態皆綠。表② 四格指紋已以 `--write --with-slow` 重填，四項計數不變。🔴 **可攜性自驗**（本機無 macOS，以 git 索引的 LF bytes 當「另一平台會看到什麼」的代理）：① 逐檔用 `git hash-object` 取 git 套 clean filter 後的 blob sha，與我方正規化 bytes 自算的 blob sha 比對，**440 檔全同** ⇒ 正規化語意＝git 的；② 以索引 bytes 重算四棵樹指紋並與回填值逐格比對——v001=8ffe3c3dabbd／v030=53a91f6d44c8／scripts=7c780156f168／autoclaude=301742da6a8b，**四格全同**（修復前同一比對為 3 格不同，僅 scripts 樹因工作樹零 CRLF 而巧合相同） |
| DEF-101-567 | 2026-07-29 | R60 round 2 SD 複審（SD-R60-R2-04，P2 blocking）——**四方中唯一一位的 findings 沒有 id 級落地** | **SD round-1 十筆 findings（`SD-R60-01`~`SD-R60-10`）在缺陷帳本與 R60 證據全文中 id 級零命中**，而 ARCH 側有；且 `SD-R60-10` 明文要求的「記入帳本」與 Pkg-D 自標 🔴 的方法論訂正皆未落地。主控 round 3 獨立實查確認：`grep SD-R60-0N docs/06_quality/` 十筆全 0 檔命中 | P2（複審成果不可追溯；下一輪讀帳本看不到 SD 這一路的裁決） | 根層缺陷帳本 | fixed@R60 round 3：①本輪起 round-2／round-3 修復波的每一筆 finding 一律**在帳本列或落地程式碼二擇一**處逐字帶回報者 finding id（含 `SD-R60-NN`／`SD-R60-R2-NN`），使四方 id 可追溯性**對稱**（🔴 **R60 r3 訂正，`SD-R60R3-04`**：原寫「每一列狀態欄」與實況不符——`SD-R60-R2-07`／`-08` 屬測試覆蓋強化、本就沒有對應的缺陷列可掛，其 id 逐字落在 `tools/tests/test_archive_defect_log.py`；改為二擇一後宣稱與實況一致，詳見 `DEF-101-605`）；②主控實查訂正 SD 的一處轉述——SD 稱「ARCH／SA／QA 皆有」，實測只有 ARCH 有（`SA-R60-01`／`QA-R60-01` 同樣零命中），故這是**四方共同的**帳本慣例缺口而非只針對 SD；③`sync_exposed_skills.py:72`／`test_dev_start.py:44` 兩處 `onerror=` 於 Python 3.14 前必遷 `onexc=`，解鎖條件＝任一 CI／venv 升 3.14 或出現第 4 個同款站點；④方法論訂正入帳：`-W error::DeprecationWarning` 對 `onerror→onexc` 稽核**零鑑別力**（CPython 3.12.11 `shutil.rmtree` 全函式零 `warnings.warn`），未來必須用「模擬移除該關鍵字」的載具 |
| DEF-101-566 | 2026-07-29 | R60 round 2 QA 複審（QA2-R60-05，P4）——**標的是主控自己 round 2 的自我揭露文字** | **主控 round 2 自我揭露第 (b) 項「pyproject.toml TOML 形狀警告 ×3，來源 `tools/dev_start.py:547`，動工基線同樣 3 次」的行號歸因經查為假。** QA 以 `git show HEAD:` 版與工作樹版分別呼叫 `_toml_deps_snapshot()` 並攔截 stdout/stderr，兩版皆 `warn?=False`、輸出皆空；且 `DEPS_FILES` 內只有一支 `.toml` 會走該路徑。這與本輪自己在多處強調的「PASS／警告聲稱必須引可重跑載具與行號」紀律**直接矛盾** | P4（advisory 警告的歸因錯誤，無功能後果；但屬取證紀律違反） | 根層取證紀律 | fixed@R60 round 3（訂正而非補證）：主控獨立重驗確認 QA 為真——`_toml_deps_snapshot()` 對真實 `AutoClaude/pyproject.toml` 的 stdout／stderr 皆為空字串、`invalid=False`，且 `DEPS_FILES` 實查只含一支 `.toml`。**故該行號歸因撤回**，改記為「round 2 觀察到 TOML 相關警告，**次數與來源皆未經載具確認**（🔴 R60 r3 追加訂正，`SD-R60R3-07`：原文留著的「3 次」與已撤回的行號歸因取證狀態完全相同，故一併不引數字）；不保留一個經查為假的行號指認」——照 QA 原話：留著會讓下一輪照著去查而必然查空。🔴 **教訓**：我對他人的宣稱要求「引可重跑載具與行號」，自己的自我揭露卻寫了一個沒實測過的行號；zero-trust 必須對稱地套在自己身上 |
| DEF-101-589 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-01`，P1 blocking；主控反駁關親測 CONFIRMED） | **兩支工具的 `_GOVERNANCE_DOCS` 同名常數成員不同**：`check_defect_log_crossref.py:420` ＝（Evidence.md, Evidence_r3.md）／`archive_defect_log.py:282` ＝（Evidence.md, Scan_Dimensions.md），各缺對方一支；實測 `r3 in _pointer_audit_files()` ＝ `False` ⇒ 本輪新生的 `Evidence_r3.md` 內 14 處指針方言零檢查。與 `DEF-101-587` 同型（搬到另一支檔就繞過守門），這次繞過的是**指針鎖** | P1 | 根層 `tools/` | fixed@R60 r3（Pkg-B）：兩支共用單一「具名治理文件」SSOT 再匯出，r3 併入稽核面。🔴 **據實記載：回報者的次級宣稱經複驗不成立**——SA 報「把 r3 加入稽核面後 rc=1、逐字指出兩筆失實居所」，Pkg-B 實測**不可重現**：r3 全部 14 處落在 code span／圍籬／術語提及例外內，加入稽核面後 rc=**0**、零誤紅。判為 **SA 側假陽性**（可能成因：沙箱只複製檔案片段導致圍籬狀態機行號錯位）。**缺陷本體（r3 不在稽核面）成立且已修，但「加進去會抓到兩筆真失實」不成立**，不得據此宣稱本修復回收了任何失實指針 |
| DEF-101-595 | 2026-07-30 | R60 r3 QA（`QA-R60R3-01`）＋Architect（`ARCH-R60R3-05`）二方獨立命中同一根因 | **並行假紅的第 4 個成因已確定**：`tools/fsm_runtime/tests/test_slv_generator.py` 用樹內固定共用暫存目錄。並行 2 次皆重現 WinError32／FileNotFoundError、隔離 2 次皆綠 | P2 | AISDLC_SDD 側 fsm_runtime 測試 | fixed@R60 r3（Pkg-C）：改 `tempfile.mkdtemp()` ＋ `shutil.rmtree(ignore_errors=True)`（只加 `missing_ok=True` 不算修好——那只讓競態不拋例外、資料仍互相污染）；v0.01 修 3 處、**v0.30 修 4 處**。修復前 v0.01 三輪 6 跑 **4 跑翻紅**（載具有鑑別力），修復後兩版各三輪 **12/12 全綠**。防復發鎖 `TestNoInTreeWritableTmpDir` 9 支落在既有檔（零新增檔），注入紅／實掃假陽性 0。🔴 **v0.01 是凍結基線，其測試檔自 2026-06-12 初始匯入後從未被改過，本輪是第一次**——主控裁決採納並已補登 `EVOLUTION_LOG`。🔴 **具名殘留：v0.02~v0.29 共 95 個同型站點未修**（§2 已機械確認不在任何自動閘門執行面內），C3 鎖已明文排除該區並寫明它們確實存在且未修 |
| DEF-101-609 | 2026-07-30 | R60 r3 **Architect 架構評估段** | 🔴 **複審方法論缺陷——把註解行讀成執行碼而導出錯誤架構前提**：Architect 主張「`AISDLC_SDD/scripts/ci-gate.ps1` 第 34-38 行已在跑 `python -m ...` ⇒ Python 在該時點必然可用」；Pkg-E 逐行實查該五行**全部是註解行**（首字元 `#`），真正的呼叫在第 50／55／62 行，且全在 `Find-GitBash`（第 22 行）**之後**、全在「找不到 Git Bash」才走的 fallback 分支內。注入實測（非讀碼推論）：PATH 抽掉 python 後 ci-gate 前導段逐行複刻**照跑且成功** | P3 | 根層複審紀律 | closed-by-decision@R60 r3：**四方複審者的證據也要被複驗**——本 repo 已有「多人獨立命中 ≠ 理由正確」的教訓，本筆是同一紀律在「單人＋讀碼推論」下的實例：**回報者的次級宣稱（架構前提）經複驗不成立**，但其**通則（逐案補不是消除平面）仍被採納**（見 `DEF-101-611`）。處置＝留痕並要求往後架構前提一律附「該行的實際內容」或注入實測；不另加機械守門 |
| DEF-101-601 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-03`） | **見 DEF-101-574（現居 archive_32）——該列引用不存在的識別字且語意相反**：該列寫 `_PS1_NARROWED_FORMS`，實際常數為 `test_local_ci_gate_shell_arg_parity.py:120` 的 `_GUARD_KNOWN_FALSE_RED_SAMPLES`；且語意寫反——會誤紅的是「**等價寫法**」，「窄化守門」設計上**應該**轉紅。該列卻自標「🔴 誠實劃界（實查、非轉述）」⇒ 該標籤本身不成立 | P2 | 根層缺陷帳本＋證據檔 | fixed@R60 r3：①文件側 Pkg-D 已修 `Evidence_r3.md:287`，並另抓到 SA 沒提的第二處同型錯誤；②`DEF-101-574` 本體**不就地改**——archive 逐字保全零刪除，**本列即為它的訂正列**。🔴 **流程教訓（本筆真正的重點）**：本輪 12 筆立帳有 **9 筆同輪即搬進 archive_32**，而 archive 零刪除 ⇒ **複審尚未複驗、該輪紀錄已凍結成永久史料**，錯誤無法就地訂正。**建議定為往後慣例：本輪立帳的條目在四方複審收斂之前不搬 archive**；`archive_33` 已照此辦理 |
| DEF-101-591 | 2026-07-30 | R60 r3 Architect 複審（`ARCH-R60R3-02`，P1 blocking；主控反駁關親測 CONFIRMED） | **Scan-H 必跑項 #4 依定義不可能完成**：`CrossPlatform_Scan_Dimensions.md:62` 訂「成對數與單邊數必須下降」，而**同輪** `ADR-XPLAT-002:411-413` 逐字證明「本 ADR 全部標的做完後它仍是同一組數字」；同檔 :57 又訂「缺一即該輪 Scan-H 未完成」 | P1 | 根層治理文件 | fixed@R60 r3（Pkg-D）：#4 拆為量測格／唯一通過判準格（改採 ADR §4.1 的 UEP）／「檔案成對數刻意不作判準」格，且只引章節指標、不複寫 §4.1 數字。🔴 **Pkg-D 主動擋掉一個沒人提的延遲同型缺陷**：UEP 有可辯護地板，若照 Architect 建議直接寫「UEP 必須下降」，觸底後 #4 又會依定義不可能完成＝同型 bug 延後數輪爆炸；故判準寫成「**達到 §4.1 為該輪訂的上限**」（觸底時上限即地板，可滿足） |
| DEF-101-597 | 2026-07-30 | R60 r3 QA 複審（`QA-R60R3-04`） | **`ONBOARDING.md` §9④ 措辭失實**：原文稱 `macos-compat-ci.yml` 因帳務停擺（`DEF-101-081`）「**永遠不會被觸發驗證**」；QA 以 `gh run list` 實查 HEAD `796c7a6`：**五支 workflow 全被觸發、全 completed/failure（3~11 秒）**，macOS smoke job 跑了 4 秒後才帳務失敗（run 30463834958）。後者意義是 main 上長期掛著一排紅燈——而那正是主控用來拒絕把 `--check-snapshot` 接進根層 unittest 閘門的理由（「會養成忽略紅燈的習慣」） | P3 | 根層 `ONBOARDING.md` | fixed@R60 r3（Pkg-F）：①§9④ 改為「會被觸發，但因帳務在數秒內失敗，macOS 步驟從未真正執行」；②凡寫「接 root-infra-ci 第 N 道」的句子逐句補注「該 workflow 現因帳務不執行」，避免把裝飾性接線算成守門數 |
| DEF-101-593 | 2026-07-30 | R60 r3 **四方全數獨立命中**（`QA-R60R3-02`／`SA-R60R3-04`／`SD-R60R3-02`／`ARCH-R60R3-01` 附帶項） | **`Spec.historical` 白名單零 stale 自檢**：runtime 注入死條目 → `--check` rc 仍為 0、零訊號。諷刺點＝同函式判準(1) 的錯誤訊息自己寫著「豁免表本身就是下一個 stale 站點」，判準(2) 卻設了一張 | P2 | 根層 `tools/sync_onboarding_baselines.py` | fixed@R60 r3（Pkg-A）：新增 `historical_problems()`，判準＝**leave-one-out**（拿掉一筆後受鎖行若不多出任何問題 ⇒ 該筆沒遮蔽任何東西）＋同值重複登記守門。注入死條目 rc 0→1 並指名該刪哪一筆，控制組 rc=0；修復前同一注入訊號數 ＝ 0。🔴 **接進生產路徑 `check()` 而非只放測試層** |
| DEF-101-599 | 2026-07-30 | R60 r3 Architect 複審（`ARCH-R60R3-04`） | **「R61 開輪即禁止新增鎖檔、只准合併」零機械強制**：全 repo grep 只命中 1 處註解 ＋ 帳本一格散文；以本輪自己訂的標準（「沒有鎖就只是散文」）衡量並未落地 | P2 | 根層 `tools/tests/` | fixed@R60 r3（Pkg-G）：`TestGuardFileCountShrinkOnlyRatchet` 6 支。設計＝**比工作樹 vs HEAD 而非寫死基線**＋**兩側都遞迴列舉**（避免複製 `DEF-101-594` 的漏法）；注入四情境紅綠正確。🔴 **刻意偏離任務書、經主控覆核採納**：計數面用 `test_*.py` 而非 `glob('*.py')`——否則 `DEF-101-561①` 指定的 R61 合併動作（抽共用 AST helper）會翻紅＝懲罰抽共用層 |
| DEF-101-611 | 2026-07-30 | R60 r3 Architect 架構評估段的減面建議，Pkg-E 判 NO-GO 並經主控覆核採納 | **`Find-GitBash` 判定單源化（PS 只枚舉候選、accept/reject 交 Python）本輪判 NO-GO**：除 `DEF-101-609` 那個錯誤前提外，另三條獨立架構理由——①失敗模式反轉（讓「偵測前置條件缺失」的偵測器自己依賴另一前置條件）；②**淨平面是增加**（PS 側判定只有 6 行純函式）；③殘餘風險已近零 | P3 | 根層 ADR | closed-by-decision@R60 r3：封存為 **ADR Phase 1-D ＋ §8 交棒項 11**。🔴 **Architect 的通則同時被採納**：ADR 新增 §4.5 **SDS（語意分歧面）** 判準，允許 ΔSDS<0 計分即使 ΔUEP=0 |
| DEF-101-598 | 2026-07-30 | R60 r3 Architect 複審（`ARCH-R60R3-03`）＋Pkg-E 獨立複量 | **護欄層行數（GLC）三處記載互相矛盾且全 stale**：`DEF-101-565` 一個值、`ADR-XPLAT-002` §4.3 另一個值、Architect 與 Pkg-E 各自實測得到第三個值。且 §4.3 自訂「唯一規則：同一 commit 前後各量一次」，而 §2 自陳基線取於工作樹髒 ⇒ **依它自己的規則，它自己的基線無效** | P2 | 根層 ADR ＋ 缺陷帳本 | fixed@R60 r3（Pkg-E 文件側／Pkg-F 帳本側）：ADR 側寫死數字改「現查值」，並訂正原規則寫法——原文被讀成「整棵樹必須乾淨」，正確判準是**量測面 `tools/tests/` 乾淨** |
| DEF-101-603 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-06`）；ARCH 與 SD 判定相反，主控裁決 | **例外 (丁) 開得比需要寬**：於治理文件注入「未加引號、非 code span、非圍籬」的散句 `立帳見主檔 R57 條目` → rc=0。現存唯一 (丁) 用例（`CrossPlatform_R60_Fix_Evidence.md:868`）經判讀合法，但形態級模糊存在 | P3 | 根層 `tools/archive_defect_log.py` | fixed@R60 r3（Pkg-B）：**主控採納 SA 的收窄**——(丁) 須落在同一行的「」或『』內。🔴 **相反意見在案**：ARCH 撤回疑慮、SD 以四發注入判定 (丁) **沒有**重開 `ARCH-R60-01③`；事實三方一致，分歧純在價值判斷 |
| DEF-101-592 | 2026-07-30 | R60 r3 Architect（`ARCH-R60R3-01`）＋SD（`SD-R60R3-01`）二方獨立命中 | **`prose_problems()` 只認 `R<N>=<M>` 字面形，同義中文散文全逸出**（`R60 為 756`／`本輪重釘 756`／`R60：756`／`round 3 實測 756` 舊判準皆 GREEN） | P2 | 根層 `tools/sync_onboarding_baselines.py` | fixed@R60 r3（Pkg-A）：判準拆為「主詞 × 連接」＋新增無輪號主詞的量測宣稱判準；六個逸出樣本舊判準全 GREEN → 新判準全 RED。🔴 **Pkg-A 另撿到四方全漏的第二筆同型逸出**（`loc` 受鎖行的 `20359`）：兩筆皆句層輪號標記的歷史敘述 ⇒ **登記而非刪散文** |
| DEF-101-600 | 2026-07-30 | R60 r3 Architect 複審（`ARCH-R60R3-06`），Pkg-D 裁決 | **「守門程式需要守門程式」已是護軌層主體**：`tools/tests` 中引用其他 `tools/` guard 模組者占九成上下，護欄層行數已超出被護生產碼 | P3 | 根層治理 | closed-by-decision@R60 r3（Pkg-D）：**降為報表項，不作 Scan-H 通過判準**——三個具名理由（定義不唯一且 repo 內無程式算它／分母是護欄總行數 ⇒ **獎勵多加鎖**的反向誘因／與 §4.3「刻意不設上限」正面衝突）逐字寫在 `CrossPlatform_Scan_Dimensions.md` 必跑項 #4 量測格 |
| DEF-101-590 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-02`，P2 blocking；主控反駁關親測 CONFIRMED） | **`check_defect_log_crossref.py:54`／`:104` 兩處寫死「帳本家族 32 檔」，實查 33**；且該句斷言的「表頭欄數全部同形」**只對帶表頭的那批成立** ⇒ 把只對部分檔成立的性質宣稱到全體，比數字過期更重一層。每跑一次 `--apply` 就再 stale 一次，違反本輪自己落地的 Scan-H 必跑項 #3 | P2 | 根層 `tools/` | fixed@R60 r3（Pkg-B）：兩處改為現查，性質宣稱同步收窄為「帶表頭的那批」 |
| DEF-101-605 | 2026-07-30 | R60 r3 SD 複審（`SD-R60R3-04`） | **`SD-R60-R2-07`／`SD-R60-R2-08` 在 `docs/06_quality/` 零命中**（其餘三方 01~06 全數有命中）⇒ `DEF-101-567` 宣稱的「四方 id 可追溯性對稱」在同一輪未達成。Pkg-F 實查：兩筆的落地**在程式碼側**且逐字帶 id——並非沒落地，是**沒有對應的帳本列可掛** | P2 | 根層缺陷帳本 | fixed@R60 r3（Pkg-F，採方案 (b)）：把 `DEF-101-567` 的修法敘述由「每一列狀態欄」改為「**帳本列或落地程式碼二擇一**，兩者皆須逐字帶 id」 |
| DEF-101-606 | 2026-07-30 | R60 r3 SD 複審（`SD-R60R3-05`） | **ADR 鎖 `_BARE_COUNT_RE` 量詞集合漏「版／檔／份／道／項／次」**，而 `ADR-XPLAT-002` L249 當下就有一個會 stale 的實例（框架版本數） | P4 | 根層 `tools/tests/` ＋ ADR | fixed@R60 r3（Pkg-G）：量詞集合加寬；**L249 改為不引數字**（版本數以 `FRAMEWORK_STATUS.md` 現查為準）——照裁決走，**沒有把舊數字改成新數字**。沙箱掃 `git show HEAD:` 原文：舊窄集合 0 命中（假綠復現）→ 加寬後 4 命中（含 L249）→ 修復後 0 |
| DEF-101-607 | 2026-07-30 | R60 r3 SD 複審（`SD-R60R3-06`） | **ID 上界「雙欄位造假」可觸達性已量化**：上界以下現查即有一批未使用號碼；空號＋不晚於上界列的日期 → GREEN（造假成立）、空號＋誠實日期 → RED。緩解＝必須連帶偽造帳本列，diff 看得見 | P4 | 根層 `tools/tests/` | fixed@R60 r3（Pkg-G）：選「加一支測試把空號算出來」而非「寫成以現查為準」。實作成**雙條件** `bool(free) == disclosed`——`if free: assert …` 在空號用光後退化成恆綠空測試 |
| DEF-101-602 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-05`，完整方言普查） | **裸「現居 archive_NN」（前方無「見」動詞）無任何判準覆蓋**：磁碟現況 0 處，注入失實宣稱 rc=0 零訊號。結構論證＝真正承載居所語意的 token 是「現居」，而它沒有「立帳見」那樣的 POINTER_VERB 硬要求 | P3（latent） | 根層 `tools/archive_defect_log.py` | fixed@R60 r3（Pkg-B）：裸「現居」另受對等硬要求（須跟得上可解析 DEF-ID 且居所與實況一致），`--check` 已把三類指針分列計數 |
| DEF-101-604 | 2026-07-30 | R60 r3 SA 複審（`SA-R60R3-07`） | **`partial` 是《格式定義》宣告的合法首詞卻無 `_STATUS_KEYWORDS` 分類器對應**，`_classify()` 回 `None`、落進 warning-only 軟出口 ⇒ `DEF-101-556` 要消滅的「只修一半被當成已修」並未消失，只是從「靜默算 fixed」搬到「靜默算含糊」 | P3 | 根層 `tools/check_defect_log_crossref.py` | fixed@R60 r3（Pkg-B）：補 `partial` 分類器＋硬斷言「每一個合法首詞都必須能被 `_classify()` 分類」（兩份程式常數互綁） |
| DEF-101-594 | 2026-07-30 | R60 r3 SD 複審（`SD-R60R3-03`） | **指紋觸發器四棵 glob 不對稱且無 WHY**：三棵 SDD 樹用非遞迴 `*.py`、AutoClaude 用 `**/*.py`。注入實測**新增子目錄檔→GREEN（漏）** | P3 | 根層 `tools/sync_onboarding_baselines.py` | fixed@R60 r3（Pkg-A）：四棵對齊 `**/*.py` 並附 WHY；沙箱注入子目錄檔舊 glob GREEN → 新 glob RED。表② 四格由 Pkg-F 於全包落地後以最終工作樹重新實測回填 |
| DEF-101-608 | 2026-07-30 | R60 r3 SD 複審（`SD-R60R3-07`） | **`DEF-101-566` 的撤回措辭仍留「觀察到 3 次 TOML 相關警告」這個同樣無載具的數字**——行號歸因已撤回，次數卻原封留著，而兩者的取證狀態完全相同 | P4 | 根層缺陷帳本 | fixed@R60 r3（Pkg-F）：`DEF-101-566` 狀態欄就地改為不引數字 |
