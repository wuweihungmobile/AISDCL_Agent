# AutoSDD Defect Log — Archive 61

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **5 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-874`、`DEF-101-873`、`DEF-101-877`、`DEF-101-875`、`DEF-101-869`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：R78 開場歸檔：本輪四方複審預期大量登帳，先釋出 bytes 餘裕
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-874 | 2026-08-06 | R77 收尾者實跑基線回填時被鎖攔下 | 本輪新增的 `tools/probe/`（量測配方的唯一可執行家，M6）有回歸鎖消費其中的 session 稽核器，但兩支 compat-CI 的 `paths:` 都沒列它 ⇒ 只改該目錄時其回歸鎖結構上不會跑（`DEF-101-042` 同構）。**本輪自造、落地當回合即被 `test_ci_paths_cover_root_consumers.py` 攔下** | P2 | 兩支 compat-CI 的 push 與 pull_request 兩組 `paths:` 各補一筆 | fixed@R77：補列後該鎖 47 passed rc=0。🔴 **補列動作本身觸發了 Scan-H⑥ 第五次互鎖**：另一道鎖以「workflow 檔是否提及該稽核器的檔名」判斷它有沒有被接成閘門步驟（逐字稿是本機資料，接成閘門會在別台機器恆紅），而收尾者在 paths 的說明註解裡寫了那個檔名字面 ⇒ 當場判紅。改法＝註解改成不含該字面的描述（鎖的判準沒錯，錯的是把會被解析的字串寫在會被解析的位置——本 repo 第五次）。兩鎖現皆綠：60 passed 與 47 passed。🔴 教訓＝新增一層目錄的成本不在檔案本身，在它需要被登記幾次，而登記動作自己也可能違反另一道鎖 |
| DEF-101-873 | 2026-08-06 | R77 Scan-H 必跑項③ 複驗（散文寫死可機械算出的數字） | 掃描指本工具兩處散文數字 stale，**逐處複驗後一真一偽**：(甲)「11 處指針當場失實」是 **R60 那次事故的時點計數**，卻被拿去跟「家族現有幾處這種指針」的族群量相比＝不是同一個量測；(乙) 另一處舉的兩則時態性敘述樣本複驗**仍在且仍零命中**，報 stale 是因為只掃主檔、而樣本住 archive | P2 | `tools/archive_defect_log.py` 訂正措辭並補機械物 | fixed@R77：(甲) 改寫成明示時點，族群量一律以 `--check` 現場印出的指針數為準；(乙) 樣本補上所在檔座標並升級為 `TestTemporalNarrativeSamplesStayUncaught`（仍在＋仍零命中＋放寬樣式必須抓得到，第三條防空斷言）。注入：樣本字串改一字 → 紅，還原後綠。🔴 教訓＝一句對的話因為缺座標被讀成錯的，下次就可能被「順手訂正」成真的錯的 |
| DEF-101-877 | 2026-08-06 | R77 收尾者以 act 真跑 root-infra（掌舵者指定的雲端替代通道） | 本輪新增的全庫 job 盤點把三項結構事實（有無該 runner／有無 services:／有無該事件觸發）都判對了，卻把結論印成「✅ 本機 act 可達」——**實測真跑到第 3 個 step 即 rc=127**：該步需要 pwsh，而 act 預設映像沒裝，GitHub 的 ubuntu runner 自帶。⇒ 判準判得到結構、判不到「映像裡有沒有那支工具」，而措辭把「解析得動」講成「驗過了」 | P2 | `AutoClaude/tools/run_act_core.py` 盤點輸出 | fixed@R77：標記改為「🟡 act 可解析」並在表尾固定印一行邊界說明（結構可達≠跑得完，唯一憑證是該次執行自己的逐步輸出）。🔴 教訓＝dry-run 全綠與真跑 rc=127 同時成立，前者只證明 YAML 寫對了 |
| DEF-101-875 | 2026-08-06 | R77 收尾者接手中斷的修復包 | 護欄層檔數棘輪換行數棘輪那一包，判準函式與逃逸偵測都已落地，但**凍結基準表本身尚未建立就因額度中斷** ⇒ 引用它的測試全數 NameError、機制蓋好沒接電。同型風險：agent 因外部錯誤中斷時磁碟副作用已落地而回報遺失，「失敗」不等於零產出 | P1 | `tools/tests/test_adr_xplat001_c1c2_lock.py` | fixed@R77：收尾者補齊逐檔基準表（當回合實測、零加減推算），刪除被取代的舊檔數棘輪測試類，並補一支**接手證明**測試——移除一道鎖若不同時證明接手者有牙，等於淨損一道防護。注入實測：人為製造淨成長即紅，收尾者自己隨後就被它抓到一次 |
| DEF-101-869 | 2026-08-06 | R77 Scan-C×G 交叉複驗 R76 的 `windows-nightly-full` 診斷 | 見 `DEF-101-834`（現居 `archive_60`）的診斷**把兩個 step 對調**：API 逐步取 run 30803941764，`bootstrap.ps1`＋`dev_start.ps1` 那步 success（log 有完成訊息＝整支跑完），ParserError 屬 `install_post_commit.ps1` 那步；「自 R48 起四輪未真跑」亦不符（R48 後該排程只有 1 次真把 runner 拉起來） | P1 | 主檔新列承載訂正（archive 為零刪除史料，不就地改寫） | fixed@R77：本列即訂正載體。實害＝`install_post_commit.ps1` 的 PS 5.1 原生路徑（`DEF-101-389` 的唯一真機驗證點）**一次都沒跑完過**卻被記成「偶然通過」；本機可驗的回歸鎖設計見 `DEF-101-866` (a) |
