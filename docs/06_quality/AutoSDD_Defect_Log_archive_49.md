# AutoSDD Defect Log — Archive 49

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **12 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-753`、`DEF-101-754`、`DEF-101-751`、`DEF-101-750`、`DEF-101-749`、`DEF-101-742`、`DEF-101-744`、`DEF-101-741`、`DEF-101-737`、`DEF-101-738`、`DEF-101-743`、`DEF-101-745`
>
> **本次操作備註**：R70：CI 修復輪期間帳本逼近 262144 硬線（動工前餘裕 421 bytes），依 DEF-101-676 判準搬遷 12 筆已結列
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-753 | 2026-08-02 | **R69 收輪 push（`edd5388`）後由雲端 `windows-compat-ci` 抓到**——本機 macOS 全套實跑全綠、R69 四輪四方複審亦未發現。同 run 的 root-infra-ci／aisdlc-sdd-ci／AutoClaude CI／macos-compat-ci 皆 success，唯 windows-compat-ci failure（run `30739865214`，`Windows smoke` job 第 6 步 `FAILED (failures=3, skipped=52)`） | 🔴 **測試把裸名 `"bash"` 當 argv[0] 交給 `subprocess`，於是 Windows 上跑的根本不是受測腳本，而是 WSL 佔位版**：`tools/tests/test_smoke_ci_sync.py:1098`（修前）`def _run(self, argv, shell: str = "bash")` ＋ `subprocess.run([shell, str(_SH), *argv])`。實測輸出即 `AssertionError: 1 != 2 : W^@i^@n^@d^@o^@w^@s^@ ^@S^@u^@b^@s^@y^@s^@t^@e^@m^@ …`（**UTF-16LE** 的 `Windows Subsystem for Linux has no installed distributions.`），`macos_smoke_local.sh` **一行都沒被執行**，三支測試卻據此斷言「腳本回了非預期 rc」＝**歸因完全錯誤的紅燈**。**根因比「PATH 順序讓 WSL 勝出」更硬**：Windows 上 `subprocess` 以 `CreateProcess(lpApplicationName=NULL)` 解析裸名，其搜尋順序為「應用程式目錄 → 當前目錄 → **System32** → Windows 目錄 → **PATH**」——System32 排在 PATH **之前**，故只要 argv[0] 是裸名，`C:\Windows\System32\bash.exe` **必定**先命中，PATH 上有沒有 Git Bash、排多前面都無關。**同一次 CI run 內即有對照組坐實此機制**：`test_dev_start.TestPickPythonGeMin` 用 `shutil.which("bash")`（只查 PATH）⇒ 取得真 Git Bash、全數通過並未出現在 skip 明細；`test_smoke_ci_sync` 用字面值 ⇒ 取得 WSL、三支全紅——**同一台機器、同一個 PATH，兩種解析路徑結果相反**，排除「PATH 上沒有 Git Bash」這個解釋。全樹掃描另查出同型站點 4 處（見分流欄）＋ `tools/tests/test_macos_smoke_skip_honesty.py:75`（五份 `_usable_bash()` 鏡射中唯一漏 System32 段排除的一份，目前僅因「WSL 無發行版時驗活會 rc=1」而 fail-closed **僥倖**未出事——一旦機器裝了發行版即失效） | P1 | `tools/tests/test_smoke_ci_sync.py`（本體）／`tools/tests/test_dev_start.py:4660`／`AISDLC_SDD/scripts/tests/test_install_post_commit_sh_windowsapps_guard.py`／`AISDLC_SDD/scripts/tests/test_install_post_commit_windowsapps_guard.py`／`AutoClaude/tests/integration/fixtures/fk_staging_1m_wrapper.py`／`tools/tests/_platform_helpers.py`（新 SSOT）／`tools/tests/test_bash_probe_spec_contract.py`（新鎖） | fixed@R70：**① 收斂 SSOT**——`tools/tests/` 內原有**兩份 fixture 用途**的 bash 探測複本（`test_bash_probe_spec_contract._probe_a_real_usable_bash_for_fixture` 與 `test_macos_smoke_skip_honesty._usable_bash`）且**排除規則不一致**，而本體檔連探測都沒有；三處收斂為 `_platform_helpers.usable_bash_for_fixture()`（git 相鄰優先 ＋ System32 整段排除 ＋ `bash_probe_spec.PROBE_CMD` coreutils 驗活，回傳**絕對路徑**）。比照 R57 `strip_ps_comments` 判例。**刻意不收斂**`test_pre_push_dispatcher`／`test_git_hooks_install_common` 兩份——那是生產端探針的獨立重寫回歸鎖，獨立性即鑑別力（見 `tools/lib/bash_probe_spec.py` docstring）。**② 修同型站點**：`test_dev_start.py` 改用新 SSOT；兩支 AISDLC_SDD 測試改用同目錄既有的 `bash_probe.usable_bash()`（該目錄其餘四支早已如此，這兩支是僅存的例外，其中 `test_install_post_commit_windowsapps_guard.py` 的 `pytestmark` 只擋 `_PWSH is None`＝在 Windows runner 上**必跑**，且其 skipif reason 逐字寫著「Windows 上可由 Git-Bash 提供」，坐實意圖與實作不符）；`fk_staging_1m_wrapper.py` 改用根層 `integration_gate_core.find_git_bash()`（修前 `_has_pg_environment()` 會把「只有 WSL 佔位版」誤判為環境齊備 ⇒ 回報 `failed` 而非誠實的 `skipped`）。**③ 補機械鎖（本機 macOS 即可判紅，不必等 Windows CI）**：`test_bash_probe_spec_contract.py` 新增兩類——(a) `TestWslStubIsNeverAcceptedAsRealBash`：注入一支**驗活會成功**但住在 `System32` 段的假 bash，斷言 SSOT 必須以**路徑規則**拒絕它（刻意讓 stub 驗活成功＝把上述「僥倖」拿掉，否則鎖到的不是 System32 規則；另有正控證明同一支 stub 放在非 System32 目錄時會被接受）；(b) `TestNoBareBashInvocationInToolsTests`：AST 掃 `tools/tests`／`AISDLC_SDD/scripts/tests`／`AutoClaude/tests` 三棵樹，禁止裸 bash/sh 當 argv[0]，並**追蹤三種間接形態**（參數預設值、模組變數綁 `shutil.which("bash")`、整條 argv 變數）——只認 `run(["bash", …])` 的掃描器對本案原始形態全盲。**修復前紅綠實測**：對 `edd5388` 工作樹該鎖判紅並逐一指名 11 處（本體 ＋ 上述 4 同型站點 ＋ 6 處 POSIX-only 合法站點）；全部處置後 `Ran 16 tests … OK (skipped=4)`。合法站點走**就地標記** `# bash-ok: <理由>`（不用行號登記表——行號會漂移）。**掃描面下限釘選** `_MIN_SCANNED_FILES=120` 防 0 命中假綠。**誠實劃界**：本鎖只看 argv[0]，看不到「把 bash 呼叫包進被執行的 .sh／composite action」那種形態；`tools/git-hooks/` 三支 dispatcher 內的裸 `bash` 因由 Git Bash 自身拉起（子行程 PATH 已前置 `/usr/bin`）判為無風險，未納管 |
| DEF-101-754 | 2026-08-02 | **R70 修 `DEF-101-753` 的 commit（`d2758b5`）push 後，由雲端 `windows-compat-ci` 抓到**——同 run 的 root-infra-ci／macos-compat-ci／AutoClaude CI／aisdlc-sdd-ci 皆 success，唯 windows-compat-ci failure（run `30742107259`，`Ran 1592 tests … FAILED (errors=1, skipped=52)`）。`DEF-101-753` 的三支紅燈確認消失，本筆是**新病** | 🔴 **`improving_103` §9.5 那條新規則落地的第一個實例，自己就違反了該規則**：§9.5 訂立「凡只在某平台才成立的判斷，回歸鎖必須有一條能在開發者本機重現該平台語意的路徑」，而依它造出來的 `TestWslStubIsNeverAcceptedAsRealBash` **只在 macOS 跑得動、在 Windows 上直接 error**。修前 `_make_stub()` 一律寫 `#!/bin/sh` shebang 腳本，Windows 上只把檔名改成 `bash.exe`；正控段 `subprocess.run([str(stub), "-c", PROBE_CMD])` 於是撞 `OSError: [WinError 216] This version of %1 is not compatible with the version of Windows you're running`（`test_bash_probe_spec_contract.py:518`）——`CreateProcess` **不認 shebang**，只認 PE 映像與 PATHEXT 副檔名，把腳本命名成 `.exe` 等於叫它去載入一個壞掉的 PE 映像。**規則的缺口是對偶方向**：§9.5 只要求「本機重現得了目標平台語意」，沒要求「這道鎖在目標平台上真的執行得起來」，於是規則只活在文件裡，沒有任何機械物在看它。**同一教訓 repo 內早有判例**：`tools/tests/test_dev_start.py:1069` 的 `_make_fake_interpreter()` 檔內註解逐字寫著「R3 QA 發現：shebang 腳本（#!/bin/sh）只在 POSIX 上可執行，Windows 上 …會撞 WinError 193（非合法 PE 格式）」——**R3 就學過的事，R70 在另一支檔上原地復發**，因為那個教訓當時只落成一段註解，沒有任何跨檔的機械物 | P1 | `tools/tests/test_bash_probe_spec_contract.py`（`_STUB_FORMS` ＋ `TestWslStubIsNeverAcceptedAsRealBash` ＋ 新 meta 鎖）／`docs/04_planning/AutoSDD_improving_103.md` §9.5（規則本文） | fixed@R70：**① stub 形態依 `os.name` 分派**——新增 `_STUB_FORMS`（`posix` → `("bash", shebang 腳本, "\n")`／`nt` → `("bash.cmd", 批次檔, "\r\n")`），兩平台皆為**真執行**，不用 `skipIf` 把 Windows 跳掉（跳掉正是這道鎖要防的病）。`.cmd` 沿用 repo 既有判例（`test_bootstrap_ps1.py`／`test_windowsapps_guard_cross_consistency.py` 的「`.cmd` 假直譯器」），差別是本處由 `CreateProcess` 對批次檔的隱式 `cmd.exe /c` 代跑拉起，而非 PowerShell。批次檔內文刻意**全 ASCII**（runner console code page 非 UTF-8）＋ **CRLF**（cmd.exe 對純 LF 批次檔行為在部分構造下未定義）。**② 修 Windows 上必假紅的比對**：正控第三段 `assertEqual(resolve(...), str(stub))` 改走 `os.path.normcase` ——Windows 的 `shutil.which()` 回的是 PATHEXT 登記的**大寫**副檔名（`bash.CMD`），與磁碟上的 `bash.cmd` 只差大小寫，直接 `==` 在 Windows 上必紅（POSIX 上 `normcase` 是 identity，鑑別力不變）。**③ 補 meta 鎖 `TestStubFormIsLaunchableOnItsOwnPlatform`（本筆的主要價值，2 支）**：把「某形態能不能被某平台啟動」變成**任何平台都可判定**的斷言——(a) 逐形態驗前提：nt 形態副檔名須在 PATHEXT 可啟動清單內、內文**不得**以 `#!` 開頭、換行須 CRLF、內文須全 ASCII；posix 形態須有 shebang 且無副檔名；(b) 以 `mock.patch.object(sys, "platform", "win32")` ＋ `PATHEXT` 驅動**真正的** `shutil.which()`（非重寫一份其邏輯）證明 Windows 形態仍在 PATHEXT 解析範圍內——否則 `usable_bash_for_fixture()` 回 `None`，`test_system32_stub_is_rejected` 的 `assertIsNone` 會因**根本沒有候選**而通過＝假綠，主判準一次都沒被執行。**macOS 本機三向缺陷注入實測（全紅、還原全綠）**：① 還原修前形態 `("bash.exe", _WSL_STUB_POSIX, "\n")` ⇒ `FAILED (failures=1)`／`AssertionError: True is not false : Windows 形態不可是 shebang 腳本…`；② 改名 `bash.sh` ⇒ `FAILED (failures=2)`／`AssertionError: unexpectedly None : Windows PATHEXT 語意下 bash 找不到 'bash.sh'…`；③ 改 LF 換行 ⇒ `FAILED (failures=1)`／CRLF 訊息；還原後 `Ran 2 tests … OK`。修復後全檔 `Ran 18 tests … OK (skipped=4)`（修前 16）。**🔴 誠實劃界（本機 macOS 零 Windows 真機，不得含糊）**：本機能證明的是「Windows 形態的**形狀與可被 `shutil.which()` 找到**」；**不能**證明的是「`CreateProcess` 真的把該 `.cmd` 交給 cmd.exe 跑起來、且 `PROBE_CMD` 中的 `&&` 在 cmd.exe 的引號解析下不被切成第二條指令」——後者只有 Windows CI 能驗。該段若失敗，正控的 `assertEqual(proc.returncode, 0, proc.stderr)` 會帶著 stderr **fail loud**，不會靜默 |
| DEF-101-751 | 2026-08-02 | R70（R69 已 commit `6a453b8` 後、`git push` 被 pre-push hook 阻斷；唯一紅燈） | **R17 鎖住的不變量已被 R69 自己的架構決策推翻，而鎖與決策沒有任何互相知會的機制**：`tools/tests/test_platform_utils_dedup.py::test_platform_judgment_helpers_defined_only_in_platform_utils`（R17／`DEF-101-231`）逐字要求 `is_windows`／`os_label`／`venv_python_path` 在**全 repo 只有一個定義點**＝`tools/lib/platform_utils.py`；R69 的 `ADR-XPLAT-003` 新增 `AutoClaude/autoclaude/utils/platform_caps.py`，它必須自帶 `is_windows()`／`is_macos()`。實測紅燈：`AssertionError: Lists differ: ['AutoClaude/autoclaude/utils/platform_caps.py', 'tools/lib/platform_utils.py'] != ['tools/lib/platform_utils.py']`。**根因不是誰寫錯，是「全 repo 一處」這個不變量從一開始就把射程訂錯**：`autoclaude` 是可獨立 pip 安裝的套件（`AutoClaude/pyproject.toml`；hatchling 未宣告 `[tool.hatch.build]`，預設只打包同名的 `autoclaude/`），根層 `tools/lib/` 不在 wheel 內 ⇒ 純 pip 安裝、脫離 monorepo checkout 時 import 必然 `ModuleNotFoundError`；反方向讓 `tools/lib/` 去 import `autoclaude` 會把 pydantic 拉進 hook／AISDLC_SDD CI 的 import graph，正是 R68 讓 `aisdlc-sdd-ci` 恆紅的形態。**本 repo 對同款情形早有判例與解法**：`DEF-101-295`（R33 Architect 裁決，逐字寫在 `AutoClaude/autoclaude/utils/logger.py` 檔內註解）對 NTFS 判準採「跨孤島各留一份 ＋ 以 parity 鎖釘住一致性」，本筆與它同構 | P1 | `tools/tests/test_platform_utils_dedup.py`（鎖）＋ `docs/04_planning/ADR/ADR-XPLAT-003-…md` §7（決策依據） | fixed@R70：不變量改寫為「**每一個相依孤島內，各 helper 只准有一個定義點**」——`_HELPER_ISLAND_SSOT` 宣告`root-shared`（SSOT＝`tools/lib/platform_utils.py`）與 `autoclaude-package`（SSOT＝`platform_caps.py`）兩島，純函式 `island_violations()` 對「島內第二個定義點」與「某島出現它不該有的 helper」兩種形態各自判紅；順帶把 `is_macos` 納入掃描（原本漏掉，實測全 repo 亦恰為兩島各一）。🔴 **刻意不做白名單**：白名單再加一筆條目即放行，孤島不變量則不論加在哪一島都紅。**雙向注入實測（真檔上機，非合成）**：① 根層孤島 `tools/_probe_root_island.py` ⇒ `FAILED (failures=1)`／`island_violations` 回 `['tools/_probe_root_island.py']`；刪除 ⇒ `Ran 16 tests … OK`；② AutoClaude 孤島 `AutoClaude/autoclaude/utils/_probe_caps_island.py` ⇒ `FAILED (failures=1)`／`Lists differ: ['AutoClaude/autoclaude/utils/_probe_caps_island.py'] != []`；刪除 ⇒ `Ran 16 tests … OK`。孤島邊界另由 `test_autoclaude_package_island_cannot_reach_root_tools_lib`（AST，動態讀 `tools/lib/*.py` 模組名、取到 <5 支即 fail-loud）機械看守——`autoclaude/` 哪天真的 import 了根層 `tools/lib/`，前提即不成立、本鎖轉紅逼人重新決策；另有 `TestIslandInvariantIsNotAToothlessWhitelist` 五支合成自證。ADR-XPLAT-003 §7 記錄「為何 AutoClaude 需要自己一份平台判斷、而這不違反 DRY」 |
| DEF-101-750 | 2026-08-02 | R69 終審 R3（QA：對本輪**新造**的鎖做變異測試） | **本輪新立的 ruff 執行者鎖是假鎖，錯誤訊息的 echo 即可滿足**：`tools/tests/test_root_infra_parity.py::TestRootToolsRuffHasExecutors` 以 `assertIn("ruff check tools/", 非註解行全文)` 判定「有沒有人真的執行 ruff」，而 `tools/git-hooks/pre-push` 快層第 ④ 段的失敗處置訊息 `echo "… ruff check tools/ 失敗 …"` 自己就含該子字串。缺陷注入實測：刪掉唯一的執行行 `if ! ruff check tools/ --no-cache …`、只留該 echo ⇒ 該類仍 **`Ran 3 tests … OK`**。與同輪 `DEF-101-743`（nightly 哨兵鎖可被處置指令 echo 滿足）是**同型復發**，且發生在同一波修復內 | P2 | `tools/tests/test_root_infra_parity.py`（雲端與本地兩道執行者鎖） | fixed@R69（收尾包）：改為純函式 `_runs_ruff_over_root_tools()` 兩層判定——先把字串字面值挖空（處置訊息只活在引號內），再要求 token 出現在**命令位置**（行首／`;`／`&&`／`\|`／`!`／`then`／`else`／`do`／`run:` 之後）；兩道執行者鎖同步改用它（雲端側原本有同一個縫，一併補）。另新增負向自證 `test_the_executor_lock_is_not_satisfied_by_an_echo`，在測試內就地注入「只留 echo」樣本並斷言必紅——防的是下一次有人把它改回 `assertIn`。雙向實測：刪執行行 ⇒ `FAILED (failures=2)`（本體鎖＋自證鎖正控同時紅）；還原 ⇒ `Ran 4 tests … OK`。判準邊界（誠實劃界）：純文字掃描，看不到「把執行包進被呼叫的外部腳本／composite action」那種形態 |
| DEF-101-749 | 2026-08-02 | R69 終審 R3（三位審查員獨立命中同一處） | **CI step 名稱把兩句假話宣告成規格，並被機械鎖鎖死**：`macos-compat-ci.yml` 與 `windows-compat-ci.yml` 的「tools/tests 外部工具相依」step 名稱逐字宣稱「清單 SSOT＝`tools/run_root_unittests.py` 的 `_EXTERNAL_TOOL_PREREQS`」與「漏裝時該 runner 會 fail-fast 指路」。實測兩句皆不成立：`grep -c _EXTERNAL_TOOL_PREREQS tools/run_root_unittests.py` ＝ **0**（真實 SSOT 在 `tools/tests/test_run_root_unittests.py:987`），`main()` 內零外部工具檢查（第四波原想做 runner 開場 fail-fast，撞 `check_loc_budget` SPECIAL_FILES 棘輪而**撤回**，該撤回的誠實劃界註解就寫在同一個 commit 的 `_EXTERNAL_TOOL_PREREQS` 檔頭 ⇒ 同一 commit 內自我矛盾）。更糟：兩句已被 `test_smoke_ci_sync.py` 的 `_CI_STEP_LOCAL_CARRIER` 登記表逐字鎖住 ⇒ **假話被鎖成規格**，改對反而變紅 | P2 | 兩支 compat-CI workflow ＋ `tools/tests/test_smoke_ci_sync.py` 登記表 | fixed@R69（收尾包）：step 名稱改為「清單 SSOT＝`tools/tests/test_run_root_unittests.py` 的 `_EXTERNAL_TOOL_PREREQS`；runner 無此層 fail-fast，漏裝時由 `ExternalToolPrereqDeclarationTest` 多紅一支點名」，登記表兩處 key 同步。負向控制實測：只改 workflow 不改登記表 ⇒ `test_smoke_ci_sync` `FAILED (failures=2)`；兩側同步後 `Ran 22 tests … OK` |
| DEF-101-742 | 2026-08-02 | R69 第四波（macOS 系統 bash 3.2.57 `/bin/bash` 直接執行 dispatcher） | **全形括號緊接變數名，在 bash 3.2 ＋ `set -u` 下整支 hook 當場中止**：`tools/git-hooks/pre-push:152` 的 `"…找不到 $SDD_CI_YML（根層消費檔清單來源…"` —— 3.2 會把全形左括號的位元組併進變數名 ⇒ `SDD_CI_YML<亂碼>: unbound variable`、rc=1，**該行之後的所有 leg 都不再執行**（實測逐字重現）。bash 5.x 名稱解析不同、`bash -n` 語法合法，兩者都攔不到；`macos-compat-ci` 雖有「以 /bin/bash 3.2 直接執行 dispatcher」步驟，但**走不到這個分支**（要 `aisdlc-sdd-ci.yml` 缺席才觸發）⇒ 這條路徑零機械訊號 | P3 | 根層護欄層（`tools/git-hooks/pre-push`） | fixed@R69：改 `${SDD_CI_YML}（`。同型寫法經全 repo 掃描僅此一處。**補上機械守門**（不只修那一行）：`tools/tests/test_bash32_compat.py` 新增純函式 `fullwidth_glued_var_problems()` ＋ `TestFullwidthGluedVariableName`（3 支）。**紅綠實測**：把修前形態注回**真實檔案**後該鎖 rc=1 並逐字指名 `tools/git-hooks/pre-push:152`，還原後 3 passed |
| DEF-101-744 | 2026-08-02 | R69 第四波（`ruff check` 冷 cache 輸出逐行覆核） | **鎖以整檔自我豁免掩護真缺陷，且工具說出口的話全 repo 零消費者**：`tools/tests/test_no_invalid_escape_sequences.py` 的新 noqa 形態鎖對**自己整檔**豁免，於是該檔真的留著壞 noqa 指令（＝那幾行其實根本沒被豁免）；`ruff check` 每次冷 cache 印 3 條 `Invalid # noqa directive` **而 rc=0**，全 repo 零消費者 ⇒ 整輪沒人發現 | P2 | 根層護欄層（`tools/tests/`／`.github/workflows/root-infra-ci.yml`／`tools/git-hooks/pre-push`） | fixed@R69：(1) 拆除整檔自我豁免（樣本改 `_HASH` 動態拼接、散文不再寫出可被解析的指令）；(2) **讓 warning 有消費者**——`root-infra-ci.yml` 第 16 道與 `pre-push` ④ 均改為 `ruff check tools/ --no-cache` 並在 stderr 出現 `^warning:` 時 `exit 1`。🔴 `--no-cache` **不可省**（實測）：該 warning **只在冷 cache 時印**，不帶它的話這道的鑑別力取決於「這台機器剛好有沒有 cache」。教訓＝**只看 rc 等於自願對工具說出口的話裝作沒聽見** |
| DEF-101-741 | 2026-08-02 | R69 第四波（AST 鎖鑑別力自檢：把鎖自己當被測物） | **平台判斷 AST 鎖可被 import 別名整支繞過，而 ADR 宣稱「一定被抓到」**：`AutoClaude/tests/test_evaluator_kill_tree.py` 第 4 節的平台／收殺探針逐字比對 `ast.Name.id`，於是 `import sys as _s`／`from os import name`／`import platform as _p`／`import os as _o` 四種形態**皆整支繞過**（實測：放進樣本檔後 4 支鎖全綠、零訊號），而 `ADR-XPLAT-003` §4 當時宣稱該類寫法「一定被抓到」——**鎖的射程與 ADR 的宣稱不一致，且不一致的方向是高估** | P2 | AutoClaude 測試層 ＋ `docs/04_planning/ADR/ADR-XPLAT-003` | fixed@R69：探針補上 **import 別名解析**（追 `import X as Y` 的綁定）與 **from-import 直取符號**判違規；新增 3 支探針自證測試（四種繞過形態注入後必紅）。ADR §4 的過頭宣稱同步訂正為**可逐條指認的射程**，並明寫仍抓不到 `importlib`／`getattr` 動態取用——**誠實劃界勝過看起來完備** |
| DEF-101-737 | 2026-08-02 | R69 第四波（跨平台鎖自檢包對新增測試做 Windows 語意複核） | 🔴 **R69 自己新增的測試在 Windows 上必紅，且其中一支是假鎖**：`tools/tests/test_dev_start.py::TestPy39PreludeStaticScan._scoped_sources` 以 `str(path.relative_to(...))` 當 dict key，Windows 上鍵為 `tools\\dev_start.py` ⇒ (a) `assertIn("tools/dev_start.py", scoped)` 於 `windows-compat-ci` **確定性失敗**；(b) `assertNotIn("tools/lib/ci_liveness.py", scoped)` 在 Windows **恆真通過＝假鎖**——而那一句正是用來保證 `ci_liveness` 不落入 prelude 射程的鎖，它在唯一有意義的平台上失去鑑別力。同型站點 `tools/tests/test_find_git_bash_parity.py:1106` | P1 | 根層護欄層（`tools/tests/`） | fixed@R69：兩處改用 `.as_posix()`（唯一跨平台穩定的字串化）；同型站點由新增判準機械掃出而非人工搜尋（見 `DEF-101-738`）。**本筆與 `/elsewhere` 家族同族、只差相對/絕對**：絕對路徑形態早有鎖，相對路徑形態的射程當時是零 |
| DEF-101-738 | 2026-08-02 | R69 第四波（回頭檢查「為什麼上一筆 P1 能溜過既有鎖」） | **鎖自己的漏洞**：R69 新立的 `TestNoPosixAbsPathLiteralInAsserts` 正則要求路徑字面值以 `/` 開頭 ⇒ **結構上抓不到相對路徑形態**，這正是 `DEF-101-737` 溜過去的原因。另一個量測面：該鎖對 `AutoClaude/tests` 的射程僅約 0.6% | P2 | 根層護欄層（`tools/tests/`） | fixed@R69：補第四道判準 `scan_path_str_identity`——(4a) **產出側**：`str(<Path 產物>)` 被當識別鍵；(4b) **斷言側**：路徑字面值 vs 同句 Path 產物，**涵蓋 pytest 裸 assert**（不只 `assertIn/assertNotIn`）。**擴面取捨的實測資料點**（供未來輪判斷是否再擴，刻意留在帳本）：十一棵掃描根的裸 assert 共 9705 個；過寬近似判準套用命中 41 筆**全為噪音**（故維持不擴），窄化後判準套用命中 0 筆（**零成本擴面**，不製造存量豁免） |
| DEF-101-743 | 2026-08-02 | R69 第四波（哨兵鎖鑑別力自檢） | **哨兵鎖被「處置指令的 echo 文字」滿足 ⇒ 真刪除零訊號**：`tools/tests/test_workflow_permission_concurrency_lock.py::TestRootInfraNightlyStalenessSentinel` 原對整段非註解文字做 `assertIn(檔名)`，而該段處置指令的 echo（`gh workflow run windows-compat-ci.yml`）本身就含該檔名 ⇒ 把 `windows-compat-ci.yml` 從 `root-infra-ci.yml` 的 `for wf in …` 查詢迴圈**刪掉之後 9 支鎖全綠**（實測） | P2 | 根層護欄層（`tools/tests/`） | fixed@R69：改為解析**被迭代的清單本身**（`_QUERY_LOOP_RE`），且迴圈數 != 1 亦紅（防「再開一個迴圈繞過」）。教訓＝**對整段文字做 `assertIn` 等於把說明文字也當成實作**，鎖的判定面必須落在被執行的結構上 |
| DEF-101-745 | 2026-08-02 | R69 第四波（ADR 量測 token 普查，同型第四度） | **ADR 寫死多波次輪的中途值，且與自己引用的規則直接矛盾**：`ADR-XPLAT-002` §1 的護欄層淨增常數 `+2294` 是多波次輪的**中途值**，用 §4.3.1 自己給的指令現查複現不出來；而它指向的 §4.3.1 明寫「刻意不寫死常數」、§8 表頭規則 3 亦然 ⇒ **同一份文件自己違反自己引用的規則** | P3 | `docs/04_planning/ADR/ADR-XPLAT-002` | fixed@R69：改為**符號級結論**（Δ生產碼=0 ∧ Δ護欄層>0 ⇒ 比例必然惡化）＋附取值指令，不再登載可現查的數字。此為 `DEF-101-713` 家族（寫死當場可現查的量測 token＝製造無看守的 stale 站點）在 ADR 上的第四度復發 |
