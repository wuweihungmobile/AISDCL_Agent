# AutoSDD Defect Log — Archive 41

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **16 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-695`、`DEF-101-690`、`DEF-101-697`、`DEF-101-692`、`DEF-101-694`、`DEF-101-696`、`DEF-101-688`、`DEF-101-678`、`DEF-101-686`、`DEF-101-685`、`DEF-101-691`、`DEF-101-687`、`DEF-101-683`、`DEF-101-679`、`DEF-101-682`、`DEF-101-681`
>
> **本次操作備註**：R67 round 2 四方複審修復列入帳後的容量輪替（EVIDENCE 包）：主檔逼近 262144 硬閘，已結列外移；帶交棒字樣與 open/partial 列一律留主檔
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-695 | 2026-08-01 | R67 round 2 四方複審（SA-R67-02＋SD-R67-01＋QA-R67-01，三方交叉命中；EVIDENCE 包） | **`tools/run_root_unittests.py` 的 `MIN_TESTS` 重釘註記逐字宣稱「於全部 10 個修復包與四方複審 agent 停工後、在最終工作樹實跑取得」並寫死三個當輪量測 token（一份 12 碼收集面指紋＋兩處 `Ran <數字> tests` 形態的計數），而那三個 token 在最終工作樹上一個都複現不出來**——該註記自己明文禁止的正是這個失效模式；同一 commit 內 `DEF-101-677` 又記著第三個數字 ⇒ R60 SA-R60-01／ARCH-R60-03 判過的「同一份 repo 對同一個數字兩種說法」原型復發（本輪第五次同類）。成因不是粗心：舊判準的「所有 agent 停工後」在**多波次收輪**（修復包→四方複審→再一波修復包）下不成立，收尾包自己不是最後一個工作者 | P2 | 根層 `tools/run_root_unittests.py` ＋ `ONBOARDING.md` §7 表① | fixed@R67R2：由本輪**最後一個工作者**在實查工作樹已停止變動後重跑 runner，取其印出的計數直接重釘 `MIN_TESTS`（實測值見該檔常數，刻意不在此抄第二份）；註記加訂兩條判準——(a) 重釘須在最後一波修復收斂之後，(b) 註記內一律不得寫死當場可現查的量測 token。(b) 由新增的 `min_tests_note_stale_tokens()` 機械強制並接進 `main()`（四處閘門都走 CLI ⇒ 有閘門看它的 rc）。注入四向：寫回指紋／`Ran <數字> tests`／`發現 <數字> 個測試`／整段刪註記 皆 rc=1，還原 rc=0。`ONBOARDING.md` §7 表① 該 token 已以 `tools/sync_onboarding_baselines.py --write` 同步 |
| DEF-101-690 | 2026-08-01 | R67 round 2 四方複審（SA-R67-01＋SD-R67-02，兩方交叉，**同輪原地復發**；CIPATH2 包） | **本輪新增的承重檔 `AISDLC_SDD/AISDLC_SDD_v0.30/conftest.py` 命中兩支 compat-CI 的 `paths:` 白名單 0 次**，與同輪剛修的是同一缺陷類別。根因兩層：(1) 盲區 F——`conftest.py` 由 pytest 依 rootdir 隱式載入，結構上不出現在任何 step 命令列、也不被任何測試 import ⇒ 路徑字面正則與 import BFS 兩套掃描器同時零訊號；(2) 該鎖的 `not p.startswith('AISDLC_SDD/')` 前綴豁免自陳理由（由 `AISDLC_SDD/**` 天然覆蓋）只對 aisdlc-sdd-ci 成立。反事實實測：還原上一輪的鎖＋兩條 paths 缺席 → rc=0（24 passed）逐字重現全綠 | P2（無活體影響：compat-CI 因帳務停擺未執行；但守門結構性失效） | 根層 `.github/workflows/{macos,windows}-compat-ci.yml` ＋ `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` | fixed@R67R2：兩支 workflow push+PR 四區塊各補 `AISDLC_SDD/conftest.py`＋`AISDLC_SDD/AISDLC_SDD_v*/conftest.py`；拆除 2 處前綴豁免；依 R54 收斂門檻改用程式化反向驗證（`_GIT_ENUMERATED_FAMILIES`，git pathspec 即時列舉 tracked∪untracked）而非疊第 4 份手寫登記字典。注入雙向：拿掉 paths rc=1／還原 rc=0；拆列舉器 untracked 半邊 rc=1／還原 rc=0。該鎖 24→30 passed |
| DEF-101-697 | 2026-08-01 | R67 round 2 四方複審（SA-R67-07；EVIDENCE 包） | **`ONBOARDING.md` §7 兩處平台歸屬殘留**：(a) 舊 provenance 小表以現行 provenance 的姿態宣稱四格皆「R60 收尾工作樹＋四方獨立重跑覆核」，與同節 R67 新立的「Windows 欄整欄 `unrecorded`、四格混世代」結論相反，且其 v0.30 一格仍寫 1747（R65 `58a829f` 已回填為 1750）；(b) 表① 欄頭寫「Windows 11（R60 收尾實測）」，而該格裝的是 R67 在 Darwin 真機量得的 `MIN_TESTS` | P3 | 根層 `ONBOARDING.md` §7 | fixed@R67R2：(a) 小表加 🔴 世代標頭＋欄名改為「R60 世代史料」，明示現行 provenance 唯一來源＝各平台 `snapshot-fingerprints-<平台鍵>` 錨，v0.30 那列補記「該值已於 R65 被取代、自此為史料」；(b) 表① 欄頭刪除「（R60 收尾實測）」，改為自陳「欄頭不代言量測時點與平台：受鎖 token 是平台中立值、只有 `skipped=N`／LOC 三數字才是 Windows 實機量測」，並逐字保全原欄頭字樣。回歸驗證：欄頭改動後 `platform_cell_index()` 仍解析到 macOS 欄 index 2／Windows 欄 index 3、`--write` rc=0（欄頭是該函式的取值面，改壞即 fail-loud） |
| DEF-101-692 | 2026-08-01 | R67 round 2 四方複審（SD-R67-02；CIPATH2 包） | **`tools/sync_onboarding_baselines.py` 的 `_FINGERPRINT_TREES` 四棵 glob 皆錨在測試樹內，決定同一次 pytest 收集結果的 rootdir `conftest.py` 落在覆蓋面之外** ⇒ 可改變 v0.30 計數而 `--check-snapshot` 仍判 ✅ rc=0。與 R60 SD-R60R3-03 修的是同一類缺口的另一個入口（那次是樹**內**子目錄、這次是樹**外** rootdir）。端到端注入（在 v0.30/conftest.py 末尾加 `collect_ignore_glob`）：修復前指紋不變（偵測不到）／修復後指紋改變（偵測到） | P2（潛在：文件數字當下仍正確，觸發器對此軸全盲） | 根層 `tools/sync_onboarding_baselines.py` | fixed@R67R2：新增 `_FINGERPRINT_ROOTDIR_CONFTESTS`（v001／v030／scripts／autoclaude 各一，對應關係取決於該次 pytest 的 rootdir 而非目錄包含關係）＋ `tree_fingerprint(..., extra_files)`（檔案不存在即不貢獻 bytes，一旦建立即改變指紋）。回歸鎖 `TestR67R2RootdirConftestIsFingerprintInput` 三支。副作用：v030 與 scripts 兩格指紋改變，已由本包（EVIDENCE）以乾淨 venv `--write --with-slow` 於最終工作樹重新量測回填 |
| DEF-101-694 | 2026-08-01 | R67 round 2 四方複審（QA-R67-05；CIPATH2 包） | **逐平台指紋錨落地後，非本機平台欄在單機交替工作流下結構上永遠 presumed stale** ⇒ 每次 pre-push 固定噴一則本機清不掉的 ⚠️，與本 repo 自訂原則（`tools/run_root_unittests.py`「常亮的警告＝背景噪音」）相衝；一旦養成略過該區塊的習慣，同段真正有牙的「本機平台欄轉紅」也會被一併略過 | P3 | 根層 `tools/sync_onboarding_baselines.py` | fixed@R67R2（設計取捨已論證並落地）：不升級為紅（紅線的前提是「有人修得動」，此處沒有）、也不全面消音（消音後換平台補量測的人不會知道另一欄已 N 天沒量）。改為：提醒移出警告頻道（stderr `⚠️` → stdout `ℹ️`）；內容由「四棵樹指紋 diff」（單機交替下必然變動、資訊量為零）換成「上次量測日期＋距今 N 天」；訊息自陳「結構性常態、不是新問題」並把注意力指回本機平台欄；`unrecorded` 另走「尚未建立基線」措辭。回歸鎖 `TestR67R2OtherPlatformNoticeIsNotAStandingWarning` 三支，注入 H/I/J 皆 rc=1、還原 rc=0 |
| DEF-101-696 | 2026-08-01 | R67 round 2 四方複審（SA-R67-06；EVIDENCE 包） | **`DEF-101-656` 狀態欄的取證四數字中，ci-gate scripts/tests 那一格記為 253，而該輪 `ONBOARDING.md` §7 表② 實際回填的是 259**（本包於 round 2 動工時親眼讀到工作樹該格為 259，另有三份 ci-gate log 與 `--check-snapshot` 佐證）⇒ 帳本與實作直接矛盾，且恰好在記錄「表② 數字與實況脫節」那筆修復的列裡原地復發 | P3 | 根層缺陷帳本（`DEF-101-656` 之訂正） | fixed@R67R2：依歸檔檔逐字保全紀律**不就地改 archive**，改以本列具名訂正——`DEF-101-656` 狀態欄第四個數字應為 **259**（該輪實際回填值）。⚠️ 不得改寫成 round 2 的新值：round 2 另有多包新增測試，`--write --with-slow` 已重新量測回填，現行值一律見 §7 表② 該格（本列刻意不抄一份）。**建議**：帳本狀態欄凡引用 §7 受管值者，一律比照 ADR §8 表頭規則 3 改為指向該格而不再抄數字 |
| DEF-101-688 | 2026-08-01 | R67 round 2 四方複審（QA-R67-03；LOCK 包） | **`tools/git-hooks/pre-push:179` 與 `tools/tests/test_pre_push_dispatcher.py:305` 同時指名一支從未存在的 `test_integration_gate_local_carrier.py` 為「另兩種腐爛」的守門者**（`ls` rc=1、全庫 find 零命中）⇒ `DEF-101-639` 修法正當性的核心論證在讀者現查時無法驗證；同 `CrossPlatform_Scan_Dimensions.md` 硬規則③ 第一點的死信形態，發生在程式碼註解上 | P2 | 根層 `tools/git-hooks/pre-push` ＋ `tools/tests/test_pre_push_dispatcher.py` | fixed@R67R2：兩處改指向真守門 `tools/tests/test_find_git_bash_parity.py` 的三個類別；新增 `TestNamedCarrierFilesActuallyExist` 掃描器（雙向斷言＋反事實引用具名登記＋stale 自檢）。注入 rc=1（commit 後仍 rc=1）／還原 rc=0。附記：掃描器另發現兩筆**非死信**的反事實引用（皆為被 `DEF-101-561` 否決的方案名），已具名登記、不需修 |
| DEF-101-678 | 2026-08-01 | R67 round 2 四方複審（ARCH-R67-04；ADRDOC 包） | **`ADR-XPLAT-002` §8 item 9 把「雲端 CI 帳務停擺」寫成穩定架構前提並據以重寫排序論證，該前提在寫下後 24 小時內翻轉**：實測 07-29~07-31 push 與 schedule 全數 failure（annotation 為帳務拒絕），08-01 三支排程 workflow 全數 success 且有真實執行時長（artifact-cleanup 24s／fsm-chaos-nightly 52s）。與同輪 §6 邊界 1 建立的「平台缺席是輪次屬性、不是 ADR 常數」同型 | P1 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` | fixed@R67R2：§6 邊界 1 逐輪表新增「該輪雲端 CI 可用性」欄＋`gh run list` 現查指令；§8 item 9 移除環境前提、原句逐字保全於新增 §8.3 並立通則（外部環境狀態一律登記逐輪表）；新增 §9.1 SC-5。注入：原句寫回 §8 → SC-5 rc=0（紅），還原 → rc=1（綠），其餘各條零串音 |
| DEF-101-686 | 2026-08-01 | R67 round 2 四方複審（SD-R67-04＋QA-R67-02，兩方交叉；ADRDOC 包） | **`ADR-XPLAT-002` §9.1 標題與首段自稱「三條」不變式，實際定義 SC-1~SC-4 四條，且同檔狀態欄寫「四條」**。§9.1 自我定位是「搬進測試時逐字照抄即可」的規格，寫錯條數會讓實作者少搬一條（最可能漏掉的 SC-4 正是本輪核心） | P3 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` | fixed@R67R2：**未採「三條→四條」的建議修法**（同輪新增 SC-5 後它會立刻再錯一次＝同輪原地復發），改為**條數一律不寫死**（與 §8 表頭規則 3 同源），並新增 SC-6 把「條數不得寫死」本身機械化、§8 item 14 補判準 (f) 保留「敘述條數 == `# SC-` 註解數」為回退路徑。修復者自查：第一版在新寫的三段散文內又寫了三次「四條」，自查後全數改正 |
| DEF-101-685 | 2026-08-01 | R67 round 2 四方複審（ARCH-R67-05；ADRDOC 包） | 重寫幅度極大的 ADR 留下的內部一致性瑕疵：(a) §4.3 交叉引用指錯 Phase（寫 2-E，LOC 預算實列 2-F；§8 item 7 依據欄一直是正確的 2-F ⇒ 全檔僅此一句錯）；(b) §9.1 標題自稱條數與實際定義條數不符（另立 `DEF-101-686`）；(c) 缺陷帳本主檔 251434／262144 ＝ 95.9% 逼近輪替上限 | P3 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` ＋ 根層缺陷帳本輪替 | fixed@R67R2：(a) 已修並補 §8 item 14 判準 (d)「內部交叉引用必須解析得到」，注入（改回 Phase 2-E）→ D2 rc=0、還原 → rc=1；(b) 見 `DEF-101-686`；(c) 本包已以 `tools/archive_defect_log.py --apply` 輪替出 archive_40 後入帳，收尾實測主檔位元組見本檔當下大小（刻意不寫死，避免再造一個 stale 站點） |
| DEF-101-691 | 2026-08-01 | R67 round 2 修復包 CIPATH2 依 SA-R67-01 第 4 項「修完自己再掃一次全 repo」 | **本輪新增的另兩支新檔（歸檔帳本 archive_38／archive_39）同屬未覆蓋**：歸檔帳本是 `tools/check_defect_log_crossref.py::main()` 以 glob **執行期**掃進來的承重輸入（量大小上限、驗歸檔索引 bullet），而 `tools/tests/test_check_defect_log_crossref.py::TestMain` 拿的就是真實 repo 跑那支 `main()`；主檔早已列入 paths、歸檔卻沒有 ⇒ 同一支鎖的輸入被切成「有觸發的一半」與「沒觸發的一半」 | P3 | 根層 `.github/workflows/{macos,windows}-compat-ci.yml` | fixed@R67R2：兩支 workflow push+PR 各補 `docs/06_quality/AutoSDD_Defect_Log_archive_*.md`（`_*` 讓輪替新增的歸檔自動涵蓋），並收進同一個 `_GIT_ENUMERATED_FAMILIES` 家族鎖。注入雙向：拿掉該條 rc=1／還原 rc=0 |
| DEF-101-687 | 2026-08-01 | R67 round 2 四方複審（ARCH-R67-03；LOCK 包） | **`tools/check_script_parity.py` 的 UEP tier 棘輪只有逐 key 降級規則，而新鮮度鎖強制「活體＋基準成對編輯」為常規工作流 ⇒ 降級與升級在 diff 上同形**：兩行編輯即可把 ADR §3.3 明文封頂的 Tier-3 項降級且全套全綠（複審注入 INJ-1c 實測 rc=0） | P2 | 根層 `tools/check_script_parity.py` | fixed@R67R2：新增獨立總量常數 `_TIER34_FLOOR = 10`（只准上修，形狀鏡像 `_UNPINNED_CEILING`）＋規則 (E)；被數的量改良為 **tier3/4 課責數**（活體 tier3/4 ∪ 基準記為 tier3/4 而已整筆移出者），使 §8 item 12 的「徹底收斂」出口不被懲罰（未照抄 proposed_fix 的純活體筆數地板——原案會弄壞既有對照組）。注入 rc=1／還原 rc=0；另證 `tier34_floor=0`（模擬修前）同一注入為 GREEN |
| DEF-101-683 | 2026-08-01 | R67 round 2 四方複審（ARCH-R67-06；ADRDOC 包） | **本輪 ΔUEP=0、ΔAC=0 而護欄層 +15.0%，判準組合量不到本輪主要工作**：獨立複量工作樹 UEP=5／AC=48、HEAD `1cbe9fd` 以 AST 重建亦為 5／48；護欄層檔數 56→56、行數 29433→33858（量測面髒 15 筆）⇒ 現行三判準對「護欄層成長」零訊號 | P2 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` | fixed@R67R2：架構裁決＝判準沒壞、本輪工作不在 UEP 定義域內，**明文否決擴充 UEP 定義**（§1.3 同型錯誤的結論是換刻度不是改寬刻度）；缺口是「護欄成長時零訊號」而非缺上限。交付：新增 §4.3.1 逐輪判準三元組（含 provenance 與「量測面髒⇒不得作基線」明標）、§4.2 rule 1 追加射程宣告、§8 item 7 追加實證並上調降維報表優先序 |
| DEF-101-679 | 2026-08-01 | R67 round 2 四方複審（ARCH-R67-02；ADRDOC 包） | **異動 B 未真正中立化**：`ADR-XPLAT-002` §5 Phase 2-B 仍留一句活的 Windows 平台前提（「本機有 Git Bash…」，在 macOS 上為假），而 §9.1 SC-4 採列舉式動詞比對、與該句的「本機**有**」只差一個動詞而漏抓 ⇒ §9.1「無一是活的平台前提」宣稱為假（stale 第四型：邊界寫對、結論寫過頭） | P2 | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` | fixed@R67R2：該欄改為指向 §6 邊界 1 逐輪表、逐字原句移入散文訂正段（避開單行巨欄整格豁免）、SC-4 動詞列舉擴充、刪除過頭結論；另全檔重掃出 4 處未被點名的 `本機` 站點，以 §2 開頭一次性總錨處置。注入：原句寫回 → SC-4 rc=0，還原 → rc=1 |
| DEF-101-682 | 2026-08-01 | R67 round 2 四方複審（SA-R67-05；ADRDOC 包） | **`CrossPlatform_Scan_Dimensions.md` 的「且 macOS 欄至今零真機量測」已被 R67 自己證偽卻未訂正**——`ONBOARDING.md` §7 `snapshot-fingerprints-darwin` 錨帶 `measured-at`／`host=Darwin-25.5.0-arm64`。成因是**授權面切分造成的責任真空**（三方都記錄、無人修） | P3 | 根層 `docs/06_quality/CrossPlatform_Scan_Dimensions.md` | fixed@R67R2：刪活斷言、逐字保全原句、改指 live 來源並區分「指紋漂移 rc」與「有無真機量測」。回歸鎖採**計數相等**形態（活斷言出現次數 == 逐字引述形態出現次數）——🔴 第一版採「同行帶『訂正』即豁免」是**假鎖**、注入不轉紅（注入點落入已含該關鍵字的單行巨欄），靠注入實測當場抓到後改正 |
| DEF-101-681 | 2026-08-01 | R67 round 2 四方複審（SA-R67-04；ADRDOC 包） | **R67 新增的第九個掃描維度 Scan-M 在治理 SSOT `docs/06_quality/CrossPlatform_Scan_Dimensions.md` 內零定義**，唯一痕跡是同輪即被歸檔的四列缺陷（`DEF-101-668`~`DEF-101-671`）⇒ 該維度的判準只存在於已歸檔敘述裡，讀 SSOT 的人查不到 | P3 | 根層 `docs/06_quality/CrossPlatform_Scan_Dimensions.md` | fixed@R67R2：補維度表列＋〈為何 R67 必須加 Scan-M〉節（與 Scan-F 分界、四條必跑項、無真機輪一律標 SKIP 不得以「沒跑＝沒問題」收輪）＋命名說明＋〈擴充新維度〉補記；並升級為通用鎖（帳本用過的每個單字母 `Scan-<X>` 都必須在維度表有列）。注入：抽掉 Scan-M 列 → 差集印出、rc=0，還原 → rc=1 |
