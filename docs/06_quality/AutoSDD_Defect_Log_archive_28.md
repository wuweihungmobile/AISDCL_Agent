# AutoSDD Defect Log — Archive 28

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **10 筆** R57 已結列（`DEF-101-491`~`499` ＋ `501`，狀態皆 `fixed@R57*` 或 `no_action_needed`），於 R59 **動工前**（2026-07-28）搬遷。
>
> **為何是這 10 筆**：R57 收尾（`archive_27` 標頭與主檔 DEF-101-493）明文指定 `DEF-101-491`~`501` 為「歸檔立帳／流程紀實／round 2~4 複審紀錄，**刻意留給 R58 直接對帳**」。R58 整輪經使用者判定失控作廢（`reset --hard 75aab89`，備份 tag `r58-pre-reset-head`／`r58-wip-backup`），**R59 承接該對帳並已完成**（本輪即以這批條目為起點：R57 的收斂狀態、`MIN_TESTS` 重釘判準、`_CI_TREE_RE` 收斂判例、失敗明細落檔等皆經覆核並延用），對帳鏈閉合，故失去留在主檔的理由。
>
> **刻意不搬的三筆，逐筆理由**：
> - `DEF-101-478`（NTFS 四處同因缺陷）、`DEF-101-486`（windows-compat-ci 檔頭失實）——R59 實測仍分別被 `ONBOARDING.md` 與 `.github/workflows/windows-compat-ci.yml` 以 DEF-ID 引用，搬走會觸發 `tools/check_defect_log_crossref.py` 的跨文件矛盾偵測。
> - `DEF-101-500`——狀態為 `fixed/deferred@R57 round 3`，其 **deferred 部分仍然活著**（ARCH-R57R3-04：repo 內同族的「同語言重複」如 `_FROZEN_SDD_VERSION_RE`／`_FROZEN_VERSION_DIR_RE` 尚未逐一檢視）。埋掉一個活的 backlog 就是斷對帳鏈，正是 R57 當初對 `DEF-101-477` 的同款顧慮，故留主檔。**本次搬遷腳本的「非已結列不得搬」斷言即當場擋下它**，非事後補察。
>
> **為何動工前搬**：R59 動工實測主檔 243,212 bytes，距 240KB（245,760）warn 線僅 **2,548 bytes**、距 256KB（262,144）fail 硬閘 18,932 bytes；而帳本自身記載單輪毛增約 27~28KB > 18,932——**先寫後歸檔必撞硬閘**。比照 `archive_23`／`archive_25` 已確立的「動工前先歸檔騰空間」先例。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-491 | 2026-07-27 | R57 動工前帳本體積評估（Scan-D SCAND-02 亦獨立命中） | R57 動工時主檔 244,028 bytes——距 `tools/check_defect_log_crossref.py` 的 240KB（245,760）**warn 線僅剩 1,732 bytes**、距 256KB（262,144）fail 硬閘剩 18,116 bytes，而 R56 單輪淨增約 20.9KB > 18,116，**先寫後歸檔必撞硬閘** | P3（帳本治理；不處理則本輪收尾必觸 CI 硬阻擋） | 本輪處理（主控） | fixed@R57：比照 `archive_23` 先例於**動工前**執行歸檔，新建 `docs/06_quality/AutoSDD_Defect_Log_archive_25.md`，搬遷 15 筆 R56 round 4~7 已結列（DEF-101-457／458／459／461／463~469／472~475，狀態皆 `fixed@R56`、crossref 掃描目標零引用）。主檔 244,028 → **217,700** bytes（搬遷完成、尚未回寫索引條目時；釋出 26,328，退出 warn 帶），補完索引條目與本列後為 218,756（R57 round 3 SA-R57R3-01 訂正：本列原只記 218,756、archive_25 標頭與索引條目只記 217,700，兩組數字各自算術自洽卻皆無時點限定詞，讀者無從分辨——差額 1,056 bytes 恰為索引條目本身的文字量）。**刻意留主檔 6 列並在索引明載理由**：460／476（歸檔立帳條目，留主檔供追溯）、462／471（流程紀錄含交接 R57 的實況敘述）、470（狀態 `open（deferred）`，不符「已結」要件）、477（其 ④⑤ 正是 R57 本輪處理中的 backlog，搬走會斷對帳鏈）。歸檔索引同步補 archive_25 完整條目、標題「二十四檔」→「二十五檔」，實測索引條目數 25 == 磁碟實際檔案數 25——**刻意避免重蹈 R56 索引漏登造成引用鏈斷裂的覆轍**（見 DEF-101-476） |
| DEF-101-492 | 2026-07-27 | R57 流程紀實（含四方對主控作業方式的批評，記事存證） | 本輪執行結構：**五維掃描（Scan-A/A2/B/C/D/E）+ R56 backlog 複核 → 18 項候選 → 四包並行修復 → 四方獨立複審 round 1（全數 REJECT，23 項）→ 三包並行修復 + 主控自理 → round 2 四方複核**。**首批掃描 workflow 全滅**：7 個 agent 並行、9 分鐘燒 120 萬 token、404 次工具呼叫後**全部撞上 session 用量上限死在回傳之前，產出 0 項發現**；主控依 R56 DEF-101-471 記載的救援手法讀 transcript 挖掘，確認 agent 未留下可用結論（thinking 未落盤、assistant text 僅 1.1KB），遂於限額重置後**改以硬性工具呼叫預算重跑**（每 agent 25 次、第 18 次起停止新調查），7/7 成功回傳、token 降至 70 萬、工具呼叫降至 167——**「限制調查深度」反而讓產出從 0 變成 18 項**，此經驗值得後續輪次沿用 | —（流程紀錄） | — | no_action_needed（流程紀錄）。**四方對主控的兩項批評，主控接受並記錄**：①**SD-R57-05**：獨立複審進行期間主控持續改寫工作樹，受審檔案在複審者兩次讀取之間實質變動（一度處於「宣稱已收斂 SSOT、實際仍是舊壞錨」與「豁免標記理由為空」的中間態），使複審基準不穩定——這是真實的流程缺陷，正確做法是**複審期間凍結工作樹**，主控自理項應排在複審前或複審後，不可並行；本輪因 session 額度壓力而妥協，如實記載。②**SA-R57-01**：主控宣稱的核心基線「根層 558 tests OK」在 SA 第一次獨立重跑時即得 `FAILED (failures=6)`——主控事後查明為 QA agent 同時進行 bug-injection 造成的並行雜訊（已以「繞過測試框架直接呼叫四處函式」的手法複驗行為正確），但**「不可穩定重現的基線」在複審者眼中就是無效基線**，SA 的判定成立；根因同 ①。最終三組基線數字皆為所有並行 agent 停工後、由主控在最終工作樹上一次性重測所得 |
| DEF-101-493 | 2026-07-27 | R57 收尾帳本體積處置（第二次歸檔） | R57 寫入 15 條新條目（DEF-101-478~492）後主檔達 245,075 bytes，**距 240KB（245,760）warn 線僅剩 685 bytes**，而 round 2／3 的複審與修復紀錄尚未寫入 | P3（帳本治理） | 本輪處理（主控） | fixed@R57：新建 `docs/06_quality/AutoSDD_Defect_Log_archive_26.md`，搬遷 R56 殘留的最後 5 列（DEF-101-460／462／471／476／477），**至此 R56 全輪條目完全退出主檔**。這 5 列在動工前的 archive_25 曾刻意留下（歸檔立帳 ×2、流程紀錄 ×2、交接 backlog ×1）；其中 DEF-101-477 的 ④（`_CI_TREE_RE` 三份複本未收斂）⑤（聯集鎖未鎖呼叫端）兩項 backlog 已由本輪完成（見 DEF-101-481／483），**對帳鏈閉合故失去留主檔的理由**。主檔 245,075 → 235,494 bytes（釋出 9,581，退出 warn 帶）。歸檔索引同步補 archive_26、標題「二十五檔」→「二十六檔」，實測索引條目數 26 == 磁碟實際檔案數 26。**交接給 R58 的實況（R57 round 3 SA-R57R3-02 補；比照 R56 DEF-101-471 先例）**：R57 收尾寫入後主檔**已重新進入 240KB（245,760）warn 帶**（`check_defect_log_crossref.py` 印 warning 但 rc=0 不阻塞；fail 硬閘為 256KB＝262,144）。**刻意不在此寫死當下 bytes 數**——本列自身的每一次增補都會改變該值（本列初稿寫下的 245,475 在補完 SA-R57R3-01/02 兩項訂正後當場就過期，是本輪反覆抓的『寫死數字必過期』同型問題），**R58 動工時請以 `wc -c docs/06_quality/AutoSDD_Defect_Log.md` 實測為準**。關鍵判斷不因數字微變而改變：R57 自身毛增約 27~28KB，**大於** warn 帶到 fail 硬閘之間的全部餘裕（16KB 上下），故 **R58 若同為長輪，動工前務必先歸檔**（**R57 收尾實況更新**：R57 一輪內已執行**三次**歸檔——動工前 archive_25〔15 筆 R56 條目〕、收尾 archive_26〔5 筆 R56 殘留〕與 archive_27〔11 筆 **R57 自身**已結列〕，第三次是因寫入 round 4 條目後主檔達 259,591 bytes、距 fail 硬閘僅 2,553 bytes 而不得不為。搬遷後主檔 242,604 bytes，**已退出 240KB warn 帶**、距 fail 硬閘 19,540 bytes。**R58 的可搬來源**＝主檔殘留的 R57 條目 `DEF-101-478`／`486`〔目前被 crossref 指定掃描目標引用，搬前須先確認引用已解除〕、`491`~`501`〔歸檔立帳／流程紀實／round 2~4 複審紀錄，刻意留給 R58 對帳〕；R57 round 4 SA-R57R4-04 曾訂正此處原寫的 478~497 少算四條，本次一併依實際歸檔結果重寫）。SA 同時指出此風險不致真的蒙蔽下一輪：Scan-D 的職責本就包含動工前實測帳本體積（DEF-101-491 正是這樣誕生的），有制度化的自我發現路徑 |
| DEF-101-494 | 2026-07-27 | R57 四方複審 round 2（交叉換位，四方**全數 REJECT**，16 項）＋round 3 三包修復 | round 1 的修復本身再度被打臉，兩項最嚴重者：①**A-R57R2-01（P1）三條 CI 掃描樹錨全部大小寫敏感**——PowerShell cmdlet 與參數名皆不分大小寫，Architect 實測把 `$files += @(get-childitem docs/scripts -recurse -filter *.ps1 -file)`（全小寫）插入 root-infra-ci.yml 的 pwsh 解析 step → `run_root_unittests.py` rc=0／584 tests OK／**零訊號**；`GCI`／`Dir` 別名同樣全綠。且 round 1 才剛改寫的 docstring「本錨唯一依賴的是列舉檔案走 Get-ChildItem 或其別名」**被 `get-childitem` 當場推翻**（它就是走 Get-ChildItem）——**這是「涵蓋／不涵蓋」假宣稱連續第二輪復發**。②**A-R57R2-02／R57R2-QA-01（P2，兩方交叉）`_strip_ps_comments` 的 here-string 起始誤判**——`re.search(r"@([\"'])\s*$", cut)` 只看行尾兩字元，任何以 `@"` 或 `@'` 收尾的**普通字串字面值**（如 `Write-Host "user@"`）都會開啟 here-string 模式，而終止條件幾乎不會出現，故**一開就吃到檔尾、其後整份檔案的註解完全不再剝除** → round 1 QA-R57-02 修掉的「用尾隨註解冒充功能碼」繞過就地復活；兩份逐字複本同時中招 | P1×2、P2×5、P3×6、P4×3（嚴重度較 round 1 的 P1×3／P2×8 實質下降） | 本輪修復（round 3 三包並行） | fixed@R57 round 3：①三條錨一律加 `re.IGNORECASE`，並**重新量測**（非推算）確認 `EXPECTED_CI_GCI_CALLS=4`／`EXPECTED_CI_SCAN_STATEMENTS=4` 不變（IGNORECASE 下別名 `gci`／`dir`／`ls` 在該 step 零意外命中）；docstring 改為三段可查證清單「已實測涵蓋／已實測不涵蓋（`[System.IO.Directory]::GetFiles`、`Get-Item`、`Resolve-Path` 三種逐一實機量過）／**未窮舉**（明文聲明不做「唯一殘餘風險是 X」宣稱）」；新增 `TestAnchorsAreCaseInsensitive`（斷言 compiled pattern 的 flags 帶 IGNORECASE，未來拿掉 flag 立即紅）與 `_KNOWN_UNCOVERED` 常駐表（不涵蓋清單也被釘住，未來被涵蓋時翻紅強迫改文件）。②here-string 偵測改為**逐字元掃描器**而非正則——`Write-Host "user@"` 的 `@` 位於字串字面值內，掃描器根本走不到，比原建議的「看前一字元」正則修法更強；同時把兩份複本收斂進 `tools/tests/_platform_helpers.py`（回應 SA-R57R2-03「同一輪內 SSOT 政策自相矛盾」——一邊收斂 `_CI_TREE_RE`、一邊新造兩份逐字複本）。③**SA-R57R2-02 三份呼叫端輸入前處理契約不一致**（一份餵剝註解後的 step、兩份餵原文，卻共用同一 EXPECTED 常數）改採「冪等正規化」：SSOT 三支函式一律先 `strip_line_comments()`，使 `f(原文) == f(已剝註解文字)` 機械恆成立，契約寫進 docstring 並以性質測試釘住。④A-R57R2-03 的 `TestSsotCallsiteLock` 三個缺口（死碼分支仍算接線、字串拼接／f-string／容器內的自寫正則漏抓、常數只要出現在檔內任一處即算引用）全補，並把六種繞過形狀固化為常駐斷言 |
| DEF-101-495 | 2026-07-27 | R57 round 2 Architect **自報作業事故**（記事存證，非程式缺陷） | 複審 agent 在還原 bug-injection 時**誤用 `git checkout -- tools/tests/test_ps1_bom.py`**，抹掉該檔未提交的整份 R57 成果（SSOT 三錨接線全失），違反任務書「絕不 git checkout」的明確指示。同一時刻另一位複審者 SA 讀到該中間態，遂回報 `SA-R57R2-01（P1）「工作樹在複審期間再度變動、根層全套 FAILED(2)」`——該回報在其觀測時點**完全正確** | P1（就其觀測時點而言）／事故本身：作業紀律違規 | 記事存證＋主控獨立複核 | no_action_needed（已還原並經獨立驗證）。**處置經過**：Architect 自行從 round 1 子 agent 的 transcript 撈回該檔最終狀態的 diff 並 `git apply`，宣稱 `git hash-object` 得 `5883d567d97bdaf57ca168fd28ed971a31ecd3d3`、與 transcript 記錄的 `index 8cf32c1..5883d56` 相符＝逐位元組還原，並主動要求主控複核。**主控獨立複核結果：成立**——親跑 `git hash-object tools/tests/test_ps1_bom.py` 得同一 hash，且 `grep` 確認 `from _ci_scan_anchors import`／`ci_fixed_trees`／`ci_gci_call_count`／`ci_scan_statement_count`／`EXPECTED_CI_GCI_CALLS` 五處接線完整存在。**教訓（已寫入後續輪任務書）**：對「工作樹有大量未提交成果」的複審輪，禁止 git 系還原指令、一律 `cp` 備份／`cp` 還原；round 3 三包全數改用此法且皆附 md5 比對，零事故。**另記**：本事故也反證了 DEF-101-492 所記 SD-R57-05「複審期間應凍結工作樹」的重要性——本輪主控確實凍結了（未改任何檔案），但**複審者自身的注入行為**同樣會製造中間態，這是原先未考慮到的一面 |
| DEF-101-496 | 2026-07-27 | R57 round 2 SD-R57R2-04（帳本完整性） | R57 的 15 條帳本列中，**本輪明確判為 deferred 的兩項完全查無記載**：C3（`aisdlc-sdd-drift-daily.yml`／`autoclaude-pg-e2e-on-label.yml` 未宣告 `concurrency:`）與 B3（「解析 LATEST 版目錄」helper 在多支測試中逐字複製）。帳本只記了「修了什麼」，沒記「刻意不修什麼與為什麼」，未來輪次無從得知這些是已評估過的決定還是漏網 | P4（帳本完整性；deferred 決定未留痕＝下一輪必重新評估一次） | 本輪修復（主控） | fixed@R57：本列即為兩項 deferred 的正式登記。**C3（concurrency）**：`aisdlc-sdd-drift-daily.yml` 為 `schedule` 觸發的 ubuntu-only 每日漂移報告、`autoclaude-pg-e2e-on-label.yml` 為 `labeled` 事件觸發，兩者皆非高頻併發情境，且**與 macOS/Windows 平台相容性無關**（`platformRelevant=false`），逾越本輪授權範圍，判 deferred 並建議由 C 軌（AutoClaude 自身能力）工作流帳本排程處理。**B3（LATEST helper 重複）**：符合 R56 分診問句認定的「同語言、同執行時機＝實作重複，該收斂」，但收斂需動到 round 3 其他修復包正在改的 `tools/tests/` 檔案，並行改動風險高於缺陷本身（該重複目前零實害、各複本行為一致），判 deferred 列 R58 backlog。**範圍延伸（R57 round 3 ARCH-R57R3-04 補）**：同家族另含 `_FROZEN_SDD_VERSION_RE` + `_exclude_frozen_sdd_versions()`（`test_windowsapps_guard_bash_parity.py:244`／`test_windowsapps_guard_cross_consistency.py:415` **逐字相同**）與 `_FROZEN_VERSION_DIR_RE`（另 2 檔變體），失效模式與 R57 本輪修的完全同型——`\d+\.\d+` 抓不到 `AISDLC_SDD_v1.0.1` 這類三段版號時四份會**同時**靜默誤分類。R58 收斂時應一併納入，否則照 B3 字面掃只會掃到 LATEST helper。**另記兩項本輪 refuted/deferred 的複審建議**：SA-R57R2-05 附帶建議的「把已被合併測試涵蓋的四支併入 `_FORM_EVASIONS` 表驅動以降低診斷雜訊」屬純體驗重構、無鑑別力增減，依 Rule 3 外科式變更原則未做；A-R57R2-04（`--%` stop-parsing 符號後的 `#` 被誤剝）經查真實 `.ps1` 未使用 `--%`，判為理論邊界 |
| DEF-101-497 | 2026-07-27 | R57 round 2 QA（R57R2-QA-03，本輪修復擴大暴露面的既有缺陷） | `AISDLC_SDD/scripts/component_sanitizer.py::sanitize_component()` 的 `_` 保留名前綴是在**截斷之後**才加，故加完可能達 `_MAX_COMPONENT_LEN + 1`。實測 `sanitize_component('CON' + ' '*3 + '.' + 'x'*100)` 回傳 **81 字元** > 上限 80。此為**既有缺陷**（`'CON.' + 'z'*100` 在 R57 之前就已 81 字元），R57 為修 DEF-101-478 而加的 `.rstrip(" ")` 並非成因，但**把「保留名 + 尾隨空白」也納入了會觸發的輸入集合，擴大了暴露面** | P4（溢出 1 字元不會立即失敗，但侵蝕該上限刻意為前後綴 `FSM-STATE-…-{track}.yaml` 預留的餘裕，且違反本函式對呼叫端的長度承諾） | 本輪修復（主控） | fixed@R57：加前綴後若超出上限則重新截斷並 `rstrip(" .")`。**重新截斷安全性的論證（寫入程式碼註解）**：前綴 `_` 使 stem 成為 `_CON` 之類、不匹配 `^CON$`，故截斷尾部不會讓它退回保留名；尾端再 `rstrip(" .")` 是避免截斷剛好切在空白/句點上而違反 NTFS「不得以空白或句點結尾」，且開頭有 `_` 故不可能被剝成空字串。新增 `TestReservedPrefixDoesNotBreakLengthCap` 2 支（長度上限＋重新截斷不得重新引入 NTFS 違規／不得退回保留名），bug-injection：移除重新截斷後 3 failed（含 R57 前就存在的 `'CON.' + 'z'*100` 形態），還原後 6 passed／23 subtests |
| DEF-101-498 | 2026-07-27 | R57 四方複審 round 3（**SA／Architect／QA 三方 APPROVE，SD 一方 REJECT**）＋主控收尾修復 | **SD-R57R3-01（P2，本輪唯一阻擋收斂的項目）**：`tools/tests/_platform_helpers.py` 的 `_PS_COMMENT_LEAD = " \t;|({,"` **漏掉右括號類與引號類收尾字元**（`)` `}` `]` `"` `'`），使「以這些字元結尾的功能碼 + 尾隨 `#` 註解」整段原樣保留 → 「錨點只認功能碼」的鎖 fail-open。SD 以本機 pwsh 7 的**真 PowerShell parser**（`[System.Management.Automation.Language.Parser]::ParseInput` 取 Comment token）取得 ground truth，逐條確認 `Write-Host (1)#c`／`if ($true) { }#c`／`$a[0]#c`／`Write-Host "a"#c`／`Write-Host 'a'#c` 五種在 PowerShell 中**確實都是註解**（對照組 `c#`／`$#` 的 Comment token 為空＝現行 lead-char 設計要保護的情形）。並以 bug-injection 實證可繞過：把 `tools/install_windows_nightly.ps1` 的功能碼 `-WakeToRun` 刪除、只在 `Write-Output "note"#…-WakeToRun…` 註解裡留字樣 → `test_windows_nightly_anchor_parity.py` **6 支全數 OK**（負對照：不留註解則正常翻紅，證明綠燈確實來自註解洩漏）。**同一類「剝除器 fail-open ＋ 邊界宣稱過寬」已連續第三輪復發**（round 1 QA-R57-02 尾隨註解未剝／round 2 A-R57R2-02 here-string 誤判／round 3 本項） | P2（latent：全語料實掃 137 支 `.ps1` 的 2,847 個真 Comment token，現況洩漏數為 0；但暴露面涵蓋 `test_windows_nightly_anchor_parity.py` 8 個錨與 `test_find_git_bash_parity.py` 的呼叫端登記表等值斷言） | 本輪修復（主控） | fixed@R57 round 3：①`_PS_COMMENT_LEAD` 補上五個收尾字元（安全性由 SD 的 parser ground truth 背書——`$#`／`c#` 的前一字元是 `$`／字母，仍不在集合內，不受影響）；②新增 `test_comment_after_closing_bracket_or_quote_is_stripped`（五種前導字元 subTest，斷言註解裡的黑名單字樣必須被剝掉且功能碼本身必須保留）與 `test_protected_forms_still_not_stripped_after_lead_set_widening`（防修復引入偽陽性）；③docstring 的「已實測涵蓋」由籠統的「尾隨行內註解」改為**明列前導字元集合**，並在「已知不涵蓋」補第 5、6 條（不在集合內的前導字元後的 `#` 一律不視為註解起點＝**設計取捨而非實作疏漏**；`_PS_HERE_STRING_LEAD` 同樣未含 `]`／`)` 故 `[string]@"` 不被辨識，方向為多剝＝fail-closed，一併登記不修）。**R57 的核心教訓在此定案：涵蓋與不涵蓋必須互補、不得留白——留白處就是下一輪的缺陷**（本項在 round 3 之前正是留白）。bug-injection 複驗：還原 lead 集合後五種形態全部 FAILED，還原後 29 tests OK、md5 相符 |
| DEF-101-499 | 2026-07-27 | R57 round 3 Architect（ARCH-R57R3-03，**唯一可能翻轉其 APPROVE 的項目，故留痕**） | Architect 獨立重跑 `python3 tools/run_root_unittests.py` **14 次**，其中 **1 次**輸出 `Ran 610 tests in 55.141s / FAILED (failures=4, skipped=4)`，其餘 13 次全綠。三種針對性重現（連跑 6 次／兩實例並行／8 顆 CPU 燒滿下 66.2s）**皆未重現**；而該 runner **不保留失敗細節**，那 4 支的名稱隨 process 消失，故無法歸因。注意 `failures=4` 與 `skipped=4` 同時出現，**不是**那 4 支 Windows 專屬 skip 轉 fail。`tools/tests/*.py` 有 30 處 subprocess/thread timeout（最短 5s），時序敏感是可能機制但壓載測試未能證實 | P4（無法指名、1/14 不可重現、方向為 fail-closed〔假紅、不放行缺陷〕；Architect 明言「報成 P3 會用不可行動的證據強迫 REJECT，違反『硬湊發現比零發現更糟』」） | 本輪修復（主控，採 Architect 建議：**不追查現在，但讓下次可診斷**） | fixed@R57 round 3：`run_with_floor()` 在 `wasSuccessful()` 為 False 的既有路徑上新增 `dump_failure_detail()`，把 `result.failures`／`result.errors` 的 test id 與完整 traceback 落檔至 `tools/.last_failure.log`（已加入 `.gitignore`）。**刻意的三項邊界**：①不動 rc、不動任何斷言，只在已經要回 1 的路徑上多寫一份明細；②寫檔失敗只印警告不拋——診斷輔助不得反過來成為新的失敗來源；③以 `inspect.getsource` 斷言落檔呼叫確實在 `wasSuccessful` 守衛**之內**（否則每次成功執行都會留下誤導性的 log）。新增 `DumpFailureDetailTest` 3 支覆蓋上述三項。**未歸因事實如實登記**，R58 若再遇同型翻紅，只需一次即可定位 |
| DEF-101-501 | 2026-07-27 | R57 四方最終核准 round 4（**Architect／SA／SD／QA 四方全數 APPROVE，blocking P0~P2 = 0**）＋主控收尾修復 | 四方共提 11 項，**全為 P3/P4、零阻擋項**。兩項 P3 值得留痕：①**SA-R57R4-01**：`dump_failure_detail()`（round 3 才為 DEF-101-499 新增的診斷落檔）**只讀 `result.failures`／`result.errors`、完全忽略 `result.unexpectedSuccesses`**，而 `wasSuccessful()` 在後者非空時亦回 False ⇒ 該模式下 runner rc=1 卻落一份**不指名任何測試的空明細**（實測僅 60 bytes 的 `（0 failures / 0 errors）` 標頭）——**正是 DEF-101-499 立意要消除的「無法歸因」狀態，在其修復當輪就以新形態復現**。②**SD-R57R4-01**：`_PS_COMMENT_LEAD` 補字元後，程式碼註解寫的「安全性理由」（「`c#`／`$#` 的前一字元是字母／`$`，仍不在集合內故安全」）**與 PowerShell 真實規則不等價**——真正的保護來源是 command/argument（bareword）解析模式，與前導字元無關；同一個 `$x#c` 在 expression 模式下 `#` 就**是**註解（pwsh 7.6.3 實測 `$c#zz` → Comment@2）。**Architect 獨立以真 parser 對 64 個實務形態差分量化了這個結構性天花板：FAIL_OPEN=27（其中 20 案 parseErrors=0＝完全合法的日常寫法，如 `$a = 1#c`／`$env:PATH#c`／`$a = [int]$b#c`）、FAIL_CLOSED=0**；SD 另以約 130 條探針得 FAIL_OPEN=37，並實證仍可用 `$note = $x#  -WakeToRun` 繞過 nightly 錨點鎖（round 3 修掉的 `"note"#…` 手法已確認關閉） | P3×3、P4×8（四方一致：不影響任何正確性、閘門或跨平台相容性） | ①②及帳本訂正本輪修復；結構性天花板列 R58 | fixed@R57 round 4：**①** `dump_failure_detail` 納入第三個 bucket（`unexpectedSuccesses` 逐一具名 + 標頭計數），並依 SA-R57R4-02／QA-R57R4-02 把 print 副作用分離為 `_write_failure_log()`——比照本檔 `windows_native_skips`／`report_windows_native_skips` 的既有慣例（純函式與列印分離；原寫法會讓每次**全綠**執行的終端輸出混入其自身單元測試 fixture 產生的落檔訊息與 ⚠️ 警告，與 R43 二審 SA 揪出的問題同型）。新增 `test_unexpected_successes_are_named` 鎖住。**②** 註解的安全性理由改為據實描述 parse-mode 二分；`strip_ps_comments` docstring 的「已知不涵蓋」第 5 條由「若 PowerShell **未來**允許更多語境…」改寫為**現行、已量測**的結構性限制（附 Architect 的 27/64 與 SD 的 37/130 數據、明標方向為 fail-open、載明全語料實測洩漏 0 故屬 latent），並明文警告「**切勿再往集合裡補字元——那是 whack-a-mole，本條存在正是為了阻止它**」，指向 R58 的正確修法（把 pwsh parser 對全語料的 Comment token 凍結成 golden fixture 做離線差分，可在 CI 不裝 pwsh 的前提下機械化 ground truth，一次消掉整個天花板；同法亦可解 `_ci_scan_anchors.py` 判例第 (3) 條的同型天花板）。**③** 帳本兩處自身錯誤經 SA 逐項回源實查抓出並訂正（見 DEF-101-500 ④ 欄與 DEF-101-493：前者把修法寫反、後者 R58 可搬範圍少算四條）。**列 R58 backlog（已於各處記載）**：ARCH-R57R4-01 parse-mode 結構性天花板與 golden 差分修法、ARCH-R57R4-02 `_ci_scan_anchors` docstring「這三種」vs 常數 4 項的**單向**同步鎖（現有斷言只在「錨變強」方向翻紅，對「常數變長」方向無感）、SD-R57R4-02 `cut_ps_inline_comment` 零生產消費端、QA-R57R4-01 `inspect.getsource` 文字比對式守衛斷言的脆弱性、QA-R57R4-03 測試 docstring 計數。**流程紀實**：R57 共執行**四輪**四方複審（round 1 全 REJECT 23 項 → round 2 全 REJECT 16 項 → round 3 三方 APPROVE 一方 REJECT 11 項 → round 4 **四方全 APPROVE** 11 項全 P3/P4），P1 軌跡 3→2→0→0、P2 軌跡 8→5→1→0。round 3 曾有三方 agent 因 API 529 Overloaded 夭折後補跑 |
