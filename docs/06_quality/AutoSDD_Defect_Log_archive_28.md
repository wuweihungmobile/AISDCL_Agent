# AutoSDD 缺陷帳本 — 歸檔 28（R58 跨平台輪 2026-07-27 動工前建立）

> **搬遷來源**：`AutoSDD_Defect_Log.md` 缺陷總表。**原文逐字保全、零刪除**（搬移非刪除）。
>
> **本檔內容**：32 筆較舊的已結列（狀態 `fixed`／`wontfix`／`no_action_needed`，且經
> `check_defect_log_crossref.py` 的 `_CLAIM_RE` 機械核對確認**未被四份 crossref 掃描目標以
> 「DEF-ID（狀態宣稱）」樣式引用**）逐字搬移：DEF-101-201／225／249／256／262／265／269／
> 289／300／303／353／361／364／374／375／376／383／389／390／391／394／395／396／408／
> 413／414／415／426／429／430／436／437。
>
> **搬遷時機與挑選判準**：R58 動工實測主檔 251,191 bytes——已在 240KB（245,760）warn 帶內、
> 距 256KB（262,144）fail 硬閘僅 10,953 bytes，而 R57 單輪帳本毛增約 41KB，**先寫後歸檔必撞
> 硬閘**，故比照 archive_23／25 先例於**動工前**執行。挑選對象刻意取 **R16~R56 較舊、非近期
> 活躍敘事脈絡**的已結列，**刻意留在主檔**：DEF-101-478／486（R57 實質修復，被 `ONBOARDING.md`
> 與 `windows-compat-ci.yml` 正文提及——雖經 `_CLAIM_RE` 實測確認不構成狀態宣稱、技術上可搬，
> 仍保留以維持 R58 對帳）、491~501（R57 round 2~4 複審紀錄與體積治理交接）、502~506（R57 落地後
> 首次真 Windows 開機開工的五筆，R58 直接續辦其未竟事項）。立帳見主檔 R58 條目。

| ID | 發現日期 | 發現情境 | 缺陷描述（file:line 或依據） | 優先級 | 處置去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-101-201 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-3） | **mac nightly 取證留存性系統性弱於 Windows**：①（已修）plist stdout/stderr 原導 `/tmp/autoclaude_nightly.{log,err}`——macOS 週期清理＋重開機清空，深夜失敗數日後排查 log 已散失；②（殘）心跳檔僅 3 行彙總、無 Windows 側 RunId log＋latest pointer 對等物，取證紀律 #3「PASS 聲稱必須引 RunId log:L」在 mac 結構上無法履行 | P2 | ① 本輪修復；② 排下輪（mac RunId log 對等） | fixed@R14（①：`install_mac_nightly.sh` heredoc 改導 `AutoClaude/logs/nightly_mac_launchd.{log,err}`〔gitignored、與心跳檔同目錄〕＋install 時 `mkdir -p` 防 launchd 缺目錄靜默丟 log＋ONBOARDING §8 範本同步＋已載入 plist 冪等重裝生效實證＋smoke 11 綠；**②殘項 open 記於本列**，下輪評估 RunId log；**② fixed@R15**：`run_local_nightly.sh` RunId log 落地——`AutoClaude/logs/nightly_mac_<ts>.log`（`exec >>` 改道＋BEGIN 首行 run_id、心跳檔第 4 行 `log=` 指標、`find -mtime +14` 14 天輪替；**心跳前 3 行契約與檔名零改動**）＋真機取證 `nightly_mac_20260720_182753.log` 四 stage PASS=4 FAIL=0（AutoClaude 3684/144、SDD 1475+1674；取證紀律 #3 mac 側自此可履行，`Nightly_Forensic_Discipline.md` v1.8 同步 mac 註記）；**ARCH-R14-REV-2（launchd log 無輪替）以需求物理消滅結案**——輸出改道後 launchd log 僅剩 exec 前啟動錯誤自限量，不建輪替機制（Rule 2）） |
| DEF-101-225 | 2026-07-21 | R16 全面掃描（CI/排程基建維度） | **`run_local_nightly.sh` RunId log 缺觸發來源記錄 + 去重 check-then-act 無鎖（TOCTOU）**：本 repo 真實 log 顯示 2026-07-21 同日出現兩輪完整 PASS=4 執行且無 skip 訊息，因記錄不完整無法判斷是合理手動重跑還是真正去重漏洞；且 launchd `RunAtLoad` 與 `StartCalendarInterval(02:00)` 兩觸發源時間重疊時，兩行程可能同時通過「今日尚未有心跳」檢查，導致重複執行整套 nightly gate | P2 | BEGIN 行加印觸發來源；去重檢查外包檔案鎖 | fixed@R16：`TRIGGER_SRC` 判定（--force/`XPC_SERVICE_NAME`/互動終端/未知四態）寫入 BEGIN 行；本機無 `flock`，改用 POSIX `mkdir` atomic lock，陳舊鎖以 PID 存活判斷清除（比照 `dev_start.py` `_acquire_bootstrap_lock()`），`trap EXIT` 保證釋放。驗證：兩行程近乎同時呼叫，第二個正確被擋＋印跳過訊息；陳舊死 PID 鎖正確清除重取；三種觸發來源分支獨立驗證正確 |
| DEF-101-249 | 2026-07-22 | R20 全面掃描 + 真機驗證（真 Windows 11 機器，本輪首次對此腳本做非 `-WhatIf` 抽象層之外的真實 cmdlet 呼叫） | **P1，真機專屬缺陷**：`tools/install_windows_nightly.ps1` 呼叫 `New-ScheduledTaskSettingsSet -DisallowStartIfOnBatteries:$false -StopIfGoingOnBatteries:$false` 建立排程設定物件——這兩個參數名在此「建構」cmdlet 上根本不存在（真機實測 `(Get-Command New-ScheduledTaskSettingsSet).Parameters.Keys` 證實：正確參數名極性相反、名稱也不同，是 `-AllowStartIfOnBatteries`／`-DontStopIfGoingOnBatteries`；物件本身的屬性名才叫 `DisallowStartIfOnBatteries`／`StopIfGoingOnBatteries`，`fix_nightly_catchup.ps1` 讀寫既有任務走的正是這兩個屬性名，那裡沒錯，兩者極易混淆）。真機呼叫 `powershell -File tools\install_windows_nightly.ps1 -WhatIf` 直接拋 `ParameterBindingException`（NamedParameterNotFound）——**連 `-WhatIf` 預覽路徑都會 crash，並非只影響真安裝**，因為 PowerShell 在决定是否 ShouldProcess 之前就先做參數綁定。R11~R19 歷輪皆只對此腳本做語法解析（`[Parser]::ParseFile`）與靜態文字斷言，從未真的呼叫過這個 cmdlet（`test_install_windows_nightly.py` 檔頭本就明文「非 Windows 主機無法真的執行」），故此缺陷從 R19 引入至今從未被發現，**是本輪身為「首次真 Windows 機器複審」而非純靜態/跨平台鏡射複審的最高價值發現** | P1 | 改用正確參數名 `-AllowStartIfOnBatteries`／`-DontStopIfGoingOnBatteries` | fixed@R20：`$settings = New-ScheduledTaskSettingsSet -StartWhenAvailable -WakeToRun -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries`；真機驗證：`New-ScheduledTaskSettingsSet` 建構後讀回 `.DisallowStartIfOnBatteries`/`.StopIfGoingOnBatteries`/`.StartWhenAvailable`/`.WakeToRun` 四屬性值皆為 `False/False/True/True`（符合預期極性）；`-WhatIf` 真機重跑成功印出 `What if: ...Register-ScheduledTask...` 且 exit=0（原本的安裝路徑本身也缺一個明確的結尾 `exit 0`，一併補上，對齊 `-Uninstall`/`-Status` 兩分支既有的明確 exit 慣例）。新增 `tools/tests/test_install_windows_nightly.py::TestInstallWindowsNightlySettingsConstruction`（`platform.system()=="Windows"` 才跑，windows-latest CI runner 可安全真跑此 cmdlet、不需系統管理員權限）：直接從原始碼抽出 `$settings = New-ScheduledTaskSettingsSet ...` 該段文字、原封不動丟給真正的 PowerShell 執行並斷言四屬性值——bug-injection 驗證：把參數名改回原錯誤版本，此測試與既有 `test_catchup_settings_match_fix_nightly_catchup_target_values`（同步訂正為斷言正確參數名、禁止錯誤參數名）皆確認變紅，還原後核對逐位元組一致 |
| DEF-101-256 | 2026-07-22 | R21 全面掃描（B 維度：Python 跨平台）＋四方一審 Architect/SA/SD/QA 交叉技術指引 | `tools/tests/_platform_helpers.py::copy_functional_interpreter()` 只複製直譯器 exe 本體＋條件式 `pyvenv.cfg`，沒有複製同層相依 DLL；當 `sys.executable` 本身**不是**透過 venv 執行時（範圍：任何未啟用 venv 的官方支援直譯器安裝路徑——`ONBOARDING.md` §1 列出的 winget／python.org 安裝器版型、與 pyenv-win，皆會命中同一情境；uv 管理的直譯器因走 `pyvenv.cfg` 分支已被涵蓋），複製出的孤立 exe 因缺少同層 DLL（如 `python311.dll`、`vcruntime140.dll`）在 Windows 上啟動失敗，`subprocess` 回傳 `rc=3221225781`（`0xC0000135` `STATUS_DLL_NOT_FOUND`），導致 `tools/tests/test_dev_start.py` 6 個呼叫 `copy_functional_interpreter` 模擬「健康既有直譯器」情境的測試假紅——純測試基建缺陷，不影響 production（已核實 `tools/dev_start.py` 全檔 `_venv_healthy()` 及全檔 grep `shutil.copy/copytree` 皆與此無關，production 從不複製直譯器） | P2 | `copy_functional_interpreter()` 新增無條件（不做 `if is_windows()` 平台分支）DLL glob-and-copy：來源目錄用獨立命名變數 `interpreter_dll_dir = Path(sys.executable).parent`（與既有 pyvenv.cfg 分支的 `.resolve().parent.parent` 完全分開，避免混用同一表達式致 glob 恆空的隱蔽退化），限縮為具名 pattern `python3*.dll`／`vcruntime140*.dll`（非裸 `*.dll` 全複製，避免誤複製 sqlite3/libssl/tcl-tk 等不必要的 DLL）；macOS/Linux 上 `sys.executable` 同層通常沒有 `.dll` 副檔名檔案，glob 自然空手，天生 no-op，三平台行為一致 | fixed@R21：`tools/tests/_platform_helpers.py::copy_functional_interpreter()` 新增 DLL 複製邏輯並更新 docstring；`tools/tests/test_dev_start.py` 新增 `TestCopyFunctionalInterpreterDllCopy`（環境無關自證測試，monkeypatch `sys.executable` 指向暫時假來源目錄）兩支測試：`test_copies_named_dll_patterns_beside_dest`（斷言具名 pattern 被複製、無關 DLL 不被複製）與 `test_no_dlls_present_is_a_safe_noop`（無 DLL 來源時安全 no-op）皆綠燈；既有 6 個呼叫點測試（`test_dev_start.py` 全套 136 tests OK, skipped=9）不受影響。**真機 e2e 驗證**（用本機真實 pyenv-win 直譯器 `C:\Users\wuwei\.pyenv\pyenv-win\versions\3.11.9\python.exe`，monkeypatch `sys.executable` 模擬「PATH 只掛 pyenv-win、未啟用 `.venv`」情境）：修復前（暫時 `git stash` 還原舊版邏輯）複製出的直譯器 `--version` 執行 `rc=3221225781`（`hex=0xc0000135`，與缺陷描述完全吻合），且 `tools/tests/test_dev_start.py` 在此情境下用 pyenv-win 直譯器跑會有 7 個測試 FAILED（既有 6 個呼叫點 + 新增 1 個自證測試）；`git stash pop` 還原修復後，同一情境複製出的直譯器 `--version` 執行 `rc=0`、`stdout=b'Python 3.11.9\r\n'`，`test_dev_start.py` 用 pyenv-win 直譯器全套 136 tests OK（skipped=9）全綠 |
| DEF-101-262 | 2026-07-22 | R23 四方前置掃描（P3/P4）：ruff 存量 baseline 數字新鮮度掃描 | `docs/06_quality/AutoSDD_Defect_Log.md`（DEF-101-018 條目）、`ONBOARDING.md` §9 表尾對照段（約第 260 行）、`AutoClaude/pyproject.toml:32` 行內註解三處皆寫「baseline 1,339 筆」，但本輪於 `AutoClaude/` 下實跑 `.venv/Scripts/python.exe -m ruff check . --statistics`（鎖版 0.15.21）實測得 **1,147** 筆——純描述性文字漂移（`grep -rn "1339"` 只命中這三處敘述文字，無任何測試/gate 拿 1,339 當機械斷言比對） | P3～P4（純文件數字新鮮度；無機械斷言受影響） | 三處皆更新為實測數字，並註明「R23 複審實測更新，原數字為舊測值」避免未來誤以為機械釘選 | fixed@R23：`AutoClaude/pyproject.toml:32` 行內註解、`docs/06_quality/AutoSDD_Defect_Log.md` DEF-101-018 列、`ONBOARDING.md` §9 表尾對照段三處皆訂正為「baseline 1,147 筆」並註記 R23 實測來源；`grep -rn "1339\|1147" docs/ ONBOARDING.md AutoClaude/pyproject.toml` 複核三處新數字一致、無殘留舊值 |
| DEF-101-265 | 2026-07-23 | R24 四方前置掃描（P3）：Scan-C CI/CD 與排程機制掃描，`windows-compat-ci.yml` 與 `ONBOARDING.md` 揭露對稱性核對 | **`windows-compat-ci.yml`／`ONBOARDING.md` 未揭露 GitHub-hosted `windows-latest` runner 實際對應 Windows Server 2022（伺服器 SKU）、與宣稱標的「Windows 11」用戶端環境的潛在落差**：mac 側 `ONBOARDING.md`（macos-compat-ci.yml 段落）已有明文揭露句「GitHub-hosted `macos-latest` runner 與開發者個人 Mac 的實際硬體／OS 版本仍可能有落差」，但 Windows 側自稱目標更具體（明確宣稱「Windows 11」而非泛稱作業系統）卻反而沒有對等揭露句，屬揭露不對稱 | P3（文件揭露缺口，不涉程式邏輯；電源管理/排程相關 API 如 `schtasks`／`New-ScheduledTaskSettingsSet` 行為在伺服器 SKU 與用戶端 SKU 間可能有出入，見 DEF-101-002／249） | 於 `ONBOARDING.md` §8 比照 mac 側句式補一段對等揭露 | fixed@R24：`ONBOARDING.md` §8 新增「R24 對稱補揭露」一段，明文揭露 `windows-latest` 現行對應 Windows Server 2022、與開發者個人 Windows 11 用戶端環境仍可能有落差，純文件補強。**一審訂正（R24，QA 一審獨立核實行號後命中）**：原「R10 Windows 側對等補償段落**後**」方向寫反，實際插入位置在 R10 段落**之前**（緊接 R11 補驗紀律段落後），內容本身正確、僅描述方向性筆誤 |
| DEF-101-269 | 2026-07-23 | R26 全面掃描 Scan-C（CI/排程基建維度） | **`windows-compat-ci.yml` 從未在真實 GitHub Actions 執行 `tools/install_windows_nightly.ps1`（即使是安全的 `-WhatIf` 預覽）**：對照 `macos-compat-ci.yml` 第 253-256 行，`macos-smoke` job 在真正的 `macos-latest` runner 上會執行 `bash tools/install_mac_nightly.sh --render-only ...` + `plutil -lint`；但 `windows-compat-ci.yml` 的 `windows-smoke`／`windows-nightly-full` 兩個 job 皆無任何一步呼叫 `install_windows_nightly.ps1`，唯一會執行它（`-WhatIf` 預覽）的地方只有開發者手動本地跑的 `tools/windows_smoke_local.ps1` [8/8]——沒人手動跑就永遠不會被驗證，形成「文件宣稱雙平台對稱、CI 自動化實際不對稱」的落差 | P2（既有大量結構驗證與真機 settings-construction 測試皆非「真實 GitHub Actions windows-latest runner 自動觸發」這層，與本 repo「compensating control 不能取代真正閘門」一貫紀律不符） | 比照 `windows_smoke_local.ps1` [8/8] 既有寫法，在 `windows-smoke` job 新增一步以 `-WhatIf` 呼叫該腳本並斷言輸出含 `Register-ScheduledTask` | fixed@R26：`.github/workflows/windows-compat-ci.yml` 於 `tools/run_root_unittests.py` 步驟後新增「install_windows_nightly.ps1 -WhatIf 預覽」步驟，以獨立子行程 `powershell.exe -File ... -WhatIf` 呼叫並 `2>&1` 合流捕捉輸出（子行程 ShouldProcess 訊息屬原生 stdout），斷言 `$LASTEXITCODE -eq 0` 且輸出含 `Register-ScheduledTask`（逐字鏡射 `tools/windows_smoke_local.ps1` [8/8] 既有驗證手法）；YAML 經 `yaml.safe_load` 語法驗證通過；`tools/tests/test_pre_push_dispatcher.py`／`test_smoke_ci_sync.py`／`test_workflow_permission_concurrency_lock.py` 三份既有引用該檔內容的測試複跑 20 tests 全綠、無回歸（新增步驟未改動既有 job/step 結構，僅插入一步）。**🔴 一審 QA bug-injection 揭露鑑別力侷限（誠實記載，非阻斷）**：QA 一審實測將 `install_windows_nightly.ps1` 真正的 `Register-ScheduledTask` cmdlet 呼叫整段刪除、只留 `$PSCmdlet.ShouldProcess($TaskName, 'Register-ScheduledTask')` 字串字面量，重跑 `-WhatIf` 後 CI 新步驟的斷言**依然通過**（PowerShell `ShouldProcess()` 的 "What if:" 訊息只是印出呼叫時傳入的字串參數，與是否真的執行對應 cmdlet 無關）。故此步驟只驗證「`-WhatIf` 預覽路徑的字面量沒被改壞」，**不驗證「非 WhatIf 的真實安裝邏輯」本身是否正確**——若未來只有第 146 行真實呼叫被改壞（例如打錯參數）而第 141 行字串未動，此 CI 步驟會誤判通過。此為「從零覆蓋提升至部分覆蓋」的淨改善，而非「完整覆蓋」，記載於此避免未來輪次高估其鑑別力（呼應 SA 一審獨立指出的「驗證強度弱於 macOS 側 `plutil -lint`」同一類觀察，QA 以實際 bug-injection 坐實）。 |
| DEF-101-289 | 2026-07-24 | R32 全面掃描 Scan-D | **`ONBOARDING.md` §7 pytest 基線唯一真相源字面數字已落後實測 +11（3,608→3,619，R27~R31 累積新增測試未回填）**：與 DEF-101-266（曾因 +1 落差記為 P4）同一根因、同一機制（無自動同步機制），僅這次落差擴大。R31 commit（`009c5fe`）本身的最終回歸驗證結果已寫明 3619 passed，但未同步觸碰 ONBOARDING.md | P3 | 重新量測並更新 §7 數字 | fixed@R33：R33 Scan-D 在**全新臨時目錄**建立乾淨 venv（`pip install -e '.[dev,notifications]'`，未裝 postgres/pgvector 選配，`pip list` 確認乾淨），巢狀 session 下實測 **3,644 passed / 210 skipped**；與 R32 污染值（3742/146）交叉比對 skip 數落差方向吻合污染假說，判定本次數字可信。`ONBOARDING.md` §7 已同步更新為此數字並訂正輪次標記為 R33（`bootstrap 出廠環境`〔非巢狀 session〕分項數字不在本次驗證範圍，維持原「待重驗」標示） |
| DEF-101-300 | 2026-07-24 | R34 Architect 架構深度評估 | **`tools/dev_start.ps1` 的 WindowsApps 空殼別名排除 guard（第 38-41 行）是第 4 個獨立實作**（與 `tools/bootstrap.ps1`、`tools/bootstrap_core.py::_is_windows_apps_stub()` 同規則、各自獨立寫），且**零測試覆蓋**——`tools/tests/test_dev_start.py` 只測 Python 核心，`check_wrapper_thinness.py` 只釘 SHA256 不驗語意，屬 DEF-101-273/279/281 同一缺陷類別第 4 次出現的結構性復發風險 | P2（同一缺陷類別的結構性復發；三處目前內容一致，但下次任一處改動不會觸發交叉告警） | 比照 DEF-101-295 手法新增交叉一致性測試鎖（不合併程式碼——`dev_start.ps1` 需在呼叫 Python 核心「之前」自行找到直譯器，屬 bootstrapping 先有雞或蛋的架構限制） | fixed@R34：新增 `tools/tests/test_windowsapps_guard_cross_consistency.py`（4 case：3 個靜態文字擷取交叉核對三處 guard 語意一致 + 1 個 dev_start.ps1 行為回歸鎖，比照 `test_bootstrap_ps1.py` 手法，複製到零 `.venv` 臨時目錄執行避免真實 `.venv` 短路 guard 分支）。bug-injection 驗證：拔掉 guard 後兩個靜態測試立即抓到（如實記載已知侷限：behavioral 測試在 macOS `pwsh` 上因 `Get-Command` 不做 Windows PATHEXT 解析而對此特定 bug 無鑑別力，只在真 Windows PowerShell/`windows-compat-ci.yml` 上才有效——`test_bootstrap_ps1.py` 同款既有測試亦有此侷限，非本輪新增問題）。**自我發現的連鎖回歸**：新測試檔以路徑直接引用 `tools/dev_start.ps1`，`AISDLC_SDD ci-gate.sh` 全套回歸時 `test_ci_paths_cover_root_consumers.py` 機械攔下 `macos-compat-ci.yml` paths 未涵蓋此消費檔（DEF-101-042 同構，比照 R27 `tools/bootstrap.ps1` 同款前例），已於 push/PR 兩份 paths 補上一行修復，10/10 passed |
| DEF-101-303 | 2026-07-24 | R34 四方二審 SD（用新角度 bug-injection 發現） | **`bootstrap.ps1` 若把 `$PyCand`/`$Py3Cand` 的賦值互換（`Get-Command python` 結果存入 `$Py3Cand`、`Get-Command python3` 結果存入 `$PyCand`，guard 邏輯本身逐字不動）**，`test_windowsapps_guard_cross_consistency.py` 現有三項靜態測試（含 DEF-101-300/302 修復的四項）皆只驗證「變數名與自己的 guard 配對」，不驗證右手邊 `Get-Command` 呼叫的命令名稱是否與變數語意相符，故全數漏放行。此為手誤型 bug，觸發機率低（需刻意寫反或複製貼上時看錯變數名），SD 判定非阻擋 | P3（低機率邊界情況，backlog；SD 明確判定不阻擋本輪 APPROVE） | 未來可補一條「候選命令名稱與變數語意對應」的靜態鎖，或依賴既有 Windows-only 行為回歸鎖（`test_bootstrap_ps1.py::test_real_python_outside_windowsapps_is_used_even_when_windowsapps_stub_present_first`）在真 Windows CI 上的巧合覆蓋 | **fixed@R37**：Architect 架構深度評估（R37）判定此缺陷根因是 `bootstrap.ps1`/`dev_start.ps1` 三處 WindowsApps guard 各自內嵌 `$PyCand`/`$Py3Cand` 中繼變數複製貼上，比照既有 `tools/lib/Find-GitBash.ps1` 先例收斂成共用函式 `tools/lib/WindowsAppsGuard.ps1::Test-IsRealPython`，三處呼叫端改為 `Test-IsRealPython -CandidateName 'python'`／`'python3'` 單行字面值呼叫，不再有可互換的中繼變數。四方一審 Architect/SA/SD/QA 皆親自驗證兩檔已無殘留 `$PyCand`/`$Py3Cand`，且新測試 `test_bootstrap_ps1_calls_shared_function_for_both_python_and_python3` 機械鎖住呼叫端字面值恰為 `'python'`/`'python3'` 各一次。Architect 二審補充措辭精確度：此修復消滅的是「兩變數互換」這一特定錯配類型，非涵蓋所有可能的呼叫端手誤（如字面值本身打錯，惟此類錯誤可見度遠高於原本埋在判斷式裡的變數名，且已被上述測試鎖住）。SD 一審另以 bug-injection 找到「呼叫端用額外 `-or` 條件覆蓋共用函式回傳值」這一新繞過手法對本輪兩份 WindowsApps guard 測試檔無鑑別力，但確認既有 `tools/check_wrapper_thinness.py` 正規化內容 hash 釘選（跨平台可用、已接入本機 CI 對等與 pre-push 紀律）構成有效正交防線，判定 P3 非阻擋，見 DEF-101-313 |
| DEF-101-353 | 2026-07-25 | R43 Scan-B（背景 agent 全面掃描：檔名淨化/防增生鎖覆蓋率） | **WindowsApps 空殼排除 guard 只收斂 `.ps1`（`tools/lib/WindowsAppsGuard.ps1`，R37）與 `.py`（`bootstrap_core.py::_is_windows_apps_stub`，R31）兩種語言，repo 內 9 支 tracked bash 腳本從未排除 Windows Store App Execution Alias 空殼**：`tools/git-hooks/pre-push`（每次 push 都會實際執行的 dispatcher 本體，2 處）、`tools/lib/git_hooks_install_common.sh`、`tools/dev_start.sh`、`tools/bootstrap.sh`、`tools/integration_gate.sh`、`AutoClaude/tools/local_ci_gate.sh`、`AutoClaude/tools/run_act.sh`、`AutoClaude/tools/sd06_w3_staging_dryrun.sh`、`AISDLC_SDD/scripts/ci-gate.sh` 皆用裸 `command -v python`/`command -v python3` 判斷可用性——Git Bash on Windows 會繼承 Windows PATH，同樣會命中 `%LOCALAPPDATA%\Microsoft\WindowsApps` 底下系統自動註冊的空殼，`command -v` 判定為「存在」，實際執行只會跳出 Microsoft Store 安裝提示，對 `pre-push` 這類阻斷式 hook 而言即為掛起 | P2（系統性預防缺口，全新未裝真 Python 的 Windows 11 機器上會實際觸發） | 本輪修復 | fixed@R43：新增 `tools/lib/windowsapps_guard.sh::is_real_python_candidate()` 共用函式（bash 側對稱 SSOT），改寫上述 9 個呼叫端一律改用該函式；同步更新 `tools/check_wrapper_thinness.py`（5 支 `.sh` hash 釘選）、`tools/check_script_parity.py`（`_SINGLE_SIDED_EXEMPT` 新增登記，並訂正 `WindowsAppsGuard.ps1` 舊註解「POSIX 側無 WindowsApps 概念」的錯誤判斷——本輪推翻）、`.github/workflows/aisdlc-sdd-ci.yml`／`windows-compat-ci.yml` 的 `paths:` 過濾器補上新消費檔（過程中兩輪才補齊：`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 揪出第一輪只補了 `windowsapps_guard.sh` 本身、漏了另外 7 個被新測試引用到的呼叫端路徑）、`AISDLC_SDD/scripts/tests/test_ci_gate_version_resolution.py` 與 `tools/tests/test_pre_push_dispatcher.py` 兩份沙盒 fixture（因新依賴需同步備妥該檔，兩者皆一度因路徑計算錯誤失敗，訂正為巢狀 `sandbox = Path(td)/"AISDLC_SDD"` 結構後修復）。新增 `tools/tests/test_windowsapps_guard_bash_parity.py`。四方一審 Architect/SD 各自獨立發現本修復本身仍有缺口，收斂於 DEF-101-355 |
| DEF-101-361 | 2026-07-25 | R44 ActiveFix 步驟 4（同批掃描找出的 LATEST／可修改版本零可用性判斷缺口） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/arch_fitness/run_self_evolution.sh`／`.ps1` 與 `AISDLC_SDD/scripts/copy_on_evolve.sh` 三支「python 呼叫零可用性判斷」**：前兩者為 LATEST（v0.30）版本，第三支為 monorepo 根層 bash-only 工具（`_SINGLE_SIDED_EXEMPT` 已登記無 `.ps1` 對等），皆裸呼叫 python 未排除 WindowsApps 空殼 | P2 | 本輪修復（LATEST／可修改版本，非凍結基線，不受 Copy-on-Evolve 限制） | fixed@R44：`run_self_evolution.sh` dot-source `tools/lib/windowsapps_guard.sh` 後 `is_real_python_candidate python \|\| exit 5`；`run_self_evolution.ps1` dot-source `tools/lib/WindowsAppsGuard.ps1` 後 `Test-IsRealPython`，找不到 `exit 7`；`copy_on_evolve.sh` 因既有孤立測試 harness（`test_copy_on_evolve.py::_run()` 會把腳本複製到與 monorepo 結構脫鉤的 tmp git repo）會讓 guard 檔案在隔離環境物理不存在，改採本檔既有的「sibling 缺席即優雅略過並 warn」慣例（非直接 fail-loud）、`PYTHON` 環境變數已指定時跳過判斷。驗證：對抗式 bug-injection（`env -i` 清空 PATH 執行 `run_self_evolution.sh` 確認 exit 5；塞入假 WindowsApps 空殼確認未被誤執行；正常 PATH 下 dry-run 正常）；`copy_on_evolve.sh` 在完整佈局（PATH 缺 python 時 exit 1、`PYTHON=` 指定時繞過）與孤立測試 harness（guard 檔案缺席時優雅略過不中止）兩種情境下皆驗證行為正確；`python -m pytest tools/tests/test_windowsapps_guard_bash_parity.py tools/tests/test_windowsapps_guard_cross_consistency.py -q` 47 passed；`cd AISDLC_SDD/AISDLC_SDD_v0.30 && python -m pytest tools/fsm_runtime/tests/ -m "not chaos" -q` 1721 passed/8 skipped（無回歸）；`cd AISDLC_SDD && python -m pytest scripts/tests/ -q` 191 passed/1 skipped（含 `test_copy_on_evolve.py` 11 項） |
| DEF-101-364 | 2026-07-25 | R44 ActiveFix 步驟 1（架構優化提案落地，呼應既有「防增生鎖只蓋到已知案例」系統性缺陷家族，如 DEF-101-355 對 `_sanitize_component` 呼叫點的同款教訓） | **repo-wide 前瞻掃描鎖（bash 側 `tools/tests/test_windowsapps_guard_bash_parity.py`、ps1 側 `tools/tests/test_windowsapps_guard_cross_consistency.py`）原本只能偵測「有做可用性判斷但繞過 SSOT」，偵測不到「完全零可用性判斷、直接裸呼叫 python」這種更原始的缺口形狀**（即 DEF-101-359／360／361 點名的檔案，本次擴充前完全不會被既有鎖攔下） | P2（防增生鎖覆蓋率破口，本身即是本輪 DEF-101-360／361 得以被系統性發現的關鍵前置修復） | 本輪落地 | fixed@R44：bash 側新增 `_has_zero_guard_python_call` ＋ `test_repo_wide_scan_finds_no_zero_guard_python_calls`（改為 repo-wide 全掃全部 tracked `*.sh`，新增 `_exclude_frozen_sdd_versions` 排除凍結版本、`_EXEMPT_SH_FILES` 附理由登記 `AutoClaude/tools/run_mutmut_in_docker.sh`〔docstring 明文僅在 Linux container 內執行，guard 語言邊界結構性不適用〕）；ps1 側新增 `_invokes_python_in_ps1` ＋ `test_python_calls_in_ps1_all_go_through_ssot`（改為 repo-wide 全掃全部 tracked `*.ps1`，不再要求先提及 WindowsApps 字面值，新增 `_EXEMPT_PS1_FILES` 登記 `AISDLC_SDD/scripts/ci-gate.ps1`〔裸呼叫僅存在於「找不到 Git Bash」的已知降級 fallback 分支〕與 `AutoClaude/tools/g0_gate_check.ps1`〔排程／人工維運後續檢查腳本，非開發者第一次接觸 repo 的進入點〕——**此兩筆豁免為執行 agent 自行判斷、非題目明確核准，建議日後複核是否成立**）。red/green 對照驗證：擴充後（DEF-101-360／361 修復前）bash 側新測試紅、列出恰為 2 個檔案（DEF-101-361 的兩支 `.sh`）；ps1 側新測試紅、列出恰為 7 個檔案（DEF-101-360 的 6 支 ＋ DEF-101-361 的 1 支 `.ps1`）；DEF-101-360／361 修復落地後兩測試轉綠。另補 13 個 helper 純函式單元測試（涵蓋已收斂／裸判斷無 SSOT／完全零檢查／變數預設值間接呼叫／整行註解／區塊註解／字串常值內提及等情境）。對抗式驗證：對 `AutoClaude/tools/local_ci_gate.ps1`／`AISDLC_SDD/scripts/copy_on_evolve.sh` 真實原始內容跑 helper 確認 flagged=True，模擬「補上 SSOT guard」後的內容確認 flagged=False，證實掃描邏輯有真實正反鑑別力。回歸：`python -m pytest tools/tests/test_windowsapps_guard_bash_parity.py tools/tests/test_windowsapps_guard_cross_consistency.py -q` 47 passed |
| DEF-101-374 | 2026-07-25 | R45 全新掃描 agent 回報，主控 zero-trust 複驗（不預設 agent 結論為真） | **[已複驗為假陽性] 掃描 agent 聲稱 `AutoClaude/tools/scaffold_sprint_section.py:148` `print(skeleton)` 印 emoji（🟡）在 Windows 非 UTF-8 主控台會 `UnicodeEncodeError` 崩潰**：主控直接以 `PYTHONIOENCODING=cp1252 python tools/scaffold_sprint_section.py --sprint 99 --title test --dry-run` 實際重現，結果 exit code 0、正常輸出，未崩潰。根因：該檔 `if __name__ == "__main__":` 區塊已在呼叫 `main()` 前呼叫 `tools/lib/platform_utils.py::init_utf8_streams()`（以 `io.TextIOWrapper` 重新包裝 `sys.stdout`/`sys.stderr` 為強制 UTF-8），只是防護手法與 agent 預期的 `.reconfigure()`／`tools/_stdio_utf8.py` 不同，agent 只檢查了後者兩種手法是否存在便下結論，未實際執行驗證 | P4（agent 誤報，非程式缺陷；記載以防未來輪次重複調查同一假陽性） | 不修復（本來就不是缺陷） | closed-by-decision：主控實測驗證為假陽性，`tools/lib/platform_utils.py::init_utf8_streams()` 保護已足夠，未做任何程式變更 |
| DEF-101-375 | 2026-07-25 | R45 全新掃描 agent 回報，主控 zero-trust 複驗（不預設 agent 結論為真） | **[已複驗為假陽性] 掃描 agent 聲稱 `AutoClaude/tools/c6_staging_validator.py` 用 `logging.basicConfig()`（未用專案 `_EncodingSafeStreamHandler`）記錄 emoji（✅/❌）在 Windows 非 UTF-8 環境會拋未捕捉例外崩潰，且聲稱已「實測驗證」推翻「logging 會靜默吞掉編碼錯誤」的預設假設**：主控獨立以檔案腳本（非 inline -c，避免 shell 轉義干擾）在 `PYTHONIOENCODING=cp1252` 下重現 `logging.basicConfig()` 預設 handler 記錄 emoji，exit code 0、正常執行完畢；進一步用診斷腳本確認 `sys.stderr.errors` 預設為 `backslashreplace`（CPython 對 stderr 的內建預設值，不受 `PYTHONIOENCODING` 覆寫、不分平台），而 `logging.basicConfig()` 未指定 `stream=` 時預設輸出至 `sys.stderr`（非 stdout）——故該檔全部 `logger.*()` 呼叫（含第 281-282／323 行）天生受 CPython 這道預設防護，不會如 agent 所稱崩潰。`_EncodingSafeStreamHandler`（DEF-87-002）要防護的是**明確綁定 `sys.stdout`** 的 handler（stdout 預設 `errors='strict'`，才會真的崩潰），與本檔用預設 stderr handler 是不同情境 | P4（agent 誤報，非程式缺陷；記載以防未來輪次重複調查同一假陽性） | 不修復（本來就不是缺陷） | closed-by-decision：主控實測驗證為假陽性，CPython stderr 預設 `backslashreplace` 已提供防護，未做任何程式變更 |
| DEF-101-376 | 2026-07-25 | R45 Architect 架構複審建議（建議 4：WindowsApps guard regex 加 canary 自我檢查），主控查證既有測試檔內容後發現建議已被既有程式碼滿足 | **[已複驗為既有覆蓋] Architect 建議「`_has_zero_guard_python_call` 等 regex 缺自我檢查 canary fixture」，實查 `tools/tests/test_windowsapps_guard_bash_parity.py::TestHasZeroGuardPythonCall`（6 case）／`TestHasUnmigratedRawCheck`（5 case）與 `tools/tests/test_windowsapps_guard_cross_consistency.py::TestInvokesPythonInPs1`（7 case）／`TestAllPythonInvocationsAreSsotProtected`（7 case）已對這些 regex 做了詳盡的已知會命中/不命中 fixture 字串單元測試**，建議所指的缺口實際不存在——Architect 應為依一般印象推論、未逐檔開啟這些測試檔核對內容細節 | P4（Architect 建議依據不足，非程式缺陷；記載以防未來輪次重複調查） | 不新增（本來就已覆蓋） | closed-by-decision：主控逐檔核對既有 fixture 測試內容確認已充分覆蓋，未新增任何測試 |
| DEF-101-383 | 2026-07-26 | R47 四方複審修復輪（r1 套件：AISDLC_SDD `install_post_commit.ps1` WindowsApps guard 覆蓋掃描） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`（v0.30＝現行 LATEST，經 `sdd_version.py` 確認）生成的 bash post-commit hook，python 偵測為裸 `command -v python \|\| command -v python3`，與同目錄 `.sh` 版生成器已具備的 WindowsApps guard（source `tools/lib/windowsapps_guard.sh` + `is_real_python_candidate`）不對稱——全新未裝真 Python（或 WindowsApps App Execution Alias 排前面）的 Windows 機器上，`.ps1` 安裝出的 hook 可能誤判空殼 `python.exe` | P1（與 DEF-101-381／382 同家族：WindowsApps guard 覆蓋不對稱，全新 Windows 機器可能卡住 commit；屬既有 guard 覆蓋掃描的殘留缺口，非本輪新增攻擊面） | 本輪修復：`.ps1` 生成器 here-string 改為比照 `.sh` 側「先 source guard、guard 檔存在才 `is_real_python_candidate` 判斷、guard 缺席則優雅退化為原裸 `command -v`」 | fixed@R47：`install_post_commit.ps1` 新增 `$GuardSrcBash` 變數並改寫 here-string 的 hook-content；新增測試 `test_installer_writes_hook_referencing_shared_bash_guard` 端到端跑真實 `.ps1` 安裝器並讀生成的 `.git/hooks/post-commit` 內容斷言其引用 `windowsapps_guard.sh` 與 `is_real_python_candidate`。驗證：`pytest scripts/tests/test_install_post_commit_windowsapps_guard.py scripts/tests/test_install_post_commit_sh_windowsapps_guard.py -v` → 7 passed, 2 skipped（2 skip 為既有 Windows-native-only PATHEXT 測試，非本輪新增缺口）；新增測試 PASSED；`test_install_post_commit_exec_bit.py` → 1 passed；`tools/check_script_parity.py`／`tools/check_wrapper_thinness.py` 皆 exit 0。**本輪（R47）QA 對抗性 bug-injection 複審**：QA 刻意反轉 `is_real_python_candidate` 的判斷邏輯（模擬「guard 條件寫反」的迴歸），測試新增的 `test_installer_writes_hook_referencing_shared_bash_guard` 是否能抓到，結果發現該測試**只對生成出來的 hook 文字做字串比對**（斷言文字中出現 `windowsapps_guard.sh`／`is_real_python_candidate` 這兩個字串），**從未真正執行生成出來的 hook**——邏輯反轉後的迴歸仍會通過字串比對、對此類迴歸零鑑別力（本輪出貨的修復本身邏輯正確、並未寫反，此為測試鑑別力縫隙而非交付缺陷）。**已於同輪由另一並行套件強化修復**：比照 `.sh` 側既有姊妹測試已具備的做法，把該測試改為端到端**真正執行**生成出來的 hook 而非僅字串比對，並以 QA 同一套 bug-injection 反轉手法覆驗，證實強化後的測試確實會轉紅、能抓到邏輯反轉迴歸。狀態維持 fixed@R47（同輪內測試強化，非新開缺陷）。 |
| DEF-101-389 | 2026-07-26 | R48 四方複審跨平台掃描（Scan-A／Scan-B／Scan-C） | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1:96` `$GuardSrcBash = Join-Path $MainCheckoutRoot "tools\lib\windowsapps_guard.sh"` 供 hook 內容中**由 bash 自己**`[ -f ]`／source 解析，原生 Windows PowerShell 5.1 的 `Join-Path` 不正規化反斜線（不同於 pwsh 跨平台版），導致 bash 在 Windows 上可能吃不到此路徑，靜默落回裸 `command -v python`（R47 DEF-101-383 才修掉的同一 P1 情境復發，只是這次是修復手法本身被路徑分隔符架空） | P1（全新未裝真 Python、WindowsApps 空殼 python.exe 排 PATH 最前的 Windows 11 機器，commit 可能卡住） | 本輪修復：該行後補 `-replace "\\", "/"` 正規化為正斜線（僅動此變數，`$HookSrcDrift`/`$HookSrcClosure` 為傳給 python.exe 的 argv，不受影響、不改動） | fixed@R48：新增 `scripts/tests/test_install_post_commit_windowsapps_guard.py::test_guard_src_bash_path_embedded_in_hook_has_no_backslash` 靜態不變量鎖（docstring 誠實記載本環境無法動態重現原生 PS5.1 場景）；`python -m pytest scripts/tests/test_install_post_commit_windowsapps_guard.py -v` 8 passed / 2 skipped（Windows-native-only）全綠。**R48 四方複審 QA 發現**上述唯一一條測試在 pwsh-on-macOS 環境下對本修復零鑑別力（pwsh 的 `Join-Path` 會自動正規化反斜線，退回修復前的寫法此測試在本機仍會綠燈），故新增第二條 `test_guard_src_bash_replace_expression_normalizes_backslash_independent_of_join_path`——不依賴 `Join-Path`，直接從安裝器原始碼萃取 `$GuardSrcBash = $GuardSrcBash -replace ...` 這一整行陳述式，在獨立 pwsh 子行程中對「用字串串接模擬原生 PS5.1 Join-Path 保留反斜線」的輸入套用該陳述式斷言結果不含反斜線，具備真實鑑別力，並經 bug-injection（移除該行）證實會轉紅。**R48 四方複審 Architect 發現**上述兩條測試僅涵蓋 pytest 面，整個 CI 矩陣從未用原生 Windows PowerShell 5.1（`shell: powershell`，非 `shell: pwsh`）實際執行過 `install_post_commit.ps1`——`windows-compat-ci.yml` 的 `windows-nightly-full` job 因此新增一個 `shell: powershell` 步驟（緊接既有「Windows PowerShell 5.1 驗證 bootstrap.ps1 + dev_start.ps1」步驟之後），實際執行安裝器並斷言產出的 hook 內嵌 `windowsapps_guard.sh` 來源路徑字串不含反斜線；因本機 macOS 無 `powershell.exe`，此步驟本身無法在本機驗證，僅完成 YAML 語法檢查（`python3 -c "import yaml; yaml.safe_load(...)"`）與人工覆核，需待下次真 Windows nightly 執行時才會有機械訊號，此限制與既有「bootstrap.ps1/dev_start.ps1」PS5.1 步驟從一開始就有的限制相同 |
| DEF-101-390 | 2026-07-26 | R48 四方複審跨平台掃描（Scan-A／Scan-B／Scan-C） | `AutoClaude/autoclaude/utils/checkpoint_manager.py:129`（修復前）`checkpoint_path()`/`exists()` 繞過檔名淨化，直接用 `self._to_id(playbook_path)` 組檔名，與 `save()`/`load()`/`clear()` 委派 `FileStateRepository._path()`（R47 DEF-101-384 已改用 `_sanitize_log_filename`）不一致——`playbook_path="con.yaml"` 時 `save()` 實際寫入 `_con.checkpoint.json`，`checkpoint_path()` 卻回傳未淨化的 `con.checkpoint.json`，`exists()` 因此對錯檔名判斷、恆回報 `False` | P2（Windows 保留裝置名／NTFS 禁用字元檔名下 `exists()` 誤判，非常態路徑但影響斷點續跑判斷正確性） | 本輪修復：`checkpoint_path()` 改呼叫 SSOT `_sanitize_log_filename`（`autoclaude/utils/logger.py`），`exists()` 委派 `checkpoint_path()` 自動一併修正 | fixed@R48：新增 `tests/test_token_checkpoint.py::TestCheckpointManager::test_checkpoint_path_matches_actual_saved_filename_for_reserved_device_name`／`test_checkpoint_path_matches_actual_saved_filename_for_forbidden_char_stem` 兩條回歸測試；`python -m pytest tests/ -k checkpoint -q` 171 passed / 11 skipped；`ruff check autoclaude/utils/checkpoint_manager.py` all checks passed；clean venv 全量 `python -m pytest tests/ -q` 3669 passed / 210 skipped（較 ONBOARDING.md R47 基線 3667/210 恰好 +2，對應本輪新增之 2 條測試，無退化） |
| DEF-101-391 | 2026-07-26 | R48 四方複審跨平台掃描（Scan-A／Scan-B／Scan-C） | `.github/workflows/windows-compat-ci.yml` 的 `on.push.paths`／`on.pull_request.paths`（修復前第 123／203 行附近）只列 `AutoClaude/tools/install_git_hooks.ps1`，未列 `.sh` 版本，但第 523／561 行 windows-smoke job 以 `shell: bash` 直接執行 `bash AutoClaude/tools/install_git_hooks.sh`——只改該 `.sh` 檔時本 workflow 完全不會被觸發，需等每週 `windows-nightly-full`（`continue-on-error`）或同批異動已列路徑檔案才間接跑到；姊妹 workflow `macos-compat-ci.yml` 已正確列出同檔（不對稱） | P1（Windows Git Bash 相關邏輯回歸驗證延遲至 nightly 非阻塞層，push/PR 閘門盲區） | 本輪修復：`windows-compat-ci.yml` push／pull_request paths 各補一行 `"AutoClaude/tools/install_git_hooks.sh"`，緊鄰既有 `.ps1` 行 | fixed@R48：`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/windows-compat-ci.yml'))"` 語法合法；`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 修改前後皆 10 passed（此既有機械鎖本身未涵蓋「workflow 步驟直接呼叫 `.sh`」這個情境，故無鑑別力，僅供不回歸佐證）；人工比對確認 `windows-compat-ci.yml` 與 `macos-compat-ci.yml` 在 `install_git_hooks.{sh,ps1}` 覆蓋上恢復對稱（push／pull_request 兩處新增行皆一致） |
| DEF-101-394 | 2026-07-26 | R50 全新五維掃描（Scan-C：路徑/編碼/子行程平台語意）＋四方一審 Architect/SA/SD/QA 一致判定必修 | `AutoClaude/autoclaude/evolution/playbook_evolver.py`／`minimax_evolver.py` 的 `_derive_part_a_evaluator()`（各自獨立重複實作）對非 pytest 的 `evaluator_command` 產生 `f"{{ {cmd}; }} \|\| true"`（POSIX brace-grouping＋`\|\|`），此字串經 `execution/evaluator.py::Evaluator.run()` 以 `subprocess.run(shell=True)` 執行——Windows 上呼叫 cmd.exe，該殼不支援 `{ ; }` 分組語法、亦無內建 `true`，必回傳非 0 exit code，使 SPLIT_STEP 演化出的 Part A 評估在 Windows 上恆報失敗，與註解明言「確保不因 Part A 僅涵蓋一半任務而誤報失敗」的設計意圖直接相反；另一同源缺陷：Gap-033 `ENV_INIT_GLOBAL` 步驟 `evaluator_command="python -c 'import fastapi' && echo 'OK'"` 用單引號包 `python -c` 參數，cmd.exe 不視單引號為字串分隔符，會被拆成兩個獨立引數恆語法錯誤。既有測試 `tests/test_gap021_028.py::test_derive_part_a_evaluator_other_cmd` 僅斷言回傳字串含子字串 `"true"`/`"\|\|"`，從未真正執行該字串，對此問題零鑑別力 | P1（Windows 上 SPLIT_STEP 演化 Part A 驗證與 Gap-033 全域注入步驟兩者皆必定失敗，非低機率邊角情境） | 本輪修復：`_derive_part_a_evaluator` 改用 `python -c "import subprocess, base64, sys; subprocess.run(base64.b64decode('<base64>').decode('utf-8'), shell=True); sys.exit(0)"`（base64 包裝原指令避開殼層轉義問題，兩平台皆用 `python -c` 執行、無條件 `sys.exit(0)` 保留原設計語意）；Gap-033 步驟改雙引號 `python -c "import fastapi" && echo OK` | fixed@R50（round 1）：既有測試改為真跑 subprocess 驗證 exit 0 且斷言結果不含 POSIX-only token（`{ `／`; }`／`\|\| true`）；為 MinimaxEvolver 獨立實作補上先前零覆蓋的對等測試；Gap-033 測試新增 evaluator_command 不含單引號斷言＋真跑 import 成功/失敗兩種情境。修復當輪一度連帶新增 4 處 `subprocess.run(text=True)` 缺 `encoding` 參數、違反既有 `tools/tests/test_subprocess_encoding_hygiene.py` 機械鎖（四方二審發現，round 2 補上 `encoding="utf-8", errors="replace"`）。主控複驗：`cd AutoClaude && ../.venv/bin/python -m pytest tests/ -q` 3778 passed／146 skipped（全綠，含 round 3 SSOT 收斂後之最終態，見 DEF-101-397）；`.venv/bin/python -m pytest tools/tests/test_subprocess_encoding_hygiene.py -q` 9 passed |
| DEF-101-395 | 2026-07-26 | R50 round 1 修復（四方一審四份措辭略異但實質相同的獨立報告，經核實為同一結構性缺陷） | `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 的消費關係掃描器（`_scan_dir_for_root_paths()`／`_scan_import_consumed_paths()`）只認得「字面路徑字串」與「import 陳述式」兩類消費形態，對 `tools/tests/test_windowsapps_guard_cross_consistency.py` 用 `_tracked_files(pattern)`（即 `git ls-files -- pattern`）動態掃描全部 git 追蹤檔案這種第三類消費形態（盲區 C）結構性零偵測；實測 10/10 全綠卻攔不下 `.github/workflows/macos-compat-ci.yml` 的 push／pull_request paths 漏列 6 支被該掃描消費的 `.ps1` 檔案（`AutoClaude/tools/{reschedule_g0_gatecheck,local_ci_gate,run_act,install_git_hooks}.ps1`／`tools/{integration_gate,lib/GitHooksInstallCommon}.ps1`）——windows-compat-ci.yml 因有 `**/*.ps1` 萬用字元不受影響，僅 macOS 側不對稱 | P1（macOS 側 push/PR 閘門對這 6 支 `.ps1` 的相關回歸驗證延遲至 nightly 非阻塞層） | 本輪修復：`.github/workflows/macos-compat-ci.yml` push／pull_request 各補 6 行；新增 `_KNOWN_GLOB_SCAN_CONSUMERS` 顯式登記字典＋`test_known_glob_scan_consumers_covered_by_ci_paths()`（動態即時執行 `git ls-files -- pattern` 取得真實結果、逐一機械斷言被對應 workflow paths 覆蓋，非寫死清單） | fixed@R50：`cd AISDLC_SDD && ../.venv/bin/python -m pytest scripts/tests/test_ci_paths_cover_root_consumers.py -v` 修復前 10 passed → 修復後 **11 passed**；bug-injection（暫時移除 macos-compat-ci.yml 一行 path）確認新測試由綠轉紅、訊息精準指出未覆蓋檔名，還原後 diff+sha256 核對一致；主控複驗 `bash scripts/ci-gate.sh` 全綠，逐軌計數 v0.01:1478／v0.30:1729／scripts/tests:227（較修復前 226 +1） |
| DEF-101-396 | 2026-07-26 | R50 全新五維掃描（Scan-D：SSOT Guard 家族涵蓋率完整性） | 兩處「僅靠人工鏡射／只鎖常數不鎖行為」的殘留缺口，尚未被任何機械 parity 測試涵蓋：①`tools/install_mac_nightly.sh::report_heartbeat()`（心跳三態判斷）與 `tools/dev_start.py::_check_nightly_heartbeat`（同語意獨立 bash/py 重寫）僅由 `TestCrossSiteLiteralLocks` 鎖住門檻常數與 launchd label 字面值，三態分支邏輯本身（未偵測/過期/新鮮）從未被跨檔行為等價測試鎖定；②`tools/macos_smoke_local.sh`／`tools/windows_smoke_local.ps1` 各自獨立內嵌 `-c core.longpaths=true` git flag，`check_script_parity.py` 的 `_SINGLE_SIDED_EXEMPT` 只驗證兩檔皆存在且已登記，未對旗標內容本身做跨檔一致性機械比對 | P3（皆為「日後單邊修改不會有任何機械訊號」的縱深防禦缺口，非現行行為缺陷） | 本輪修復：①新增 `TestNightlyHeartbeatCrossSiteBehavioralEquivalence`（從 `install_mac_nightly.sh` 原始碼動態擷取 `report_heartbeat()` 函式本體、在獨立 bash 子行程對同一心跳檔執行，與 `dev_start.py._check_nightly_heartbeat()` 分類結果比對 4 個代表性年齡）；②新增 `check_script_parity.py::_check_git_longpaths_flag_parity()`（剝除註解後比對兩側旗標字面值） | fixed@R50：①bug-injection（`install_mac_nightly.sh` 的 `-gt` 誤改 `-lt`）證明 4 個 subTest 由綠轉紅，還原後 sha256 核對相符；②兩輪 bug-injection（首版未剝註解誤判仍綠，修正後正確轉紅），新增 `TestGitLongpathsFlagParity` 4 個情境測試；主控複驗 `.venv/bin/python tools/check_script_parity.py` 全綠（`✅ git longpaths 旗標鎖：兩側皆含 '-c core.longpaths=true'`），`.venv/bin/python -m pytest tools/tests/ -q` 486 passed／4 skipped／40 subtests passed |
| DEF-101-408 | 2026-07-26 | R52 round 2/3 四方複審發現（Scan-D 延伸／SA-SD 交叉確認） | `AutoClaude/autoclaude/evolution/_evaluator_derivation.py::derive_part_a_evaluator()` 用 `re.search(r'\bpytest\b', cmd)` 判斷是否為 pytest 指令（`'python(3) -m pytest ...'` 形態因含 `pytest` 字串而命中），但取可執行檔頭時直接用 `base.split()[0]`，對此形態拿到 `'python'`/`'python3'` token 而非 `'pytest'`，產出語法上不存在的 `'python --collect-only'`（實測 rc=2 unknown option），與函式「pytest 指令僅做 collect-only 確認語法正確」設計意圖相反；三層架構白名單明文允許 `pytest`/`python`/`python3` 三種頭部形態，屬生產可達路徑（`playbook_evolver.py`/`minimax_evolver.py` 皆委派至此共用函式） | P2（SPLIT_STEP Part A evaluator 推導在特定合法輸入形態下產出無效指令） | 已修復：改用 `tokens.index("pytest")` 定位 `pytest` token 本身，保留其前綴（如 `python -m`）一併帶入頭部；找不到時保留舊行為兜底（理論上不會發生） | fixed@R52（`tests/test_gap021_028.py` 新增 parametrize 測試；round 4 SA 親自呼叫函式驗證產出可執行的 `--collect-only` 指令，rc=0，3915 tests collected） |
| DEF-101-413 | 2026-07-26 | R53 Workflow 五維掃描（Scan-B＋Architect-Design 兩維度交叉獨立發現）＋round 1 四方（Architect/SA/SD/QA）一致確認 | `AutoClaude/autoclaude/infra/adapters/sdd_to_playbook_adapter.py` 的 `_EVALUATOR_TEMPLATES` 白名單（docstring 自稱「唯二合法 evaluator 模板」）中，`[0]` 已於 R52（DEF-101-403）修復移除裸字面值 `python` 前綴，但 `[1]`（`"python -m tools.{module} {args}"`）仍原封不動保留與修復前完全相同的裸字面值 `python -m` 前綴——同一白名單抽象內部安全等級不一致；`_build_evaluator_cmd()`（唯一消費點）自始只索引 `[0]`，故 `[1]` 為全倉零呼叫點、零測試涵蓋的死碼，一旦未來被啟用會在零示警下重現 R52 剛修復的同一 rc=127 缺陷。Architect 進一步指出此非能機械比照 `[0]` 修法（去前綴留 pytest）或姊妹 evolution 模組（`sys.executable`）直接套用的個案——`tools.{module}` 為 repo 相對 Python 模組路徑非可安裝 console script，本檔 R52 docstring 已明文排除 `sys.executable`（編譯期/執行期兩段式設計） | P2（白名單內部一致性缺口；死碼但屬同一缺陷型態未同步修復） | 已修復：四方一致建議「移除死碼 vs 保留並專屬設計」二選一並記錄理由，採納移除死碼——`_EVALUATOR_TEMPLATES` tuple 簡化為單一字串常數 `_EVALUATOR_TEMPLATE`，docstring 由「唯二合法」訂正為「唯一合法」，`_build_evaluator_cmd()` 同步移除索引；原地留下 R53 決策理由註解防未來重新引入第三種修法 | fixed@R53（`tests/infra/test_sdd_to_playbook_adapter.py` 82 passed；round 2 四方 Architect/SA/SD/QA 逐一複驗死碼徹底移除、無殘留呼叫點，一致 APPROVE） |
| DEF-101-414 | 2026-07-26 | R53 Workflow 五維掃描（Scan-A＋Scan-C 交叉獨立發現）＋round 1 四方一致確認，AISDLC_SDD 子專案 CI paths meta-lock（同族 DEF-101-037→042→068(a)→391→395→402→406） | `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 兩組獨立缺陷同輪浮現：(1) R52 新增的盲區 D 登記表 `_KNOWN_LITERAL_PATH_CONSUMERS` 只補了 `run_local_nightly.sh` 一筆，但同一消費檔 `tools/tests/test_dev_start.py` 內以完全相同 `self._REPO`／class 屬性 `_REPO` 手法另外讀取了 `run_local_nightly.ps1`（1 處）與 `install_mac_nightly.sh`（3 處呼叫點，橫跨 3 個測試類）卻未登記——現行因兩份 workflow 萬用字元＋明列項意外雙重覆蓋而無假綠，但登記表本身零機械保護；(2) `_JOIN_RE`（`os.path.join(REPO_ROOT, ...)` 慣用法掃描器）只認無底線字面 token，對 `tools/tests/` 下 21/24 個測試檔慣用的底線前綴 `_REPO_ROOT` 命名結構性零命中，與並用的 `_SLASH_RE`（先前靠無錨定子字串「巧合」命中，同時隱含 `FOO_REPO_ROOT` 誤判假陽性風險）行為不對稱；兩正則自身先前亦無獨立單元測試驗證涵蓋面 | P2×2（現行皆非斷裂，但機械鎖登記不完整／正則死角，屬『安全網保護面不完整』的現在進行式風險） | 已修復：(1) 補齊 `AutoClaude/tools/run_local_nightly.ps1`／`tools/install_mac_nightly.sh` 兩筆登記（各登記 windows-compat-ci.yml + macos-compat-ci.yml 雙 workflow）；(2) 改用共用 `_REPO_ROOT_TOKEN`（`_REPO_ROOT`／`REPO_ROOT`／`_monorepo_root()` 三選一）＋前置負向 lookbehind 使 `_JOIN_RE`／`_SLASH_RE` 對稱一致且不新增假陽性；新增 7 條回歸測試（含 2 條直接對正則物件斷言涵蓋面） | fixed@R53（`test_ci_paths_cover_root_consumers.py` 12→19 passed；`tools/tests/test_dev_start.py` 147 passed；round 2 四方複驗對稱行為＋`FOO_REPO_ROOT` 防呆案例皆正確，一致 APPROVE） |
| DEF-101-415 | 2026-07-26 | R53 round 1 SD 主動發現（非候選清單內，鏡片式覆核 R52 DEF-101-408 修復時揪出邊界殘留） | R52 對 DEF-101-408（`derive_part_a_evaluator()` 產出語法不存在的 `python --collect-only`）的修復不完整：判斷「是否為 pytest 指令」用 `re.search(r'\bpytest\b', cmd)`（有邊界子字串比對），取可執行檔前綴卻用 `tokens.index("pytest")`（要求精確相等 token）——兩判準不等價：當 `pytest` 只以有邊界子字串形式出現在複合 token（如路徑片段 `tests/pytest/test_foo.py` 或點分模組路徑 `mypkg.pytest`）而非獨立 token 時，前者判「是」但後者 `ValueError` 落回 `tokens[0]`，產出與 R52 修復前完全相同的無效指令，此消費點（`minimax_evolver.py`/`playbook_evolver.py`）傳入的 `evaluator_command` 非受限白名單，此輸入形態並非人為刁鑽案例 | P2（DEF-101-408 同一缺陷類型在邊界輸入下復發） | 已修復：新增共用 helper `_pytest_invocation_index(tokens)`，偵測與擷取兩處改用同一判準（僅認 `tokens[0]=='pytest'` 或 `-m` 後緊接 `pytest`），徹底消除兩處判準不等價的可能 | fixed@R53（`tests/test_gap021_028.py` 新增 2 條參數化回歸測試，bug-injection 反向驗證修復前確實 FAILED；round 2 SA/SD/QA 複驗共用判準邏輯正確，一致 APPROVE；SA round 2 另指出修復後 `derive_part_a_evaluator` 內部殘留一次不可達的 `pytest_idx` 重算 else 分支，屬 P3 次要簡化空間非功能缺陷，未修復，見帳本 watch） |
| DEF-101-426 | 2026-07-27 | R55 Workflow 五維掃描（Scan-A／Scan-B 交叉獨立發現）＋round 1 四方（Architect/SA/SD/QA 四位皆獨立命中同一根因）一致確認 | `tools/macos_smoke_local.sh:53` 用裸 `command -v python` 判斷 python 是否存在，未經全 repo 唯一的 WindowsApps guard SSOT（`tools/lib/windowsapps_guard.sh::is_real_python_candidate`）保護；其在 `tools/tests/test_windowsapps_guard_bash_parity.py` 內的豁免理由（「macOS 上不會出現 WindowsApps 假殼」）與同檔自身檔頭註解、`ONBOARDING.md` 記載的「本腳本亦會在 Windows Git Bash 下實跑」情境自相矛盾；四方以構造 WindowsApps 目錄下假 python stub 的隔離 PATH 皆各自獨立重現「裸判斷誤判存在」的行為 | P3（bug-injection 已重現真實假陰性行為，四方交叉獨立命中） | 已修復：改用 `. tools/lib/windowsapps_guard.sh` + `is_real_python_candidate python` 取代裸判斷（比照 `integration_gate.sh`／`bootstrap.sh`／`dev_start.sh` 既有呼叫慣例），並將本檔自 `test_windowsapps_guard_bash_parity.py` 的 `_EXEMPT_SH_FILES` 移至 `_CALLER_FILES` 正式列管 | fixed@R55 round1（`bash tools/macos_smoke_local.sh` 端到端 PASS=13/FAIL=0；`python3 -m pytest tools/tests/test_windowsapps_guard_bash_parity.py -v` 28 passed/24 subtests passed；bug-injection 反向驗證修復前後行為差異真實存在，round 2 QA 獨立重現確認） |
| DEF-101-429 | 2026-07-27 | R55 Workflow 五維掃描（Scan-E Architect-Design 發現）＋round 1 四方一致確認，DEF-101-238（R16 起 39 輪未清償收斂債）擴散實證 | `AutoClaude/autoclaude/infra/adapters/sdd_to_playbook_adapter.py` 的 shell 注入黑名單字元集（DEF-101-238 記載的原始基準）已擴散至 `goal_freeze_gate.py`／`tools/three_tier_to_playbook.py` 兩處呼叫端各自重複宣告字面值，彼此註解互相宣稱「⊇ 基準集合」卻零機械驗證超集關係是否成立 | P3（SSOT 收斂債持續擴散，架構層一致性風險） | 新增 `AutoClaude/autoclaude/utils/shell_deny_chars.py` 匯出 `BASE_DENY_CHARS` 作 SSOT，三處呼叫端改為 `BASE_DENY_CHARS \| {專屬追加字元}` 顯式聯集寫法；刻意不動 `execution/mutation_applier/_conditional.py`／`core/services/mutation/_conditional_evaluator.py`（CONDITIONAL 家族，白名單 regex 為主的不同機制，理由詳見程式碼內 R55 決策註解） | fixed@R55 round1（逐字元集合比對三處新舊 `_DENY` 值 100% 相等；新增 `tests/test_shell_deny_chars_parity.py` 3 passed；`python3 -m pytest tests/ -q` 3811 passed/146 skipped/0 failed；`tools/check_loc_budget.py` total=20437≤cap=20438；round 2 QA／SA 各自獨立在全套 pytest 重跑中遇到 1 次同一測試的不可重現偶發失敗，經排查判定為多位審查子代理共用同一份工作樹時的瞬間並行雜訊〔同 DEF-101-217 已記載模式〕，非真實回歸——後續共 10+ 次重跑〔QA 6 次＋SA 獨立重跑＋主控收尾重跑〕皆乾淨通過） |
| DEF-101-430 | 2026-07-27 | R55 Workflow 四方複審 round 2（SD 獨立發現，複核 round 1 DEF-101-427 新增工具自身的掃描邊界） | round 1 新增的 `tools/check_gha_action_versions.py` 有兩項掃描邊界缺口：(1) `scan()` 只 `glob("*.yml")`，靜默漏掃 GitHub Actions 官方同樣支援且語意等效的 `.yaml` 副檔名——現況 11 支 workflow 皆為 `.yml` 故不觸發，但與工具自身存在的目的（防止「新增 workflow 沿用舊版本」被機械鎖看見）直接矛盾；(2) `_USES_RE` 未先剝離行內 `#` 註解，若未來有人暫時註解掉一行舊版本 `uses:` 宣告會被誤判為現行宣告，產生假紅燈 | P3＋P4（前者為真實漏判風險，後者為未來假陽性風險，皆未在現況觸發） | (1) `scan()` 改為同時掃描 `*.yml`／`*.yaml`；(2) 新增 `_strip_yaml_comment()`（比照同輪 `_strip_bash_comment` 手法，只在引號外才承認 `#` 起始註解），掃描前先剝離註解 | fixed@R55 round2（原始重現腳本二次確認：修復前 `.yaml` 內容漏判、修復後正確偵測；`python3 -m pytest tools/tests/test_check_gha_action_versions.py -q` 12 passed；`python3 tools/check_gha_action_versions.py` 對真實 repo 仍全綠 56 處一致，確認未產生假陽性；`python3 tools/run_root_unittests.py` 496 passed） |
| DEF-101-436 | 2026-07-27 | R55 流程紀實（非程式/文件缺陷，記事存證） | R55 四方複審已完整跑完 round 1（四方一致 REJECT，8 項發現）與 round 2（Architect/SD/SA REJECT＋QA APPROVE，4 項發現，其中 2 項為對 round 1 新增工具自身的複核發現、2 項為 ONBOARDING.md 文件落差）並各自完成對應修復；round 3（第三輪覆核，用於確認 round 2 修復是否落地）已派出四方 agent 但**在其回傳結果前**，使用者明確要求「先告一個段落、commit+push main」以便換模型執行下一輪，主控依指示中止該輪背景 Workflow | N/A（流程狀態記錄，非缺陷） | 主控接手執行 round 3 原本會做的獨立覆核：讀 diff 逐一核對 round 1/round 2 全部修復（`tools/macos_smoke_local.sh`／`tools/check_gha_action_versions.py`／`AutoClaude/autoclaude/utils/shell_deny_chars.py` 等）、修復 ONBOARDING.md §6/§6.1 過期計數（DEF-101-431）、於全新乾淨 venv 下重跑 AutoClaude pytest、重驗 LOC 預算、重跑三個本地 CI 閘門全套 | fixed@R55（本輪四方複審在 round 2 收斂前中止，非「全數 APPROVE」式完美收斂；已由主控親自驗證取代未完成的 round 3，如實記載不美化為已達成四方全數 APPROVE 的收斂狀態。**主控收尾驗證結果**：`AutoClaude` 全新乾淨 venv pytest → 3713 passed/210 skipped〔3710+3 精確吻合，ONBOARDING.md §7 已同步訂正〕；`python3 AutoClaude/tools/check_loc_budget.py` → total=20437≤cap=20438 violations=0；三個本地 CI 閘門全數重跑：`AISDLC_SDD/scripts/ci-gate.sh`（AISDLC_SDD_v0.01:1478／v0.30:1729／scripts/tests:235 全通過）、`AutoClaude/tools/local_ci_gate.sh`（7 道全 PASS）、根層 `tools/integration_gate.sh`（4 PASS/1 SKIP，SKIP 為已知缺 cc-switch CLI 環境限制 DEF-01-007，非本輪引入）皆綠燈；下一輪如有餘裕可視需要對本輪修復做一次獨立覆核） |
| DEF-101-437 | 2026-07-27 | R56 Workflow 五維掃描（Scan-A）＋round 1 四方確認，落地 R55 裁定不修的 DEF-101-432 | `AutoClaude/autoclaude/models/escalation.py::generate_handover_checklist()` 產生的「接手行動清單」由 `to_markdown()` 包進 bash 圍欄落盤成 escalation dump，人類會原樣複製到終端執行，但內容多處 POSIX-only：`python -m py_compile tests/test_*.py`（cmd.exe/PowerShell 不對外部程式做 glob 展開，py_compile 收到字面 `tests/test_*.py`）、`cat <log> \| tail -50`（無 tail；單引號在 cmd.exe 非引號字元）、`grep -rn ... \| head -20`（無 grep/head）、`autoclaude {playbook_path}` 未引用（Windows 路徑常含空白）。另 `plugins/checkpoint/_escalation.py:55` 為第 5 處 | P2（Windows 11 使用者照抄必失敗；R55 曾因 LOC 零餘裕裁定不修） | 本輪修復 | fixed@R56：採**零邏輯行淨增**手法（行尾註解併入 Windows 等效寫法，`count_loc()` 只跳過空行與行首 `#`），完整等效指令與論證記於函式開頭註解區塊。修復中另訂正一項回報者未察覺的細節：`Select-String -Path tests\*` **無遞迴語意**，實測命中 `assert.*==` 的檔案非遞迴 36 個 vs 遞迴 237 個（漏 85%），照抄卻以為已檢查完比沒有提示更危險，故改用 `gci -r tests\|sls`。`playbook_path` 加雙引號（雙引號是 sh 與 cmd.exe 唯一通用的引用方式）。註解明載未來改為 `sys.platform` 條件產生指令屬架構正解，須走 ADR-SD07-001 §5.2/§6.2 per-file override 核准，而非續用行尾註解規避 tier 上限 |
