# AutoSDD 缺陷帳本 — 歷史歸檔 #04（Defect Log Archive 04）

> **性質**：`AutoSDD_Defect_Log.md` 的**已結缺陷列歸檔（之四）**，R14 跨平台輪（2026-07-20）依既定輪替規則建立（主檔 258KB 已進 240KB 預警區，crossref 每跑必示警）。
> **內容**：主檔「缺陷總表」中**已結列（fixed / closed-by-decision）共 169 列**逐字搬移（只增不刪、零修改；含 R9 時因 crossref 引用而留存、現已無**狀態宣稱型**引用的 DEF-20-001——機制溯源型註解引用仍存在於兩 compat workflow，不在 crossref checker 判讀範圍（R14 SA-R14-REV-4 措辭精確化）——及 DEF-101 系列已結列至 DEF-101-187）。
> **未搬**（仍在主檔 live SSOT）：未結列（open / routed / wontfix）與被 crossref 掃描目標引用的列，共 30 列——清單見主檔「歷史複驗註記」段。
>
> **與主檔的關係（讀者須知）**：
> - live 狀態查詢一律以主檔「缺陷總表」為唯一權威；本檔僅供已結缺陷的歷史佐證與審計回溯。
> - 「只增不刪」：內容與搬移當下主檔各列**逐字一致**（git 歷史亦可佐證）。

---

## 已結缺陷列（原主檔逐字搬移）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|----|----------|----------|------------------------|--------|----------|------|
| DEF-20-001 | 2026-06-16 | improving_20 結案後 zero-trust 複核（三鏡 Architect/SA-SD/QA + 主迴圈，全 PASS；流程面觀察非技術缺陷） | **AISDLC_SDD 閉環結案缺少「結案證據強制重推導」之可執行閘門/hook**（流程缺陷）：improving_20 之所以發生「幻覺工具輸出」事故（前 session 編造整套四件套——測試 passed、commit hash、push 成功、三鏡 PASS、潔淨度數字皆虛構），根因之一是框架**無任何機制要求結案數字（pytest passed、ci-gate、commit/push）必須由獨立驗證者就當前 repo 真實狀態重新推導後才接受**。現行反幻覺紀律僅存於 agent 跨 session 記憶（`no-fabricated-tool-output`）與 `AutoSDD_ZeroTrust_Audit_NN.md` 敘事，**未落為 AISDLC_SDD_v0.01 的 hook/SCG 閘門**——下次換 session 仍可能重演。佐證：`.claude/hooks/` 現有 `session_start.py`/`context_ledger_*`/`post_commit_drift.py` 皆無「結案宣稱數字 vs repo 真實狀態」交叉核對；SCG-4/5（PR/RTM 關卡）依賴人工填寫，無機械重推導門。本輪複核能抓出事故純靠人工派三鏡，非框架保證 | P2（治理閉環誠信缺口，影響所有跨 session 結案之可信度；非阻擋本輪——本輪已人工複核全 PASS） | routed 下一輪 B 軌（框架程式/hook 缺陷）：設計「結案證據重推導」hook——例如 `post_commit_drift.py` 擴充或新增 `closure_evidence_verify` hook，於結案 commit 時自動就 repo 真實狀態重跑/比對 improving_NN 文件宣稱的關鍵數字（至少 pytest passed 計數 + HEAD hash + 演化版 floor），落差即 fail-closed 擋下；使反幻覺紀律由「agent 自律」升級為「框架機械強制」。先入根層 Defect_Log，待 SDD_improving_Automation_NN（軌道②）開 RFC 帳本 | **fixed@improving_21（v0.12）**（2026-06-16：reactive 紀律落為框架機械閘門。**W-21-1 廉價層 fail-closed**：`AISDLC_SDD_v0.12/tools/fsm_runtime/closure_evidence.py:verify_git_facts` 對 improving_NN.md 末尾 `closure-evidence` 契約宣稱的 `claimed_commits`/`claimed_tag` 以 `git cat-file -e`+`merge-base --is-ancestor`+`rev-parse --verify` 就 monorepo 根真實狀態重推導〔白名單正則消毒、list-form argv shell=False〕，任一無法重推導→FAIL〔直擊「編造 commit/push/tag」事故核心〕。**W-21-2 昂貴層 inconclusive fail-closed**：pytest passed/ci-gate floors **不重跑**〔守 hook <2s budget〕，改驗綁定當前 HEAD 的 rederive 證書〔`write_rederive_cert` stamp HEAD〕，契約 base_sha≠HEAD 或證書缺失/數字不符→INCONCLUSIVE〔絕不假綠，比照 embodied_grounding 零觀測語意〕。thin hook `.claude/hooks/closure_evidence_verify.py`〔git toplevel 定位、exit 0、fail-soft、advisory 不阻擋 commit，verdict 寫 `.git/CLOSURE_EVIDENCE_VERDICT`〕；安裝 `tools/install_hooks/install_post_commit.{sh,ps1}` 串接 drift+closure。測試 `test_closure_evidence.py` **19 case**〔tmp 真實 git repo 驗 cat-file/merge-base/rev-parse 真實行為、突變證非假測試〕。**決策不新增 R-9.x**〔advisory hook 不需規則承載，同 DEF-10-002 + Rule 2；治理規則承載+catch 覆蓋面+SCG-4/5 機械閘門 routed 未來輪〕；**不動 FSM/`*.tla`→免五軌 TLC**〔transition_rules.py+5 tla 對 v0.11 逐位元零差異〕。RFC `SDD_improving_Automation_27.md`。三鏡 zero-trust PASS〔修復回合 1：文件路徑落差〕、零退化 AutoClaude 3112/v0.12 1574/lint 8/ci-gate exit 0/潔淨 852。見 `AutoSDD_improving_21.md` / `AutoSDD_ZeroTrust_Audit_21.md`） |
| DEF-101-001 | 2026-06-30 | improving_101 階段一/設計（C 軌 SD_09 W1 鎖定機制 zero-trust，掌舵者質疑「空轉一個月是好設計嗎」） | **SD_09 W1 mutation 鎖定判準把「源碼演進證據」綁死成「日曆天數」**：`mutation_baseline_lock.py:197-203` M-05 去重鍵＝同 UTC 日期（不看 sha）+ `run_local_nightly.ps1:25` schtasks `/SC DAILY` + `autoclaude-ci.yml` cron 每日一次 → unique sha 每日最多 +1、7 個 unique sha 至少需 ≥7 日曆天、idle 日重複 sha 稀釋 tail7 → 鎖定空轉數週至一月。mutation test 確定性使「每日重測同 sha」零信心增益；原防單日抖動目的（ADR-SD08-002 §2.4）已被 unique sha + ±2pp tolerance（ADR-SD09-009 §5.5）+ `compute_consistency_warning`(L265) 三機制取代 → 日曆綁定為無防護目標的遺留懲罰 | P2 | 框架程式缺陷 RFC → **ADR-SD09-011**（PM signoff 2026-06-30）→ improving_101 落地 | fixed@improving_101（證據：`mutation_baseline_lock.py` 去重鍵 UTC日期→`source_sha256`〔`_dedup_key` sha 優先/date fallback〕+ `compact_history_by_sha` 方案A壓縮〔真實 30筆→6筆/4 unique sha 不假鎖定〕+ `--migrate-compact-sha` 子命令；觸發解耦＝新 `autoclaude-mutation-on-change.yml`〔現名；源碼變動觸發〕+ pre-push hook opt-in 偵測 + nightly 角色轉監控；受控突變 MUT-101-1 驗紅；反作弊強度不減〔ADR §3 對照〕） |
| DEF-101-002 | 2026-06-30 | improving_101 結案後掌舵者維運指示（「所有 Windows Schedule task Jobs 都需喚醒啟動才作用；避免排程失敗」） | **schtasks 排程缺「喚醒啟動」→ 機器睡眠/休眠時 nightly 漏跑**：`tools/fix_nightly_catchup.ps1` 設了 `StartWhenAvailable`（關機補跑）但**只印不設 `WakeToRun`**（原 L27 僅 Write-Output 檢查值）→ 機器睡眠/休眠時 02:00 排程到點不主動喚醒執行 → 觀察期 idle 漏跑（與「schtasks 漏跑日高度敏感」同源；schtasks.exe `/create` 預設 WakeToRun=false + StartWhenAvailable=false） | P2 | ops 加固（C 軌，improving_101 維運跟進，非完整 improving 輪） | fixed@v2026.06.30-53（ops hotfix；證據：`fix_nightly_catchup.ps1` 補 `$t.Settings.WakeToRun=$true` + 成功判定納入 WakeToRun + powercfg 喚醒計時器提示〔四面完整覆蓋：StartWhenAvailable 關機補跑/WakeToRun 睡眠喚醒/DisallowStartIfOnBatteries=false 電池不擋啟動/StopIfGoingOnBatteries=false 電池中途不殺——後者為掌舵者實機跑出 StopIfGoingOnBatteries=True 揪出之第 4 中斷風險〕；`reschedule_g0_gatecheck.ps1` 同補 WakeToRun + StopIfGoingOnBatteries；`run_local_nightly.ps1` .NOTES 補「建後務必 elevated 跑 fix_nightly_catchup.ps1」提示；3 檔 PowerShell parse 0 errors、BOM 正確〔ASCII 檔無 BOM、中文檔有 BOM〕。⚠ 實際套用需**實機 elevated** 跑 `fix_nightly_catchup.ps1`，本 session 僅驗腳本語法/設定值正確、無法驗 schtasks 實機喚醒行為） |
| DEF-101-006 | 2026-07-10 | 跨平台修復輪四方複審（SA 複審新發現 P2-N2） | **ruff 未鎖版 → lint 結果跨機器漂移**：`AutoClaude/pyproject.toml` dev extras 的 ruff 無版本上限，fresh bootstrap 裝到 ruff 0.15.20 對全倉 `ruff check .` 報 1,339 筆既有 issue（E501/I001/UP045 等，遍布 HEAD 既有檔案）；pre-commit hook 只 lint staged 檔、CI 無 ruff step 故不紅燈，但根 CLAUDE.md 教學指令 `ruff check .` 在新環境滿版報錯，且與「地端=CI 同版」精神不符（本輪已將被觸碰 6 檔修至全綠） | P2 | dev extras 鎖定 ruff 版本（如 `ruff==0.x.y`）+ 擇期分批清理存量或調整教學指令範圍 | fixed@四方複審第三輪修復（2026-07-10；證據：`AutoClaude/pyproject.toml:25,27` dev extras 鎖 `pytest==9.0.3`＋`ruff==0.15.21`〔pytest 與 `AISDLC_SDD/AISDLC_SDD_v0.01/requirements-ci.txt` 同版，一併消除 fresh bootstrap「先裝 9.1.1 再降版」的安裝順序依賴〕；重裝後實測 full pytest 基線 3,529/181 不變、lint-imports 8 kept、ruff baseline 1,339 筆不變＝跨機器漂移根因〔未鎖版〕已除；升版規則比照 requirements-ci.txt 本機先升跑全綠再回寫釘選。**殘餘帳目拆分**：存量 1,339 筆 ruff baseline 待分批清理 → **DEF-101-018** 續追） |
| DEF-101-007 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；隔離 victim repo PoC 實證） | **linked worktree push 時 GIT_DIR/GIT_WORK_TREE 洩漏進 pytest 子行程 → 真 git fixture 改操作「使用者真 repo」**：git 對 pre-push hook 注入絕對路徑 `GIT_DIR`（hook 又補 `GIT_WORK_TREE`），傳染進閘門 pytest 的真 git fixture（tmp repo 內 git init/add/commit）後 cwd=tmp 被 env 覆寫——髒工作樹時把使用者 WIP 偷 commit 上真分支、乾淨樹時 fixture 炸掉致閘門假紅；QA 複審終裁輪另實證**破壞面更廣**：敵意 env 下 fixture 的 `git init` 經 commondir 把 `core.worktree=<worktree路徑>` 寫入主庫 `.git/config`，主 checkout 自此所有 git 操作被重定向 | P0 | 本輪即修（雙層防禦：hook 呼叫點 env 清洗＋fixture 層自清） | fixed@四方複審第三輪修復（證據：hook 層 `AISDLC_SDD/.githooks/pre-push:25`〔DEF-101-026 移除死碼後行號〕、`AutoClaude/tools/git-hooks/pre-push:72,85`〔pytest 與 mutation 呼叫點〕加 `env -u GIT_DIR -u GIT_WORK_TREE`；fixture 層 v0.30 `tools/fsm_runtime/tests/test_closure_evidence.py:24` 與 `AISDLC_SDD/scripts/tests/test_copy_on_evolve.py:42` 增 `_clean_git_env()`〔清 GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE〕；隔離 victim repo PoC 前紅後綠實證。殘餘邊角＝中間凍結版 fixture 與 `_run_git` 繼承 env，帳列 DEF-101-020／DEF-101-022） |
| DEF-101-008 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **根層 dispatcher 對「rename 移出子專案」fail-open**：git rename 偵測使 `diff --name-only` 只列新路徑一條，`git mv AutoClaude/x docs/x` 這類移出子專案的變更舊路徑消失 → 來源子專案閘門靜默漏跑（`ONBOARDING.md` §6「已知縫隙」原僅承認 merge/rebase 繞過） | P1 | 本輪即修（dispatcher diff 停用 rename 偵測） | fixed@四方複審第三輪修復（證據：`tools/git-hooks/pre-commit:40` 與 `tools/git-hooks/pre-push:65` diff 加 `--no-renames`〔rename 拆回 A+D 兩條路徑，舊路徑重新可見照常分流〕；隔離 repo `git mv` 前紅後綠） |
| DEF-101-009 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B1） | **根層 `aisdlc-sdd-artifact-cleanup.yml` 7 天清理會殲滅 AutoClaude 30/90 天取證 artifact**：workflows 上移 monorepo 根層後 artifact 命名空間合一，原邏輯對「整個 repo」刪超齡 artifact——AutoClaude 取證產物（`mutation-reports-*`/`ac4-*` retention 30 天、`perf-baseline-*`/`mutation-history` 90 天）全數 > 7 天門檻，Nightly 取證紀律證據鏈會被排程性斬斷；檔頭註解「對齊各 workflow 的 retention-days」失實 | P1 | 本輪即修（刪除 allowlist 化） | fixed@四方複審第三輪修復（證據：`.github/workflows/aisdlc-sdd-artifact-cleanup.yml:58` `ALLOWLIST_PREFIXES`——僅 `ci-arch-fitness-`/`arch-fitness-findings`/`fsm-chaos-report-` aisdlc-sdd 家族前綴可刪，未命中一律保留〔fail-safe 寧留勿刪〕；檔頭補前綴↔來源 workflow 對照表、失實註解照實改寫） |
| DEF-101-010 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B2） | **`autoclaude-mutation-on-change.yml` 宣稱 unique-sha 累積但 fresh checkout 永不累積**：`.mutation_history.jsonl` 已出庫（`AutoClaude/.gitignore`），CI fresh checkout 每 run 從空 history 起、run 完即蒸發 → ADR-SD09-011 的 7 unique-sha 鎖定閘門在 GitHub Actions 側永不推進 | P1 | 本輪即修（artifact 三段式持久化） | fixed@四方複審第三輪修復（證據：`.github/workflows/autoclaude-mutation-on-change.yml:55` restore〔gh api 下載最新未過期 `mutation-history` artifact，失敗印告警從空 history 起累不 fail；行號含 DEF-101-023 concurrency 增行後〕→ accumulate → `:109` upload〔`if: always()`，history retention 90 天＋本次報告 30 天〕；permissions 補 `actions: read`。殘餘＝90 天 retention 上限，帳列 DEF-101-021） |
| DEF-101-011 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 A3） | **NTFS 敵意檔名零防護**：macOS(APFS) 允許的檔名在 Windows(NTFS) checkout 直接炸掉或靜默碰撞——路徑段含 `< > : " \| ? *`/控制字元、以空白/句點結尾、保留裝置名 CON/PRN/AUX/NUL/COM1~9/LPT1~9、大小寫碰撞（NTFS 不分大小寫互相覆蓋）皆無閘 | P2 | 本輪即修（根層 pre-commit 檔名閘） | fixed@四方複審第三輪修復（證據：`tools/git-hooks/pre-commit:56-135` `_ntfs_seg_bad` 對本次新增（A/C）路徑逐段檢查＋`git ls-files` 大小寫碰撞比對，命中 fail-loud rc=1、取不到新增清單亦擋下；bash 3.2 相容；5 場景實測〔非法字元/保留名/尾隨空白/尾隨句點/大小寫碰撞〕全數攔下） |
| DEF-101-012 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 A4＋B4） | **根層基建自身變更零閘門**：根層 `tools/`（dispatcher hooks／bootstrap／整合閘門）與 `.gitattributes`/`.editorconfig`/`.github/` 過去無任何 pre-commit 檢查與 CI 覆蓋，改壞 dispatcher/bootstrap 只能事後在 hook 爆炸時發現 | P2 | 本輪即修（本機 leg＋CI 雙層） | fixed@四方複審第三輪修復（證據：本機層 `tools/git-hooks/pre-commit:45-48,138-166` 新增根層基建 leg〔變更命中根層基建路徑即對變更到的 `.sh`／無副檔名 hook 檔跑 `bash -n`，失敗 rc=1〕；CI 層新增 `.github/workflows/root-infra-ci.yml`〔bash -n＋pwsh `Parser::ParseFile`＋`git ls-files --eol` EOL 守門，paths 觸發、`contents: read` 最小權限、零外部 action〕） |
| DEF-101-013 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA；複審 B3） | **dependabot pip 生態漏掃 AutoClaude**：根層 `.github/dependabot.yml` pip 條目原僅涵蓋 AISDLC_SDD，`AutoClaude/pyproject.toml` 相依無自動更新掃描 | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`.github/dependabot.yml:34` 補 `directory: "/AutoClaude"` pip 條目〔weekly，與既有條目同格式〕） |
| DEF-101-014 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **`AutoClaude/tools/local_ci_gate.ps1` 缺 python 前置守門＋`Invoke-Gate` 殘值 fail-open**：未啟 venv 時各 gate 逐一噴錯難診斷；scriptblock 拋例外（如 CommandNotFoundException）不更新 `$LASTEXITCODE`，前一 gate 殘值 0 令本 gate 假 PASS | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`local_ci_gate.ps1:37` 補 `Get-Command python` 前置守門〔與 `.sh` 版 `command -v python` 對稱〕；`:85` `Invoke-Gate` 每次執行前 `$global:LASTEXITCODE=0` 重置＋try/catch 例外明確判 FAIL〔PS 5.1 相容〕） |
| DEF-101-015 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **fsm_runtime v0.30 四處 subprocess `text=True` 無 encoding**：zh-TW Windows 預設 cp950，子程序輸出含中文即 UnicodeDecodeError——`closure_evidence.py` `_run_git`、`sandbox_runner.py` DockerBackend、`tlc_runner.py` TLC 呼叫、`tests/test_closure_evidence.py` `_git` helper 四處 | P2 | 本輪即修（v0.30＋閘門雙層） | fixed@四方複審第三輪修復（證據：v0.30 `closure_evidence.py:96`、`sandbox_runner.py:254`、`tlc_runner.py:94`、`tests/test_closure_evidence.py:39` 補 `encoding="utf-8", errors="replace"`；`AISDLC_SDD/scripts/ci-gate.sh:26`／`ci-gate.ps1:11` 補 `PYTHONUTF8=1`〔對齊 AutoClaude local_ci_gate 同名設定〕。凍結版同病依紀律不回改，帳列 DEF-101-019） |
| DEF-101-016 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **根層 `.gitattributes` `.githooks/*` 為死規則**：含斜線 pattern 錨定根層，而根層無 `.githooks/` 目錄 → 規則永不命中（`AISDLC_SDD/.githooks/` 實際靠子專案自帶 `.gitattributes` 兜底） | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`.gitattributes:24` 改 `**/.githooks/*`＋註解照實〔明示子專案自帶 `*` 兜底規則仍會遮蔽本行、非可靠兜底，各子專案 `.githooks` 行尾政策仍須自身完備〕） |
| DEF-101-017 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect/SA/SD/QA） | **AutoClaude pre-push 失敗提示只印 Windows 指令＋header 單平台語意**：閘門失敗指引僅給 `run_act.ps1`，header 稱「在 Windows 跑本機 Python」，macOS 開發者無從對照 | P2 | 本輪即修 | fixed@四方複審第三輪修復（證據：`AutoClaude/tools/git-hooks/pre-push:7-8` header 雙平台語意；`:94-95` 失敗提示補 macOS/Linux `bash tools/run_act.sh --job test`＋Windows `run_act.ps1` 雙行） |
| DEF-101-023 | 2026-07-10 | 跨平台修復輪四方複審第三輪（Architect 複審終裁輪，DEF-101-010 修復之衍生） | **`autoclaude-mutation-on-change.yml` restore→upload 非原子、無 concurrency 序列化**：mutation run 長 20~45 分鐘，連續兩次 push token_guard 時後開始先結束的 run 讀不到另一 run 的 sha，較晚 `created_at` 的 artifact 蓋掉對方 → 該筆 unique sha 永久丟失（方向安全：少計不多計、不會假鎖定，僅 7-sha 閘門推進變慢） | P2 | 複審終裁輪即修（一行 concurrency 序列化） | fixed@四方複審第三輪修復（證據：workflow 增 `concurrency: {group: mutation-history-accrual, cancel-in-progress: false}`——排隊不取消，每筆 sha 都入鏈；yml 解析驗證通過） |
| DEF-101-024 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-011 修復之覆蓋缺口） | **NTFS 檔名閘漏擋反斜線 `\`**：`tools/git-hooks/pre-commit` 非法字元 case 僅列 `< > : " | ? *`——macOS 端誤建含 `\` 檔名並 commit 後，Windows checkout 直接 `error: invalid path` 整庫癱瘓，正是此閘要擋的類別（SD bash 3.2 單元實測 `back\slash.txt` 判 CLEAN 放行實證） | P2 | 複審終裁輪即修（case pattern 補一項） | fixed@四方複審第三輪修復（證據：case 補 `*'\'*`、錯誤訊息同步列 `\`；檔內實際引號寫法實測 HIT；`bash -n` 過） |
| DEF-101-026 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-007 修復之殘留死碼） | **AISDLC pre-push 的 `export GIT_WORK_TREE` 成死碼**：DEF-101-007 修復後其唯一下游消費者（ci-gate.sh）已被下一行 `env -u` 清洗，export 無作用且註解易誤導後人「此 env 有人在用」 | P3 | 複審終裁輪即修（移除死碼區塊） | fixed@四方複審第三輪修復（證據：`AISDLC_SDD/.githooks/pre-push` 移除 guarded export 四行，僅剩 `env -u` 清洗呼叫；`bash -n` 過） |
| DEF-101-027 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-015 清掃之同檔漏網） | **`test_copy_on_evolve.py:241` `text=True` 無 encoding**：`command -v python` 探測 subprocess 漏補 encoding（輸出為路徑，實害極低，但與本輪清掃標準不一致） | P3 | 複審終裁輪即修（一行補齊） | fixed@四方複審第三輪修復（證據：補 `encoding="utf-8", errors="replace"`；`scripts/tests/` 130 passed 複驗） |
| DEF-101-028 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD live 實證，終裁 (f) 核准修法） | **hook wiring cwd 相對路徑 → PreToolUse 全工具硬鎖死**：三份 settings.json 的 hook command 為裸 `python <相對路徑>`，hook 子行程 cwd≠專案根（Agent SDK 子 session／Bash cd 殘留）時 python 缺檔退出碼恰為 2＝PreToolUse deny 語意 → matcher 涵蓋的 Write/Edit/Read/Bash/Task 全滅（審查管線被鎖死 live 實證；雙平台同構） | P0 | 本輪即修 | fixed@四方複審第四輪（證據：根 4＋AutoClaude 6＋v0.30 3 條 command 全改 python -c shim——python 自讀 `CLAUDE_PROJECT_DIR` 錨定+os.chdir、缺檔 fail-open exit 0、argv/stdin/exit code 原樣傳遞〔目標腳本 exit 2 阻斷語意保留〕；contract test `AISDLC_SDD/scripts/tests/test_hook_wiring_cwd_safety.py` 三道鎖＋錨定行為鎖擴及三份 settings；ONBOARDING §4 同步） |
| DEF-101-029 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **根層 `sdd_hook_router.py` stdin 未設 UTF-8**：只 reconfigure stdout/stderr，zh-TW Windows pipe 預設 cp950，payload 含中文即 `sys.stdin.read()` UnicodeDecodeError → router 崩潰 fail-open（DEF-43-001 只修了 child 端沒修自身入口） | P1 | 本輪即修 | fixed@四方複審第四輪（證據：`.claude/hooks/sdd_hook_router.py:42-49` reconfigure 迴圈擴為 stdin/stdout/stderr＋防非 TextIOWrapper；cp950 模擬修前重現、修後全過） |
| DEF-101-030 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **AutoClaude 五支 hook＋v0.30 兩支 context_ledger 裸 `sys.stdin.read()`**：同 cp950 病灶，共 8 個使用點；阻斷級 enforce_docs_path 於「Write 內容含中文」即靜默失效、check_ps1_encoding 恰在其存在目的場景失效 | P1 | 本輪即修 | fixed@四方複審第四輪（證據：8 處統一改 `sys.stdin.buffer.read().decode("utf-8","replace")`＋無 buffer 回退；新增 `AutoClaude/tests/tools/hooks/test_hooks_stdin_utf8.py` 6 case 含阻斷保真；凍結版 v0.01~v0.29 依紀律未動） |
| DEF-101-031 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SA） | **macOS 桌面通知靜默降級 log-only**：`notifications` extra 在 darwin 只裝 plyer，plyer darwin 後端需未宣告的 `pyobjus` → `notify()` 必炸，三層 fallback 無聲落 log；ESCALATION 桌面通知為引擎核心行為 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：`notifier.py` 插入 darwin `osascript` fallback〔`on run argv` 參數化、注入免疫、timeout=10〕＋`tests/utils/test_notifier.py` 10 case；macOS 實機發出通知） |
| DEF-101-032 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SD） | **v0.30／根層／AutoClaude settings.json 無 `PYTHONUTF8` env**：直啟子專案 session 時 hooks 無 UTF-8 防護（v0.30 hooks 原無 reconfigure、🔴 emoji 輸出 cp950 UnicodeEncodeError） | P2 | 本輪即修 | fixed@四方複審第四輪（證據：三份 settings.json env 皆補 `"PYTHONUTF8": "1"`；凍結版 v0.01~v0.29 不回改，見 DEF-101-040） |
| DEF-101-033 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **子專案 .ps1 零 CI 語法覆蓋**：root-infra-ci pwsh parse 與 paths 僅根層 tools/，改壞 `local_ci_gate.ps1`／`run_local_nightly.ps1`（42KB）只能在 Windows 本機爆 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：root-infra-ci pwsh parse 擴至 AutoClaude/tools/＋AISDLC_SDD/scripts/＋動態 LATEST 版〔regex+version 排序鏡射 ci-gate.sh〕，凍結版排除；15 支 active .ps1 容器實跑全過） |
| DEF-101-034 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **.ps1 UTF-8 BOM 政策無 CI 機械複核**：唯一強制為 Claude Code session 內 hook，人工編輯器／GitHub web 可讓無 BOM 含中文 .ps1 入庫 → zh-TW PS5.1（cp950）誤讀 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：root-infra-ci 併入「含非 ASCII 的 active .ps1 必須有 EF BB BF」檢查，範圍與 pwsh parse 同源；負向容器實測攔截/放行正確） |
| DEF-101-035 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **NTFS 檔名閘僅本機 pre-commit，CI 無對等**：`--no-verify`／GitHub web／未裝 hooks 的 clone 可繞過，一旦入庫全部 Windows checkout 癱瘓 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：新增 `tools/check_ntfs_paths.py`〔忠實移植 `_ntfs_seg_bad`＋大小寫碰撞，git ls-files 全量〕入 root-infra-ci；實跑 27,328 路徑 0 違規＋9 敵意/6 正常單測；root-infra-ci 觸發改全變更〔終裁 (d) 准〕） |
| DEF-101-036 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **雙腳本 .sh/.ps1 對等無機械守護**：bootstrap／integration_gate／local_ci_gate 三對閘門清單純靠人工雙改，單點漂移風險 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：新增 `tools/check_script_parity.py`〔step 標籤序列比對＋pytest 釘選雙處同版〕入 root-infra-ci，現況 3+5+9 標籤一致＋負向測試正確判不等；其餘三對無標籤錨點明文豁免於 ONBOARDING §6） |
| DEF-101-037 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect） | **跨專案 bridge 測試 CI paths 盲區**：AutoClaude bridge 測試實際消費 AISDLC_SDD v0.01/v0.02/v0.05 檔案，但 autoclaude-ci paths 只有 `AutoClaude/**`——AISDLC_SDD-only 變更改壞凍結版要等下次 AutoClaude push 才暴露 | P2 | 本輪即修 | fixed@四方複審第四輪（證據：autoclaude-ci push＋pull_request paths 補 `AISDLC_SDD/AISDLC_SDD_v0.01/**`、`v0.02/**`、`v0.05/**`，消費點以 grep 實證） |
| DEF-101-038 | 2026-07-11 | 跨平台修復輪四方複審第四輪（四方 P3 彙總） | **P3 × 17 全數修復**：根層缺 README／PTY macOS 差異未文件化／`Thumbs.db` 忽略不對稱／根 CLAUDE.md 單平台路徑／GUI commit venv PATH 雷區未文件化／`integration_gate.ps1` 缺 python 守門＋LASTEXITCODE 重置／`ci-gate.ps1` 覆蓋差距單向擴大／`run_act.sh` pipefail+grep -q 違規／bootstrap 沿用 .venv 無平台形狀檢查／`copy_on_evolve.sh` 檔頭直執行示範／`download_bge_m3.py` docstring Windows 路徑／aisdlc-sdd-ci artifact 只傳 v0.01／pytest 釘選雙處無一致性檢查／equivalence 測試裸 python PATH／v0.01 post_commit_drift docstring 不符（known-gap 化）／contract test 錨定鎖擴及三份 settings／parity 未覆蓋對明文侷限 | P3 | 本輪即修 | fixed@四方複審第四輪（證據：README.md 新增、.gitignore 補 Thumbs.db/desktop.ini、CLAUDE.md:11 中性化、ONBOARDING §2/§4/§5/§6/§6.1/§9 多處、ci-gate.ps1 薄委派 bash、run_act.sh herestring、bootstrap×2 形狀 fail-fast、equivalence conftest、aisdlc-sdd-ci glob、parity 腳本含釘選比對；統一驗證 pytest 3543/181＋scripts/tests 全綠） |
| DEF-101-039 | 2026-07-11 | 跨平台修復輪四方複審第四輪（Architect 終裁 (c) 另立） | **NTFS 檔名閘（hook＋CI）皆無 MAX_PATH 長度檢查**：Windows 未開 `core.longpaths` 時 >260 字元路徑 checkout 失敗；現況最長 tracked 路徑 142 字元、含 clone 前綴仍餘裕充足，急迫性低 | P3 | 後續輪 hook＋CI 兩側同步加「≤180 warn / ≤200 fail」保守長度閘（預留 clone 前綴） | fixed@跨平台第五輪（證據：`tools/git-hooks/pre-commit` A3 閘第 4 項 >200 擋下/>180 預警〔code point 計數＝刪 UTF-8 連續位元組，locale 無關〕；`tools/check_ntfs_paths.py::_length_level` 同門檻同語意；contract test `AISDLC_SDD/scripts/tests/test_ntfs_length_gate.py` 14 case（本輪 SA 複審實測 `pytest --collect` 訂正計數，原誤標 13）六道鎖〔純函式邊界 180/181/200/201、hook 實跑同四點釘死、存量 >fail 零違規、CI main() 接線鎖、`_ntfs_seg_bad` 跨語言 parity、CJK C-locale 無關；cacheinfo 暫存不落地建檔〕；實跑 27,333 tracked 路徑 0 違規、最長 142） |
| DEF-101-041 | 2026-07-11 | 跨平台修復輪四方複審第四輪（SA 發現，Architect 終裁 (a)(b) 准） | **v0.02~v0.04 arch-fitness.json＋findings.json 誤入庫殘留**：政策（DEF-11-001，v0.05 起）建立前複製的三版 SENSE 輸出被追蹤，evidence 欄嵌機器絕對路徑（`D:\` 實錘）→ 雙平台輪替每次重生即 diff 抖動（已實證發生） | P3 | 本輪即修 | fixed@四方複審第四輪（證據：六檔 `git rm --cached` 出庫〔磁碟保留〕＋`AISDLC_SDD/.gitignore` 補兩個排除 block；零消費點以 grep 實證；僅改追蹤狀態不改凍結版位元組） |
| DEF-101-042 | 2026-07-12 | 跨平台修復輪四方複審第五輪（Architect/SA/SD/QA 四方一致；SA 擴及既有案例） | **contract test 落 CI 觸發盲區（DEF-101-037 同構復發）**：`test_ntfs_length_gate.py`（消費根層 `tools/check_ntfs_paths.py`＋`tools/git-hooks/pre-commit`）與既有 `test_hook_wiring_cwd_safety.py`（消費根層＋AutoClaude 的 `.claude/settings.json`）唯一 CI 載體為 aisdlc-sdd-ci（經 ci-gate scripts leg），但其 paths 僅 `AISDLC_SDD/**`；root-infra-ci 雖全變更觸發卻無 pytest step——**只改根層消費檔的變更（如把 `_LEN_FAIL` 改 2000）三道鎖全數沉默**，要等下次 AISDLC_SDD 變更才暴露 | P2 | 本輪即修 | fixed@第五輪（證據：aisdlc-sdd-ci push＋pull_request paths 各補 **5** 條根層消費檔〔tools 兩支＋兩份 settings.json＋複審輪 Architect/SD 交叉抓漏之 `.claude/hooks/sdd_hook_router.py`——兩支 router contract test＋ci-gate router lint 硬閘消費〕；檔頭補「根層消費檔 paths」設計說明；不在 root-infra-ci 裝 pytest——保其「零依賴安裝 <1 分鐘」契約。**治本**：新增 meta contract test `test_ci_paths_cover_root_consumers.py`——正則掃描 scripts/tests 全部 `_monorepo_root()` 消費點、機械斷言 ∈ workflow paths＋push/PR 對稱鎖＋已知 5 條防正則退化鎖；負向驗證＝抽掉 router 行即紅——037→042→router 三度同構打地鼠自此機械終結） |
| DEF-101-043 | 2026-07-12 | 跨平台修復輪四方複審第五輪（Architect F-2＋QA P2-2 同源） | **長度閘存量鎖與 warn 帶語意矛盾**：防線 3 硬斷言全量 tracked ≤180，但閘門政策明定 181~200＝warn 可入庫（hook exit 0、CI exit 0）——合法 warn 路徑一旦在 `AISDLC_SDD/**` 外入庫（該處 aisdlc-sdd-ci 不觸發），下一個無辜的 AISDLC_SDD 變更在防線 3 爆紅（延遲、錯誤歸因），三閘對同一問題給兩種答案 | P2 | 本輪即修 | fixed@第五輪（證據：防線 3 門檻改 `>_LEN_FAIL` 常數引用〔閘門本應攔下卻在庫＝自我矛盾，語意收斂單一故事〕；warn 帶政策維持第四輪終裁 (c) 規格原文；docstring 明文「warn 帶依政策可入庫，不以 180 斷言」防回退） |
| DEF-101-044 | 2026-07-12 | 跨平台修復輪四方複審第五輪（QA mutation 實證 M1/M6 假綠） | **CI 版 `main()` 長度接線零測試鎖**：防線 1 只鎖 `_length_level` 純函式——整段集成刪除（M1）或 fail 降級寫入 warnings（M6）後測試照綠；root-infra-ci 實掃因存量全合規也照綠＝閘門靜默失效的假綠窗口 | P2 | 本輪即修 | fixed@第五輪（證據：防線 4 兩 case——工具複製至 sandbox（`_REPO_ROOT=parents[1]` 自然錨定）實跑 main()：201→exit 1＋「路徑過長」、190→exit 0＋⚠ stderr〔同時鎖「warn 不影響退出碼」〕；QA M1/M6 mutation 重放已被殺，見第五輪複審驗證） |
| DEF-101-045 | 2026-07-12 | 跨平台修復輪四方複審第五輪（SA） | **pytest 基線三文件矛盾（交叉引用斷裂）**：ONBOARDING §7 宣稱「與根層 CLAUDE.md 記載的 2026-07-11 實測基線 3,543/181 一致」，但根 CLAUDE.md:76 實記 3,529@2026-07-09、AutoClaude/CLAUDE.md 標頭＋文末元數據同滯留 3,529——被引用的內容不存在（第四輪 +14 case 未同步兩份 CLAUDE.md） | P2 | 本輪即修 | fixed@第五輪（證據：根 CLAUDE.md:76、AutoClaude/CLAUDE.md 標頭與元數據三處同步 3,543/181@2026-07-11＋「第四輪 +14 case」註記；本機實測 3,543/181 佐證） |
| DEF-101-046 | 2026-07-12 | 跨平台修復輪四方複審第五輪（SA） | **CLAUDE.md 計數漂移家族**：根 CLAUDE.md「16 Plugin／12 ports」vs 機械生成 Snapshot「18 active/19 靜態、Port 18 個」（漂移 2 plugin＋6 port）；AutoClaude/CLAUDE.md:111 手寫目錄樹「14 active/15 靜態」與**同檔** Snapshot 自我矛盾——根檔對 AISDLC_SDD 數字有「指向 FRAMEWORK_STATUS」政策，對 AutoClaude 數字卻裸寫零守護 | P2 | 本輪即修 | fixed@第五輪（證據：根 CLAUDE.md 兩處數字中性化＋明文「Plugin/Port 計數一律見 [Architecture Snapshot]，與 FRAMEWORK_STATUS 同政策」；AutoClaude/CLAUDE.md:111 去數字改指 Snapshot——手寫數字歸零，漂移面拆除） |
| DEF-101-047 | 2026-07-12 | 跨平台修復輪四方複審第五輪（P3 彙總：Architect F-3~F-7＋SA×3＋SD×2＋QA×2） | **P3 × 11 全數處置**：①註解算術差一（MAX_PATH=260 含 NUL，260−60=200 邊界超限）②hook 邊界釘定精度（QA M5 warn 門檻漂移存活；190 取樣容許 [190,200] 漂移帶）③C locale 下 `${#f}` 退化 byte 計數→CJK 67 字元誤擋且與 CI 分歧（SD/QA 實證）④`_ntfs_seg_bad` 第 1~3 項跨語言雙實作零 parity 鎖⑤換行檔名 hook 逐行讀 fail-open 疑點（Architect F-5）⑥merge/rebase/cherry-pick 繞過 pre-commit 未註記⑦Windows+WSL 環境 `shutil.which("bash")` 誤選 System32 佔位 bash→四檔 bash 類測試紅燈而非 skip⑧ONBOARDING §6 攔截原因清單漏列長度閘⑨ONBOARDING §2「30 個凍結版 SDD hooks」措辭不精確⑩`wiring.py` docstring 漏列 rtm_writeback/translation_learner⑪ONBOARDING §9 尾註漏 DEF-101-040 對應 | P3 | 本輪即修 | fixed@第五輪（證據：①三處註解改「260 含 NUL 可用 259、259−200＝59」②防線 2 四點 180/181/200/201 hook 實跑釘死③hook 改「刪 UTF-8 連續位元組」code point 計數（locale 無關）＋防線 6 LC_ALL=C CJK case④防線 5 Python 表驅動 11 case＋hook 實跑保留名/禁字⑤**查證不成立**——SD/QA 雙實證 git 對含控制字元路徑恆 C-quote（不受 quotepath=false 影響），引號化表徵含 `"`/`\` 觸發第 1 項攔截，兩側封閉，結論記入 CI 版 docstring⑥hook 檔頭補註（root-infra-ci 兜底）⑦新增 `scripts/bash_probe.py` 共用 helper（git 相鄰 Git Bash 優先＋排除 System32），四檔 skipif/argv 升級⑧⑨⑪ONBOARDING 三處補正⑩docstring 補列兩 key；scripts/tests 135→148 全綠） |
| DEF-101-048 | 2026-07-12 | 跨平台修復輪四方複審第五輪之**複審輪**（Architect REJECT 阻塞 1＋SD/QA APPROVE 附帶殘餘） | **複審輪殘餘 × 5**：①（P2，Architect 阻塞項/SD 交叉印證）DEF-101-042 修法掃漏根層 `.claude/hooks/sdd_hook_router.py`——`test_router_version_path_safety.py:31`＋`test_session_start_hub_timeout_budget.py:40`＋ci-gate `router_hook_coverage_lint` 硬閘皆消費該檔，單改 router 時三道鎖本機＋雲端雙盲（037→042→本筆三度同構）②（P3，Architect/SD）`check_ntfs_paths.py:46` 常數區塊殘留舊算術「260−60」與同檔 docstring 矛盾（047① 宣稱三處實漏第四處）③（P3，SD）`test_ntfs_length_gate.py:172` 新引入 E501（AutoClaude convention line-length=100；「ruff 7 檔綠」原聲明僅預設規則集成立）④（P3，QA 探針 M10/M11 存活）CI `main()` 的 seg_bad 接線與大小寫碰撞段零行為鎖——大小寫碰撞（A3 第 3 項）兩側皆無行為測試⑤（P3，QA 實測）flen 管線每檔 3 spawn，863 檔 Copy-on-Evolve commit 於 Windows Git Bash 估 25~75s 可感延遲 | P2×1＋P3×4 | 本輪即修（複審輪內閉合） | fixed@第五輪複審輪（證據：①paths 補第 5 條＋meta contract test 治本〔詳 DEF-101-042 欄〕②:46 改「可用 259−59＝200」四處歸一③172 行斷言拆行；另 I001（import 分組隨 cwd 而異之歧義）於終裁輪由 Architect 抓出、四檔 `# isort: skip` 註記補修，:46 修正註解自身 E501 同輪拆行——終態以釘選 ruff 0.15.21 `--isolated --line-length 100 --select E,F,I,UP` 於 monorepo 根與 AISDLC_SDD 雙 cwd 實測全綠（sister 三檔 I001 同驗）④防線 4 擴充 `test_ci_main_seg_bad_and_case_collision_wiring`——staged CON.txt＋Readme.md/readme.md 斷 exit 1＋兩訊息；M10/M11 重放各 1 failed 被殺⑤hook 前濾 `[ "${#f}" -gt 180 ]` 短路——任一 locale 下 ${#f}≥code point 數故 ≤180 必然合規，現況最長 142 → 全量零 spawn，超標者才進精確管線；scripts/tests 148→152 全綠、還原後 mutation 基線 14 passed。**另 SD 未驗殘餘觀察**：WindowsApps WSL bash 別名理論上繞過 System32 字串排除——macOS 無法實證，`bash_probe` 的 `echo ok` 驗活對此無鑑別力，known-observation 待 Windows 實機輪驗證） |
| DEF-101-049 | 2026-07-12 | 排程 nightly 紅燈追查（push 後兩封失敗通知；本機 CI 對等容器沙箱實證） | **`alembic/env.py` 交易語意缺陷 → `alembic upgrade head` 靜默整包 rollback**：R56 P0-1e 的預建 alembic_version（`exec_driver_sql`）觸發 SQLAlchemy 2.0 autobegin，alembic 偵測「呼叫端已有進行中交易」切換 caller-managed 模式不再自行 commit，而 env.py 從未 commit → `with connect()` 結束 close() 把 16 個 migration＋版本戳＋CREATE TABLE 整包 rollback——**exit 0、零輸出（env.py 無 fileConfig 本就無 INFO log）、零資料表**；pg-contract／pg-e2e-nightly 兩 job UndefinedTable 之根因，所有呼叫端（2 CI job＋labeled-PR workflow＋local_ci_gate 雙腳本＋Windows nightly）同受害 | P0 | 本輪即修 | fixed@nightly 追查輪（證據：env.py 預建表後補 `connection.commit()` 結清 autobegin、alembic 回自管交易；CI 對等 pg17 fresh 容器實證修前 0 表／修後 46 表＋`alembic_version=0016 (head)`；三處 workflow migration 步驟補持久化守門 `alembic current \| grep -q "(head)"`——同類靜默回滾未來就地現形；沙箱換 alembic 1.16.5 同病、排除新版 regression。**雲端取證**：labeled PG E2E run 29177170726〔最新 main merge ref〕migration＋守門步驟綠） |
| DEF-101-050 | 2026-07-12 | 排程 nightly 紅燈追查（DEF-101-049 修後 recall 仍 0.000 續查） | **pg-e2e 的 X1 seed 步驟自 monorepo 匯入（0cda8aa，2026-06-12）即遺失**：recall 測試前置明載「`tools/seed_kb.py` 已 seed 100 列」，但新舊 workflow 皆無 seed 步驟（git 考古實證 0 命中）；fixture JSON 在庫使測試不走 skip 分支 → 查詢空 `knowledge_entries` → recall 恆 0.000（p95 對空表反而假綠通過） | P1 | 本輪即修 | fixed@nightly 追查輪（證據：autoclaude-ci pg-e2e-nightly＋autoclaude-pg-e2e-on-label 兩 workflow 於 migration 後補 `seed_kb.py --mock-pg-seed --pg-dsn $AUTOCLAUDE_TEST_PG_DSN` 步驟〔seed 同時重生 fixture 與當次資料嚴格配對；工具自帶 +asyncpg strip〕；本機 fresh 容器完整鏈 migrate→seed→pytest 實證 recall/p95 **2 passed**〔修前 recall=0.000 紅〕。**雲端取證**：labeled PG E2E run 29177170726〔空 commit 取證 PR #8，最新 main〕Seed 步驟「Inserted 100 mock entries」＋pytest 2 passed/1 skipped——與本機一致；首次取證誤用 dependabot PR #7 為載具因 merge ref 過期（未含 02:21Z 修復）而無效，已作廢） |
| DEF-101-052 | 2026-07-12 | 雲端取證輪（兩次 labeled run「Comment AC4 progress on PR」步驟同紅） | **labeled PG E2E workflow 缺 permissions 宣告**：預設 GITHUB_TOKEN 唯讀 → github-script 對 PR 留言恆 `HttpError: Resource not accessible by integration`，每次 labeled run 帶一顆無關紅 X 干擾取證判讀 | P3 | 本輪即修 | fixed@雲端取證輪（證據：job 補 `permissions: contents:read + pull-requests:write`；dependabot 觸發情境依 GitHub 政策仍強制唯讀屬平台限制，已於註解明文「取證勿用 dependabot PR 當載具」） |
| DEF-101-053 | 2026-07-12 | DEF-101-051 四方審查（QA 提疑 + 主 agent 探針取證） | **`test_pg_existing_schema_lock.py` CRUD fixture 缺 `poolclass=NullPool`（R56 只修姊妹檔 `test_pg_state_repository_contract.py`，此檔遺漏）**：預設 QueuePool 跨 `asyncio.run` 快取連線→loop 關閉後重用死連線。4 支 CRUD 測試全受害：`test_checkpoint_run_id_uniqueness_enforced`／`test_playbook_versions_self_fk_chain` 於首個 execute（在 `pytest.raises` 外）撞 `RuntimeError: attached to a different loop`＝**紅**；`test_playbook_runs_status_check_constraint`／`test_kb_outcome_check_constraint` 把唯一 execute 包在 `pytest.raises(Exception)` 內→loop RuntimeError 被當例外吞→**假綠**（CHECK 從未真正被測；Rule 9 為錯誤原因通過，比紅更危險）。取證：scratchpad 探針複製 fixture 實跑 status-check body→`EXCEPTION TYPE: RuntimeError`（非 CheckViolation）；0016 與 0017 皆重現，與 DEF-101-051 改動無關 | P2 | SD_10 PG-track follow-up：比照 R56 加 `poolclass=NullPool`；修後 2 假綠轉真綠、uniqueness 轉綠、versions 撞 DEF-101-054。**不得用 xfail 規避**（鐵律）。非 pg-contract 硬閘範圍（該 job 只跑 `test_pg_state_repository_contract.py`），故不阻塞本輪轉硬閘 | **fixed@四方審查閉環**（NullPool 修復〔比照 R56 姊妹檔〕+ 4 支 CRUD `pytest.raises(Exception)` 收緊為 `match=` 綁定 DB 實際 constraint 名；真 pg17 實跑 4/4 CRUD 真綠：2 假綠〔status/kb CHECK〕轉真測、uniqueness/versions_self_fk 由紅轉綠〔versions 同時撞 DEF-101-054 已一併修〕。四方審查 QA 揪出 `EXPECTED_CHECK_CONSTRAINT_NAMES` 為死常數〔定義但無 assertion 消費〕P2 → 補 parametrized `test_check_constraint_names_match_baseline` 使 4 判別欄 CHECK 於離線層真正上鎖〔Rule 9〕。衍生 DEF-101-055〔ORM vs DB constraint 命名分歧〕。三契約檔真 PG 92P/2S、ruff clean、LOC 0） |
| DEF-101-054 | 2026-07-12 | DEF-101-051 四方審查（DBA/QA/Architect 三方同指） | **`playbook_versions.project_id` 平行時間炸彈仍活體生效**（與 DEF-101-051 runs 同構、0017 只修 runs 未修 versions）：0010 建 `ck_versions_post_cutoff_has_project`（`project_id IS NOT NULL OR created_at < '2026-05-20'`）+ FK 欄，但 `PlaybookVersion` ORM 未映射 project_id、`pg_playbook_repository._persist()` INSERT 不帶 project_id → cutoff 後任何經 PG 落地的 evolution 版本必撞 CheckViolation。目前僅因 PG playbook 持久化未上 production 而 dormant（`test_playbook_versions_self_fk_chain` 修掉 DEF-101-053 loop bug 後即會顯此紅） | P1 | SD_10 PG-track follow-up：比照 runs (c) 政策為 versions 補 `version_kind` 判別欄（或補 project_id 端到端接線）；屬另一 orphan-policy 決策，需使用者/SD 定調後動 schema | **fixed@四方審查閉環**（比照 runs (c) 政策：alembic **0018** 加 `version_kind`∈{standalone,project_scoped} 判別欄，CHECK 由時間 cutoff 改 `version_kind<>'project_scoped' OR project_id IS NOT NULL`〔無時間依賴→消除 versions 時間炸彈〕；`PlaybookVersion` ORM 補 `project_id`〔0010 已建 DB 欄，此前 ORM 未映射＝半成品〕+ `version_kind` + 2 CHECK；`.py`/`.sql` 雙路徑。真 pg17：upgrade→head、downgrade 0018→0017 可逆〔舊 CHECK 還原為 NOT VALID＝陷阱防護〕、既有列天然滿足新 CHECK。契約測試 `test_alembic_0010` T9→0018 語意 + T13/T13b/T13c〔比照 runs T10 系列，match= 綁 constraint 名〕、schema_lock DDL baseline 補欄+CHECK。三方審查〔DBA/QA/Architect〕全 **PASS 無 P0/P1/P2**。**DBA 提 production 前置**：上線前確認 dormancy〔`SELECT count(*) WHERE project_id IS NOT NULL`〕，若非零需補 `UPDATE...SET version_kind='project_scoped' WHERE project_id IS NOT NULL`〔見 NextAction〕。PG playbook 持久化未上 production 故現 dormant） |
| DEF-101-059 | 2026-07-13 | Mac/Windows 相容性優化四方複審（SD 直接靜態分析＋QA 真實 `git commit` 觸發，兩位獨立發現同一缺陷的不同表徵） | **`.claude/hooks/post_commit_drift.py`／`closure_evidence_verify.py` 在 git worktree 下因 `GIT_DIR` 污染 `git rev-parse --show-toplevel` 而失效，與 `install_post_commit.*`（DEF-101-057）同根因、不同檔案**：兩支 hook 的 `_repo_root()`/`repo_root_from()` 舊版用 `git rev-parse --show-toplevel` 搭配顯式 `cwd=`（指向 hook 所在版本目錄）定位 monorepo 根；git 對 worktree 觸發 post-commit hook 子行程會注入 `GIT_DIR`（指向 `<主repo>/.git/worktrees/<name>`），此時 `--show-toplevel` 無論 `cwd` 為何一律退化成直接回顯 `cwd` 本身（不再向上尋根）。兩位獨立審查者從不同角度切入命中同一根因：SD 直接呼叫腳本邏輯，REPO_ROOT 誤算成 worktree 根目錄（其 `.git` 為指標檔非目錄）→ naive `repo_root/".git"` join 崩潰 `NotADirectoryError`；QA 用真實 `git worktree add`＋真實 `git commit` 觸發，REPO_ROOT 誤算成 hook 版本目錄本身（該路徑無 `.git`）→ `git_dir.exists()` 恆 False → 靜默跳過，Rule 9.17.1 drift 監控與 DEF-20-001 結案證據反幻覺閘門在 worktree 下的真實 commit 完全靜默失效且無人察覺 | P1 | 本輪即修 | **fixed@Mac/Windows 相容性優化四方複審**（`_repo_root()`/`repo_root_from()` 改用 `git rev-parse --path-format=absolute --git-common-dir` 取 `.parent`，`--git-common-dir` 對「有無 `GIT_DIR`、cwd 在主 repo 或 worktree 內任何子目錄」皆能正確解回真正共用的 `.git`，同時堵住 SD 的崩潰模式與 QA 的靜默模式；新增 `tools/fsm_runtime/tests/test_post_commit_drift_worktree.py` 用真實 `git init`→`git worktree add`→真實 `git commit` 觸發驗證，退回舊碼可重現兩位審查者各自的失效模式、套用修復後一般 checkout＋worktree 皆通過；既有 `test_post_commit_drift.py`／`test_closure_evidence.py` 共 32 個測試與全套 1670+ 測試無回歸。SD 複審另指出 `tools/fsm_runtime/closure_evidence.py` 共用模組自己的 `repo_root_from()` 仍為舊寫法，僅影響 build 報告落點、不涉 `.git` 寫入安全，非阻塞，留待下一輪一併收斂） |
| DEF-101-061 | 2026-07-14 | Mac/Windows 相容性四方複審（QA 複審輪實測 commit d6dfeed 觸發之真實 windows-compat-ci run 發現） | **`sandbox_runner.py::docker_available()` 只查 `docker info`（daemon 可達性），不保證能實際跑 Linux 容器**：windows-latest GitHub runner 上 `docker info` 回報成功，但 `DockerBackend.run()` 對 Linux-only 映像 `busybox` 執行 `docker run`（含 `--cap-drop ALL`／`--user 65534:65534`／`--pids-limit` 等 Linux 專屬安全旗標）卻回傳 nonzero exit、stdout 為空——`@requires_docker` 因此誤判「可跑」而非確定性 skip，導致 `test_docker_backend_real_pass`／`test_docker_backend_e2e_through_fsm` 兩測試在 windows-smoke（非 continue-on-error 硬閘）確定性失敗，連續兩次 push+rerun 皆同樣失敗、非偶發網路抖動。根因研判：Windows runner 的 Docker daemon 常態預設為 Windows containers 模式，對 Linux-only image 執行會直接失敗（no matching manifest），但此與「daemon 可達」是兩件事，舊版探測混為一談 | P1 | 本輪即修 | **fixed@Mac/Windows 四方複審 2026-07-14**（兩階段修復：第一版 `docker_available()` 改為 `docker info` 通過後試跑裸 `docker run --rm busybox true`，push 後真實 windows-compat-ci 驗證**仍同樣失敗**——證明裸探測過關但 `DockerBackend` 實際使用的完整安全旗標組合〔`--network none`／`--cap-drop ALL`／`--read-only`／`--user 65534:65534`／`--pids-limit`／`--cpus` 等〕在該環境下仍不相容，問題不在映像本身能否執行，而在旗標組合。第二版改為探測直接複用 `DockerBackend().run()` 走同一段程式碼、同一組 `DEFAULT_SECURITY_PROFILE`，才能真實反映測試等一下要做的事現在做不做得到；任一層失敗一律回 False 讓測試正常 skip。本機以相同安全旗標 `docker run --rm --cap-drop ALL --user 65534:65534 --pids-limit 256 ... busybox` 實測成功排除「旗標本身語法有誤」的可能性；`test_phase_h.py` 36 case 全綠、`AISDLC_SDD` 全套 ci-gate v0.01:1478／v0.30:1670／scripts/tests:152 無回歸；同輪一併發現並修復 CI paths 白名單第三個同類缺口〔Architect 複審輪發現：`tools/fsm_runtime/tests/**` 只涵蓋測試檔，未涵蓋同目錄下 `sandbox_runner.py` 等 78 支原始碼本體——本次修改的 `docker_available()` 恰好就是該未涵蓋目錄下的檔案〕，已放寬為 `tools/fsm_runtime/**`；已推 windows-compat-ci 真實觸發驗證——**commit 186c650 觸發之真實 run（29271469978）確認 `Windows smoke: success`**，`gh api` 查 job log 摘要 `1475 passed, 7 skipped`（v0.01）／`1665 passed, 9 skipped`（v0.30），skip 數與先前一次成功 run 完全一致，證實 docker 測試是被正確判定「環境不可用」而乾淨 skip，非殘留錯誤被其他機制吞掉；QA 複審輪逐一核對四個 workflow（windows/macos-compat-ci、root-infra-ci、aisdlc-sdd-ci）在此 commit 上皆綠燈。**四方複審過程中，Architect 複審輪又發現第四個同類缺口**：`.claude/settings.json`（`.claude/hooks/**` 的手足檔案，且已有真實歷史回歸——即 DEF-101-032 的 `PYTHONUTF8` 修復成果就在此檔）未被任何 pattern 涵蓋；鑑於同一問題類別連續復發四次，Architect 建議換結構性解法而非再補窄 glob——已將三條窄 pattern（`tools/install_hooks/**`、`tools/fsm_runtime/**`、`.claude/hooks/**`）合併為兩條寬 pattern（`AISDLC_SDD/AISDLC_SDD_v*/tools/**`、`AISDLC_SDD/AISDLC_SDD_v*/.claude/**`），一次性涵蓋整個 `tools/` 與 `.claude/` 子樹（含未來新增子目錄），用 `fnmatch` 驗證確認涵蓋 `.claude/settings.json`、`tools/arch_fitness/`（Architect 次要觀察項）等全部已知手足路徑） |
| DEF-101-062 | 2026-07-14 | Mac/Windows 相容性四方複審（commit 186c650 與 f71f284 之間，同一份未變動的 `docker_available()` 程式碼在連續三次真實 windows-compat-ci run 中出現不一致結果） | **windows-latest GitHub-hosted runner 的 Docker Linux 容器支援本身不穩定，非本框架程式碼可控的確定性 bug**：DEF-101-061 第二版 `docker_available()`（直接複用 `DockerBackend().run()` 走完整安全 profile 探測）在 commit 186c650 觸發的真實 run 中成功（`Windows smoke: success`，docker 測試乾淨 skip），但同一份完全未變動的程式碼在下一個 commit（f71f284，僅改 workflow paths + defect log，未觸及 `sandbox_runner.py`）觸發的真實 run 中卻失敗（`test_docker_backend_real_pass`／`test_docker_backend_e2e_through_fsm` 確定性斷言失敗），rerun 後再度失敗——連續三次真實 CI run 呈現 1 成功／2 失敗，證實這是 GitHub-hosted windows-latest runner 對 Linux 容器（LCOW）支援本身的環境不穩定性，不是可以靠更精確的探測邏輯消除的確定性 bug（探測本身已經是與正式測試完全同碼路徑，仍無法穩定預測後續執行結果） | P2 | 本輪即修：`test_docker_backend_real_pass`／`test_docker_backend_e2e_through_fsm` 兩測試（需要容器**成功**執行並正確回傳輸出）改用新 marker `requires_docker_success`（`not _DOCKER or sys.platform.startswith("win")`），在 Windows 上一律 skip；`test_docker_backend_real_runtime_fail`（只驗證「容器執行失敗」情境）維持原 `requires_docker`，不受影響——環境不穩定本身也會產生 `nonzero_exit`，恰好符合該測試預期，不需要排除。macOS/Linux 環境 Docker 原生穩定，兩測試在該平台仍正常執行（本機 macOS 實測 3 個 docker 測試皆 PASSED） | **fixed@Mac/Windows 四方複審 2026-07-14**（兩層修復：① `test_phase_h.py`（LATEST v0.30）新增 `requires_docker_success` marker 套用至兩個「需要成功執行」的測試；② push 後真實 windows-compat-ci **仍然失敗**——查明原因是 `ci-gate.ps1` 同時跑「凍結基線 v0.01 + LATEST v0.30」雙軌，v0.01 的 `test_phase_h.py` 是舊版無條件 `requires_docker`，依框架治理規則「凍結基線不可在原地修改」不能直接比照修 v0.30 那樣改原始碼。改在 CI 組態層級用標準 `PYTEST_ADDOPTS` 環境變數對兩個測試做 keyword 排除（`windows-compat-ci.yml` 的 `ci-gate.ps1` 步驟），v0.01／v0.30 兩軌皆吃得到、零框架程式碼變動、不違反凍結版紀律；本機以 `PYTEST_ADDOPTS` 模擬驗證 v0.01 正確 deselect 2 個測試、`ci-gate.sh` 全套通過（v0.01:1476／v0.30:1668／scripts/tests:152，較先前各少 2 即被排除的兩測試）；已推 windows-compat-ci 真實觸發驗證是否穩定收斂） |
| DEF-101-063 | 2026-07-14 | R3 Mac/Windows 相容性複審（全新 session 不採信既有 APPROVE 自稱、獨立重新複核；Architect 發現） | **CI paths 白名單仍有結構性缺口，且 DEF-101-061 聲稱的驗證方法論本身有誤**：①`windows-compat-ci.yml`／`macos-compat-ci.yml` 皆漏根層消費檔——`.claude/hooks/sdd_hook_router.py`（有真實歷史回歸 DEF-43-001）、`.claude/settings.json`（DEF-101-028 hook wiring 修復現場）、`AutoClaude/.claude/settings.json`；DEF-101-061 自稱「用 fnmatch 驗證確認涵蓋 `.claude/settings.json`」，經逐位元組核對，該驗證實際命中的是 `AISDLC_SDD/AISDLC_SDD_v0.30/.claude/settings.json`（版本化子目錄下的同名檔），對根層目標檔完全零匹配——即 CLAUDE.md 自身警告的「⚠️ 路徑陷阱」在 CI 修復過程真實發生，且逃過同一輪四方複審。②`windows-compat-ci.yml` 另漏 `AISDLC_SDD/scripts/**` 整個目錄（僅列 `install-hooks.ps1` 一支，`ci-gate.sh` 本體／`router_hook_coverage_lint.py`／`scripts/tests/*.py` 皆未涵蓋）；`macos-compat-ci.yml` 雖有 `**/*.sh` 涵蓋 `.sh` 檔，但 `.py` 檔（`router_hook_coverage_lint.py`、`scripts/tests/*.py`）同樣未涵蓋 | P0（①）＋P1（②） | 本輪即修 | fixed@R3 四方複審 2026-07-14（兩份 workflow 皆補 `.claude/**`／`AutoClaude/.claude/**`／`AISDLC_SDD/scripts/**` 三條寬 pattern；本機以 Python `fnmatch` 逐一核對三個根層消費檔＋`scripts/` 全部 tracked 檔案皆已涵蓋） |
| DEF-101-064 | 2026-07-14 | R3 Mac/Windows 相容性複審（QA 發現） | **`tools/tests/**`（含 `test_pre_commit_dispatcher_sigpipe.py` SIGPIPE 回歸鎖 + `test_dev_start.py` 平台邏輯 mock）從未在真實 Windows/macOS runner 上執行過一次**：`windows-compat-ci.yml` 的 paths 甚至未涵蓋此目錄；`macos-compat-ci.yml` 雖已涵蓋，但兩份 workflow 皆無任何 step 呼叫這批測試——唯一執行位置是 `root-infra-ci.yml` 固定 `runs-on: ubuntu-latest` 的 `python3 -m unittest discover -s tools/tests`，與 windows/macos-compat-ci.yml「在真實目標平台驗證平台假設」的存在理由相悖；`test_dev_start.py` 內大量用 `mock.patch.object(sys, "platform", ...)` mock 跨平台邏輯，但這些 mock 假設本身從未在真實對應作業系統上驗證過 | P2 | 本輪即修 | fixed@R3 四方複審 2026-07-14（`windows-compat-ci.yml` 補 `tools/tests/**` path；windows-smoke／macos-smoke 兩 job 皆新增 `python -m unittest discover -s tools/tests -p "test_*.py" -v` 實際執行步驟；本機 macOS 實測全綠） |
| DEF-101-065 | 2026-07-14 | R3 Mac/Windows 相容性複審（QA 發現） | **windows-nightly-alert／macos-nightly-alert 的「紅色路徑」（`gh issue create`/`gh issue comment`）從未在真實環境執行驗證過**：以 `gh run list --json event` 核對，windows 側 16 筆歷史 run 中 0 筆 `schedule`（僅 4 筆 `workflow_dispatch` 驗證到 green-path no-op 分支），macos 側 6 筆歷史 run **全部**為 `push` 事件，`macos-nightly-alert` 至今**連 green path 都未曾觸發一次** | P1 | 本輪即修 | fixed@R3 四方複審 2026-07-14（兩份 workflow 的 `workflow_dispatch` 補 `simulate_nightly_failure` boolean input，check step 支援手動覆寫為失敗以驗證開單/留言/自動關單三條分支邏輯，不需等待真實失敗；已透過受控 `workflow_dispatch` 驗證，見本輪 commit 說明） |
| DEF-101-066 | 2026-07-14 | R3 Mac/Windows 相容性複審（SA 發現） | **`ONBOARDING.md` 對 hypothesis 版本鎖定的敘述與實況不同步**：commit `d6dfeed`（2026-07-13）已將 `AutoClaude/pyproject.toml` 的 `hypothesis` 精確鎖版為 `6.156.6` 並同步將 Defect Log DEF-101-058 標記為 fixed，但該 commit 只改了 `ONBOARDING.md` 一行（COM0-9/LPT0-9 措辭），未同步 §9 known-gap 表與其註腳——`ONBOARDING.md:236,238` 仍宣稱「未實際修改依賴版本」「DEF-101-058（open，記事存證）」，與已修復實況矛盾，屬「改程式碼/帳本忘同步姊妹文件」的跨文件同步缺口 | P2 | 本輪即修 | fixed@R3 四方複審 2026-07-14（`ONBOARDING.md` §9 該列與註腳改為反映 DEF-101-058 已 fixed 之實況；順帶補充 Java/PowerShell 版本量化建議、Windows Defender 雷區、DEF-101-060 於 §9 露出，SA 架構最佳化建議一併採納） |
| DEF-101-069 | 2026-07-14 | DEF-101-064 修復落地後首次真實 windows-compat-ci workflow_dispatch 驗證（run 29333618619，QA 要求的收尾驗證觸發） | **`tools/tests/` 有史以來第一次在真實 windows-latest 上執行，立即揪出 4 類共 16 個真實失敗（99 個測試中 4 failures + 12 errors），完整驗證了 DEF-101-064「先前只在 ubuntu-latest 上 mock 過」這個結構性缺口的嚴重性**：①（10/16，根因同一）`tools/dev_start.py` 的 `_warn()`／`_hr()` 對含 `⚠️`/`✅` 等符號的 `print()` 呼叫，在 Windows 非 UTF-8 終端（預設 cp1252 codepage）下 `UnicodeEncodeError` 直接崩潰——`main()`（CLI 入口）已有 `sys.stdout.reconfigure(encoding="utf-8")` 保護，但測試套件與任何未經 `main()` 直接呼叫模組內部函式的呼叫端完全不受此保護，此為 CLI 入口以外的真實健壯性缺口（非僅測試假象）；②（1/16）`test_fetch_failure_degrades_to_offline` 測試 fixture 直接 `shutil.rmtree()` 刪除含 git 唯讀物件檔的目錄，Windows 上刪唯讀檔案需先清屬性，POSIX 上父目錄可寫即可刪除（純測試 fixture 缺陷，非生產碼問題）；③（4/16）`TestCacheRestoreTrustRestoredBranch`／`TestStepVenvPrevNoneHealthCheck`／`TestRootLevelBootstrapIncompleteMarker` 的「健康直譯器」fixture 用 POSIX shebang 腳本（`#!/bin/sh`）模擬，`_venv_healthy()` 實際 `subprocess.run([py, "--version"])` 在 Windows 上對非 PE 格式檔案撞 `WinError 193`，使「應視為健康」的測試情境在 Windows 上永遠走到「健檢失敗」分支（純測試 fixture 的跨平台可攜性缺陷，`_venv_healthy()` 本身行為完全正確——正確地把不可執行的檔案判定為不健康）；④（1/16）`test_peek_reflects_real_child_process_lifetime` 在子行程 `proc.wait()` 後檢查 `_peek_bootstrap_lock()` 應回 None，但 Windows 上父行程（測試本身）仍持有子行程的 `Popen` handle 未關閉，`OpenProcess` 因此仍能開啟該「殭屍」行程物件而誤判仍存活——與正式場景（另一個全新 dev_start 行程檢查素不相識的 PID、從未持有其 handle）不同，屬測試本身未正確模擬「無人持有 handle」情境 | P0（①，範圍達 10/16 且為 CLI 入口以外的真實健壯性缺口）＋P3（②③④，皆為測試 fixture 跨平台可攜性問題，非生產邏輯錯誤） | 本輪即修 | fixed@R3 收尾驗證輪（① 將 `main()` 內的 `sys.stdout`/`sys.stderr` UTF-8 重設搬到模組載入時執行，涵蓋所有呼叫路徑非僅 CLI 入口，並移除 `main()` 內原本的重複程式碼；② 新增 `_rmtree_force()` 測試輔助函式，`onerror` 清除唯讀屬性後重試；③ 改用 `shutil.copy(sys.executable, py)` 複製當前真正在跑的直譯器本體取代 shebang 腳本，三平台皆為合法可執行檔（`test_valid_same_platform_cache_is_restored` 的內容比對斷言同步從 `read_text` 改 `read_bytes` 比對真實二進位內容）；④ 在第二次斷言前 `del proc` 顯式釋放本行程持有的 handle。本機 macOS 複測 99 passed 無回歸；已再次觸發 windows-compat-ci workflow_dispatch 驗證真實 Windows 上是否全綠，見後續 run 記錄）。**第二輪真實驗證（run 29334937643）再揪出 2 個同款③類遺漏**：`TestBootstrapIncompleteMarker` 類別下 `test_healthy_and_no_marker_still_reuses_without_bootstrap`／`test_partial_failure_leaves_marker_and_forces_rebootstrap_next_run` 兩測試也用同款 shebang fixture 且未被首輪清單覆蓋（首輪這兩個測試因 UnicodeEncodeError 崩潰而掩蓋了底層的 WinError 193 問題，①修復後才顯露）——同款手法修復（`shutil.copy(sys.executable, ...)`），本機複測 99 passed，第三輪真實 Windows 驗證見後續 run 記錄 |
| DEF-101-067 | 2026-07-14 | R3 Mac/Windows 相容性複審（Architect/SD/QA 各自發現之次要項，合併記錄） | **五項次要發現，皆非阻擋項**：①（Architect）`pty_wrapper.py` docstring 誤稱「Windows PTY 包裝器」，實為 macOS/Linux/Windows 三平台共用之唯一實作（`wexpect` 於 pyproject.toml 已 scope 為 `win32`，非 win32 平台恆走 subprocess fallback）；②（SD）DEF-101-057 對 v0.01 `install_post_commit.{sh,ps1}` 的風險敘述失真——實測任何一般 checkout（非僅 worktree）皆會炸 `hook source not found`，觸發條件比原記載更廣（原記載誤植為「需同時滿足舊版目錄+worktree 兩條件」）；③（QA）`test_docker_backend_real_runtime_fail` 的 Windows 豁免僅靠經驗證據支持（5 敗 1 成的真實 CI log 核對），無程式碼層面形式化保護；④（QA）`macos-compat-ci.yml` 先前未像 `windows-compat-ci.yml` 對其 `ci-gate.ps1` fallback 覆蓋率限制那樣，誠實揭露「docker 測試在 macos-latest 官方 runner 上因無 Docker daemon 而恆被 skip」；⑤（Architect）`tools/check_ntfs_paths.py` 缺席於 `windows-compat-ci.yml` 的 paths（`macos-compat-ci.yml` 有），風險低（純字串邏輯、`root-infra-ci.yml` 恆跑一層防護） | P3×5 | ①③④本輪即修；②為既有條目敘述訂正；⑤下輪視情況補齊 | ①③④ fixed@R3 四方複審 2026-07-14（①docstring 訂正；③於 `test_phase_h.py` 補程式碼註解記錄脆弱點，不變更行為；④於 `macos-compat-ci.yml` ci-gate.sh 步驟補誠實揭露註解）；②為 DEF-101-057 敘述訂正，本檔該筆條目維持 append-only 不回改，訂正另見本行；⑤ open（記事存證，P3 不阻擋） |
| DEF-101-070 | 2026-07-15 | 四方複審 S13：DEF-101-068(b)「腳本雙軌維護降為薄wrapper+Python核心」候選逐一評估定案 | **(b) 原記事把 5 支候選腳本（`bootstrap`/`integration_gate`/`local_ci_gate`/`install_git_hooks`/`install-hooks`）當同一籃子「排入下一輪評估」，經四方重新逐一檢視，5 者成本/風險並不均質，不宜一刀切**。差異化定案（讀 `tools/bootstrap.ps1`／`AutoClaude/tools/install_git_hooks.ps1`／`AISDLC_SDD/scripts/install-hooks.ps1` 現行內容核實後拍板）：①`install_git_hooks.ps1`／`install-hooks.ps1` — **reject** 全面轉 Python 核心：實測兩支腳本真正共用的邏輯只有一個 `git config core.hooksPath` 呼叫，其餘皆為 Windows-only 提示文字，轉核心只多一層間接、不會消除重複；真正該抽的窄段（Git Bash 偵測樣板）已於 S11 落地為 `tools/lib/Find-GitBash.ps1`（見 DEF-101-068(c) 已落地註記），此項無殘留動作。②`integration_gate.{sh,ps1}`／`local_ci_gate.{sh,ps1}` — **accept，正式排入下一輪**：比照 `tools/dev_start.py` 既有模式（Python 核心以 subprocess 呼叫各平台既有 gate 腳本＋集中計數/報告邏輯），因兩者永遠在 bootstrap 之後執行、venv 保證存在，無雞生蛋問題，可行性高。③`bootstrap.ps1`／`bootstrap.sh` — **defer**：兩檔剛於本輪之前的 F4（`uv pip install` hotfix）修過，屬活躍檔案；此刻疊加「殼層只留找直譯器、建 venv/裝依賴/fail-fast 搬進 Python 核心」的大重構，風險/收益比不划算，留待穩定一段時間後再評估，本輪不動其程式碼 | P3（架構決策，非缺陷） | 定案：①reject（免列入待辦）／②排入下一輪候選（優先序最高）／③defer（不早於下下一輪） | closed-by-decision（本輪四方複審拍板；`tools/bootstrap.ps1`／`install_git_hooks.ps1`／`install-hooks.ps1` 三檔本輪僅讀取核實現況、未變更程式碼——決策結論為「維持現狀」；DEF-101-068(b) 原「待後續輪評估是否投入」的模糊狀態至此以本行定案取代，(b) 本身狀態不回改，訂正見本行） |
| DEF-101-071 | 2026-07-15 | 四方複審 S15：DEF-101-068(d)「`check_script_parity.py` 加深至雙邊實際執行+輸出 diff」定案評估 | **(d) 加深方向本身正確，但本輪查無任何已知缺陷是由此深度缺口造成**：重讀 `tools/check_script_parity.py` 全文核實，其 docstring 第 4~8、19~24 行已自承邊界——「本工具只機械比對『標籤序列』（數量、順序、字面文字），不比對、也無能力比對各 step 背後的實作內容是否語意對等」「通過（exit 0）不代表兩份腳本行為完全一致」——即現行粒度已誠實揭露、不構成掩蓋性缺陷；「同名 step 語意分歧」目前是理論風險，非本輪或先前各輪任何一次真實故障的根因，投入產出比明顯低於同帳本其餘 7 項架構建議（(a)(b)(c)(e)(f)(g)(h)） | P3（架構建議，非缺陷） | 記事存證，defer；待未來真的出現一次「同名 step 語意分歧」的真實事故後，依該事故具體形狀設計加深方案（而非現在憑空猜一個要 diff 什麼） | closed-by-decision（本輪四方複審拍板 defer；`tools/check_script_parity.py` 本輪僅讀取核實 docstring 已忠實揭露此邊界、未變更程式碼與文件——決策結論為「維持現狀，記事存證」；DEF-101-068(d) 原「待後續輪評估是否投入」的模糊狀態至此以本行定案取代，(d) 本身狀態不回改，訂正見本行） |
| DEF-101-072 | 2026-07-15 | 四方複審 S17：DEF-101-068(f)「生產端接線三段式檢核（觸發/執行/斷言）制度化為可重用 meta-lint」定案評估 | **(f) 方向本身正確，但屬 8 項架構建議中設計範圍最大、最開放的一項，不該塞進本輪批次逕行動工**：「三段式檢核」要機械化必須先回答一整組尚未有共識的設計問題——如何機械判定一段 CI/hook/workflow 邏輯「有沒有真正被觸發」（`workflow_dispatch` 模擬？真實 `schedule` 事件？兩者等價嗎）、「有沒有真正被執行」（exit code？stdout 關鍵字？）、「斷言」要斷言什麼（副作用如 `gh issue create` 是否真的發生？需要 mock 到什麼邊界？）——這些若不先定案，直接動工只會產出一支看似通用、實則綁死本輪已知案例（DEF-101-064/065/069）的一次性腳本，違反規格先行紀律；DEF-101-068 原文本身也已明說「本輪不逕行實作，範圍限於本輪 P0~P2 阻擋項」 | P3（架構建議，非缺陷） | 記事存證，defer 到下一輪；下一輪動工前須先產出小型 ADR/RFC 定義範圍與可行做法（設計先行），而非直接寫 meta-lint 腳本 | closed-by-decision（本輪四方複審拍板 defer；本輪未新增/修改任何 meta-lint 程式碼或 workflow——決策結論為「維持現狀，記事存證，下一輪先走 ADR/RFC 設計階段」；DEF-101-068(f) 原「待後續輪評估是否投入」的模糊狀態至此以本行定案取代，(f) 本身狀態不回改，訂正見本行） |
| DEF-101-073 | 2026-07-15 | 四方複審 S22（architecture-review-own-finding，Architect 自行審查發現）：DEF-101-068(c)「git hooks liveness 判定邏輯收斂單一實作」補完落地 | **S11 只收斂了四支呼叫端腳本（`integration_gate.{sh,ps1}`／`local_ci_gate.{sh,ps1}`）共用 `tools/check_hooks_liveness.py`，卻遺漏第五個獨立重寫同款判定邏輯的呼叫端——`tools/dev_start.py` 的 `step_hooks()`（每日自動修復，step 5/7）**：兩處各自維護一份「core.hooksPath 是否等於預期 dispatcher 目錄＋三支 hook 檔是否齊備」的判定，且行為早已分歧——`dev_start.step_hooks()` 有 linked worktree 感知（core.hooksPath 由所有 worktree 共用、永遠指向主 checkout 絕對路徑，比較基準須用 git-common-dir 推回主 checkout，見 Architect 更早複審 P1 迴歸）、`check_hooks_liveness.py` 的 advisory 版完全沒有，在 linked worktree 下會誤用本 worktree 自己的根目錄比對而假警告「未生效」。三層架構（bootstrap 一次性建置／dev_start 每日自動整備自癒／git hooks dispatcher 強制閘門）本身依觸發時機分層合理、無循環依賴，問題僅出在同一判定邏輯被獨立重寫兩次 | P2 | 本輪即修（與 DEF-101-068(c) 合併執行） | **fixed@S22**（`tools/check_hooks_liveness.py` 抽出三個不含任何 git 子行程呼叫、不印訊息的純函式：`resolve_expected_hooks_dir()`（含 linked worktree 推回主 checkout 邏輯，補上 advisory 版原本缺失的正確性）、`is_hooks_effective()`（路徑比對＋三支 hook 檔齊備，`is_file` 檢查以 callable 注入，讓 `dev_start.py` 可傳入自家 `_safe_is_file()` 保留既有 OSError 防護、不被迫改用裸 `Path.is_file()`）、`evaluate()`（組合前兩者的單一判定入口，回傳 `LivenessResult` dataclass）；`check_hooks_liveness()`（CLI advisory 入口）與 `dev_start.step_hooks()` 皆改呼叫 `evaluate()`，兩處僅保留合理的行為分歧（如何取得 git 輸入、拿到結果後自動修復或只印警告），判定演算法本身不再有第二份實作。`tools/tests/test_check_hooks_liveness.py` 新增 `TestResolveExpectedHooksDir`／`TestIsHooksEffective` 兩個純函式測試類別＋1 個 linked worktree advisory 警告文案回歸測試（5→13 個測試），既有 `test_dev_start.py` 的 `TestStepHooksLinkedWorktree`／`TestStepHooksIsFileOSError` 兩組迴歸測試**零修改**且維持全綠（證明重構未改變 `dev_start.step_hooks()` 對外可觀察行為）；`tools/tests/` 全套 `python -m unittest discover -s tools/tests -p "test_*.py"` 實測 **Ran 135 tests, OK (skipped=9)**（獨立複審 [E36] 校正：unittest 摘要「Ran N tests」的 N 含被 skip 者，非等同 pytest 的 passed 計數，改用 pytest 重跑得 126 passed + 9 skipped = 135 total，0 failed 為真；含本輪新增 8 個測試），`py_compile` 通過） |
| DEF-101-074 | 2026-07-15 | 四方複審 [E24]：DEF-101-068(a)「CI paths 白名單單一事實來源機械鎖只涵蓋 1/4 個 workflow」補完落地 | **`test_ci_paths_cover_root_consumers.py` 原僅讀 `aisdlc-sdd-ci.yml` 一份、只掃 `AISDLC_SDD/scripts/tests/` 一個目錄，對 `windows-compat-ci.yml`／`macos-compat-ci.yml`／`root-infra-ci.yml` 三份 workflow 完全零機械保護——S5 複審實測發現的「`windows-compat-ci.yml` 漏 `check_ntfs_paths.py`／`check_script_parity.py`」正是這個結構性缺口的活生生案例：即使當輪已手動補上兩條 paths，若日後再漏補，本鎖並不會紅** | P1 | 本輪即修（依 S12 修正版做法，泛化為可讀 4 個 workflow，但為每個 workflow 各自定義消費者集合，不可盲套同一套正則） | **fixed@[E24]**：①`aisdlc-sdd-ci.yml`／`windows-compat-ci.yml`／`macos-compat-ci.yml` 三份改用 `_WORKFLOW_TEST_DIRS` 對照表（workflow ↔ 其實際執行的測試目錄：`AISDLC_SDD/scripts/tests/` 用 `_monorepo_root()` 慣用法、`tools/tests/` 用 `REPO_ROOT` 慣用法）逐一參數化跑「消費檔 ⊆ paths」機械斷言＋push/PR 對稱鎖，不同目錄各自的正則慣用法不盲套同一套；②`root-infra-ci.yml` 刻意不設 paths 過濾（NTFS 敵意檔名閘需對任何路徑生效），硬套①同一套正則無意義，改立兩道專屬鎖：`test_root_infra_ci_has_no_paths_filter`（鎖死「刻意不設 paths」設計決策本身，防被誤「補全」重新引入假綠盲區）＋`test_root_infra_ci_bash_and_py_scan_roots_have_no_stray_scripts`（`git ls-files` 掃全部 `.sh`／`.py`，斷言扣除 `AutoClaude/`／`AISDLC_SDD/`／`.claude/`（各自已有獨立 CI／paths 鎖）與 `tools/`（root-infra-ci 實際掃描根）後為空集，堵住「新增根層腳本目錄逃過語法守門」的同構盲區）。實測：`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 9 個測試（含新增 6 個）**9 passed**；`AISDLC_SDD/scripts/tests/` 全套 **158 passed, 0 failed**，無回歸 |
| DEF-101-075 | 2026-07-15 | 四方複審 [E26]：DEF-101-068(d)/(f)/(g) 綜合判斷定案（QA 覆核） | **(d)(f) 已分別由 DEF-101-071／DEF-101-072 定案 defer，本行僅補齊尚缺的 (g) 正式關閉紀錄，避免帳本上 (g) 只停留在 DEF-101-068 自身狀態欄位的口頭「已落地」、無獨立可追溯條目**：逐一核實——(d) `check_script_parity.py` 語意加深方向正確，但現行「標籤序列比對」粒度已對應到本輪真實抓到的宣告漂移（DEF-101-070 三腳本定案時的核實依據），尚無一次真實「同名 step 語意分歧」事故佐證需要加深，維持 DEF-101-071 defer 決議不變；(f) 生產端接線三段式檢核（觸發/執行/斷言）制度化範圍在 8 項架構建議中最開放，DEF-101-068 原文本身已載明「本輪不逕行實作」，維持 DEF-101-072 defer 決議不變；(g) 以 `git grep simulate_nightly_failure -- .github/workflows/` 核實，`windows-compat-ci.yml`／`macos-compat-ci.yml` 兩份 workflow 皆已落地此 boolean input（對應 DEF-101-065 修復），QA 覆核確認無殘留動作 | P3（架構建議，非缺陷） | (d)(f) 沿用既有 defer 決議（不重複定案）；(g) no_action_needed | closed-by-decision（本輪僅讀取 `tools/check_script_parity.py`／DEF-101-068 原文／`.github/workflows/{windows,macos}-compat-ci.yml` 三處核實現況，未變更任何程式碼或 workflow——決策結論：(d)(f) 維持 DEF-101-071／DEF-101-072 既有 defer 結論不變、不重複定案；(g) no_action_needed，simulate_nightly_failure 已於兩份 workflow 落地確認。DEF-101-068(d)/(f)/(g) 至此皆有獨立可追溯的關閉條目，帳本狀態不回改，訂正見本行） |
| DEF-101-076 | 2026-07-15 | 四方複審 [E27]：DEF-101-068(e)/(h) 補完落地 — implement_now | **(e) 帳本↔姊妹文件狀態跨文件同步落差的機械守護仍缺席**：DEF-101-066 當輪已真實復現一次「commit 只改帳本、`ONBOARDING.md` 未同步」的漂移，屬正則掃描可精準偵測的形狀，但當輪只手動修正、未落地成可重複執行的機械檢查。**(h) `gh issue create`（nightly-alert 分支）缺 `--label`，且實測是兩邊都缺、非只 windows 一邊**：`windows-compat-ci.yml`／`macos-compat-ci.yml` 皆缺，與既有慣例（`aisdlc-sdd-fsm-chaos-nightly.yml` 已有 `--label "p0,chaos,fsm-runtime"`）不一致，兩檔應一起修 | P2 | 本輪即修 | **fixed@[E27]**：(e) 新增 `tools/check_defect_log_crossref.py`（範圍先窄鎖 `ONBOARDING.md`：正則抽取「緊鄰括號的 `DEF-\d+-\d+` 狀態宣稱」與帳本表格該 ID 最新狀態欄比對，不一致即 exit 1；docstring 誠實自承邊界——不理解自然語言、不掃無括號的裸引用、不拆巢狀提及），wired 進 `root-infra-ci.yml` 新增一道步驟；`tools/tests/test_check_defect_log_crossref.py` 新增 12 個測試（`_classify` 關鍵字判別、帳本解析、跨文件比對、`main()` 端到端，含對真實 repo 現況跑一次的迴歸鎖）。(h) `windows-compat-ci.yml`／`macos-compat-ci.yml` 的 `gh issue create` 皆補 `--label "p1,windows,nightly"`／`--label "p1,macos,nightly"`，兩檔同步修正、不獨厚一邊。**驗證**：`python3 tools/check_defect_log_crossref.py` 對真實 repo 現況跑出 `✅ 帳本 150 筆有效狀態紀錄、1 份掃描目標皆無矛盾`（exit 0）；`python -c "import yaml; yaml.safe_load(...)"` 確認兩份 workflow YAML 語法合法；`tools/tests/` 全套 `python -m unittest discover -s tools/tests -p "test_*.py"` 實測 **Ran 135 tests, OK (skipped=9)**（獨立複審 [E36] 校正：正確拆分為 126 passed + 9 skipped = 135 total，0 failed 為真），無回歸 |
| DEF-101-077 | 2026-07-15 | 四方複審 [E31]：獨立子 agent 對「DEF-101-068(a)(c)(e)(h) 是否真正落地」提出質疑，要求重新驗證 | **[E31] 質疑當時的 git 快照裡確實只看得到 F1~F5，尚未包含 DEF-101-073~076 的落地成果，故該質疑在其觀測時點成立；但本輪指派的獨立修復 agent 重新對照當下工作樹逐一核實後，確認 DEF-101-073~076 描述的四項落地（(a)(c)(e)(h)）皆非文件自稱、而是工作樹中真實存在且可執行的程式碼與 CI 接線**，未發現任何「宣稱已做但實際沒做」的落差 | P3（複核，非新缺陷；查證結果為既有落地屬實） | 本輪以獨立重跑驗證指令收尾，不重複造輪；若日後這批檔案被誤刪/回退，本行可作為「應該存在」的查核基準 | **no_action_needed（[E31] 查證結果：已落地屬實，非本輪需修復項）**：獨立重新逐一核實——① `tools/check_hooks_liveness.py`（181 行）＋`tools/lib/Find-GitBash.ps1`（36 行）／`tools/lib/GitHooksInstallCommon.ps1`（129 行）皆為完整實作，`grep` 確認四呼叫點（`tools/integration_gate.{sh,ps1}`／`AutoClaude/tools/local_ci_gate.{sh,ps1}`）與三個 dot-source 點（`ci-gate.ps1`／`install_git_hooks.ps1`／`install-hooks.ps1`）皆已改一行呼叫、`tools/dev_start.py:81,1323` 亦已改呼叫共用 `check_hooks_liveness.evaluate()`；② `AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py` 確認已泛化為 `_WORKFLOW_TEST_DIRS` 對照表涵蓋 4 份 workflow（非僅 1 份）；③ `tools/check_defect_log_crossref.py`（149 行）＋`tools/tests/test_check_defect_log_crossref.py`（166 行）存在且已 wired 進 `root-infra-ci.yml`（新增一道步驟，`git diff` 可見）；④ `windows-compat-ci.yml:448`／`macos-compat-ci.yml:402` 的 `gh issue create` 皆已補 `--label "p1,windows,nightly"`／`--label "p1,macos,nightly"`。**重新實測（非複製既有帳本數字，獨立跑出）**：`python3 tools/check_defect_log_crossref.py` → `✅ 缺陷帳本跨文件狀態一致：帳本 151 筆有效狀態紀錄、1 份掃描目標皆無矛盾`（exit 0，151 筆因帳本持續累積而略高於 DEF-101-076 記載的 150，數字有機成長本身即為佐證非造假複製）；`python -m unittest discover -s tools/tests -p "test_*.py"` → **Ran 135 tests, OK (skipped=9)**（獨立複審 [E36] 校正：正確拆分為 126 passed + 9 skipped = 135 total，0 failed 為真）；`python -m unittest tools.tests.test_check_hooks_liveness -v` → **13 passed**；`cd AISDLC_SDD && python -m pytest scripts/tests/ -q` → **158 passed**；`cd AutoClaude && (venv 正確啟用後) python -m pytest tests/ -q` → **3567 passed, 195 skipped, 0 failed**（與 F1~F5 驗證基線一致，無回歸）。結論：[E31] 於本輪查證時點已無殘留落差，帳本狀態不回改，訂正見本行 |
| DEF-101-078 | 2026-07-15 | 四方複審 [E35]：DEF-101-068(d) `check_script_parity.py` 加深方向的再一次覆核（獨立修復 agent 派工） | **[E35] 覆核結論與 DEF-101-071（首次定案）／DEF-101-075（QA 二次覆核）完全一致，未發現需推翻既有 defer 決議的新事證**：重讀 `tools/check_script_parity.py` 全文（`docstring` 第 4~8、19~24 行）確認邊界揭露文字仍在、未被移除或弱化；核對本輪（S1~S31）實際抓到的相關缺陷 S5／S6，兩者現象皆為「CI workflow paths 白名單漏收 script 路徑」（分流至 DEF-101-068(a)／DEF-101-074 CI paths 單一事實來源鎖），與「同名 step 語意分歧」（parity 比對粒度不足才會漏抓的那種缺陷形狀）無關；截至本輪仍查無一次真實「兩邊 step 標籤相同、實作語意卻分歧」的事故可供設計加深方案的具體形狀。維持 defer：加深方向本身正確，但現在動工只能是憑空猜測要 diff 什麼，投入產出比低於帳本其餘項目 | P3（架構建議，非缺陷） | 沿用 DEF-101-071／DEF-101-075 既有 defer 決議，不重複定案；記事存證供下一次覆核直接引用，免重複分析 | **closed-by-decision（[E35] 覆核維持 defer，本輪未變更 `tools/check_script_parity.py` 任何程式碼或 docstring）**：驗證 `python3 tools/check_script_parity.py` 於當下工作樹仍 exit 0（`✅ 雙平台腳本對等檢查通過（3 對腳本 + pytest 釘選）`，3 對腳本標籤序列一致 + pytest 釘選一致），確認本次「不修」的決策未意外破壞既有守護；`tools/tests/` 全套 `python -m unittest discover -s tools/tests -p "test_*.py"` 實測 **Ran 135 tests, OK (skipped=9)**（獨立複審 [E36] 校正：正確拆分為 126 passed + 9 skipped = 135 total，0 failed 為真），無回歸。DEF-101-068(d) 至此累積三次獨立覆核（DEF-101-071／DEF-101-075／本行）皆得出相同 defer 結論，帳本狀態不回改 |
| DEF-101-079 | 2026-07-15 | 四方複審 [E36]：DEF-101-068(f) 「下一輪動工前須先產出小型 ADR/RFC」前置條件的補齊落地 | **DEF-101-072／DEF-101-075 皆已定案 defer 生產端接線三段式檢核（觸發/執行/斷言）meta-lint 化，且明文要求「下一輪動工前須先產出小型 ADR/RFC 定義範圍與可行做法」，但截至 [E36] 發現前，該前置條件本身從未被實際履行——帳本上只有「應該寫」的決議文字，沒有真的產出過這份文件**，屬於「規格先行紀律要求的產物尚未落地」的缺口，而非要求逕行實作 meta-lint 本體（範圍最開放、DEF-101-068 原文已明說本輪不做） | P3（架構建議，非缺陷） | 本輪即補：產出小型 RFC 文件，**不含任何 meta-lint 程式碼**，僅定義問題陳述、三個未決設計問題（觸發/執行/斷言各自的機械判定草案）、明確 Non-Goals、下一輪建議範圍提案；本輪與下一輪皆維持 defer 實作 | **fixed@[E36]**：新增 `docs/02_architecture/RFC-ProductionWiring-ThreeStageCheck-Scope.md`，交叉引用 DEF-101-064／065／068(f)／069／072／075 之既有事證，§3 逐一列出「觸發如何機械判定（reachability vs 經驗已驗證兩層級）」「執行如何機械判定（排進執行圖 vs 真的跑到底 vs 環境是否與正式場景一致）」「斷言邊界在哪（副作用類分支不可在一般 CI 自動觸發真實副作用，斷言以 `workflow_dispatch` 手動模擬為主）」三個未決問題，§5 提出「先窄後寬／先靜態可達性掃描後動態驗證／明確排除自動建立真實副作用」三點下一輪範圍建議，§6 明定本文件通過僅代表前置條件履行、不自動授權下一輪逕行開工。本輪未變更任何 CI workflow（含 `.github/workflows/windows-compat-ci.yml`）或既有程式碼——DEF-101-068(f) 仍維持 defer，本行僅補齊其自身要求的設計階段產物 |
| DEF-101-080 | 2026-07-15 | 獨立複審（新 session，不採信既有四方 APPROVE 自稱，逐條核對帳本內部一致性時發現） | **DEF-101-070（本檔上方，四方複審 S13）與同一輪稍後產出的 `tools/lib/GitHooksInstallCommon.ps1` docstring 直接互相矛盾，帳本自身未察覺**：DEF-101-070 用來 reject 進一步整併 `install_git_hooks.ps1`／`install-hooks.ps1` 的核心論據是「實測兩支腳本真正共用的邏輯只有一個 `git config core.hooksPath` 呼叫，其餘皆為 Windows-only 提示文字」；但 `GitHooksInstallCommon.ps1:6-12` docstring 自陳「逐行比對...全文（各約 92-115 行）發現：linked worktree 偵測...HooksDir 正規化...安裝前存在性驗證迴圈...安裝後 curOk 正規化比對驗證...Git Bash 缺失警告文案這五個區塊...共用範圍約占每檔 115 行中的 90 行」，且親讀現行 `install_git_hooks.ps1:24-61`／`install-hooks.ps1:10-38` 確認兩檔皆已 dot-source 該共用檔並呼叫 `Assert-NotLinkedWorktree`／`Get-DispatcherHooksDir`／`Test-DispatcherHooksPresent`／`Test-GitHooksPathInstalled` 四個函式——`GitHooksInstallCommon.ps1:28-114` 逐一核對皆為含條件判斷／迴圈／`exit` 的實質邏輯，並非「Windows-only 提示文字」（真正的提示文字僅 `Write-GitBashMissingWarning`，`GitHooksInstallCommon.ps1:116-129`，14 行）。DEF-101-077／078 覆核 DEF-101-068(c)(d) 落地情形時，皆把 `GitHooksInstallCommon.ps1` 的存在當「(c) 已落地」的正面證據引用，卻未回頭檢查它是否與 DEF-101-070 用以 reject (b) 的措辭相容，致此矛盾延續三輪覆核未被發現 | P2（帳本文件內部事實矛盾；不影響現行程式碼行為，(b)/(c) 的既有處置**結論**仍站得住腳，僅 DEF-101-070 的**理由敘述**失準——非阻擋項） | 本輪即修（帳本 append-only、不回改 DEF-101-070 原文，訂正另立本行） | **fixed@獨立複審（訂正 DEF-101-070 敘述，不變更任何程式碼）**：核實 (c) 抽出後，`install_git_hooks.ps1`／`install-hooks.ps1` 呼叫端本身已各自精簡為 65 行／42 行（2026-07-15 `wc -l` 實測），共用的判斷/驗證邏輯已收斂至 `GitHooksInstallCommon.ps1`（129 行）單一真相源，(b)「全面轉 Python 核心」訴求的實質目的（消除重複、單一處修 bug）已透過 (c) 達成，**reject 結論本身不變**；但 DEF-101-070「其餘皆為 Windows-only 提示文字」一句與同輪自身產出矛盾，正確敘述應為「兩腳本除各自保留的產品特有文案（`install_git_hooks.ps1` 特有的 `-Uninstall` 支援、兩專案不同的閘門說明文字）外，剩餘邏輯已於 (c) 抽出至共用檔，(b) 若再疊加一層 Python 轉譯間接層，效益已被 (c) 提前吸收，故仍以 reject (b)、維持現狀為正確定案」。本行僅訂正 DEF-101-070 的事實性錯誤描述，DEF-101-070 原文依帳本紀律不回改；本次未觸碰 `install_git_hooks.ps1`／`install-hooks.ps1`／`GitHooksInstallCommon.ps1` 任何程式碼 |
| DEF-101-082 | 2026-07-15 | 最終複審輪專家駁回：DEF-101-068(c)/S11/E23「雙軌維護降為共用模組」的解法本身仍是兩份獨立實作 | **`tools/lib/GitHooksInstallCommon.ps1`＋`tools/lib/git_hooks_install_common.sh` 把同一段判定邏輯各自用 PowerShell 與 bash 重寫一份，而非沿用本 repo 同一輪內就有活範例的「Python 單一核心＋兩邊各一行呼叫」模式**：對照組 `tools/check_hooks_liveness.py` 被 `tools/integration_gate.{sh,ps1}`／`AutoClaude/tools/local_ci_gate.{sh,ps1}` 四個呼叫點共用一份 `evaluate()`，`tools/dev_start.py` 同樣呼叫該共用函式——判定邏輯全域只有一份；反例 `GitHooksInstallCommon.ps1:6-12` docstring 自陳「共用範圍約占每檔 115 行中的 90 行」，解法卻是把這 90 行分別謄寫成 `.ps1`（129 行）與 `.sh`（75 行）兩份宣稱等價、實際從未被機械比對過的實作（`tools/check_script_parity.py` 範圍未涵蓋這兩檔），重新引入了它宣稱要消除的雙軌漂移風險類別 | P2（架構重複；DEF-101-070/080 皆已核實兩檔功能正確，本行是「消除重複的手段本身仍重複」的架構層 finding，非行為缺陷） | 本輪即修：新建 `tools/git_hooks_install_common.py` 作為判定邏輯單一真相源（`assert-not-linked-worktree`／`get-hooks-dir`／`assert-hooks-present`／`check-installed` 四個 CLI 子指令），`GitHooksInstallCommon.ps1`／`git_hooks_install_common.sh` 改為呼叫它的薄殼層，僅保留各自平台原生的呈現層（PowerShell `Write-Host` 顏色／bash 全域變數回傳慣例）；公開函式簽名（`Assert-NotLinkedWorktree`／`Get-DispatcherHooksDir`／`Test-DispatcherHooksPresent`／`Test-GitHooksPathInstalled`／`assert_not_linked_worktree`／`get_dispatcher_hooks_dir`／`assert_dispatcher_hooks_present`／`check_git_hooks_path_installed`）維持不變，四個呼叫端（`install_git_hooks.ps1/.sh`、`install-hooks.ps1/.sh`）零改動；`tools/tests/test_dev_start.py::TestHooksConstantsConsistency` 改為讀取新 python 檔的 `HOOK_FILENAMES`／`hooks_dir` 宣告 | **fixed@本輪**：新建 `tools/git_hooks_install_common.py`（143 行，四子指令）；`GitHooksInstallCommon.ps1` 129→114 行、`git_hooks_install_common.sh` 90→60 行皆改為呼叫該檔的薄殼層。**過程中額外抓到並修復一個真實回歸**：初版薄殼層對 `--prefix $Prefix`（$Prefix='' 時）用兩個 token 呼叫 python，PowerShell 5.1 呼叫原生 exe 時會靜默吞掉空字串引數（`& python -c "..." foo --prefix $Prefix bar` 實測回傳 `['-c','foo','--prefix','bar']`，`--prefix` 後直接接下一個位置引數），導致 `AISDLC_SDD/scripts/install-hooks.ps1`（唯一以無 `-Prefix` 呼叫 `Assert-NotLinkedWorktree` 的呼叫端）實跑觸發 argparse `error: argument --prefix: expected one argument`；改用單一 token `"--prefix=$Prefix"` 形式修復並重新實測通過。**驗證**（見下方 verification 欄位對應之逐步實測，非複製記憶）：四函式（Assert-NotLinkedWorktree／Get-DispatcherHooksDir／Test-DispatcherHooksPresent／Test-GitHooksPathInstalled）與四對應 bash function 皆直接呼叫驗證正確；`install_git_hooks.sh/.ps1`／`install-hooks.sh/.ps1` 四支呼叫端安裝／解除往返皆驗證正確（含修復前失敗、修復後通過的 `install-hooks.ps1` 無前綴呼叫）；四支呼叫端於 linked worktree 下皆正確 rc=1 拒絕（對齊 `windows-compat-ci.yml:281-318`／`macos-compat-ci.yml:209-245` 既有 CI 斷言的確切手法）；`python -m unittest discover -s tools/tests -p "test_*.py"` → 139 tests, OK(skipped=9)；`python3 tools/check_hooks_liveness.py`／`check_script_parity.py`／`check_wrapper_thinness.py`／`check_defect_log_crossref.py` 全數 exit 0 無回歸；測試全程主 checkout 的 `core.hooksPath` 於每次往返驗證後皆已還原為 `tools/git-hooks`（`git config --get core.hooksPath` 複驗）；新檔皆 LF-only（`\r\n` 位元組檢查）符合 `.gitattributes` 政策 |
| DEF-101-083 | 2026-07-15 | 獨立複審最終輪（SD 發現）：`tools/check_wrapper_thinness.py` 黑名單式業務邏輯偵測可被繞過 | **`.sh` 側只收 `"python -c"`、`.ps1` 側只收 `"foreach ("`，皆非其對應變體字串的子字串，可完全繞過偵測**：實測對 `dev_start.sh` 追加 `python3 -c "..."`、對 `dev_start.ps1` 追加 C-style `for ($i=0; ...)` 或 `Get-Content x.json \| ForEach-Object {...}`，逐一以真實 `_FORBIDDEN` 字典比對皆命中空清單（完全繞過），業務邏輯可原封不動外溢回 wrapper 卻仍拿到 ✅ | P2 | 本輪即修 | **fixed@獨立複審**：`.sh` 黑名單加入 `"python3 -c"`；`.ps1` 黑名單加入 `"for ("`（C-style 迴圈，與既有 `"foreach ("` 不同拼法）與 `"ForEach-Object"`（管線 cmdlet 迭代；別名 `%` 因過於通用會誤中一般內容，刻意不收）。**驗證**：`tools/tests/test_check_wrapper_thinness.py` 新增 3 個回歸測試（`test_forbidden_python3_dash_c_in_sh_detected`／`test_forbidden_c_style_for_loop_in_ps1_detected`／`test_forbidden_foreach_object_cmdlet_in_ps1_detected`）皆重現繞過情境並確認修復後正確攔截；`python tools/check_wrapper_thinness.py` 對現行 `dev_start.sh/.ps1` 仍全綠（新關鍵字未誤中既有合法內容）；`python -m pytest tools/tests -q` 全數通過 |
| DEF-101-084 | 2026-07-15 | 獨立複審最終輪（SD 發現）：`tools/check_defect_log_crossref.py` 的 `_load_ledger_status()` 與自身 docstring「僅取最後一列」承諾矛盾 | **同一 DEF ID 若最後一列狀態欄文字無法辨識任何已知關鍵字，舊實作會靜默沿用更早一列的分類值**：迴圈只在 `label = _classify(status_cell)` 為真時才覆寫字典（`if label:` 才賦值），若最後一列 `_classify()` 回傳 `None`，字典殘留更早一列寫入的舊值，與 docstring 明文「僅取最後一列的狀態為準」矛盾——實測建構帳本 fixture（第一列 `fixed`、第二列故意寫不含任何合法關鍵字的文字）重現：`ledger.get('DEF-88-001')` 仍回傳 `'fixed'`，完全忽略最後一列 | P2 | 本輪即修 | **fixed@獨立複審**：`_load_ledger_status()` 改為每一列（只要 ID 格式合法）一律覆寫 `status[def_id] = _classify(status_cell)`（無論能否分類），型別改為 `dict[str, str \| None]`；`_scan_target()` 相應改用 `def_id not in ledger`（真查無此 ID）與 `ledger[def_id] is None`（帳本有此 ID 但狀態文字含糊）兩種情況分開報錯，避免混淆訊息。**驗證**：新增 `test_last_row_unclassifiable_does_not_inherit_earlier_row`／`test_ledger_status_none_flagged_distinctly_from_unknown_id` 兩個回歸測試；既有 `test_last_row_wins_on_duplicate_id`／`test_unknown_id_flagged`（真查無此 ID 訊息不變）等全數維持通過；`python tools/check_defect_log_crossref.py` 對真實帳本（158 筆有效狀態紀錄）仍全綠；`python -m pytest tools/tests -q` 全數通過 |
| DEF-101-085 | 2026-07-15 | 獨立複審最終輪（Architect/SD 發現）：`tools/git_hooks_install_common.py`（本輪新建 SSOT）自身有 cwd 依賴 bug + 完全零單元測試 | **`is_hooks_path_installed()` 對相對路徑形式的 `core.hooksPath` 用「呼叫當下 cwd」解析，而非依 repo 根目錄解析，與同一輪判定同一件事的 `tools/check_hooks_liveness.py`（`is_hooks_effective()` 已正確先與 `repo_root` 組合再 `resolve()`）行為不一致**：實測 `hooks_dir` 為絕對路徑、`current_value='tools/git-hooks'`（相對路徑）時，cwd=repo 根目錄呼叫得 `True`，cwd=子目錄呼叫同組輸入得 `False`——同一組輸入依呼叫環境給出不同答案。且本檔（存在理由即「消除雙軌漂移」）完全沒有函式級單元測試、也未被既有機械漂移防護涵蓋 | P3 | 本輪即修 | **fixed@獨立複審**：`is_hooks_path_installed()` 簽名新增 `repo_root: Path` 參數，改用 `(repo_root / current_value).resolve()`（`Path.__truediv__` 對絕對路徑第二運算元會直接取代整個路徑，故 `current_value` 為絕對路徑時行為不變），與 `is_hooks_effective()` 演算法一致；`cmd_check_installed()` 呼叫端補上 `git rev-parse --show-toplevel` 取得 `repo_root`。**驗證**：新增 `tools/tests/test_git_hooks_install_common.py`（14 個測試）涵蓋 `is_linked_worktree`／`missing_hook_files`／`is_hooks_path_installed`（含 cwd 獨立性回歸鎖與錯誤 repo_root 情境）三個純函式、CLI 子指令 smoke test（`get-hooks-dir`／`check-installed` 對真實 repo）、兩份呼叫端薄殼（`GitHooksInstallCommon.ps1`／`git_hooks_install_common.sh`）行數上限守門（刻意不比照 `check_wrapper_thinness.py` 用關鍵字黑名單——這對 wrapper 天生需要迴圈解析 subprocess 的結構化 stdout，套用黑名單會誤中合法內容，行數上限已足夠作為早期示警）；`python -m pytest tools/tests -q` 全數通過（148 passed, 9 skipped，較本輪起始 130 passed 新增 18 個測試） |
| DEF-101-086 | 2026-07-15 | 獨立複審收尾：DEF-101-073/076/077/078 四筆帳本紀錄把 `unittest discover` 摘要誤譯為 pytest 慣用語 | **commit `1175555` 訊息與帳本 DEF-101-073/076/077/078 四處重複把 `python -m unittest discover` 輸出「`Ran 135 tests ... OK (skipped=9)`」誤譯為「`135 passed, 9 skipped, 0 failed`」**：unittest 的「Ran N tests」N 本身即含被 skip 者，並非等同 pytest 的 passed 計數；改用 `python -m pytest tools/tests -q` 對同一份程式碼跑，正確拆分為 `126 passed, 9 skipped`（135 = 126+9）。0 failed 本身為真，不影響底層程式碼正確性，但代表 DEF-101-077（自稱「獨立子 agent重新驗證」）等至少兩輪「獨立複核」皆只複製前一輪錯誤數字、未真正核對 unittest 輸出語意 | P3（文件精確度，非程式碼缺陷） | 本輪即修 | **fixed@獨立複審**：已逐一訂正 DEF-101-073/076/077/078 四筆原文的計數措辭（改為「Ran 135 tests, OK (skipped=9)」+ 校正說明），原文其餘內容不回改，僅訂正計數誤譯部分 |
| DEF-101-087 | 2026-07-15 | R4 Mac/Windows 相容性四方複審修復輪：DEF-101-067⑤ 狀態訂正（帳本 append-only，原列不回改） | **DEF-101-067⑤（本檔上方）記載「`tools/check_ntfs_paths.py` 缺席於 `windows-compat-ci.yml` 的 paths（`macos-compat-ci.yml` 有）」並標記狀態「open（記事存證，P3 不阻擋）」——QA 覆核發現該缺口已於同一輪稍後的 R4 複審修正實際落地，⑤ 描述的缺口已不存在，原列狀態文字已過期** | **現況核實**：`.github/workflows/windows-compat-ci.yml:61-68` 為 R4 複審修正（2026-07-14，S5 發現）之記錄段落，明載「DEF-101-067⑤ 記載 `tools/check_ntfs_paths.py` 未被本 workflow 涵蓋的缺口重新核對仍存在...且新發現 `tools/check_script_parity.py` 同樣缺席...補 paths 白名單兩條 + windows-smoke 新增兩個實際執行步驟」；親讀確認 `:127-128`（push `paths`）與 `:153-154`（pull_request `paths`）皆已列入 `tools/check_script_parity.py`／`tools/check_ntfs_paths.py` 兩條，`windows-smoke` job 於 `:395-397` 新增執行 `check_script_parity.py`、`:399-401` 新增執行 `check_ntfs_paths.py` 兩個實際步驟，與對照組 `macos-compat-ci.yml` 對稱 | P3 | 無（狀態訂正，非新缺陷） | **fixed@R4 複審 2026-07-14**（`windows-compat-ci.yml:61-68` R4 複審修正說明段落 + `:127-128`／`:153-154` paths 白名單 + `:395-401` windows-smoke 實際執行步驟為修復當輪之直接證據；DEF-101-067 原列依帳本 append-only 紀律不回改，訂正見本行） |
| DEF-101-088 | 2026-07-15 | R4 Mac/Windows 相容性四方複審修復輪（QA P3）：`install_git_hooks.{sh,ps1}`／`install-hooks.{sh,ps1}` parity 標籤鎖評估定案 | **QA 要求為 `AutoClaude/tools/install_git_hooks.{sh,ps1}` 與 `AISDLC_SDD/scripts/install-hooks.{sh,ps1}` 四支腳本比照 `check_script_parity.py` 既有 `marker_pairs` 模式補齊機械標籤鎖（QA 自評風險較低，行為層已被 windows/macos-compat-ci.yml 的安裝/解除/worktree 拒絕實測覆蓋，仍要求補齊）** | **逐一重讀四支腳本現行內容評估**：四者皆為 37～66 行的線性單一動作流程（assert worktree → 取 hooks dir → 驗證 dispatcher hooks 存在 → `git config core.hooksPath` → 驗證安裝結果 → 印狀態），不像 `run_act.{sh,ps1}` 有六個語意獨立、各自可命名的段落；真正曾經在兩腳本間重複的判定邏輯已由 DEF-101-082（本檔上方）抽出至 `tools/git_hooks_install_common.py` 單一真相源，`install_git_hooks.{sh,ps1}`／`install-hooks.{sh,ps1}` 現僅剩呼叫該共用函式 + 各自平台原生提示文字（Windows-only 的 `-Uninstall`／Git Bash 缺失警告、兩專案不同閘門說明），對這四支腳本加 `[n/m]` 標籤會是為了湊格式而做的形式主義，不像 Fix 1（`run_act`）補標籤是填補真實的機械訊號盲區 | P3（架構建議，非缺陷） | 記事存證，暫緩；`tools/check_script_parity.py` docstring 已同步記錄本次評估結論與此決策條目編號，供未來覆核直接引用 | **closed-by-decision（本輪 R4 複審修復 agent 評估 defer）**：`tools/check_script_parity.py` docstring 新增「已評估但暫緩納入的候選」段落記錄本結論；四支腳本本輪僅讀取核實現況、未變更任何程式碼——決策結論為「維持現狀，不排入 `marker_pairs`」，與同帳本 DEF-101-070 對這四支腳本中兩支（`install_git_hooks.ps1`／`install-hooks.ps1`）的既有 reject 全面轉 Python 核心結論方向一致；`python tools/check_script_parity.py` 驗證本次未變更程式碼邏輯，既有 4 對腳本 + pytest 釘選比對維持全綠 |
| DEF-101-091 | 2026-07-16 | DEF-101-089 訂正（帳本 append-only，原列不回改）：commit 5d7fa1a 推上 main 後，主 agent 執行同一輪的 `git push` 時，pre-push hook 內部呼叫的 `python -m pytest`（同樣繼承正常 PATH，`claude` 在列）於同一 70% 位置實機重現同款死結，**證實 DEF-101-089 的影響面比原記載更嚴重**：不只是「本機開發驗證體驗缺口」，而是會**直接卡死 `git push` 本身**（任何裝有 `claude` CLI 且從巢狀 Claude Code session 執行 `git push` 的開發者，含本專案主要受眾，皆會複現，無法單純靠「跑驗證前先動 PATH」workaround 繞過——push 是使用者發動的單一指令，不像手動驗證可以事先調整環境）。原判斷「倉促加偵測邏輯風險蓋過收益」的評估基礎（僅本機驗證便利性）不再成立，改為即時動工 | **主 agent 檢查本機環境變數（`env \| grep -i claude`）確認 `CLAUDECODE=1` 是 Claude Code 執行環境啟動時官方設定的變數（連同 `CLAUDE_CODE_SESSION_ID`／`CLAUDE_CODE_ENTRYPOINT` 等一併存在），非行程樹猜測、非 heuristic——用它做巢狀 session 偵測是可靠訊號，加上此偵測不是「倉促猜測」而是有明確、穩定契約的官方訊號，原 defer 理由不再站得住腳** | P2→P1（重新評估：實際會阻斷 push，非僅體驗缺口） | 本輪即修（原 DEF-101-089 workaround 升級為程式碼層修復） | **fixed@主 agent 即時修復**：`tests/test_gap014_020.py`／`tests/test_gap039_049.py` 的 `requires_claude_cli` skipif 條件由 `shutil.which("claude") is None` 擴充為 `shutil.which("claude") is None or os.environ.get("CLAUDECODE") == "1"`，兩檔皆補 `import os`。**驗證**：不隱藏 PATH（`claude` 正常在列、`CLAUDECODE=1` 正常存在）情況下，`python -m pytest tests/test_gap014_020.py tests/test_gap039_049.py -k "requires_claude_cli or gap044 or gap048 or TestGap017SkipToRunnerExtended or TestGap016CMinimaxEvolverFallback or batch_mutations"` 原本會卡死的 8 個測試全數正確 SKIPPED、0.80 秒內完成（先前同款指令會在 70% 處無限掛起）；`python -m pytest tests/ -q` 全套不隱藏 PATH 重跑，同樣 **3557 passed, 206 skipped, 0 failed** 且無卡死（與 DEF-101-089 隱藏 PATH 版本數字完全一致，證明修復未改變任何測試的實際涵蓋範圍，只是把「巢狀死結」這一種失敗模式換成「正確 skip」）。一般 CI runner（無 `CLAUDECODE`）與非巢狀本機開發環境行為不受影響。**附帶清理**：這兩個檔案要 commit 就必須通過 `tools/git-hooks/pre-commit` 的 `ruff check "${staged_py[@]}"`（整檔 lint，非僅 diff），而 `ruff check` 對 HEAD 版本原檔逐一實測即發現**與本次改動無關的既有 lint 債（`test_gap014_020.py` 24 筆／`test_gap039_049.py` 15 筆，import 排序、未用變數、行過長）**——早於本輪即存在，只是這兩檔至今未被任何一次 staged commit 真正觸發過整檔 lint。為了讓本次修復可提交，一併以 `ruff check --fix`（安全自動修正 import 排序/未用 import）+ 人工核實逐一移除確認無副作用的死變數賦值（`call_count`／`applied_mutations`／`correction_count`／`skip_log_count`／4 處 `result = runner.run(...)`）+ 折行超長註解/字串收斂。**驗證**：`ruff check tests/test_gap014_020.py tests/test_gap039_049.py` 由 39 個錯誤降為 0；`python -m pytest tests/test_gap014_020.py tests/test_gap039_049.py -q` 全數維持通過無回歸（80 passed/11 skipped）；全套 `python -m pytest tests/ -q` 仍 3557 passed/206 skipped/0 failed |
| DEF-101-090 | 2026-07-16 | 同上（主 agent 補跑 AutoClaude 全套 pytest 首次通過但結尾噴出非測試失敗的 stderr 錯誤，追查後發現） | **`AutoClaude/tests/utils/test_notifier.py::TestDarwinOsascriptFallback::test_non_darwin_skips_osascript` 未 mock `_try_win10toast`，導致在裝有 `win10toast` 套件的 Windows 機器上該測試會真實觸發一次桌面 toast 通知**：`notifier.py::notify()` 對 `_try_win10toast()` 的呼叫本身**沒有平台守門**（第 65 行，紧接在 `if sys.platform == "darwin" and _try_osascript(...)` 之後、無 `elif`／`else` 限定），該測試把 `sys.platform` mock 成 `"linux"` 只是為了讓斷言聚焦於「不呼叫 osascript」，卻未意識到這不會阻止程式碼繼續往下走到 `_try_win10toast()` 並在真實 Windows 環境下真的執行；`show_toast(..., threaded=True)` 開啟一個背景視窗訊息迴圈執行緒，測試結束未等其收尾，Python 直譯器退出時該執行緒的 WNDPROC callback 仍被呼叫但參數已被回收，噴出 `WNDPROC return value cannot be converted to LRESULT` / `TypeError: WPARAM is simple, so must be an int object (got NoneType)`——不影響 pytest exit code（噪音出現在 pytest 摘要之後），但污染 stderr 且是真實側效應洩漏，非測試隔離本意 | P3（測試隔離缺陷，非生產邏輯錯誤；不影響 exit code） | 本輪即修 | **fixed@主 agent 親自修復**：`test_non_darwin_skips_osascript` 補上 `@patch(f"{_MOD}._try_win10toast", return_value=False)`，並將原本的 `mock_run.assert_not_called()` 保留、新增 `mock_toast.assert_called_once()`（如實反映程式碼真實會呼叫到它，而非誤植為不呼叫）。**驗證**：`python -m pytest tests/utils/test_notifier.py -v` 8 passed、無 WNDPROC 錯誤；`python -m pytest tests/ -q` 全套重跑 3557 passed/206 skipped/0 failed，且**結尾不再出現 WNDPROC/LRESULT 錯誤**（修復前後對照：兩次獨立全套執行，僅本次無此雜訊） |
| DEF-101-092 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（SD 發現） | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.sh`／`.ps1` 用 `git rev-parse --show-toplevel` 算來源路徑，在 linked worktree 下該指令回傳呼叫端 worktree 自己的根目錄（非主 checkout），卻拿它組出 `LATEST` 版本目錄搜尋起點與 `HOOK_SRC_DRIFT`／`HOOK_SRC_CLOSURE`（drift/closure hook 來源 `.py` 路徑）；`HOOK_TARGET`（寫入目的地）本身用 `--git-common-dir` 是對的**：worktree 一旦被移除，寫入共享 `.git/hooks/post-commit` 內嵌的來源路徑就會指向已不存在的 worktree，而 hook 本身用 `\|\| true` 靜默吞掉這個失敗——drift（Rule 9.17.1）與 closure（DEF-20-001）兩個 advisory 閘門會在主 checkout 上永久靜默失效、零告警。既有 CI（`windows-compat-ci.yml`/`macos-compat-ci.yml`）皆在 worktree 還存在時就讀取斷言，從未驗證「worktree 移除後」這個關鍵時刻，故未抓到 | P1 | 本輪即修 | **fixed@本輪**：兩檔皆改用 `MAIN_CHECKOUT_ROOT`／`$MainCheckoutRoot`＝`dirname(GIT_COMMON_DIR)`（`--git-common-dir` 在任何 linked worktree 下都正確指向主 checkout 的 `.git`，其父目錄即為主 checkout 根目錄）取代 `--show-toplevel` 來源，`HOOK_TARGET`／`$HookTarget` 不變。**驗證（皆為本輪實測，非推理）**：①一般 checkout 下跑 `.sh` 一次，確認寫入內容路徑正確、`core.hooksPath` 不受影響；②建 linked worktree（`git worktree add`）分別於其中執行修復後 `.sh`／`.ps1`，讀出寫入共享 `.git/hooks/post-commit` 的內容，路徑皆指向主 checkout 絕對路徑、不含 worktree 暫存路徑；③**關鍵回歸測試**：`git worktree remove --force` 移除該 worktree 後，重新讀取 hook 內容，`Test-Path`/`test -f` 確認內嵌 `.py` 路徑仍存在於磁碟——PASS；④**反向對照**：用**修復前**的原始腳本重跑同一流程（worktree 內執行→移除 worktree→讀 hook），`Test-Path` 回報 `False`（路徑指向已刪除的 `AISDCL_Agent_wt3`），證明本測試確實能抓到原始 bug、非空包彈；⑤`bash -n install_post_commit.sh` 語法檢查 PASS，`.ps1` 以 `[System.Management.Automation.Language.Parser]::ParseFile` AST 解析 PASS；⑥`python tools/check_script_parity.py`／`python tools/check_ntfs_paths.py` 皆綠燈、未觸及這兩個檔案。**CI 硬化**：`windows-compat-ci.yml` 既有「非 ASCII 路徑編碼損毀斷言」step 因本修復而失去原本靠「linked worktree 中文路徑名」重現非 ASCII 內容的能力（來源路徑不再含呼叫端 worktree 目錄名），改用**獨立本地 clone**（非 worktree）建到中文路徑下延續驗證 `-Encoding ascii`→`UTF8Encoding` 編碼修復仍有效（實測 `content -match 測試目錄`＝True、`content -match '[?]'`＝False）；另新增獨立 step「worktree 移除後仍應解析出有效路徑」；`macos-compat-ci.yml` 既有 worktree step 內追加移除後重新讀取＋`test -f` 驗證片段。兩檔 YAML 皆通過 `yaml.safe_load` 語法檢查 |
| DEF-101-093 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（SA 發現） | **`macos-compat-ci.yml` 的 `macos-nightly-full` job 仍停留在舊模式：只 `pip install -e ".[dev]"` + 裸 `python -m pytest tests/ -q --tb=short`，未涵蓋 `windows-nightly-full` 已改執行的 `local_ci_gate.ps1`（editable 哨兵/LOC 預算/CLAUDE.md 行數/snapshot 可重現/import-linter/pytest 全套）對等範圍，兩平台深度回歸網覆蓋不對稱** | P2 | 本輪即修 | **fixed@本輪**：改裝 `pip install -e ".[dev,lint]"`（import-linter 需要）+ 執行 `bash tools/local_ci_gate.sh`（取代裸 pytest，避免同 job 內重複跑全套）。**驗證（本機實測，非僅語法檢查）**：`bash -n tools/local_ci_gate.sh` 語法檢查 PASS；`python -c "import yaml; yaml.safe_load(...)"` 確認 `macos-compat-ci.yml` YAML 合法；於本機（Windows + Git Bash，已裝對應依賴）實際執行 `bash tools/local_ci_gate.sh`，全 7 個 gate 皆 PASS（editable sentinel／LOC budget／CLAUDE.md<=400／CLAUDE.md line<=800／snapshot --check／import-linter 8 kept 0 broken／pytest **3557 passed, 206 skipped**），總結印出「全部通過 — 可安全 push」 |
| DEF-101-094 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（SA 發現） | **`tools/check_defect_log_crossref.py` 的 `_CROSSREF_TARGETS` 只涵蓋 `ONBOARDING.md`/`windows-compat-ci.yml`/`macos-compat-ci.yml`，漏掉 `AutoClaude/docs/05_development/SD10_PG_Contract_NextAction.md` 這份現行文件——該檔第 3 行宣稱「DEF-101-051 帳本 = fixed」，但第 52 行「追蹤」段落仍宣稱「DEF-101-051（open，SD_10 PG-track）」，同檔自相矛盾且與帳本第 152 行 DEF-101-051 實際狀態（fixed）不一致，因不在掃描範圍內未被機械鎖抓到** | P2 | 本輪即修 | **fixed@本輪**：`_CROSSREF_TARGETS` 新增 `AutoClaude/docs/05_development/SD10_PG_Contract_NextAction.md` 這一項（僅此一檔，未擴大為全 repo 掃描）。**驗證**：①加入新目標、**修正文件前**先跑 `python tools/check_defect_log_crossref.py`，確實回報「SD10_PG_Contract_NextAction.md：DEF-101-051 宣稱狀態「open」，帳本實際狀態為「fixed」」且 exit 1，證明新目標真的有效非形式主義；②將該檔第 52 行「DEF-101-051（open，SD_10 PG-track）」訂正為「DEF-101-051（fixed，SD_10 PG-track）」；③重跑 `check_defect_log_crossref.py` 轉綠燈（「帳本 167 筆有效狀態紀錄、4 份掃描目標皆無矛盾」，exit 0）；④`python -m pytest tools/tests/test_check_defect_log_crossref.py -q` 全數通過（22 passed），既有測試以 `mock.patch.object(m, "_CROSSREF_TARGETS", ...)` 隔離，未因新增目標而壞掉 |
| DEF-101-095 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（SD 發現，第三輪繞過；前兩輪見 DEF-101-083） | **`tools/check_wrapper_thinness.py` 黑名單第三批繞過寫法（皆原始碼字面比對先天限制）**：bash `for((i=0;i<3;i++)); do…done`（C-style for 無空格）繞過 `"for "`；PowerShell `foreach($x in $y){…}`（無空格）繞過 `"foreach ("`；PowerShell `[System.Text.Json.JsonSerializer]::Deserialize(...)`（.NET JSON 反序列化）繞過 `"ConvertFrom-Json"`/`"ConvertTo-Json"`；PowerShell `(1,2,3).ForEach({...})`（陣列 `.ForEach()` 方法，非 `ForEach-Object` cmdlet）繞過 `"ForEach-Object"` | P2 | 本輪即修 | **fixed@本輪**：`_FORBIDDEN` 新增 4 個關鍵字（`"for("`／`"foreach("`／`"[System.Text.Json"`／`".ForEach("`），與既有帶空格版本並收。**驗證（逐一重現繞過情境再驗證修復，測完精確 Edit 撤銷）**：於 `tools/dev_start.sh`／`tools/dev_start.ps1` 末尾暫時各自附加對應繞過寫法，`python tools/check_wrapper_thinness.py` 4 種寫法皆被正確攔截（分別報 `'for('`／`'foreach('`／`'[System.Text.Json'`／`'.ForEach('`）；`git diff --stat tools/dev_start.sh tools/dev_start.ps1` 確認撤銷後零殘留、`check_wrapper_thinness.py` 轉回全綠。新增 4 個回歸測試至 `tools/tests/test_check_wrapper_thinness.py`（`test_forbidden_c_style_for_no_space_in_sh_detected`／`test_forbidden_foreach_no_space_in_ps1_detected`／`test_forbidden_system_text_json_in_ps1_detected`／`test_forbidden_array_foreach_method_in_ps1_detected`），`python -m pytest tools/tests/test_check_wrapper_thinness.py -v` **14 passed**；`python -m pytest tools/tests -q` 全套 **156 passed, 9 skipped**，無回歸 |
| DEF-101-096 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（QA 發現） | **`windows-nightly-full`/`macos-nightly-full` 兩 job 皆設 `continue-on-error: true`，此設定只影響「整個 workflow run 對外呈現的結論」，不影響同 job 內 step 間隱式 `if: success()` 串行**：若某個較早的 step 失敗（如安裝依賴），後續深度回歸 step（`local_ci_gate.ps1`/`.sh`、AISDLC_SDD fsm_runtime pytest）當晚會被靜默 skip，但 job 結論只顯示「失敗」而非「跳過大半」，`nightly-alert` 開的 issue 也看不出「深度回歸其實只跑了一步就死」，掩蓋覆蓋率被腰斬的事實 | P2 | 本輪即修 | **fixed@本輪**：`windows-nightly-full` 的 4 個非基礎設施 step（PS5.1 驗證 bootstrap/dev_start、安裝 lint 依賴、`local_ci_gate.ps1`、AISDLC_SDD fsm_runtime pytest）與 `macos-nightly-full` 的 2 個非基礎設施 step（`local_ci_gate.sh`、AISDLC_SDD fsm_runtime pytest）皆加 `if: ${{ !cancelled() }}`（非 `always()`——`always()` 連使用者手動取消都會硬跑，`!cancelled()` 才是「前面失敗仍照跑、使用者主動取消時不硬跑」的正確語意）；`checkout`/`Set up Python` 兩基礎設施 step 不動。**驗證**：`python -c "import yaml; yaml.safe_load(...)"` 確認兩份 YAML 語法合法；另用 PyYAML 解析後逐一列印兩 job 每個 step 的 `name`/`if` 欄位，實測確認：`checkout`/`Set up Python 3.11` 兩步 `if=None`（維持預設隱式 `success()`），其餘全部 4（windows）/2（macos）步皆正確帶上 `if= ${{ !cancelled() }}` |
| DEF-101-097 | 2026-07-16 | 2026-07-16 Mac/Windows 11 相容性 R8 四方獨立複審（Architect P3 建議，非缺陷）評估後決定不做 | **`windows-compat-ci.yml` 用「workflow 層級單一 `concurrency` + `github.event_name` 併入 group key」（`cancel-in-progress: true`，同時套用於 smoke/nightly-full/nightly-alert 三個 job）；`macos-compat-ci.yml` 改用「job 層級各自 `concurrency`」（smoke 用 per-ref+cancel:true；nightly-full/nightly-alert 用固定字串 group+cancel:false）**。實際比對後發現這不只是「機制風格不同」：兩者對「schedule/workflow_dispatch 觸發的 nightly 深度回歸若彼此重疊」的**行為本身不同**——windows（workflow 層級 cancel-in-progress:true）會讓較新的重疊 nightly run **直接取消**較舊的一整個 run（含其下游 nightly-alert）；macos（job 層級 cancel-in-progress:false）則是**排隊序列化**，不取消舊 run。這是實質政策差異，非單純寫法統一可對齊 | P3（非缺陷，架構一致性建議） | 評估後決定不做（本輪範圍外） | **closed-by-decision（本輪評估，未改動任何檔案）**：對齊兩者寫法需先決定「重疊 nightly run 該取消還是排隊」這個政策問題本身——不是無風險的機械式統一，而是需要人工拍板的行為變更，超出「把其中一份寫法對齊到另一份、不改變任何行為」的可安全執行範圍（prompt 明文允許在此情況下跳過，Rule 2 簡單優先 > 為求徹底而勉強做有風險改動）。兩份 workflow 現況皆能達成「push 不誤殺 nightly」的核心目的，未發現會出錯的具體場景，故維持現狀，留待未來若真有 nightly 重疊撞期的實際需求時再一併決策 |
| DEF-101-098 | 2026-07-16 | R8 四方二審（Architect 發現）：DEF-101-092（P1 install_post_commit 路徑修復）遺漏連帶更新既有官方回歸測試，實跑 FAIL | **`AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_install_post_commit_worktree.py::test_install_writes_hook_to_shared_git_dir_from_worktree` 在 DEF-101-092 修復後的 commit（`6d8e52b`）上實跑會真實 FAIL**：該測試斷言 hook 內容應含 worktree 自身的非 ASCII 目錄名「測試目錄」——這正是**修復前**的舊（有 bug）行為（`--show-toplevel` 解回 worktree 自身根目錄，來源路徑因而內嵌 worktree 名稱）；DEF-101-092 的 P1 修復刻意把來源路徑改回解析「主 checkout」根目錄，修復後 hook 內容正確地**不再**含 worktree 名稱，導致這份舊測試斷言必然失敗。`windows-compat-ci.yml` 對 `.ps1` 版本的 CI 內嵌驗證已同步更新（改用獨立 clone 到中文路徑），但 `.sh` 側這份官方 `ci-gate.sh` 會自動收集執行的 pytest 回歸鎖被漏掉——commit message 提及的「獨立重跑 tools/tests」指 monorepo 根層 `tools/tests/`，不含此檔案，故未被本輪原驗證流程覆蓋 | P1（阻斷性；若不修復，下次跑 `ci-gate.sh`/`aisdlc-sdd-ci.yml` 的 `tools/fsm_runtime/tests/` 會紅燈） | 本輪即修 | **fixed@主 agent 即時修復**：`_make_fake_monorepo()` 新增 `repo_name` 參數，讓呼叫端可指定主 checkout 建在非 ASCII 路徑；`test_install_writes_hook_to_shared_git_dir_from_worktree` 改為把主 checkout 本身建在「主檢出目錄」（非 ASCII），worktree 改用 ASCII 名「worktree_checkout」，延續驗證編碼正確性（斷言 hook 內容含「主檢出目錄」且無 U+FFFD/`?` 替代字元），並新增一條斷言鎖死修復意圖本身：hook 內容**不應**含 worktree 自己的路徑片段（`"worktree_checkout" not in text`）——若復發即代表來源路徑解析退回 `--show-toplevel`。**驗證**：`python -m pytest AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_install_post_commit_worktree.py -v` 修復前 1 failed/2 passed（本行開頭即為此次實跑輸出）、修復後 **3 passed**；`cd AISDLC_SDD/AISDLC_SDD_v0.30 && python -m pytest tools/fsm_runtime/tests/ -m "not chaos" -q` 全套 **1666 passed, 8 skipped, 34 deselected, 14 subtests passed**，無其他回歸 |
| DEF-101-099 | 2026-07-16 | R9 Mac/Windows 11 相容性四方獨立複審（Architect 發現，主 agent 親核：本機 `(Get-Command bash).Source`＝`C:\WINDOWS\system32\bash.exe` 即活案例） | **`tools/integration_gate.ps1:76` [2/5] 裸呼叫 `bash scripts/ci-gate.sh`，繞過 repo 自建的 `tools/lib/Find-GitBash.ps1` 防禦**：Windows 11＋WSL 機器上 PowerShell 把 `bash` 解析到 System32 的 WSL bash → [2/5] 在 WSL Linux 環境跑 ci-gate.sh（看不到 Windows venv）必假紅；標準 Git for Windows 無 WSL 機器則 `$ErrorActionPreference=Stop` 下 CommandNotFoundException 直接中斷整支（[3/5] 以後全不跑）。CI 蓋不到：windows-smoke 只跑 `-SkipFull`（不含 [2/5]），且 GH runner 的 PATH bash 本來就是 Git Bash | P2 | 本輪即修 | **fixed@R9**：dot-source `Find-GitBash.ps1`、[2/5] 改 `& $bashExe`；找不到 Git Bash → 印安裝提示、`$global:LASTEXITCODE=1` 後 `return`（記 FAIL 續跑不中斷）。驗證：Parser AST 0 錯誤；`Find-GitBash` 實回 `C:\Program Files\Git\bin\bash.exe`（非 System32）；隔離 harness 兩情境（真 bash exit 0→段 PASS；模擬 $null→段 FAIL 進 failures、後續段照跑、終端 exit 1） |
| DEF-101-100 | 2026-07-16 | R9 四方複審（Architect 發現，主 agent 親核：`grep -c FLEET_FSM` .sh=14 vs .ps1=0、152 vs 99 行） | **LATEST（v0.30）`run_tlc.ps1` 與 `run_tlc.sh` 已實質漂移：.ps1 缺整條 FLEET_FSM 驗證軌（5a safety＋5b liveness）且缺 LEGACY 兩軌警語**——Windows 使用者跑 .ps1 全綠會誤信形式化驗證通過，同一變更在 macOS 跑 .sh 會在 5a/5b 抓到 FLEET 違規。緩解：五軌權威路徑 `python -m tools.fsm_runtime.tlc_runner`（ci-gate.sh --full-tlc）不受影響 | P2 | 本輪即修 | **fixed@R9**：檔頭補 LEGACY 警語（同義鏡射 .sh:4-7）＋補 Step 5a/5b 兩段 FLEET_FSM（config/spec/`-workers auto`/log/判定訊息逐一對照 .sh，轉寫為 .ps1 既有 Tee-Object＋$LASTEXITCODE 慣例）。驗證：AST 0 錯誤；新增區塊逐字抽出以 PS5.1 driver 實跑，三段 TLC 真跑全綠（SDD_FSM 855 distinct states／FLEET safety 7／FLEET liveness 12，皆 No error）。已知既有限制（非本輪引入，原版重現）：檔頭 `java -version 2>&1` 在 PS5.1 有 stderr 包裝限制，檔案宣告引擎本為 pwsh |
| DEF-101-101 | 2026-07-16 | R9 四方複審（Architect 發現，主 agent 親核：windows-compat-ci 無任何 `git commit` 觸發 step、macos 版 :249-259 有） | **windows-smoke 缺「安裝 hooks → 真實 `git commit` 經 core.hooksPath 觸發 dispatcher」對等步驟**：dispatcher 三支 hook 在 Windows 由 msys bash 執行（與 macOS /bin/bash 3.2 不同的第三種 runtime），pre-commit 的 NTFS 檔名閘正是為保護 Windows 而生，卻只在 macOS 被真跑過 | P2 | 本輪即修 | **fixed@R9**：windows-compat-ci.yml 新增 `shell: bash` step（拋棄式 CI checkout 內安裝→probe commit→grep `[pre-commit dispatcher]` marker 斷言 dispatcher 真的經 git 執行→負路徑改「壞語法 tools/*.sh 被 bash -n 閘擋下」〔macos 版 CON.txt 負路徑在 NTFS 建不出檔案不可移植〕→revert＋uninstall 還原）。驗證：YAML safe_load＋結構斷言；step script 逐字抽出於本機 Git Bash（msys＝目標環境）fake clone 實跑 STEP_EXIT=0，真 repo core.hooksPath 未被污染 |
| DEF-101-102 | 2026-07-16 | R9 四方複審（Architect 發現） | **windows-compat-ci.yml 兩份 paths 白名單漏 `AISDLC_SDD/AISDLC_SDD_v0.01/requirements-ci.txt`**（macos 版 :79/:106 有；windows-smoke 實際安裝該檔跑雙軌 ci-gate）——只改鎖版檔時 windows 側靜默不觸發 | P3 | 本輪即修 | **fixed@R9**：push/pull_request 兩份 paths 各補一行。驗證：YAML safe_load＋python 斷言兩處 `in paths` 皆 True |
| DEF-101-103 | 2026-07-16 | R9 四方複審（Architect 發現） | **windows-smoke 只跑一次 bootstrap.ps1（全新建立），`tools/bootstrap.ps1:77-86`「既有 .venv 沿用＋形狀檢查」分支零 CI 覆蓋**（macos 版跑兩次：全新＋沿用） | P3 | 本輪即修 | **fixed@R9**：新增「重跑 bootstrap.ps1（沿用情境）」step（pwsh；`*>&1` 合流捕捉、斷言 rc=0＋python.exe 存在＋輸出含「偵測到既有 .venv→沿用」）。驗證：YAML＋scratchpad fake root 以 PS5.1 文件教學呼叫式實跑，沿用分支訊息實出、斷言式對真實輸出套用＝True（完整快樂路徑待 CI 實跑——bootstrap 無隔離參數，依鐵律不對真 repo 跑） |
| DEF-101-104 | 2026-07-16 | R9 四方複審（Architect 發現，主 agent 親核：macos-compat-ci 全檔 grep 無 zsh 字樣） | **`tools/dev_start.sh` 的 zsh 專屬邏輯（`ZSH_EVAL_CONTEXT` sourced 偵測＋`${(%):-%x}` 路徑解析）零機械驗證**：檔頭推薦 `source`、macOS 預設互動 shell 即 zsh，但 macos-smoke 全用 bash 跑——與 R5 已修的「PS5.1 vs pwsh 引擎不一致」同構的 mac 側缺口 | P3 | 本輪即修 | **fixed@R9**：macos-smoke 新增 `zsh -c 'source tools/dev_start.sh --no-sync; ...'` step（斷言 rc=0＋VIRTUAL_ENV 非空；dev_start.py 無 --check-only，`--no-sync` 為既有最小副作用旗標）。驗證：YAML＋結構斷言；**本機（Windows）無 zsh，zsh 實際行為待 CI 恢復首跑（誠實揭露）** |
| DEF-101-105 | 2026-07-16 | R9 四方複審（Architect 發現，主 agent 親核 4 處；批次 3 自查另抓到 2 處漏網） | **守門工具權威描述落後實況（doc drift）**：ONBOARDING:147「其餘三對…run_act 暫無機械比對」（實際 run_act 已入 4 對機械鎖）、:163「八道」（實際十道）；root-infra-ci.yml:4「八道」、:24「三對…釘選雙處」（實際四對/三處）、:179 step name「三對」；Nightly 紀律 §1「下列 13 條」（實際 19 條）。crossref 只守 DEF 狀態宣稱，抓不到這類數字漂移 | P3 | 本輪即修 | **fixed@R9（批次 3）**：六處全數訂正（含自查抓到的 root-infra-ci.yml:4 與 Nightly §1 兩處漏網）。驗證：`grep -rn "三對\|八道\|16 條\|707 行"` 對五份文件零殘留（rc=1）；root-infra-ci.yml YAML safe_load OK、僅動註解/step name 字串 |
| DEF-101-106 | 2026-07-16 | R9 四方複審（Architect 發現） | **`check_script_parity._extract_gate_calls` 只認單引號**（`{call}\s+'...'`）：成對腳本兩側同步把某 gate 呼叫改雙引號 → 該 gate 從兩側序列同時消失、序列仍相等 → 綠燈但守護面靜默縮小（「空清單」防呆只在全部抽不到時觸發） | P3 | 本輪即修 | **fixed@R9**：regex 改 `(?:'([^']+)'\|"([^"]+)")` 合併 group；新增 `_MIN_EXTRACT_COUNTS`（bootstrap=3/integration_gate=5/run_act=6/local_ci_gate=9）下限釘選＋紅燈訊息指路更新釘選。驗證：工具實跑仍 4 對全綠；新建 `tools/tests/test_check_script_parity.py` 5 測試 passed（雙引號抽取/混合保序/低於下限紅燈/達標通過/訊息指路） |
| DEF-101-107 | 2026-07-16 | R9 四方複審（SA 與 SD 獨立發現同款，主 agent 親核：`git log --oneline --all -- "AutoClaude/tools/install_hooks/"` 零輸出） | **ONBOARDING.md:238 與本帳本 DEF-101-056 列引用「全 repo 歷史從未存在」的路徑 `AutoClaude/tools/install_hooks/install_post_commit.ps1`**（子專案張冠李戴；正確為 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/install_hooks/install_post_commit.ps1`）——存活 R4~R8 五輪未被抓到（crossref 只比對狀態宣稱、不驗路徑存在性，天然盲區） | P3 | 本輪即修 | **fixed@R9**：ONBOARDING:238 改為動態 LATEST 寫法＋R9 訂正註記；DEF-101-056 原列比照 DEF-101-086 先例就地訂正路徑（並註記訂正出處＝本列）。驗證：`ls AutoClaude/tools/install_hooks` No such file、grep 兩檔僅剩刻意保留的歷史對照引用 |
| DEF-101-108 | 2026-07-16 | R9 四方複審（SA 發現，主 agent 親核：`grep -c '^### 紀律'`=19、wc -l=762〔R9 改後 817〕） | **`Nightly_Forensic_Discipline.md` 三處現況漂移**：標題與 §2 稱「16 條」（實 19 條，#17~#19 新增後未同步）；header 表「707 行」（實測 762，R9 改後 817）；AutoClaude/CLAUDE.md 摘要引「完整版 v1.2」（實 v1.5）。讀者以標題為準會漏讀 #17~#19（含跨平台關鍵的 #18 mutation 隔離樹/#19 import 路徑一致性） | P3 | 本輪即修 | **fixed@R9（批次 3）**：標題/§1/§2 計數 16（13）→19；header 表行數快照**移除**（防再漂移，行號註記列本就聲明以錨點關鍵字為準）；補 R9 載具三變更註記；元數據 v1.5→v1.6；CLAUDE.md 摘要連結 v1.2→v1.6（19 條）。驗證：grep 零殘留、AutoClaude/CLAUDE.md 仍 400 行、長行契約測試 4 passed |
| DEF-101-109 | 2026-07-16 | R9 四方複審（SA 與 QA 獨立發現同款） | **pytest 全套基線文件（根 CLAUDE.md:79／AutoClaude CLAUDE.md:3,325／ONBOARDING:180）皆未註記 CLAUDECODE 巢狀變因**：DEF-101-089/091 修復使 `CLAUDECODE=1`（本專案主要開發環境）下為 3,557/206，與文件基線 3,566/196 差 −9/＋10——skipped 差已超出 ONBOARDING 自宣「±個位數」，開發者會誤判退化去追鬼 | P3 | 本輪即修 | **fixed@R9（批次 3）**：三份文件補巢狀變因註記（基線數字本身不改，仍對「非巢狀＋乾淨 bootstrap」成立）。AutoClaude/CLAUDE.md:3 行長 777/800 餘裕僅 23 字元，註記落於文末元數據 v7.9（行內本有「同文末元數據」既有指引），並同步「最後更新」日期。驗證：AutoClaude 長行契約測試 4 passed、wc -l 400/222 皆守住預算 |
| DEF-101-110 | 2026-07-16 | R9 四方複審（SA 發現，主 agent 以工具本尊函式親核重現：unclassifiable=['DEF-87-001','DEF-101-077','DEF-101-089']） | **crossref `_load_ledger_status()` 對 3 筆帳本列回 None 卻被計入「174 筆有效狀態紀錄」**：DEF-87-001 狀態欄含未跳脫 `\|` 切壞表格欄位；DEF-101-077「no_action_needed」與 DEF-101-089「workaround-applied」非帳本格式定義的合法狀態詞彙——機械鎖對這 3 筆退化 | P3 | 本輪即修 | **fixed@R9（批次 1）**：①DEF-87-001 列 code span 內 `\|` 跳脫（僅一字元、先 grep 確認全檔唯一）；②`_classify` 增詞彙：`workaround`→open、`no[_ ]action[_ ]needed`→closed-by-decision；③main() 分離「有效」與「含糊」計數（含糊>0 印 warning 列 ID、不 fail）。驗證：直接探測三筆→fixed/closed-by-decision/open、vague=0；089 轉 open 未使任何掃描目標轉紅；新增 3 回歸測試、全檔 pytest 通過 |
| DEF-101-111 | 2026-07-16 | R9 四方複審（SA 發現，主 agent 親核：v0.01=140、v0.30=161） | **根 CLAUDE.md:123 硬編「~140 py 檔」**：數字錨定凍結基線 v0.01，LATEST v0.30 已 161 檔（+15%）；同檔上一段明文「資產計數一律見 FRAMEWORK_STATUS.md、本檔不重複數字」——自打政策且已實際漂移 | P3 | 本輪即修 | **fixed@R9（批次 3）**：移除數字，改「（FSM 引擎）」。驗證：grep `~140 py` 零殘留 |
| DEF-101-112 | 2026-07-16 | R9 四方複審（QA 發現，主 agent 親核 dispatcher 旗標與 fail-safe 分支） | **根層 `tools/git-hooks/pre-push` 只有 run_autoclaude/run_sdd 兩旗標——成功判定「純根層變更」的 push 一個閘門都不跑**；root-infra-ci 的 10 道守門有 7 道本地零接線（pwsh parse+BOM／EOL-.ps1／parity／py_compile／tools/tests unittest／crossref／wrapper-thinness）。DEF-101-081 停擺期間＝改壞根層工具鏈可零機械訊號直推 main | P1 | 本輪即修 | **fixed@R9（批次 1）**：新增 `run_rootinfra` 旗標（逐行 case 偵測「非兩子專案」路徑，bash 3.2 相容、內層 heredoc 不干擾外層 stdin 迴圈）＋root-infra leg（py_compile 全 tools/*.py→unittest discover tools/tests→四守門逐支累積；python 缺席 fail-loud）；fail-safe 分支三旗標全設。驗證：bash -n；fake monorepo 三情境（純根層→僅 root-infra 觸發／僅 AutoClaude→不觸發／stdin 空→三 leg 全跑）＋兩負面（守門 stub 失敗照跑完 rc=1／PATH 剝除 python rc=1）；真實 repo 純根層 commit 對 `ffd5977..6612301` 餵 stdin 首跑全綠（unittest 175 OK(skipped=9)，同輪後續再增 2 編碼回歸測試至 177＋四守門全 ✅）。**殘留（QA 二審精確化，如實揭露非宣稱全解）**：root-infra-ci 10 道中 6 道（NTFS/parity/py_compile/unittest/crossref/wrapper）已本地接線；其餘——pwsh parse+BOM（僅 CC hook 局部護）、EOL .ps1（.gitattributes 機制仍有效，缺政策回歸偵測）、EOL .sh（純根層 commit 不觸發 AutoClaude 子 hook 的 staged 檢查）、bash -n（pre-commit A4 有、push 時無全量）四道仍無完整本地對等，風險屬低、留待後續評估 |
| DEF-101-113 | 2026-07-16 | R9 四方複審（QA 發現；終裁 REJECT 的兩 P1 之二） | **macOS 側機械驗證在停擺期間為真空**：ONBOARDING 明載 macOS 本機排程未實作、以 macos-compat-ci.yml 為補償控制——該補償控制即 DEF-101-081 停擺主體；bash 3.2 語法回歸、.sh 安裝腳本 worktree 防護、cross_same_flavor、9 條 POSIX-only 測試（Windows 上永 skip，本輪實測 OK(skipped=9)）目前任何機器都不自動跑。失敗場景：dispatcher 加一行 bash 4+ 語法 → 本地全綠上 main → Mac 使用者 commit 當場炸 | P1 | 本輪落地補償控制（真 macOS 實跑受硬體限制，殘留誠實揭露） | **fixed@R9（批次 2，補償控制落地＋殘留揭露）**：新增 `tools/macos_smoke_local.sh`（〔R10 SA-1/DEF-101-141：移除行數快照防漂移——曾兩度誤植（231→311，落地當下實為 314 且隨修補演進），比照 DEF-101-108 手法〕：bash -n 全量／dispatcher 直呼煙霧／兩支安裝腳本往返+worktree 拒絕／LATEST install_post_commit worktree+移除後斷言〔含 DEF-101-092 回歸鎖〕／NTFS+parity 守門；全程 OS temp fake clone、含未 commit 變更 clone 盲區警示），檔頭載明與 macos-compat-ci.yml 同步維護；ONBOARDING §8 補揭露。驗證：本機 Git Bash 實跑 **PASS=10 FAIL=0** 兩次（批次 2 fixer＋主 agent 各自獨立）；bash 3.2／BSD 相容靜態自查表（無 declare -A/mapfile/`${var,,}`、無 sed -i/readlink -f/grep -P/BRE 交替、mktemp 帶模板）。**殘留已於 R11 關閉**：2026-07-17 真 Mac（macOS 26.5.2 arm64、`/bin/bash` 3.2.57）實跑 **PASS=10 FAIL=0**（本輪多次獨立實跑：主 agent 首驗＋Fixer B 修改 install_post_commit 後端到端重驗＋隔離 clone 驗證）；同輪並實證「smoke 全綠 ≠ 根層 unittest 全綠」（DEF-101-149 兩失敗即 smoke 綠燈下抓到）——平台全綠證據自此須含該平台 `run_root_unittests.py` 輸出，不得只附 smoke 彙總 |
| DEF-101-114 | 2026-07-16 | R9 四方複審（QA 發現，主 agent 親核：contract 檔僅 :26 DSN skipif、無 pg_real marker） | **pg-contract 為 CI 硬閘（DEF-101-051 教訓後轉阻塞）但本地驗證網預設完全不跑**：local_ci_gate 僅 `-Pg` opt-in；pre-push 全套 pytest 中 contract 測試因無 DSN 全 skip；nightly pg-e2e 只跑 pgvector recall 單檔——停擺期間破壞 PG contract 可通過全部預設本地閘門 | P2 | 本輪即修 | **fixed@R9（批次 1）**：nightly pg-e2e stage 追加 `tests/contract/test_pg_state_repository_contract.py`（該 stage 內 DSN 已設）。**刻意獨立 pytest 呼叫**（主 agent 複核認可）：①該檔無 pg_real marker，併入 `-m pg_real` 會整檔 deselect＝假接線；②不寫 .ac4_junit.xml，防 contract 結果污染 ac4_nightly_collector 的 AC4 觀察期取證。rc 以 [ref] 捕捉（D-10 模式）、stage 尾端非零回寫 stage rc（紀律 #1 真實 rc） |
| DEF-101-115 | 2026-07-16 | R9 四方複審（QA 發現，主 agent 親核：run_local_nightly 五 stage 無全套 pytest、07-16 實跑 2m49s） | **windows-nightly-full 的「每日全套深度回歸」職能無本地排程對等物**：本地全套驗證只剩 push 時 pre-push 單一觸發點，連續數日無 push（或一次 --no-verify）期間環境漂移型退化零機械偵測；停擺期間亦無 CI nightly 兜底 | P2 | 本輪即修 | **fixed@R9（批次 1）**：nightly 前置新增 Stage「local-ci-gate full (對齊 windows-nightly-full)」（置於 Stage 0 之前——PG env 於 Stage 0 後才設，此位置等同開發者手動跑、不受 nightly PG env 污染；經 Invoke-Stage continue-on-error＋rc 捕捉；summary 行/JSON 附加 local_ci_gate 欄位、既有五欄不動）。驗證：AST 0 錯誤；呼叫形式與檔內 .NOTES 慣例一致；實際首跑待今晚 schtasks（明晨查 nightly_latest.log） |
| DEF-101-116 | 2026-07-16 | R9 四方複審（QA 發現，主 agent 親核：檔尾無 exit、schtasks Last Result 實查恆 0） | **run_local_nightly.ps1 終端 exit code 恆 0**：stage 失敗只進 log/summary，schtasks「上次結果」永遠 0x0——CI 端同款 continue-on-error 設計有 nightly-alert job 配對，本地把設計抄了一半；停擺期間 alert 通道也死，nightly 失敗偵測 100% 靠人工讀 log | P2 | 本輪即修 | **fixed@R9（批次 1）**：結尾依各 stage rc 計算終端 exit（失敗→exit 1；SKIP=-1 與 WARN=2 不計——rc=2 排除依據檔內既有 P0-6 契約「WARN 不算 fail」，主 agent 親核 :187-193 屬實，否則 perf 觀察期每晚假紅）；決策先 `Log "END exit decision"` 取證、exit 置於 latest pointer 更新之後（不破壞既有「失敗不中斷後續 stage」設計）。驗證：harness 五組 rc（全 0→0／含 SKIP→0／含 WARN→0／含 1→1／1+99→1 列兩者）；Nightly 紀律 v1.6 同步「Last Result 不可再假設恆 0x0」 |
| DEF-101-117 | 2026-07-16 | R9 四方複審（QA 發現） | **AutoClaude pre-push lint-imports 未安裝軟跳過並宣稱「CI 仍會檢查」**：同一驗證項在姊妹閘門 local_ci_gate.sh 判 FAIL、hook 卻軟跳過；停擺期間「CI 兜底」宣稱失真＝fail-open | P3 | 本輪即修 | **fixed@R9（批次 1）**：未安裝分支改 `fail=1`（訊息保留 `uv pip install -e .[lint]` 指引），對齊 gate_importlinter 語意。驗證：bash -n；lint-imports 實跑 8 kept 0 broken（正常路徑不變） |
| DEF-101-118 | 2026-07-16 | R9 四方複審（QA 發現） | **`tools/tests/test_git_hooks_install_common.py` cwd 無關性回歸鎖含恆真斷言**：以完全相同輸入、相同 cwd 連呼兩次 `assertEqual(result_a, result_b)` 恆真，並不能如註解所稱「模擬 cwd 不同」；實際防護靠 assertTrue 的副作用路徑，未來若有人精簡掉 assertTrue，鎖會無聲失效 | P3 | 本輪即修 | **fixed@R9（批次 1）**：改為真切 cwd（第一次 `os.chdir(repo_root)`、第二次 chdir 無關 temp 目錄，try/finally 還原），保留 assertTrue，註解改述真實機制。退化情境：實作若退回依 cwd 解析，兩次結果不同 → assertEqual 轉紅。驗證：單檔 15 tests OK |
| DEF-101-119 | 2026-07-16 | R9 四方複審（SD 發現並實測重現：`env -u PYTHONUTF8` 下 `UnicodeDecodeError: 'cp950' codec can't decode byte 0x96`） | **`tools/git_hooks_install_common.py`／`tools/check_hooks_liveness.py` 的 `_run()` `text=True` 無 encoding**——DEF-101-068(c)/S11 新建的 SSOT 自身漏帶 encoding（與自家慣例 closure_evidence.py 等不一致）：zh-TW Windows（locale cp950）＋repo 路徑含非 ASCII＋一般終端（無 Claude Code session 注入的 PYTHONUTF8）下，git 輸出 UTF-8 解碼炸裂 → get-hooks-dir 假報「不在 git repo 內」rc=1、**四支安裝腳本全數無法安裝**；liveness 偵測靜默 no-op（真沒裝也不警告）。安裝鏈路 6 支 wrapper/lib 無任何一層 PYTHONUTF8 緩解（grep 零命中） | P2 | 本輪即修 | **fixed@R9（批次 1）**：兩檔 `_run()` 補 `encoding="utf-8", errors="replace"`。驗證：修復**前**於 OS temp 中文名 fake repo 以 `env -u PYTHONUTF8 -u PYTHONIOENCODING` 重現炸裂（先證能重現再修）；修復**後**同環境正確輸出中文路徑 rc=0、check-installed OK=1、liveness 正反向皆正常；新增 `TestCliNonAsciiRepoPathEncoding` 回歸測試（含「以 repo 子目錄為 cwd」尖銳案例）；unittest 15 OK、ruff 綠 |
| DEF-101-120 | 2026-07-16 | R9 四方複審（SD 發現，靜態論證＋主 agent 親核原文；macOS `re_format(7)` 官方文件級行為差異） | **`AutoClaude/tools/run_act.sh:40` 用 BRE `\|` 交替**——GNU grep 擴充，macOS BSD grep 中 `\|` 為字面字元 → pattern 變字面字串永不命中 → macOS 上 `gh extension install nektos/gh-act` 的 fallback 偵測整條死（exit 127）；`run_act.ps1:59` 用 .NET regex 交替正常＝雙平台 parity 在此點已斷。macos-compat-ci 不跑 run_act.sh（需 Docker），零 CI 覆蓋 | P3 | 本輪即修 | **fixed@R9（批次 1）**：收斂為 `grep -q 'gh-act'`（`nektos/gh-act` 本含子字串，交替冗餘）＋註解記載 BSD 不支援 BRE `\|`。驗證：bash -n；herestring 正反實測（命中 rc=0／無關輸入 rc=1）；check_script_parity 仍綠（run_act 6 標籤不受影響） |
| DEF-101-121 | 2026-07-16 | R9 四方複審（SD 發現，AST 掃描盤點；生產碼 `autoclaude/` 已全數乾淨） | **測試層/開發工具層殘存 `text=True` 無 encoding 共 12 站點**（根層 tools/tests 三檔、AISDLC_SDD/scripts/tests 三檔、AutoClaude/tools 三支工具）——與 DEF-101-119 同根因家族；官方閘門路徑已有 PYTHONUTF8 緩解（ci-gate.sh:26 等），但裸跑（非 ASCII %TEMP%/使用者名）會假紅或 crash | P3 | 本輪即修 | **fixed@R9（批次 1）**：12 站點批次補 `encoding="utf-8", errors="replace"`（以實際 grep 盤點為準；test_pre_commit_dispatcher_sigpipe.py:30 既已有 encoding 未動）。驗證：9 檔 py_compile OK；根層三測試檔 105 passed/9 skipped；AISDLC_SDD 三檔 28 passed；AutoClaude 三工具 py_compile＋ruff（既有 17 筆 ruff findings 全非本次改動行、ruff 非 AutoClaude 閘門，依 Rule 3 未動、如實揭露） |
| DEF-101-122 | 2026-07-16 | R9 四方複審（SD 發現，範圍外附帶；主 agent 親核 :1946/:2012 原文） | **v0.30 `scenarios/testing/SOP.md` 教學 GNU-only 指令**：`xargs -r`（BSD xargs 無 -r）、`grep -oP`（BSD grep 無 -P）——框架宣稱支援 Mac 本機開發，使用者照抄即失敗（v0.01 同位置屬凍結版豁免） | P3 | 本輪即修（LATEST；凍結版依紀律不回改） | **fixed@R9（批次 2）**：`xargs -r docker rm -v` → `while read -r id; do docker rm -v "$id"; done`；`grep -oP 'postgres:\K...'` → `sed -n 's/.../\1/p' \| head -1`（POSIX BRE）＋跨平台註解。驗證：樣本輸入實測新舊寫法輸出等價（`postgres:15.4`→`15.4`、`"postgres:15-alpine"`→`15-alpine`）；殘留 `grep -oP` 命中僅說明性註解 |
| DEF-101-123 | 2026-07-16 | R9 主 agent 收尾發現（寫入本輪帳本前量測：主檔 272,384 bytes） | **缺陷帳本主檔已默默超過 DEF-99-001 自訂輪替界線（<256KB Read 上限）且政策零機械守門**——「逼近即開新 archive」規則自 improving_99 訂立以來從未被任何工具強制，超線靠人眼發現 | P3 | 本輪即修 | **fixed@R9（主 agent）**：①依政策執行輪替——缺陷總表中 DEF-01～DEF-100 系列**已結列（fixed/closed-by-decision）64 列**逐字搬遷 `AutoSDD_Defect_Log_archive_03.md`（109KB）；未結 11 列（open/routed/wontfix）與被 crossref 目標引用的 DEF-20-001 留主檔（272KB→164KB）；歸檔指標段同步。②`check_defect_log_crossref.py` 加機械守門：≥240KB warning、≥256KB fail（訊息指路輪替程序）＋2 回歸測試。驗證：crossref 實跑綠（輪替當下 110 筆有效、加入 R9 25 列後 135 筆；4 目標無矛盾——被移列皆未被任何目標引用）。追記：Architect 二審 OBS-3 → 守門擴充為主檔＋每一 archive 檔皆量測（同 warn/fail 閾值），32 tests passed；`test_check_defect_log_crossref.py` 27 passed |
| DEF-101-124 | 2026-07-16 | R9 四方二審（SD 發現 D-1，兩個獨立最小重現實證；批次 2 修復時已自揭同款限制、Architect 二審亦註記） | **v0.30 `run_tlc.ps1` 於 Windows PowerShell 5.1 完全不可用（既有缺陷、非 R9 引入）**：`$ErrorActionPreference=Stop` + 檔頭既有 `java -version 2>&1` 在 PS5.1 會把 native stderr 包成 ErrorRecord 拋 NativeCommandError → 腳本在 Java 檢查行即中斷（rc=1），永遠跑不到任何 TLC 步；R9 新增 5a/5b 段鏡射既有 `*>&1 \| Tee-Object` 模式（java 偶發寫 stderr 時同理）。方向為 fail-loud 非假綠；檔案文件宣告引擎本為 pwsh；五軌權威路徑（python -m tools.fsm_runtime.tlc_runner）與 .sh 版不受影響 | P3 | 本輪加最小防護；完整 PS5.1 支援（改 `2>$null` 手法 vs 明文 pwsh-only）屬設計選擇留待後續 | **fixed@R9（主 agent，最小防護落地；完整 PS5.1 支援留待後續設計選擇）**：檔頭加 PS 版本守門——`$PSVersionTable.PSVersion.Major -lt 6` → 印明確導流訊息（pwsh 7+／run_tlc.sh／tlc_runner 三替代）exit 2，將「難解的 NativeCommandError 中斷」換成「明確 fail-loud 導流」。驗證：Parser AST 0 錯誤；PS5.1 實跑 → 導流訊息＋exit 2。**同輪一併處置 SD 二審三條記事觀察**：O-1 → `macos_smoke_local.sh` 檔頭補 git ≥ 2.31 前置需求注記；O-2（crossref 新詞彙無 IGNORECASE）→ 不改：大寫變體落點是 None→「含糊」warning 而非錯誤分類，fail-safe 方向可接受、帳本現存實例皆小寫已命中；O-3 → `test_ntfs_length_gate.py` 存量一致性鎖站點補 `errors="replace"`（14 passed 重驗） |
| DEF-101-125 | 2026-07-17 | R10 四方複審（Architect ARCH-1，唯一 P2 架構發現） | **根層消費檔本地驗證缺口**：`aisdlc-sdd-ci.yml` paths 承認 5 個根層消費檔（`tools/check_ntfs_paths.py`、`tools/git-hooks/pre-commit`、兩份 `.claude/settings.json`、`.claude/hooks/sdd_hook_router.py`）之回歸鎖住在 `AISDLC_SDD/scripts/tests/`，但本地 SDD leg 只在 push 涉 `AISDLC_SDD/` 時觸發；root-infra leg 的 py_compile 不含 `.claude/hooks/`、`tools/tests` 無 NTFS/router 測試、NTFS 守門對乾淨 repo 恆綠 → CI 停擺期間改壞消費檔可零機械訊號直推 main（不在 DEF-101-112 四道殘留清單內） | P2 | 本輪即修（pre-push 補消費檔 leg） | fixed@R10（`tools/git-hooks/pre-push`：新增消費檔偵測——機械解析 `aisdlc-sdd-ci.yml` paths 非 AISDLC_SDD 條目（單一真相源不手抄）、命中且 SDD leg 未觸發時補跑 `AISDLC_SDD/scripts/tests`；py_compile 擴至 `.claude/hooks/`；功能鎖 `test_pre_push_dispatcher.py` 情境 4、清單接線鎖 `test_root_infra_parity.py`） |
| DEF-101-126 | 2026-07-17 | R10 四方複審（QA-1） | **pre-push dispatcher 零自動化回歸測試**：R9 P1 修復（root-infra leg）與三 leg 分流全靠當輪一次性手動驗證，tools/tests 僅有 pre-commit 的 SIGPIPE 測試；重構改壞分流（如改回 grep 管線）時 bash -n 照綠 → 純根層 push 再次零閘門且無第二訊號 | P2 | 本輪即修（功能測試） | fixed@R10（`tools/tests/test_pre_push_dispatcher.py` 6 情境：純根層/僅子專案/空 stdin fail-safe/消費檔/刪分支/子 hook 缺失 fail-loud；tmp fake repo 真跑 dispatcher、marker 檔鐵證；受控突變 2 式驗紅） |
| DEF-101-127 | 2026-07-17 | R10 四方複審（QA-2） | **「跑 0 個測試也算 PASS」結構性 fail-open**：`python -m unittest discover` 對發現 0 個測試回 rc=0（Python 3.11 實測 `Ran 0 tests ... OK`）；四處 gate（pre-push leg ②、root-infra-ci step 8、windows/macos-smoke）同句裸 discover——目錄改名/pattern 不符/路徑打錯 → 全部回歸鎖靜默消失仍全綠 | P2 | 本輪即修（數量下限釘選） | fixed@R10（新增 `tools/run_root_unittests.py`：discover 後先斷言 countTestCases ≥ MIN_TESTS=177 再執行，低於下限 exit 1；四站點全數換裝；自身測試 `test_run_root_unittests.py` 3 case 含真樹下限鎖；比照 parity `_MIN_EXTRACT_COUNTS` 慣例） |
| DEF-101-128 | 2026-07-17 | R10 四方複審（QA-3） | **nightly「防假 pass 守門自身觸發」反而 WARN 綠出場**：`run_local_nightly.ps1` validate_mutmut_log 失敗分支設 `LASTEXITCODE=2`，但 Invoke-Stage 定義 rc=2=WARN 不算 fail、R9 終端 exit 決策亦排除 rc=2 → mutmut 換版/輸出漂移時 log 驗證失敗、schtasks Last Result 仍 0（正是 R9 修復要杜絕的形狀；DEF-101-116 修復敘述只以 perf undersample 論證 rc=2 排除，未覆核此早於 P0-6 的 fail 路徑） | P2 | 本輪即修 | fixed@R10（該分支改 rc=1〔真實失敗：mutation 沒真的跑〕；靜態錨點 case 20 鎖住「必須 =1、禁止 =2」） |
| DEF-101-129 | 2026-07-17 | R10 四方複審（QA-4） | **pgvector recall pytest rc 未捕捉**：R9 只給後加的 contract pytest 用 [ref] 捕捉，recall pytest 的 rc 被 collector（main() 恆 return 0）與 progress_check（連續 ≥3 次未達綠線才非零）覆蓋 → 單日真紅 stage rc=0 假綠、最快連紅 3 天才間接翻紅；CI 對等 job 該 step 是當場硬紅 | P2 | 本輪即修 | fixed@R10（`$recallRcRef = [ref]` 捕捉 + stage 尾端非零回寫〔D-10 同款〕；靜態錨點 case 21） |
| DEF-101-130 | 2026-07-17 | R10 四方複審（QA-5） | **R9 三項 nightly 修復零回歸錨點**：`test_run_local_nightly_static.py` 的存在理由是「每個 nightly 修復配一個靜態錨點」，但 R9 的 Stage L／PG contract／終端 exit decision 三段 grep 全無匹配——任何人「精簡 nightly」都能無聲退回 R9 前狀態 | P2 | 本輪即修 | fixed@R10（補 case 17~19〔Stage L／contract [ref]／exit decision+SKIP/WARN 語意+真 exit 1〕，並為 R10 修復加 case 20~24，全檔 24 case 實跑綠） |
| DEF-101-131 | 2026-07-17 | R10 四方複審（QA-6） | **CI 停擺期間 SDD chaos 家族零本地補償**：`aisdlc-sdd-fsm-chaos-nightly.yml`（Rule 9.9.4「必跑、連 3 日失敗鎖 main」）只掛在停擺的雲端；chaos 被 PR 閘門明文排除（`-m "not chaos"`）→ FSM runtime 只在 chaos 情境破壞有界停機的變更可全綠直推 main，streak-lock 機制整條死亡且不在任何 defer 清單 | P2 | 本輪即修（nightly 補 stage） | fixed@R10（nightly 新增 `sdd-fsm-chaos` stage 鏡射 CI 兩步：v0.01 pytest `-m chaos`〔34 tests 實測 31s〕+ 100 輪 chaos_runner sweep〔實測 8s〕、UTC 日期 seed、不依賴 Docker 恆跑、rc 納入 summary 與終端 exit 決策；靜態錨點 case 22） |
| DEF-101-132 | 2026-07-17 | R10 四方複審（Architect ARCH-2） | **root-infra-ci.yml 十道 step ↔ pre-push root-infra leg 守門清單雙份硬編碼、無同步鎖**：未來新增第五支守門工具進 CI 而忘改 pre-push 時，本地永遠不跑新守門且兩處各自綠燈（與已 defer 的 OBS-2 同類但對象不同——本 leg 為常設本地閘門，不隨 CI 恢復降級） | P3 | 本輪即修（meta 測試） | fixed@R10（`tools/tests/test_root_infra_parity.py`：抽取 yml 內 `python3 tools/*.py` 具名守門呼叫斷言 pre-push 皆有對應＋數量下限 ≥5，另鎖消費檔清單必須機械讀 yml；DEF-101-112 四道非 python 步驟天然不入抽取集合、以 python 重寫時自動落入本鎖） |
| DEF-101-133 | 2026-07-17 | R10 四方複審（Architect ARCH-3 + 拍板案(b) 落地；scratch 實測重現） | **LATEST 版本解析 8+ 站點 3 語言實作漂移、語意分歧已實證**：bash 三重 glob 尾端未錨定（`ls -d AISDLC_SDD_v0.[1-9]*`）→ `AISDLC_SDD_v0.30.bak`／檔總管複製品「v0.30 - Copy」被 `sort -V \| tail -1` 選中（Architect 實測：bash 選 .bak、Python 選 v0.30）；且全部站點掃磁碟非 git tracked——未 commit 複製目錄可讓「SDD 閘門綠燈實為測錯樹」 | P3 | 本輪落地拍板案(b)（SSOT 化） | fixed@R10（新增 `AISDLC_SDD/scripts/sdd_version.py` 單一真相源：LATEST ≝ git tracked（含 index）∧ fullmatch `^AISDLC_SDD_v\d+\.\d+$` ∧ (major,minor) 最大；未 tracked 合法名目錄印警告〔Copy-on-Evolve 新版先 git add 才進閘門，刻意設計〕、git 不可用 fallback 磁碟掃描；9 站點全遷移：ci-gate.sh、macos_smoke_local.sh、check_script_parity.py、root-infra-ci.yml、windows-compat-ci.yml×3、macos-compat-ci.yml×2；對抗性測試 `test_sdd_version.py` 7 case〔.bak／- Copy／未 tracked／檔案非目錄／v0.9→v0.10／v1.0／CLI〕；ci-gate 意圖鎖 4 case 全綠；rfc_lifecycle_lint 服務「既存版本集合」語意刻意不遷移〔見 sdd_version.py docstring〕） |
| DEF-101-134 | 2026-07-17 | R10 四方複審（拍板案(a) 落地；Architect 建議「正規化內容釘選」） | **wrapper 薄殼守門黑名單軍備競賽 + 成對腳本 enrollment 缺口**：`check_wrapper_thinness.py` 黑名單三輪被繞（`for(`／`python3 -c`／`.ForEach(`），列舉惡意不可判定；且 marker_pairs/thinness 對象皆硬編碼——新增一對 .sh/.ps1 不掛任何守門零機械訊號（`ONBOARDING.md` §6 原明文承認「改任一邊須人工同步」） | P3 | 本輪落地拍板案(a) | fixed@R10（①thinness 改制：正規化內容 sha256 釘選為權威判定〔剝註解/空行後對表，任何實質變動紅燈指路 `--print-hash` 更新釘選；註解調整不觸發〕、行數上限第二訊號、黑名單降級 hash 紅燈時診斷輔助——16 case 含「黑名單沒收錄的新樣板〔curl 管線 sh／Invoke-RestMethod〕hash 必攔」意圖鎖；②enrollment 發現鎖：`check_script_parity.py` 掃三目錄同名對，斷言每對 ∈ {parity 4 對, thinness 1 對, 明文豁免 3 對〔install_git_hooks／install-hooks＝DEF-101-088、ci-gate＝薄委派殼〕} 且無 stale——8 對全納管、3 case 測試） |
| DEF-101-135 | 2026-07-17 | R10 四方複審（SD-1 與 QA-7 獨立交叉發現） | **macos_smoke_local.sh [3b]/[3d] worktree 拒絕情境可假 PASS**：`git worktree add` rc 未檢查，add 失敗時 `( cd "$wt" && bash ... )` 的 cd 失敗 rc=1 恰等於「拒絕成功」預期值 → 受測腳本根本沒執行也 PASS（R9 P1 補償控制可信度失真；同檔 [2] 段有 `\|\| exit 9` 手法未沿用；CI 版因 step 級 bash -e 不受影響） | P3 | 本輪即修 | fixed@R10（兩段 add 顯式 if 檢查〔失敗 fail 不假 PASS〕+ subshell 內 cd 失敗即 exit 9 哨兵落 fail 分支；bash -n 過；windows_smoke_local.ps1 自首日即帶同款防護） |
| DEF-101-136 | 2026-07-17 | R10 四方複審（SD-2；PS5.1 Parser ANSI/UTF-8 雙讀實驗佐證機制） | **根層共用庫 `tools/lib/Find-GitBash.ps1` UTF-8 無 BOM 且含 9 行中文**：PS5.1 對無 BOM 檔以 ANSI（zh-TW=cp950）解讀；現況中文全在註解不炸（僅 Get-Help 亂碼），但任何編輯在中文行尾接程式碼即重演 cp950 吞引號 parse 斷裂（47 支凍結版前科同機制）；DEF-101-003 普查範圍未含後建的 tools/lib/ | P3 | 本輪即修 | fixed@R10（補 UTF-8 BOM〔實測檔頭 EF BB BF〕，與同目錄 GitHooksInstallCommon.ps1 一致；機械守門既存於 root-infra-ci step 2〔遞迴掃 tools/〕，其本地接線屬 DEF-101-112 已定案 defer） |
| DEF-101-137 | 2026-07-17 | R10 四方複審（QA-8） | **check_hooks_liveness `_run()` 的 R9 encoding 修復無回歸鎖**：13 個既有 case 全 mock `_run`，重構移除 `encoding="utf-8"` 時測試依然全綠 → 非 ASCII repo 路徑下 liveness 退回「無法判定＝不警告」靜默失效（同輪同款修復在 git_hooks_install_common 有鎖，此處漏對稱） | P3 | 本輪即修 | fixed@R10（`TestRunEncodingRegression`：mock subprocess.run 斷言 encoding="utf-8" + errors="replace" 參數） |
| DEF-101-138 | 2026-07-17 | R10 四方複審（QA-9） | **crossref 的 archive 大小守門（R9 二審補）零測試覆蓋**：既有測試只覆蓋主檔 fail/warn 兩帶，fixture 目錄天然無 archive 檔，archive glob 迴圈從未被驗證會紅——glob pattern／parent 路徑改壞時主檔測試仍綠，DEF-99-001 政策的一半守門無聲失效 | P3 | 本輪即修 | fixed@R10（隔離子目錄 fixture〔archive glob 掃 parent，共用 tmp 會互相污染——測試內註明〕+ fail/warn 兩 case，斷言輸出含「帳本歸檔」與檔名） |
| DEF-101-139 | 2026-07-17 | R10 四方複審（QA-10） | **Windows 側「執行級」.ps1 驗證真空（macOS 補償控制的鏡像缺口）**：install 兩腳本 worktree 拒絕、LATEST `install_post_commit.ps1` 實跑＋移除後路徑斷言、非 ASCII 路徑安裝等驗證只活在停擺的 windows-compat-ci；SDD 回歸測試 docstring 自承只鎖 `.sh` 版 → `.ps1` 版單獨迴歸時 `.sh` 測試綠、本地零訊號直到帳單恢復 | P3 | 本輪即修（本地聚合腳本） | fixed@R10（新增 `tools/windows_smoke_local.ps1`〔PS5.1 相容、UTF-8 BOM、CRLF、Parser 0 error〕：active .ps1 Parser 全量＋fake repo＋install_git_hooks.ps1／install-hooks.ps1 往返與 worktree 拒絕＋LATEST install_post_commit.ps1 worktree 實跑＋移除後路徑斷言＋中文路徑〔煙霧測試〕安裝抽驗；PASS=8 FAIL=0 實跑綠；LATEST 走 sdd_version.py SSOT〔resolver 取真 repo 路徑屬永久設計——驗證工具不必來自被測樹〕；worktree add 全帶 DEF-101-135 防護；兩平台 smoke 自此對稱） |
| DEF-101-140 | 2026-07-17 | R10 四方複審（QA-11） | **「Docker 長期不可用」無升級訊號**：Docker off 時 mutation/pg-e2e（含 R9 補的 PG contract 本地對等）/drift 全 SKIP、終端 exit 0——單次 SKIP 屬合理設計，但 CI 停擺期間 PG contract 唯一機械通道繫於 02:00 時 Docker Desktop 是否開著，連續 N 天 SKIP 無任何 fail-loud | P3 | 本輪即修 | fixed@R10（`.docker_skip_streak` 檔累計〔AutoClaude/.gitignore 排除〕：Docker 可用即清零、連續 ≥3 次列入 finalFailures → exit 1 翻紅 Last Result；靜態錨點 case 23 含「恢復可用必清零」防永久紅） |
| DEF-101-141 | 2026-07-17 | R10 四方複審（SA-1） | **缺陷帳本行數快照再度漂移**：DEF-101-113 列宣稱 macos_smoke_local.sh「311 行〔SA 二審已訂正 231 誤植〕」，但該檔全歷史唯一 commit 即為 314 行——訂正值本身在落地當下就錯（同 commit 內 O-1 又加了檔頭注記）；「行數快照」宣稱天生易漂，Nightly 紀律 header 已為此移除行數快照、帳本列反而又寫一個 | P3 | 本輪即修 | fixed@R10（該列移除行數快照、改註記兩度誤植史實，比照 DEF-101-108 防漂移手法） |
| DEF-101-142 | 2026-07-17 | R10 四方複審（SA-2） | **nightly END 進度與紀律 #13 的 M-05 語意對 mutation 軌已過時＋進度虛報**：ADR-SD09-011 後 mutation 去重鍵＝source_sha256（同日多 sha 全計入），「同日多 run 不進帳」語意反轉；且 `mutation=N/7` 用 Get-JsonlCount 原始列數 vs should_lock 的 7 unique sha 門檻——R10 實測 live 檔 29 列僅 5 unique sha，原格式虛報 29/7 | P3 | 本輪即修 | fixed@R10（新增 `Get-MutationUniqueCount`〔module 過濾＋只計非空 source_sha256 證據，legacy 缺 sha 不計〕；END 行改 `mutation=U/7 unique-sha (records=N; delta=…)`、其餘三軌維持 M-05 語意並於行內標示分軌；紀律 #13 補 R10 訂正段＋交叉引用紀律 #12/ADR-SD09-011；靜態錨點 case 24；連帶揭露 DEF-101-148） |
| DEF-101-143 | 2026-07-17 | R10 四方複審（SA-3） | 根 `CLAUDE.md` nightly 指令註解「6 stage」但括號只列舊 5 個名字（漏 R9 的 local_ci_gate） | P3 | 本輪即修 | fixed@R10（更新為「7 stage（local_ci_gate/mutation/pg-e2e/perf/drift/obs/sdd-chaos）」，含 R10 新增 chaos stage） |
| DEF-101-144 | 2026-07-17 | R10 四方複審（SA-4 與 Architect ARCH-4 獨立交叉發現） | `ONBOARDING.md` §6.1 表收錄準則不一致：表列 8 支遷移 + root-infra-ci（非遷移）共 9 支，磁碟實有 11 支——同為非遷移新增的 windows-compat-ci.yml／macos-compat-ci.yml 不在表內；依表盤點 CI 接線會漏掉恰是跨平台工程最關鍵的兩支 | P3 | 本輪即修 | fixed@R10（表尾補兩列，註明非遷移新增、本地補償對等腳本與詳見章節） |
| DEF-101-145 | 2026-07-17 | R10 四方複審（SA-5） | 硬編計數違反自訂 SSOT 鐵律：`ONBOARDING.md` §3「30 個版本目錄 v0.01~v0.30」（v0.31 誕生即漂移且無機械守門）；`AISDLC_SDD/CLAUDE.md` 樹狀圖「9 個 SDD CI/CD 規格／55+ 參考指南／15 個場景指令集」同類硬編 | P3 | 本輪即修 | fixed@R10（ONBOARDING 改「全部版本目錄（清單/最新版號見 FRAMEWORK_STATUS.md）」；CLAUDE.md 樹狀圖三處去計數——數字一律回 SSOT） |
| DEF-101-146 | 2026-07-17 | R10 四方複審（SA-6） | `root-infra-ci.yml:48` 檔尾註解自引「第 4 道 NTFS 檔名閘」，但同檔十道清單中 NTFS 為第 5 道（2026-07-13 插入 EOL(.ps1) 後未同步）；ONBOARDING 明文「以 workflow 檔頭註解為準」——權威源自身編號錯置 | P3 | 本輪即修 | fixed@R10（去序數改稱「NTFS 檔名閘」，杜絕未來重編號再漂） |
| DEF-101-147 | 2026-07-17 | R10 四方複審（SA-7） | Nightly 紀律 #14 取證引用 `logs/nightly_2026-05-26_020001.log:172-174` 為死連結：logs/ gitignored 且該檔已輪替，任何機器循文件找證據必落空（紀律 #3 證據鏈對歷史條目不可復驗） | P3 | 本輪即修 | fixed@R10（改文字存證＋加政策句「已輪替的關鍵取證一律改文字存證，不留死連結」；紀律文件 v1.6→v1.7） |
| DEF-101-148 | 2026-07-17 | R10 修復期主 agent 親驗（SA-2 修復時比對 live jsonl 揭露；zero-trust 對歷史宣稱回溯查證） | **improving_101 宣稱的方案 A 壓縮（30→6 筆）從未在本機 live 檔落盤**：`.mutation_history.jsonl`（untracked 本機檔）實為 29 筆、含 21 筆同 sha=20940e1b 的舊「UTC 日去重」時代 idle 累積（timestamp 全在 2026-06-25 前）＋7/14、7/16 兩筆新制紀錄——與 improving_101 結案宣稱「真實 30→6 筆/4 unique sha」不符；帳面進度（「4 unique 距 7 差 3」）與實況（5 unique 差 2）亦漂移。教訓：agent 宣稱的**本機副作用**（非入庫產物）須親驗檔案實況，與 no-fabricated-tool-output/紀律 #17 同家族 | P2 | 本輪即修（實跑官方壓縮） | fixed@R10（實跑 `mutation_baseline_lock.py --migrate-compact-sha`：29→7 筆〔5 sha + 2 legacy date〕、自動備份 `.pre_sd09_010.bak`；壓縮後 unique sha=5〔20940e1b/4af78567/5208cff3/55013d0a/5a44cbba〕，距鎖定門檻 7 差 2 次真實 token_guard 演進；紀律 #13 R10 訂正段已載明。**R11 補註**：本列取證屬 untracked 本機檔（Windows 機），R11 於 Mac 上無法跨機器複驗——「本機副作用類」fixed 宣稱的複驗天生綁定該機器，跨平台複審時如實標注即可，非帳實矛盾） |
| DEF-101-149 | 2026-07-17 | R11 真 Mac 首跑（`run_root_unittests.py` 實測 FAILED (failures=2)，同時 smoke PASS=10 綠——兩訊號分歧即本案價值） | **測試假路徑平台不中立：Windows 全綠 ≠ Mac 全綠**：`tools/tests/test_check_hooks_liveness.py` 兩 case 以 `Path("D:/repo")` 撰寫假絕對路徑——`"D:/repo"` 僅在 Windows 是絕對路徑，POSIX 上 `repo_root / current_value` 的「絕對路徑取代」語意不成立（join 成 `D:/repo/D:/repo/…`、resolve 後恆不等）→ 傳絕對 current_value 的兩 case 在 Mac/Linux 恆 False；root-infra-ci 跑 ubuntu 為何沒抓到？該兩 case 於 R10（Windows 輪）新增後 CI 停擺（DEF-101-081），「測試自身的平台中立性」此前無任何守門件在守（守門件的守門件缺位，Architect B6） | P2 | 本輪即修 | fixed@R11（`ABS_FAKE_REPO` 平台中立常數收斂至既有 `tools/tests/_platform_helpers.py`，test_check_hooks_liveness.py 全檔套用〔含原本自洽但易被複製的 pattern〕；新增 `tools/tests/test_platform_neutral_paths.py` 機械掃 tools/tests 裸磁碟機假路徑——**首跑即另抓 `test_check_wrapper_thinness.py:30` 的 `Z:/` 一處既有違規**一併修復；root unittests 197→205 全綠、真 Mac 實跑取證） |
| DEF-101-150 | 2026-07-17 | R11 Shell 掃描（agent 全量掃描 152 支 .sh + 6 支 hook 後唯一 P2） | **v0.30 `install_post_commit.sh:27` LATEST 解析仍用被 R10 淘汰的 `ls \| sort -V \| tail -1` 未錨定 glob**：`.bak`/檔總管複製品會污染選版、把錯誤版本路徑寫進共享 post-commit hook（R10 ARCH-3 缺陷本體）；ci-gate/smoke/CI 全已委派 `sdd_version.py` 且在乾淨 clone 跑，永遠測不到本腳本內部這條分歧（假綠）；檔頭註解自稱「對齊 ci-gate.sh 的 sort -V」已 stale；`.ps1` 對等檔同病（`Get-ChildItem+[version]` 未錨定、掃磁碟非 tracked） | P2 | 本輪即修 | fixed@R11（兩支皆委派 `scripts/sdd_version.py --sdd-root`〔SSOT 定案「呼叫端不得再自行實作」〕、直譯器缺席 fail-loud；v0.30 回歸鎖 fixture 同步放入 resolver〔3 tests 綠〕；Windows 側兩處 hook 內容斷言 regex 放寬相容新舊格式〔僅靜態+pwsh 7 驗證，PS 5.1 待 Windows 輪〕；v0.01~v0.29 凍結版同病依紀律不回改；端到端驗證＝隔離 clone 疊上 R11 變更後 scratch commit 的 smoke [4] 全綠＋v0.30 worktree 回歸鎖 3 tests〔INSTALL_SCRIPT 工作樹直呼〕，本 repo 工作樹 smoke [4] 依設計自 HEAD clone、未 commit 變更不在其驗證範圍） |
| DEF-101-151 | 2026-07-17 | R11 主 agent 親驗（`env -i PATH=/usr/bin:/bin sh -c 'command -v python'` 落空實證） | **install_post_commit 產生的 hook 內容裸 `python`＋`\|\| true`——現代 macOS 乾淨 PATH 只有 `python3`**：git hook 執行環境不吃 venv，Mac 無 venv 終端 commit 時 drift/closure advisory 永久靜默失效零告警（與 DEF-43-008「`\|\| true` 吞掉、閘門靜默全滅」同家族；ONBOARDING §3 雖明文「啟用 .venv 後 python 才存在」，hook 卻在未啟用情境也會被 git 觸發） | P2 | 本輪即修 | fixed@R11（.sh/.ps1 兩產生器寫出的 hook 統一加 `PY="$(command -v python \|\| command -v python3 \|\| true)"` fallback；皆缺時 stderr 明示警告後 exit 0 不阻擋 commit〔advisory 語意不變、但不再無聲〕；實測兩產生器輸出逐位元一致〔R11 四方複審 SD/QA/SA 三方獨立實測揪出：.ps1 here-string 原缺檔尾 LF、差 1 byte——已補 LF 使宣稱為真（pwsh 7 cmp 取證），見 DEF-101-164〕；對本 repo 實裝煙測後還原） |
| DEF-101-152 | 2026-07-17 | R11 Python 掃描（對 v0.01 掃出）＋Fixer B 對 v0.30 核實 | **v0.30 `sandbox_runner.py` except 僅接 `TimeoutExpired`**：`UnicodeDecodeError`/`OSError` 會冒泡中斷沙箱執行（encoding="utf-8" 本體先前輪已補、v0.01 同病屬 §9 known-gap 既揭露〔DEF-101-019〕不回改）；`tlc_runner.py` v0.30 已補 encoding、無事可做（掃描對象是凍結基線造成的誤報，如實校正） | P3 | 本輪即修 | fixed@R11（except 擴為 `(TimeoutExpired, UnicodeDecodeError, OSError)`，維持「回傳失敗觀測不冒泡」語意；v0.30 pytest 1669 passed） |
| DEF-101-153 | 2026-07-17 | R11 Python 掃描 | **`scripts/sdd_version.py` 錯誤分支 print `❌`（U+274C 非 cp950 可編碼）且無 stdout/stderr UTF-8 防護**：Windows 非 UTF-8 console 走「找不到版本目錄」分支時 UnicodeEncodeError 反而掩蓋真錯誤（CI gate 場景致命）；同群組 `scripts/` lint 腳本皆已防護、此檔為唯一漏網 | P3 | 本輪即修 | fixed@R11（main() 頂部補 reconfigure，逐字抄 gitignore_coverage_lint.py 既有慣例） |
| DEF-101-154 | 2026-07-17 | R11 Python 掃描 | **v0.30 fsm_runtime 五支 CLI（id_registry/intent_decomposer/media_size_check/spec_debate/tlc_runner）print 中文/`→`/`≥` 無 stdout 防護**：zh-TW（cp950）可編碼不炸，但 en-US Windows（cp1252）console 直跑即 UnicodeEncodeError | P3 | 本輪即修 | fixed@R11（五支 main() 頂部補同款 reconfigure；v0.01 同病凍結揭露） |
| DEF-101-155 | 2026-07-17 | R11 Architect 檢視（bootstrap 對子三處語意漂移中唯一實害級） | **`bootstrap.sh` 建 venv 後無「rc=0 假成功」驗證**：`.ps1` 於 2026-07-13 runner 真實事故後已補 `Scripts\python.exe` 存在性 fail-fast（bootstrap.ps1:129-135），`.sh` 漏對稱——mac/Linux 側同款假成功會漏到 pip 階段才炸；且 parity 工具只比標籤序列，此類語意漂移天生不可見（工具 docstring 自認） | P3 | 本輪即修 | fixed@R11（venv 建立後補 `[ -x "$VENV_DIR/bin/python" ]` fail-loud，step 標籤零變動 parity 保持綠） |
| DEF-101-156 | 2026-07-17 | R11 Architect 檢視（B1 結構性風險） | **parity 納管語意僅「同名成對」——單邊新增腳本零訊號**：`_discover_pairs` 只在 .sh/.ps1 同名成對存在時納管，新增單邊腳本永遠沒人問「該不該有另一平台版」；現存孤兒實例：兩支 smoke（stem 刻意不同）、ops 排程家族四支、bash-only 工具五支 | P3 | 本輪即修 | fixed@R11（enrollment 擴為全量四類：成對納管／薄殼 hash 釘選／成對豁免〔DEF-101-088 類〕／**單邊豁免**〔11 支各附一行決策依據（run_local_nightly.ps1 於 DEF-101-163 落地 .sh 後轉成對豁免，終態 10 支）〕；unknown 單邊與雙向 stale〔檔案消失／對邊已出現〕皆 fail-loud；新增 4 案回歸測試；run_local_nightly.sh 落地後依 TODO 轉成對豁免） |
| DEF-101-157 | 2026-07-17 | R11 Architect 檢視（B2 結構性風險） | **smoke↔CI 四份手寫實作與 ONBOARDING §8 數字純靠註解同步、零機械互鎖**：同一 P1 回歸鎖以兩語言方言存在四份（兩 smoke＋兩 compat workflow），改 CI 忘改本地零訊號；且 compat 兩 yml 原本連指回 smoke 腳本的注記都沒有（互鎖無錨點） | P3 | 本輪即修 | fixed@R11（新增 `tools/tests/test_smoke_ci_sync.py`：MIN_PASS/$MinPass 與 ONBOARDING §8 `PASS=N` 宣稱集合互鎖＋分組標籤下限釘選＋四向同步注記存在斷言〔防 0 命中假綠〕；compat 兩 yml 補指回注記；連帶 `test_ci_paths_cover_root_consumers` 抓出新測試擴大消費集合——兩 workflow paths 補列 ONBOARDING.md／對面平台 smoke／對面 workflow 共 3+3 條後 9 passed） |
| DEF-101-158 | 2026-07-17 | R11 Shell 掃描 | **repo 對「BSD mktemp 是否必帶模板」存在兩套互斥假設**：macos_smoke_local.sh:70／run_act.sh:82 帶模板並註解稱「BSD 必須帶」；ci-gate.sh:81,127 與 v0.30 init_project.sh:151 裸用——R11 真 Mac 實測兩種皆可（現代 BSD 已不強制），但認知漂移會誤導後續腳本 | P3 | 本輪即修 | fixed@R11（裸用三處統一帶模板＋一行 WHY：帶模板為最保守跨平台寫法；既有「必帶」註解屬保守正確不動） |
| DEF-101-159 | 2026-07-17 | R11 Shell 掃描 | v0.30 `init_project.sh:1` shebang `#!/bin/bash` 違反全 repo `#!/usr/bin/env bash` 慣例（唯一例外；30 版皆如此） | P3 | 本輪即修 | fixed@R11（v0.30 改 env bash；歷史版凍結不回改） |
| DEF-101-160 | 2026-07-17 | R11 Shell 掃描 | **`sd06_w3_staging_dryrun.sh` 計時用 `date +%s%N`（16 處）GNU-ism 無載具揭露**：Linux 與現代 macOS BSD date 支援 %N（R11 真 Mac 實測），但老舊/嚴格 BSD 會輸出字面 `N` 使 `set -e` 下算術崩潰 | P3 | 本輪即修 | fixed@R11（檔頭補載具限定揭露：Linux staging DBA 工具〔需 psql/pg_dump/alembic〕、老 BSD 不支援、Windows 走 WSL；16 處實作不動——Simplicity First） |
| DEF-101-161 | 2026-07-17 | R11 文件掃描 | `root-infra-ci.yml` 檔頭十道編號與 job 實體 step 順序不對位（py_compile 檔頭列第 7 道、實體緊接 bash -n 為第 2 個執行）；ONBOARDING 明文「以 workflow 檔頭註解為準」，編號易被誤讀為執行序 | P3 | 本輪即修 | fixed@R11（檔頭補「編號為清單序、與實際執行順序無關」一行註記，杜絕誤讀；不重排 steps） |
| DEF-101-162 | 2026-07-17 | R11 主 agent 讀 `AISDLC_SDD/CLAUDE.md` 發現（文件掃描漏網——其僅核對路徑存在性未核機制描述） | **`AISDLC_SDD/CLAUDE.md` 檔頭兩處仍以「`sort -V \| tail -1` 動態取最高」描述 LATEST 解析機制**：R10（DEF-101-133）已定案 `sdd_version.py` SSOT（git tracked＋錨定 fullmatch＋數值排序），override 級權威文件自身描述停在被淘汰的舊機制 | P3 | 本輪即修 | fixed@R11（框架版本行與 🔴 版本狀態段兩處改為 SSOT 描述；全檔 `sort -V` 零殘留） |
| DEF-101-163 | 2026-07-17 | R11 Architect 檢視（D1 拍板）＋額度治理（使用者既有指令：CI 額度緊張以本地驗證為主） | **macOS 本機 nightly 載體缺席**（§8 明示缺口：launchd 範本備妥但「待 run_local_nightly.sh 就緒」）；同時 compat 兩 workflow 每日 cron（06:00/07:00 UTC，macOS runner 計價倍率高）於額度停擺期持續空燒 | P3 | 本輪即修 | fixed@R11（**拍板：不移植 929 行 .ps1**〔會重演 bootstrap 雙實作問題〕，新增 `AutoClaude/tools/run_local_nightly.sh` 薄聚合器四 stage 串接既有腳本〔macos_smoke → 根層 unittests → AutoClaude local_ci_gate → SDD ci-gate；任一失敗 exit 1 對齊 .ps1 R9 exit 語意〕；launchd 範本轉「已就緒」；parity 登記為成對豁免〔兩支語意刻意不同〕；compat 兩 cron 每日→每週一＋保留 workflow_dispatch。**紅路徑注入驗證加碼取證**：注入假失敗實測時攔到聚合器自身一個綠路徑永不觸發的 bash 3.2 bug——裸 `$_rc` 後緊接全形括號被 locale 相依變數名解析吞併、`set -u` 下 unbound 假死於首個 FAIL 分支，已全數改 `${_rc}` 大括號展開並以紅→綠兩輪實跑取證〔與 DEF-101 家族既載的 multibyte 變數名陷阱同款，dispatcher 註解早有前例〕；綠路徑四 stage 實跑 PASS=4 FAIL=0、紅路徑 exit 1 且失敗 stage 列名正確） |
| DEF-101-164 | 2026-07-18 | R11 四方獨立複審（Architect/SA/SD/QA 全 APPROVE、零 P1/P2） | **R11 收斂彙總——三組 P3**：①取證措辭過度宣稱兩處（DEF-101-150「smoke [4] 端到端實跑」與 ONBOARDING §8「端到端重驗」——實際證據實體是隔離 clone scratch commit＋worktree 回歸鎖，本 repo 工作樹 smoke 依設計測不到未 commit 變更）＋DEF-101-151「逐位元一致」宣稱差 1 byte（.ps1 here-string 缺檔尾 LF）＋本 repo `.git/hooks/post-commit` 煙測殘留中間版（裸 python 無 fallback）；②平台中立掃描 regex 網眼——只抓「Path( ＋雙引號＋大寫磁碟機＋正斜線」單形態，漏 r/f 前綴、反斜線、小寫磁碟機與裸字串（原病灶形態）；③mac 薄聚合器七軌去向帳目不全（僅交代 mutation/pg-e2e/perf/obs 四軌，drift 與 sdd-fsm-chaos 去向未載） | P3 | 本輪即修 | fixed@R11（D1 `.ps1` 補檔尾單一 LF 使逐位元一致為真〔pwsh 7＋bash 各於乾淨 temp fake repo 實裝，cmp BYTE_IDENTICAL 取證〕；D2 以最終版產生器重灌本 repo hook〔含 PY fallback＋正確 v0.30 絕對路徑〕；D3 兩處措辭訂正為證據實體精確描述；D4 聚合器檔頭＋ONBOARDING §8 補 drift〔Windows nightly 取證帳本紀律承載〕與 sdd-fsm-chaos〔非平台敏感純 Python 回歸，Windows nightly＋CI chaos workflow 覆蓋〕去向；D5 掃描 regex 補強為「任意字串字面值以磁碟機路徑開頭」＋註解尾剝除＋PureWindowsPath/PurePosixPath 豁免＋`# platform-ok:` 標記，雙變異注入紅自證；D6 P4 批次八項〔parity 測試 stale docstring 改既成事實、根 CLAUDE.md 對等清單補 run_local_nightly.sh、DEF-101-156「11 支」補終態 10 支註記、test_smoke_ci_sync 裸 assert 改 fail-loud raise、聚合器 STAGE_TOTAL 變數化＋chmod 755、v0.30 install_post_commit LATEST 錯誤訊息補精度＋.ps1 python 前置檢查、新增 v0.30 reconfigure 釘選測試 test_cli_utf8_reconfigure.py、check_script_parity 豁免清單互斥自檢〕。**揭露延後項**：ARCH-5/SD-4〔regex 錨定收緊與 PS 5.1 實測〕待 Windows 輪；ARCH-3〔納管邊界外 tools/lib 異名對〕＝docstring 既宣告掃描邊界；ARCH-8〔launchd 啟用無機械查核〕＝ONBOARDING 已如實揭露 |
| DEF-101-165 | 2026-07-18 | R12 Shell 掃描（真 Mac 第二輪；附 git init 最小 repro） | **`AISDLC_SDD/scripts/install-hooks.sh:23` 的 `chmod +x .githooks/* scripts/*.sh` 在 POSIX 弄髒工作樹**：5 支索引 100644 的 scripts/*.sh 被改磁碟 755，core.filemode=true 下憑空產生 5 檔 mode-only diff、誤 commit 即違反 ONBOARDING §6 執行權限政策；Windows（filemode=false）完全看不到——Mac 單邊受害的平台不對稱；功能上該 chmod 對 scripts/*.sh 已無必要（全部以 bash 呼叫、hook 實際入口為根層 dispatcher） | P3 | 本輪即修 | fixed@R12（chmod 縮限為 `.githooks/*`〔索引本就 755，冪等不弄髒〕＋WHY 註解；smoke fake-clone 情境不受影響） |
| DEF-101-166 | 2026-07-18 | R12 Shell 掃描（`git ls-files -s` 全量盤點） | **ONBOARDING §6「755 入庫僅限 tools/git-hooks/」政策句與索引 6 支非 hook 755 腳本漂移**：bootstrap／integration_gate／install_git_hooks／local_ci_gate／run_act 五支歷史 755 無任何直呼載體（全 repo 零 `./xxx.sh` 呼叫端）；run_local_nightly.sh 則是 R11 D6 為 launchd ProgramArguments 刻意 755——政策句兩頭都不準 | P4 | 本輪即修 | fixed@R12（五支正規化 644；政策句改寫為真實集合：hook 檔〔tools/git-hooks/＋.githooks/〕＋launchd 直呼載體 run_local_nightly.sh） |
| DEF-101-167 | 2026-07-18 | R12 Python 掃描 | **v0.30 `test_post_commit_drift_worktree.py` `_run()` helper `text=True` 無 encoding**：DEF-101-121 家族漏網站點（該檔 2026-07-13 隨 DEF-101-059 新建，晚於清掃輪）；zh-TW Windows＋非 ASCII tmp 路徑下 git 錯誤訊息解碼炸或亂碼污染斷言；官方閘門 PYTHONUTF8=1 已保護、裸跑 pytest 才踩 | P3 | 本輪即修 | fixed@R12（補 encoding="utf-8", errors="replace"，逐字對齊同目錄 test_closure_evidence.py 慣例；2 passed） |
| DEF-101-168 | 2026-07-18 | R12 Python 掃描（AutoClaude/tools 全量清查——此前帳本從未宣稱完成過該面清查） | **AutoClaude/tools 9 支 CLI 無 stdout/stderr UTF-8 防護**（seed_kb〔`≥` 連 zh-TW cp950 都炸〕/check_loc_budget/perf_regression_check/check_frozen_surface_shim/pg_dump_to_yaml/drift_log_ga_check/observability_ga_check/mock_brain_server/mutation_analysis）：DEF-82-001/87-003 慣例部分落地未全量；官方載具已設 PYTHONUTF8=1、實害限 Windows console 手動直跑（DEF-101-148 即此類維運手跑先例）。**修復期取證修正**：stderr 預設 backslashreplace——cp950 下印 `≥` 到 stderr 不崩潰、會毀損成「反斜線＋小寫 u＋2265」六字元的 backslashreplace 逸出序列（SA 一審/二審兩度訂正：本句先前兩版都想寫該逸出序列字面、反斜線兩度被寫入管道吞掉致自相矛盾——改全中文描述根絕，SA-3R 收案標準改 bytes 覆核）；真崩潰在 stdout（strict），本修復兩者皆治（同直譯器 cp950 探針法紅→綠實證） | P3 | 本輪即修 | fixed@R12（9 支 main() 頂補雙流 reconfigure loop，逐字複製 download_bge_m3 等 4 支既有雙流慣例；mock_brain_server 補 import sys；151 passed/3 skipped 目標測試〔選集＝tests/tools/test_{drift_log_ga_check,mock_brain_server,mutation_analysis,observability_ga_check,perf_regression_check,shim_check}.py＋tests/contract/test_{loc_budget_tiered,perf_regression_check}.py＋tests/test_gap021_028.py＋tests/integration/test_pgvector_real_recall.py，SA 一審要求補可稽核選集〕＋LOC 閘門綠；同輪連帶 `_conditional.py` 補 cmd.exe/shell=True 跨平台警語 docstring〔PY-4，零行為變更——Architect 一審 ARCH-2 要求入帳〕） |
| DEF-101-169 | 2026-07-18 | R12 Shell/文件掃描交叉發現（DOC-1＋SH-3） | **LATEST 解析「sort -V \| tail -1」被淘汰機制描述殘存 SSOT 文件自身與 10+ 處 docstring**：FRAMEWORK_STATUS.md 第 8 行（生成器 framework_status_snapshot.py:134 模板）＋ scripts/ 六支 lint／copy_on_evolve.sh 兩處／scripts/tests 四處——DEF-101-162 修復面僅 AISDLC_SDD/CLAUDE.md，漂移點恰是全 repo 被指向的 SSOT；且三支 ci-gate 硬閘 lint（skill_header_sync/sync_exposed_skills/framework_status_snapshot）以 rfc_lifecycle_lint 磁碟語意做閘門選版，sdd_version.py 豁免註記未涵蓋 | P3 | 本輪即修 | fixed@R12（模板改 SSOT 描述＋實跑 --write 重生 FRAMEWORK_STATUS.md＋--check 綠；全部 stale docstring 收斂；**SH-3 拍板＝明文定案豁免不遷移**：copy_on_evolve 建版後未 git add 即呼叫三支 lint --write 同步新版，tracked 語意會選不到剛建新版、正是 DEF-58-002/59-001/96-001 要根除的復發路徑，且隔離 harness 無 git；sdd_version.py 豁免註記擴明為兩類消費者並載完整 WHY；殘餘分歧「合法名未 tracked 目錄」fail-loud 非假綠；sdd_version.py 呼叫端清單 docstring 同輪補齊〔ARCH-R12-8〕） |
| DEF-101-170 | 2026-07-18 | R12 文件掃描（計數/歸屬類批次） | **文件計數與歸屬漂移五處**：①AISDLC_SDD/CLAUDE.md「SDD CI/CD 規格完整清單（9 個）」磁碟實有 12（漏 DRIFT_MONITOR/HUB_SYNC/PRODUCTION_FEEDBACK 三列，違反同檔「計數回 SSOT」鐵律）；②根 CLAUDE.md「37 場景 × 100 輪」——實測 -m chaos 收集 34（v0.01/v0.30 皆同）且 ×100 輪屬另一支 chaos_runner sweep，37 無任何可稽核實數對應；③chaos-nightly yml 檔頭「13 chaos tests」同病（權威源檔頭，DEF-101-146 同家族）；④ONBOARDING §6.1「PS 5.1 專測」誤植於 windows-smoke 列——實體在 windows-nightly-full（R5/S8 拍板非 PR 閘門）；⑤AISDLC_SDD/CLAUDE.md「最後更新 2026-06-05」stale（該檔 R11 才改過） | P3 | 本輪即修 | fixed@R12（①去計數＋補三列照檔頭實填；②③去計數改忠實描述；④移正歸屬並註明訂正；⑤改 2026-07-18＋「權威以 git log 為準」防漂註記；連帶根 CLAUDE.md jobs 句補「push 閘門 jobs」限定語〔DOC-7〕） |
| DEF-101-171 | 2026-07-18 | R12 Architect 檢視（A 軸：DEF-101-070 ② 定案 accept「優先序最高」擱置兩輪即欠帳；三例實證語意漂移 DEF-101-100/120/155 證明標籤 parity 天花板） | **local_ci_gate.{sh,ps1} 雙實作收斂欠帳落地**：141+167 行雙業務邏輯收斂為 `AutoClaude/tools/local_ci_gate.py` 單核心（306 行；9 gate 語意逐行對照移植、gate 名稱/輸出字樣/exit 語意凍結）＋兩薄殼（23/39 行，只做確認直譯器→參數映射→轉呼叫；介面 --act/--pg/-Act/-Pg/-PytestArgs 完全相容、呼叫端零改動）；薄殼轉 check_wrapper_thinness hash 釘選（與 dev_start 同類），parity 的 gate-call 抽取與 _MIN_EXTRACT_COUNTS['local_ci_gate'] 依計畫退場；editable 哨兵改 in-process import（真正檢驗 editable 指向，舊 python -c 的 cwd 遮蔽使哨兵恆弱）；23 個單元測試（mock subprocess：順序凍結/失敗聚合〔例外與非零 rc 兩型〕/參數取代/act 平台分派/PG 清理防吞；初版 21，QA 一審 QA-3 補 editable 哨兵紅/綠 2 案）。**integration_gate 對本輪不收斂、揭露 defer**：其 Windows 腿依賴 Find-GitBash 探測（SDD ci-gate 為 bash-only），Mac 上無法真機驗證，defer 至 Windows 輪與 PS 5.1 實測同批 | P2（欠帳） | 本輪落地 | fixed@R12（真跑取證：收斂後 `bash tools/local_ci_gate.sh` 7 gates 全 PASS、AutoClaude 3,682 passed/144 skipped〔=R11 基線 3,661＋新增 21 案例，本機 venv 含選配、非出廠環境數；巢狀 Claude Code session（CLAUDECODE=1）下量測——SA 一審複證 `env -u CLAUDECODE` 為 3,693/133、總數同 3,826，差異即 requires_claude_cli 條件 skip（DEF-101-091 變因）〕、exit 0；.ps1 薄殼 pwsh 7.6.3 Parser 0 錯誤＋全 CRLF＋BOM——**PS 5.1 實跑續掛 Windows 輪**與既有揭露同批） |
| DEF-101-172 | 2026-07-18 | R12 Architect 檢視（C 軸風險 1+2：ARCH-8 原 defer 判準「ONBOARDING 已揭露」在「主力開發機已實際移至 Mac」新事實下不再充分） | **nightly 排程層活性零機械查核**：CI 停擺（DEF-101-081）期間本地 nightly 是唯一每日兜底層，launchd/schtasks 是否真的在跑無任何訊號；深度取證五軌 100% 繫於 Windows 機 02:00 活著，跨機斷載零提示 | P3 | 本輪即修（心跳哨兵） | fixed@R12（`run_local_nightly.sh` 結尾成功/失敗皆寫心跳檔 `AutoClaude/logs/nightly_mac_latest.log`〔gitignored；寫入失敗不改 exit 語意；`${var}` 大括號展開守 DEF-101-163 教訓〕，Windows 側沿用既有 nightly_latest.log；`dev_start` 步驟⑥新增 `_check_nightly_heartbeat` 三態 advisory——缺席提示/mtime>8 天 `_warn`/新鮮 OK，OSError 不得使 dev_start 失敗；三態單元測試＋ONBOARDING §8 心跳節；**跨機限制如實揭露**：心跳檔為本機 untracked 產物、只守本機排程活性〔DEF-101-148 同性質〕） |
| DEF-101-173 | 2026-07-18 | R12 Architect 檢視（A/F 軸：唯一有漂移前科的對〔run_tlc，DEF-101-100 .ps1 曾缺整條 FLEET_FSM 軌〕恰在 parity 掃描邊界外且 R9 修後零機械鎖；Copy-on-Evolve 每次升版都會複製出新的邊界外對子） | **parity 納管邊界不含 LATEST 版 tools**：`_PAIR_SCAN_DIRS` 僅三目錄非遞迴，v0.30 tools 下 4 對同名 .sh/.ps1（init_project／install_hooks/install_post_commit／formal/run_tlc／arch_fitness/run_self_evolution）＋1 支單邊（verify_traceability.sh）完全無人納管 | P3 | 本輪即修 | fixed@R12（enrollment 掃描擴至 LATEST tools **遞迴**〔sdd_version.py SSOT 解析、失敗 fail-loud 不得靜默縮邊界〕；登記 key 用 `LATEST/tools/…` 相對路徑——升版不失效；**run_tlc 上機械鎖**：兩側 FSM 軌錨點 token multiset 比對（SD 一審 SD-2 由「排序去重」集合升級為含重複次數，連「同 token 多次引用單側刪其一」一併攔）＋下限釘選 6，恰攔 DEF-101-100 型「單側缺整條軌」漂移，紅/綠 fixture 注入自證；其餘三對親讀定類成對豁免附依據、verify_traceability.sh 單邊豁免；凍結版依鐵律不掃） |
| DEF-101-174 | 2026-07-18 | R12 Architect 檢視（F 軸網眼 1：DEF-101-149 已實證病灶類別在其他三個測試樹零守門） | **平台中立假路徑掃描僅覆蓋 tools/tests 單目錄**：AISDLC_SDD/scripts/tests、AutoClaude/tests（遞迴 271 檔）、LATEST fsm_runtime/tests 同款「磁碟機假路徑」病灶無機械訊號 | P3 | 本輪即修 | fixed@R12（掃描面擴為四樹〔LATEST 以 sdd_version.py subprocess 解析、失敗 fail-loud〕＋**per-tree** 掃描檔數下限釘選（10/15/217/59；SD 一審 SD-3 由全域 >200 升級——全域下限對單樹縮面不敏感）＋逐檔豁免清單附 WHY 與 stale 自檢〔test_perception.py＝Windows 專屬 mock 字串親讀 20 筆核可〕；**首跑即抓到本輪 GATE 新測試檔 2 筆磁碟機 mock 命中**——親讀核可為合法後補 `# platform-ok:` 行內標記，與 R11 首跑抓 `Z:/` 同款「守門有效性自證」） |
| DEF-101-175 | 2026-07-18 | R12 修復期整合縫（四修復專家平行作業的可預期縫隙，主 agent 接線時發現） | **三處測試整合縫**：①check_wrapper_thinness 兩測試 fixture 只鋪 dev_start 對，R12 新增 local_ci_gate 釘選後「檔案不存在」污染斷言；②test_dev_start 既有 longpaths 測試斷言 SUMMARY 精確等值，心跳註記附加後失配；③GATE 新測試檔 2 筆磁碟機 mock 命中擴面掃描（見 DEF-101-174） | P3 | 本輪即修 | fixed@R12（①缺席數改隨 `len(_PINNED_SHA256)`＋comment-only 測試釘選表縮至受測子集；②改斷言前綴保留原意圖〔誤觸發會以「已設」開頭仍紅〕；③行內豁免標記；縫合後根層 unittests 219 全綠〔R11 205＋GUARD/R12 新增案例；一審收斂後為 225，見 DEF-101-176〕） |
| DEF-101-176 | 2026-07-18 | R12 四方獨立複審（Architect/SA/SD/QA 一審**全 APPROVE、零 P1/P2**；SA 附條件=帳本空行 commit 前必修） | **R12 一審收斂彙總——4 P3＋15 P4 全數同輪修畢（含各方建議 defer 至 R13 者，依「所有問題徹底全修」鐵律不延）**：①ARCH-1 defer 足跡斷鏈→本列補明：**ARCH-R12-5**（subprocess `text=True` 無 encoding 的機械掃描守門）defer——存量 R9 全清（DEF-101-121）且本輪親驗零違規，「新增站點漏帶」守門待該類缺陷再現，與 DEF-101-071 defer 判準一致；**ARCH-R12-6**（smoke/CI/nightly 宣告式驗證矩陣 SSOT）defer——現行互鎖網（數字/標籤/清單三層）已覆蓋所有已知漂移類別，重構成本高於效益，待互鎖網再漏接真實語意漂移時重評；**ARCH-R12-7**（pytest 基線數字四處人工同步收斂單一出處）defer 至下輪文件批次——不為此單獨對三份 override 級文件開手術；②SA-1 帳本 202 空行切斷 live SSOT 表（P3）→刪除併回；③QA-1 薄殼釘選「表清空全綠」＋parity↔thinness 雙清單零交叉（P3）→鍵集合釘選測試＋`_check_thinness_cross_lock` 交叉鎖（缺鍵/多鍵雙向紅，3 案例紅綠自證）；④QA-2 gate 名單第二訊號單點化風險（P3）→pre-push 檔內＋ONBOARDING 雙載體明文「直跑 pytest 勿改經 local_ci_gate」；⑤SD-2 run_tlc 集合語意盲區→升級 multiset（floor 5→6，fixture 同步）；⑥SD-3 中立掃描全域下限→per-tree 釘選（10/15/217/59）；⑦QA-3 editable 哨兵零覆蓋→紅/綠 2 案（21→23 tests）＋QA-3/SD-5 測試行程狀態隔離 autouse fixture；⑧QA-5/SD-4 心跳檔名兩端零繫結→契約錨點測試 2 案（檔名一致＋呼叫位置在彙總後 exit 前，Windows 側同構）；⑨QA-9 MIN_TESTS 177→210 ratchet；⑩措辭批次：SA-2「step 全為 pwsh」訂正（實為 14 pwsh＋1 bash 刻意載具）、SA-3 `≥` 逸出字樣、SA-4 補 151 passed 可稽核選集、SA-5 補巢狀 session 變因、SA-6 政策句補 AutoClaude/tools/git-hooks/、SA-7/SD-6 liveness「開頭已內建」兩處改核心位置、SA-8 sdd_version 呼叫端補列、SA-9/ARCH-3 核心 docstring 時態與守門來源訂正、ARCH-2 `_conditional.py` 入帳、SD-1 `-PytestArgs` 邊角揭露入 docstring。ARCH-4/5 為 remark 級不動作；QA-4 於二審轉正——QA 二審揭露主 agent「既有 rename fixture」引用失實（該案例當時不存在，QA 以活體 EXP4〔.ps1 全改名 5v5〕實證產品碼行為正確），已補 `test_renamed_track_same_count_is_red_via_compare`（兩側等量僅名異、_compare 專屬路徑 meta 防護）使宣稱為真。**收斂後驗證**：根層 unittests **226** OK（219＋6 一審收斂守門案例＋1 二審 QA-4 轉正案例）、GATE 測試 **23** passed、parity 綠（含交叉鎖與 multiset 軌鎖）、thinness 綠、ruff 觸及檔集乾淨 | P3 | 一審收斂 | fixed@R12（本列即收斂記錄；二審四方全 APPROVE——SD P5 口徑註記、QA-4 引用失實〔補 5v5 改名紅案例轉正〕即時處置；**SA 二審兩條件同輪結案**：SA-10〔P3 操作性〕本輪觸及的 8 支 AutoClaude 既有 .py 帶 20 筆既有 ruff 債（HEAD 基線同版複驗零新增），staged 後 pre-commit 整檔 lint 必擋 commit——比照 DEF-101-089 先例順手清畢（13 筆 --fix 安全自動＋9 筆 E501 手動斷行〔含 1 筆 fix 連動〕；訊息重排經受影響測試 676 passed 驗無行為依賴），觸及檔集 ruff 全綠；SA-3R〔P4〕帳本 U+2265 逸出序列描述反斜線兩度被寫入管道吞掉，改全中文描述根絕並以 bytes 覆核收案） |
| DEF-101-177 | 2026-07-19 | R13 Python 掃描（AST 全量 765 檔）＋Architect 檢視（ARCH-R13-2：R12 defer 判準「該類缺陷再現」被打破——R12「親驗零違規」係行級掃描方法盲區，encoding= 在下一行的跨行呼叫誤判掩蓋真違規） | **AutoClaude/tests 樹 4 檔 8 站點 subprocess text=True 無 encoding（DEF-101-121 家族漏網樹）**：test_perf_regression_check.py 2 站點、test_steps_orchestrator_decomposition.py 1、test_topology_bridge_e2e.py 1、test_mutmut_exit_code.py 4——該樹從未入歷輪清掃盤點面（帳面盤點止於根 tools/tests＋SDD scripts/tests＋AutoClaude/tools）；zh-TW Windows 裸跑 pytest 時子行程診斷含非 ASCII tmp 路徑即 cp950 解碼炸或亂碼污染斷言；官方閘門 PYTHONUTF8=1 已保護故 P3 | P3 | 本輪即修＋機械守門落地 | fixed@R13（8 站點補 encoding="utf-8", errors="replace" 逐字對齊慣例；**ARCH-R13-2 守門落地**：新增 tools/tests/test_subprocess_encoding_hygiene.py——AST 判準〔尾名 run/Popen/check_output/check_call/call＋text=True 或 universal_newlines=True 常量＋無 encoding〕、六樹遞迴掃描〔根 tools／AutoClaude tools＋autoclaude＋tests／SDD scripts／LATEST fsm_runtime 以 sdd_version.py SSOT 解析、失敗 fail-loud 不縮面〕、per-tree 檔數下限釘選 18/36/160/217/24/129〔首跑實掃 23/46/200/272/31/162 八折取整〕、行尾 encoding-ok 豁免以 tokenize COMMENT 辨識〔字串內同形字不誤判〕＋stale fail-loud、parse 失敗列報；紅綠自證〔注入違規紅→修復綠〕；受影響 9 檔選集 301 passed/4 skipped） |
| DEF-101-178 | 2026-07-19 | R13 Python 掃描（cp950 可編碼性輸出探針全樹掃描） | **DEF-101-168 家族兩個目錄外漏網**：AutoClaude/scripts/migrate_file_to_pg.py（R12 清查面為 AutoClaude/tools 不含 scripts/，該目錄僅此 1 支 py）與 alembic/env.py 印 ❌ 到 stderr 無 UTF-8 防護——cp950 主控台亂碼降級（stderr 預設 backslashreplace 不崩潰）；alembic upgrade head 為 CLAUDE.md 明載手動指令、migrate 為選配 postgres 一次性維運工具，皆手動裸跑才踩 | P4 | 本輪即修 | fixed@R13（migrate_file_to_pg 於 main() 頂、env.py 於模組頂〔import 後、首個 ❌ 輸出前〕補雙流 reconfigure loop，逐字複製 seed_kb.py 既有慣例；py_compile＋受影響選集綠） |
| DEF-101-179 | 2026-07-19 | R13 Python 掃描 | **測試層檔案 I/O 無 encoding 潛在債 14 站點＋1 組反向意圖鎖**：test_gap010.py 4 處 open("w") 寫 yaml.dump（:401 帶 allow_unicode=True，playbook 欄位一旦含中文即 cp950 寫入炸）、test_gap039_049.py 1 處裸讀與寫端 UTF-8 不對稱、test_drift_log_snapshot.py 2 處＋test_mutation_baseline_lock.py 7 處 read_text() 裸讀 ensure_ascii=False＋UTF-8 寫的產物；test_perception.py bat-shim fixture 3 站點屬「維持預設反而正確」（.cmd 由 cmd.exe 以系統碼頁解讀、內容 ASCII-only） | P4 | 本輪即修 | fixed@R13（14 站點補 encoding="utf-8"〔drift_log_snapshot 兩處補後超行長，拆兩行避免新增 E501〕；bat-shim 加 3 行意圖鎖註解防未來「好心補 utf-8」修壞；受影響選集 301 passed/4 skipped；連帶依 R12 SA-10 先例清償觸及檔既有 ruff 債——15 筆 I001/F401 以 --fix 安全清＋10 筆 E501 手動斷行〔語意零變更，斷行後受影響測試 70 passed 複驗〕，觸及檔集 ruff 全綠） |
| DEF-101-180 | 2026-07-19 | R13 Architect 檢視（ARCH-R13-1＝ARCH-R12-7 欠帳，上輪明文排入本輪；主 agent 覆核裁定：Architect 對 README 站點的發現正確、文件掃描專家漏掃 README——兩專家分歧本身即「多站點人工同步不可維護」的又一自證） | **pytest 基線數字多站點人工同步已實際漂移**：活站點實為 5 檔（R12 盤點「四處」漏 AutoClaude/README.md 整檔——badge 行 8＋232/236/344/424/428 記 3,567/195〔2026-07-15〕，與根 CLAUDE.md／ONBOARDING §7／AutoClaude/CLAUDE.md 兩站點的 3,566/196〔2026-07-13〕兩組「現行基線」並存互斥）——ARCH-R12-7「漂移是遲早」預言成真 | P2（欠帳） | 本輪落地 | fixed@R13（**SSOT 收斂**：唯一站點＝ONBOARDING §7〔加 🔴 錨點句〕；其餘 4 檔全數去數字改指標〔README badge 改 tests-passing；歷史快照行加 baseline-ok 行內豁免共 7 筆附 WHY〕，兩組互斥數字以「去數字」免裁決消滅；**機械鎖**：新增 tools/check_pytest_baseline_sites.py〔判準＝行含 passed/skipped 且含千分位或 ≥4 位連續整數，badge URL-encoded 形態實測命中；非 SSOT 命中紅、SSOT 零命中紅〔anchor 自檢〕、掃描檔缺席 fail-loud、豁免行每次執行列印稽核〕＋9 個 fixture 測試〔含紅綠與缺席案〕；**接線**：root-infra-ci 第 11 道〔檔頭十道→十一道〕＋pre-push root leg 第 5 支守門＋兩支 compat workflow push paths 各兩區塊〔此為修復期整合縫——SDD 抓漏測試 test_ci_paths_cover_root_consumers 於 ci-gate 首跑即發動攔下、DEF-101-042 同構，補列後 9 passed、ci-gate 重跑 rc=0〕；ONBOARDING §6「四支→五支」與 §6.1 清單同步；MIN_TESTS ratchet 210→235〔實況 244〕；已否決替代案＝FRAMEWORK_STATUS 式生成器——基線是量測值非磁碟可導出，生成器只是搬運人工數字，Rule 2 否決） |
| DEF-101-181 | 2026-07-19 | R13 Architect 檢視（ARCH-R13-3）＋CI 掃描（CI-1：launchctl list／LaunchAgents／crontab 三查皆空、心跳檔不存在——R12 哨兵鳴叫屬實非誤報；另證實 DEF-101-081 帳務停擺持續：最近 12 筆 run 全 failure、root-infra job 2 秒終止、annotation 明載 billing/spending limit） | **主力開發機 nightly 排程「最後一哩」未啟用**：啟用動作多步手動（改 plist 絕對路徑→放置→launchctl load），CI 停擺期間本地 nightly 是唯一每日兜底層，R12 心跳哨兵「會叫」但告警閉環斷在執行端「沒人接」 | P2（ops） | 本輪落地（一鍵化） | fixed@R13（新增 tools/install_mac_nightly.sh——install〔BASH_SOURCE 推導 repo 絕對路徑→heredoc 產 plist〔02:00、鍵值對齊 ONBOARDING §8 範本〕→plutil -lint→launchctl 冪等 unload/load〕、--uninstall、--status〔launchctl 比對＋心跳三態鏡射 dev_start 語意、門檻同 8 天〕、--render-only〔僅產＋lint 供 smoke〕；bash 3.2＋BSD stat 相容、644 入庫以 bash 呼叫、非 Darwin fail-loud、全程 ${var} 大括號守 DEF-101-163；fake HOME＋launchctl stub 全循環驗證＋plist 真 plutil OK；smoke 新增 [6/6] render＋lint 步——PASS 10→11、MIN_PASS 同步 11、ONBOARDING 現行宣稱改 PASS=11〔歷史 PASS=10 宣稱改形不改值，test_smoke_ci_sync 文件↔釘選互鎖照章發動後轉綠〕；parity 單邊豁免登記；ONBOARDING §8 一鍵化改寫。**如實揭露**：真 launchctl 載入屬本機 ops 本輪未執行，待使用者核可後 bash tools/install_mac_nightly.sh 一鍵啟用；跨機盲區依 ARCH-R13-7 defer 附判準〔見 DEF-101-186〕） |
| DEF-101-182 | 2026-07-19 | R13 Architect 檢視（ARCH-R13-4）＋CI 掃描（CI-3 獨立交叉印證：git ls-files 全量比對掃描邊界，邊界外 tracked .sh/.ps1 恰 tools/lib 3 支） | **parity enrollment 掃描邊界漏 tools/lib/**：git_hooks_install_common.sh ↔ GitHooksInstallCommon.ps1 異名對等品（正是 R11 ARCH-3 記錄的邊界外對子）＋Find-GitBash.ps1 單邊零納管；且未來任何人在該目錄新增成對腳本零 enrollment 訊號——與 DEF-101-134「新增成對腳本為機械攔截」宣稱存在邊界縫隙 | P3 | 本輪即修 | fixed@R13（_PAIR_SCAN_DIRS 增列 tools/lib〔3→4 目錄，docstring 同步〕；4 筆單邊豁免附 WHY〔異名對兩筆互註＋test_git_hooks_install_common.py 行數守門註記；Find-GitBash＝PS 專屬探測 helper；install_mac_nightly＝launchd 專屬、Windows 對等＝schtasks 家族 fix_nightly_catchup.ps1〕；納管數 13 對＋11 單邊→13 對＋15 單邊〔查證該計數為工具動態輸出，無測試或文件釘選站點需同步〕；新增 3 測試案例斷言納管與豁免依據非空；parity 全綠、parity 測試 26 passed） |
| DEF-101-183 | 2026-07-19 | R13 Shell 掃描（SH-2；本輪 46 支 .sh/.ps1/hook 全量掃描唯一新發現——bash -n 27/27、pwsh Parser 19/19 零錯、PS7-only 語法與 API AST 掃描 0 命中、CRLF/BOM bytes 全數與 .gitattributes 一致） | **v0.30 run_self_evolution.ps1 PS 5.1 相容宣稱與自家文件化崩潰模式矛盾**：:83 註解自稱「避免三元相容 5.1」，但 :34 EAP=Stop × :142 rollback 段 git 指令帶 2 轉 null 重導正是 bootstrap.ps1/run_act.ps1/run_tlc.ps1 三處明載的 PS 5.1 NativeCommandError 模式——Windows autocrlf 警語走 stderr 即 FSE_ROLLBACK 中途中斷、工作樹停在半回滾態 | P4 | 本輪即修 | fixed@R13（比照同版 run_tlc.ps1〔DEF-101-124 先例〕於 param() 後加 PS 主版本小於 6 的 fail-loud 導流〔exit 5——1~4 已被既有語意占用，行內註明〕；:83 註解改寫與新現實一致；改後 bytes 取證 BOM efbbbf＋CR 166/166＋pwsh 7.6.3 Parser 0 錯誤。**如實揭露**：PS 5.1 真機導流行為未驗〔本 Mac 無 5.1〕，與既有 PS 5.1 續掛同批 Windows 輪） |
| DEF-101-184 | 2026-07-19 | R13 CI 掃描（CI-4~8 一致性批次；workflows 11 支全讀、全程僅靜態修改與本地驗證、未觸發任何 CI——帳務停擺＋額度紀律） | **workflows 一致性五處**：①root-infra pwsh parse＋BOM 掃描對 AISDLC_SDD/scripts 非遞迴〔現況子層無 .ps1 零實害，潛在縮面〕；②macos-nightly-alert 與 nightly-full 共用 concurrency group——同 group 單一 pending 位驅逐可使告警 job 靜默 cancelled 而非 failure〔告警路徑失效，R7 同類機制病〕；③windows-compat 兩處 dot-source dev_start 未帶 --no-sync，與 macos 側三處不對稱〔多一次網路 fetch〕；④autoclaude-ci 整檔與 chaos-nightly 無顯式 permissions 宣告；⑤EOL 守門方向不對稱——.sh 只掃 tools/ 而 .ps1 全庫 | P4 | 本輪即修（action 版本升級除外） | fixed@R13（①補 -Recurse〔本機 pwsh 實跑該 step body：19 支 active .ps1、0 error〕；②alert 改獨立 group macos-compat-ci-nightly-alert 附 WHY 註解〔僅靜態驗證〕；③兩處補 --no-sync〔查證 dev_start.ps1 $RestArgs ValueFromRemainingArguments 透傳＋dev_start.py --no-sync flag 存在＋macos 側同款寫法；僅靜態〕；④補 workflow 層 permissions: contents: read〔逐檔查證無 comment/label/push 寫行為；chaos-nightly 的 track-streak job 既有 job 層 issues:write 覆寫不受影響〕；⑤放寬為全庫 '*.sh' '*.bash'＋三處無副檔名 hooks 目錄〔前置盤點 git ls-files --eol 全庫 166/166 皆 LF 含凍結版，故不需排除凍結版；合成 CRLF/mixed 陽性兩案自證可攔；範圍依據入 step 註解〕；5 檔 PyYAML parse OK。**defer 存證**：action 版本升級〔checkout@v4→v5、setup-python@v5→v6 等〕CI 停擺無法實跑、屬行為變更不可盲改——判準＝帳務恢復後首個綠色窗口單一 commit 批次對齊至根層其他 workflows 現行版並以 workflow_dispatch 實跑各 job 全綠才視為關閉） |
| DEF-101-185 | 2026-07-19 | R13 文件 zero-trust 掃描（R12 fixed 宣稱 DEF-101-165~176 逐筆回溯 **12/12 屬實**、僅 1 筆計數級小失實即 DOC-9；五份指引文件全文逐行＋計數自數不信互抄；機械閘門 framework_status --check 與 snapshot_sync --check 實跑綠）＋主 agent 覆核 | **文件缺陷九項批次**：DOC-1 根 CLAUDE.md 軌道①③「下一份」欄與磁碟脫節 100 個檔號〔improving_02 vs 現存最大 101——防混淆總圖自身成事故源〕（P3）；DOC-2 兩份子 CLAUDE.md 合計 10+ 處引用全 repo 不存在的 AISDLC_v0.09/〔Agent 自動載入表 6 個 YAML、INIT、SELECTOR、命名規範等，照文件執行必失敗〕（P3）；DOC-3 AutoClaude/CLAUDE.md 檔頭 07-10 vs 元數據 07-17 日期矛盾；DOC-4「ADR 共 17 條」磁碟實數 18〔ADR-SD09-011 未同步〕；DOC-5 紀律 8 的 CRLF 逸出字樣自身格式毀損〔反斜線遭吞、行被物理切斷——SA-3R 同族〕；DOC-6 根 CLAUDE.md「AISLDC_SDD」錯字 3 行 4 處〔位於防混淆總圖節〕；DOC-7 AutoClaude/CLAUDE.md 本機 CI 對等段只列 .ps1 載具；DOC-8 useMacWin 兩版 [6/7] 未反映 R12 起心跳檢查；DOC-9 帳本 DEF-101-171 稱單核心「306 行」與該檔唯一入庫狀態 312 行不符〔SA-10 E501 斷行連動後敘述未回改〕 | P3/P4 批次 | 本輪即修 | fixed@R13（DOC-1 兩欄改 version-agnostic「現存最大號＋1、動工前 ls 實查、本欄不快照具體號次」；DOC-2 AutoClaude/CLAUDE.md 補未入庫警語段＋AISDLC_SDD/CLAUDE.md 兩處註記，皆改指 AISDLC_SDD 現行資產；DOC-3 統一 2026-07-19＋「權威以 git log 為準」；DOC-4 去計數改「以目錄實數為準」；DOC-5 全中文描述重建整行；DOC-6 錯字 4 處全修；DOC-7 補 macOS 四支 .sh 對照指向 ONBOARDING §6；DOC-8 兩版補心跳三態說明〔advisory 不會 ❌〕；DOC-9 本列即訂正——**DEF-101-171 帳面「306 行」應為 312 行**，歷史列不回改以本列為準；AutoClaude/CLAUDE.md 改後恰 400 行預算守住〔_check_claude_md 零行長違規〕＋元數據 v7.11） |
| DEF-101-186 | 2026-07-19 | R13 Architect 檢視（defer 足跡存證，防 ARCH-1 斷鏈教訓）＋CI 掃描（CI-2）＋各掃描零發現面存證 | **R13 defer 與裁決項存證**：①ARCH-R13-5 integration_gate.{sh,ps1} 雙實作收斂 defer 維持——Windows 腿依賴 Find-GitBash 於 Mac 無法真驗，判準＝Windows 真機在手輪與 PS 5.1 實測同批；②ARCH-R13-6 bootstrap.{sh,ps1} 為最後一對真雙業務邏輯〔非註解非空白 102/149 行——SA-R13-2 訂正 Architect 原引「68/139」為口徑不可重現值〕defer——零漂移前科＋變更頻率低＋3-step 標籤 parity 在位，判準＝出現一次真實語意漂移、或下次實質修改 bootstrap 邏輯時順勢收斂〔且須 Windows 可驗輪〕；③ARCH-R13-7 心跳哨兵跨機盲區〔Windows 深度 nightly 斷載在 Mac 永不可見〕defer——判準＝再次「事後」發現 Windows nightly 斷載超過 7 天，屆時落地 git 可見產物〔drift_log_history 最後 commit 時間〕的 mac 側 advisory；④ARCH-R13-8 新增 CLI 忘帶 reconfigure loop 無機械守門 defer——main() 樣板 AST 檢測過脆 false red 成本高，判準＝該類 console 崩潰/亂碼再現一次；⑤ARCH-R12-6 宣告式驗證矩陣 SSOT defer 維持，判準不變〔互鎖網漏接一次真實語意漂移即重評〕；⑥**CI-2 裁決項（送使用者）**：**6 個 daily cron＋2 個 weekly**〔SA-R13-1 訂正 CI 掃描原稱「7 daily」；daily＝drift-daily／arch-fitness／artifact-cleanup／fsm-chaos-nightly／autoclaude-ci ×2〕額度政策未隨 R11 降頻治理〔估月耗 1,500~2,700 分鐘量級，足以獨力吃穿 Free 額度；停擺期間不消耗僅每日產約 6 筆 failure 噪音〕——本輪保守不動排程，建議＝帳務修復前由使用者裁決：保留 chaos 每日〔Rule 9.9.4 硬需求〕、pg-e2e/perf/mutation/arch-fitness 降週頻或改 on-change；⑦AutoClaude/tmp_lint_check.py 臨時探查腳本殘留〔2026-06-12 入庫、自帶 UTF-8 wrapper、無跨平台問題〕＝雜物清理候選 remark，非相容性缺陷本輪不動作 | P3/P4 記事 | defer 附判準／送裁決 | fixed@R13（本列即存證記錄，無程式變更；零發現面同錄——Python：8 已修家族 zero-trust 複驗全數守住〔DEF-101-121/167/168、閘門 PYTHONUTF8、SIGALRM 守門、symlink fallback、bash_probe、shell=True 明文設計〕、fcntl/locale/multiprocessing/strftime/glob 順序等 12 類零發現；Shell：bash 4+ 特性/BSD-GNU 差異/引號展開/exit 語意/CRLF+BOM/工作樹弄髒/hook 鏈全零新發現；CI：artifact 路徑/cache key/continue-on-error 面/PYTHONUTF8/基礎設施檔全零缺陷、DEF-101-112 殘留 4 道重核對無新事實不升級） |
| DEF-101-187 | 2026-07-19 | R13 四方獨立複審（Architect/SA/SD/QA 一審**全 APPROVE、零 P1/P2**；SA/SD/QA 曾因 session 額度中斷夭折、以 SendMessage 保留上下文續作完成——續作前工作樹經守門快篩證實無攻擊實驗殘留） | **R13 一審收斂彙總——5 P3＋12 P4 全數同輪修畢（含各方標可選項，依「徹底全修」鐵律不延）**：①ARCH-R13-REV-1/QA-R13-2〔雙方獨立印證；QA 活體攻擊實錘「_SCAN_FILES 刪一行→守門綠＋9 測試綠＋塞回漂移數字照綠」零訊號〕→新增真實清單釘選測試 TestRealRepoConfigPinning〔5 檔＋SSOT〕＋同構延伸 TestScanRootsConfigPinning 釘 encoding 守門樹清單〔LATEST 名正規化、升版不失效〕；②ARCH-R13-REV-2 心跳契約第三站點〔安裝器 --status 硬編檔名＋門檻僅靠註解宣稱同值〕→test_dev_start 契約類擴 1 案錨定檔名＋HEARTBEAT_MAX_AGE_DAYS=8＋秒級比較迴歸鎖；③ARCH-R13-REV-3 跨樹守門「住所根層、掃描面跨子專案」錯位〔只改子專案 py 的 push 本地零觸發該判準〕→ONBOARDING §9 known-gap 明文承認〔Architect 核可之收斂形式；擴 dispatcher leg 成本效益待下輪〕；④SD-R13-1 安裝器心跳整數天除法在 (8,9) 天窗口誤判新鮮、與 dev_start 浮點語意背離〔8.5 天最小 repro〕→改秒級比較〔與浮點天大於 8 精確等價〕＋邊界紅綠實測；⑤SD-R13-2/QA-R13-1〔雙方印證〕空 WHY 的 baseline-ok 仍具豁免力→_raw_why 非空才放行＋案 10 紅綠自證；⑥P4 批次全修：ARCH-R13-REV-4/SA-R13-3 守門粒度＝檔案級誠實劃界入 docstring 與規則節、ARCH-R13-REV-5 四小樹納管〔根 .claude/hooks／AutoClaude/scripts〔恰為 DEF-101-178 實證漏網目錄〕／AutoClaude/alembic／LATEST .claude/hooks——六樹→十樹、新樹 floor 1/1/15/4、Architect 以同判準預掃四樹零違規〕、SD-R13-3 安裝器改 write-validate-install〔mktemp→lint→mv，防壞檔殘留〕、SD-R13-4 launchctl 比對改 awk 第 3 欄全字等值、SD-R13-5 docstring 補「別名匯入」與「encoding=None」兩劃界〔六樹實掃零站點〕、SD-R13-6 EOL 掃描補 core.quotePath=false〔非 ASCII 檔名引號包裹形漏掃〕、SD-R13-7 ONBOARDING §7「與根層 CLAUDE.md 一致」句收斂後成循環引用→訂正、SD-R13-8 parity 單邊 stale 訊息三成因分流〔對邊實存查證〕、QA-R13-3 macos-compat-ci smoke job 補 install_mac_nightly render＋lint step〔四向互鎖缺角，首次真跑取證待帳務恢復〕、SA-R13-1 CI-2 cron 計數訂正〔7 daily→6 daily＋2 weekly〕、SA-R13-2 bootstrap 行數口徑訂正〔68/139→非註解非空白 102/149〕、SA 觀察之安裝器訊息斷行空格連帶修。**remark 級不動作（附依據與再評判準）**：ARCH-R13-REV-6 三套豁免標記機制差異＝各載體刻意取捨、docstring 均誠實交代；SD-R13-9 PS 導流閘值 -lt 6 與 stderr 包裝實至 7.2 之落差＝6.x~7.1 全 EOL 實害趨零、與 run_tlc R9 D-1 先例一致性優先，判準＝任一檔再動該閘時兩處同步改 7.2 版本比較；ARCH-R13-REV-7 ARCH-R13-8 defer 判準「再現」是否含掃描實證之口徑張力留下輪統一。QA 攻擊 18 式全數紅該紅綠該綠、官方數字全部真機重現、還原取證與交付清單一致；SA 25 項數字重現 23 屬實 2 訂正、R12 兩類病灶〔表切斷/逸出自毀〕零復發；SD 審查中撞見 QA 活體探針中間態並自行排除誤判〔四方獨立性自證；探針一律走 scratchpad 之提醒已納規〕 | P3/P4 | 一審收斂 | fixed@R13（本列即收斂記錄；收斂後複驗＝根層 unittests 全綠〔案數隨新增釘選/契約案上修〕、五守門綠、smoke PASS=11、基線守門 11 案、encoding 守門 7 案。**二審 4/4 全 APPROVE〔皆無條件〕**：Architect 三條件親跑驗收全過〔含重放一審縮面攻擊證實縫已物理閉合；門檻錨點動態繫結 dev_start 常量獲評優於原建議〕；SA 收斂 3/3 精確、彙總列抽驗零失實、§7 訂正句保真覆核通過；SD 九筆逐項落地驗證〔秒級等價 8.5/8.0/8天+1秒 三點實測、set -e rc 傳遞逐路徑覆核〕、二審新攻五邊角全未命中；QA 六式攻擊驗收全過〔R2-1 縮面攻擊具名翻紅、R2-2 空 WHY 綠轉紅、R2-6 相似 label 誤綠關閉〕、還原取證與交付清單一致。**二審 remark 入帳〔顯示層/架構註記〕**：SD 三筆顯示層下輪順手——過期訊息「距今 8 天大於 8 天」表面矛盾〔判定走秒正確、顯示為截斷天數〕、install 情境 render 成功訊息印 temp 路徑、stale 分流於「成對＋縮面」複合情境偏訊息 a；QA 如實註記——TestScanRootsConfigPinning 釘選期望與組態同檔共存〔掃描器＝測試檔之固有架構〕，同檔改兩處可雙綠、惟 diff 審查醒目且 baseline 側為跨檔釘選，風險面接受留紀錄；Architect/SA 共同催辦——**帳本主檔達輪替上限 98%＋，下輪必須執行已結列搬遷 archive〔DEF-99-001 政策，crossref 每跑必示警〕**） |
