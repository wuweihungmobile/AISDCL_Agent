# AutoSDD Defect Log — Archive 33

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **13 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 5 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-558`、`DEF-101-587`、`DEF-101-555`、`DEF-101-588`、`DEF-101-529`、`DEF-101-530`、`DEF-101-575`、`DEF-101-570`、`DEF-101-584`、`DEF-101-585`、`DEF-101-586`、`DEF-101-583`、`DEF-101-582`
>
> **本次操作備註**：R60 round 3 收尾包（Pkg-F）動工前歸檔。**為何要在此時歸檔**：動工實測主檔 241,225 bytes，距 warn 245,760 僅 4,535，而本輪 round 3 四方複審尚須新增約 20 筆條目 ⇒ 不先歸檔必撞 warn（SA-R60R3 已警告「以現增速撐不到一輪」）。**六筆 --ack-handoff 的具名理由**（DEF-101-529／530／555／558／570／575）：六列狀態皆為 fixed@R60，判準④ 命中的交棒字樣經逐列人工判讀**皆非本列自身遺留的未承接交棒**——529／530 的 backlog 字樣出自「本列即是承接者」（分別承接 DEF-101-517／DEF-101-526）；555 的 backlog 出自它把 DEF-101-333／336／338 改派為未指派 backlog，而那三列以 open 續留主檔、承接語意不隨本列搬走；558／570 的「下一輪」屬散文（「下一輪讀到不存在的證據」／「下一輪不要重辯」）；575 的 backlog 出自它引述所治病灶的名稱。**刻意不搬的三筆**：DEF-101-566／567／573 本輪仍要就地訂正（SD-R60R3-07／SD-R60R3-04／QA-R60R3-01），搬進 archive 即因逐字保全而無法訂正——此即 SA-R60R3-03 指出的形態。**本輪新立的 DEF-101-589 起一律留在主檔**，不在本次搬遷面內。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-558 | 2026-07-28 | R60 round 1 四方複審（SA-R60-05／SA-R60-06／QA-R60-09／ARCH-R60-01 之帳本側） | **帳本家族文件自身四處失實／自我矛盾**：① `CrossPlatform_R60_Fix_Evidence.md:3` 宣稱逐字保全 `DEF-101-527`~`554` **全部**，實查只有 **26** 節（529~554），**無 527／528 兩節**，帳本那兩列的「完整證據見 …」是**懸空指針**；② `AutoSDD_Defect_Log_archive_30.md:5` 留著被同輪自己證偽的餘裕預測（「即使毛增 45KB 仍不觸及 240KB warn 帶」——實際毛增約兩倍、主檔一度衝到 **275,979** 撞破硬閘），與同一標頭末段「本標頭**不對餘裕做定性宣稱**」自我矛盾，且「本次歸檔自身有三項缺陷」漏列這第四項；③ `archive_05`／`archive_21` 在 `git status` 顯示 M 但 blob 與 HEAD 逐位元組相同（僅 stat-dirty），而 Pkg-5 曾把這兩檔的 M 推測為守恆算式的變因——該推測不成立；④ `archive_26:9`／`archive_27:9` 各有一處立帳指針宣稱 `DEF-101-493` 在主檔（**實居 archive_28**），`archive_25:11` 與主檔 :180／:183 的立帳指針則是無具體 ID 的散句 ⇒ 兩型皆逸出指針稽核（完整逐項證據見 `CrossPlatform_R60_Fix_Evidence.md` 的 `## DEF-101-558` 節） | P3（帳本家族自身記載失實；不影響程式行為，但本帳本是四方複審與後續輪的唯一 SSOT，失實指針＝下一輪讀到不存在的證據） | 本輪 round 2 帳本包全數訂正；指針稽核的**機械擴面**屬 `tools/archive_defect_log.py` 擁有包，已提跨包請求 | fixed@R60：① 證據檔補 `## DEF-101-527`／`## DEF-101-528` 兩節，標頭同時收斂為「本檔涵蓋 527~560、各節以 `## DEF-ID` 為錨」以消滅範圍歧義；② `archive_30:5` 的餘裕預測**就地標 ⚠️ 並指向訂正處**（比照 R59 SA-R59-P2-1 慣例，不刪原文），🔴 揭露段補**第四項**；③ round 2 獨立複驗 `git hash-object` 與 `git rev-parse HEAD:<file>` 兩側同 blob、`git diff --name-only` 不含此二檔 ⇒ **零淨變更、不可能是變因**（真變因是 CRLF 污染，已由 `DEF-101-528` 正確歸因），並在證據檔該段就地加 ⚠️ 訂正註；④ 五處指針全部改成帶 DEF-ID 且註明現居的可稽核形態。實測：修復前家族內「立帳見」命中 **11** 處（3 處無 DEF-ID＝主檔 :180／:183、`archive_25:11`；另 2 處有 ID 但居所宣稱失實）；修復後（含本輪新增的 8 處指針）**19** 處、**19/19** 皆帶可解析 DEF-ID 且居所與實況一致——權威輸出＝`python tools/archive_defect_log.py --check` REAL_RC=0 |
| DEF-101-587 | 2026-07-29 | R60 r3 Pkg-P11 自查（**非四方回報**） | **R60 證據檔逼近 256KB Read 上限且零機械守門**：`CrossPlatform_R60_Fix_Evidence.md` 距 `256*1024` 只剩約 1.3KB，而全 repo **無閘門量它的體積**——`check_defect_log_crossref.py` 的輪替守門只涵蓋帳本主檔與 `..._archive_*.md` glob ⇒ 再 append 一次就超過 Read 單次上限，複審者無法一次讀完證據 | P2（與 `DEF-99-001`／`DEF-101-123` 同型：「政策有上限、卻無機械守門」——R9 即這樣讓帳本默默長到 272KB；本檔是同一個病在**帳本家族之外**的復發，且它正是複審的主要閱讀對象） | 根層 `tools/` 體積守門＋拆分政策（本包無 `.py` 所有權） | fixed@R60 r3（主控接手——Pkg-P13 撞 API session 上限中途死亡且**零觸碰磁碟**，經 mtime 實查確認後由主控完成）：①**拆分**：以輪次為軸，round 3 的 19 節搬至姊妹檔 `CrossPlatform_R60_Fix_Evidence_r3.md`，入口檔**保留原名原地**（帳本有 13 處指向它，改名會讓那些指針全部失實＝`ARCH-R60-01` 家族的病）並加對照表；搬移以「移除對照表區塊後逐位元組等於拆分前的保留部分」機械驗證（非長度相減——長度對得上但內容被改一樣會過）。實測 260,963 → 入口檔 196,719 ＋ 姊妹檔 66,693。**零失實指針**：6 處具名節指針全部 ≤560、都留在入口檔。②**守門**：`check_defect_log_crossref.py` 新增 `oversize_problems()` 純函式 ＋ `_GOVERNANCE_DOCS` 具名涵蓋面，與帳本共用 `_LEDGER_*_BYTES`（**因為上限來源同一個**＝Read 工具單次讀取上限）。六向鑑別力實測：超線 fail／warn 帶只警告 rc 不變／正常靜默／具名檔缺席 fail-loud／涵蓋面清空後 0-0（證明該常數承重）。③**手驗機械化**：新增 `TestEvidenceFamilyPointersResolve` 4 支，把「帳本具名節指針必須在被指名的檔內找得到」「同一 DEF-ID 不得同時存在兩檔（＝拆分時複製而非搬移）」釘死。crossref 測試 51→61 支。🔴 落地時實際踩到並修正：涵蓋面原由 `_DEFECT_LOG.parent` 推導，測試 mock 帳本位置時治理文件跟著「搬去」不存在的位置 ⇒ 7 支既有測試假紅；治理文件在哪與帳本在哪是兩件事，已改為各自獨立解析並補一支鎖釘住 |
| DEF-101-555 | 2026-07-28 | R60 round 1 四方複審（QA-R60-04，blocking） | **七維掃描 34 項發現中有 4 項零落地——無修復、無帳本條目、`CrossPlatform_R60_Fix_Evidence.md` 的 `not_fixed` 亦零提及＝靜默丟棄**。四項為：【1】Scan-G **G-02**（CONFIRMED／P2）`DEF-101-333`／`336`／`338` 三列指向 **R41**（19 輪前）且全帳本家族查無改派；【2】Scan-A **A-03**（CONFIRMED／P3）`DEF-101-377` 的「R47 複驗已自癒、零 crlf」今日實測證偽；【3】Scan-C 反駁者自找 #1 後半：根層行尾守門的 `w/`（工作樹）半條判準唯一載具是 ubuntu-only workflow，在該平台結構恆綠、會紅的 Windows 側零載具；【4】Scan-G **G-03** 殘留：`DEF-101-422`／`435`／`470` 三列的「交棒他軌」全是死信。R59 的孤兒清掃只掃字面 `R58`，`R41` 一處未動（完整證據見 `CrossPlatform_R60_Fix_Evidence.md` 的 `## DEF-101-555` 節） | P2（治理缺口：掃描→落地之間沒有機械對帳面，四項全在「有結論但無承接」狀態被丟棄；且 G-02 違反的判準正是 R59 自己寫進 `CrossPlatform_Scan_Dimensions.md:149` 的孤兒規則——維度定義者自己漏掉三筆同型孤兒） | 本輪 round 2 帳本包逐項落地／逐項具名不修理由；【3】的機械載具跨包（`tools/tests` 擁有包），見 `DEF-101-557` | fixed@R60（帳本側四項全部落地，零靜默）：【1】333／336／338 三列各就地附「🔴 R60 改派」段（改派為**未指派 backlog**，體例比照 R59 `DEF-101-521` 對 `DEF-101-500` 的改派，並附 round 2 實測）；【2】`DEF-101-377` 附「🔴 R60 訂正」段（今日實測值＋根因訂正為「複驗紀錄必須戳上平台與機器指紋」）；【3】另立 `DEF-101-557` 記為**已知缺口**（具名不修＋可直接執行的落地建議）並提跨包請求；【4】422／435／470 三列各附改派段、訂正失實容器名，並在 `CrossPlatform_Scan_Dimensions.md` 新增**硬規則③（跨軌交棒回執）**。逐項實測數字見各該列狀態欄與 `CrossPlatform_R60_Fix_Evidence.md` 的 `## DEF-101-555` 節 |
| DEF-101-588 | 2026-07-29 | R60 r3 主控自查（**非四方回報**）——`--write --with-slow` 首次實跑當場炸開 | **主控在本輪新寫的程式碼裡，犯了同輪 Pkg-P10 剛修掉的同一個缺陷**：`sync_onboarding_baselines._run_cigate()` 以裸 `subprocess.run(["bash", ...])` 呼叫 `ci-gate.sh`，而本機 PATH 上的 `bash` 會解析到 **WSL 的 `C:\Windows\System32ash.exe`**。實測 rc=126、逐字 `scripts/ci-gate.sh: /mnt/c/Users/.../pyenv-win/shims/python: /bin/sh^M: bad interpreter`——`/mnt/c/` 是 WSL 專有前綴（Git Bash 用 `/c/`），該 shell 把 `python` 解析成一支 CRLF 的 Windows 批次腳本；連帶 LATEST 版本解析失敗、只測了凍結基線。 | P2（新引入之跨平台缺陷；`--with-slow` 是收輪回填的唯一路徑，壞了等於表② 永遠無法回填） | 根層治理工具（主控自持） | fixed@R60 r3：改走既有 SSOT `integration_gate_core.find_git_bash()`（回傳**絕對路徑**，把「PATH 當下解析到誰」這個變因整個移除），找不到即 `BaselineToolError` fail-loud 並明說「取值來源壞掉、不是文件 stale」。🔴 **本列的價值在於它坐實了 Pkg-P10 的判斷**：P10 說「本機是否觸發高度依賴啟動殼」，本案實證——同一台機器上 `shutil.which('bash')` 在某個 Python 行程回 Git Bash，而稍早 `subprocess` 實際跑到的是 WSL。⇒ **「本機沒觸發」永遠不能當成缺陷不存在的依據**。🔴 **更該記的是形態**：本輪耗費 13 個修復包在治「同一語意 N 份實作」，而主控**在寫治理工具時仍下意識寫了裸 `bash`**——證明這類缺陷靠紀律與 review 攔不住，只能靠「唯一入口 ＋ 機械鎖」。承接 R61：把「禁止裸 `bash`／`python` 呼叫」做成掃描鎖（同 `_local_cell_split_sites()` 的 AST 形狀偵測形狀） |
| DEF-101-529 | 2026-07-28 | R60 Scan-G 交棒的 DEF-101-517 backlog 解鎖條件（archive_30:53 明文：「下一輪先評估這兩條較便宜的路徑，再決定是否需要走 stage 路線」），由 Pkg-4 Scan-C 執行評估並落地 | **Windows 側執行級補償控制 `tools/windows_smoke_local.ps1`（PASS=12）沒有任何自動觸發器＝補償控制自己沒有心跳**。它正是 DEF-101-139 為「雲端 CI 帳務停擺（DEF-101-081）」而建的 Windows 側唯一活體驗證管道，而 R59 逐項 grep 實測確認 `run_local_nightly.ps1` 對它零呼叫、只能手動觸發（也解釋了它為何腐化到讓 R59 踩到 DEF-101-511）。mac 側對照：`run_local_nightly.sh` 的 `[1/4]` 每日自動跑 `macos_smoke_local.sh`。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P2（承接 DEF-101-517 原評級：補償控制無自動觸發器；本輪已收斂） | 本輪修復（Pkg-4 Scan-C）：落地交棒清單中的路徑①（獨立 schtasks 任務），路徑②（dev_start 心跳哨兵加提醒）評估後不採，理由見狀態欄 | fixed@R60：**評估兩條交棒路徑後選路徑①**（獨立 schtasks 任務），並在形態上選「任務集合」而非新增旗標——`install`／`-Uninstall`／`-Status`／`-WhatIf` 四模式一律作用於整組（AutoClaude_Nightly 02:00 ＋ **AutoClaude_WindowsSmoke 01:00**），故 `param()` 不需動、排程能力對照契約的 switch 集合仍是 {Uninstall, Status}、零 churn；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-530 | 2026-07-28 | R60 Scan-G backlog 接續（承接 DEF-101-526 明文交棒的 R60 候選；Pkg-7 落地） | DEF-101-526 記載的治理衝突「LOC tier 滿載檔 × lint 斷行互斥、且兩者都是硬閘」在 R59 只取行內 noqa 緩解，衝突本身交棒 R60 並指定方向：「把它列為固定掃描檢查點（**如** `check_loc_budget` 對餘裕 ≤ 3 行的檔印 warning），讓下一個踩到的人事先知道」。落地前實測確認缺口存在：`check_loc_budget.py` 只在 `over_by > 0`（已破線）才有 `[TIER]` 訊號，滿載檔（餘裕 0）與餘裕充足的檔輸出**一字不差**；…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） | P3（治理衝突的前瞻性告知缺口；現況無新增違規，但下一個動這些檔的人必然重踩） | 本輪落地（Pkg-7） | fixed@R60：`AutoClaude/tools/check_loc_budget.py` 新增 `TIER_WARN_MARGIN = 6` ＋ 非阻塞 `[TIER-WARN]` 段（逐檔列 tier／預算／LOC／餘裕，附三條可操作指引：修 E501 用行內 noqa 而非斷行、勿加 per-file-ignores、說明寫 `#` 不寫 docstring 因後者被 count_loc 計入），`--json` 同步曝露 `tier_warn_margin` 與帶 `headroom` 的 `tier_warn_band`。…（完整證據見 `CrossPlatform_R60_Fix_Evidence.md`） |
| DEF-101-575 | 2026-07-29 | R60 r2 QA 複審（`QA2-R60-06`） | **W605 存量轉義債無承接輪次，且原處置理由寫錯方向**：`ruff --isolated --select W605` 原有 **7 errors** 落在 3 支檔，皆登記在 `tools/tests/test_no_invalid_escape_sequences.py` 的 `_KNOWN_DEBT`，但**無承接輪次、帳本亦無對應條目** ⇒「無承接者的 backlog」＝本輪硬規則② 在治的形態，只是換了載體就逃過稽核 | P2（Scan-G 家族：backlog 換載體即逃過稽核） | 根層 `tools/tests/` | fixed@R60 r3。🔴 **修復時的重要反轉（Pkg-P3 實測發現）**：原名冊三筆的 WHY 都寫「處置＝整串改 raw（零語意變更）」，實測**該判斷是錯的**——raw 化會讓同一 docstring 內**既有的合法轉義**跟著改變 rendered 內容（三檔 `ast.get_docstring()` 皆 `differs_if_made_raw=True`）；故**首選改為「把該處反斜線加倍」**（對非法轉義是恆等變換，三檔 rendered docstring 的 len 與 sha256 前後相同），raw 只是次選。Pkg-P5 實測 `ruff` → `All checks passed!` rc=0，`_KNOWN_DEBT` **已空**。見證據檔 `## DEF-101-575` 節 |
| DEF-101-570 | 2026-07-29 | R60 r2 Architect 複審（`ARCH-R60R2-05`） | **「立帳見」第三種方言逸出稽核**：兩份治理文件都寫成 `立帳見缺陷帳本 DEF-…` 形態，以生產物件實測 `POINTER_RE.findall()` 回 `[]`（scope 當時只收 `本表`／`主檔`）、`_quotation_kind()` 回 None、檔案又不在 `_family_files()` ⇒ **同一動詞在家族內是硬錯誤、在家族外零檢查** | P2（稽核面缺口，兩處真實指針無人看管） | 根層 `tools/archive_defect_log.py` | fixed@R60 r3：根因＝稽核面與語法使用面不一致。**主控初裁(乙)「家族外禁用」後自我推翻**（禁掉語法＝把偵測面一起丟掉，而 `DEF-101-555` 是待 R61 承接的活列，被搬走該句即靜默失實），改採(甲) 擴面：scope 增收 `缺陷帳本`、稽核面擴為 `_pointer_audit_files()`、例外增為四種、(丁) 判定刻意留在呼叫端。**裁決全文見證據節，下一輪不要重辯**。Pkg-P5 實測 32／34 檔、rc=0，且本包自己的初稿被這道鎖當場擋下 3 筆 ⇒ 具鑑別力。見證據檔 `## DEF-101-570` 節 |
| DEF-101-584 | 2026-07-29 | R60 r3 Pkg-P7 自查 P7-4＋Pkg-P11 複核 | **收斂不完整＝把作廢宣稱鑄成永久史料**：round 2 推翻方案(乙) 時刪了 `check()` 程式，卻漏改 `apply()` 標頭手寫的「七項」保全判準清單與 `_fenced_line_numbers` docstring 的「判準⑦ 用」（該字面刻意不在本列複寫，WHY 見證據節） | P2（**archive 是零刪除史料檔，每跑一次 `--apply` 就把失實宣稱鑄成一份新永久紀錄**——與本工具立帳要消滅的病同型，同輪同工具復發。Pkg-P11 界定 blast radius：全部 archive 逐檔搜該字面**零命中** ⇒ 從未真的被 `--apply` 寫出過，屬地雷未踩到、非既成污染） | 本輪修復 | fixed@R60 r3：升為 `CHECK_CRITERIA`／`MOVE_CRITERIA` 兩 SSOT 常數，`check()` 成功訊息與 `apply()` 寫的 archive 標頭**全由常數生成**（項數現場算）⇒ 兩份說法結構上不可能出現；鎖見 `TestCriteriaListIsASingleSsot`（證據檔 `## DEF-101-584` 節） |
| DEF-101-585 | 2026-07-29 | R60 r3 Pkg-P7 自查（P7-5），沙箱重現 | **判準(6) 稽核面與判準(4) 不對稱**：治理文件內的「見主檔 DEF-x」失實宣稱**零檢查**——沙箱重現正則命中、`--check` rc=0、零訊號 | P2（同一類失實宣稱在帳本家族內硬擋、在治理文件內完全放行；判準(4) 已於 round 2 擴面而 (6) 沒跟上＝同工具內部稽核面自我不一致） | 本輪修復 | fixed@R60 r3：稽核面收斂到與判準(4) **同一份** `_pointer_audit_files()` SSOT，並補 (甲) code span ＋ (丙) 圍籬區塊兩種對稱例外。實跑（各處數以 `--check` 現場輸出為權威）：治理文件側命中**全落在例外內** ⇒ 零誤紅；家族側命中**零處**落在圍籬 ⇒ 補 (丙) 對家族稽核面零削弱。**刻意不設 (丁) 對等例外**：(丁) 是為「動詞在卻沒跟 ID」的誤報而開，而 (6) 的正則**沒有 ID 就根本不命中** ⇒ 無物可豁免（WHY 全文見證據檔 `## DEF-101-585` 節） |
| DEF-101-586 | 2026-07-29 | R60 r3 Pkg-P9 裁決（架構洞見立帳；`DEF-101-581` 根因歸納） | **SSOT 紀律必須與「引用面契約鎖」成套供應**。P9 原話：「這次的形狀比一般 refactor 事故更難防：**受害者是因為遵守 SSOT 紀律才被打斷**（它沒自寫第二份正則，照規矩引用上游）。所以任何『鼓勵引用 SSOT』的紀律都會同步放大『上游一改就斷』的暴露面 ⇒ SSOT 紀律必須與引用面契約鎖成套供應，否則等於一邊要求依賴、一邊不保護依賴。」 | P2（架構級紀律缺口；併記三點**閘門盲區**判斷——射程本來就不含、非實作漏洞） | `P7`＝「不准長出複本」前半套、`P9`＝「引用面不得懸空」後半套，方向相反、缺一不可；通用化承接＝**R61** | fixed@R60 r3（本輪僅補**單一鎖檔**的引用面契約；R61 承接＝那三支掃描純函式升級為 `tools/` 通用閘門、覆蓋全庫。細節見證據檔 `## DEF-101-586` 節） |
| DEF-101-583 | 2026-07-29 | R60 r3 Pkg-P11 自查（與 `DEF-101-580` 同機制、落在另一支工具且後果更重；**非四方回報**） | **`archive_defect_log.py` 的切欄複本會依誤讀的狀態把「狀態不明的列」真的寫進 archive**。實測構造：狀態欄留空 ＋ 分流去向欄寫「已於上游 fixed 故不另修」⇒ 修前 `_cells` 只切出 6 欄、`cells[-1]` 讀到**分流欄**、`cls='fixed'`、`blockers=[]` ⇒ **判為可搬** | P2（crossref 那側只是讀錯狀態並印出來；**本工具會依這個裁決真的把該列寫進零刪除的 archive** ⇒ 狀態不明的缺陷靜默下葬，與 `DEF-101-517`／`526` 誤搬同型） | 本輪修復（雙防線） | fixed@R60 r3：①切欄改以**表頭實測**定位欄位（不再靠 `if c.strip()` 過濾）；②新增搬遷判準⑤「欄數 != 表頭者一律不判讀狀態、一律不可搬」作為第二道防線（見證據檔 `## DEF-101-583` 節） |
| DEF-101-582 | 2026-07-29 | R60 r3 Pkg-P11 自查（**非四方回報**，如實標明無 finding id） | **ADR C1/C2 鎖以散文註解宣告帳本欄序，第四欄欄名與磁碟不符**：散文寫「現象與證據」，磁碟真表頭逐字為「現象與證據（file:line）」⇒ 散文與資料不符，且兩者之間**無任何機械關係可偵測**，任一側改動皆靜默 | P2（今日零活體後果〔該欄不被任何判準消費〕；價值在於它是「散文複本必然過期」的**現行實證**，且是新斷言落地**第一次執行就在真實資料上抓到**的一筆） | 本輪修復 | fixed@R60 r3：欄名散文升為 `_COL_HEADERS` tuple，並以新測試把「散文欄序／寫死索引／磁碟真表頭／閘門 `_table_layout` 定位結果」**四者對釘**（見證據檔 `## DEF-101-582` 節） |
