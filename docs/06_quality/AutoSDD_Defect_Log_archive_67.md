# AutoSDD Defect Log — Archive 67

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **147 筆已結列**（原搬遷候選 148 筆，`DEF-101-676` 因 `tools/tests/test_archive_defect_log.py::TestUnlockConditionIsMechanicallyChecked::test_headroom_matches_what_def676_claims` 硬性要求該列留在主檔作掃描標的而事後保留、退回主檔，不計入本檔）。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-764`、`DEF-101-268`、`DEF-101-243`、`DEF-101-392`、`DEF-101-755`、`DEF-101-418`、`DEF-101-235`、`DEF-101-733`、`DEF-101-758`、`DEF-101-798`、`DEF-101-703`、`DEF-101-797`、`DEF-200-214`、`DEF-101-693`、`DEF-101-336`、`DEF-101-795`、`DEF-101-889`、`DEF-101-238`、`DEF-200-096`、`DEF-200-189`、`DEF-101-866`、`DEF-200-063`、`DEF-200-132`、`DEF-101-561`、`DEF-200-021`、`DEF-101-206`、`DEF-101-388`、`DEF-200-090`、`DEF-200-131`、`DEF-200-161`、`DEF-200-044`、`DEF-200-178`、`DEF-200-069`、`DEF-200-123`、`DEF-200-087`、`DEF-200-101`、`DEF-200-162`、`DEF-200-085`、`DEF-200-112`、`DEF-200-156`、`DEF-200-068`、`DEF-200-081`、`DEF-200-127`、`DEF-200-208`、`DEF-200-119`、`DEF-101-917`、`DEF-200-026`、`DEF-200-117`、`DEF-101-560`、`DEF-200-150`、`DEF-200-144`、`DEF-200-130`、`DEF-200-020`、`DEF-200-022`、`DEF-200-114`、`DEF-200-126`、`DEF-200-160`、`DEF-200-092`、`DEF-101-936`、`DEF-200-010`、`DEF-200-149`、`DEF-200-052`、`DEF-101-941`、`DEF-200-151`、`DEF-200-138`、`DEF-200-176`、`DEF-200-192`、`DEF-200-148`、`DEF-200-153`、`DEF-200-184`、`DEF-200-067`、`DEF-200-187`、`DEF-200-140`、`DEF-200-110`、`DEF-200-082`、`DEF-200-102`、`DEF-200-070`、`DEF-101-978`、`DEF-200-045`、`DEF-101-726`、`DEF-200-093`、`DEF-200-104`、`DEF-200-046`、`DEF-200-164`、`DEF-200-050`、`DEF-200-059`、`DEF-101-377`、`DEF-200-066`、`DEF-200-049`、`DEF-200-107`、`DEF-200-073`、`DEF-200-091`、`DEF-101-919`、`DEF-200-055`、`DEF-200-115`、`DEF-200-154`、`DEF-200-135`、`DEF-200-143`、`DEF-200-097`、`DEF-101-351`、`DEF-101-998`、`DEF-101-242`、`DEF-200-061`、`DEF-200-064`、`DEF-200-152`、`DEF-101-297`、`DEF-200-111`、`DEF-101-565`、`DEF-200-054`、`DEF-200-174`、`DEF-200-062`、`DEF-200-120`、`DEF-200-179`、`DEF-200-074`、`DEF-200-060`、`DEF-200-071`、`DEF-101-200`、`DEF-200-180`、`DEF-200-109`、`DEF-101-068`、`DEF-200-095`、`DEF-200-108`、`DEF-101-918`、`DEF-200-181`、`DEF-200-122`、`DEF-200-204`、`DEF-101-500`、`DEF-200-058`、`DEF-200-146`、`DEF-200-219`、`DEF-200-139`、`DEF-101-278`、`DEF-200-080`、`DEF-200-210`、`DEF-200-077`、`DEF-200-056`、`DEF-200-057`、`DEF-200-103`、`DEF-200-076`、`DEF-200-078`、`DEF-200-072`、`DEF-200-079`、`DEF-200-094`、`DEF-200-177`、`DEF-101-596`、`DEF-200-100`、`DEF-200-218`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：`--apply` 原始輸出即 148 筆／108781 bytes（見本檔提交紀錄）；`--apply` 後跑
> `tools/tests/test_archive_defect_log.py` 發現 `TestUnlockConditionIsMechanicallyChecked::test_headroom_matches_what_def676_claims`
> 硬性要求 `DEF-101-676` 留在主檔作掃描標的，故事後把該列逐字移回主檔（原位置：`DEF-101-675`
> 與 `DEF-101-701` 之間），本檔同步移除、標頭筆數 148→147。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-764 | 2026-08-03 | R71 本包依任務書做「程式碼引用了帳本沒有的 DEF 編號」全樹反查時當場揭露（這條反查紀律本身即本輪的產物之一） | **同一個 `DEF-101-759` 被指派給兩個不相干的缺陷、橫跨 9 個程式碼站點，而全庫沒有任何機械物會說話**：(A) pyenv-win 無副檔名 POSIX shim（`tools/lib/WindowsAppsGuard.ps1:140`／`:183`，2 處）；(B) nightly 心跳彙總行解析／fallback 文案掩蓋探針失效（`tools/lib/baseline_origin.py:124`／`:318` ＋ `tools/tests/test_doc_loc_baseline_freshness_r60.py:76`／`:2579`／`:2617`／`:2664`／`:2700`，7 處）。兩者皆為**未提交新增**（`git show HEAD:` 對兩檔的 `DEF-101-759` 皆零命中），即本輪兩個平行 workflow 各自取號、彼此看不見對方。同一次反查另證實 `DEF-101-759`~`762` 四筆**全程未入帳**卻已被 16 個程式碼站點引用。**為何既有防線沒抓到**：`tools/check_defect_log_crossref.py` 的掃描面 `_CROSSREF_TARGETS` 只有 `ONBOARDING.md` ＋ 兩支 compat workflow ＋ `AutoClaude/docs/05_development/SD10_PG_Contract_NextAction.md` ＋ 根層 `docs/04_planning/ADR/ADR-*.md` glob ——**原始碼（`.py`／`.ps1`／`.sh`）完全不在掃描面內**。於是①「程式碼引用了一個帳本沒有的 DEF-ID」零訊號；②「同一個 DEF-ID 在程式碼裡指涉兩個不同缺陷」連概念上都沒有檢查（帳本側判準(2) 只管「同一 ID 在同一份檔內不得出現兩列」，管的是帳本內部，不是程式碼）。本次能抓到，是**人工**跑 `git ls-files` 與 `git ls-files -o --exclude-standard` 聯集全樹 grep、再與帳本家族的列集合做差集才抓到的 | P2 | `tools/check_defect_log_crossref.py`（掃描面／新判準）＋ (B) 的 7 處程式碼站點改號 | fixed@R92：(a) (B) 語意 8 站點已改號 759→763（帳本 763 列即 (B)，唯一一致解＝較小引用面）；(b)(c) 由 test_defect_id_reference_integrity.py 承載（11 passed）；DEF-200-* 缺口載體＝DEF-200-015；詳§G-764 |
| DEF-101-268 | 2026-07-23 | R25 四方二審/三審（SA 獨立在無其他審查員並行干擾下仍重現同款假紅，鎖定更精確根因） | **`tools/__pycache__/*.pyc` 位元碼快取寫入競態導致 pytest 偶發假紅**：R25 四方一審四位審查員並行執行時，SA／SD／QA 皆回報 `run_root_unittests.py`/`test_dev_start.py` 偶發假紅（`_WINDOWS_EXIT_DECISION_RE` 誤判「backend」/「high-end」為真收尾錨點）。主控最初判定根因為「並行審查員彼此對共用主樹做暫時性 bug-injection 互踩」（見上方 DEF-101-263⑤ 段落，Architect 二審親身重演佐證：`git checkout --` 誤用一度造成主樹真的短暫存在壞版本）。**三審階段 SA 進一步驗證**：在**單一 pytest 呼叫、無其他審查員干擾**的隔離情境下，仍以約 1/15 機率重現同款假紅；純 Python 緊迴圈直接呼叫 `_check_nightly_heartbeat` 連跑 3000 次零誤判（排除正則邏輯本身缺陷）；改查 `__pycache__/dev_start.cpython-311.pyc` 與原始碼 mtime 幾乎同一分鐘、加 `PYTHONDONTWRITEBYTECODE=1` 重跑 15 次零失敗——佐證另有一條獨立成因：pytest 對同一份共用磁碟 `.pyc` 的位元碼快取讀寫存在競態，短暫視窗內可能載入到修復前的舊編譯版本。**兩條根因並存、互不排斥**：跨審查員主樹突變互踩（[[parallel-mutation-audit-collision]]）與位元碼快取寫入競態，皆屬「多個行程同時存取同一份共用檔案系統資源」這一大類，只是載體不同（原始檔本身 vs `.pyc` 快取） | P4（純測試執行環境層面的間歇性噪訊，不影響任何生產邏輯正確性；R25 四方三審對本輪 4 項程式修復本身已一致 APPROVE，此項純屬環境可靠性觀察） | （原文照錄§G-268） | closed-by-decision@R92：維持現狀不停寫 bytecode（hook 延遲緩解器；__pycache__ 已 gitignore；基線量測會變動）；詳§G-268 |
| DEF-101-243 | 2026-07-22 | R18 四方一審（QA 獨立 bug-injection 反向驗證，複審時合記三項非阻斷測試保護力缺口） | QA 複審 R18 修復（`windows_smoke_local.ps1` 新增 `[7/7]` 步驟、`ONBOARDING.md`/`AutoClaude/README.md`/`AutoClaude/CLAUDE.md` 文件同步）時，用 bug-injection 與靜態核查揪出三項非阻斷測試保護力縫隙：**①（P3）`$MinPass` 數值本身的語意正確性無機械鎖**——`tools/tests/test_smoke_ci_sync.py` 只交叉比對 `ONBOARDING.md` 宣稱數字與腳本 `$MinPass`/`MIN_PASS` 釘選值是否一致，不驗證釘選值是否等於實際步驟數；QA bug-injection 實測「整段刪除 `[7/7]` 步驟本體、保留 `$MinPass=10` 不變」會被 `test_scenario_groups_consistent_and_floored`（分組標籤與總數不符）攔到，但「只改錯 `$MinPass` 數值、步驟仍在」這種情境無專屬鎖。**②（P3）`AutoClaude/README.md` sprint-verified badge 與 `AutoClaude/CLAUDE.md` 檔尾「最後更新」日期皆為純人工欄位、零機械新鮮度檢查**——`grep -rn "sprint-verified\|sprint_verified"` 全 repo 零命中任何驗證腳本，不同於同檔已被 `tools/check_pytest_baseline_sites.py` 機械鎖住的 pytest 基線數字。**③（P3）`tools/tests/test_dev_start.py::TestLaunchdNightlyLoaded` 對 `_launchd_nightly_loaded()` 只測了 `"darwin"`/`"linux"` 兩態，缺 `"win32"` 專屬測試案例**（R18 本輪把該函式判斷式改用 `platform_utils.is_macos()`，行為等價、既有測試仍全過，但 Windows 分支從未被專屬案例直接驅動過） | P3×3（QA 二審判定均非阻斷，R18 本輪合入不受影響） | （原文照錄§G-243） | fixed@R92：②badge 已刪、CLAUDE.md 檔尾日期已由機械 snapshot 取代（①③＝R19 修畢）；詳§G-243 |
| DEF-101-392 | 2026-07-26 | R48 Architect 架構複審（跨平台相容性架構深度評估，非缺陷掃描） | Architect 全面檢視現行跨平台相容性架構後結論「整體不需大改」（WindowsApps guard／`component_sanitizer` 共用抽層／CI 對稱化等既有機制已由 parity 測試鎖住，非隨意堆疊的戰術補丁），但點名一項值得正式決策的架構層議題：**Copy-on-Evolve 凍結基線政策已兩度（R44／R45）被迫打破鐵律回補**（fsm_runtime 路徑穿越 DEF-101-357/358、`_sanitize_component` 弱化版），根因同構（同一份邏輯散落 N 份凍結拷貝），而凍結版本數仍隨框架演化線性增長（現 29 支凍結＋1 支 LATEST），`ci-gate.sh` 亦從未機械觸碰中間 28 支，長期人工回補成本與相容性風險曲線可能已逼近政策原始假設（「凍結版不會再被戳到」）的邊界 | P3（架構政策層前瞻議題，非現行缺陷；不影響任何現行測試/CI 判準，兩次破例回補當下皆已個別走完人工核准流程） | 需要人工／PM 決策是否要就 Copy-on-Evolve 政策本身開一份正式 ADR 重新評估（例如：主動排查其餘凍結版重複邏輯、評估版本分級凍結、或維持現狀但提高逐版例外回補的機械化程度）——不屬於本輪掃描修復可自行拍板的範圍，比照既有「架構前瞻建議先開放討論，不代表本輪需動手」判例（如 DEF-01-009 watch item）處理 | closed-by-decision@R92：掌舵者 R84 已裁 CoE 政策（LATEST 原地改、CoE 僅快照時開新版），版本增長已停（實測 30 支同 R60 值）；ADR-XPLAT-001（Accepted）為判例 ADR；詳§F-392 |
| DEF-101-755 | 2026-08-02 | R70 依 `DEF-101-754` 做同型風險掃描（「還有哪些測試在 Windows 上造 stub、它們為什麼沒炸」）時查出 | **「Windows 側同構實作的行為鎖」在 Windows 上是殭屍**：`tools/tests/test_dev_start.py::TestGetPythonGeMinPowerShell` 的類別 docstring 逐字宣稱「Windows 側同構實作的**行為**鎖（本機零 Windows，故以 pwsh 真的執行）」並引 `ADR-XPLAT-002 §3.2`「字面比對型 parity 不算機械釘選——.ps1 的候選鏈必須真的被執行過」。但該類 3 支測試中**唯一真的執行 `Get-PythonGeMin` 候選鏈**的那支（`test_skips_sub_311_candidate`，:4850）掛著 `@unittest.skipIf(os.name == "nt", "shim 為 POSIX sh 腳本")` ⇒ **在 Windows 上必 skip**，另兩支是文字／正則比對。淨效果：這支 `.ps1` 唯一真正出貨的平台上，該類的行為鑑別力**等於零**，而 docstring 讀起來像它有。skip 理由本身誠實（shim 確是 POSIX sh 腳本），但那正是 `DEF-101-754` 同一根因的**另一個處置分支**——754 選了「兩平台各造合適形態」，本筆選的是「Windows 直接跳過」 | P2 | `tools/tests/test_dev_start.py::TestGetPythonGeMinPowerShell`（:4826~）／`docs/04_planning/ADR/ADR-XPLAT-002` §3.2 | fixed@R99｜條件(a) Windows 真機 4/4 零 skip 已達成；條件(b) Windows CI 實跑外部阻塞，拆出 DEF-200-185 追蹤，見 AutoSDD_External_Blocked_Log.md |
| DEF-101-418 | 2026-07-26 | R53 round 2 Architect 主動發現（非候選清單內，複核 DEF-101-414 `_JOIN_RE`/`_SLASH_RE` 修復時揪出的殘留不對稱） | R53 修復 `_JOIN_RE`／`_SLASH_RE` 對底線前綴 `_REPO_ROOT` 的零命中缺陷時，新增的前置負向 lookbehind `_NOT_WORD_BEFORE` 只防禦「前綴側」誤判（如 `FOO_REPO_ROOT`），未對稱防禦「後綴側」誤判（如假設變數命名為 `_REPO_ROOTS`／`REPO_ROOT2` 這類以 `REPO_ROOT` 為前綴的更長識別字，因無後向邊界檢查仍會被錯誤截斷命中）；程式化驗證確認全倉 `tools/tests/*.py` 唯二實際出現的識別字僅 `REPO_ROOT`／`_REPO_ROOT`，且下游 `_consumed_root_paths()` 仍有「磁碟上檔案實際存在」過濾器作為第二層安全網，故此為純理論殘留風險 | P3（現行零真實觸發案例；安全網自身尚未完全對稱的殘留缺口） | 復燃條件＝未來出現新命名慣例（如 `_REPO_ROOTS`／`REPO_ROOT2`）時需補強後綴側負向前瞻——已於狀態欄查證該條件從未成立，非待辦 | closed-by-decision@R80（包 C 實查：本列自訂的復燃條件是出現 `_REPO_ROOTS`／`REPO_ROOT2` 這類更長識別字；全樹搜 `REPO_ROOTS\b` 與 `REPO_ROOT[0-9]` 兩式合計命中 1 處、就是本帳本描述假設情境的這一列自己，程式碼零命中 ⇒ 條件從未成立。詳見 CrossPlatform_R80_Scan_Findings.md §C） |
| DEF-101-235 | 2026-07-21 | R16 全面掃描殘留 P3 觀察項（未修復，記事存證） | 本輪掃描發現但評估為低優先、暫不修復的殘留項：① `tools/dev_start.py` `_pid_alive`/`_list_pid_ppid_pairs_windows` 的 `ctypes.windll.kernel32.OpenProcess`/`CreateToolhelp32Snapshot` 未設定 `restype`（預設 32-bit，實際回傳 64-bit HANDLE），理論殘留無已知真實觸發；② `tools/windows_smoke_local.ps1:136` `Test-InstallRoundtrip` 假設 `.git` 是目錄，在 linked worktree 下會誤判，目前未觸發（呼叫點皆傳完整 clone）；③ `AutoClaude/tools/run_local_nightly.ps1:921` `Copy-Item` 更新 `nightly_latest.log` 指標無重試（不像同檔 `Add-LogLineSafe` 有退避重試），窄競態視窗；④ PS 5.1 版本守衛（DEF-101-124 家族）仍在 `run_tlc.ps1`/`run_self_evolution.ps1` 兩處逐字重複，未抽出共用模組 | P3 | 各項成本/風險評估後暫緩，是否觸發或另處理已依 ADR-XPLAT-011 §1 逐項裁決（詳見狀態欄，非待辦） | closed-by-decision｜ADR-XPLAT-011 §1 四子項皆有裁決：①維持已知邊界不修（附退場條件）②③訂正為已修復④訂正為架構前提改變。R99 書記逐項現查覆核 |
| DEF-101-733 | 2026-08-02 | R69 開輪唯讀 `gh run list` 核對 R68 交付物在雲端的實況 | 🔴 **本輪最重要的發現：本機綠 ≠ 對面綠，而落差偵測機制當時沒攔下來**。R68 押上 main 的 `375f291`，commit message 逐字寫「**收斂為閘門全綠**」——那句只涵蓋**本機 macOS 閘門**。雲端實況（唯讀實測）：`24c5f34`（R67）四支 CI **全綠** → `375f291` 的 `windows-compat-ci`／`aisdlc-sdd-ci`／`root-infra-ci` 皆 **failure**，僅 `macos-compat-ci`（與 `AutoClaude CI`）success ＝**全綠轉三紅**。根因＝`DEF-101-727`（POSIX 路徑字面值）、`728`／`729`（跨樹 import）、`719`＋`721`。**這是使用者訴求第 3 點「雙向落差」的活體標本**：本機唯一有真機的平台是 macOS，也恰好是唯一沒紅的那支 ⇒ **本機閘門對非 macOS 面結構性盲**。完整敘事見 `AutoSDD_improving_103.md` | P1 | 根層收輪流程（commit message 措辭紀律＋push 後複核紀律） | fixed｜pre-push 新增 push 前唯讀 gh run list 廣播，R99 書記核對程式碼與描述一致 |
| DEF-101-758 | 2026-08-02 | R70 由 `useMacWin.md` 包實查回報、本包複驗（`DEF-101-751` 的同型病長在**生產端每次開工都會跑的第一道檢查**上） | **`tools/dev_start.py::step_sync` 的工作樹乾淨判斷對未追蹤檔天然隱形**：`tools/dev_start.py:745` 逐字為 `_git("status", "--porcelain", "--untracked-files=no")` ⇒ 新建但未 `git add` 的檔案完全不在 dev_start 視野內。**第二層盲區**：該 dirty 警告位於 `behind > 0` 之後（`:774`），而 `:766` 的 `if behind == 0` 提前 return ⇒ **與 origin 同步時即使工作樹髒也完全不提醒**。與 `DEF-101-751`（untracked 對 `git ls-files` 掃描面隱形，害四輪四方複審＋多次全套閘門全綠、`git add` 那刻才翻紅）**同一個病**，差別只在 751 長在測試掃描器、本筆長在開工第一道檢查 | P3 | `tools/dev_start.py::step_sync` | fixed@R99｜_check_ci_liveness 委派 tools/lib/ci_run_status.py，test_dev_start.py 250 passed/12 skipped、check_loc_budget rc=0（R99 書記複驗重跑通過） |
| DEF-101-798 | 2026-08-04 | R74 Scan-E（掌舵者第 4 點：Windows 側低級錯誤的結構根因） | **治理鐵律與機械物大面積斷線**：根 `CLAUDE.md` 四條鐵律只有 1 條有機械物；鐵律三的 8 項觸發清單只有 4 項有掃描器，而 `DEF-101-766`（`$env:PATHEXT` ＋ 副檔名判斷）正好落在沒有掃描器的那兩格——有掃描器的那幾項，缺陷在寫出來的當回合就被擋掉，所以不會留到複審。另一筆同源：`AutoClaude/.claude/settings.json` 註冊 6 支治理 hook，根 `.claude/settings.json` 只橋接 1 支，而 Claude Code 不遞迴子目錄載 hook ⇒ 在 monorepo 根 session 下另外 5 支一行都不會跑，但根 `CLAUDE.md` 此前把其中一支寫成「強制」 | P1 | 本輪修復（機械物盤點表 shrink-only 棘輪 ＋ 宣稱↔註冊一致性鎖 ＋ 訂正射程措辭） | closed-by-decision@R92：4 支 hook 不橋根層、維持劃界（P0 判例＋雙向鎖 TestR74/TestR79 已釘）；詳§G-798 |
| DEF-101-703 | 2026-08-02 | R68 Scan-C／Scan-N 交叉命中（唯讀 `gh run list` 實查，零 Actions 額度） | **P1，本輪最嚴重**：兩支 `*-nightly-full`（`.github/workflows/windows-compat-ci.yml:982`／`macos-compat-ci.yml` 對應 job）自 2026-07-14 起 **18 天零成功、連續 5 次排程全紅**，橫跨 R48~R67（含 R67 一輪 40 筆修復）無人察覺。它們是**唯一**會在真 Windows／真 macOS 上跑完整 AutoClaude 測試樹的通道 ⇒ 「四支 CI 全綠」只涵蓋 **push 事件**。三道既有哨兵結構上都偵測不到：(a) `*-nightly-alert` 與被監控者同屬 GitHub Actions 計費平面；(b) `dev_start` 的 CI 活性哨兵只取 `--limit 1`、不分事件類型；(c) 心跳哨兵只讀本機 log mtime | P1 | 根層 `.github/workflows/` ＋ `tools/lib/ci_liveness.py`（本機平面外偵測） | closed-by-decision｜移入外部阻塞軌（GitHub Actions 帳務），見 AutoSDD_External_Blocked_Log.md |
| DEF-101-797 | 2026-08-04 | R74 Scan-H ＋ PKG-4（查 R73 主軸「鎖射程只圈一個站點」是否已治） | **同型復發三處，其中一處是 R73 才剛治過的那條鎖自己**：① `PATHEXT` 類判準只有一個站點級鎖（綁 `dev_start.ps1` 某函式），新寫的 Python 版 `os.environ["PATHEXT"]` 全 repo 零掃描；② `test_ps_engine_ssot` 的反增生鎖射程只到 `tools/tests/*.py`（**非遞迴**），射程外有活違規；③ `DEF-101-777` 宣稱射程已擴至整個 `tools/` 樹（實測成立），但偵測器逐行掃描＋短視窗**被一次換行繞過**，⇒ 樹裡此刻有一筆真違規而鎖是綠的 | P1 | 本輪修復（換行繞過修正 ＋ 射程遞迴化 ＋ PATHEXT 全庫形態掃描） | fixed｜test_ps_engine_ssot.py 多樹根＋AISDLC_SDD 端 pwsh 優先序已修，兩檔測試 rc=0（R99 書記複驗重跑通過；A1「必轉紅」宣稱經證偽，詳見 CrossPlatform_R99_Ledger_Closure.md） |
| DEF-200-214 | 2026-08-23 | R100 收尾窗口（主控自身失誤 D2，照實入帳） | **假陰性**：主控對一份**正確**的工具讀數宣告「可能有嚴重問題、今天所有配速裁決的分母都要重新檢查」，而決定性判別當下就在手上——物理約束「pct 大跌 ＋ 倒數也大跌」自我矛盾，一步即可排除。方向與 DEF-200-203 相反（那是把過期當現值，這是把正確判為壞），故不併列 | P2 | 判別式應寫成工具的一行輸出（`--reconcile` 已有輸入面，缺這條物理一致性檢查） | fixed@R103：`tools/lib/quota_reconcile.py` R1（隱含視窗內 pct 單調）＋R2（不得包住觀測到的 reset）判準已落地並掛在 `session_resume_planner.py --reconcile`；構造真實輸入 `reconcile('session',pct=50.0,minutes=396.0,window=300.0,...)` 實跑得 `verdict=incompatible`，CLI 端到端 `--reconcile "session=50%,396m"` 亦驗證可跑不崩潰 |
| DEF-101-693 | 2026-08-01 | R67 round 2 四方複審（QA-R67-04；CIPATH2 包） | **`DEF-101-640` 原處置欄把 `_CI_STEP_LOCAL_CARRIER` 的「macos-smoke 22 步 ＋ windows-smoke 22 步逐步歸屬」兩半並列陳述、未區分取證強度**：windows-smoke 那半邊是**讀** `tools/windows_smoke_local.ps1` 與 `AutoClaude/tools/run_local_nightly.ps1` **推得**的，R67 全輪無 Windows 機器可實跑核對；且 `test_named_local_carriers_actually_exist` 只保證指名的檔案存在，**不保證該檔真的執行了那件事**。此二限制原先只存在於當輪修復回報 JSON 內、repo 內 grep 零命中 | P3 | 交棒：**下一個 Windows 真機輪**在原生 Windows 上逐列覆核 windows-smoke 半張表（22 步）——已依狀態欄移入外部阻塞軌，非本帳本待辦 | closed-by-decision｜移入外部阻塞軌（Windows 實機），見 AutoSDD_External_Blocked_Log.md |
| DEF-101-336 | 2026-07-24 | R40 Scan-C（CI 腳本雙軌對等性掃描，追查凍結基線歷史一致性） | **DEF-101-056 記載已與現況不符**：該筆宣稱「v0.01～v0.29 依框架治理規則不予回改」，但兩天後的 commit `687abac`（2026-07-15）實際回改了 28 份「凍結版」`install_post_commit.ps1`（`Out-File -Encoding ascii` 改 `WriteAllText`），直接違反凍結基線 Copy-on-Evolve 鐵律，且**選擇性**回改（同批姊妹缺陷 DEF-101-057 未被回改，`v0.15` 現況第 4 行仍硬編 `.git\hooks\post-commit`）——DEF-101-056 記載成為與實況矛盾的假記事 | P2（帳本記載與實況矛盾，且揭露凍結基線鐵律曾被實際打破而無機械訊號攔截） | 本輪記事訂正 | closed-by-decision@R92：不建凍結版禁改機械鎖（ci-gate 兩軌＋FROZEN_SHELL_FORM_MAX 已守、deny 面有 P0 判例）；詳§G-336 |
| DEF-101-795 | 2026-08-04 | R74 Scan-M（查 AutoClaude_WindowsSmoke 的階段性退出條件） | **`AutoClaude_WindowsSmoke` 全庫查不到任何成文退出判準**，而它的立案前提（雲端 CI 帳務停擺、真機通道是唯一訊號）已經翻轉。本輪的反面證據要一併記入判斷：DEF-101-787 這筆 P0 是**雲端 runner** 抓到的、不是本機 smoke ⇒ 「真機通道不可取代」這個論點在本輪並未取得新證據，反而是雲端通道再次證明其價值 | P2 | 本輪落地退出判準草案（`tools/windows_smoke_local.ps1` 檔頭 ＋ 安裝器說明） | closed-by-decision@R92：掌舵者裁決 WinSmoke 排程**留任**（(a) R79 已達成、(c) 僅退場適用）；Windows session 應以 Get-ScheduledTaskInfo 驗 NextRunTime；詳§G-795 |
| DEF-101-889 | 2026-08-07 | R78 收輪回填包實測（乾淨 venv 首次被強制啟用時顯形） | **一句 provenance 涵蓋四格、卻只對一格負責**：`sync_onboarding_baselines.py` 的 `_run_autoclaude_pytest()` 釘死 `sys.executable`，而 `_run_cigate()` **不傳 env、完全繼承呼叫端 PATH**；`measure_provenance()` 卻把由 `sys.executable` 推導的 `interpreter=` 蓋在**涵蓋全部四格**的那一條錨上 ⇒ PATH 的 python 與 `sys.executable` 不同時，該欄對三格是假話。定性＝**工具缺陷不是操作缺陷**（`ci-gate.sh` 自身契約正確，缺 python 時會 fail-loud） | P2 | 承接輪次：**R79** | fixed@R99｜_run_cigate() 改以 sys.executable 推導 env，R99 書記複驗程式碼與描述一致 |
| DEF-101-238 | 2026-07-21 | R16 四方一審（SD 獨立審查，全庫搜尋比對兩個姊妹實作） | **`conditional.py` 的 `_SAFE_COND_PATTERN`（不含 `!`）與姊妹實作 `execution/mutation_applier/_conditional.py` 的白名單（含 `!`）字元集不完全一致**：後者無 `_DENY_CHARS` 第二層黑名單，純靠白名單過濾，理論上會放行 `!`。SD 確認 POSIX/cmd.exe 非互動殼下 `!` 無特殊展開意義，非安全漏洞 | P4（資訊性，兩套白名單字元集不一致但無安全危害） | （原文照錄§F-238） | closed-by-decision@R92：R55 收斂同源 deny-chars 族（SSOT＋parity 鎖）、R85 明文裁決 CONDITIONAL 白名單對**維持不收斂**並改名分義；詳§F-238 |
| DEF-200-096 | 2026-08-12 | R85／P8 `--pace` 實跑 | **任何週級軸 ≥ notice 門檻時，5 小時軸的加速結構上表現不出來**：session／five_hour 兩軸 band=free cap=None，而 seven_day 63%／far 經 `PACE_FAR` 0.5 夾成 cap=4 ⇒ 畫面只印最終 cap，看不出「誰在夾、為什麼」。**非 bug，是未寫下來的政策** | P2 | 政策寫進 ADR-XPLAT-005，並讓 `--pace` 印出夾點 | fixed@R103：`python3 tools/session_resume_planner.py --pace` 現查輸出已逐軸列出 `binding=seven_day` 與各軸 band／cap／note 明細（例：`kind=seven_day 28% 剩 7387 分鐘 band=free horizon=far cap=None note=burn-ahead`），原「看不出誰在夾、為什麼」的缺口已解除 |
| DEF-200-189 | 2026-08-22 | 交接載體根治包（ADR 自陳缺口的機械面） | `ADR-XPLAT-012` 條文五 §6「Phase 1 落地後 **5 輪**內須提 Phase 2 提案」在帳本家族內**零列**：當回合 `grep -c XPLAT-012` 主檔 **0**、66 份 archive **0 檔**。該 ADR `:290` 自陳「尚未有到期時點的具名常數……本輪未建立」⇒ 義務只活在散文裡，5 輪走完不會轉紅 | P2 | 比照 `_REPIN_NET_CAP_DUE_ROUND` 建到期常數；該常數住 `test_adr_xplat001_c1c2_lock.py`（非本包持有面，鐵律七） | fixed@R103：`pytest tools/tests/test_adr_xplat001_c1c2_lock.py -k Phase2FiveRoundDeadlineIsMechanical -q` 實跑 `7 passed`，到期常數已機械化落地 |
| DEF-101-866 | 2026-08-06 | R76 收尾｜手動 dispatch `windows-compat-ci.yml` 驗 nightly-full 修復 | Actions 帳務停擺復發（`DEF-101-081` 同型）：run `31023606162` 三 job 全 `conclusion=failure`、`steps` 長度 0、2~4 秒結束，annotation 逐字 `spending limit needs to be increased`；同 sha 的 push run 全 success ⇒ 額度在 21 分鐘內耗盡。連帶讓 smoke 排程退出判準 E1 當場失效 | P1 | 承接輪次：**R78**（額度恢復前 smoke 排程維持每日、不得退場） | fixed@R99｜條件(a) 本機 PS5.1 parse-error 紅綠對照已達成（R77）；條件(b) 雲端端到端外部阻塞，拆出 DEF-200-186 追蹤，見 AutoSDD_External_Blocked_Log.md |
| DEF-200-063 | 2026-08-11 | R84／W3 訴求 7 的未驗面與剩餘失明 | 🔴 **Windows 真機一項未驗**（本輪全程在 mac）：彈窗是否真的停止、`pythonw.exe` 載具是否解析得到、`-WindowStyle Hidden` 實效三者皆無憑證。剩餘失明：①該載具真機上可能整份不存在 ⇒ 守衛 fail-open 全靜默；②凍結 29 份 SDD `settings.json` 只被「份數相等」一條守著，不判形態 A~F；③`claude_md_freshness.py` 起子行程未帶 `CREATE_NO_WINDOW` | P1 | 真機驗收＝`claude -p --debug hooks` 取 `Hook SessionStart.*success` 並同時看負面表徵（只看其一必被騙） | closed-by-decision\|見AutoSDD_External_Blocked_Log.md |
|DEF-200-132|2026-08-15|R90 補洞包 F（洞 2 落地時撞牆）|**兩道鎖合起來使「登記」在任何單一持有面都做不到**：①以現名登記 ⇒ `test_check_defect_log_crossref.py::TestGovernanceDocRegistrationIsComplete` 的 `registered==glob(on_disk)` 紅，實測差集 `{'Quota_R90_CrossAccount_Experiment.md'}`；②改名 ⇒ `R89_HANDOFF.md:354` 反引號宣稱變幽靈（`resolve_doc_path`→`missing`）⇒ `TestR81GhostPathClaims` 紅，且 `_GHOST_PATH_BASELINE` 只准變小。同型先例＝該檔檔頭的 R80 死結|P2|收尾單人窗口原子做：改名＋登記＋同步 PRD 兩處與兩份 HANDOFF（鐵律七）|fixed@R100：兩鎖同綠（見DEF-200-131）|
| DEF-101-561 | 2026-07-29 | R60 round 1 四方複審（ARCH-R60-09） | 護欄層已與被護的生產系統同體積，該輪三個 blocking 假綠全在護欄層自己身上＝新鎖正以比它擋下的缺陷更高的速率生產新的未受檢面；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §12 | P3（架構趨勢判斷） | 不加同構的鎖，改做合併／精簡 | fixed@R61（首詞不變）：R61 已逐項回覆①②③並轉記 DEF-101-614。🔴 本列承載的「禁止新增鎖檔、只准合併／刪除」政策不因結案而失效，機械載體＝test_adr_xplat001_c1c2_lock.py 的 _FROZEN_GUARD_FILE_COUNT。詳見 CrossPlatform_R85_Ledger_Closure.md §12 |
| DEF-200-021 | 2026-08-10 | R83 訴求 6b（依額度水位自動決定扇出寬度）驗收 | 加速器 `tools/lib/quota_policy.py::_pace_of` 在 production **恆死**：live 快取現查 7 軸中 **3 軸**（`weekly_scoped`／`nimbus_quill`／`spend`）`resets_at` 恆 `null` ⇒ horizon 恆有 `none` ⇒ `min(1.0, fastest)` 永遠夾住，`_MULTIPLIER['near']=2.0` 這條加速路結構上到不了。且**無「我現在能派幾個」的 CLI 出口**（`session_resume_planner.py --help` 12 旗標無一為此）⇒ 6b 未成立 | P2 | 無期程軸不該參與 pace 聚合（那是常設 null 不是「暫時不知道」）＋補現查扇出寬度的 CLI | fixed@R84（回執 DEF-200-054） |
| DEF-101-206 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-9） | 深度 nightly 五軌（mutation/pg-e2e/perf/drift/obs）單押 Windows 一台實體機——R12 已如實揭露「心跳只能守本機」；CI 停擺＋R14 GitHub 深度軌降週頻雙重放大此單點（Windows 機斷載時五軌全球僅剩每週一 GitHub 窗口，且該窗口自身停擺中） | P4 | 記事存證；根本解＝帳務恢復（DEF-101-081）＋跨機心跳（超本輪範圍） | closed-by-decision@R84（記事存證、零行動項：根本解＝`DEF-101-081`，其現況＝需帳號所有者處理、無法以程式碼修復 ⇒ 語意是決策結案而非不修）｜與 DEF-101-081 交叉參照 |
|DEF-101-388|2026-07-26|R47 修復 DEF-101-386 時撞見|`v0.05`～`v0.29` 各版凍結的 FF-17 斷言（`arch_fitness.py`）仍認舊版 `ci-gate.sh` glob 慣用語（R10 已改走 SSOT `sdd_version.py`）⇒ 手動指定歷史版本時 pytest 先失敗；原文逐字保全於 CrossPlatform_R89_Closure_Evidence.md §DEF-101-388|P3（僅影響手動指定歷史版本的 debug 路徑）|不修復程式碼（凍結版依紀律不回改；比照 DEF-101-359／382，known-gap 記入本列）|closed-by-decision（舵手裁決存查 `R89_HANDOFF.md` §6d）：凍結歷史版不予 Copy-on-Evolve 例外——ci-gate 只測基線＋LATEST 兩軌，改中間版無機械物會驗。原文見同節|
| DEF-200-090 | 2026-08-12 | R84 收斂（F4；四方複審產出未落地為檔案） | R84 四方複審共 9 筆 blocking＋數十筆 findings，但**複審產出從未成為 repo 內的檔案**（收斂當回合 `git status` 實查：工作樹零份 R84 複審文件）⇒ 收斂端只拿得到派工書轉述的**條目標題**，沒有各條的證據、重現法與判準。SD-04~09、SA84-01~05、Architect A-01~A-09、QA-01／02／09 因此狀態**未知**，既不得讀成已處置、也不得讀成已否決 | P1 | 逐筆清單見 `CrossPlatform_R84_Scan_Findings.md` §G；R85 第一動作＝四方複審產出一律先落檔再收斂 | fixed@R99｜四方複審先落檔，複驗通過 |
|DEF-200-131|2026-08-15|R90 補洞包 F（縮減複審包 2 轉入）|治理文件的**唯一發現面是檔名前綴**：`tools/lib/governance_docs.py:198` `_GOVERNANCE_DOC_GLOB="CrossPlatform_*.md"` ⇒ 別的命名結構上掃不到，`unregistered_governance_docs()` 對它們恆回空。實例＝`docs/06_quality/Quota_R90_CrossAccount_Experiment.md`（PRD `:79`／`:1372` 兩處修憲唯一「依據」），未登記而 `tools/check_defect_log_crossref.py` rc=0 且逐字報「具名治理文件 44 份皆已登記」|P2|發現面改判「帳本級義務」而非檔名；上線前對 `docs/06_quality/*.md` 全量假紅普查|fixed@R98：glob 改樣式元組＋登記該檔；4 passed rc=0|
| DEF-200-161 | 2026-08-19 | R96 四方複審（SA／QA／主控三方獨立命中） | **`--pace` 印的可派數沒扣掉本視窗已用次數**：`pace_report()` 算了 `live_dispatches()` 卻只餵給 `record_burn()`，而 `pace_line()` 印的是 `recommended_fanout`（cap 本身）⇒「現在可派 2 個」與「已用 2 次故不執行」同刻並存；而根 CLAUDE.md 指定派工前查 `--pace` ⇒ 該入口直接誤導派工決策 | P2 | `pace_line()` 改印 `cap − live` | fixed@R96（包 B）：改印 `min(rec, cap−live)`，cap 與 live 兩個原始值都保留；鎖＝`WindowUsageIsToldTheSameWayByBothOutletsTest` 4 支 rc=0（本回合實測）；詳 Closure_Evidence §8 |
| DEF-200-044 | 2026-08-11 | R83 收輪：等待機制靜默失效 | **共同形態＝「等待／確認的機制自己靜默壞掉，失敗表徵與『還在正常進行』相同」**。①`nohup … &` 脫離完成追蹤＋漏掛 Monitor ⇒ **00:39→01:27 零工作 48 分**（舵手）；②`until ! pgrep -f <字面>` 並行兩支**兄弟互匹**（rc=0）不退出（本包）；本輪卡死 3 殼（舵手）；③讀 rc 接管線 ⇒ 真敗讀成 exit 0（舵手）。**會再犯**：失效面在指令字串與通知裡，不成檔案 ⇒ 掃描器看不到 | P1 | 正解＋總則落 `CLAUDE.md`〈鐵律六〉 | fixed@R84（回執 DEF-200-068）：**零攔截器**，見 `DEF-200-045` |
| DEF-200-178 | 2026-08-21 | R98 收尾（Win→mac 切換後在當前 HEAD 重跑 nightly 兩紅 stage） | **假綠 A**（真跑真綠、但結構上不可能紅）致 4 支假紅：`test_worktree_paths.py:83`／`test_block_destructive_git_r83.py::_as_windows` 注入 `ntpath` 卻未換分隔符，而 `worktree_paths.py:48` 舊版用 `os.sep`（不在 `os.path` 下）⇒ 混血平台。見 `CrossPlatform_R98_Mac_Closure_Evidence.md` §1 | P1 | 掌舵者於四種修法實測後裁決 A4：`os.sep`→`os.path.sep` ＋測試整包 `patch(os,"path",ntpath)`（2→1 patch），使「只換一半」結構上不可能 | fixed（R98 收尾）：`run_root_unittests` rc=0 Ran 3512 OK(skipped=44) |
| DEF-200-069 | 2026-08-11 | R84／W8 鐵律五普查的可重跑性＋鐵律三 floor | ①`CLAUDE.md` 鐵律五自陳「10,106 筆 git 指令片段、假陽性 0」在 repo 內**零可重跑產物**（`grep` 只命中那句宣稱本身）⇒ 落 `tools/probe/shell_command_corpus.py`（母體兩面各自標明、輸出可 diff）；②鐵律三棘輪的兩個 floor 常數各**落後表格現值一格** ⇒「拆掉一支掃描器就紅／刪掉一列危害就紅」實測不成立 | P1 | 兩筆皆以可重跑產物＋方向鎖收斂 | fixed@R84（回執 DEF-200-050）：抽取器第一版的錨只有 git token ⇒ waitform 恆 0 命中（零命中與「很乾淨」同形），已訂正 |
| DEF-200-123 | 2026-08-15 | R89 掌舵者戳破假前提（「我的 monthly spend limit 本來就滿」） | 主控把**錯誤訊息的字面**當成根因，寫進交棒書／多個 commit、還當前提餵給 Architect；落款實證該量連續 15 列皆 100.0＝**常數，不可能是變因**。既有 Stop 判準只認值域 ⇒ 對因果宣稱失明 | P1 | fixed@R89：形態判準 `error_literal_mechanism_hits()`（1,039 支逐字稿普查：命中 3／真陽 3／假紅 0）＋正向工具 `variate_contrast.py`。兩個被證偽的形狀與逐筆判讀＝`CrossPlatform_R89_Closure_Evidence.md` §DEF-200-118 | fixed（劃界：3 筆同一字面 ⇒ 其他訊息召回率未知） |
| DEF-200-087 | 2026-08-12 | R84 收斂（F4；四方複審 SD-03／QA-03） | 根 CLAUDE.md 兩節各**複寫**一份本該只有一個家的清單：①鐵律五〈擋不到什麼〉逐字寫「heredoc body」與「`subprocess` 直接跑 git」為擋不到，而本輪正是修掉這兩件（實測皆 exit 2）⇒ 複本在寫下的同一輪就成假話；②鐵律六寫「兩條判準」、`waitform_hits()` docstring 寫「三條」、`DEF-200-068` 的③又是另一件事＝**三個家三種內容**且互不一致皆零訊號 | P2 | 兩節改為指向 SSOT（hook 檔頭〈誠實劃界〉節／`waitform_hits()` docstring）＋現查指令 | fixed@R84：兩條現查指令實跑 rc=0 |
| DEF-200-101 | 2026-08-12 | R85／P3 無人看管授權邊界稽核 | **mac 上 `AUTOSDD_UNATTENDED` 擋不住 commit／push**：擋 git 歷史的 `_GIT_WRITE_RE`／`_GH_WRITE_RE` 只住 `lint_powershell_command.py`，其 matcher 實查為 `PowerShell` 且 `os.name != 'nt'` 一律 exit 0；姊妹 hook `block_destructive_git.py`（matcher `Bash\|PowerShell`）射程明文**不含** commit／push（:186） | P1 | mac 側開 Auto Pilot 前必補 | fixed@R103：`AUTOSDD_UNATTENDED=1` 餵 `git commit` 給 `.claude/hooks/block_destructive_git.py` 實測 `rc=2` 並擋下（stderr 明文：「這是掌舵者開啟 Auto Pilot 時的條件，禁止動 git 歷史」），mac 側該缺口已補 |
| DEF-200-162 | 2026-08-19 | R96 閘門取證專員 | **失敗證據是單槽固定檔名、每次覆寫、且 gitignored**：`tools/run_root_unittests.py` 的 `_FAILURE_LOG` 寫死 `tools/.last_failure.log`（當回合實查 `git check-ignore -v` → `.gitignore:76`）⇒ 下一次跑就把上一次的失敗證據抹掉，而「兩次跑出同一組紅」這種取證動作結構上留不下並排證物 | P3 | 改帶時間戳／HEAD 短碼的檔名並保留最近 N 份（形狀照 nightly log 的 14 天輪替） | fixed：檔名搬 `tools/lib/failure_log_rotation.py`（帶 HEAD 短碼＋時間戳），保留最近 8 份，`.gitignore` 改 glob；`SPECIAL_FILES` 具名調高 754→759 |
| DEF-200-085 | 2026-08-12 | R84 收斂（F4 文件包；四方複審 QA-B1／QA-06） | ①`improving_108`／`R84_HANDOFF` §訴求5 寫「本輪落 **38** 列（`046`~`083`）」，而**同格自附的現查配方**當場就給出別的值（訂正當回合＝39／`046`~`084`，此後每落一列再變）⇒ 被同格配方當場證偽；文件另一處自己就寫著 `DEF-200-084`；②同兩處寫 bytes 死結「三條治法都是放寬」，而 findings §E-4 與 `DEF-200-053` 分流欄各已具名一條**非放寬**出路 | P1 | 數字拿掉只留配方；「無非放寬解」訂正為「兩條出路皆未做，因工作量與持有面」 | fixed@R84：三份文件同輪訂正 |
| DEF-200-112 | 2026-08-14 | R88 開場（掌舵者指出訂閱窗與 fallback 池是兩件事） | 🔴 R89 實查訂正射程（R88 版過寬）：`reset_branch()` 早已分 `arm`／`notify`／`escalate`，escalate 不武裝哨兵 ⇒ ①「哨兵空等」是假的。真缺口只在兩個人機出口（`--pace`、閂鎖後訊息）說不出「只能等人」 | P1 | 接電既有 `throttle_horizon_line()`，**不新增 band 值**（R73 判例）；free 帶不印（否則宣告不存在的節流）。鎖＝`QuotaPaceOutletIsReachableTest` 3 測＋雙向注入自證。詳情＝`CrossPlatform_R89_Closure_Evidence.md` | fixed@R89（①資訊面；②cap 面為掌舵者裁決非缺陷） |
| DEF-200-156 | 2026-08-19 | R96 Windows 真機切換輪 | **節流訊息缺「本視窗已用幾次」⇒ 被擋者無法得知配額狀態**：一般扇出分支有印 `live`，而 Workflow 分支沒有 ⇒ 只能猜。實害已發生：本輪主控據此把 `cap` 誤讀成「同時活著的 agent 上限」而序列化四方複審 | P2 | Workflow 分支補印 `live`，與扇出分支對齊 | fixed@R96：`quota_throttle_message()` 的 `UNBOUNDED_FANOUT_TOOLS` 分支；鎖＝`WindowUsageIsToldTheSameWayByBothOutletsTest` rc=0（**不寫支數**：原記 4，Architect 實測 5，一律現查；原引 `test_quota_policy` 與本修無關，QA 訂正）；詳 Closure_Evidence §3／§8 |
| DEF-200-068 | 2026-08-11 | R84／W8 鐵律六（等待機制自己靜默壞掉） | 共同形態＝「等待／確認的機制自己壞掉，失敗表徵與『還在正常進行』相同」。兩個形態上 PreToolUse 極窄 lint：①`nohup … &`（脫離 harness 完成追蹤，R83 實帳 48 分鐘零工作）；②`until ! pgrep -f <字面>` 並行兩支**兄弟互匹**不退出。🔴 R84 訂正：原寫「三個」，③（讀 rc 接管線）零 lint，拆 `DEF-200-086` | P1 | 判準併入既有 `block_destructive_git.py`（已在讀 Bash 指令字串、平台中立），不新開觀測面 | fixed@R84（回執 DEF-200-044）：兩條各有注入探針＋真實語料實跑 |
| DEF-200-081 | 2026-08-11 | R84／W6 框架側的未竟事與共享 infra 紅 | ①新 `sdd-zero-trust-auditor` **未登記進 `AISDLC_SDD_INIT.md` 的 `auto_load_config`** ⇒ 沒有任何場景會載入它（檔在、但一次都不會被叫到）；②SSOT 自身互斥：`SDD_SPEC_FIRST_GATE.md` 與 `SDD_GUIDE.md` 對 SCG-2/3/4/6 的英文閘門名不一致；③`ci-gate` rc=1 的 6 筆全屬共享 infra：`aisdlc-sdd-ci.yml` 兩處 paths 缺 `tools/lib/sdd_latest.py`、`test_hook_wiring_cwd_safety.py` 2 支、`test_install_windows_nightly.py` 2 支 | P1 | ③的逐字修法見各包回報；**不得以降低 `checked>=3` 繞過** | fixed@R85：①②③ 全綠，見 §P9-1 |
|DEF-200-127|2026-08-15|R90 包 C（skip reason 登記的正解複核）|🔴 **repo 自己登記的正解是錯的**：`test_gap014_020.py:103`／`test_gap039_049.py:59` 的 skip reason 逐字要人「改用 fake-executor 重寫」，而該 fixture 組的是**另一條程式路徑** ⇒ 照登記正解做＝**換掉受測單元**。真正正解＝補一層 `PtyWrapper` patch，包 C 實證 11/11 PASS、斷言零修改。全文見 CrossPlatform_R89_Closure_Evidence.md §DEF-200-127|P1|補 patch 層而非重寫；並訂正兩處 skip reason 的錯誤指路。包 C 已獲批落地|fixed@R91（主控裁決，包 C 已落地）：見 CrossPlatform_R91_Scan_Findings.md §DEF-200-127|
| DEF-200-208 | 2026-08-23 | R100 收尾窗口（護欄層 C2，兩份取證衝突的定案） | 🔴 **兩鎖互為死結，兩包各對一半**：(a) `guard_line_problems()` **2 筆紅**（磁碟 `86452 → 87544`＝+1092 未重釘 ＋ 6 檔漂移、容忍 0）；(b) `repin_growth_problems()` 回 **`[]`**、R100 淨額 **355**／cap **850** ⇒ 綠。不重釘則 (a) 紅；重釘則 `355+1092=1447 > 850` ⇒ (b) 紅（超額 **597**）。**無不紅的路**`prose` 桶 +63（4119→4182）見§D-13 | P1 | 四方裁決 b＝一次性豁免（不得調 cap 或棘輪常數） | fixed@R101：例外名冊落地，凍結表重釘，drift=0，詳 `CrossPlatform_R101_Scan_Findings.md` |
| DEF-200-119 | 2026-08-15 | R89 收尾／SA B-1＋QA 複審 | **引用方向倒置**：`quota_policy.decide()` 的 `cap=min(cap,1)` 地板以 `R87_HANDOFF.md:20`「主力軸只有 1%」為立案事實，而該行逐字住在事故表的「**錯誤的證據①**」列。落款 `quota_burn.jsonl` 第 5~8 列證偽：`five_hour` 11 分鐘內 1.0→63.0（Δ62pp）⇒ 13 個 agent 跑了 634 秒才死，**不是被擋在派工口**；R87 死因至今未知 | P2 | 地板已拆（`cap=binding.cap`）＋原地留墓碑。`ca9985b` message 第③點含該筆已證偽引用（改不掉）⇒ 登記於 `CrossPlatform_R89_Closure_Evidence.md`；主體改動為純 PRD 推導 | fixed@R89 |
|DEF-101-917|2026-08-07|R79 XPLAT 包（新落差面與 .ps1 行尾）|git 索引檔案模式在 Windows 結構上不可觀測（`core.filemode=false`，27544 支 tracked 只有 7 支 100755），而 30 個版本樹的 tools README 教 mac 使用者裸跑 `./init_project.sh`（索引 100644，rc=126）|P2|新增只讀 `git ls-files -s` 的判準：文件教裸跑者須 100755、100755 檔檔首須是 shebang 且無 BOM|closed-by-decision（舵手裁決存查 `R89_HANDOFF.md` §6d）：LATEST 那支已改 bash 形態；凍結版 87 筆轉**受監視欠債**（`_BARE_SH_DOC_DEBT_FROZEN` 雙向精確比對）。證據見 `CrossPlatform_R79_Debt_Audit.md` 的 `## DEF-101-917` 節|
| DEF-200-026 | 2026-08-10 | R83 skip 剖面升為阻斷後的連帶稽核 | `tools/lib/skip_group_policy.py` 的 `tools/tests@darwin` 兩張天花板表皆 `platform: 44`＝**零餘裕**：實測 census 43→0、44→0、**45→2 筆** ⇒ mac 側新增任一支 skip 即 runner 轉紅。方向正確，但這是本輪新引入的耦合面 | P2 | 🔴 R83 收斂訂正（SA-10）：原文寫「把 44 支 `[MAC-NATIVE-ONLY]` 往聯集零 skip 收」＝標籤錯且語意反轉（該標籤的測試在 mac **會跑**）；被 skip 的 44 支是 `[WINDOWS-NATIVE-ONLY]` ⇒ 正解＝確認它們在 `tools/tests@win32` 跑到 | closed-by-decision@R84：只登記耦合面，非放寬 |
| DEF-200-117 | 2026-08-14 | R89 收尾（push 三次失敗後逐層診斷） | **pre-push 印 `All checks passed!` 卻回 rc=1**：git 對 hook 非零只印 `failed to push some refs`、無 `remote:`／`rejected` 行 ⇒ 外觀與網路／權限問題相同，`--verbose` 也不多說一字。真因在 log 中段（本輪為 root-infra 的 `--check-snapshot`），而尾段那句「全過」把它蓋掉。診斷成本＝三次 push，每次跑一遍全套閘門 | P2 | 未修。修法＝末段在 rc≠0 時印失敗摘要與定位指引，且**不得**印「全部通過」。診斷順序已寫進主控記憶 | fixed｜pre-push rc≠0 印失敗摘要不印全過，複驗通過 |
| DEF-101-560 | 2026-07-28 | R60 round 2 帳本包落地前斷言當場抓到 | 帳本列含未轉義字面 pipe 使該列被切成 8~9 欄，兩道閘門取到的「最後一欄」是散文碎片；全家族 15 列欄數異常（主檔 1＋archive 14）；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §11 | P3（閘門分類與真實狀態相反） | 主檔即時修；archive 側 14 列具名不修 | fixed@R60（首詞不變）：主檔 DEF-101-524 兩處 pipe 轉義後回到 7 欄並附零語意變更證明；row_arity_problems() 已落地、主檔零豁免。archive 側 14 列仍具名不修，理由與承接建議見 CrossPlatform_R85_Ledger_Closure.md §11 |
| DEF-200-150 | 2026-08-19 | R96 Windows 真機切換輪 | **替身掛在已不在鏈上的函式**：替身掛 `access_token`，而 R82 改道後真實鏈路是 `measure()→measure_detail()→token_detail()→_fetch_token()`（`quota_meter.py`，不寫行號）⇒ 該替身自 R82 起一行未生效。🔴 同一機制 **R91 已診斷並寫下正解**（R91_Scan_Findings §I-22：走生產注入點 `platform`／`runner`）⇒ 不得稱「十三輪無人發現」 | P1 | 替身改掛 `meter.token_detail`；未採 R91 正解，殘留風險見 DEF-200-167 | fixed@R96：該類 6 支 rc=0、0.014s（修前會真的打網路）；詳 CrossPlatform_R96_Closure_Evidence.md §2① |
|DEF-200-144|2026-08-16|R95 Pkg-B（PRD §15.5 紅線 10 結案載體，承接 DEF-200-115 的裁決缺口）|裁決落地：禁寫清單住 hook 內 `_GOV_EXACT`＋`.claude/hooks/*.py` 前綴一處；逃生口 `AUTOSDD_GOVWRITE_GUARD_OFF`（啟動 claude 前設，模型碰不到）；無人值守 exit 2、有人值守 rc=1 出聲不阻斷、專案根外同名檔放行|P1|載體＝`block_destructive_git.py` govwrite 族；matcher 擴為 Bash／PowerShell／Write／Edit／NotebookEdit（零新 hook 檔）|fixed@R95：回歸鎖 `TestGovernanceFilesAreReadOnlyWhenUnattended` 當回合實跑 10 passed＋19 subtests；九格 rc 矩陣＝CrossPlatform_R95_GovWrite_Evidence.md §3|
|DEF-200-130|2026-08-15|R90 包 E（掌舵者裁決：平台綁定的債改用平台條件記）|**欠債記帳模型由輪次改平台條件**：輪號記債在 mac 輪必然到期而做不掉（`DEF-200-112`）⇒ 出口只剩「再推一輪」。落地＝登記表 `_PLATFORM_BOUND_DEBTS` ＋ 真探針 `probe_pgvector_bge_m3_staging()`（門檻 1000 列 bge-m3）＋ 三態 payable／blocked／unmeasurable，住 `AutoClaude/tests/test_conftest_windows_native_skip_report.py`；注入自證實查 12 支|P2|已落地。`unmeasurable ⇒ 紅` 是 fail-loud 判斷非實測結論，可裁|fixed@R91（稽核複驗）：見 CrossPlatform_R91_Scan_Findings.md §DEF-200-130|
| DEF-200-020 | 2026-08-10 | R83 訴求 6e（睡眠中也要能被喚醒）驗收 | **不得宣稱達成**：mac 睡著不會醒，今天成立的唯一原因是 `pmset sleep 0` 這個**不在 repo 裡、不隨 clone 走**的機器設定（同 R73 把一台機器的安裝路徑寫成常數的判例）。當回合實測：`pmset -g sched` 輸出 **0 bytes**（零排程喚醒）、`pmset -g custom` → AC Power 段 `sleep 0`。⇒ 換一台沒設過的 mac，續航鏈在闔蓋那刻整條斷掉而**零出聲** | P1 | 改走由 repo 自己下的喚醒憑證（`pmset schedule wake`／`caffeinate`）並納入取證 | closed-by-decision@R85：憑證與劃界見 §P9-5 |
| DEF-200-022 | 2026-08-10 | R83 訴求 6C（85% 水位要做準備動作）驗收 | `prepare` 帶（85%）只有一個沒人印的 cap 值，**零準備動作**：沙箱實測 `PostToolUse×Read@86%` → rc=0／stderr **0B**／任務書 0 份；同水位 `PreToolUse×Agent` 亦 rc=0／0B／0 份 ⇒ 兩事件都靜默。而落點已無餘裕：`tools/lib/quota_gate.py` 現查 **400/400、headroom 0**（`guardrail_lib` tier）⇒ **必須先騰位**，合法出口只有刪死碼／docstring 改 `#` 註解／拆職責 | P2 | 先騰 LOC 再實作 85% 準備動作；**不得調高 LOC 預算**（那是把債寫成合法額度） | fixed@R84（回執 DEF-200-055） |
| DEF-200-114 | 2026-08-14 | R89 查證換帳號後快取是否沿用舊帳號讀數 | `account_posture()` docstring 宣稱 `plan_fingerprint` 用途是「方案變更 ⇒ 燃燒率作廢重學」，該用途**零實作**（唯一消費端只印字串）。後果：`quota_burn.jsonl` 去重鍵只有 `measured_at` ⇒ 換帳號後新舊樣本混算攤提，不隨 TTL 自癒 | P2 | （原文照錄同上 §6） | fixed@R89：詳§F-114 ｜R94 二次訂正（Architect REJECT 承接，取代 R93 版判讀，account_key 已解同方案換帳號盲區）：逐字保全於 CrossPlatform_R91_Scan_Findings.md §J-1，交叉引用 DEF-200-122／ADR-XPLAT-009 §2.2/§6/§4 |
|DEF-200-126|2026-08-15|R90 包 C（PlaybookRunner 六角接縫調查）|`playbook_runner.py:113-116` 註解宣告 IExecutor／IEvaluator／IBrain「可選注入」，實測三個 `_port_*` 的 AST Attribute 節點**全為 `ctx=Store`、零 Load**，24 個建構點零個傳該 kwarg、運行時探針讀 0 次。危害＝**誤導修法**（傳 fake executor 什麼都不會發生），`lint-imports` 對此失明。逐項實測見 CrossPlatform_R89_Closure_Evidence.md §DEF-200-126|P2|拆除（LOC 實測 −8、方向正確、無鎖阻擋）。包 C 已獲批落地|fixed@R91（主控裁決，包 C 已落地）：見 CrossPlatform_R91_Scan_Findings.md §DEF-200-126|
| DEF-200-160 | 2026-08-19 | R96 四方複審（Architect） | **`_RUNTIME_SKIP_CEILING_MAX` 的 shrink-only 宣稱零觀測者**：`tools/lib/skip_group_policy.py` 該表上方逐字寫「天花板只准降」，而當回合實查全 repo **無 `_FROZEN_` 對應常數、無方向鎖**，唯一判準只比「基線 vs 天花板」⇒ 兩個一起加大即無判準可擋（靜態實查，未跑注入） | P2 | 補方向鎖（天花板只准下修），形狀照既有 `_FROZEN_*` 史料對照；或把兩表的差額做成棘輪 | fixed：新測試檔補凍結快照＋方向鎖（放測試檔非生產模組，該檔僅餘 2 行 LOC）；含紅綠自證，6 passed |
| DEF-200-092 | 2026-08-12 | R85／P6 框架側幽靈依賴掃描（四桶） | 四桶幽靈依賴判準的假陽性使掃描結果不可讀：擴面前 **302 筆**命中、收斂後 **0 findings** ⇒ 舊判準幾乎把全部命中都算成違規，而「三百筆要逐一辯護」的鎖活不過一輪（同 `DEF-101-867` 訊噪比判例） | P2 | P6 本輪擴面並收斂 | fixed@R85：🔴 立案數字經 R85 收尾由 199／198 訂正為 302／301（低報約三分之一，假數字住三個家）；今日活分母＝1，性質經合成注入確認為**純寫入面判準**非裝飾品。逐筆證據＝`CrossPlatform_R85_Ledger_Closure.md` §R85-CLOSE-AGT11 |
|DEF-101-936|2026-08-08|R80 包 F S8-01 凍結版|同型缺陷在凍結版 v0.01~v0.29 的 `init_project.sh` 共 116 個站點（29 支 × 各 4 行）。Copy-on-Evolve 禁改 ⇒ 本輪不修，登記為可見欠債 `_SELF_HELP_DEBT_FROZEN = 116`（雙向精確比對：多一筆＝新增同型缺陷，少一筆＝有人動了凍結版）。附帶風險：`--sdd` 預設版本 `0.01` 正指向壞的那一批|P2|交棒（需 Copy-on-Evolve 例外授權，歷來三次例外皆經掌舵者明文核准）|closed-by-decision（舵手裁決存查 `R89_HANDOFF.md` §6d：不予例外，轉受監視欠債）：詳見 CrossPlatform_R80_PackF_Posix_Evidence.md S8-01 節|
| DEF-200-010 | 2026-08-10 | R83 mac 側首次建 `run_act` runner 映像 | **mac 專屬**：Apple Silicon 上 `.actrc` 的 `--container-architecture linux/amd64` 走 QEMU，而 buildkit 的 deadline 涵蓋「拉 2.29GB 基底 ＋ 跑 RUN 層」整段 ⇒ 回 `DeadlineExceeded`；把 `docker pull` 拆出來先做即可（RUN 層本身只 6.4s）。**本列零機械物零站點**：當回合 `grep -rn DeadlineExceeded`（排除 .git／.venv）全庫 0 命中 | P3 | mac 側建映像步驟先 pull 再 build，並寫進 ONBOARDING／useMacWin 的 act 前置 | fixed@R91：站點已落 ONBOARDING.md Docker 列，見 CrossPlatform_R91_Scan_Findings.md §DEF-200-010 |
| DEF-200-149 | 2026-08-19 | R96 Windows 真機切換輪 | **共用 fixture 預設值靜默關掉 3 支 Windows 專屬接線鎖**：`_isolated_env()` 自 R84 起預設 `real_scheduler=False` ⇒ `AUTOSDD_SENTINEL_OFF=1`，而 `arm_when_earned()` 首行讀它即 return disabled（`context_budget_guard.py:814`）⇒ 1 真紅 ＋ 2 假綠；該組帶 `[WINDOWS-NATIVE-ONLY]`，R84~R95 十二輪在 mac 全 skip | P1 | 兩處改 `real_scheduler=True`（替身 planner 不碰真排程器） | fixed@R96：`SentinelWiringTest._sessionstart`／`._posttooluse` 兩處（不寫行號＝不會過期）；該組 11 支 rc=0；詳 CrossPlatform_R96_Closure_Evidence.md §2② |
| DEF-200-052 | 2026-08-11 | R84 收斂（C5 帳本包，bytes 死結第一刀） | 兩條線分開處置：①**主檔 bytes** 由 `archive_66` 洩壓（10 筆已結列，`226077 → 219925`，釋出 6152 B）；②**`_UNPINNED_HANDOVER` 死結**解除——`DEF-101-206` 結案觸發 stale ⇒ 白名單 6→5、天花板 6→5、`UNPINNED_HANDOVER_CEILING_HISTORY` 追加 5（tail=1 未逾 `_SEAL_TAIL_MAX`，封印不必動） | P1 | 刻意**只搬 ≤700 B 的已結列**：搬超長列會讓豁免當場過期並要求下修 `defect_ledger_index.py` 兩個天花板，該檔非本包持有面 | fixed@R84：crossref rc=0、`archive_defect_log.py --check` rc=0 |
| DEF-101-941 | 2026-08-08 | R80 包 F BSD 掃描 | BSD vs GNU 差異 22 類（`sed -i`／`readlink -f`／`stat -c`／`base64 -w`／`grep -P`／`find -printf`／`sha256sum`／`readarray` 等）在 29 支 active 腳本命中 0；載具自驗 22/22 條 regex 對已知壞樣本皆命中。今天零違規，但這 22 類無任何機械物在守 | P3 | 交棒（家在 `tools/tests/test_bash32_compat.py` 的 `_PATTERNS` 與 `tools/macos_smoke_local.sh` 散文清單，皆跨出本包持有面） | fixed@R98（本輪已修復）：`base64 -w`／`sha256sum` 兩條補進 `_PATTERNS`＋`_BAN_TOKEN_SAMPLES`＋散文清單；`test_bash32_compat.py` → 31 passed rc=0 |
| DEF-200-151 | 2026-08-19 | R96 Windows 真機切換輪 | **守衛拿 `os.sep` 字面比對未正規化路徑 ⇒ Windows 放行條件恆假**：`_DISPOSABLE_WT` 以 `os.sep` 建，而 `_resolve_dir()` 刻意不正規化 ⇒ 混合分隔符比不到 ⇒ routine teardown 全被誤擋，而該守衛普查註解自陳這一類佔新增命中 100%、零事故形態 | P2 | 比對前 `.replace` 正規化（POSIX 為 no-op）；零程式行增加（該檔 tier 750／餘裕 0） | fixed@R96：`test_block_destructive_git_r83` 全檔 rc=0（**不寫支數**：包 C2 同輪仍在增補，一律現查）；LOC rc=0；詳 CrossPlatform_R96_Closure_Evidence.md §2⑥ |
|DEF-200-138|2026-08-15|R92 機械 compaction＋84/94 訊號帶設計包|`autocompact_posture()` 判 enabled 只讀 `~/.claude.json` 與兩個 kill env；官方 settings.md 載明 `autoCompactEnabled` 也住 settings.json 頂層 ⇒ posture 對整條 settings 鏈失明，`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` 現值亦無處可見|P2|已修@同輪：判定鏈第 3 站＝settings 鏈逐檔各讀、任一層 false 即點名；報告加 pct override 行；解析改走 `guard.settings_value`。設計＝`ADR-XPLAT-008`|fixed@R92：控制組親測 `--check-autocompact` rc=0（第 3 站＋pct override='90' 行實印）；注入組 rc=1 逐檔點名[設計包回報]|
| DEF-200-176 | 2026-08-21 | R98 PRD Gap Analysis（AutoClaude↔AISDLC_SDD 橋接複驗） | `SddToPlaybookAdapter._read_contract_spec()` 用 `sorted(candidates)[0]` 挑檔；AISDLC_SDD 官方模板同時有 `TEST-CONTRACT-SPEC-*`／`CONTRACT-TEST-SPEC-INTEGRATION-*`／`ENV-CONTRACT-SPEC-*` 等皆含「CONTRACT+SPEC」的檔名，字母序可能讓 adapter 靜默讀錯規格檔，SCG 閘門仍會「通過」（讀到的仍是一份規格，只是不是對的那份） | P2 | 詳見 `docs/06_quality/CrossPlatform_R98_PRD_Gap_Analysis.md` §4.4；本輪僅發現、未修 | fixed@R99｜官方命名優先+無法唯一鎖定fail-loud，84 passed複驗通過 |
| DEF-200-192 | 2026-08-23 | R100 配速診斷包（`--pace` 第三行複核） | **攤提具名的軸不是它實際取值的那一軸**：`quota_pace.amortize()` 舊實作以 `Amort(short[0][0], total[0][0], …)` 具名（＝同窗長清單的**第一**軸），而 `remaining`／`used` 取的是 `max()` ⇒ argmax 不是第一軸時 `explain()` 逐字說錯軸名 | P2 | 改以 `max(…, key=pct)` 的 argmax 具名；平手取前者 ⇒ 諸軸等值時字面逐位元不變、數值面零改動 | fixed@R100：`tools/lib/quota_pace.py` :321~:333；方向鎖 `test_quota_policy.py::TestAmortizationNamesTheAxisItActuallyUsed` 當回合 `Ran 4 tests` `OK` |
|DEF-200-148|2026-08-17|掌舵者定級「會破產的嚴重 BUG」（原話，要求務必紀錄）|主控閒置盲區：三結構洞——①閒置零工具呼叫致水位機制全睡；②哨兵只認已撞線不做預防；③撞線通知到達時主控可能同在 limit 內（08-16/08-17 兩次實證，詳R96§6）|P0|已規格化＝PRD Token_監控與喚醒機制_PRD_v2.1.md §4.5.7（v2.1.6，R-4.5.7-1~3）；過渡紀律＝Resume 證據檔 §L-5|fixed：commit `1c3a4c1` 已落地 PRD §4.5.7／§4.5.8；回歸鎖 10 passed（`ControllerIdlePrepareWatchTest`／`PatrolNoticeIsDesktopNotHookTest`／`ArmedDriftSelfHealTest`）；訂正過期措辭|
| DEF-200-153 | 2026-08-19 | R96 Windows 真機切換輪 | **家目錄與被觀測目錄同一個 ⇒ 第三方副作用打破「不寫檔」斷言**：`--check` 會 spawn PowerShell，而它在被隔離的 HOME 下建出空的 `AppData\Roaming`。磁碟殘留實證：`ctxguard-cli-y1ob2mj6` 內含該目錄，同批其他五個只有 `s.jsonl`；mac 走 launchctl 故恆綠 | P2 | HOME 族分離到 `tmp/home`；判準改全樹 `rglob` 快照 ＋ `_HOME_ARTIFACT_DIRS` 具名例外（自陳盲區＝一個目錄名） | fixed@R96：`PlannerCliTest` rc=0（**不寫支數**：包 C2 同輪仍在增補）；詳 CrossPlatform_R96_Closure_Evidence.md §2⑨ |
| DEF-200-184 | 2026-08-21 | R98 收尾（DEF-200-178 射程劃界） | `block_destructive_git.py:559` 的 `_dir_prefix()` 用 `os.sep`；現查該處**不是缺陷**——它防的是檔案系統根 `"//"` 恆假的漏擋，該邊界在 `is_under_disposable_worktree()` 上結構不可達，且失敗方向相反（誤擋＝fail-closed）。見 `CrossPlatform_R98_Mac_Closure_Evidence.md` §1 末段 | P3 | 裁決：寫法正確 ⇒ 非缺陷。watch 仍有效：整包 `ntpath` 注入擴展到 `is_foreign_tree()` 前須先處理 `os.sep` | closed-by-decision（R98 收尾）：`_dir_prefix` 該路上被呼叫 0 次、加/不加 patch 判決逐項相同 |
| DEF-200-067 | 2026-08-11 | R84／W2 收尾單人窗口的四件未竟事 | ①ps 兩鎖合併**實測後刻意回退**（成品在 scratchpad `W2_MERGED_test_ps51_compat.py`），需 `git rm tools/tests/test_ps_engine_ssot.py`——別包已從工作樹刪除但留下 28 行引用 ⇒ 兩支鎖檔共 16 筆紅；②`_FROZEN_GUARD_LINES` 與兩份 `.md` 的 `<!-- guard-total:R84 -->` 標記須由收尾單人窗口一次重釘（本輪全樹 +1747~+1898，分屬六包）；③跨檔參照稅使淨減法在並行輪次結構上做不完 | P1 | 重釘是 C7 單人窗口專屬動作；提前重釘＝把紅挪家 | closed-by-decision@R92：詳§G-067 |
| DEF-200-187 | 2026-08-21 | R98 收尾（書記帳本減半波，複驗 P1 回報） | `python AutoClaude/tools/check_loc_budget.py` 現查 `tools/sync_onboarding_baselines.py` raw-line 餘裕＝0（`[special<=1499] .../sync_onboarding_baselines.py: 1499 （餘裕 0 行）`），SPECIAL-WARN 非阻塞但下次編輯該檔前必須先拆職責 | P3 | 下一個觸碰該檔的包，先讀 SPECIAL-WARN 段再動手 | fixed@R103：`python AutoClaude/tools/check_loc_budget.py --json` 現查 `sync_onboarding_baselines.py` 未列於 `root_tools_violations`／`root_tools_warn_band` 任一清單、`total_violation=false`，原「餘裕 0」狀態已解除 |
|DEF-200-140|2026-08-16|R94 SD 獨立複審（阻塞項，四方複審 REJECT→修復→複驗）|`account_key_of()` 量不到（標頭缺席）時 `core_signature()` 退回裸桶名指紋此前**完全靜默**——`state.usable()` 為真卻 `account_key is None` 時零觀測性，正是 `Quota_R90_CrossAccount_Experiment.md` §2.2 實測「29%（10/35）跨方案指紋逐字相同」碰撞面會命中的路徑|P2|比照既有六種退化路徑同套接上 `note_degraded()`；`usable()==False` 或 account_key 齊全時不得誤觸發|fixed@R94：ADR-XPLAT-009 §6/§4；3 支命名回歸鎖 3 passed；詳 CrossPlatform_R91_Scan_Findings.md §J-2|
| DEF-200-110 | 2026-08-14 | R88 追查 `test_full_run_fail_escalates` 轉紅 | AutoClaude **引擎測試讀開發機的真實額度快取**：`FileQuotaMeterAdapter` 預設 `tempfile.gettempdir()/autosdd_quota.json`＝護欄層活體快取，帳號撞月度上限時該檔含 `extra_usage 100`，`read_worst_pct()` 取最高軸 ⇒ TokenGuard 每步 HALT。失敗訊息（`assert False`）一個字都不提額度 ⇒ **失效表徵與被測邏輯無關** | P1 | `AutoClaude/tests/conftest.py` 加 autouse fixture 只換**預設**路徑（顯式傳 `path=` 的契約測試不受影響）；不改 `tempfile.gettempdir`（全域副作用） | fixed@R88 |
| DEF-200-082 | 2026-08-11 | R84／W3 訴求 7 的**兩層靜默失效**（本列與 `DEF-200-060` 同一主題） | ① CC 在 Windows 的 **shell form** hook 是用 console 子系統的 `bash.exe -c` 起的 ⇒ 每觸發一次就閃一個視窗；② 改 **exec form** 之後，載具解析不到時 CC 只記一行 ERROR 就**放行（fail-open）** ⇒ 六支守衛全部靜默失效，而螢幕上的表徵正是「終於不閃窗了」——**與修好完全相同** | P1 | ⇒「不閃窗了」永遠不算驗收通過；正負兩面必須一起看（正面憑證只有 `--debug hooks` 拿得到） | fixed@R84（判準與散文皆落地） |
| DEF-200-102 | 2026-08-12 | R85／P4 M5 注入矩陣稽核 | **`_injection_criteria()` 只有 8 條判準，而 M5 有 11 題**：實測 `tools/tests/test_platform_neutral_paths.py:4332` 回出 8 個鍵（drive-literal／intree-tmpdir／posix-abs-assert／call-obj-repr／path-str-identity／pathext-guard／text-io-encoding／foreign-platform-api）⇒ 分母凍結在 R79 判準集，docstring 兩處宣稱為假 | P1 | 常數／消費端／史料全同住該檔＝單包可修（鐵律七） | fixed@R88（`_injection_criteria()` 實查 **12** 鍵＞M5 的 11 題；逐鍵清單＝`docs/06_quality/CrossPlatform_R88_Closure_Evidence.md`） |
| DEF-200-070 | 2026-08-11 | R84／W8 鐵律六與鐵律三的剩餘失明面 | ①`DEF-200-045` 的另一半「**該掛的沒掛**」確認無觀測點：Monitor 掛沒掛、完成通知在講哪一個行程，逐欄實查皆不在 payload 任何欄位、也永不成檔 ⇒ 攔截器接得住「寫出壞形態」，接不住「該掛的沒掛」；②鐵律三「`shell=True` 原生殼差異」那格靜態掃描回 **0 命中**＝假的安心（真正送進殼的指令來自 playbook＝使用者輸入，不在 repo 裡） | P2 | ②要的是執行期契約或輸入面正規化，不是靜態掃描 | closed-by-decision@R92：詳§F-070 |
| DEF-101-978 | 2026-08-08 | R81 開場（並行扇出撞輪號時鐘） | 帳本時鐘由「發現情境」欄推得，本輪第一列落地前仍停在 R80 ⇒ `TestR71CodeRoundLabelsNeverExceedLedgerCurrentRound` 對每個已寫下 R81 標籤的並行包都紅（當回合實測 20 站點／7 支檔），而每包看到的都是「別人造成的紅」，正解卻是開帳本列 | P2 | 建議把「開輪先寫帳本第一列」由靠人記得升為開輪 SOP 的機械第一動作 | fixed@R99｜開輪 SOP 已補「先寫帳本第一列」為機械第一動作（AutoSDD_Iteration_Prompt_Template.md:151-156），R99 書記複驗通過 |
| DEF-200-045 | 2026-08-11 | R83 收輪：`DEF-200-044` 機械物裁決 | **零攔截器、只有散文守著**，但非「結構上不可能」：`block_destructive_git.py` 已在 PreToolUse 讀 Bash 指令字串且平台中立，補「`nohup` 與 `&` 併現」「`until`／`while` 條件內裸 `pgrep -f`」兩判準即可 | P2 | 本包不實作（會動 deny 面） | fixed@R85：兩判準已落於 `waitform_hits()`，當回合逐形態實測有牙（`nohup … &`／`until ! pgrep -f X`／`while pgrep -f "python.*X"` 皆命中）。射程外：「該掛的沒掛」由 `DEF-200-070`① 承接、讀 rc 接管線由 `DEF-200-086` 承接 |
| DEF-101-726 | 2026-08-02 | R69 第三波多包並行時本包自身被 LOC 閘門打紅 | 容量事實登帳：dev_start.py 貼 SPECIAL 上限、monorepo total 落入預警帶；實害＝一包只加 18 行註解就打紅全樹 LOC 閘；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §14 | P2 | 本帳本＋AutoClaude/tools/check_loc_budget.py | fixed@R74（首詞不變）：R75 複驗兩半皆達標、R77 訂正 (a) 半邊的量測面（raw line vs count_loc）。🔴 total 餘裕已再度落回預警帶，由 DEF-200-042 承接（現查 check_loc_budget.py --json）。全文見 CrossPlatform_R85_Ledger_Closure.md §14 |
| DEF-200-093 | 2026-08-12 | R85／P6 框架 lint fail-open 稽核 | **4 支 lint fail-open ＋ 2 支測試把 fail-open 釘成契約**：最嚴重者 `AISDLC_SDD/scripts/rfc_lifecycle_lint.py` 在 `_latest_active_dir()` 回 None（目錄**從未找到**）時仍印「✅ 無已決 RFC 滯留」＝對一個沒掃過的目錄做正面斷言，假綠與真綠表徵完全相同 | P1 | 改 fail-closed，並補「版本目錄不存在即非零 rc」回歸鎖 | fixed@R85：P9 複驗 `rfc_lifecycle_lint.py:156` 已有 AGT-12 fail-closed 段、新測 `test_main_no_version_dir_exits_nonzero`（:212）；五支 lint 測試 **79 passed rc=0** |
| DEF-200-104 | 2026-08-12 | R85／P4 console spawn 掃描面稽核 | **SDD LATEST 的 hook 有 3 支 `git` spawn，而沒有任何 console-spawn 判準看得到它們**：AST 實測 `AISDLC_SDD/<LATEST>/.claude/hooks/` 內 `closure_evidence_verify.py:68`／`post_commit_drift.py:54`／`:72` 三處 `subprocess.check_output(["git",…])`；既有兩個掃描面只涵蓋根 `.claude/hooks/` 與 `AutoClaude/tools/hooks/` | P2（Windows 靜態推論） | 掃描面加第三棵樹 | fixed@R88（3 站點補 `CREATE_NO_WINDOW` ＋ SDD LATEST 成為**第三個掃描面**；證據＝`docs/06_quality/CrossPlatform_R88_Closure_Evidence.md`） |
| DEF-200-046 | 2026-08-11 | R84 帳本包（交棒書指路實查） | `R83_HANDOFF.md` §1／§3 把 10 支 mac 紅的家指向主帳本 `DEF-200-030`~`DEF-200-039`，實測主帳本**不含** 031~034／036~038 ⇒ 照著 grep 得 0 命中、最可能誤判成「缺陷沒立列」而重做一次已完成的歸因；十列實居 `AutoSDD_Defect_Log_archive_65.md` | P1 | 主帳本已留一行可 grep 的指路（見〈缺陷總表〉前 R84 段，含 archive 檔名）；交棒書三處字面由收斂階段訂正 | fixed@R84：指路改成判準⑥ 認得的單點方言逐支寫一次，`--check` rc=0、居所指針 15 → **25** |
| DEF-200-164 | 2026-08-19 | R96 四方複審（QA） | **`.perf_baseline.toml` 資料不自洽：`p99 < p95`**（當回合實測 `dry_run_e2e` `p95_ms=5.191`／`p99_ms=4.257`）。根因＝`AutoClaude/tools/perf_baseline_lock.py` 寫檔時 `p95_ms` 取 7 次的 `max(p95s)`、`p50_ms`／`p99_ms` 取 `tail_last`（最後一次），`samples` 硬寫 `MIN_SAMPLES` 而非實際樣本數 ⇒ 三個欄位來自不同母體 | P3 | 三個分位數取同一次（或同時取 max），`samples` 照實寫 | fixed@R99｜perf_baseline_lock.py 三分位數改同出一筆，test_perf_baseline_lock.py 19 passed（R99 書記複驗重跑通過） |
| DEF-200-050 | 2026-08-11 | R84 帳本包（鐵律五普查的可重跑性） | `CLAUDE.md` 鐵律五自陳「10,106 筆 git 指令片段、假陽性 0」，而 `grep -rn "10,106\|10106"` 只命中那句宣稱本身，`tools/probe/` 5 支無一產生該語料 ⇒ 交棒書要後人「用同樣的方法」結構上做不到（與 R77 分群無產物同型，那筆已由 `misstep_attribution.py` 修好） | P1 | 落 `tools/probe/git_command_corpus.py`（母體兩面各自標明、輸出可 diff 的 .jsonl），並把 10,106／1,031／30／254 標成量測值 | fixed@R84（回執 DEF-200-069）：`tools/probe/` 非本包持有面 |
| DEF-200-059 | 2026-08-11 | R84／W7 mac 續航未達成面（訴求 6e） | 🔴 **6e 未達成，只做到「失效可偵測」**：本輪只讓 `pmset sleep != 0` 會出聲，未讓 repo 自己下喚醒憑證（`pmset repeat`／sudo 依裁決不做）⇒ 換一台沒設過的 mac，續航鏈在闔蓋那刻仍整條斷掉。同族兩筆：回收臂零自動觸發點（`gc()` 只有 CLI 入口、`--apply` 預設關）；孤兒哨兵 `_s` 由單元測試真的註冊 launchd ⇒ 移除後跑那支測試就重生 | P1 | 喚醒憑證納入取證；回收臂接上 hook 面（需單人窗口） | closed-by-decision@R92：詳§F-059 |
|DEF-101-377|2026-07-25|R45 全新掃描 agent 回報|本機工作樹 `.sh`／`.py` 行尾為 CRLF 而 git blob 為 LF（`.gitattributes` 宣告 `eol=lf`）＝本機環境衛生、非 repo 缺陷，全新 clone 不重現；三輪實測與根因訂正原文逐字保全於 CrossPlatform_R89_Closure_Evidence.md §DEF-101-377|P4（非專案缺陷）|不修復（非專案缺陷，修復屬破壞性操作，需使用者自行決定）|wontfix（依分流欄自述訂正首詞，判例同 DEF-101-359／382）：活躍面已有機械物 `TestActiveSourceEolIsRatchetedSeparatelyFromTheFrozenSurface`，不再零觀測者。全文見同節|
| DEF-200-066 | 2026-08-11 | R84／W2 M1 護欄層棘輪治本（ARCH-01） | 護欄層棘輪自稱「淨行數只准往下」，而重釘紀錄**每一列都是上升、零列下降**（R77→R83 +24,895／+46%）⇒ 成熟度 M1 照現行機制永遠做不到。同族第二筆：護欄層 raw 行數的 21% 是註解／散文，而散文警語攔阻力已實證為 0，卻 100% 計入 M1 分子 | P1 | 款(10)(11)＋後設鎖：單輪成長綠、**相鄰三輪皆正即紅**、起算錨 shrink-only；散文行以史料搬遷排除出分子 | fixed@R84（回執 `DEF-101-947`）：真表零違規、合成注入必紅且零串音 |
| DEF-200-049 | 2026-08-11 | R84 帳本包（S2 可執行性實量） | **只持帳本的包結構上結不掉列**：未結列中多數 >700 bytes 且已在豁免清單，而兩條棘輪皆**零餘裕** ⇒ 在那些列追加任何字即紅。**`DEF-101-992` 復發** | P1 | 出口＝結案＋瘦身成索引（R82 判例） | fixed@R85（與 `DEF-101-992` 同一動作）：本輪實走一次完整出口——15 列已結超長列瘦身成索引、原文逐字進 `CrossPlatform_R85_Ledger_Closure.md`，兩條棘輪同步**下修**。🔴 仍未關：棘輪是相等斷言 ⇒ 餘裕恆為 0，那一半由 `DEF-200-053` 承接 |
| DEF-200-107 | 2026-08-13 | R87 開工額度判讀 | **模型推翻機制**：舵手把 `extra_usage.is_enabled=false`／`spend.enabled=false` 誤讀成「池關著≠節流軸」，於 `quota_meter.bucket_readings()` 排除兩軸 ⇒ 13 個 subagent 全撞 `monthly spend limit`、燒 1,319,703 tokens、零產出。真意＝`used 610>limit 500` 已撞月度上限。缺口在**取數層無不變式**：判讀層「halt 一票否決」完好，卻拿不到輸入 | P1 | 已還原＋原地留事故墓碑；鎖＝`tools/tests/test_quota_policy.py::TestR87TheMeterMayNotDropAThrottlingAxis`（4 測，含鑑別力自證） | fixed |
| DEF-200-073 | 2026-08-11 | R84／W5 `tools/tests@darwin` 天花板零餘裕的裁決 | 兩張天花板表皆 `platform: 44`＝零餘裕（實測 43→0、44→0、**45→2 筆**）⇒ mac 側新增任一支 Windows-only 根層測試即當場紅。本輪判定：**這是刻意設計**（零餘裕才咬得動），但代價此前**無任何地方寫明合法處置** ⇒ 補寫合法處置＝確認被 skip 的 44 支 `[WINDOWS-NATIVE-ONLY]` 在 `tools/tests@win32` 真的跑到，而不是把天花板調高 | P2 | 只寫明處置，不動門檻 | closed-by-decision@R84（回執 DEF-200-026）：非放寬，門檻一格未動 |
| DEF-200-091 | 2026-08-12 | R85 P1 帳本洩壓包（bytes 死結出路①） | 帳本時鐘由「發現情境」欄推得，本列落地前仍停在 R84 ⇒ 已寫下 R85 標籤的並行包全數被輪號鎖打紅（當回合實測 5 站點／3 支檔），而每包看到的都是「別人造成的紅」——`DEF-101-978` 的同一形態 | P2（窗口型，落地即關） | 本列即出口 | fixed@R85：落地當回合窗口關閉。同批＝`DEF-200-010`／`012`／`015`／`020`／`023`／`042`／`043` 七列承接輪次由 R84 就地推到 R85（R84 皆未動工，逐列 byte 數不變，原值可由 git diff 複驗） |
| DEF-101-919 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | `os.replace()` 覆寫被其他行程開著的目的檔在 Windows 拋 WinError 5，POSIX 恆成功；既有知識只涵蓋 unlink 的 WinError 32 | P3 | 原語清冊＋站點普查＋雙平台行為測試 | fixed@R85：原語清冊與雙錯誤碼已落地並雙向對帳（`_WINDOWS_OPEN_FILE_HOSTILE_PRIMITIVES`＝os.replace→5／os.unlink→32、掃描面須為清冊子集、站點普查對帳帳本），兩平台各斷言自己那一半而**不用 skip**。當回合實跑 `TestDirEntryPrimitivesAreAccountedFor` → `Ran 7 tests … OK`、rc=0 |
| DEF-200-055 | 2026-08-11 | R84／W1 訴求 6C（85% 水位要有準備動作） | `prepare` 帶（85~95%）只有一個沒人印的 cap 值、**零準備動作**：沙箱實測 `PostToolUse×Read@86%` 與 `PreToolUse×Agent@86%` 皆 rc=0／stderr 0 B／任務書 0 份＝兩事件全靜默。另兩筆同族：6a 回報給人的 binding 軸恆是**零消耗那一軸**（live 實測 `nimbus_quill`）；pace 係數不可由 env 調（掌舵者訴求逐字要求可參數化） | P1 | 先騰 LOC 再實作，**不得調高 LOC 預算**（那是把債寫成合法額度） | fixed@R84（回執 DEF-200-022）：三筆皆落地 |
| DEF-200-115 | 2026-08-14 | R89 讀 PRD §15.5 紅線 10 後實查本 repo | **守衛可被受守衛者關閉**：`.claude/settings.json` `env` 區塊注入 hook 行程，全 repo 零 hook 保護該檔（原文逐字＝GovWrite 證據檔 §5） | P1 | 裁決缺口由 DEF-200-144 承接落地（原處置原文同上 §5） | fixed@R95：詳 DEF-200-144｜🔴 R95 修復包訂正（2026-08-17 就地追記）：144 漏 `.env`（`env` 區塊同義載體，QA 探針實證可寫）⇒ 原 fixed 實為**部分結案**；補 `.env` 進 `_GOV_EXACT` 後**完全結案**（專測＋矩陣＝GovWrite 證據檔 §2.2/§3 列 10） |
| DEF-200-154 | 2026-08-19 | R96 Windows 真機切換輪 | **Windows 檔案鎖：logger handler 未關 ⇒ tempdir 清不掉**：`RotatingFileHandler` 握著 log，Windows 不允許刪除開啟中的檔 ⇒ 清理拋 WinError 32，而測試在**本體已通過之後**才紅。第二站點是修好第一站點才露出來的（先前靠「handler 已存在故不重建」躲過） | P2 | 抽 `_release_autoclaude_log_handles()` 兩站點共用；不用 `ignore_cleanup_errors`（會吞掉真實洩漏） | fixed@R96：該檔 10 passed；`local_ci_gate` rc=0（修前 rc=1）；詳 CrossPlatform_R96_Closure_Evidence.md §2⑦ |
|DEF-200-135|2026-08-15|R91 開場（掌舵者指名「75~90% 帶全程沒有訊號」）|**`.claude/hooks/context_budget_guard.py` 的 75% 分支是 `stderr + exit 0`，而 exit 0 的 stderr 依官方契約不進模型 context** ⇒ 該帶結構上零訊號（實測 1h49m／45 turns）。換通道同時暴露第二個**休眠**缺陷：PRD §4.3 壓縮觸發是三個 AND，該 hook 只實作 `K_ctx ≥ 75`|P1|單一發射口 `platform_utils.emit_to_model` ＋ 依 `quota_gate.draining()` 三態分流；立案、實驗與逐項證據＝`CrossPlatform_R91_Scan_Findings.md`|fixed@R91：餘項歸 DEF-200-137；詳§F-135|
|DEF-200-143|2026-08-16|R95 Pkg-D（PRD §4.5.4／§8-10）|喚醒只有 `claude -p -r` 一條路：逐字稿缺檔/為空/超上限時指令直接失敗且零降級——五段續航全數觸發成功、最後一步空轉（機制蓋好沒接電同型）|P1|`choose_resume_route()` 三態選路：RESUME→FRESH 單向降級（不帶 -r、prompt 指向磁碟任務書）、任務書缺席 REFUSE 不 spawn|fixed@R95：11 支回歸鎖當回合實跑 11 passed；痕跡記 route_chosen/why；上限 `AUTOSDD_RESUME_MAX_TRANSCRIPT_BYTES`（預設 32MiB=觀測 max 5 倍）；詳 CrossPlatform_R95_Resume_Strategy_Evidence.md|
| DEF-200-097 | 2026-08-12 | R85／P8 額度政策鍵分類稽核 | `AUTOSDD_QUOTA_FANOUT_CAP` **結構與渲染自相矛盾**：`tools/lib/quota_policy.py:540` 以 `EnvVar(…"fanout_cap_override"…"int"…)` 登記＝政策鍵，而 `--print-env-example` 把它渲染在 `── 既有逃生口 ──`（:42）標題下、夾在 `GUARD_OFF` 與 `SENTINEL_OFF` 之間 ⇒ 讀者會以為它是開關 | P3 | 擇一：改渲染分節，或改結構歸類 | fixed@R91：走分流欄的「改結構歸類」，`section` escape→policy 並重生 `.env.example`，實測見 CrossPlatform_R91_Scan_Findings.md §DEF-200-097|
| DEF-101-351 | 2026-07-24 | R45 同 DEF-101-350 情境下額外發現 | test_bash_probe_spec_contract.py::test_usable_bash_rejects_candidate_when_path_lacks_dirname 在本機失敗（疑 MSYS2 runtime 補齊 PATH）；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §6 | P3（不阻擋 push 閘門） | 待原始 Windows/MSYS2 環境複驗 | fixed@R71（首詞不變）：R75 在 Windows 11 原生 PowerShell 上實跑該支 PASSED、同檔 19 passed；成因＝R71 為 DEF-101-628 落地的 _platform_helpers.honours_external_path 一併治好。逐次量測見 CrossPlatform_R85_Ledger_Closure.md §6 |
| DEF-101-998 | 2026-08-09 | R82 收尾清殘留機密時逐檔實查 | AutoClaude_Guide.md 住兩個家且已漂移（docs/ 版 0.3.0／2026-05-14 對 docs/04_planning/ 版 1.1.0／2026-06-13），而同一段 PG 設定其實住四個家（另加 README.md 與 DB_Only_Switch_Runbook.md）⇒ 同一把密碼要在四處各清一次，漏一處就等於沒清 | P2 | 定一份為 SSOT、其餘改成指針，別讓憑證段落被複製 | fixed@R99｜DB_Only_Switch_Runbook.md 定為 PG 設定 SSOT，兩份 AutoClaude_Guide.md 已改指針，R99 書記複驗通過；兩份 Guide 整檔版本漂移非本輪範圍 |
| DEF-101-242 | 2026-07-21 | R17 四方一審（QA 獨立 bug-injection 反向驗證） | 兩個「測試保護力比表面弱」的缺口：① find_git_bash parity 只做結構等價比對不驗行為；② R241① 手動補的三行 CI paths 本身無機械鎖；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §7 | P2×2（均非阻斷） | 已於 R19 補行為測試與存在性鎖（DEF-101-244①②） | fixed@R19（首詞不變）：R75 複驗兩鎖皆綠（TestFindGitBashBehavior 8 passed／test_ci_paths_cover_root_consumers 34 passed）。全文見 CrossPlatform_R85_Ledger_Closure.md §7 |
| DEF-200-061 | 2026-08-11 | R84／W3 hook 形態普查的兩個失明面 | ①`AISDLC_SDD` 各版 `settings.json` **全數 shell form**，而 `FROZEN_SETTINGS_PREFIX` 把 30 份結構性排除在 `SHELL_FORM_CENSUS` 之外 ⇒ R80／R81 宣告的「兩格皆 0」是把分母縮掉換來的；②`hook_form_problems()` 對 `AutoClaude/.claude/settings.json` 回 **12 筆假紅**（判準 B／E 做字面比對，而 `args[0]`／`command` 帶 `../`）⇒ A~F 判準無法擴到子專案那份 | P1 | 分母補回＋B／E 改正規化後比對 | fixed@R84：擴面後真表零違規，合成注入逐款轉紅 |
| DEF-200-064 | 2026-08-11 | R84／W0 LOC tier 治本（ARCH-04／ARCH-05） | ①`guardrail_lib=400` 把 `tools/lib/` 整層當同一種東西，**合成面（hub）被 leaf 的門檻擋死**：`quota_gate.py` 實測 400/400 餘裕 0、fan-out 5 ⇒ 新增 `guardrail_hub` 具名 tier（budget 引用 service 值、成員只准縮、fan-out 由 AST 現查）；②`SPECIAL_FILES` raw-line 棘輪缺**下界**咬人判準 ⇒ 門檻可高於現值，自陳「只准往下改」不成立 | P1 | 兩者皆以判準落地 | fixed@R84：`check_loc_budget.py --json` rc=0，鎖檔 15 passed、8 次注入全紅 |
| DEF-200-152 | 2026-08-19 | R96 Windows 真機切換輪 | **三支 mac 專屬鎖的前提在 Windows 結構上造不出來**：(a) `chmod(0o500)` 對 NTFS 無效；(b) `_write_plist()` 會跑 macOS 專屬 `plutil -lint`（WinError 2 ⇒ 依設計刪檔回 False）；(c) `arm()` 的 `warn_if_sleepy()` 不帶 platform ⇒ 非 darwin 依設計不出聲 | P2 | (a) 改「父層是檔案」＋新增一支注入鎖驗第二層；(b) 掛既有 `_pm` 替身；(c) 注入 darwin。三者皆讓測試在兩平台真的跑 | fixed@R96：兩類 14 支 rc=0；詳 CrossPlatform_R96_Closure_Evidence.md §2③④⑤ |
| DEF-101-297 | 2026-07-24 | R33 修復 DEF-101-295 控制字元淨化時交叉測試發現 | tools/git-hooks/pre-commit 的 _ntfs_seg_bad() 偵測不到路徑段內嵌的 \n（printf 把它變成行分隔符而被消耗）；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §5 | P4（CI 端 check_ntfs_paths.py 仍會擋，非完全繞過） | 已於 R78 修 | fixed@R78（首詞不變）：加一道不經行分隔載具的 bash case pattern，原 grep 留為第二層；Git Bash 5.2.37 逐形態紅→綠與 27534 條 tracked 路徑零假紅全文見 CrossPlatform_R85_Ledger_Closure.md §5 |
|DEF-200-111|2026-08-14|R88 建 pgvector 自動通道時就地發現|`_NO_AUTOMATED_CHANNEL` 逐字寫「本 repo 目前沒有任何自動通道會跑這兩支」，而 R88 就是建通道那一輪 ⇒ 同 commit 內變成假話；原文逐字保全於 CrossPlatform_R89_Closure_Evidence.md §DEF-200-111|P2|就地改寫為「通道已存在＋接線憑證＋平台綁定」|fixed@R88（本輪複驗）：`test_pgvector_hnsw_recall.py:181` 現文三要件齊備。🔴 分流欄第二句「無鎖守散文宣稱」**未解、亦未併入他列**，見 CrossPlatform_R89_Closure_Evidence.md §DEF-200-111|
| DEF-101-565 | 2026-07-29 | R60 round 2 Architect 複審（ARCH-R60R2-06） | DEF-101-561③ 把觸發條件推給未來＝再拖一輪，而該條件在 R60 已滿足（護欄層一輪 +11.6%、行數已超過被護的生產碼）；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §13 | P3（架構級趨勢判斷） | 根層治理／R61 開輪條件 | fixed@R60 round 3（首詞不變）：561③ 已訂正為「現在即判定已觸發」、R61 開輪即進入「禁止新增鎖檔」；主控自我揭露與 Scan-H 第 4 條必跑項全文見 CrossPlatform_R85_Ledger_Closure.md §13 |
| DEF-200-054 | 2026-08-11 | R84／W1 訴求 6b（cap＝f(水位%,距 reset)） | 6b① `_pace_of` 的否決權被**零煞車力的軸**持有：live 7 軸中 3 軸 `resets_at` 恆 `null` ⇒ horizon 恆含 `none` ⇒ `min(1.0, fastest)` 永遠夾住，加速路在 production 結構上到不了；6b② 全 repo 無出口能回答「我現在能派幾個 agent」（`--help` 12 旗標無一為此） | P1 | 無期程軸不參與 pace 聚合（常設 null ≠ 暫時不知道）＋補現查扇出寬度的 CLI 出口 | fixed@R84（回執 DEF-200-021）：判準與 CLI 皆落地，注入紅綠自證 |
| DEF-200-174 | 2026-08-20 | 治理修復輪查 windows/macos-compat-ci 持續 steps=[] | `gh api .../jobs` 實測：兩份 workflow 近 5 次跑皆 `conclusion=failure`、`steps=[]`、**`runner_id=0`**（從未真的派到 runner，~2 秒即敗）；同期 ubuntu-latest 的 root-infra-ci 正常。workflow `runs-on` 語法無誤 | P2 | 非程式碼可修：疑 GitHub Windows(2x)／macOS(10x) runner 計費倍率撞帳號 spend limit；需帳號所有者查 Billing 或降頻觸發 | closed-by-decision｜移入外部阻塞軌（GitHub Actions 帳務），見 AutoSDD_External_Blocked_Log.md |
| DEF-200-062 | 2026-08-11 | R84／W3 console 子行程的兩個站點 | ①`AutoClaude/tools/hooks/check_sh_eol.py::_run_git` 對 `git.exe` 的 `subprocess.run` 無 `CREATE_NO_WINDOW`——父行程若是 `pythonw`（GUI 子系統）就會自己開一個 console；②`install_windows_nightly.ps1` 兩支 `schtasks` Action 起的是 console 的 `powershell.exe` 且無 `-WindowStyle Hidden`，第一層 S4U 已實測漂成互動階段 | P2 | 兩站點各自補旗標；判準面由 `ConsoleFreeSpawnTest` 承接（其射程本輪由 14 支擴到含 `AutoClaude/tools/hooks/**`） | fixed@R84 |
| DEF-200-120 | 2026-08-15 | R89 收尾／QA 複審 N-1（注入普查） | 保險軸鎖的判準寫成「黑名單四個訂閱軸」（`test_quota_policy.py` 與 `test_r82_quota_axis_and_shipped_defaults.py` 兩家），只罩 7 個活體軸中的 4 個 ⇒ 把 `weekly_scoped` 兩家一起加進 `FALLBACK_KINDS` **紅 0 支**（全綠）＝`DEF-200-107` 的形狀對它完全失明 | P2 | 兩家同步翻面成**白名單以外一律紅**（`ALLOWED_FALLBACK`，成員逐一附 PRD 出處），並各附「注入 `weekly_scoped`：舊判準綠／新判準紅」的合成自證 | fixed@R89 |
| DEF-200-179 | 2026-08-21 | 同上 | 鏡射鎖靜默歸零 3 支（鐵律七）：R98 把 `EnvVar(...)` 自 `quota_policy.py` 搬到 `quota_policy_env.py`，AutoClaude 側兩鎖仍寫死舊路徑（`test_r82_quota_axis_and_shipped_defaults.py:337`／`test_r86_pace_contract.py:47`，該兩檔 0 改動）。見 `CrossPlatform_R98_Mac_Closure_Evidence.md` §2 | P1 | 指標改指宣告真實所在。🔴 兩檔各持一半、不可一律改指新檔（帶別字面仍住舊檔，實測逼出）⇒ r86 保留兩指標 | fixed（R98 收尾）：兩檔 180 passed；`ruff` All checks passed! |
| DEF-200-074 | 2026-08-11 | R84／W5 收緊建議原樣落地＝5/5 假紅 | R83 對訴求 3 掃描器的收緊建議**原樣落地會產生 5/5 假紅**：`_POSIX_ONLY` 缺「行內 `VAR=value cmd` 前綴」這個 `$env:X=` 真正的對面寫法 ⇒ 5 個合法站點全被判違規。**這一筆的價值在於「上一輪的建議不是規格」**：照抄一個沒有實跑過的收緊建議，代價是一次全紅的閘門，而被關掉的閘門比沒有閘門更糟 | P1 | 先實跑再落地；建議欄一律標「未實跑」 | fixed@R84：補齊對面寫法後真表零違規 |
| DEF-200-060 | 2026-08-11 | R84／W3 訴求 7（Windows session 結束後仍有彈跳視窗） | 🔴 **掌舵者當面確認的現象＝定位依據**：黑框一閃即消、**約每 15 分鐘一次** ⇒ 對上的是額度哨兵 `--sentinel-tick`（`schtasks` 每 15 分鐘醒一次），**不是** session 內的 hook。⇒ 驗收條件 **AC-(c) 由「缺料、無法歸因」轉為「已定位」** | P1 | 兩層分別處置：hook 側轉 exec form＋GUI 子系統載具；schtasks Action 側補 `-WindowStyle Hidden` | fixed@R84（定位）：真機未驗，見 `DEF-200-063` |
| DEF-200-071 | 2026-08-11 | R84／W5 PG skip 的真因（訴求 S1） | ①**PG 容器 healthy ≠ DB 已 migrate**：剎車④ 拒絕注入時無可貼修法，97 支 PG skip 一支未掉（R83 交棒書把它寫成「最便宜的一塊」＝過報）；②剖面標記 `AUTOCLAUDE-PG-DSN-IN-EFFECT` 問錯問題（問「有沒有人設過 DSN」而非「DSN 真的生效」），且測試汙染讓它與 autodetect 理由互斥而**零人出聲** | P1 | 修法是 docker＋alembic 兩步，不是一行 docker 指令 | fixed@R84：判準改讀生效面，注入紅綠自證 |
| DEF-101-200 | 2026-07-20 | R14 Architect 架構檢視（ARCH-GAP-2） | 排程「初次安裝」能力反向不對等——mac 有一鍵安裝器、Windows 沒有（fix_nightly_catchup.ps1 僅能校正既有任務）；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §3 | P2 | Windows 輪（需實機驗證）：升級為 ensure 語意＋加 -Status | fixed@R23（首詞不變）：核心訴求由 tools/install_windows_nightly.ps1 落地（DEF-101-245／248／249／253／255），三筆 R15 riders 逐項訂正全文見 CrossPlatform_R85_Ledger_Closure.md §3 |
| DEF-200-180 | 2026-08-21 | 同上（macOS 側 pre-push 實跑 rc=1） | CI paths 漏登記 6 支（**同形態第四次重犯**，前三次 R80／R81／R86）：R98 新建的 `tools/lib/` 六支被 `AISDLC_SDD/scripts/tests` 消費卻未列入兩支 compat-CI paths ⇒ 只改該檔時回歸鎖不跑（DEF-101-042 同構）。見 `CrossPlatform_R98_Mac_Closure_Evidence.md` §3 | P2 | 兩支 workflow 的 push 與 pull_request 各具名補列 6 支（沿用前三輪慣例，不用整片 glob） | fixed（R98 收尾）：`test_ci_paths_cover_root_consumers.py` 47 passed |
| DEF-200-109 | 2026-08-14 | R88 開場重驗基線（掌舵者交棒書宣稱基線綠） | R87 為訴求 6c 在 `tools/lib/quota_gate.py` 加 24 行 ⇒ 該檔 **524 > 500**（`guardrail_hub`），連帶把 5 支「tier 預警帶必須非阻塞」契約測試染紅（斷言 rc==0、實得 1）；**該輪未重跑 AutoClaude pytest**，交出的是紅基線而交棒書寫綠 | P1 | 依 override_reason 指定的出口拆職責：人話面整族抽出 `tools/lib/quota_messages.py`（leaf），`quota_gate` re-export 保消費端零改動 ⇒ 524→無違規 | fixed@R88 |
| DEF-101-068 | 2026-07-14 | R3 Mac/Windows 相容性複審（四方架構最佳化建議彙整） | 跨平台架構優化建議清單 (a)~(h)：CI paths 單一事實來源／雙軌腳本降薄殼／liveness 與 Git Bash 偵測抽共用模組／parity 加深至行為比對等；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §2 | P3（架構建議，非缺陷） | 記事存證；(a)(c)(e)(g)(h) 已落地，(b)(d) 具名 defer | closed-by-decision（首詞不變）：逐子項落地狀態與 R14 訂正全文見 CrossPlatform_R85_Ledger_Closure.md §2 |
| DEF-200-095 | 2026-08-12 | R85／P8 額度工具實跑 | 🔴 **`tools/probe/reset_window_distribution.py` rc=1**：`AttributeError: module 'context_budget_guard' has no attribute '_RESET_RE'`（該檔 :95 取 `guard._RESET_RE`，而該原語住 `tools/lib/quota_limits.py:72`，`context_budget_guard.py` 的具名 import 清單未含它）⇒ 根 `CLAUDE.md` 兩處「reset 數字一律現查」的紀律**結構上執行不了** | P0 | `tools/probe/`＝P2 持有面，本包不動 | fixed@R88（證據＝`docs/06_quality/CrossPlatform_R88_Closure_Evidence.md`） |
| DEF-200-108 | 2026-08-13 | R87 掌舵者裁決（「配置 Agents 前要先知道 Account Type 與有無 Usage credits」） | 派工前無從得知**帳號有無可用 credits**：事故當下訂閱窗尚餘 37%、credits 已爆且停用 ⇒ 扇出全滅而主 session 照常。`--pace` 只講得出水位，講不出「訂閱窗用完之後還有沒有救」 | P1 | `quota_meter.account_posture()` ＋ `quota_gate.posture_line()`（讀不出來一律判無 fallback）；鎖＝`test_quota_policy.py::TestR87AccountPostureIsKnownBeforeDispatch`（5 測） | fixed |
| DEF-101-918 | 2026-08-07 | R79 XPLAT 包（新落差面與 .ps1 行尾） | Windows 的 Git Bash 上 `[ -x ]` 是「檔首是不是 shebang」的內容猜測而非權限位元：親測加 BOM 由 EXECUTABLE 翻成 NOT-EXEC，且 `chmod +x` 之後仍 NOT-EXEC ⇒ post-commit dispatcher 那道守衛在 Windows 側檔首多任何位元組就靜默 exit 0 | P3 | 已寫入 ONBOARDING 執行權限政策段，並由 `DEF-101-917` 的判準覆蓋一半 | fixed｜post-commit 補 BOM 反向判準，R99 書記核對程式碼與描述一致（bash -n 語法通過） |
| DEF-200-181 | 2026-08-21 | 同上（Source Control 殘留一支未追蹤檔） | `.gitignore` 涵蓋面遺失：`9ef67f8`（DEF-200-162 換 pattern）把 `tools/.last_failure.log` 換成 `..._*.log`，而 `*` 不匹配空字串 ⇒ 舊固定檔名失去涵蓋，既有 checkout 上的殘檔永久顯示為未追蹤雜訊。見 `CrossPlatform_R98_Mac_Closure_Evidence.md` §6 | P3 | 依 `a6c0733` 先例（納 ignore）補回舊固定檔名一行並註明 `*` 不匹配空字串 | fixed（R98 收尾）：`git status --porcelain -uall` 該支已不再出現 |
| DEF-200-122 | 2026-08-15 | R89 收尾／SA 複審條件 5（換帳號實測） | `quota_pace.segments()` 的翻頁啟發式**只在任一軸下降時斷點** ⇒ 對方案變更造成的**上升**不連續結構上失明：`seven_day` 22→86 發生在 10 分鐘內且 `live=0`（物理上不可能是真燃燒），照樣被當同一區段算進 `estimate_ratio()` | P2 | （原文照錄 CrossPlatform_R93_Plan_Change_Adaptive_Evidence.md §6） | fixed@R93：ADR-XPLAT-009 §2.1/2.2 分區過濾消解；154+300／381+8+146 subtests passed rc=0；詳§6 |
| DEF-200-204 | 2026-08-23 | R100 收尾窗口（PRD v2.1 交付稽核 B1／B2） | **PRD §4.2.4 D 段遲滯完全未實作**：H1~H7 一格未動、`AVAILABILITY_EXIT_STREAK`／`AVAILABILITY_MIN_DWELL_SECONDS` 兩鍵 grep 0 命中 | P2 | 已交付，詳本列狀態欄與 `CrossPlatform_R102_Scan_Findings.md` | fixed@R102：H2~H7 到位（`quota_availability.py`／`quota_stability.py`／`quota_boot_check.py`），四方終審 4/4 APPROVE_WITH_FIXES；殘留（不阻塞）：H1 fixture 未落地、啟動自檢 60 秒佔位值待 P0 觀測資料校準 |
| DEF-101-500 | 2026-07-27 | R57 round 3 四方其餘 P4（Architect ×3／QA ×1／SD ×2） | 六筆不影響正確性的 P4 統一登記：判例節雙重路由／_platform_helpers 契約脫節／凍結版正則家族未涵蓋／docstring 涵蓋清單對齊等；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §9 | P4 ×6 | ①②④⑥ R57 修復；③⑤ 已於後續輪收斂 | fixed@R57 round 3（首詞不變）：逐子項處置、R57 round 4 對本欄「寫反」的自我訂正全文見 CrossPlatform_R85_Ledger_Closure.md §9 |
| DEF-200-058 | 2026-08-11 | R84／W7 mac 續航（ZT-03 憑證蒸發＋3.11 專屬 import） | ①R83 的 P0（等父行程退場才 bootout）唯一決定性痕跡住 `$TMPDIR`，複審日已蒸發 ⇒ 零可稽核憑證（同 `DEF-200-023` 的痕跡可靠性主題）；②`quota_escalation` 進 hook 鏈射程後帶著 3.11 專屬 `from datetime import UTC`，而 mac 內建 `python3` 常年 3.9 ⇒ 多出一個靜默失能面（POSIX hook 載具吃 PATH 上任意 python3） | P2 | 痕跡改持久居所；import 加相容回退 | fixed@R84 |
|DEF-200-146|2026-08-17|R95 哨兵存活四修（ADR-XPLAT-004 §2.9；PRD §4.5.6）|喚醒鏈三層連鎖故障（2026-08-16 事故）：任務書覆寫砸 RELAY＋哨兵靜默自我解除＋halt binding 單軸 escalate ⇒ reset 時零排程、空轉八小時|P0|四修落地（保 RELAY／自癒＋分形＋loud alert／哨兵活性欄／多軸 reset 裁決）；立案與殘項＝Resume 證據檔 §L-4、Pace 證據檔 §7-R95-修4|fixed@R95：cbg Ran 416 OK(skipped=8)、qp Ran 182 OK；紅面自證 errors=4；root_tools_violations=[]|
| DEF-200-219 | 2026-08-24 | R100 收尾窗口（push 被本機閘門擋下，第二輪逐項排除） | `6fea8a3` 新增的 `R71` 全樹掃描抓到漏帶 `round-label-ok`：`AutoClaude/tests/contract/test_loc_budget_tiered.py:339`／`tools/tests/test_adr_xplat001_c1c2_lock.py:1340,5679,5680`；補標後一行顯示寬度超線觸發 `test_e501_debt_only_shrinks`，拆行修復又使護欄層自身逐檔漂移 | P3 | 已直接修復，無需承接 | fixed@R102：round-label-ok 補標＋顯示寬度拆行＋護欄層重釘一次到位 |
|DEF-200-139|2026-08-16|R93 容量自適應攤提落地（Developer 實作 ADR-XPLAT-009 時發現）|`pace_report()` 呼叫順序先 `record_burn` 才呼叫 `burn_ratio`，`last_fp` 逐字取「落款最後一列」時多數情況正是本次剛寫下的 ⇒ `last_fp == signature` 恆成立，換方案當下**結構上永遠不出聲**（雙時刻真跑：兩次不同指紋 plan_note 皆空字串）|P2|`burn_ratio()` 內 `last_fp` 改排除本次 `measured_at` 後取得|fixed@R93：ADR-XPLAT-009 §2.4；B4 測試鎖三段式行為|
| DEF-101-278 | 2026-07-23 | R28 四方一審 Architect（複核 DEF-101-277 時延伸檢查） | ONBOARDING §6.1 與 root-infra-ci.yml 檔頭對 check_script_parity.py 的概括描述過簡；原文逐字保全於 CrossPlatform_R85_Ledger_Closure.md §4 | P4（兩處一致的既有精簡摘要，非新漂移） | 非必要精確化 | fixed@R78（首詞不變）：① ONBOARDING 那一處已不存在（R68 整段刪除）；② workflow 那一處已就地改寫。逐處實查全文見 CrossPlatform_R85_Ledger_Closure.md §4 |
| DEF-200-080 | 2026-08-11 | R84／W6 框架文件的兩處互斥（DOC-01＋falsified_claims） | ①v0.30 的 `FILE_DIRECTORY_RULES.md`（寫檔前必查的權威）樹根**寫死 v0.01**、零次提 Copy-on-Evolve ⇒ 照它走會寫進凍結基線；②`AISDLC_SDD/CLAUDE.md` 的 CoE 段與 `FRAMEWORK_STATUS.md` 對同一件事互斥，且與實務（原地改 LATEST）不符 ⇒ 三個來源兩種說法，讀者照哪一個走都可能違規 | P1 | 以磁碟現況為準訂正兩處，不新增第三個家 | fixed@R84 |
| DEF-200-210 | 2026-08-23 | R100 收尾窗口（護欄層 C4） | `ONBOARDING.md` §7 表② **macOS 欄 presumed stale**：正確回填需**出廠環境 venv**（不得帶 pg extras），而磁碟上已無該 venv。🔴 `--check-snapshot` 接在 **pre-push** ⇒ 會擋 push | P2 | 重建乾淨 venv 後 `--write --with-slow`；R102 push 收尾追加回合實跑兩次（`ae30c35`／`4845724`），詳 `R102_HANDOFF.md` §6 | fixed@R102：兩次回填皆驗證 rc=0。新教訓（venv 建樹內污染測試）記入 memory |
| DEF-200-077 | 2026-08-11 | R84／W6 agent 的憑證紀律（AGT-01／02／07） | ①04／05／07 三支核心 agent **零實測憑證紀律**，而 `sdd-evaluator` 早已載有 fail-closed 接地紀律＝同一份知識只有一半的家有；②QA agent 推薦用夾具卻**不管夾具真實性**（R83 一輪同形態犯 3 次）；③QA 交付物不要求附「當回合真跑的指令＋rc」⇒ 假綠零成本 | P1 | 三筆共用一份 `EVIDENCE_DISCIPLINE.md`，agent 側改為引用而非各抄一份 | fixed@R84 |
| DEF-200-056 | 2026-08-11 | R84／W1 額度族架構債（ARCH-03／07／10） | ①`quota_meter` 分工圖與 `quota_ledger` WHY③ 的立案理由已過期；②`autosdd_quota.json` 自稱唯一的家，實則 AutoClaude 側另有硬編；③`quota_escalation` 是全族唯一 `from lib import`，與 `quota_gate` 檔頭明文牴觸；④`claim_refresh_slot` 的生產暫存路徑無注入點 ⇒ 測試會吃掉真的 180 秒補量名額 | P2 | 四筆同族一次收斂 | fixed@R84：四筆皆落地 |
| DEF-200-057 | 2026-08-11 | R84／W7 mac 續航（SA-05 6e 誠實化＋ARCH-06） | ①「睡著的 Mac 不會被 launchd 喚醒」此前只活在一行註解裡，武裝路徑對 `pmset sleep != 0` 完全靜默 ⇒ 新檔 `tools/lib/endurance_env.py` 持有「這台機器的事實」＝電源姿態＋持久痕跡居所；②「什麼時候可以刪任務書」有兩個家（按 session vs 按齡），程式碼自陳而零機械物 | P2 | 兩筆皆以判準落地，不以散文結案 | fixed@R84 |
| DEF-200-103 | 2026-08-12 | R85／P4 失誤歸因重跑 | **最大失誤桶「宣稱先於查證」連兩次量測居首（control lift 亦第一），零機械物**：現行處置只有根 `CLAUDE.md` 鐵律四那段散文，而散文對「當下的模型」零攔阻力（R71 已實證同型） | P1 | 須與「改分群關鍵詞表」同包派工，否則判準與史料分屬兩個持有面（鐵律七） | fixed@R88（證據＝`docs/06_quality/CrossPlatform_R88_Closure_Evidence.md`） |
| DEF-200-076 | 2026-08-11 | R84／W6 agent 錨點與 SSOT（AGT-03／AGT-04） | ①RTM 的 AT 欄位錨在 **SCG-4**，與 SCG SSOT 互斥（SCG-4＝PR Review、SCG-5＝RTM Completeness），同一字串複寫 **14 檔**；②`ci-gate` 已跑 3 支 agent lint，卻**無任何機械物**守「agent 內 SCG-N 錨點 ↔ SCG SSOT」⇒ 錨錯了沒有人會知道 | P1 | 新增 `agent_scg_anchor_lint.py` 並接進 ci-gate | fixed@R84：14 檔逐字訂正，lint standalone rc=0 |
| DEF-200-078 | 2026-08-11 | R84／W6 agent 的跨平台盲區與版本漂移（AGT-06／AGT-05） | ①SD／Architect agent 對跨平台差異**零判準**：mandatory ADR 三條無一問 OS／shell／編碼／路徑 ⇒ 本 repo 付過最多學費的那一類缺陷，在設計階段完全沒有人問；②27 支 agent 的 `version` 全寫死 `v0.18`（框架已 v0.30）且**無 lint 守** | P2 | ①補進 mandatory ADR 問句；②版本改為現查並上 lint | fixed@R84 |
| DEF-200-072 | 2026-08-11 | R84／W5 剖面登記面的兩個缺口 | ①`AutoClaude/tests@darwin` 三個剖面**未登記且不在 `_FULL_SUITE_RUNNERS` 分母內** ⇒ `ci_platform_coverage_problems()` 回 `[]` 而不是一筆缺口，mac 側整片失明（分母 0＝沒有東西可違反）；②`skip_tag_policy._TREE_FILE_FLOORS['AISDLC_SDD/scripts/tests']` 下限過期（釘 23、實測 30 支＝77% < 80%） | P2 | 兩者皆補回分母而非放寬門檻 | fixed@R84 |
| DEF-200-079 | 2026-08-11 | R84／W6 agent 樹缺的兩個角色（AGT-09／AGT-10） | ①共通 `spec_compliance_check` 缺**訂正協議** ⇒ 訂正一句假話時寫下另一句假話（本 repo 已實證多次的形態）；②agent 樹**沒有任何 reviewer／零信任稽核角色**，而四方獨立複審是本 repo 最常用的工作形態＝工具鏈與實際工作方式脫節 | P2 | ①寫進共通段；②新增 `sdd-zero-trust-auditor-zh.yaml` | fixed@R84 |
| DEF-200-094 | 2026-08-12 | R85／P6 SCG SSOT 互斥稽核 | `SDD_GUIDE.md` 的英文閘門名與 SCG SSOT `SDD_SPEC_FIRST_GATE.md` **四支互斥**（SCG-2／3／4／6），而兩份都被當權威引用 | P2 | 以 `agent_scg_anchor_lint.py` 機械消費的那一份為準，訂正 GUIDE 並逐字保留原文 | fixed@R85：P9 以自製抽取器＋正對照組（已知命中 `RTM Completeness Gate`）複驗 SCG-0~6 **互斥數=0**，GUIDE:30-38 保留被訂正原文 |
| DEF-200-177 | 2026-08-21 | R98 四方複審 blocking（DEF-200-131 平行懸案裁決） | `AutoSDD_R85_Archive_Proposal.md` 檔頭懸案（是否納管 `_GOVERNANCE_DOCS`）R85 收尾未處理；R98 現查裁決：**不納管**——未執行提案（§6 清單 R98 前零執行）、無帳本列以本檔為居所、內文座標為 R85 快照易漂移 | P3 | 詳見該檔檔頭新增裁決段落；未改 `governance_docs.py` | closed-by-decision（R98） |
| DEF-101-596 | 2026-07-30 | R60 r3 Pkg-C 全樹掃描順帶發現（**非四方回報／Pkg-C 自查**） | **`AutoClaude/tools/hub_sync.py:478` 把 outbox 路徑寫死在生產碼裡**，使 `test_hub_sync.py` 有與 `DEF-101-595` 同型的並行競態。差別＝**根因在生產碼**，改測試無法根治 | P2 | AutoClaude 側生產碼 | fixed@R99｜outbox_root 改可注入參數，test_hub_sync.py 55 passed rc=0（R99 書記複驗重跑通過） |
| DEF-200-100 | 2026-08-12 | R85／P3 mac 真機測試 | mac 上 11 支 claude-CLI 測試 **逾 600s 未完成**、成因未歸因 ⇒ 那一族在 mac 的實際狀態未知，既不得讀成綠、也不得讀成紅 | P2 | 先歸因（pty spawn？CLI 互動等待？）再決定是否標記 skip | fixed@R88（證據＝`docs/06_quality/CrossPlatform_R88_Closure_Evidence.md`） |
| DEF-200-218 | 2026-08-24 | R100 收尾窗口（push 被本機閘門擋下，逐項排除） | 三項既存缺陷首次被 `git push` 觸發的 `run_root_unittests.py` 抓到，逐字座標與修法見 `CrossPlatform_R102_Scan_Findings.md` §D | P2 | 三項皆已直接修復，無需承接 | fixed@R102：詳見 §D |
