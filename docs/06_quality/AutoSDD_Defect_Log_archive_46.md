# AutoSDD Defect Log — Archive 46

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **2 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-705`、`DEF-101-708`
>
> **本次操作備註**：R68 入帳後第二次輪替（archive_45 因 --ack-handoff 為逗號分隔單一參數、我誤用三次 flag 致只有最末筆生效，本次補搬前兩筆）。判準④ 具名承認 2 筆 false positive，皆本輪自撰、逐字複核無待接工作：DEF-101-705（marker 解鎖條件 出現在引述 DEF-101-702 的解鎖條件）、DEF-101-708（marker deferred 出現在引述硬規則② 散文原文）。🔴 本次輪替後主檔距 fail 線仍不足 DEF-101-676 自訂的 10240 健康門檻——該判準是真實訊號，本輪選擇據實訂正 DEF-101-676 狀態，而非再 ack 更多列去湊數字（湊數字正是該列自己立判準要防的事）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-705 | 2026-08-02 | R68 修復波執行中（主控自查，非四方回報） | **修復波 11 包在執行中全數撞週配額上限而中止**（`weekly limit`，11/11 agent error），留下 34 檔已改＋4 檔新增、約 3170 行的**半套工作樹**，且**無任何一包交回結構化回報** ⇒ 「哪一筆 finding 真的修好了」在本輪結束時沒有可信來源。工作樹當場以 `git stash create` ＋ tag `R68-wip-preserved` 保全（CLAUDE.md〈可重啟點四條件〉），隨後由主控逐一收斂：根層 unittest 由 **23 筆紅**收到 0 紅，並補上半套修復缺的回歸鎖 | P2（流程；不影響任何程式行為，但會讓後續讀者誤把 git diff 讀成「已驗證的修復」） | 根層迭代流程（收輪紀律） | fixed@R68（**僅指流程處置已完成**，非指 69 筆已修）：本列即誠實揭露載體。處置＝(a) 存活清單落 `CrossPlatform_R68_Scan_Findings.md` 並在檔頭明寫「修復狀態不在本表」；(b) `DEF-101-702` 的解鎖條件要求逐筆判定附實跑取證；(c) 本輪一切「已修」宣稱以帳本列為準、不以 git diff 為準。**教訓**：並行修復包的規模須先對配額餘裕做預算，否則保全成本高於節省的時間 |
| DEF-101-708 | 2026-08-02 | R68 Scan-G（沙箱普查，HEAD 版工具 × HEAD 版帳本） | **硬規則② 只落地了前半句**：散文寫「任何 `deferred`／`backlog` 必須指向一個存在的輪次**或**明確標為『未指派』」，而 `orphan_backlog_problems()` 只做了「輪號比大小」，後半句在程式裡是 `if not handovers: continue` 一個**短路出口**。實測 77 筆未結列中僅 6 筆（8%）帶輪號而真的走到比較、15 筆寫「未指派」，其餘 **61 筆兩者皆無全部短路放行**，其中 43 筆帶「排入下一輪」「擇機」「留待下輪」這類**散文式延後**——正是規則想擋的 | P2 | 根層 `tools/check_defect_log_crossref.py` | fixed@R68：新增 `unpinned_handover_problems()` 落地後半句＋存量豁免 `_UNPINNED_HANDOVER_GRANDFATHERED`（57 筆，棘輪 `_UNPINNED_HANDOVER_CEILING` 只准變小）＋ `stale_grandfather_problems()` 自檢防死名單。🔴 兩道綁定 `_DEFECT_LOG == _DEFAULT_DEFECT_LOG`（豁免名單是相對真實主檔的存量，餵合成 fixture 會兩向假紅）；該綁定不得退化為「靜默關掉」，由 `TestUnpinnedHandoverAndStaleGrandfather` 的結構鎖守（見 `DEF-101-707`） |
