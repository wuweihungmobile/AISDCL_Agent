# AutoSDD Defect Log — Archive 27

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 11 筆 **R57 自身**已結列（DEF-101-479／480／481／482／483／484／485／487／488／489／490，狀態皆 `fixed@R57*`），於 R57 **收尾**（2026-07-27）搬遷。
>
> **為何破例搬遷本輪自身條目（第三次歸檔）**：R57 是異常長的一輪——五維掃描 18 項候選、**四輪**四方複審（round 1 全 REJECT 23 項／round 2 全 REJECT 16 項／round 3 三方 APPROVE 11 項／round 4 四方全 APPROVE 11 項）、累計修復 60+ 項，單輪帳本毛增約 41KB。動工前已搬 15 筆（archive_25）、收尾再搬 5 筆（archive_26）仍不足：寫入 round 4 條目後主檔達 **259,591 bytes**，距 `tools/check_defect_log_crossref.py` 的 256KB（262,144）**fail 硬閘僅剩 2,553 bytes**，任何後續改動都會直接觸發 CI 硬阻擋。比照 archive_24 已確立的判準（「該輪已完全收斂的自身條目」）執行。
>
> **刻意留在主檔的 R57 條目**：`DEF-101-478`（NTFS 四處同因缺陷——被 crossref 指定掃描目標引用）、`DEF-101-486`（windows-compat-ci 檔頭失實——同樣被引用）、`DEF-101-491`（歸檔立帳）、`DEF-101-492`（流程紀實含掃描 workflow 全滅的教訓）、`DEF-101-493`（帳本體積治理與 R58 交接）、`DEF-101-494`~`501`（round 2~4 的複審與修復紀錄，**留給 R58 直接對帳**）。
>
> **位元組變化**：主檔 259,591 → 約 241.8KB（釋出約 17.8KB，退回 fail 硬閘之下約 20KB 餘裕；仍在 240KB warn 帶內，`check_defect_log_crossref.py` 印 warning 但 rc=0 不阻塞）。立帳見主檔 DEF-101-493 與本輪 R58 交接段。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-479 | 2026-07-27 | R57 Scan-A2（實際執行路徑）＋round 1 Architect F4／QA ⑤／SD 兩項正則訂正 | **`uv pip install -e .[dev,notifications]` extras 未加引號，在 macOS 預設 shell（zsh）下整條指令根本不會執行**：zsh 自 Catalina 起為預設登入 shell，對未加引號的 `.[dev,notifications]` 做 filename generation；repo 內無匹配檔名時 zsh **在執行指令之前就中止命令列**。三段實測對照：`zsh -c 'echo REACHED .[dev,notifications]'` → `zsh:1: no matches found`／rc=1（echo 從未執行）；加引號版 → `REACHED .[dev,notifications]`／rc=0；`bash -c` 同一行 → 正常。故此為**單邊平台缺陷**——Windows（PowerShell／Git Bash）開發者永遠不會遇到，macOS 開發者看到的則是一個與套件完全無關的錯誤訊息。諷刺的是 ONBOARDING §7 各輪校正註記中維護者自己實跑時寫的都是加引號版，可見實務上已知，但**權威指令站點一律沒加** | P3（macOS 上「照文件從零安裝」的第一步即失敗，且錯誤訊息完全不指向真因） | 本輪修復（D 包 4 份文件＋主控擴大至全 repo＋新機械鎖） | fixed@R57：①D 包修 `CLAUDE.md`／`ONBOARDING.md`（含 §5 雷區表新增一列）；②**主控複核發現 D 包受檔案邊界所限漏了使用者最先讀的文件**，補完 `AutoClaude/README.md`(9)／`docs/AISDLC_Agent_UserGuide.md`(4)／`AutoClaude/docs/AutoClaude_Guide.md`／`AutoClaude/docs/04_planning/AutoClaude_Guide.md`／`AutoClaude/docs/08_deployment/Local_CI_Parity_Guide.md` 共 5 份 16 處；③**round 1 QA-R57-05／ARCH-04 交叉揪出更要命的一類**——`AutoClaude/tools/git-hooks/pre-push:78` 與 `AutoClaude/tools/local_ci_gate.py:186` 是**執行期真的印給使用者複製貼上**的訊息（push 被閘門擋下當刻的唯一指引），mac 使用者照做 → `no matches found` → 再 push 再被擋，形成迴圈；已修；④新增機械鎖 `tools/tests/test_extras_quoting_zsh_safety.py`（4 test）：掃描面 tracked `*.md`+`*.sh`+`*.py`+三處 git-hooks（`.ps1` 刻意排除，PowerShell 無此 glob 語意）、掃描面下限 500 防靜默縮面、正確形態下限 20 防「整段刪光也綠」、語言中立的 `zsh-glob-ok:` 行內豁免＋豁免稽核（理由非空＋總數上限，防豁免退化為後門）；⑤round 1 SD 另訂正該鎖自身兩處：旗標段 `(?:-[A-Za-z]+\s+)*` 只吃單槓短旗標故 `pip install --upgrade -e .[dev]` 整條漏報（實測 old=False→new=True），前方 lookbehind `(?<!["'])` 為死碼（6 組樣本在有/無它之下結果完全相同，因 `\.\[` 本身已要求該位置是 `.`）。bug-injection：回退任一處引號→紅、豁免理由留空→紅、還原→全綠 |
| DEF-101-480 | 2026-07-27 | R57 Scan-D（守門鑑別力）＋round 1 ARCH-06／QA-R57-07／SD-R57-01 三方追打 | **根層 unittest 數量下限釘選 `MIN_TESTS` 自 R15 起連續 11 輪未重釘**：釘 290、實況 530，鑑別力失效 45%——`tools/tests/` 可靜默蒸發 240 個測試而四道 gate（pre-push root-infra leg／root-infra-ci step 8／windows+macos smoke）依然全綠。根因不只是「忘了改」，而是**下限語意天生只在往下掉時說話**，實況上漲時零訊號 | P2（守門工具自身鑑別力單調衰減且無任何提醒機制） | 本輪修復（A 包＋round 1 三方追打後重新設計） | fixed@R57：重釘為最終實測值，並補**兩層且門檻刻意不同**的機械提醒：①WARN 層 `warn_ratchet_drift`（實況 > 下限 × `RATCHET_WARN_RATIO`=1.10 即印一行「該重釘了」，只印不擋）；②保鮮期層 `test_current_pin_is_not_already_stale`（實況 > 下限 × `RATCHET_STALE_RATIO`=1.25 即 FAIL）。**兩層門檻必須不同**——round 1 ARCH-06 實測 `ratchet_drift_message(698, 558)` 回傳非 None，證明單一 1.25 門檻下「只 WARN 不 fail」的註解與行為互相打臉（WARN 一響即等於閘門已紅，WARN 層形同虛設）；現行 [1.10, 1.25] 緩衝區的語意是「先被提醒、還沒被擋，收輪時順手重釘」。純 WARN 擋不住「11 輪沒人重釘」的心理機制（常亮警告＝背景噪音），故必須有一道會紅的線。代價明說：累積新增超過 MIN_TESTS × 0.25 支測試就**必須**重釘。**另記一項主控自身的方法論錯誤（SD-R57-01／QA-R57-07 抓出）**：本輪兩度以「基準 + 本包新增支數」的算式推得 552／558，兩次都當場就與實況不符（實況 562／584）——原因是並行修復包與四方 agent 持續在增減測試。該行註解已改為明文判準：**重釘＝所有並行 agent 停工後、主控在最終工作樹實跑並直接填「發現 N 個測試」的 N，不做任何加減推算** |
| DEF-101-481 | 2026-07-27 | R57 R56-backlog 複核（F1）＋**Scan-E（Architect-Design，架構裁定改判）**＋round 1 Architect F1/F2（P1×2）＋QA-R57-01 | **CI 掃描樹三份鎖的抽取式全部硬綁 `-Path` 具名參數，而 PowerShell 的 `-Path` 是可省略的位置參數**：往 `root-infra-ci.yml` 第 2 道插入第 5 棵樹 `$files += @(Get-ChildItem docs/scripts -Recurse -Filter *.ps1 -File)` → `test_ps1_bom`／`test_smoke_ci_sync`／`test_ps51_compat` 共 20 支全綠，擴面完全隱形。**這推翻了 R56 round 7 在三份註解寫下的「不論路徑長什麼樣，多一棵樹必紅」宣稱**（該宣稱自稱「與字元類完全無關」屬實，但仍與**參數形態**強相關）。實害：CI 與本機三處掃描面分歧時零訊號，該組鎖唯一的職責正是偵測此分歧；反向（本機加樹 CI 未加）則使該樹的 `.ps1` 逃出 CI 的 PS5.1 語法解析與 UTF-8 BOM 守門 | P2（守門靜默失效）→ round 1 追加 **P1×2** | 本輪修復（A 包收斂 SSOT，round 1 兩度追打後補完） | fixed@R57 round 2：①A 包新增 SSOT `tools/tests/_ci_scan_anchors.py` 並**改判 R56「三份獨立複本有鑑別力價值、刻意不收斂」的裁定**——理由：三份讀的是同一個檔案的同一個 step，彼此不構成獨立觀測，實證就是同一盲點被抄三遍、同時失效；②**round 1 Architect F1（P1）指出收斂後仍是淨退化**：新錨 `-Recurse -Filter *.ps1 -File` 雖修好位置參數（V1），卻對「`-Filter "*.ps1"` 加引號」「`-Include` 取代 `-Filter`」「`Join-Path` 計算式路徑」三種形態全盲，**其中兩種是 R56 舊錨抓得到的**；且 SSOT docstring 與三份 assert 訊息又寫下「與引號界定全部無關／任何參數形態必命中」的**同款假宣稱，正是本輪要根治的缺陷類別當場復發**。已補第三條 cmdlet 出現次數錨 `CI_GCI_CALL_RE`（含 `gci`/`dir`/`ls` 別名，以 `(?<![\w$.\-])` 開界避免 `$dir`/`.ls` 偽陽性），對 V2/V5/V6/V7 與 QA-R57-01 的「位置參數＋`-Filter` 前移」組合全部翻紅；三處宣稱全面改寫為逐條列出「涵蓋／不涵蓋」的可查證措辭（明列殘餘風險＝改用 `[System.IO.Directory]::GetFiles` 不在涵蓋內、且 cmdlet 錨不剝註解故註解提及也會 +1）；③**round 1 Architect F2（P1）指出 SSOT 沒有呼叫端鎖**——實測把任一呼叫端改回自寫舊正則，558 支測試全綠 rc=0，`git grep` 確認除 3 個 import 外零強制力，違反本 repo 既有「SSOT 必配 callsite lock」慣例。已補 `TestSsotCallsiteLock` 四支（import 完整性／實際呼叫／不得自寫 `Get-ChildItem` 正則／roster 等值鎖），**全走 `ast` 而非文字比對**——Python `ast.parse` 建樹時即丟棄註解、字串字面值也不會變成 `ImportFrom`/`Call` 節點，故 QA-R57-02 那種「接線留在尾隨註解裡」的繞過在結構上無法成立，另補一支合成原始碼測試證明之 |
| DEF-101-482 | 2026-07-27 | R57 R56-backlog 複核（F3）＋round 1 QA-R57-02／QA-R57-03 | **PowerShell 呼叫端鎖只驗 helper 自身、且剝註解不完整**：①`tools/tests/test_find_git_bash_parity.py` 原 11 支測試**全部只驗 `Find-GitBash.ps1` helper 自身行為，零測試驗證三支呼叫端真的有呼叫它**——把呼叫端改回自寫 `(Get-Command bash).Source`（＝DEF-101-099 當年在 `integration_gate.ps1` 踩過的原坑）全部照綠；「三支」計數亦無機械鎖（R56 backlog 項目 4）。②A 包補鎖後，**round 1 QA 立刻找到繞過**：`_code_only()` 只剝 `<# #>` 區塊註解與整行 `#`，**沒剝尾隨行內註解**，故「保留 dot-source、把呼叫換成內聯偵測、行尾留 `# was: = Find-GitBash`」即可全綠。③同源盲點亦存在於 A6 新增的 `test_windows_nightly_anchor_parity.py`，其 docstring 還自稱「錨點只認功能碼行」 | P2（呼叫端接線零覆蓋；補鎖後仍可單一動作繞過） | 本輪修復（A 包補鎖，round 2 補剝註解） | fixed@R57 round 2：①A 包新增 `TestFindGitBashCallSites` 3 支——呼叫端登記表以 `git ls-files '*.ps1'` 全 repo 掃描比對（**等值非下限**：多一個未登記呼叫端→紅、某支悄悄不再用共用 helper→也紅）、逐支驗「dot-source ＋ 實際呼叫」兩件事都在功能碼行上、呼叫端不得重新實作偵測特徵（`bin\bash.exe`／`system32`）；②round 2 新增 `_cut_inline_comment()`（逐字元追蹤單/雙引號狀態，`''`／`""` 連寫與雙引號內反引號跳脫皆處理；`#` 只在引號外且位於行首或空白/`;`/`\|`/`(`/`{`/`,` 之後才視為註解起點，比照本 repo 既有 `test_ps51_compat.split_code_comment` 判準）＋ `_strip_ps_comments()`，兩支測試檔同步植入。bug-injection：QA 原樣的「內聯偵測＋尾隨註解」繞過手法修復後立即 FAILED，還原後全綠 |
| DEF-101-483 | 2026-07-27 | R57 R56-backlog 複核（F4） | WindowsApps guard 雙錨聯集 `_matches_stub_anchor()`（R56 round 6 為 DEF-101-473 抽出）**只在 helper 層被兩支單錨測試守住，掃描呼叫端本身沒被鎖**——把掃描迴圈改回內聯 `_STUB_NAME_RE.search(text) and _STUB_PREDICATE_RE.search(text)`（＝繞過 helper ＋ 把 ∪ 收緊成 ∩ 的複合動作，R56 已記載此動作可讓變體 L 完整逃逸）時，該檔 42 支全綠。R56 round 7 QA 曾自評「需兩個獨立動作複合、判為 backlog 不修」 | P3（保護力缺口，需複合動作觸發） | 本輪修復 | fixed@R57：`TestNoOrphanWindowsAppsImplementation` 新增 `test_scan_loop_goes_through_matches_stub_anchor`——以 `inspect.getsource` 讀掃描迴圈原始碼（先 regex 剝 docstring，因該 docstring 詳述兩個裸錨沿革會造成假紅），斷言必須出現 `_matches_stub_anchor(` 且不得內聯任一裸錨。bug-injection：施以 R56 記載的完整複合繞過後 `Ran 43 tests / FAILED (failures=1)`，還原後 `OK (skipped=1)`（注入前該檔 42 支全綠，證實舊有兩支單錨測試對此複合動作零訊號） |
| DEF-101-484 | 2026-07-27 | R57 R56-backlog 複核（F2）＋round 1 SD-R57-04／QA-R57-06 | `tools/tests/test_platform_utils_dedup.py` docstring 宣稱「全 repo 機械掃描」但**實際掃描面只有 AutoClaude/ 與 tools/**——`AISDLC_SDD/`（含 scripts/、conftest.py、各版 fsm_runtime/）與 `.claude/hooks/` 下的 .py 全在掃描面外（R56 backlog 項目 3） | P3（名實不符：讀 docstring 者會誤以為已全域覆蓋而不再追查） | 本輪修復 | fixed@R57：選**擴大掃描面到名副其實**而非下修 docstring——實測擴面零代價（三個函式名的命中集合完全不變、新增偽陽性 0），既然沒有成本，改 docstring 等於自願放棄一塊真實覆蓋。改用 `git ls-files '*.py'` 而非 `rglob`（AISDLC_SDD/ 下實測 4,829 支 .py 多為 venv/快取，rglob 既慢又得維護排除清單；追蹤檔天然排除，且與姊妹鎖 `test_windowsapps_guard_cross_consistency` 的 repo-wide 掃描同政策）。bug-injection：往舊掃描面外的 `AISDLC_SDD/conftest.py` 追加第 9 份複製貼上定義 → 舊掃描面模擬全綠（假陰性）、新掃描面 FAILED。**round 1 SD/QA 追加**：改用 `git ls-files` 後 `_scan_repo_py_for` 直接對每個回傳路徑 `read_text` 而無 `OSError` 處理，只要有任一 tracked .py 不在工作樹（`git mv` 進行中／已刪未 stage／sparse checkout）整支測試會 error 而非給出有意義訊號；已修（刻意不改成靜默忽略——那會變 fail-open） |
| DEF-101-485 | 2026-07-27 | R57 Scan-C（CI/CD 對稱性） | macOS 本地 smoke 聚合器 `tools/macos_smoke_local.sh` 有一整步 [7/7] 靜態錨點鎖（守 `run_local_nightly.sh` 的 RunId log／14 天輪替／當日去重），**Windows 側完全沒有對等守門**——而 `AutoClaude/tools/run_local_nightly.ps1` 的對等機制（RunId log `nightly_{Today}_{RunId}.log`、`nightly_latest.log` pointer、14 天輪替且排除 pointer）與 `tools/install_windows_nightly.ps1` 的 `-StartWhenAvailable`／`-WakeToRun` 全部存在，**機制對稱、只有守門不對稱** | P3（Windows 側 nightly 機制退化無訊號） | 本輪修復 | fixed@R57：落地方式選「補 unittest」而非「補 `windows_smoke_local.ps1` 步驟」——新增 `tools/tests/test_windows_nightly_anchor_parity.py` 6 支靜態錨點鎖。三項理由記於該檔 docstring：①macOS [7/7] 自述「唯讀 grep 工作樹、平台無關」，本就不需要 pwsh；②補成 unittest 則四道守門（pre-push root-infra leg／root-infra-ci step 8／windows+macos smoke）全部跑得到，覆蓋面嚴格大於只補進 Windows 本機 smoke（後者只有真 Windows 機器手動跑才生效，而本輪主題正是 DEF-101-348「Windows 專屬守門長期沒在 Windows 上跑過」）；③補進 `.ps1` 需連動 `$MinPass` 與 `test_smoke_ci_sync` 的步驟語意鎖，且在 macOS 上無法真跑驗證。誠實記載：macOS 側的「RunAtLoad 當日去重」在 Windows 側**本質上不需要**（schtasks 無對應的登入即觸發語意） |
| DEF-101-487 | 2026-07-27 | R57 Scan-C | `windows-smoke`（阻斷式 push 閘門）`timeout-minutes: 25`，工作量相當的 `macos-smoke` 為 35——方向與兩種 runner 的實際速度差**相反**（Windows runner 通常較慢，且 Windows Defender 即時掃描會顯著拖慢 pip/pytest，這點 ONBOARDING §5 自己就有記載） | P4（尚未實際超時，但餘裕方向錯置，一旦觸發即為阻斷式閘門偽紅） | 本輪修復 | fixed@R57：`timeout-minutes` 25 → 35 與 macos-smoke 對齊，並就地補 WHY 註解說明「Windows runner 較慢＋Defender 掃描」的方向性理由，避免未來有人再依「步驟數相近」把它調回去 |
| DEF-101-488 | 2026-07-27 | R57 Scan-A2 | `ONBOARDING.md` §7「常用驗證指令」是整份雙平台文件中**唯一只給 bash 形態、未給 Windows 對等**的指令區：`PYTHONUTF8=1 lint-imports` 的 `VAR=value cmd` 前綴語法**在 PowerShell 不存在**，會報 `The term 'PYTHONUTF8=1' is not recognized`（實測 pwsh 7） | P3（雙平台文件的單邊缺口，Windows 使用者照抄必失敗） | 本輪修復 | fixed@R57：§7 補 PowerShell 對等區塊（`$env:PYTHONUTF8=1; lint-imports`）。**誠實記載未竟之處**：`tools/tests/test_onboarding_parity_interlock.py` 只鎖 §6 bash-only 清單與 §8 排程家族兩處錨定行、**未涵蓋 §7 指令區**，故本次補齊沒有機械鎖看守，未來 §7 新增 bash-only 指令不會有訊號——列為 R58 backlog |
| DEF-101-489 | 2026-07-27 | R57 Scan-B | `AISDLC_SDD/scripts/tests/test_sanitize_component_callsite_frozen_versions.py` 的 component_sanitizer 呼叫點 AST 前瞻掃描**只涵蓋各版本的 `tools/fsm_runtime/`**，`.claude/hooks/`（跨 30 版共 139 支 .py）與 `tools/arch_fitness/`（30 支）從未進入掃描面；且原本只有「版本數 >= 30」的下限，**掃描面本身縮小不會被發現** | P4（前瞻鎖的掃描面缺口；擴面當下 0 offender，價值全在前瞻） | 本輪修復 | fixed@R57：新增模組層 `_SCAN_SUBDIRS = ("tools/fsm_runtime", ".claude/hooks", "tools/arch_fitness")` 與 `_MIN_SCANNED_DIRS` 下限，內層改為對三個子目錄各跑一次 `iter_module_files` + `find_offenders`，offender 標籤由 `[version]` 改為 `[version/subdir]`。先實測確認缺口為真而非照抄回報（169 支生產 .py 從未被此鎖看過、但全數 0 offender，故擴面對現況判定零改變、不會因凍結版本出現不可修的紅燈而卡死）。bug-injection：在 `AISDLC_SDD_v0.30/.claude/hooks/post_commit_drift.py` 植入未淨化呼叫點後轉紅 |
| DEF-101-490 | 2026-07-27 | R57 round 1 SA-R57-05＋C4 覆核（WebSearch 查證） | ①`tools/check_gha_action_versions.py` 只掃**根層** `.github/workflows/*.yml`（非遞迴、寫死單一目錄），`git ls-files` 實查掃描面外尚有 **30 份** git-tracked 的 `AISDLC_SDD/AISDLC_SDD_v0.0X/**/*.yml` workflow 檔從未被版本一致性守門看過；②DEF-101-434（`actions/upload-artifact@v5` 於 ubuntu job 仍跑 Node20、GitHub 2026-09-16 起自 runner 移除 Node20）R55/R56 兩輪裁定「與平台無關、deferred」——今天距該日已不足兩個月 | P3（守門掃描面盲區）／P3（時程逼近的已知 CI 中斷風險） | 本輪修復 | fixed@R57 round 2：①擴面納入凍結版 workflow 並補掃描面下限（防靜默縮面，本 repo 慣例）；②WebSearch 查證後確認 Node20 退場時程仍如帳本所載（2026-06-16 起 runner 預設改 Node24），`actions/upload-artifact` 已升至 v6（Node24），本輪一併由 v5 升 v6，`check_gha_action_versions.py` 實跑確認「4 個 actions/* action、共 56 處 `uses:` 宣告皆同名同版」。**DEF-101-434 至此結案**（原 open→fixed@R57） |
