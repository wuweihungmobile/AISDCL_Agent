# 跨平台輪次成熟度判準 M1〜M6（SSOT）

> **這是 M1〜M6 的唯一家。** 任何其他文件（含各輪 `CrossPlatform_R*_Scan_Findings.md`、
> `R*_HANDOFF.md`、ADR）談到這六條時，一律指向本檔，不得再抄一份判準表。
>
> 🔴 **為何搬家（R78 ARCH-05）**：這六條原本住在 `docs/06_quality/CrossPlatform_R76_Scan_Findings.md`
> 的 §R76-MATURITY——也就是說，**治理層的成熟度 SSOT 寄生在一份「輪次專屬的掃描發現文件」裡**。
> 那種文件按定義是「某一輪當時看到了什麼」的凍結記錄，不會有人回頭維護它；而 M1〜M6 是要**逐輪**
> 拿來判「這一輪算不算成熟」的活判準。兩種壽命不同的東西放同一個檔，後果在 R78 現形：M5 的攔截率
> 在三個地方各寫了一份，三份全部停在**修復前**的值，而**沒有任何機械物在守它的新鮮度**。
>
> 🔴 **本檔的體例（就是那道新鮮度機制）：量測值不寫在文件裡，只寫「哪一支載具會印出它」。**
> 沒有寫下來的數字不會過期。守這條的機械物：
> `tools/tests/test_doc_loc_baseline_freshness_r60.py::TestR78MaturityCriteriaSsot`。

## 共同立場（四方唯一完全一致的一句）

**「閘門全綠」與「成熟」無關。** R75 的 12 筆 blocking 有 8 筆是「閘門自己沒有鑑別力／
射程失明」；R76 存活的 12 筆裡，**沒有一筆**會被任何現有閘門攔下。所以成熟度的量必須
量「缺陷穿過幾道閘」，不能量「有幾道閘」——後者可以靠新增鎖無限刷分，而新增鎖正是
本 repo 目前缺陷的最大單一來源。

## M1〜M6　判準表（每一條都可用本 repo 現成載具跑出數字）

| # | 判準（可證偽的斷言） | 量測配方（**數字一律現跑，本表不登載**） | 成熟門檻 | 來源 |
|---|---|---|---|---|
| **M1** | UEP 抵達可辯護地板，且該狀態被**正式承認**而非每輪重新解釋 | `python -m unittest tools.tests.test_adr_xplat001_c1c2_lock`（末行印 `[Scan-H triplet]` 三元組）＋ `ls` ADR-XPLAT-002 §8.1 回執表。自 R65 起 ΔUEP 連續為 0 的**狀態**仍成立；§8.1 自 R67 建立至今**空表** | 二擇一：§8.1 出現回執且 UEP 較現值少 1；**或** ADR 正式宣告現值為終態並把 `_EXEMPT_PAIRS` 凍成 shrink-only、不再列為「目標」 | Architect |
| **M2** | 假宣稱密度單調下降 | 分子＝該輪複審抓到的「失實宣稱」筆數；分母＝該輪新帳本列數，**現查**：`python tools/check_defect_log_crossref.py --unresolved-count` 取當前輪代號後，數「發現情境」欄提及該輪的列（跨主檔＋所有 archive）。🔴 **本表不得登載分母常數**——R76 實測分母比當時寫死的值大了約四成，兩個密度值因此都偏高，**而它就長在專門用來量失實宣稱的那條判準上** | **在修好下列三點之前不得宣告達標**：<br>①**分子零≠滿分**：該輪未執行四方複審一律判 **N/A**，禁記 0（R72／R74／R77 皆為額度中止而零複審，照原算式反而完美達標）。<br>②**分母為 0 時不適用**：`current_round()` 由帳本推得且刻意 fail-open 落後一輪，輪初該輪零新列 ⇒ 開場除以零，此時只能記 N/A，**不得**讀成任一極端。<br>③**門檻是絕對值不是比率**：連續三輪**≤1 筆且無任何一筆 P1**才算達標（寫成比率會讓讀者以為一兩筆還在容許範圍） | SA |
| **M3** | 新增判準的**第三方**注入通過率＝100%（作者自證不計分） | 複審者對該輪每一支新鎖各做一次注入，逐筆記紅綠 | **連續兩輪 100%，且抽樣面含既有鎖庫隨機 20 支**（既有鎖抽樣至今未做＝目前最大量測缺口） | SD |
| **M4** | 「宣稱射程 ≡ 實作射程」零落差 | 對每一道鎖的散文宣稱逐句取其**可執行判準**重跑（`TestR75IronLawMechanismSubstance` 是這條的第一個機械化實例） | **一輪內 0 筆**，且該檢查本身有注入證明 | SD |
| **M5** | 雙向注入攔截率：mac→Win 與 Win→mac 兩個方向都 ≥80%，且差距 ≤10pp | 🔴 **現跑載具，本欄不寫數字**：`python -m unittest tools.tests.test_platform_neutral_paths.TestXplatInjectionMatrix`，`setUpClass` 末行印 `[Xplat injection matrix] Win2mac=<hit>/<total> mac2Win=<hit>/<total>`。語料每輪強制抽換 ≥2 題防過擬合，且題數只准增（`test_the_corpus_covers_both_directions_and_is_not_shrinking`），攔截數只准升（`test_the_interception_rate_only_improves`）。**已知的質性缺口**：Win→mac 命中的全在檔名／路徑／編碼層，**程式碼語意層仍是 0**；mac→Win 那一格**不需要 mac 真機**（是靜態掃描面的缺口），需要 mac 真機的是執行期那一半 | 兩方向皆 ≥80% 且差距 ≤10pp，**連續三輪不倒退** | QA |
| **M6** | **從未被執行過的測試歸零**：每一支測試都至少在某條**會被人看到結論**的軌上真的跑過一次 | 對每個 skip 群組實跑「解除條件」並記 rc；`Skipped_Test_Inventory` §4.7.1 的三基線即該形態 | 盤點文件內「零覆蓋」欄為 0，且每一格都有當輪實跑 rc 佐證 | Fixer |

## 為什麼是這六條，而不是「缺陷數下降」

- **M1** 量的是「護欄層有沒有停止自我增殖」。Architect 的核心立場逐字是：成熟不是「缺陷變少」，
  是「護欄層停止自我增殖，而缺陷從結構性降級為單點」。UEP 連 9 輪不動卻仍掛在「目標」欄，
  就是護欄層在原地繞圈的證據。
- **M2／M4** 量的是「這一輪講了幾句與磁碟不符的話」。SA 的判準：那條曲線目前**沒有在降**，
  因為每輪都在造新的鎖、新的鎖又帶來新的散文宣稱。
- **M3** 是唯一能防「加一道沒有鑑別力的鎖來刷分」的設計：**作者自證不計分**。
- **M5** 直接對應掌舵者第 3 點（mac 開發時 Windows 不落差，反之亦然），而且它量的是缺陷
  穿過幾道閘，加鎖不會讓它上升。
- **M6** 對應掌舵者第 5 點（挖深、清技術債）。實證：**skip 是技術債裡最便宜的藏身處**，
  因為它在摘要裡長得像「乾淨」。

🔴 **這六條刻意沒有一條是「缺陷數 ≤ N」**。本 repo 已有判例證明那種門檻會被「少寫幾列帳本」
滿足；六條全部是**比率或結構性狀態**，把列藏起來只會讓分母變小而分子不變，數字反而更差。

## 現況總判：**未成熟**（六條裡 0 條達標）

| 判準 | 達標？ | 距離（**質性描述；凡量測值一律指向載具**） |
|---|---|---|
| M1 | ❌ | 需要一次 ADR 級拍板（不需 code） |
| M2 | ❌ | 仍差一個數量級；且判準本身的三個結構缺陷（分子零≠滿分／分母為 0 不適用／門檻改絕對值）R76 才寫進上表 |
| M3 | ❌ | 第三方抽樣面尚未含既有鎖庫 |
| M4 | ❌ | 每輪仍有數筆散文與實作不符（R78 本身就修掉五筆） |
| M5 | ❌ | 兩個方向都遠低於 80%，且**程式碼語意層仍是 0**。R77 動工前 mac→Win 為零攔截（整類無判準），同輪補上第六道判準後兩個方向都往上動了——**現值跑上表 M5 那一行的載具**，本欄不登載 |
| M6 | ❌ | 「零覆蓋」已從三位數壓到個位數，但同時曝出數支從未被執行過的紅 |

> 🔴 **誠實劃界（本檔的機械物守得到什麼、守不到什麼）**：
> `TestR78MaturityCriteriaSsot` 守的是 ①本表只有一個家（別處出現 M 列必須指回本檔）、
> ②M5 那一列必須具名載具、③治理面上任何「方向＋攔截率」字面值要嘛與現場活值相符、
> 要嘛帶 `xplat-rate-history:` 標記說明它是歷史值。
> **它守不到**「達標欄的人工評定有沒有跟上這一輪」——那仍是人工判斷，本輪未機械化，
> 刻意不發明一個「幾輪內必須重評」的門檻來假裝守到了。
