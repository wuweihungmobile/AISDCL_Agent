# AutoSDD Defect Log — Archive 25

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 15 筆 R56 round 4~7 已結列（狀態 `fixed@R56`／`fixed@R56 round 6`，且未被 `ONBOARDING.md`／`.github/workflows/{windows,macos}-compat-ci.yml`／`AutoClaude/docs/05_development/SD10_PG_Contract_NextAction.md` 四份 crossref 掃描目標以「DEF-ID」樣式引用），於 R57 **動工前**（2026-07-27）搬遷。搬遷 ID：DEF-101-457／458／459／461／463／464／465／466／467／468／469／472／473／474／475。
>
> **搬遷時機的理由（沿用 archive_23 的「動工前先騰空間」判準）**：R56 收尾交接明載「R57 若同樣是長輪，應在寫入前先評估歸檔」。R57 動工時實測主檔 244,028 bytes——距 `tools/check_defect_log_crossref.py` 的 240KB（245,760）**warn 線僅剩 1,732 bytes**、距 256KB（262,144）fail 硬閘剩 18,116 bytes，而 R56 單輪淨增約 20.9KB > 18,116，**先寫後歸檔必撞硬閘**。故比照 archive_23 先例於動工前執行。
>
> **判準說明**：沿用 archive_24 已擴大的判準「該輪已完全收斂的自身條目（`fixed@`／`wontfix`）＋零 crossref 引用」。R56 round 4~7 區間共 20 列（DEF-101-457~476），其中 **5 列刻意留在主檔**：`DEF-101-460`（archive_23 立帳）／`DEF-101-476`（archive_24 立帳）——歸檔立帳條目留主檔供追溯，比照既有慣例；`DEF-101-462`／`DEF-101-471`（流程紀錄，含交接 R57 的實況敘述）；`DEF-101-470`（狀態 `open（deferred）`，不符「已結」要件）。另 `DEF-101-477`（R56 round 7 四方收斂確認）亦留主檔——其 ④⑤ 兩項正是 R57 本輪處理中的 backlog，搬走會使本輪對帳斷鏈。
>
> **被程式碼引用不阻止搬遷**：`DEF-101-461` 目前仍被 `tools/tests/test_ps51_compat.py` 引用。此依 R56 round 6 SA 舉出、主控採納的既定判例——crossref 只掃四份指定文件、不掃程式碼，故「被程式碼引用」不構成阻卻事由（先例：archive_23 內的 DEF-101-420 仍被 `tools/check_gha_action_versions.py` 等三處程式碼引用）。
>
> **位元組變化**：主檔 244,028 → 217,700 bytes（釋出 26,328），退出 240KB warn 帶，距 fail 硬閘回復 44,444 bytes 餘裕。立帳見 DEF-101-491（現居 archive_28）。（🔴 R60 round 1 ARCH-R60-01 ④ 訂正：原文的立帳指針只寫到「主檔 R57 條目」、不帶任何 DEF-ID，逸出 `tools/archive_defect_log.py --check` 的指針稽核。）
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-457 | 2026-07-27 | R56 round 1 root 修復包（複核 R55 新接線的守門同步鎖） | `tools/tests/test_root_infra_parity.py` 的「root-infra CI ↔ pre-push 守門同步鎖」數量下限停在 5、實際已 7，且方向單一（只驗 CI⊆pre-push）——實測**刪掉 R55 才新接的第 12 道 CI 閘門，三個測試全綠** | P2（同步鎖對本輪剛加的閘門零鑑別力） | 本輪修復 | fixed@R56：下限更新並改為雙向集合相等；另新增 `test_header_guard_list_tool_names_match_steps`（12 道全覆蓋、檔頭↔step 雙向比對） |
| DEF-101-458 | 2026-07-27 | R56 Scan-E（Architect 架構最佳化評估）＋round 3 Architect 追認「全輪未修亦未進路由清單」，主控收尾親自處理 | **LOC 總量 cap 的治理缺口**：`AutoClaude/tools/check_loc_budget.py` 的總量 cap（total <= baseline × 1.20）自 ADR-SD07-001 v1.0 起**從未有重新校準程序**——§6.1/§6.2 全是 per-file override，全文無 §6.3、無任何總量 baseline 條文；工具 docstring 自述「保留作 sanity check 防爆漲」但實作上 `has_violation` 含 `total_violation` → `return 1`，與 tier/absolute 違規**同級阻塞**（宣稱與行為背離）；`--update` 旗標全 repo 零接線卻未說明是刻意。後果已具體發生：R52 破線後只能靠精簡 docstring 收回、R53/R55 連續貼線、**兩項已確認的真實缺陷（DEF-101-422／432）因「改了會破總量硬閘」而被延後不修**——護欄實質阻擋修復卻無正規出口 | P4（治理文件缺口；但已實際造成兩項缺陷延後，影響非文件性） | 本輪修復（主控） | fixed@R56：ADR-SD07-001 新增 **§6.3「總量 baseline 重新校準程序」**——(a) 觸發條件（已破線，或連續 2 輪 total >= cap-10）；(b) 必要證據（--json 報表＋成長歸因逐項判定死碼/重複實作/真實新功能）；(c) **順位原則「先減後調」**（第一順位刪死碼／收斂重複實作、第二順位零/負增行手法、第三順位才調 baseline，並明文禁止為騰額度而精簡記錄 WHY 的既有註解——後者是本 repo 跨輪防回歸的核心機制，見 R53 DEF-101-416 裁定）；(d) Architect+SD 雙簽＋書面理由＋`.loc_baseline` 與 PR 同 commit。同步訂正 `check_loc_budget.py` docstring 的宣稱矛盾（明示為阻塞硬閘、非 sanity check）並註明 `--update` 是刻意不接線。本輪 DEF-101-438 刪死碼釋出 82 行即為 §6.3 第一順位的實例，已寫入條文作為範例 |
| DEF-101-459 | 2026-07-27 | R56 round 3 Architect bug-injection 證偽＋主控獨立複驗＋主控追查連鎖引用 | **DEF-101-433 的前提不成立**（見該列狀態更新），且其「比例原則裁定」被兩處當作方法論判例引用——`tools/tests/test_windowsapps_guard_cross_consistency.py:478` 與 `docs/06_quality/CrossPlatform_Scan_Dimensions.md:49`。一個建立在不成立前提上的裁定被當先例沿用，會讓未來輪次繼續以錯誤依據放行「靜態掃描類防護不追殺」的判斷 | P3（錯誤判例的連鎖擴散；三個領域 agent 皆以「越界」為由未處理，主控路由清單原亦漏收） | 本輪修復（主控） | fixed@R56：①`check_wrapper_thinness.py` docstring 補職責邊界互相標註（本檔管「已註冊薄殼不退化」、`check_script_parity.py` 反向驗證「新增薄殼是否被註冊」，兩者互補，缺一即前瞻盲區）——這正是本則連兩輪被誤判的根因；②兩處判例引用改錨定到 **DEF-101-333**（R40 三方各自構造出不同類型繞過後的四方一致裁定，才是乾淨的方法論邊界判例），並就地註明改錨原因。主控獨立複驗：建 `tools/zz_r56main.{sh,ps1}` 探針→`check_script_parity.py` rc=1 紅燈並列出三條納管途徑→移除後 rc=0→`git status` 零殘留 |
| DEF-101-461 | 2026-07-27 | R56 主控收尾自查（因 skipped 數異常觸發警覺） | **主控本人的量測方法論失誤**：動工前用 `python3 -c "import psycopg2" 2>&1 \| tail -1` 檢查 venv 污染，成功 import 時「零輸出」被誤讀為「乾淨」（實際恰恰相反），據此在四方任務書寫下錯誤基線「根層 .venv 未受污染（本輪已實測）」；收尾時在該受污染 .venv 量得 3829 passed / **146** skipped，因 skipped 自 210 驟降 64 才觸發警覺，回查證實 .venv 內有 psycopg2 2.9.12＋SQLAlchemy 2.0.51，數字全程不可用。此為 R13／R32 同族教訓第三次復發（前兩次是「量測時用錯 venv」與「既有 venv 渾然不覺被污染」，本次是「檢查手法本身無鑑別力」） | P3（若未察覺會把虛高數字寫入 SSOT） | 本輪處理（主控） | fixed@R56：改於全新臨時目錄建乾淨 venv 重量（import psycopg2／sqlalchemy 皆 ModuleNotFoundError 確認），正式基線 **3731 passed / 210 skipped**，已回填 ONBOARDING.md §7 並在該處誠實揭露本失誤。**方法論訂正（供後續各輪沿用）**：污染檢查一律改用在乾淨環境會**印出東西**的寫法（如 `python3 -m pip list \| grep -E "psycopg2\|sqlalchemy"`），判準是「輸出在乾淨與污染兩種情況下長得夠不夠不一樣」，而非「指令有沒有報錯」；另 skipped 數字是比 passed 更靈敏的污染哨兵（passed 增加永遠像好事，skipped 驟降才會露餡） |
| DEF-101-463 | 2026-07-27 | R56 round 3 Architect 提出、補完包 B 落地（SA round 4 指出本項未獲專屬帳本列，依 R55 DEF-101-431 先例補立） | R56 的 Scan-E 架構評估產出一項判準——**WindowsApps guard 的三語言等價實作（bash／PowerShell／Python）為何結構上不可收斂為單一 SSOT**——但未落實到 `docs/06_quality/CrossPlatform_Scan_Dimensions.md`。該檔已有同款體例先例（「架構判準：evaluator_command 分診問題（DEF-101-412）」）。缺了這段，每一輪的 Architect 都會重新把「三份實作＝SSOT 未收斂」列為候選發現再逐一論證掉——R56 本輪 Scan-E 就是這樣花掉一次 | P4（判準未文件化導致跨輪重複論證） | 本輪修復 | fixed@R56：新增「架構判準：WindowsApps guard 三語言等價實作為何不可收斂（R56 Scan-E）」三段式區段——①**bootstrap 悖論**：guard 的職責是「在取得可用 Python 之前判斷 Python 可用性」（`tools/git-hooks/pre-push` 的 `source` guard → `is_real_python_candidate python` → 才有全檔第一個 `python` 呼叫，順序經修復方與 Architect 各自實查），以 Python 為單一 SSOT 邏輯上不可行；②**既有折衷**：`tools/lib/bash_probe_spec.py` 的「資料抽 SSOT、驗活執行邏輯刻意保三份獨立以維持鑑別力」，三份實作改以機械 parity 鎖保證等價而非收斂為一份；③**分診問句**：「這個重複是實作重複（該收斂），還是 bootstrap 期／語言邊界造成的必要複本（該改用 parity 鎖）？」。修復方另主動把原文的**絕對行號**錨定改為**符號錨定**（附可重跑的 grep 覆核指令），因該檔本輪正在被改動、行號必漂移 |
| DEF-101-464 | 2026-07-27 | R56 round 3 SA 提出、主控收尾處理（SA round 4 指出本項未獲專屬帳本列，依 R55 DEF-101-431 先例補立） | `ONBOARDING.md` §6.1 第 1 道仍寫「（根層 `tools/git-hooks/`＋`tools/*.sh`）」，與本輪把 `root-infra-ci.yml` 第 1 道 `bash -n` 擴為全庫 tracked active `*.sh`＋三處 git hooks 目錄（凍結版排除）的實況不符；同行第 12 道亦仍逐一列舉四個 action 名，而該白名單已於本輪被判定為 fail-open 並整個移除 | P3（雙平台唯一權威文件的範圍宣稱過期，與 R55 DEF-101-431 同族連續第二輪發生） | 本輪修復（主控） | fixed@R56：§6.1 第 1 道改為「全庫 tracked active `*.sh`＋三處無副檔名 git hooks 目錄；凍結版 `v0.01~v0.(N-1)` 依紀律不回改故排除、LATEST 版納入，政策與第 2 道一致」，並明載「檔數下限分兩段釘選，數字刻意只住 step 內註解——第二個計數站點即第二個會漂移的站點」；第 12 道改為「全部 `actions/*` 官方 action（含子路徑 action）跨全部 workflow 版本唯一」並說明「刻意不再列舉 action 名（原四名白名單本身即 fail-open），且 workflow 檔頭側再出現列舉會被 `tools/tests/test_root_infra_parity.py` 擋下」 |
| DEF-101-465 | 2026-07-27 | R56 round 4 三方交叉印證（Architect F1／SD P3-SD-2／QA F-1 互不知會、各自 bug-injection 命中同一根因）＋round 5.2 QA 再揪出第二層 | WindowsApps guard **Python 側**防增生鎖有兩層鑑別力缺口：**(1) 兩錨皆大小寫敏感**——`_STUB_NAME_RE`／`_STUB_PREDICATE_RE` 無 `re.I`，把字面值或函式名寫成 repo 慣用的大寫 `WindowsApps` 即整組逃逸（變體 L `any(x == "WindowsApps" for x in p.parts)`、變體 N `def isWindowsAppsStub`）。三方一致指出這**不受 DEF-101-333 比例原則判例覆蓋**：該鎖 docstring 自稱邊界只有「嵌進更大字串」（變體 K），裸大寫字面值落在其自稱涵蓋內；且大寫正是姊妹語言 SSOT `tools/lib/WindowsAppsGuard.ps1:55` `-notlike '*\WindowsApps\*'` 的既有寫法，仿照它在 Python 側新寫判斷式者最自然就會打出這個形狀。**(2) 候選集合被 `scoped_prefixes` 縮面**——16 支生產 `.py` 完全不被掃（`.claude/hooks/sdd_hook_router.py`、`AISDLC_SDD/conftest.py`、`AISDLC_SDD/scripts/` 下 14 支），而同檔兩支 `.ps1` 掃描與姊妹鎖 `test_windowsapps_guard_bash_parity.py` 的 `.sh` 掃描**都是無前綴 repo-wide**——三語言中唯獨 Python 側被縮面 | P3（防增生鎖鑑別力；Python 側破了本鎖即零訊號，無第二層兜底） | 本輪修復（兩階段） | fixed@R56：**(1)** 兩錨各補 `re.I`（`_STUB_NAME_RE` 寫為 `re.MULTILINE \| re.I`，MULTILINE 未被覆蓋）。修復方與 SD／Architect 三方**各自獨立**重建候選集合掃描實測：378 支候選加旗標前後命中集合完全相同（`pre_run_validator.py`＋`bootstrap_core.py`），新增偽陽性 0。已證偽的「大小寫敏感（刻意）」理由段整段刪除並留「勿再寫回」註記（實查 `pre_run_validator.py` L70/L98 的 `WindowsApps` 後接空白、引號界定本就不匹配，且該檔早在 `_APPROVED_SECOND_IMPLS`）。**(2)** 主控取消 `scoped_prefixes` 縮面，改與姊妹掃描同政策（只排凍結版與測試檔）：候選 378→394，實測命中集合不變、偽陽性 0。bug-injection 雙向證明：注入 canonical 實作至原本掃不到的 `AISDLC_SDD/scripts/bash_probe.py` → 紅；還原 → 綠、零殘留。另新增 `TestStubAnchorDiscriminatingPower` 7 支**常駐鎖**把注入結論固化（掃描鎖只在 repo 真出現第二實作時才翻紅，旗標被悄悄拿掉時零訊號——這正是本缺口存活到第四輪的原因）。既知邊界收斂為 K（嵌進更大字串）與 O（字串串接） |
| DEF-101-466 | 2026-07-27 | R56 round 4 三方交叉印證（Architect F2／SD P3-SD-3／SA-1）＋round 5.1 Architect 追加第四處＋round 5.1 SA 追加 macOS 側反向失衡 | 「四棵 `.ps1` 掃描樹 ＋ per-tree 檔數下限」硬編**現存四處**（`root-infra-ci.yml` 第 2 道、`test_ps51_compat.scan_trees()`、`windows_smoke_local.ps1` 的 `$ps1Trees`、`test_ps1_bom._scan_prefixes()`），本輪原僅鎖住其中一對。實測零訊號：抽掉 `windows_smoke_local.ps1` 一整棵掃描樹 → 根層 518 測試全綠。對照組 macOS 側同款注入立即紅——**修完 CI 層的平台不對稱後，把不對稱平移到了「機械守門密度」這一層**，而 `windows_smoke_local.ps1` 的 R56 註解正**自稱**「與 root-infra-ci 第 2 道同四棵樹」，`test_smoke_ci_sync.py:269` 更明文立下「凡兩份硬編實作互稱鏡射本 repo 一律建鎖」。round 5.1 SA 另指出反向失衡：`[E]` 的修法只做 Windows 側，macOS 對稱物 `test_bash32_compat.TestScanConfigPinning` 仍只釘 keys 不釘 floors | P3（本輪自身造成的新平台不對稱，與 DEF-101-451 同構、方向相反） | 本輪修復（兩輪疊代） | fixed@R56：新增 `test_smoke_ci_sync.py::test_ps1_parse_scan_surface_matches_root_infra_ci` 三向鎖（樹集合＋Floor 逐值＋兩處皆須實呼叫 `sdd_version.py` SSOT，且刻意同時要求 `--sdd-root` 旗標——只認裸檔名會被該 `.ps1` 的 Fail-Item 訊息字串滿足，是同檔 bash 版鎖踩過的陷阱）；round 5.1 補上第四處（`test_ps1_bom.py` 對 CI 自建互鎖）與 macOS 側對稱修法（`test_bash32_compat` 改為 keys＋floors 逐值等值）。`[E]`（`test_tree_keys_and_floors_pinned` **名為 pin floors 實作只比 keys**，`_floor` 從未進入斷言）一併修正。四向 bug-injection 全數轉紅（抽樹／改 Floor／CI 側改樹名／抽掉 SSOT 呼叫）。主控另訂正三處「硬編現存三處」措辭為四處——**兩份 round 5 產物互相矛盾**（發現方已訂正、被訂正的那一句未回填），與 round 4 `[I]`／DEF-101-431/456 同構的多站點敘述漂移 |
| DEF-101-467 | 2026-07-27 | R56 round 4 SD P3-SD-1 ＋ round 5.1 SD 量化誤植 ＋ round 5.2 SA 假陰性補列 | `tools/tests/test_ps51_compat.py` 的判準覆蓋面與其自我宣稱不符：**(a)** 漏掉**三元運算子 `? :`**——repo 自己的 `tools/windows_smoke_local.ps1:13` 明文列禁令 4 項（`&&`/`\|\|` 鏈接、三元、`??`、`?.`），新鎖只涵蓋 3 項，而這把鎖存在的理由正是「不再依賴人工宣稱」；**(b)** 檔頭「代價（如實揭露）」段量化數字誤植——「329 行」實為 block-comment（311）＋here-string（18）合計，卻掛在 here-string 規則名下，把該規則覆蓋面誇大約 17 倍，同句「全庫」實應為「四棵樹掃描面」；**(c)** 補上三元判準後，其 `(?<!\\\|)\s\?\s.*?\s:\s` 要求「`?` 後有空白**且**冒號兩側皆有空白」，但 PS7 語法只要求 `?` 前有空白，`$c ? 1:2`／`$c ?1 : 2`／`$c ? 1 :2`／`$c ? $a :$b`／`$c ? 1:$b` 皆為合法 `TernaryExpressionAst`（**R56 round 6 訂正**：原列的 `$c ? $a:$b` 經 Architect／SD／SA 三方各自獨立 AST 複驗＋主控親跑證偽——真值分支以變數結尾且緊接 `:` 時 `$a:` 被當 scope-qualified 變數，PS7 本身即 parse error〔errs=3〕，非假陰性；真正判準是**冒號左側是否為變數**而非兩側有無空白）（PS 5.1 必 parse error＝本鎖守備目標）卻不命中——此**假陰性**方向未列入同段剛擴寫的已知缺口（原文只列偽陽性方向） | P3/P4（Windows 11 內建引擎的語法守門覆蓋不足＋文件誠實度） | 本輪修復（三輪疊代） | fixed@R56：(a) `_PATTERNS` A 組加入三元判準，並補 negative case 回歸樣本（`\| ? { … }` Where-Object 別名、`@{ a = 1 }`），實測對 21 支 active `.ps1` 零偽陽性；(b) 拆為兩個各自標明口徑的數字並明寫原文誇大倍數；(c) 主控依 SA 建議只做文件揭露、**刻意不放寬 regex**（實測放寬冒號空白條件會讓 `… \| ? { $_ -ne $env:TEMP }` 這類「別名＋`$env:X`」同行寫法變偽陽性，收緊代價高於收益），於「代價」段補列假陰性方向的四個具體形態、附「`$true?1:2` 全無空白者經 AST 驗證非三元故不需涵蓋」的界定，並明載「檔頭 A 組所列 `? :` 僅涵蓋全空白形態，該禁令仍部分依賴人工複核」。另 QA B-2（`_HERE_STRING_RE` 行內 `@"`／`@'` 誤啟 here-string 會遮蔽其後真違規）依同一原則只做具名揭露、不改 regex（實害為零：掃描面內僅 2 處且都已落在既開啟區內） |
| DEF-101-468 | 2026-07-27 | R56 round 4 Architect F4 ＋ round 5.1 四方（Architect／SA／SD／QA）**全部四位**獨立指出同一殘留 | ADR-SD07-001 §6.3（R56 本輪新訂）觸發條件 ② 是「連續 2 輪 `total ≥ cap − 10`」，但 `check_loc_budget.py` 只在 `total > cap` 才有訊號，落在預警帶時輸出與正常態一字不差——ADR 自己的「緣起」段記載 R53(=cap)／R55(=cap−1) 兩次都是靠審查員逐字讀輸出才發現。round 5 補上 WARN 後，四方**全部四位**立即獨立指出第二層問題：該預警帶**自身零回歸鎖**，且 `TOTAL_WARN_MARGIN` 與 ADR §6.3 的「10」形成**第二個無機械鎖的硬編站點**——正是本輪 DEF-101-466 判 REJECT 的同一形狀（且修復方在散文中自承未做測試，`skippedItems` 卻填「（無）」） | P3/P4（新訂治理條文只靠人眼；補的訊號自身又成為新的無鎖硬編） | 本輪修復（兩輪疊代） | fixed@R56：`check_loc_budget.py` 新增 `TOTAL_WARN_MARGIN=10` 與非阻塞 `[WARN]`（rc 不變、不進 `has_violation`）；round 5.1 補上 `tests/contract/test_loc_budget_tiered.py` 三支鎖（共 7 個 case，位於 `tests/` 不計入 `autoclaude/` LOC 總量）——① `test_total_warn_margin_matches_adr_sd07_001_section_6_3` **自 ADR 正文抽數字比對**，改任一站點未同步即翻紅；② `test_warn_band_boundary_and_rc_invariant` 釘住四態邊界與「WARN 非阻塞、rc 不得改變」；③ `test_warn_band_json_payload_matches_text_mode` 釘住 JSON 兩欄。同時把 `check_loc_budget.py` 與 ADR 兩處「須人工同步」的散文措辭改為指向該鎖——審查員一致指出病灶不只在缺測試，也在措辭把同步義務寫成人工承諾 |
| DEF-101-469 | 2026-07-27 | R56 round 4 Architect F3 ＋ SA-2 ＋ SA-4（三項小型文件/追溯修正，合併記載） | **(a)** `_evaluator_derivation.py` 本輪把「裸 `python` → `sys.executable` 絕對路徑」擴到 pytest 分支（方向正確），但產出字串會寫進**落盤 YAML**（`evolved_*.yaml`／`*.mutated.yaml`），而 `dev_start.py::_ensure_venv_shape()` 的 `.venv-cache-posix`／`.venv-cache-windows` 設計正是為支援「共用工作目錄來回切換平台」——該情境下 macOS 產生的檔帶著 `/Users/…/.venv/bin/python` 切到 Windows 即失效；**(b)** `playbook_persistence_plugin.py:96` 與 `test_playbook_persistence.py:98` 兩處註解引用**錯誤缺陷編號**（寫 DEF-101-437＝escalation POSIX-only，實應為 DEF-101-442＝檔名淨化第三 sibling），依註解回查帳本者會落到錯的一列；**(c)** 根層 `.gitattributes:7-10` 寫死「根層 / AutoClaude / AISDLC_SDD **三檔**必須各自完備」，本輪新增 `AISDLC_SDD_v0.30/.gitattributes` 後全庫實為 4 份，且該新檔檔頭自己就寫「本檔 `*` 規則會遮蔽父層…須自行完備」 | P4（(a) 已知取捨未記載；(b)(c) 本輪自身引入的追溯/計數漂移） | 本輪修復 | fixed@R56：(a) 於檔頭記載「落盤產物不可攜」為已知取捨，並指出架構正解是在執行期（ShellEvaluator）解析直譯器而非生成期硬編進持久化檔——**刻意不改任何邏輯**（回報者自己也不主張本輪修，且無更好的單行替代：`python3` 在 Windows venv 不存在）；(b) 兩處改為 DEF-101-442；(c) 改為不寫死份數的措辭（「根層與**各**子專案／版本子樹的 `.gitattributes` 必須各自完備」），消滅這個會隨新增版本子樹而過期的計數站點 |
| DEF-101-472 | 2026-07-27 | R56 round 6 四方最終複核（Architect／SD／SA **三方各自獨立以 pwsh 7.6.3 `Parser::ParseInput` + `FindAll(TernaryExpressionAst)` 複驗**，主控再親跑一次確認） | `tools/tests/test_ps51_compat.py` 檔頭 round 5 補列的「三元假陰性形態」清單中，**`$c ? $a:$b` 一例經 AST 實測不成立**（errs=3／ternary=0：真值分支以變數結尾且緊接 `:` 時，`$a:` 被當 scope-qualified 變數，PS7 本身即 parse error），卻被寫成「合法 `TernaryExpressionAst` 卻不命中」，且該段自稱「以本機 pwsh 7 真 AST 實測補列」。SD 進一步指出這不只是筆誤而是**自我矛盾**：同段下一句才說「不放寬 regex，因為會讓 `$env:X` 這類寫法變偽陽性」，而 `$a:$b` 正是 `$var:NAME` 形狀——後續維護者若照前半句放寬，命中的正是後半句警告的偽陽性。同一句已複製進帳本 DEF-101-467 (c) 段 | P4（誠實揭露段落反向製造假缺口，會誘使下一輪去做本輪明確裁定不做的事；且掛「真 AST 實測」取證標籤卻與 AST 相反） | 本輪修復 | fixed@R56 round 6：主控親跑 pwsh 7.6.3 逐案複驗（`$c ? $a:$b` errs=3／`$c ? $a: $b` errs=3／`$c ? $a :$b` errs=0 ternary=1／`$c ? 1:$b` errs=0 ternary=1／`$c ? 'a':'b'` errs=0 ternary=1／`$c ? ($a):($b)` errs=0 ternary=1／`$true?1:2` errs=0 ternary=0）後訂正兩處：假陰性清單改列七個形態（**R56 round 7 二次訂正為六個**：Architect／QA／SD 三方再度交叉發現，其中 `$c ? 'a':'b'` 其實**會命中**——`split_code_comment()` 把字串字面值抹成空白後該行變成 `$x = $c ?  : `，反而製造出判準所需的 ` ? … : `，故「兩分支皆為引號字串字面值」是**已被涵蓋**、不是缺口。根因是 round 6 只驗了「是否為合法三元」（AST 面）卻沒驗「本鎖是否真的漏抓」（掃描器面），兩者是兩件事；主控首次複驗時又把 `scan_source(source, rel)` 參數傳反、掃到檔名字串而得出全 0 假結論，是同輪 venv 污染檢查（DEF-101-461）之後第二次「驗證手法本身無鑑別力」。已於檔頭改列六例、另把該形態加入 `test_scan_source_detects_each_pattern` 常駐樣本，避免日後字串改抹成空字串時這份意外覆蓋靜默消失，見 DEF-101-477）、另立「不需涵蓋非缺口」三種形態並附 PS7 錯誤訊息原文，並點出**真正的判準是「冒號左側是否為變數」而非兩側有無空白**（`$c ? 1:$b` 成立、`$c ? $a:1` 不成立）。帳本 DEF-101-467 (c) 同步訂正 |
| DEF-101-473 | 2026-07-27 | R56 round 6 QA 最終複核（其他三位皆未命中的新角度，附三段紅綠對照） | **雙錨的「聯集」本身完全無鎖**：`test_windowsapps_guard_cross_consistency.py` 的掃描判定原為內聯 `_STUB_NAME_RE.search(text) or _STUB_PREDICATE_RE.search(text)`，把該處 `or` 改成 `and`（3 個字元）→ 全檔 40 支測試維持 `OK`、變體 L（改名＋裸大寫字面值）完整逃逸；還原 `or` 即紅，證明逃逸的唯一成因就是該運算子。round 5 新增的 `TestStubAnchorDiscriminatingPower` 7 支鎖住的是兩個錨**各自**的 `re.I`／`MULTILINE`／引號界定，唯獨沒鎖住 round 5 真正的交付物「∪」——三個零件兩個有鎖、最關鍵的沒有。QA 另指出**同檔內部不一致**：姊妹 class 測的都是組合後的純函式（`_invokes_python_in_ps1` 等），唯獨 stub 側測裸 regex 常數 | P3（保護力缺口；觸發情境具體——同檔「精度取捨」段明載放寬字面值會多 5~6 支偽陽性，未來任一次偽陽性壓力下「改成兩錨都要命中」都是最自然的收緊動作，且不會讓任何測試翻紅） | 本輪修復 | fixed@R56 round 6：抽出純函式 `_matches_stub_anchor(text)`（掃描端改呼叫它，與同檔既有慣例對齊），並於 `TestStubAnchorDiscriminatingPower` 補 2 支**單錨樣本**斷言——`test_union_catches_name_only_reinvention`（只有名錨命中：`def isWindowsAppsStub` ＋ 字面值以 `("win" + "dowsapps")` 串接）與 `test_union_catches_predicate_only_reinvention`（只有判斷式錨命中：`def _store_alias_path` ＋ 裸大寫 `"WindowsApps"`），兩者在 `∩` 語意下必死。主控 bug-injection 複驗：改 `or`→`and` 後兩支新測試立即 FAILED（修復前為全綠 40 passed），還原後 41 passed／1 skipped、零殘留 |
| DEF-101-474 | 2026-07-27 | R56 round 6 SD／SA 交叉獨立發現（SA 另附算術反證） | `ONBOARDING.md` §7 的「本輪 +32」逐項拆解中，`test_ps51_compat.py` 記為 **6 支**，實測為 **7 支**（`pytest --collect-only` 7 tests；`git diff` 新增 `def test_` 亦為 7）。SA 算術反證：逐檔加總 ps51 7＋cross_consistency 7＋gha 10＋root_infra 4＋smoke 2＋bash_parity 1＋ps1_bom 1＝32，恰等於 496→528；若 ps51 為 6 則總和只有 31，與實測矛盾。成因為 round 5 為三元判準新增 `test_ternary_pattern_does_not_flag_where_object_alias` 後該分解句未回填 | P4（§7 為全 repo 基線唯一站點，`check_pytest_baseline_sites.py` 守的是數字站點位置、守不到散文分解） | 本輪修復（主控） | fixed@R56 round 6：`6 支` → `7 支`，並就地註明訂正來源與漏算的測試名 |
| DEF-101-475 | 2026-07-27 | R56 round 6 QA backlog B-1（實測 11 支全綠，主控採納修復） | 三向／四向鎖的 **CI 側抽取正則字元類過窄且無抽取數量下限**：`Get-ChildItem -Path ([A-Za-z0-9_/]+) -Recurse` 無法表達含 `.` 或 `-` 的路徑。實測往 `root-infra-ci.yml` 第 2 道插入第 5 棵樹 `Get-ChildItem -Path .github/scripts -Recurse` → `test_ps1_bom`＋`test_ps51_compat`＋`test_smoke_ci_sync` **11 支全 OK（擴面完全隱形）**；同一注入改為無點路徑 `AutoClaude/autoclaude` 則 3 支 FAILED。`.ps1` 側已有 `len(ps1_trees) == 4` 抽取數量守門，CI 側無對等物＝fail-open | P4（守門工具自身的靜默縮/擴面盲區，與本輪 DEF-101-444 同族） | 本輪修復 | fixed@R56 round 6（**round 7 訂正宣稱並補強**）：round 6 的修法為「字元類擴充納入 `.`／`-` ＋ 補抽取數量下限」，但 round 7 QA 實證**該下限是冗餘的**——把它中性化後注入仍紅，既有 set-equality 早已涵蓋同一組輸入，真正生效的只有字元類擴充；且 Architect／QA 交叉實測指出仍有兩種形態全盲：引號界定路徑 `-Path "docs/scripts"`、以及 `-Path (Join-Path ".github" "scripts")` 計算式路徑（該 step 第 4 棵樹正是這種寫法、新增時照抄最自然），三支鎖對這兩種注入原本全綠。round 7 已在三處補上**與字元類完全無關**的 `Get-ChildItem\s+-Path` 出現次數斷言（預期 4＝三棵固定樹＋LATEST 計算式樹），主控 bug-injection 複驗兩種變體皆由全綠轉為三支同時 FAILED、還原後 20 passed|
