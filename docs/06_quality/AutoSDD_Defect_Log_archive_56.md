# AutoSDD Defect Log — Archive 56

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **10 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-706`、`DEF-101-786`、`DEF-101-646`、`DEF-101-787`、`DEF-101-793`、`DEF-101-789`、`DEF-101-788`、`DEF-101-799`、`DEF-101-791`、`DEF-101-800`
>
> **本次操作備註**：（未填）
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-706 | 2026-08-02 | R68 收斂半套工作樹時撞上（`AutoClaude/tools/check_loc_budget.py` 實跑） | **`autoclaude/` 總量 LOC 逼近硬 cap，餘裕僅 2 行**：本輪跨平台修復令 total 由 ~20292 增至 20450 > cap 20438（＝`.loc_baseline` 17032 × 1.20），閘門 `violations=1`。已依 ADR-SD07-001 §6.3「先刪死碼／收斂重複實作，最後才調 baseline」壓縮至 20436（violations=0）——但那只是把數字壓回線內，**結構性壓力未解**：下一筆生產碼修復即再破線 | P2 | `AutoClaude/autoclaude/`（收斂面）＋ ADR-SD07-001 §6.3 正式程序 | fixed@R74｜🔴 R75 訂正首詞（原文逐字接於後）：**partial**（R69 落地收斂項，解鎖條件①未達標故不結案）。已識別的具體收斂標的：`execution/evaluator.py::kill_process_tree()` 與 `perception/pty_wrapper.py::close()` 是**同一套行程樹回收的兩份實作**，R69 已抽至 `autoclaude/utils/platform_caps.py` 合一（ADR-XPLAT-003），**該包 4 檔淨 −21 LOC**、`violations=0`；平台判斷點同步由 7 處/3 檔收斂為 2 處/1 檔，並以 `AutoClaude/tests/test_evaluator_kill_tree.py` 第 4 節四道 AST 鎖（含 shrink-only 棘輪＋函式身分鎖）防復發。🔴 **仍為 partial 的理由（誠實，R69 第三波訂正）**：解鎖條件① 要求餘裕 ≥ 100 行，而**餘裕一行都沒有增加**——本列原文逐字宣稱「實測 `total=20436 → 20415`、**餘裕 2 行 → 23 行**」，R69 QA 以 `check_loc_budget.py --json` 實查得 `cap − total` **仍是 2 行**，與 `ONBOARDING.md` §7 表① live 格互相打臉。根因＝**射程偷換**：−21 行是 ADR-XPLAT-003 §3 表列**那四個檔**的淨值（為真），但同輪其他包已把該額度全數消耗，`autoclaude/` **全樹**淨值為 0（見 `DEF-101-712`）。🔴 **本列自即日起不再登載餘裕／total 的具體數字**（寫死當場可現查的量測 token＝製造無看守的 stale 站點，`DEF-101-713` 家族）——**唯一真相源＝`cd AutoClaude && python tools/check_loc_budget.py --json` 現查 ＋ `ONBOARDING.md` §7 表① live 格**。結構性壓力與生產碼凍結**皆完全未解除**。承接輪次：**未指派**。解鎖條件（可直接執行）＝① `cd AutoClaude && python tools/check_loc_budget.py` 印出的 `cap − total` ≥ 100 行；② 或走完 §6.3 正式程序（Architect + SD 雙簽）重新校準 baseline。**禁止**為了讓閘門轉綠而直接上調 `.loc_baseline`｜🔴 **R75 複驗（類別 A）**：🔴 解鎖條件① 逐字為「`cap − total` ≥ 100 行」。現查 `check_loc_budget.py --json`：`total=20296`、`cap=20438` ⇒ **餘裕 142 行 ≥ 100**，併 `total_violation=false`、`total_warn_band=false`、`tier_violations=[]`、`absolute_violations=[]`；`baseline` 仍 17032 ⇒ 是收斂生產碼達標、非上調 baseline，符合本欄禁令。⚠️ 該次 rc=1 來自**兩支根層護欄檔**的 special 棘輪，與 `autoclaude/` 總量無關，已另行交棒。 |
| DEF-101-786 | 2026-08-04 | R73 四方二審（SA blocking ②-2，以 `git log -S` 定位寫入時點） | 🔴 **護欄層生產碼裡的三句假事實，而且寫下當天就為假**。`tools/lib/baseline_origin.py` 的 `SMOKE_EVIDENCE['win32']` 會被 `--check-snapshot` 逐字印給讀者，內容斷言 Windows smoke 「沒有任何落地產物／全程只 `Write-Host`／探針再怎麼寫都讀不到」——三句皆假：`tools/windows_smoke_local.ps1:111,127` 的 `Start-Transcript` 落點 ＋ `:125` 的 14 天日期輪替，是 **R71 同一個 commit（1e5214b）** 加進去的；磁碟實查有 `windows_smoke_latest.log` ＋多支日期檔，且內含可解析的 `===== 彙總：PASS=n FAIL=n =====` 行。⇒ 該段的「解鎖條件（下一輪可執行）」在寫下的那一刻就已達成，卻跨 R72、R73 兩輪無人回看，Windows 第二條每日真機證據至今仍在平台覆蓋判定之外。**這是 `DEF-101-757` 的又一次復發，且復發在專門用來防這類事的模組裡**（同一段自己正在批`DEF-101-756` 的那個形態）。另同檔兩處寫死排程時刻 | P1 | `tools/lib/baseline_origin.py`（`SMOKE_EVIDENCE`／D-4 評估段／兩處時刻） | fixed@R74｜🔴 R75 訂正首詞（原文逐字接於後）：**partial@R73** — 三句假事實與兩處寫死時刻**已訂正**（改為陳述 log 落點確實存在、並明確標示「目前是人工可查的指路，不是量測值，也不得讀成已納入覆蓋判定」）；「下一輪可執行」這種**無主詞交棒**改為具名承接（SA 指出正是那種寫法讓它蒸發兩輪）。**仍未做（承接輪次：R74）**：把 smoke 升為第三個 `NIGHTLY_HEARTBEATS` 條目——需新增其逐平台解析契約並連動平台覆蓋判定，會改動 `--check-snapshot` 輸出面（本輪同時在回填 ONBOARDING §7 快照），屬需獨立驗證的設計變更，故**不在**本輪順手改｜🔴 **R75 複驗（類別 A）**：殘留逐字為「把 smoke 升為第三個 `NIGHTLY_HEARTBEATS` 條目」。現查 `tools/lib/baseline_origin.py:111-121`：R74 **經論證刻意不採那個形狀**（那張表的鍵是平台鍵、三道既有鎖依賴此契約，塞 `win32-smoke` 偽平台會打壞「鍵＝平台」語意），改以獨立 `SMOKE_HEARTBEATS`（:119）＋`SMOKE_EVIDENCE`（:123）＋`daily_evidence()` 併攏達成同一目的。訴求本體已達：`--check-snapshot` 現查輸出逐字含「R74 起本工具**已**把它接成探針，故本行前半段是量測值、這條每日真機證據已在平台覆蓋判定視野內」。 |
| DEF-101-646 | 2026-08-01 | R67 Scan-E（DOCADR 包；`--print-collapse` 現查 ＋ §8 item 11 逐字回讀） | **ADR-XPLAT-002 §4.5（`:551-585`）的 SDS 到期機制自己過期**：「R61 開輪時應重新盤點，不得沿用本數字」是綁死已過去輪次的一次性義務，R61 未執行、此後五輪零盤點，於是「不得沿用」這句話被沿用六輪。🔴 訂正掃描員兩處宣稱：其提報的「漏記第 2 站點」經現查為 `tier3_os_primitive`＋`tier4_forbidden` 雙重出界、依 §4.5 計分規則 3 不列入盤點面，故 SDS=1 未被證偽；提報的「零機械承接者」亦經 §8 item 11 證偽 | P3（原 P1，四方複審降級；is_real_bug=false） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §4.5 | closed-by-decision｜🔴 R75 訂正首詞（原文逐字接於後）：partial@R67：殘留真核（到期機制綁死已過去輪次）已改為每輪可重跑、答案來自登記表的形態（`--print-collapse` 現查＋指名 §8 item 11 為活體容器）；finding 其餘兩項如其所述**不成立**，已把反證逐條寫進 ADR 以免下輪重議。標 partial 而非 fixed，是因為 finding 敘述與落地範圍不等價｜🔴 **R75 複驗（類別 B）**：殘留真核（到期機制綁死已過去輪次）R67 已改為「每輪可重跑、答案來自登記表」，另兩項 finding 原判不成立且反證已入 ADR ⇒ 名下零待辦；`partial` 之所以留，本欄自述是「finding 敘述與落地範圍不等價」的**記帳顧慮**，不是未完成的修復。`python tools/check_script_parity.py --print-collapse` → **rc=0**，印出 `UEP=5／AC=48／THINNESS_ENROLLED=7／PINNED_SHA256=14／EXEMPT_PAIRS=5／SINGLE_SIDED_EXEMPT=18`（登記表現算，非寫死快照）。 |
| DEF-101-787 | 2026-08-04 | R74 開場實查 R73 收輪後的雲端結果（R73 收官時四支仍在跑、未等到結論） | 🔴 **雲端 `windows-compat-ci` 在 R73 收輪 commit `82eee92` 是 failure**（run 30838711666；同 commit 另三支 success）。唯一失敗案＝R73 新增的 Bash 阻斷鎖。根因：CPython `sys.stderr` 預設 `errors='backslashreplace'`，`.claude/hooks/block_bash_on_windows.py:93` 直接 `sys.stderr.write(_GUIDANCE)`，locale 編碼表達不了 CJK 時整段指引降解為 `\uXXXX` 字面。主控雙向實測：`PYTHONIOENCODING=cp1252` → 逃脫字面（與 CI log 逐字相同）；本機預設 CP950 → 正常中文 ⇒ **本機結構上不可能紅**。🔴 **這同時是功能缺陷不只是測試紅**：非 CJK 語系 Windows（en-US／GitHub windows-latest）使用者被 exit 2 硬阻斷卻讀不到替代指引，而這支 hook 存在的唯一理由就是「純文件約束無攔阻力」。姊妹檔 `.claude/hooks/sdd_hook_router.py:56-60` 早有 UTF-8 reconfigure，本檔沒有 | P0 | 本輪修復（hook 端強制 UTF-8；測試端剝除繼承環境） | **fixed@R74** — 主控獨立複驗（非採信 agent 宣稱）：cp1252＋剝除 `PYTHONUTF8` 下 rc=2、中文可讀、無 `\uXXXX`；`{"tool_name":"Read"}` 仍 rc=0（射程未擴大）。殘留＝雲端該支轉綠尚待本輪 push 後驗證（見 DEF-101-796） |
| DEF-101-793 | 2026-08-04 | R74 Scan-M（掌舵者直接提問：兩支排程 job 是階段性驗證，能否快結束） | 🔴 **SD_09 觀察期實質不可達，成因是拍板結果沒進 code**：PM 已於 2026-08-03 拍板新判準（`ADR-SD09-012`），但 `AutoClaude/tools/ac4_progress_check.py` 一行未改，於是進度停在舊口徑 `observation_days=9/14`——而該口徑是 rolling-window-days，每晚重測同一份 sha 為零增益，**空轉多久都到不了**。同源第二筆：nightly 每晚照跑完整 mutmut（實測約 2 分 54 秒），但 mutation 軌早已鎖定且按 `source_sha256` 去重 ⇒ 對退出判準零增益；`ADR-SD09-011` §2.2 說的「nightly 轉監控角色」只改了文件沒改觸發 | P1 | 本輪修復（把 PM 拍板判準落進 code ＋ mutation 軌零增益重測降頻 ＋ 達標憑證） | **fixed@R74** — 主控親跑覆核：`status=ready`、新口徑 `green_streak=43/14`、`ready_for_labeled_pr=True`、`staleness_days=0/30`、rc=0。⇒ **觀察期其實早已達標**；是否據此停用／降頻排程屬掌舵者拍板，機械側已備妥憑證 |
| DEF-101-789 | 2026-08-04 | R74 PKG-1（問「既有編碼鎖為何沒攔到 DEF-101-787」） | **編碼機械鎖只守一半**：`tools/tests/test_subprocess_encoding_hygiene.py:153` 判準逐字 `if flag is None or "encoding" in kwargs: continue` ⇒ 帶 `encoding` 即放行。出事那一行 `subprocess.run(cmd, …, text=True, encoding="utf-8", errors="replace")` **百分之百合規**。也就是說 repo 用一整套 AST 掃描守住「parent 解碼」方向，「child 是否保證吐 UTF-8」連判準都沒有。失效場景：任何人再寫一支印中文的 hook／CLI 並以 `sys.executable` 起它做逐字斷言 ⇒ 全庫掃描皆綠，唯一發現通道是雲端 Windows runner | P1 | 本輪修復（加判準二「child 編碼」，含一跳 argv 解析 ＋ 三種保護名偵測 ＋ 具名豁免與 stale 自檢） | **fixed@R74** — 納管 26 站點（floor=20）。實戰鑑別力：本輪另一包新建的 `tools/check_scheduled_task_drift.py` 當場被判紅（8 處非 ASCII 無保護），補上後轉綠 |
| DEF-101-788 | 2026-08-04 | R74 PKG-1（順 DEF-101-787 根因往上游查載具） | **鎖的綠燈由環境注入供應，不是被測物的性質**：`tools/tests/test_check_hooks_liveness.py` 的 `_run_hook` 未傳 `env=`，子行程 `PYTHONUTF8` 全靠繼承；本機唯一來源是 `.claude/settings.json` 的 `env.PYTHONUTF8=1`（User／Machine scope 實測皆空）。⇒ 從 agent session 內跑 pre-push／local gate 一律綠，雲端 runner 沒有該變數就紅。🔴 **同一份知識早已在樹裡且有鎖**：`tools/tests/test_find_git_bash_parity.py:1176` 逐字要求剝除繼承、`tools/tests/test_git_hooks_install_common.py:188-191` 有 `_env_without_utf8_overrides()` ——知識在、鎖只圈舊站點，新站點照樣踩進去 | P1 | 本輪修復（`_child_env()` 顯式剝除 ＋ 新增「剝環境下指引仍可讀」顯式 case） | **fixed@R74** — 鑑別力已證：剝環境後、hook 未修時當場 3 紅 rc=1；hook 修好後全檔 OK rc=0 |
| DEF-101-799 | 2026-08-04 | R74 Scan-A ＋ PKG-6（自由掃描命中兩筆載具／閘門缺陷） | ① **bootstrap 完成指引的 Windows 分支教使用者跑裸 `bash scripts/ci-gate.sh`** → 實際落到 WSL Linux，與 ONBOARDING §6 的 Windows 入口相矛盾（`tools/bootstrap_core.py:315`）。與 R73 才治過的 `DEF-101-778` 同一個病（執行規則的載具教壞掉的作法），只是站點不同——又一筆射程問題。② **`AISDLC_SDD/scripts/ci-gate.sh:85` 的 LATEST 解析帶容錯會吞掉 rc=126**，閘門靜默降為只測凍結基線且回 rc=0；同檔註解宣稱的守門並不覆蓋這條路徑。⇒ 閘門靜默降級是最危險的一類，因為它回報綠 | P1 | 本輪修復（指引改走 `Find-GitBash` SSOT 並加防復發鎖；ci-gate 退出碼分級 fail-loud） | **fixed@R74**（未經主控獨立複驗，依 PKG-6 回報之鑑別力實測；R75 宜抽驗） |
| DEF-101-791 | 2026-08-04 | R74 Scan-T ＋ PKG-2（開場實查歸檔壓力時發現搬不動） | **缺陷帳本死結**：主檔 248,048 bytes 已越 warn 線（245,760），距 fail 線僅 14,096，而 `archive_defect_log.py --plan` 實算**可搬 0 筆**。根因不是「沒有已結列」，是**歸檔判準把已結列誤判成活躍**——其中一筆逐字可指：判準② 的正則把 `fail-open` 這個**設計術語**當成活躍狀態字（連字號通過 ASCII 邊界 lookbehind）。⇒ 形成「逼近 bytes 上限唯一出路是歸檔、歸檔又被狀態判準卡住」的閉環 | P1 | 本輪修復（修判準假陽性 ＋ 逐筆查證改寫 ＋ 執行 archive_55） | **fixed@R74** — 主控現查實測：主檔 248,048 → **233,559 bytes**（降 14,489，回到 warn 線下），表格列 113 → **102**，新增 `AutoSDD_Defect_Log_archive_55.md` |
| DEF-101-800 | 2026-08-04 | R74 PKG-6（`.sh` 行尾量測，掌舵者第 3 點最直接的落差來源） | **量測推翻了原始前提，故刻意不做批次改檔**。診斷階段的「144 支 tracked `.sh` 是 CRLF」未區分 index 與工作樹；以 `git ls-files --eol` 分別量測後，`.gitattributes` 的正規化規則覆蓋完整（8 個代表路徑含凍結版、LATEST、`docs/`、無副檔名新檔皆 NORMALIZED），無 `.sh` 路徑逸出規則 ⇒ clone 到 mac 會得到 LF。若照原前提批次改檔，是**製造 diff 而非修缺陷** | P3 | 本輪處置＝不改檔，改為補事中閘門（pre-commit 行尾閘攔「CR 進 commit」這個結果，不論成因） | **fixed@R74** — 主控親驗閘門有牙（見 DEF-101-796）。本列刻意保留，供未來輪引用「先量測再動手」這個判準 |
