# AutoSDD Defect Log — Archive 57

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **9 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-765`、`DEF-101-770`、`DEF-101-721`、`DEF-101-777`、`DEF-101-699`、`DEF-101-556`、`DEF-101-674`、`DEF-101-652`、`DEF-101-710`
>
> **本次操作備註**：R75 容量包：逐筆查證後具名承認 9 筆判準④ 誤報／已完成交棒的已結列（556 交棒語出現在缺陷描述、674 出現在被引述的反例字串、721/765/770/777 分別為「下一輪辨認版本」「孤兒 backlog 機制名」「_backlog 檔名片段」「逼下一輪去修」等非交棒語；652 的殘餘已拆為 DEF-101-810 獨立未結列承接；699 的殘餘登記在 ADR-XPLAT-002 §8 item 14 且受 test_adr_xplat001_c1c2_lock.py 掃描；710 的待辦已由 R74 完成）。🔴 本標頭的「歸檔來源筆數」與「搬遷清單」兩個資料欄由本輪就地訂正為 9 筆：工具判為可搬的 12 筆中，DEF-101-804／805／806 為本輪（R75）新列，主控明令不准搬而 --apply 無排除入口（見 DEF-101-811），故三列已逐字還原回主檔、本檔不留其副本；判準散文一字未改
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-765 | 2026-08-03 | R71 本包為新列決定輪號時，發現同一批未提交改動裡三種輪號並存（帳本的「當前輪」正是由這一欄機械推得） | **同一批 48 檔未提交改動裡 `R70`／`R71`／`R72` 三種輪號並存，而 `tools/check_defect_log_crossref.py::current_round()` 取帳本「發現情境」欄的最大 `R\d+` 當硬規則② 的比較基準**。實況逐項：(a) 帳本已結列最大為 R70 且**已 commit**（`fbc9bb5`，`DEF-101-756`／`757`／`758`）；(b) `DEF-101-752` 狀態欄以承接輪次欄位樣式把殘餘站點指給 R71（此處刻意拆開引述、不逐字重現該欄位樣式，否則本列會被承接稽核當成**本列自己也指派了 R71**——本包第一版就踩到，`check_defect_log_crossref.py` 當場對本列印出 fail-open 窗口警告）；(c) 本批新程式碼**多數自稱 R71**（`tools/lib/ci_liveness.py:101`／`:198`／`:263`／`:287`、`tools/tests/test_dev_start.py:4859`／`:5304`／`:5346`、`tools/tests/test_bootstrap_ps1.py:18`／`:49`、`tools/tests/test_ps51_compat.py:104`／`:546`）；(d) 但當時的 `tools/tests/test_native_stdout_utf8_decoding.py:2`（🔴 該檔已於 R72 併入 `tools/tests/test_dev_start.py` 並刪除，見 `DEF-101-762` 修復位置欄；此處逐字保留當時的原始語料，是本列的證據，不是可追的現存路徑）與 `tools/tests/test_git_hooks_install_common.py:236` 自稱 **R72**；(e) 主控任務書逐字稱本輪為 **R70**。**本包對後果做過實測**（以構造輸入呼叫生產物件、未動主檔）：讓當前輪變成 72 時，`DEF-101-752` 立即被判孤兒 backlog（逐字「狀態仍未結案，卻把承接者指向 R71，早於當前輪 R72」）⇒ `check_defect_log_crossref.py` rc=1；填 71 則零命中。**為何既有防線沒抓到**：`current_round()` **只讀帳本**，程式碼註解裡的輪號對它完全不可見，兩邊可以無限漂移；而 `ADR-XPLAT-002 §6` 的逐輪覆蓋表是人工補列——`DEF-101-757` 已逐字載明「缺列不會有任何東西轉紅」 | P3 | 輪號 SSOT 之界定（既有機械源＝`tools/check_defect_log_crossref.py::current_round()`）＋ 本批註解內的逐字輪號 | fixed@R71（**主控 2026-08-03 裁定（PM 授權下）：本批＝R71**；下列原始證據逐字保全）。**本包處置＝依證據擇一並公開理由**：本輪 7 列的「發現情境」欄一律填 **R71**，理由是(a) 帳本 R70 列已 commit ⇒ 未提交批次是**下一輪**、(b) `DEF-101-752` 逐字指定 R71 為承接者、(c) 本批程式碼多數自稱 R71、(d) 填 R71 時兩道閘門皆 rc=0，填 R72 會讓 `DEF-101-752` 假紅（上述實測）。**未驗證**：無法判定 (d) 的兩處 R72 是刻意的子輪次編號還是誤植——本包不動程式碼故不逕行訂正。**解鎖條件**：(a) 主控裁定本批究竟是 R71 還是 R70（若裁定 R70，本輪 7 列的「發現情境」欄須一併改回，否則 `current_round()` 會多跳一輪）；(b) 兩處 R72 依裁定訂正；(c) 評估是否讓「程式碼註解輪號 ↔ 帳本當前輪」進入某道機械檢查，或在同一處明文寫下「為何不檢查」與承接輪次——依 `DEF-101-757` 入規：**已知的鎖射程缺口不得只以劃界結案**。🔴 **裁定與落地（2026-08-03）**：(a)(b) **已解**——本批定為 **R71**；逐處判讀後共訂正 **131 個 token**：超前值 12（程式碼 9 ＋ 本表 `DEF-101-762` 列 3），落後值 119（含 3 支 `TestR70*` 類別名與其在本表的 2 處引用）。(c) **已補鎖，非劃界**——`tools/tests/test_check_defect_log_crossref.py::TestR71CodeRoundLabelsNeverExceedLedgerCurrentRound`（10 支）：判準＝程式碼散文的 `R<n>` > `current_round()` 即紅，對引用往輪天然免疫；`.py` 只掃註解＋docstring、`.ps1/.sh` 整行、同行 `round-label-ok` 豁免 7 筆（射程與否決過的三個判準逐字寫在該類別上方）。**雙向實測**：`ci_liveness.py:101` 注入 `R72` ⇒ 紅並指名該行；同處改注入「R70 那輪做了 X」⇒ 綠；還原後 10/10 綠。**誠實劃界**：只抓超前值，落後值機械上分不出「自稱」與「引用」，仍靠人工判讀 |
| DEF-101-770 | 2026-08-03 | R72 文件治理盤點（起點＝`docs/04_planning/Archive/README.md` 自陳「歸檔不是可以順手做掉的雜務、需人工拍板」，該狀態已跨多輪未解） | **兩條治理規則正面相撞，使歸檔停擺並累積出一批「看起來精確、實則過期」的站點**：(i) 慣例要求已結案迭代四件套搬進同層 `Archive/`；(ii) `DEF-101-633` 要求歷史歸檔帳本逐字保全、不得改寫其散文。**本輪實查斷鏈規模**（以 git index 為真相源全樹掃描）：指向被搬檔案的路徑引用共 **298 處、散在 176 份檔**（🔴 **初稿誤植「78 處／54 檔／15／19」，同輪複查四個數字全部對不上，此為訂正值**；見 `DEF-101-772`），其中 **16 處**住在逐字保全的 `docs/06_quality/AutoSDD_Defect_Log_archive_*.md`、**176 處／92 檔**住在 `AISDLC_SDD/AISDLC_SDD_v0.XX/` 凍結版、**8 處**住在 AutoClaude 子專案（`three_tier_to_playbook.py:3`／`run_bridge_e2e.py:3`）——這兩類分別受 `DEF-101-633` 與 Copy-on-Evolve 禁止就地改寫，**故「搬檔同時同步更新引用」在規則上不可能做完**，衝突不是沒人做而是真無解出口。另查出兩個已實體化的過期站點：(a) `tools/tests/test_ntfs_trailing_space_device_name.py:577` 的 R67-A15 三站點清單把**檔案位置**寫死在上層，搬檔即 `FileNotFoundError`；(b) `docs/06_quality/Archive/README.md` 逐字宣稱「封存 `_01.md ～ _12.md`、最新 active＝`_13.md`」，與實況差近 90 號（與 04_planning 側 R71 已訂正的同型假資訊，只是沒人回頭看這一份） | P2 | `docs/04_planning/Archive/README.md`／`docs/06_quality/Archive/README.md`／`tools/tests/test_ntfs_trailing_space_device_name.py`／`tools/tests/test_adr_xplat001_c1c2_lock.py` | fixed@R72：**裁決＝不改引用、不留 stub、不列舉映射表，改以一條可推導的轉址規則**（`docs/<層>/<四件套檔名>` 上層不中即改到 `docs/<同層>/Archive/<同檔名>` 找），適用形態只有 `AutoSDD_improving_<N>.md`（含 `_backlog`）與 `AutoSDD_ZeroTrust_Audit_<N>.md`，**刻意不外溢到任意 `docs/**.md`**（否則真死連結會被洗成看似可解析）。理由：映射表要有人維護、每搬一次補一列、漏補即 stale＝製造第二個會過期的站點；規則零維護且可機械驗證。**落地**：`git mv` 搬 103 支（計畫 53 支、審計 50 支），上層依慣例只留最新一輪計畫。**為何最新一輪非留不可（機械依賴，非美觀）**：根 `CLAUDE.md` 取號規則＝「`docs/04_planning/` 現存最大號＋1」，且 `tools/tests/test_check_defect_log_crossref.py::test_real_ledger_current_round_is_two_digit_and_not_the_planning_dir_max` 以非遞迴 glob 取上層清單並 `assertTrue(...)`，上層清空即紅。**新增三道鎖**（併入既有宿主檔，未新增 .py——`tools/tests/` 檔數為 shrink-only 棘輪，實測現值恰等於凍結值 53）：`TestArchivedIterationDocRefsResolve` 的 (1) 四件套引用經轉址後必須解析得到（**零白名單上線**，實測全 repo 解析不到者 0）、(2) 至少一處引用非靠轉址不可解析（反向鎖：歷史引用若被就地改寫成 Archive 路徑即紅）、(3) 已歸檔輪號必須全部小於上層輪號＝歸檔只准由最舊往下搬（把「歸檔做完了沒」變成 index 上的可驗事實，這是積壓多輪的結構性對策）。**bug-injection 雙向實測**見 `AutoSDD_ZeroTrust_Audit` 缺席說明與本輪回報。**順修**：(a) 三站點清單改經 `resolve_doc_ref()` 解析（清單指名文件、不指名它今天住哪）；(b) `test_adr_xplat001_c1c2_lock.py::read_wide()` 的 `glob`→`rglob`，否則 SC-9 掃描面會隨歸檔靜默縮小＝`DEF-101-757` 同型（實測納入 Archive 後 SC-9 problems 仍為 0）。⚠️ **誠實劃界**：轉址規則只保證「檔案找得到」，**不**保證引用的章節錨點仍有效；`docs/06_quality/` 上層現無 active 審計檔（最新幾輪未產出四件套之審計），這是照實反映、不是漏搬 |
| DEF-101-721 | 2026-08-02 | R69 第三波帳本包實跑 `AISDLC_SDD/scripts/tests/` 時（動工前該目錄 303 passed 全綠） | **新增的根層消費檔未同步進兩支 compat-CI 的 `paths`**：`test_ci_paths_cover_root_consumers.py` 的 `[macos-compat-ci.yml]`／`[windows-compat-ci.yml]` 一度 **2 failed**，訊息＝「根層消費檔未列入 … paths：`['AutoClaude/.loc_baseline']`」。根因＝`tools/tests/test_doc_loc_baseline_freshness_r60.py` 為取 ADR 的 `baseline=` token 而新增 `_ADR_LOC_BASELINE` 直讀 `AutoClaude/.loc_baseline`，未同步 `paths`。**與 `DEF-101-042` 同構**：只改該檔時回歸鎖不會跑，缺口要別人跑全套才顯形 | P2 | `tools/tests/test_doc_loc_baseline_freshness_r60.py`（消費面）；備選解＝兩支 compat-CI 的 4 個 `paths` 區塊各補該檔 | fixed@R69（第三波，**採「消去消費面」而非「補 paths」——後者要同步 4 個區塊、消費面每多一個就再欠一次**）：改由 `measure_adr_loc_live()` 走既有的 `check_loc_budget.py --json`（該輸出本來就含 `baseline`），`_ADR_LOC_BASELINE` 已移除、理由逐字寫進該函式 docstring。憑證＝`grep -c _ADR_LOC_BASELINE tools/tests/test_doc_loc_baseline_freshness_r60.py` = **0**；`python -m pytest AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py -q` → **34 passed**（本包紅→綠實測，中途曾為 2 failed）。🔴 **本列的第一版狀態寫 `open` 且已寫進帳本，收尾複跑時才發現他包已修**——如實留下這段訂正，正是本輪 `DEF-101-733` 在治的「宣稱與實況脫節」同型。🔴 **R69 第四波訂正（終審點名）**：上段自述的修法（「消去 `AutoClaude/.loc_baseline` 的消費面」）**與實況互斥**——實測 `printf 17033 > AutoClaude/.loc_baseline` 即讓 `tools/tests/test_doc_loc_baseline_freshness_r60.py::TestR69AdrMeasurementTokensAreLive::test_real_adrs_carry_no_unreproducible_measurement_token` 由 OK 轉 FAILED（經 `check_loc_budget.py --json` 子行程**間接**消費）⇒ 消費面是活的，只是換了一條路徑。**正確處置（現況即此）**＝保留兩支 compat-CI 的 4 條 `paths`，並把該檔登記進盲區 B `_KNOWN_SUBPROCESS_ONLY_CONSUMERS`（`AISDLC_SDD/scripts/tests/test_ci_paths_cover_root_consumers.py`），刪任一條即紅。狀態維持 `fixed@R69`——**修法對了、自述錯了**，逐字保留原自述以便下一輪辨認版本 |
| DEF-101-777 | 2026-08-04 | R73 舵手驗證 R72 交棒的「PS 7 未安裝，補驗連兩輪做不到」那筆時，順線查全庫同型句 | 🔴 **鎖射程只圈一個站點，四輪後缺口自己發火（`DEF-101-757` 同型復發）**。R69 訂正了 `tools/tests/_ps_engine.py` 把「這台機器沒有 pwsh 7」寫成常數的毛病，並上鎖 `test_ps_engine_ssot.py::TestNoStaleLocalEngineClaims`——但鎖只掃**那一個檔**。2026-08-04 本機裝上 PowerShell 7.6.4（`shutil.which("pwsh")` 實測命中 `Program Files/WindowsApps/Microsoft.PowerShell_7.6.4.0_x64__8wekyb3d8bbwe/pwsh.EXE`，size=301368、**非 0 byte 佔位版**）後，射程外的同型句子同時變成假事實。擴射程後實測 **12 處命中／掃描 79 檔**，其中 **5 筆為真違規**：① `tools/lib/windows_skip_tags.py`（🔴 **護欄層**，拿現在為假的前提描述豁免依據）；② `tools/tests/test_install_windows_nightly.py`（宣稱「走不到兩引擎皆有的分支」，而該分支現在**每次都走得到**——本輪實測 `production_engine()` 正確回 5.1、23 passed，此即 R72 交棒那筆補驗的結案證據）；③④⑤ 鎖自己的檔案 3 處（**鎖犯了鎖在抓的病，而鎖看不到自己**）| P1 | `tools/tests/test_ps_engine_ssot.py` 擴射程 ＋ 5 個站點改寫 | **fixed@R73** — 射程由單檔擴至**整個 `tools/` 樹**（刻意含 `tools/lib/` 生產碼：只掃測試就會漏掉本輪最嚴重的護欄層那筆）。帶兩類結構性豁免、各有獨立鎖：(a) **skip 理由**走 `ast` 判定（`skipUnless`／`skipIf`／`skipTest` 的理由字串是對活檢查結果的描述，不是常數；不豁免會當場製造 2 筆假紅，而假紅會逼下一輪去「修」正確的碼）；(b) **偵測器自身樣本**用行尾 `# stale-sample:` 標記（沿用 repo 既有 `# ps7-ok:` 慣例），並以 `test_stale_sample_marker_stays_bounded` 鎖住標記居所，防它被拿去豁免真違規。5 筆真違規全部改寫為指向現查（`available_engines()`）。**鎖有牙的證據是真實存量而非合成注入**：擴射程後由 RED（12 筆真 offender）→ 修完 GREEN（45 passed）。🔴 **過程中鎖當場抓到舵手自己**：我寫的訂正註記逐字引述了那句假話（與 R69 當年留下 `R69 訂正：原註記寫死「…」`一模一樣的形態），被鎖攔下兩次後改為不逐字引述——樹裡不留假句子才是目的 |
| DEF-101-699 | 2026-08-01 | R67 round 3（SC 鎖包落地 → 本收尾包散文閉環）；標的＝`ADR-XPLAT-002` §9.1 與 `docs/06_quality/CrossPlatform_Scan_Dimensions.md`〈常設自檢〉 | **§9.1 各條不變式（`# SC-*`）自寫成規格起即是死信**：SA-R67-03 實測全 repo 零可執行消費者，注入違規形態後根層測試與七支根層工具全綠 ⇒ 依 Scan-H 判準⑤「可重跑但沒有任何閘門看它的 rc ＝ 不可重跑」，它們是「規格 ＋ 已驗證的實作」而非活體守門；本輪三項頭號架構異動（Phase 3 解封／平台前提中立化／§8 交棒表機制化）的防回流能力等於「靠下一個讀 ADR 的人記得手打這幾條 grep」 | P2 | 根層 `tools/tests/test_adr_xplat001_c1c2_lock.py`（§9.1 末段具名指派的承接容器；擴充既有檔而非新增，守 `DEF-101-561`③ 檔數棘輪） | fixed@R67R3：(a) 各條落成 `TestSection91InvariantsAreLive`／`TestSection91InvariantsHaveTeeth`／`TestSection91SpecIsBoundToTheseLocks`，該檔在 `tools/run_root_unittests.py` 收集面內 ⇒ 被 pre-push root-infra leg 消費，注入任一條的違規形態即該 runner 由 rc=0 轉 **rc=1**（端到端，非僅單元層轉紅）；注入矩陣為**完美對角**（注入 SC-N 恰只 SC-N 轉紅＝零串音，紅燈指得出是哪一種違規），另封三條 fail-open：區段抽不到／帳本家族枚舉為空／維度表抽不到定義列一律回報違規而非靜默零命中。(b) **落地當下即令散文成假話，本包同輪訂正四處**：ADR 狀態欄、§8 表頭 (1c)、§8 item 14、§9.1 末段，外加〈常設自檢〉SC-7 末段——一律**逐字保全原句再改寫**（同 §8.3 體例），並明寫「接線解決的是『沒人看 rc』，不是『射程夠不夠寬』」，三段式邊界一字未改。(c) §8 item 14 承接者由「未指派」改為 **R67 已交付 (a)(b)(e)(f)**（SC-2／SC-3／SC-5／SC-6 各承接一條），殘餘 **(c)(d)**（§5 Phase 表↔§8 對應、內部交叉引用可解析）需跨區段建索引比對、不在字面樣式射程內，該列**維持開啟**，勿因「§9.1 已接線」誤判結案 |
| DEF-101-556 | 2026-07-28 | R60 round 1 四方複審（SA-R60-04，blocking） | **`DEF-101-554` 狀態停在 `open（待主控還原）`、內文指示後續輪對已經乾淨的工作樹跑 `git checkout -- .../governance/rules/`，而該還原早已完成 ⇒ 帳本把已完成的事記成未完成**。實害三層：`_classify` 判為 `open` 並計入 crossref 活躍狀態、`archive_defect_log.py` 判準②永遠擋它搬遷、R61 會把它當活 backlog 承接並執行一條無事可做且屬破壞性的指令。該列的 1724／1725 兩個數字亦已過期 | P2（Scan-G 家族的**反向形態**：Scan-G 抓「留無主 backlog」，本筆是「留已無事可做的 backlog」；與 `DEF-101-434`〔結案記錄被埋〕同一機制、方向相反） | 本輪 round 2 帳本側訂正 | fixed@R60：`DEF-101-554` 狀態改 `fixed@R60`，附還原完成的三項獨立實測憑證（governance `git status --porcelain` 輸出 0 bytes／39 支規則檔 `fire_count` 全 0／`test_arch_fitness.py` 93 passed 且 `::test_ff9_repo_no_structural_fail` 單跑 1 passed，跑完再驗 governance 仍 0 變更），移除對後續輪的 `git checkout --` 指示、改寫為事故留痕，並訂正 1724／1725 → 現行 **1736 passed／6 skipped**。**同時複查本輪其餘活躍列**：`DEF-101-550` 確實仍未修（真 `open`，只補承接輪次）、`DEF-101-541` 的 `routed` 半邊拆為 `DEF-101-559`、`DEF-101-548` 呼叫端已完成故收斂為單一 `fixed@R60`。**round 2 另掃出並訂正 3 筆存量非法首詞**（`DEF-101-238`「R55 部分收斂」／`DEF-101-358`「RESOLVED@R45」／`DEF-101-500`「fixed/deferred@」）：一律以「補合法首詞 ＋ 原文完整保留於後」處置，三筆 `_classify` 結果**皆不變**（`open`／`open`／`fixed`）⇒ 零語意變更；此舉使 SA-R60-08 ③ 建議的「狀態欄首詞必須合法」硬斷言可**零白名單**落地（否則它上線第一天就要豁免 4 筆）（完整證據見 `CrossPlatform_R60_Fix_Evidence.md` 的 `## DEF-101-556` 節） |
| DEF-101-674 | 2026-08-01 | R67 DOCRULE 包自審（新落地硬規則② 機械鎖的誠實劃界） | **硬規則② 機械鎖只做關鍵字比對、不做否定語意分析**：「無回執」「零改派」照樣被當成「已載明」而放行。實測 `DEF-101-561` 目前正是靠狀態欄一句「無輪次無回執」（描述的還是另一個子項）通過本鎖；它**實質上不是孤兒**（同列已載明 R61 Architect 的逐項回覆＝真有回執），故結論湊巧正確、機制不精確。另 `status@Rnn` 時點寫法與不含 `R\d+` 的散文式指派亦不涵蓋 | P3 | 根層 `tools/check_defect_log_crossref.py` ＋ 治理文件（誠實劃界，已釘常駐斷言） | fixed@R74（本列標的＝硬規則② 出口判準的否定語意缺口，**R74 已收掉**：判準本體外移 `tools/lib/defect_ledger_index.reassign_hit()`，除否定前綴外一併收掉原先**未具名**的兩種白拿形態（跨欄、code span 引述）；原本設計成「缺口被收掉時翻紅」的常駐斷言已同步改寫為正向斷言（`tools/tests/test_check_defect_log_crossref.py`）。原欄文自下方冒號後完整保留、語意零變更）：`open watch`（已於 `orphan_backlog_problems()` docstring 與 `CrossPlatform_Scan_Dimensions.md` 硬規則② 以 R57 判例的「已實測涵蓋／已實測不涵蓋／未窮舉」三段式記載；常駐斷言 `test_known_uncovered_negated_reassignment_still_escapes` 會在有人收掉否定語意時翻紅、強迫同步更新兩處清單。收掉需否定語意分析，屬逐行正則相對 AST／NLP 的結構性天花板，同 DEF-101-333 的比例原則裁定） |
| DEF-101-652 | 2026-08-01 | R67 Scan-F（CLI 包；沙箱實跑七種參數變體） | **`AutoClaude/tools/run_local_nightly.sh:53,112` 只認 `--force`**：無心跳時 `--help` 直接開跑 4-stage nightly（沙箱實測落下 `nightly_mac_*.log` 並跑進 macos_smoke 7 個子步驟）；有心跳時 rc=0 卻印「今日已有心跳…跳過本輪」——查說明被記成一次成功的 nightly 去重。`--forse`／`-f`／`--Force`／`--FORCE`／裸位置參數等七種變體實測 rc 全為 0 | P2（原 P1，四方複審降級） | AutoClaude nightly 載體 `tools/run_local_nightly.sh` | fixed@R67：補 `print_usage()` ＋ `case` 參數關卡（`--help` rc=0 不執行任何 stage、未知/多餘參數 rc=2）；回歸鎖 `AutoClaude/tests/tools/test_run_local_nightly_sh_static.py` 新增 8 支沙箱實跑鎖；缺陷注入雙向實測 rc=1（7 FAILED，且 `--help` 再度產生 nightly log）→ rc=0。🔴 **殘餘且未修**：`AutoClaude/tools/run_local_nightly.ps1`（1149 行）同樣沒有頂層 `param()`／`-Help`，Windows 對等缺口仍在，改派為：未指派（解鎖條件＝有 Windows 真機可實跑驗證的一輪） |
| DEF-101-710 | 2026-08-02 | R68 Scan-G（同上普查） | **`_classify()` 取「最早出現的關鍵字」使狀態訂正被讀反**：一列若在狀態欄先寫舊狀態、後段才寫「→ fixed」訂正，分類器仍judge為未結並計入未結存量。實測 `DEF-101-432` 即此形態（首詞判未結、後段有 `→ fixed`）；同族的 `DEF-101-433` 已用「把首詞改為已結 token、原文以反引號逐字保留於後」的體例處理過，可作範式 | P3 | 根層 `tools/check_defect_log_crossref.py` ＋ 帳本體例 | fixed@R68（**偵測面**）：新增「狀態首詞待更新」warning，逐列指出「首詞判未結、後段卻有已結訂正」並附 `DEF-101-433` 體例指路。🔴 **`DEF-101-432` 本身的首詞未改**（改他人撰寫的狀態欄需逐字核對其訂正是否確為現況，超出本輪授權面），改派為：未指派。解鎖條件＝回讀 `DEF-101-432` 全欄確認訂正即現況後，依 `DEF-101-433` 體例改首詞，並使本 warning 對該列消失 |
