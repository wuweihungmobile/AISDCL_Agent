# AutoSDD Defect Log — Archive 64

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **35 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段、角引號引述、與「訂正首詞（原文…接於後）：」之後到下一個 `｜` 為止的舊狀態引文內的字樣皆不算，R68 收窄／R76 補第三種）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-422`、`DEF-101-274`、`DEF-01-007`、`DEF-101-968`、`DEF-101-935`、`DEF-101-976`、`DEF-101-973`、`DEF-101-972`、`DEF-101-021`、`DEF-101-970`、`DEF-101-957`、`DEF-101-969`、`DEF-101-975`、`DEF-101-025`、`DEF-101-937`、`DEF-101-962`、`DEF-101-971`、`DEF-101-939`、`DEF-101-958`、`DEF-101-966`、`DEF-101-963`、`DEF-101-955`、`DEF-101-965`、`DEF-101-940`、`DEF-101-942`、`DEF-101-949`、`DEF-101-964`、`DEF-101-953`、`DEF-101-948`、`DEF-101-954`、`DEF-101-943`、`DEF-101-945`、`DEF-101-944`、`DEF-101-956`、`DEF-101-952`
>
> **判準全過但以 `--only`／`--keep` 具名排除、刻意留在主檔者**（DEF-101-811；排除留痕，非無聲少搬）：（無）
>
> **本次操作備註**：R81 開場洩壓
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-422 | 2026-07-26 | R54 Workflow 四方複審 round 1（autoclaude 領域，對應候選清單發現 1，SA 回報且回報者本人已自我標註非本輪授權範圍） | `AutoClaude/autoclaude/evolution/_evaluator_derivation.py:53-64` `derive_part_a_evaluator()` 對 pytest 類指令做 `head = " ".join(tokens[: pytest_idx + 1])` 時，會把 `pytest_idx` 之後（含測試目標路徑等位置引數）全部捨棄——例如 `"pytest tests/test_auth.py -v"` 剝除旗標後產出的 Part A 指令變成 `"pytest --collect-only"`（掃描整個測試樹而非目標檔案），既有測試（`tests/test_gap021_028.py::TestGap026SplitStepEvaluator`）因 `pytest --collect-only` 掃全樹仍 `rc=0` 而無鑑別力偵測此聚合效應；經本輪 autoclaude 領域 agent 重新查證屬實，且與作業系統平台無關（macOS/Linux/Windows 行為一致） | P3（真實缺陷但為一般功能缺陷，非 Mac/Windows 跨平台相容性議題；回報者已明確標註本輪不建議處理） | 本輪刻意不修復：因（a）回報者於發現描述中明確自我標註「非本輪跨平台相容性授權範圍」「不建議本輪處理」；（b）`AutoClaude/tools/check_loc_budget.py` 本輪動工前實測 `total=cap=20438` 零餘裕，任何非本輪授權範圍的 `autoclaude/` 異動都會直接觸發 CI 硬阻擋，貿然修復風險與授權外觀皆不划算。建議修法方向：改用「保留所有非旗標 token（含位置引數），僅剝除已知旗標」取代目前的 `tokens[:pytest_idx+1]` 整段截斷，並補充回歸測試（斷言目標路徑仍出現於推導結果中） | closed-by-decision@R80（包 C：分流欄本身已是拍板結論——逐字寫「本輪刻意不修復」並列兩條理由，R60 又實測交棒容器從未被更新 ⇒ 不是「還沒排到」，是已決定不在跨平台軌處理。要改由 AutoClaude 軌處理請另立新列。詳見 CrossPlatform_R80_Scan_Findings.md §C） |
| DEF-101-274 | 2026-07-23 | R27 Architect 架構專項檢視（跨平台架構最佳化評估延續） | **`AutoClaude/tools/check_loc_budget.py` 的 `SCAN_ROOT` 掃描範圍縫隙（DEF-101-271）本身缺乏機械後盾**：DEF-101-271 訂出「`tools/dev_start.py` 超過 2000 行即升級為該輪必修」的具體觸發門檻，但該門檻的偵測完全依賴審查員每輪手動 `wc -l`——若某輪遺漏比對或 `dev_start.py` 在輪次之間悄悄膨脹，沒有任何機械閘門會攔下，等同「用人工紀律守護人工紀律」，與本倉其餘一切皆有機械 gate 的風格不一致（R27 實測 `dev_start.py` 現況仍為 1772 行、零成長，尚未觸發必修門檻） | P3（本輪未觸發必修，`dev_start.py` 距 2000 行門檻尚有 228 行餘裕；純屬治理機制本身的健壯性縫隙，非緊急功能缺陷） | 將 `SCAN_ROOT` 擴為清單或另建輕量掃描涵蓋根層 `tools/`／`AutoClaude/tools/`，給薄殼化後的 Python 核心檔訂一個 orchestrator 級上限（Architect 建議 1500，使 `dev_start.py` 現值 1772 立即顯性化為既有超標、需拆），使 2000 門檻自動可執行而非靠記憶；`AutoClaude/tools/ab_compare_backends.py`（720 行）距絕對紅線 750 僅 30 行，納管後可提早預警 | fixed@R80（包 C 實查：**本列的主張今日為假**。它抱怨「只能靠審查員手動 wc -l、零機械閘門」，而 `check_loc_budget.SPECIAL_FILES['../tools/dev_start.py'] = 2000` 已在（R68 落地，該處註解逐字引本列），根 `tools/` 另有 ROOT-TOOLS 分級。剩下的 orchestrator 級門檻是人工拍板題，居所＝`DEF-101-271`。詳見 CrossPlatform_R80_Scan_Findings.md §C） |
| DEF-01-007 | 2026-06-12 | W8 整合驗收 | `cc-switch` 未安裝（`Get-Command cc-switch` 不存在），計畫 §5.2 多模型後端 A/B 驗收無法執行；`integration_gate.ps1` [5/5] 段落以 SKIP + 指引處理（非偽綠：SKIP 明示於輸出） | P3 | 環境工具補裝後依 §5.2 手動執行 A/B（指標：一次通過率 / CORRECTION 次數 / SDD_CONTRACT_VIOLATION 次數 / token 峰值）；俟環境補裝 | closed-by-decision@R80（包 C：立案前提是環境工具未安裝，非程式可修；倉內阻塞已於 improving_10 全數移除，此後歷輪複驗結論相同、近兩個月零變化，且不阻擋任何鏈（gate [5/5] 是明示 SKIP 非偽綠）⇒ 決定接受。詳見 CrossPlatform_R80_Scan_Findings.md §C） |
| DEF-101-968 | 2026-08-08 | R80 包 B（act Linux 容器實跑） | `resets 9am` 這個牆上時刻沒被綁在任何時區上：錨點取機器時區、`now` 由呼叫端給 ⇒ 兩個框架，`sentinel_decide` 的「reset 過了沒」在 UTC 容器與 UTC+8 本機翻面（act 逐字 `'arm_reset' != 'probe'` 兩支）。Windows 本機結構上看不見 | P1 | 訊息括號自報 IANA 時區 ⇒ `guard.declared_zone()` 優先、次之 `now` 的時區、naive 補 offset；`sentinel_decide` 傳 `now.tzinfo`；`register_endurance` 丟 offset 前先 `astimezone()` | fixed@R80｜鎖＝`ResetFrameIsNotTheMachineClockTest`。注入：錨點改讀機器時區 ⇒ TZ=UTC0 下 5/5 紅、還原 0 紅 |
| DEF-101-935 | 2026-08-08 | R80 包 F S8-01 | `AISDLC_SDD/AISDLC_SDD_v0.30/tools/init_project.sh:42` 自己的 `-h` 教人跑 `./init_project.sh`，而該檔 git 索引模式是 100644 ⇒ mac/Linux 一 clone 照著做即 rc=126；bash 3.2 容器內以 HEAD 樹實測 rc=126，改索引模式 100755 後同指令 rc=0。既有 exec bit 鎖只掃 `.md`，腳本自述用法不在射程 | P2 | 已修（`git update-index --chmod=+x`，內容零變更）＋補鎖 `check_script_parity.py::_check_self_help_advertises_executable_form` | fixed@R80：紅綠自證（還原後 rc=1）；收尾同步 ONBOARDING §6 exec bit 政策句涵蓋面。詳見 CrossPlatform_R80_PackF_Posix_Evidence.md S8-01 節 |
| DEF-101-976 | 2026-08-08 | R80 收尾（為回填表② 實跑 ci-gate.sh 才顯形） | 本輪新建／新被消費的五支 `tools/lib/` 共用層未列入 `windows-compat-ci.yml`／`macos-compat-ci.yml`／`aisdlc-sdd-ci.yml` 的 paths ⇒ 只改那幾支時其回歸鎖在雲端不觸發（DEF-101-042 同構）。十三道根層閘門與 act 皆全綠，唯一看得到它的是 ci-gate，而 ci-gate 不在收輪閘門清單內 | P2 | 三支 workflow 的 push／pull_request 兩塊同步補列 | fixed@R80｜補列後 `test_ci_paths_cover_root_consumers.py` rc=0（47 passed）。🔴 一般化：收輪閘門清單與 ci-gate 是兩個不相交的觀測面，前者全綠不蘊含後者 |
| DEF-101-973 | 2026-08-08 | R80 包 B（act Linux 容器實跑） | Windows 專屬測試沒有平台守衛：`test_the_action_uses_a_no_console_interpreter` 斷言字面 `pythonw.exe`，而 POSIX 沒有 `pythonw`（`quiet_python()` 依約回 `sys.executable`）⇒ 容器必紅。鐵律三被寫成平台常數 | P2 | 換成兩平台同一條：Action 載具須等於 SSOT `guard.quiet_python()`（行為面）＋ planner 須真的呼叫它（來源面，讓 POSIX 上改成 `sys.executable` 照樣紅）＋ Windows 上該解析器須真回 `pythonw.exe` | fixed@R80｜刻意不加 skip 站點（`_SITE_CLASS_CENSUS` 是相等棘輪，新站點會逼別包重釘），改用平台條件斷言 |
| DEF-101-972 | 2026-08-08 | R80 包 B（QA-02＝SD-04） | 被判為 P0 根因的假前提仍原文留在**兩個家**：`sentinel_decide()` 的規格註解與其回歸測試 docstring 都還寫「跑得動武裝指令 ⇒ 額度是通的」，而武裝零 API 呼叫。讀 planner 的人拿到已被推翻的規格，而那支測試守的是 production 走不到的分支 ⇒ 綠燈替假話背書 | P1 | 兩處同步訂正並指向 `guard.unhandled_limit_event` 的復原證據判準；測試標註為相容性測試並指名真正守現行語意的那組 | fixed@R80｜現行語意判準＝`UnhandledLimitDetectionTest`（事件晚於全域最後一次成功回應 ⇒ 未處理） |
| DEF-101-021 | 2026-07-10 | 跨平台修復輪四方複審第三輪（known-gap 盤點，DEF-101-010 殘餘） | **mutation artifact 累積鏈 90 天上限**：GitHub artifact retention 上限 90 天——連續 90 天無 token_guard 源碼變動觸發 `autoclaude-mutation-on-change.yml`，鏈上 `mutation-history` 過期 → 累積歸零重累；且 GitHub 側與 Windows 本機 nightly（`run_local_nightly.ps1`）為兩條互不同步的獨立累積點 | P3 | 平台上限如實揭露；known-gap 記入根層 `ONBOARDING.md` §9 | closed-by-decision@R80（包 C：平台上限非本 repo 可修，另有第二累積點 ⇒ 決定接受。見 R80_Scan_Findings §C） |
| DEF-101-970 | 2026-08-08 | R80 包 B（SA-R80-02、掃描 S7-02） | S1「不要爆」的阻斷臂**命中面為 0**：`BLOCKING_TOOLS` 圈的三個名字在本 harness 的 8,106 次 tool_use 裡出現 0 次（實際叫 `Agent`／`Workflow`）；R79 為它建的鎖只把 matcher 與射程釘成相等，保證的是「兩個都寫錯時也一致」 | P1 | 補 `Agent`／`Workflow` 進常數與 PreToolUse matcher（舊三名保留＝上游標準名）；新增 `blocking_reach_problems()`：圈到的名字須與實測 tool_use 名稱有非空交集，量不到時不判紅 | fixed@R80｜鎖＝`PreToolUseBlockTest` 三支。注入：改回 R79 三名 ⇒ 3/3 紅 |
| DEF-101-957 | 2026-08-08 | R80 收尾單人窗口 | 兩道硬閘互為對方的違規：`unregistered_governance_docs()` 教人「請在 `_GOVERNANCE_DOCS` 補一筆」，而該清單住的 `tools/check_defect_log_crossref.py` 受 SPECIAL_FILES raw-line 棘輪管、餘裕實測只剩 3 行（< 判準要求的 5）⇒ 照訊息做即撞棘輪，本輪包 F 新生的證據檔因此登記不進去、連帶 15 支測試紅 | P2 | 把單調增長的登記面下沉 `tools/lib/governance_docs.py`（再匯出，物件同一性不變） | fixed@R80：閘門檔 1471→1419 行、餘裕 3→55；**未調高任何門檻**。先例＝`tools/lib/defect_ledger_index.py` |
| DEF-101-969 | 2026-08-08 | R80 包 B（SD 複審 R80-SD-01） | P0 修復引入**反向靜默自毀**：`unhandled_limit_event()` 把任何 `<synthetic>` 都當撞線，而那是 harness 對所有合成訊息共用的標記（`API Error`／中斷皆然）⇒ 以中斷收尾的 session 被判成未處理撞線 → escalate → **哨兵自我刪除**。原註解的 0.0% 假陽性是單一時點橫斷面，量不到這個縱向情境 | P0 | 候選登記前先過 kind 篩選：只有 `LIMIT_SESSION`／`LIMIT_SPEND` 算撞線 | fixed@R80｜鎖＝`UnhandledLimitDetectionTest` 三支新判準（含鑑別力反證）。注入：篩選失去鑑別力 ⇒ 3/3 紅 |
| DEF-101-975 | 2026-08-08 | R80 二審（NEW-ARCH-R80B-02＝QA2-N4） | 唯一仍紅項對量測入口隱形：act 兩平台一致卡在 ONBOARDING §7 表② presumed-stale 觸發器，收斂方交棒給 `DEF-101-747`，而該列首詞 `fixed@R74` ⇒ 不進 `--unresolved-count`、可被 archive 搬走 | P2 | 本列即該缺口的獨立載體（同 DEF-101-736） | fixed@R80｜repo 外 `venv-clean-r80`（只裝 `.[dev,notifications]`、pgextras absent）跑 `--write --with-slow` 回填 Windows 欄四格＋指紋/provenance，`--check-snapshot` rc 1→0；**未**走 `--allow-pg-extras`；回填前先解掉擋路的 ci-gate 真紅（DEF-101-976） |
| DEF-101-025 | 2026-07-11 | 跨平台修復輪四方複審第三輪（SD 複審終裁輪，DEF-101-012 修復之語意註記） | **根層基建 bash -n leg 檢查「工作樹版本」而非 staged blob**：`tools/git-hooks/pre-commit` root-infra leg 以 `bash -n "$TOPLEVEL/$f"` 檢查工作樹檔——staged 壞＋工作樹好的罕見組合會本機假綠入庫 | P3 | 與既有 ruff-on-worktree 慣例一致故不改本機行為；known-gap 記入 `ONBOARDING.md` §9 | closed-by-decision@R80（包 C：與既有 ruff-on-worktree 慣例一致，雲端 root-infra-ci 機械攔回 ⇒ 決定接受。詳見 CrossPlatform_R80_Scan_Findings.md §C） |
| DEF-101-937 | 2026-08-08 | R80 包 F S8-02 | shellcheck 全庫零接電：`# shellcheck disable=` 指令散在 19 支腳本共 23 處（作者相信它在跑並為它寫豁免），而 `.github/` 對 `shellcheck` 命中數為 0 ⇒ 從未執行過一次。規模實測（29 支 active 腳本、`koalaman/shellcheck:stable`）：error 1／warning 13／info 20／style 20 | P2 | 已接電：新增 `tools/run_shellcheck.py`（雙向基線棘輪，載具缺席 rc=2 fail-loud）＋ `.github/workflows/shellcheck-ci.yml` | fixed@R80：存量基線 11 筆、只准變小；未放寬任何門檻。詳見 CrossPlatform_R80_PackF_Posix_Evidence.md S8-02 節 |
| DEF-101-962 | 2026-08-08 | R80 包 C（SA-R80-03） | 本輪不是淨減法輪，且閘門靠**調高棘輪基線**通過：`_FROZEN_GUARD_LINES` 63056→64584（+1528）。掃描 S5-02 已指出「收費站不是棘輪，自助放行寫在判準訊息裡」，本輪又走一次 | P2 | 選項 (b)：明文承認＋逐檔清單與必要性辯護；並把該要求由訊息文字升為判準 | fixed@R80：`repin_log_problems()` 款(9)`[未附刪除清單]`——正淨額列須①量化刪除交代或標 `[非淨減法輪]`，且②指名具名 .md。注入 3 紅 4 綠。相關 `DEF-101-947`。詳見 CrossPlatform_R80_Scan_Findings.md §D |
| DEF-101-971 | 2026-08-08 | R80 包 B（SD 複審 R80-SD-03） | 機械鎖的**紅燈訊息裡**住著本輪自己判定為假的那句話：`detached_conflict_problems` 的 docstring 與失敗訊息逐字寫「DETACHED 會抵銷 CNW」，而同輪重量已證那是**載具效應**（uv trampoline 不轉傳 creationflags），真直譯器那一列是 0 | P2 | 改寫成成立的理由並帶射程劃界（`python -m venv` 回退路徑未驗），SSOT 指向 `guard.NO_WINDOW` 旁的四載具矩陣 | fixed@R80｜規則不變（該組合在本 repo 載具上仍實測會彈視窗），只換理由；注入自證那支的措辭同步訂正 |
| DEF-101-939 | 2026-08-08 | R80 包 F S8-03 | `AutoClaude/tools/run_mutmut_in_docker.sh:184` 的 `RESULTS_RC=$?` 讀到的是複合區塊最後那個 echo 的 rc ⇒ **恆為 0**，`mutmut results` 真失敗時完全不反映在 `log written exit=` 那行。bash 3.2 最小重現：舊寫法 `OLD_RC=0`、新寫法 `NEW_RC=1` | P2 | 已修（rc 緊貼受測指令落地，區塊 rc 另存，取先非零者） | fixed@R80：`bash -n` 於 bash 3.2 容器 rc=0；判準交由 S8-02 接電的 shellcheck SC2319/SC2320 看守，刻意不另寫第二份正則掃描器。詳見 CrossPlatform_R80_PackF_Posix_Evidence.md S8-03 節 |
| DEF-101-958 | 2026-08-08 | R80 收尾單人窗口 | 同型在另兩支護欄層檔同時發生：`check_script_parity.py` 1730 > 1618（raw-line 棘輪，HEAD 值＝門檻＝零餘裕）、`probe/audit_session.py` 816 > 750（`guardrail_cli` 分級）；兩者都是本輪修復包**新增判準**撐破的 | P2 | 判準本體下沉 `tools/lib/self_help_exec_parity.py` 與 `tools/lib/rc_after_pipe_real.py` | fixed@R80：1730→1616、816→線內，`check_loc_budget` rc=0 violations=0；行為零變更（parity rc=0、`--selftest` 10/10 綠、舊判準對照仍錯 6/10）。**未調高任何上限** |
| DEF-101-966 | 2026-08-08 | R80 包 A（SA-05） | `carrier_liveness_problems()` 在非 Windows **一律回空**＝把「沒有 Windows 載具」寫成「沒有載具問題」；POSIX 條目吃 PATH 上任意 python3（macOS 內建 3.9 < 本 repo 下限 3.11），檔不在／無執行位元／直譯器太舊三種失效表徵相同且 fail-open | P1 | 新增 `posix_carrier_problems()`：存在＋X_OK＋shebang 解析＋版本 ≥ `POSIX_MIN_PY`，並與 bootstrap SSOT 對帳 | fixed@R80：`TestPosixCarrierLiveness` 7 格；弄壞（回空／門檻改 3.8）→紅、還原→綠 |
| DEF-101-963 | 2026-08-08 | R80 包 C（R80-SD-05） | 八維掃描 88 筆發現多數未入帳；六個修復包全在額度上限被打斷，未修的只活在 workflow journal（commit 後即清掉）⇒ 缺口既沒補、也沒變成可查的量測值 | P2 | 落成具名證據檔逐筆標落地狀態，並同步登記 `_GOVERNANCE_DOCS`（DEF-101-957 判例） | fixed@R80：新建 `CrossPlatform_R80_Scan_Findings.md`（88 列）並登記，crossref rc=0。🔴 88 筆全部**未複驗**（verdicts=0），落地欄是包級推論非逐筆重驗 ⇒ 重驗承接 R81 |
| DEF-101-955 | 2026-08-08 | R80 hook exec-form | `.claude/settings.json` 的 hook 全走 shell form ⇒ 每次觸發彈一個 conhost 視窗（R80 實測：shell form 一次 hook 產生 1 個 conhost，exec form 產生 0 個）。連帶發現 **8 支解析器對 exec form 失明**（只認 `command` 字串裡的檔名，陣列形態一律漏掉） | P2 | 20 條 hook 全轉 exec form ＋ 新增 `.claude/hooks/_hook_launcher.py`；8 支解析器補 exec form 解析面 | fixed@R80：零視窗實測；解析器改動由 `tools/lib/hook_wiring.py` 統一供給 |
| DEF-101-965 | 2026-08-08 | R80 包 A（ARCH-02＝SD-02） | `hook_wiring` 對 `type` 有三種慣例；形態鎖那種是 `get("type")!="command"` 整條跳過 ⇒ 退回 shell form 只要順手省掉 `type` 就能逃過 A~F 全部判準（實測舊判準 0 問題、新判準 1 問題，而 `settings_targets` 看得見它） | P1 | 收斂成單一 `is_command_hook()`（argv 解析／形態鎖／載具枚舉共用），並補第六格注入 | fixed@R80：`TestHookEntriesAreExecForm` 注入 6 ＋ type 慣例三站點鎖，弄壞→紅／還原→綠 |
| DEF-101-940 | 2026-08-08 | R80 包 F S8-06 | `tools/lib/git_hooks_install_common.sh:1` 是全庫 168 支 tracked `.sh` 中唯一沒有 shebang 的一支，shellcheck 判 SC2148 error；後果不是「會炸」（它只被 source）而是**整支檔的其餘判準一併降級** ⇒ 實質落在 lint 盲區 | P3 | 已修：檔首加 `# shellcheck shell=bash`（刻意不加 shebang，加了會誤示它可直接執行） | fixed@R80：修後 `-S error` findings 由 1 降為 0。詳見 CrossPlatform_R80_PackF_Posix_Evidence.md S8-06 節 |
| DEF-101-942 | 2026-08-08 | R80 包 C S5-03 | 「找可用 bash」七份實作、五份是手抄本：三份 `_usable_bash()` 剝 docstring 後 AST 逐字相同（雜湊 9797b0251822），另兩份 `_bash_exe()` 已漂移成 `except OSError` vs `except Exception`；檔頭「各消費者獨立重寫維持鑑別力」的宣稱經 AST 實測證偽 | P2 | 五份收斂至 `tools/tests/_platform_helpers.py::usable_bash_for_fixture()` | fixed@R80：覆蓋逐案承接且更硬。詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-03 節 |
| DEF-101-949 | 2026-08-08 | R80 包 C S5-03(e) | `tools/lib/git_hooks_install_common.sh` 102 > 100 行薄殼上限：本包一字未動該 `.sh`（`git diff --numstat` 為 `5 0`），是他包在本輪加的 5 行（S8-06 的 shellcheck 指令＋4 行說明）撐破的 | P2 | 禁止調高 `_SH_MAX_LINES` | fixed@R80：收尾包把那 4 行說明併成 2 行（指令行必須留在檔首故不動），`.sh` 回到 100 行、判準綠。**未調高上限**。詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-03(e) 節 |
| DEF-101-964 | 2026-08-08 | R80 包 A（ARCH-01 複驗） | 載具存在性鎖斷言的是**機器狀態**不是 repo 內容 ⇒ windows-smoke 在 bootstrap 前就跑根層 unittest、未 bootstrap 的全新 clone 亦然，兩者必紅（實測 problems len=1） | P1 | 機器狀態交回 `tools/check_hooks_liveness.py`（advisory＋`$CI` 豁免）；單元測試改守機器無關的「宣告載具＝bootstrap 產得出來的那個」 | fixed@R80：判準零放寬，SSOT `bootstrap_core.VENV_DIR`；注入 4 格紅綠 |
| DEF-101-953 | 2026-08-08 | R80 續航修復 | `claude --add-dir` 是**變長參數**：把它排在 prompt 之前會把 prompt 一起吃進目錄清單 ⇒ rc=1，而續航協定的**五段稽核痕跡全綠**（排程有 `NextRunTime`、log 有時間戳、狀態寫 `armed`）⇒ 失敗完全靜默，正是本 repo 反覆在治的「取證全綠但事情沒發生」 | P0 | 參數順序改為 prompt 在最後；痕跡增記子行程 rc | fixed@R80：ADR-XPLAT-004 同步；紅綠自證＝還原參數順序即 rc=1 |
| DEF-101-948 | 2026-08-08 | R80 包 C S5-11 | `tools/probe/xplat_injection_matrix.py`（305 行）零自動化消費者 —— 判定**保留**：它是 DEF-101-796 的唯一可重跑產物，刪掉等於把該缺陷放回去；零消費者是設計（`--apply` 改共用工作樹，需停工窗口） | P3 | 可接電的只有 `--dry-run` 半邊，屬加行故交棒 | closed-by-decision@R80：詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-11 節 |
| DEF-101-954 | 2026-08-08 | R80 續航修復 | 額度哨兵的偵測面失明：`handled_through` 把「武裝指令跑得動」當成「額度是通的」，而武裝本身是**零 API 呼叫**（只讀逐字稿＋排 schtasks）⇒ 撞線期間該判準恆為真，哨兵永遠不會轉成續航排程 | P0 | 判準改為只認「真的花過 token 的探測」的結果 | fixed@R80：本輪修畢；`--sentinel-tick` 四分支判定鎖同步 |
| DEF-101-943 | 2026-08-08 | R80 包 C S5-03(b) | 上一列漂移出來的 `except OSError` 讓 timeout 逸出：`subprocess.TimeoutExpired` 繼承 `SubprocessError` 非 `OSError`，而該呼叫在模組層 `skipUnless` 內 ⇒ 候選卡住即整檔在 collection 期就炸。兩份實作的行為分歧零測試比對 | P2 | 隨上一列一併收斂到共用 helper | fixed@R80：詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-03(b) 節 |
| DEF-101-945 | 2026-08-08 | R80 包 C S5-05 | 已死的 `_MARKER_PAIRS` 仍被三個檔案寫成「新腳本第一條納管途徑」：空清單自 R16、迴圈跑零次，紅燈訊息卻教人去填它 | P2 | 刪名冊與迴圈，保留仍有活消費者的 `_extract_markers`／`_compare` | fixed@R80：R77-34「刪死碼收益低於風險」據此翻案。詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-05 節 |
| DEF-101-944 | 2026-08-08 | R80 包 C S5-04 | 「獨立重寫維持鑑別力」被當成全庫慣例引用，射程從未寫清楚 ⇒ 同棵樹裡的純函式複本也拿它當免死金牌 | P2 | 射程訂為「被硬邊界隔開、不可能 import 的真獨立實作」；同棵樹的純函式複本不適用 | fixed@R80：判別式與四處引用訂正詳見 CrossPlatform_R80_Subtraction_Evidence.md S5-04 節 |
| DEF-101-956 | 2026-08-08 | R80 收尾實查 | `context_budget_guard.quiet_python()` 的註解宣稱兩層防線（`pythonw.exe` ＋ `CREATE_NO_WINDOW`），實作只有一層 ⇒ **幽靈符號級的假宣稱長在防彈窗的那支 hook 自己身上** | P2 | 補實第二層（無 console 父行程下 spawn console 子行程必帶的 creationflags） | fixed@R80：舵手當回合補實並實測 |
| DEF-101-952 | 2026-08-08 | R80 續航修復 | Token reset 後的自動續跑在**錯的工作目錄**啟動：排程 Action 未指定 cwd，`claude -p -r` 於 `%SystemRoot%\system32` 開跑 ⇒ 所有相對路徑判準與 repo hooks 全部失準，而排程 `NextRunTime` 照常有值 | P0 | 續跑指令改為顯式落在 repo 根 | fixed@R80：端到端證實——續跑自報 `CWD=` repo 根 |
