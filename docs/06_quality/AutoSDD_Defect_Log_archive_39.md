# AutoSDD Defect Log — Archive 39

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **6 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串）／③未被 crossref 掃描目標做過可辨識狀態宣稱／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 7 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在主檔索引段有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-626`、`DEF-101-629`、`DEF-101-630`、`DEF-101-616`、`DEF-101-661`、`DEF-101-662`
>
> **本次操作備註**：R67 收輪第二次輪替（建立餘裕，非為搬空）：第一次輪替後主檔 258995 bytes、距硬線 262144 僅 3149 bytes 且可搬列歸零＝下一輪連加一列都會撞硬閘。本次以 --ack-handoff 具名承認 6 筆判準④ 誤報：DEF-101-616（fixed@R63，marker「下一輪」出現在**引述 HANDOFF_PROSE_RE 既有五種詞面**的缺陷描述裡）、DEF-101-626（fixed@R66，marker 在引述 ADR 過期敘述的散文裡）、DEF-101-629（fixed@R66，同）、DEF-101-630（fixed@R66，marker 在「下一輪讀者依此座標核對會對不上」的後果描述裡）、DEF-101-661（fixed@R67，marker 實為函式名 orphan_backlog_problems()）、DEF-101-662（fixed@R67，marker 在引述 ADR 交棒表原文與 item 12 反例的敘述裡）。六筆逐一回讀確認狀態皆已結案、列內無任何待接工作。**刻意不 ack** DEF-101-652（run_local_nightly.ps1 對等缺口未修）與 DEF-101-663（596／610 改派的具名載體），兩者有真實待辦，一律留主檔
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-626 | 2026-07-31 | R66 Review round 1（SD，独立審查文件狀態一致性） | **`ADR-XPLAT-002` 頂部狀態欄的「R66 更新」段逐字自稱「純文件即時性訂正，未落地任何 Phase」，但本輪同時落地的另一個修復包（`DEF-101-624`：新增 `tools/lib/sdd_latest.py`，10 支 `tools/tests/*.py` 改為委派、2 處 `.match()` 誤用修正為 `.fullmatch()`）正是該 ADR §5 明文列出的 **Phase 2-C**（LATEST 版本解析樣板 10 份→1）與 **Phase 2-D**（frozen 版本 regex 5 份→1）——自相矛盾。§5 Phase 2 表 2-C／2-D 兩列亦未同步追加落地後的等價性證明段落（同表 2-A／2-E 兩列皆已有），對照下形成「本輪自稱沒做的事其實同輪已經做了」的記錄失真。SD 複審逐檔核對 `git diff` 並執行 `grep -rln '"sdd_version.py"' tools/tests/*.py`／`grep -rn -E "_?FROZEN_(SDD_VERSION\|VERSION_DIR)_RE\s*=" --include=*.py tools/`（🔴 R66 收尾訂正 `DEF-101-631`：原引用第二支指令 `grep -rn "AISDLC_SDD_v\d"` 因標準 grep BRE 不支援 `\d` 恆為空／exit 1，SD 當時據以下的「零殘留」結論並非真判準，已換用此可執行等價寫法重新驗證）兩鏡皆回零殘留，確認 Phase 2-C／2-D 邏輯上確實已完整落地、程式碼本身無問題，缺陷純粹在 ADR 文件自身的狀態欄與 Phase 2 表未同步。此為 `DEF-101-610`／`DEF-101-615` 記載的「同輪內並行修復包互不知情、致設計文件前提失效」失效模式第三次復發，且復發在本輪負責訂正過期敘述的那個修復包自己身上 | P3（不影響任何測試或程式碼正確性，純文件自我陳述失真；但作為專案高度依賴的架構決策記錄，不修正會誤導下一輪讀者以為 Phase 2-C／2-D 仍待辦，可能致重工或漏審，故 major 非 blocking） | `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md` | fixed@R66：狀態欄「R66 更新」段末追加「R66 二次訂正」段，逐字保留原「未落地任何 Phase」句（保留史料），追加說明本輪實際落地兩類項目（原句所述三項文件/工具操作＋`DEF-101-624` 的 Phase 2-C／2-D）；§5 Phase 2 表 2-C／2-D 兩列各自追加「🔴 已於 R66 落地」證據段（比照 2-A／2-E 既有格式），「前置／signoff」欄由「並行包全部停工」改註記「已解除」。**R66 現查覆驗**（非引用 `DEF-101-624` 歸檔紀錄，本次重新實測）：`grep -rln '"sdd_version.py"' tools/tests/*.py` → 空（0，原基線 10）；`grep -rn -E "_?FROZEN_(SDD_VERSION\|VERSION_DIR)_RE\s*=" --include=*.py tools/`（原指令 `grep -rn "AISDLC_SDD_v\d"` 恆為空／exit 1，`DEF-101-631` 訂正）排除 `tools/lib/sdd_latest.py` 本體 → 0 筆殘留（原基線 5）；`python tools/check_script_parity.py` rc=0（全綠，含 tier 棘輪／異名對等品等既有斷言逐行不變）；`python tools/check_script_parity.py --print-collapse` → `UEP=5`／`AC=48`；`cd tools/tests && python -m unittest test_bash32_compat test_component_sanitizer_shared_layer_lock test_platform_neutral_paths test_ps1_bom test_ps51_compat test_sanitize_component_frozen_sdd_versions_lock test_subprocess_encoding_hygiene test_windows_forbidden_filename_parity test_windowsapps_guard_bash_parity test_windowsapps_guard_cross_consistency -v` → **163 tests, OK**；`python tools/run_root_unittests.py` → **1123** 個測試、`OK (skipped=10)`、rc=0（下限 1069）；`ls tools/tests/*.py \| wc -l` → 56（不變，GLC 檔數 +0 屬實）。**後續複驗追記**（同輪，Fix-applier 覆核本列修復內容時發現）：本列修復文字內引用的 `.match()`→`.fullmatch()` 座標（`test_component_sanitizer_shared_layer_lock.py:52`／`test_sanitize_component_frozen_sdd_versions_lock.py:109`）為誤植，現行檔案 `grep -n "\.fullmatch("` 實查該兩處呼叫式分別位於 `:92`／`:152`（修復前舊碼 `.match()` 則在 `:103`／`:162`，隨改寫漂移），已修正 ADR 內兩處座標為現查行號；不影響本列缺陷本身的判定與其餘驗證數字，僅座標精度訂正 |
| DEF-101-629 | 2026-07-31 | R66 Review round 2（Architect，独立審查） | **`ADR-XPLAT-002` §5 Phase 2-C 列的「🔴 已於 R66 落地」與「R66 現查逐條覆驗」段悄悄置換掉原始驗收判準③**（原文明載：「bug-injection：任一消費者改回自帶 `_latest_root` ⇒ dedup 鎖須紅」，且指定手法為「擴充既有的 `tools/tests/test_platform_utils_dedup.py`（已內建 `_scan_repo_py_for(pattern)` 與 repo-wide『共用 helper 不得有第二份定義』機制，加模式即可）」）：`DEF-101-624` 落地當下只補了 `DEF-101-627`（`sdd_latest.py` 模組自身行為的回歸鎖，落在 `test_component_sanitizer_shared_layer_lock.py`／`test_sanitize_component_frozen_sdd_versions_lock.py`），完全沒有動 `test_platform_utils_dedup.py`；而「R66 現查逐條覆驗」段仍沿用①②③④⑤五個編號逐條列出證據，但③實際對應的是 `check_script_parity.py --print-collapse`（概念上是原始判準⑤「ΔUEP=0」），末尾附的 bug-injection RED/GREEN 實測則是 2-D 的 `.match`/`.fullmatch` 缺陷驗證，與原始判準③（消費者復發自帶定義）毫無關係——整段文字未有一字承認「判準③沒做」。**親自驗證**：修復前對 `tools/tests/test_platform_utils_dedup.py` 執行 `grep -n "sdd_latest\|resolve_latest"` → 零輸出，證實這道鎖確實從未落地 | P2（不阻擋功能正確性——`sdd_latest.py` 本身邏輯已由 `DEF-101-627` 覆蓋——但屬架構治理文件的誇大宣稱：任一消費者未來被還原成自帶 `_latest_root` 實作，此前沒有任何機械鎖能偵測，且 ADR 文字誤導下一輪讀者以為此收斂已受完整反悔保護） | 根層 `tools/tests/test_platform_utils_dedup.py`（補上判準③本尊）＋ `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md`（§5 Phase 2-C 覆驗段訂正） | fixed@R66：`test_platform_utils_dedup.py` 新增 `_SDD_LATEST_DEF_RES`（`resolve_latest_name`／`resolve_latest_root`／`exclude_frozen_sdd_versions` 三函式，比照既有 `_EXTRA_DEF_RES` 手法）與 `test_sdd_latest_helpers_defined_only_in_sdd_latest`，沿用本檔既有 `_scan_repo_py_for(pattern)` repo-wide 掃描機制（無需新增鎖檔，符合 `DEF-101-561③` 棘輪、不影響 GLC 檔數）。**bug-injection 紅綠實測**（直接對 `tools/tests/test_ps1_bom.py` 暫時注入、測完立即以 Edit 還原，`git diff` 確認還原後零殘留）：注入 `def resolve_latest_root(sdd_root): return sdd_root` 於該檔頂層後，`python -m pytest tools/tests/test_platform_utils_dedup.py -k sdd_latest -v` → `1 failed`（`AssertionError: Lists differ: ['tools/lib/sdd_latest.py', 'tools/tests/test_ps1_bom.py'] != ['tools/lib/sdd_latest.py']`，RED）；還原後同指令 → `1 passed`（GREEN，控制組）。全檔 `python -m pytest tools/tests/test_platform_utils_dedup.py -v` → `9 passed`。`python tools/run_root_unittests.py`（Bash）→ 帳本回填前 `Ran 1139 tests ... FAILED (failures=1, skipped=10)`（新增測試計入總數但 `test_defect_id_reference_integrity` 因帳本尚缺本列而轉紅）；帳本回填後同指令 → `Ran 1139 tests ... OK (skipped=10)`，rc=0（`DEF-101-627` 收輪基線 1138 + 本列新增 1 支測試 = 1139，`test_defect_id_reference_integrity` 隨帳本回填轉綠）。ADR `§5 Phase 2-C` 列同步追加「R66 三次訂正」段，逐字保留原覆驗段（保留史料，誠實標記③曾缺失），追加說明判準③本尊已補齊、指向本列 |
| DEF-101-630 | 2026-07-31 | R66 Review round 2（SA，独立審查） | **`ADR-XPLAT-002` §5 Phase 2-D 列記載的 `.fullmatch()` call-site 行號座標又已過期**：該列文字明載「2 處 `.match()` 誤用（`test_component_sanitizer_shared_layer_lock.py:92`／`test_sanitize_component_frozen_sdd_versions_lock.py:152`，R66 現查現行檔案 `grep -n \".fullmatch(\"` 逐行核對行號，取代修復前舊行號 103／162 已隨改寫漂移）」，但這兩個座標本身也是同輪產物——`DEF-101-627` 稍後在這兩檔各自新增約 12 行的 module docstring／新測試類別，把生產呼叫式又往下推。**親自驗證**：`grep -n "\.fullmatch("  tools/tests/test_component_sanitizer_shared_layer_lock.py tools/tests/test_sanitize_component_frozen_sdd_versions_lock.py` 實測生產呼叫式現行分別位於 `:104`（`sdd_latest.FROZEN_VERSION_DIR_RE.fullmatch(p.name)`）與 `:164`（`sdd_latest.FROZEN_VERSION_DIR_RE.fullmatch(p.name)`），與該列宣稱的 92／152 不符。此為本 ADR 自己在同一句話裡命名的『同輪內並行修復包互不知情、致設計文件前提失效』失效模式（`DEF-101-610`／`615`／`626`）**第四次復發**，且復發點恰好就在該句話自身——具高度反諷性 | P3（非阻擋：純文件座標，不影響邏輯或測試結果；但若不訂正，下一輪讀者依此座標核對會對不上而誤判） | `docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md`（§5 Phase 2-D 列） | fixed@R66：§5 Phase 2-D 列逐字保留原「92／152」段（保留史料，比照 2-A／2-C／2-E 既有慣例），追加「R66 四次訂正」段訂正為現查 `:104`／`:164`，並誠實記載本座標對頻繁演進的測試檔本質上易碎、往後同型引用建議一律標「R66 現查、非恆定」以提醒讀者現查而非直接採信 |
| DEF-101-616 | 2026-07-30 | R63 四方複審 SA-R63-01（`tools/archive_defect_log.py` 判準④ 覆核） | **`HANDOFF_PROSE_RE`（`tools/archive_defect_log.py:140`）對「向未來輪次交棒」的散文只匹配「下一輪／下輪」「R\d+候選」「解鎖條件」「deferred」「backlog」五種詞面，遺漏「留待」「承接者」「改派」等同語意但不同措辭的交棒用詞**：SA 複審親自重跑正則，發現 `archive_35.md` 的 `DEF-101-614`（「…留待 R62」）與 `DEF-101-615`（「…承接者改派 R63」）兩筆歸檔列即為舊正則實際漏放的真實案例——`archive_35.md` 標頭「判準④ 攔下、刻意未加 `--ack-handoff` 而留在主檔者：（無）」即為證據（若舊正則曾命中其一，該欄不會是空）。人工覆核確認該兩列本身標的皆已完成、不算誤歸檔，但正則本身確有此偵測盲區 | P3（非阻擋；歷史漏放列經人工覆核皆屬安全，本質為判準覆蓋率缺口） | 根層 `tools/archive_defect_log.py` | fixed@R63：`HANDOFF_PROSE_RE` 新增「留待」「承接者」「改派」三詞面（`\|` alternation 追加，不改動既有五種詞面）；新增回歸測試 `tools/tests/test_archive_defect_log.py::TestHandoffProseDetectionCoversAlternatePhrasing`（正樣本取 `archive_35` 的 `DEF-101-614`／`DEF-101-615` 真實列，證明新詞面命中且舊詞面集合確實漏放，2 case + 2 subtests 綠）；重跑 `python tools/archive_defect_log.py --plan` 確認現行主檔 0 可搬／0 需具名承認／96 不可搬三數字皆不變（新詞面對現行帳本零行為變化，96 筆全數已被判準①②③擋下，無列因新詞面由「可搬」轉「需具名承認」） |
| DEF-101-661 | 2026-08-01 | R67 Scan-H（DOCRULE 包；掃描員沙箱注入合成孤兒列實證） | **六道根層閘門之一的 `tools/check_defect_log_crossref.py:602` 對硬規則②（孤兒承接輪次）零鑑別力**——注入「open 且承接輪次 R2」的合成列後閘門仍 rc=0，還把它算成一筆有效狀態紀錄；規則自 R59 訂立、`docs/06_quality/CrossPlatform_Scan_Dimensions.md:191` 早已指名本檔為可行宿主，八輪未接線 | P3（原 P2，四方複審降級） | 根層閘門 `tools/check_defect_log_crossref.py` | fixed@R67：新增純函式 `orphan_backlog_problems()` ＋ `current_round()` 並於 `main()` 接硬閘 rc=1。當前輪由帳本「發現情境」欄最大 `R\d+` **現查**、不寫死。**明文否決**掃描員兩處建議：①「列內任一 `R\d+`」實測會燒掉 70 列未結列中的 56 列，改為 5 條承接語境樣式後只命中 6 列；②「用 improving_NN 最大號當當前輪」實查該號已達 101 而複審輪僅 66，照做會整本全紅。沙箱四段紅綠實測皆對稱；新增 35 支測試，散文↔程式雙向綁定 6 支 |
| DEF-101-662 | 2026-08-01 | R67 Scan-G（DOCADR 包；`git log -S` 現查六輪異動 ＋ 完成判準物證回查） | **ADR-XPLAT-002 §8 交棒表 item 7／8（`:866`、`:867`）承接者寫成開放下界「R62+」**，自 R60 寫下後六輪零異動，且「需 PM signoff」無任何指名到檔的容器＝硬規則③ 第一點禁止的死信形態；其完成判準物證 `tools/.loc_baseline` 現查不存在（rc=1） | P3（原 P2，四方複審降級） | 根層 `docs/04_planning/ADR/ADR-XPLAT-002-*.md` §8 表頭 ＋ item 7/8/13/14 ＋ §8.1／§8.2 ＋ §9.1 SC-2 | fixed@R67：依硬規則③ 第三點改列「未指派」＋前置持有人，新增 §8.1 signoff 容器與 §8 三條表頭規則（禁 `R<N>+`／signoff 須有具名容器／完成判準禁寫死常數），並新增 item 13／14 補本表自身覆蓋面缺口。公允記載：掃描員「`R<N>+` 結構上不可能到期」為偽——item 12 曾寫 R64+ 並於下一輪交付，該反例已逐字寫進表頭規則 1 |
