# 四方審計撿回稿（R130；b1ef81f 兩項功能驗證完備性）round-label-ok

> 2026-09-06 產出。來源＝背景 Workflow `wf_e22124d0-ce1` 撞 session 上限前完成的 8 個 agent
> （4 份獨立審計 Architect/SA/SD/Developer ＋ 合併 22 筆）。**對抗證偽 66 個 agent 與 design 未跑**
> （撞上限）。合併清單各筆為**單一來源、未經三鏡證偽**，除下列已由主控親驗者外，餘為待驗假設。
> 原始四份 JSON：`/private/tmp/claude-501/-Users-wuweihong-Antigravity-AISDCL-Agent/812a9c3b-75a2-4b8a-a5da-1c0ddb38baf8/scratchpad/salvage/`。

## 主控親驗（2026-09-06，rc/檔實讀）

- **M-01 CONFIRMED（P0）**：喚醒 tick（launchd 跑 `--sentinel-tick`）plist EnvironmentVariables **只有 PATH**（`plutil -p` 實 dump）。`probe_quota()` 的無人零付費分支 `session_resume_planner.py:489` 與 `relay_machine.no_progress_limit():117` 皆 gate 在 `os.environ.get(AUTOSDD_UNATTENDED)`；全 repo 僅 `session_resume_planner.py:1157`（spawn 子 claude）設此變數，tick 行程自身從不設 ⇒ 免費端點 `endpoint_probe_verdict()` 答不出時，真喚醒路徑會 fall-through 到付費 `claude -p` 探針（註解自陳 ≈31,847 tokens），且 INV4 夾 1 不生效。**INV1／INV4 在生產喚醒路徑上是死碼。**
- **本機排程面（唯讀現查）**：無 T-*／GHOST 殘留；合法哨兵兩支（`b80b257b` 前一 session＋本 session `812a9c3b`）。基線全套 `Ran 3990 tests in 711.410s`、`OK (skipped=45)`、rc=0；跑完無新種 T-*（機器實跡，非閘門）。

## 合併發現 22 筆（未證偽；id｜宣稱｜嚴重｜型別｜提出方）

| id | claim | sev | gap_type | raised_by | title |
|----|-------|-----|----------|-----------|-------|
| M-01 | 1 | P0 | design_gap／false_or_overstated_claim | Architect（A-01、A-02）,SD（SD-01）,Developer（D-01） | INV1／INV4 的無人判準鍵在 AUTOSDD_UNATTENDED，但排程器叫起的 tick 行程（launchd／schtasks）結構上沒有這個變數 ⇒ 生產路徑上「零付費探針」是死碼、INV4 夾 1 不生效且 .env／使用者環境可放寬 |
| M-02 | 2 | P1 | false_or_overstated_claim／false_green_risk | Architect（A-04）,SA（SA-06）,SD（SD-02）,Developer（D-02） | 「真的種排程＝0 次」為過度宣稱：GHOST 測試每次全套仍以子行程真 `launchctl bootstrap gui/<uid>`（Windows 真 Register-ScheduledTask），只靠 addCleanup 拆；`_isolated_env` 的 AUTOSDD_SENTINEL_OFF=1 被 arm 路徑無視；ADR §7 Q2＝A（opt-in）未落地 |
| M-03 | 2 | P1 | test_not_in_gate | Architect（A-05）,SA（SA-07）,SD（SD-03）,Developer（D-03） | 「全套 3990 測試絆線實證 REAL_PLIST_WRITE=0」是一次性、repo 外的絆線包裝，不在任何閘門；ADR §3.6 leak_fence 未接、三支 CI 與 pre-push 裸呼叫 runner 且無任何閘門對整個 runner 行程設 AUTOSDD_SENTINEL_OFF ⇒ repo 內對「真排程器被寫」零機械偵測，未來忘了注入的測試會靜默真種 |
| M-04 | 2 | P1 | design_gap | Architect（A-06 結構半）,SA（SA-06 證據面）,SD（SD-02／SD-03）,Developer（D-02） | 洩漏修法是點修不是結構修：第一 arm 接縫（register_endurance→select().arm）零護欄、`select()` 無沙箱縫（ADR §3.2 SandboxBackend 未落地）；直呼 `_arm_sentinel`／`settle_window`／`_rearm_after_stop`／`register_endurance`／`_arm_endurance` 的站點全靠「記得注入」 |
| M-05 | 1 | P1 | missing_test | SA（SA-03）,SD（SD-04） | INV2／INV3 的生產接線（`_run_resume` → `choose_resume_route(followup_ok=followup_allowed(state))` → `resume_argv(allow_followup=…)`）零測試：把 followup_ok 突變成恆 True 或恆 False 全綠 |
| M-06 | 1 | P1 | design_gap | Architect（A-03） | INV2「續跑不得以 fan-out 為第一動作」只靠 prompt 不注入 Workflow 提示，無任何機械阻擋：無人姿態檔不 deny Task／Agent／Workflow，PreToolUse 只在 context ≥90% 才擋 |
| M-07 | 1 | P1 | false_green_risk | SD（SD-05） | INV5 wiring 測試的假後端忽略 `prefix` 參數 ⇒ `_arm_sentinel` 列舉前綴突變成 `AutoSDD_Sentinel_`（看不到 SessionResume 族）仍全綠；且 `_JOB_PREFIXES` 與 planner 命名函式是兩份字面、無耦合測試 |
| M-08 | 1 | P1 | false_or_overstated_claim | SA（SA-01）,Developer（D-06） | 「2 輪對抗複審全修」「全套根層閘門綠」在 repo 內無任何可稽核紀錄；唯一綠證住前一 session scratchpad，且絆線那一輪實為 FAILED (failures=8, errors=1) |
| M-09 | 2 | P2 | missing_test／design_gap | Architect（A-06 AST 射程半）,SA（SA-08）,SD（SD-07） | SchedulerHygieneTest 射程有洞：只掃自己一檔（`Path(__file__)`）、只認 attribute-call 三個 tick 名；`_heal_armed_drift` 直呼不在 hygiene_ticks；`_sentinel_off_lifted()` 解 pin 的函式未被要求注入 `sb.select`；直呼 arm 進入點與 test_mac_endurance_r83.py 九處 `LaunchdBackend().arm` 皆無 AST 鎖 |
| M-10 | 2 | P2 | false_green_risk | SD（SD-08） | 假後端保真度無鎖：`_StatefulFakeSchedulerBackend`／各 `_Fake`／`_StubBackend` 與真 `_BACKENDS` 契約面無對稱測試，行為差異（arm 不檢查任務書存在、disarm 恆 0、list_jobs 永不 None）只靠各測試自律 |
| M-11 | 2 | P2 | ci_gap | Architect（A-07 獨立立案）,SA（SA-07 提 mac 孿生）,SD（SD-09 證據面「mac 零現查」）,Developer（D-03 提 Linux NoCarrier） | mac 側零真機事後現查（回歸鎖 win32-only）、Linux runner 結構上不可能偵測任何真 job、`gc()` 母體仍為前綴 ⇒ 若再種下 T-*／GHOST 殘留，三支 CI 與 SessionStart 回收都看不見；ADR §3.3 owned_jobs／§3.4 orphan_verdict 未落地 | <!-- stale-premise-ok: 四方審計原文逐字引述（平台覆蓋缺口為 R130 當輪四方判讀，非永久常數）-->
| M-12 | 2 | P2 | design_gap | SD（SD-10）,Developer（D-03 B 面） | 真 `$TMPDIR` 根層的測試殘骸每次全套持續累積（資源回收 B 面未落地、無鎖）：直呼 `_resume_tick` 的測試未 patch `tempfile.gettempdir`，`endurance_log_path(plan)` 先寫 `woken` 到真暫存根 |
| M-13 | 1 | P2 | design_gap | Architect（A-08） | INV5 單一擁有者只在 `_arm_sentinel` 一站強制；`_arm_endurance`／`--register-schtasks` 建 `AutoSDD_SessionResume_<sid>` 時不查 owner ⇒ 同 session 雙 job 的事故形態仍可從手動路徑重現 |
| M-14 | 1 | P2 | design_gap | SD（SD-06） | INV5 defer 回 rc=0：PATROL_HANDBACK 路上若 -Once 續跑排程拆除失敗（仍在清單）⇒ `_arm_sentinel` 把那支「已響過、不會再響」的 -Once 當成另一個 owner 而不武裝哨兵，喚醒鏈斷線卻 rc=0、無 loud |
| M-15 | 1 | P2 | missing_test | SA（SA-04） | INV4 的 env→`settle_window`→`no_progress_limit()` 組合（無人第一次無進度即停）無端到端測試；現有測試直接傳 `no_progress_limit=1` 給 `resolve()` |
| M-16 | 1 | P2 | false_or_overstated_claim | SA（SA-02）,Developer（D-06 部分） | 「可見性實地驗過」只到 in-process `main()` 接線，無真實 CLI 行程輸出證據；本機從未產生過 `autosdd_unattended_outcome.jsonl` |
| M-17 | both | P2 | doc_ledger_mismatch | Architect（A-09）,SA（SA-09）,SD（SD-09）,Developer（D-05） | ADR-XPLAT-015 狀態文字、缺陷帳本與落地實況三方不一致：ADR 仍稱「未落地任何生產碼」而 §4-1/§4-2/§4-3(a)(b) 已落地、§5 步 3~7 未落地無標記、候選測試名未回填（`HealArmedDriftTest` 幽靈名已流入任務書）、§6 LOC 值 stale；帳本 DEF-200-266~271 六列仍寫「工作樹待 commit」、DEF-200-270／271 標 fixed 與 M-01／M-02／M-11 矛盾、無 open 列承接 §5 步 3~7 |
| M-18 | both | P2 | design_gap | Developer（D-04）,Architect（A-03／A-05／A-08 提 headroom 0）,SD（SD-03 提 runner 餘裕） | LOC 餘裕已被本 commit 吃光：quota_escalation.py 400/400（ADR 寫 377/400 已 stale）、schedule_backend.py 372/400 不足以容 SandboxBackend、run_root_unittests.py 756/759、context_budget_guard.py 1089/1089；§3.2＋§3.6 及 M-01／M-06 落地前必先搬碼，並行包會在同一檔互撞 |
| M-19 | 1 | P3 | false_or_overstated_claim | SA（SA-05） | 「雙平台」實為雙後端 in-process 注入；Windows 真機排程行為（`Get-ScheduledTask` 解析、pythonw 編碼）程式碼自陳「量不到、待 Windows 實測」，INV5 真消費 `list_jobs` 卻無 `[WINDOWS-NATIVE-ONLY]` 真機測試 |
| M-20 | both | P3 | design_gap | SA（SA-10） | INV1~5／FIX 鎖的存在性無註冊表釘住；MIN_TESTS 餘裕 95 大於新增測試數，整批刪除 INV 類不會觸發任何閘門 |
| M-21 | 2 | P3 | doc_ledger_mismatch | SA（SA-11） | R129 Scan Findings 稱 ONBOARDING 指紋基線已由 `--write` 回填，但 commit b1ef81f 未含 ONBOARDING.md（敘述與 commit 內容不一致，原因未歸因） |
| M-22 | 2 | P3 | missing_test／design_gap | Architect（A-10）,SD（SD-11） | Windows 側 `_exact_listing(task)` 以 `-like '<task>*'` 精確查名：label 含 `[`／`?`／`*` 時假陰性 ⇒ 每 tick 誤判漂移重掛（§4-1 在 Windows 的邊界未鎖）；`_unsafe_name` 未拒絕這些字元（ADR §3.8 已登記修法但未落地） |

## 四方一致認可、已成立的面（verified_ok）

- INV1～INV5 的純判準各自只有一個家＋一個生產接線站點，且純函式層具紅綠鑑別力（前提：in-process、無人旗標已在 env 中——生產 tick 是否帶旗標見 M-01）
- INV/FIX 九個測試類別＋ArmedDriftSelfHealTest＋DisarmClearsTheArmedStampTest＋SchedulerBackendNeverTouchesRealSchtasksTest 在本機 `AUTOSDD_SENTINEL_OFF=1` 隔離執行全綠；任務書給的 `HealArmedDriftTest` 是幽靈名
- 新測試被 run_root_unittests discovery 收到（3990／MIN_TESTS 3895），三支 CI ＋ pre-push root-infra leg 都裸呼叫該 runner，INV/FIX 測試無平台 skip、在 ubuntu／macos-latest／windows-latest 三 runner 皆真的執行
- T-f4b 止血 §4-1（精確查名）／§4-2（第二接縫尊重 SENTINEL_OFF）／§5 步 1~2 已落地且有雙向紅綠自證；原 :4071 站點（今 :5019）已注入假後端；SchedulerHygieneTest 對兩支 tick 的判準不對稱且有紅綠自證
- FIX2（PureWindowsPath）跨平台可證偽；FIX3 可見性有函式級＋兩個 main() 出口接線鎖，no_progress 停止真的落結局檔、QUOTA_STOP 不落，真 CLI `--check`／`--pace` rc=0；FIX4 端到端釘第一窗
- 帳本 DEF-200-266／268／269 點名的回歸鎖名稱存在；DEF-200-269 併修（拆哨兵漏斗清 armed stamp）有紅綠鎖
- 本機排程面現況（唯讀現查）無 T-*／GHOST 殘留；合法哨兵為**兩支**（訂正主控「只有一支」）；T-f4b bootout log 在 commit 後兩次全套未再長大（機器實跡，非閘門判準）
- test_mac_endurance_r83.py 九處直呼 `LaunchdBackend().arm(`／`backend.arm(`／`register_endurance(` 皆已隔離真載具（駁主控「是否隔離未查」——已查，皆隔離；但無鎖要求，見 M-09）
- 主控三個量測值可重現，LOC／護欄線現值已取到
- ruff 對五支改動檔全綠
- §3.6 leak_fence 若接進 runner，其 import 鏈為 stdlib＋repo 內模組，零相依探針不會被絆

## 詳細 evidence／proposed_verification／files_to_touch（逐筆）

### M-01 [P0] INV1／INV4 的無人判準鍵在 AUTOSDD_UNATTENDED，但排程器叫起的 tick 行程（launchd／schtasks）結構上沒有這個變數 ⇒ 生產路徑上「零付費探針」是死碼、INV4 夾 1 不生效且 .env／使用者環境可放寬
- **evidence**：〔實讀〕INV1 唯一 gate＝tools/session_resume_planner.py:489 `if os.environ.get(UNATTENDED_ENV):`（probe_quota 唯一付費 spawn 站點 :496 `[claude, "-p", "ok", ...]`；grep 全 tools/lib、hook 無第二個 `claude -p` spawn 站點〔Architect 實測〕）；呼叫點 :1215 `verdict = probe_quota(args.probe_command)` 在 `_resume_tick`＝tick 行程本身。INV4 唯一 gate＝tools/lib/relay_machine.py:114-117 `if os.environ.get(unattended_authz.UNATTENDED_ENV): return 1 / return _int_env(RELAY_NO_PROGRESS_LIMIT_ENV, ...)`，唯一呼叫 :413 在 settle_window（由 `_resume_tick` :1289 呼叫，同在 tick 行程）。〔實讀〕該變數唯一注入站＝planner:1157 `env={**os.environ, UNATTENDED_ENV: "1"}`——注入的是被 spawn 的子 `claude` 行程，不是 tick 自己；tools/lib/unattended_authz.py:34-37 自述「由 `_run_resume()` 在 spawn 那一刻注入子行程環境…正常互動 session 一律沒有這個變數」。〔Developer 實測〕`grep -n 'os.environ\[\|environ.setdefault\|environ.update' tools/session_resume_planner.py tools/lib/quota_escalation.py tools/lib/relay_machine.py tools/lib/resume_route.py` 零命中。〔實讀〕載具環境：tools/lib/schedule_backend.py:637 `"EnvironmentVariables": {"PATH": os.environ.get("PATH", "")}`（launchd 只帶 PATH）；schtasks 側 planner:664-667／706-707 `runner_action_argument` 只帶 `<planner> <tick> --plan --task-name`，無 `$env:`；`main()` :1447 `quota_gate.apply_env_defaults(os.environ)` 的 ENV_SPEC（tools/lib/quota_policy_env.py）無 UNATTENDED（只命中 :133 SENTINEL_OFF〔實測〕），但 :130 含 `AUTOSDD_RELAY_NO_PROGRESS_LIMIT` ⇒ repo `.env` 的 RELAY 上限會被填進 tick 行程、`.env` 寫 5 就續燒 5 窗。〔實測〕本機兩支活哨兵 plist `plutil -p` ⇒ `"EnvironmentVariables" => {"PATH" => …}` 只有 PATH；`launchctl print gui/501/AutoSDD_Sentinel_b80b257b-…` ⇒ `environment = { OSLogRateLimit => 64, PATH => …, XPC_SERVICE_NAME => AutoSDD_Sentinel_b80b257b-… }`，無 AUTOSDD_UNATTENDED〔SD〕。〔實測〕`env -u AUTOSDD_UNATTENDED` 下 import planner 並呼叫 `apply_env_defaults` ⇒ `before: None／after: None`〔Architect〕。〔實測〕決定性重演：以與 launchd 相同環境（無 UNATTENDED、SENTINEL_OFF=1）in-process 跑 `planner._resume_tick(args)`，`endpoint_probe_verdict` patch 成 None、`sb.select` 假後端、`subprocess.run` spy ⇒ `env AUTOSDD_UNATTENDED in tick process: None／resume_tick rc = 0／paid \`claude -p ok\` spawns = 1 [['claude', '-p', 'ok', '--model', 'haiku', '--output-format', 'json']]`〔Architect〕；`env -i PATH=… HOME=… AUTOSDD_SENTINEL_OFF=1` 模擬 tick 環境 ⇒ 「paid probe spawned: 1 ['claude', '-p', 'ok', '--model'] source= claude-p/error-envelope／no_progress_limit() in tick-like env: 1／no_progress_limit() with env=5 (no UNATTENDED): 5」〔Developer〕；同型 `planner.probe_quota('claude')` ⇒ `paid spawn argv: ['claude', '-p', 'ok', '--model'] | source: claude-p/text`〔SD〕。〔Developer 實測〕真 tick 痕跡 `$TMPDIR/autosdd_resume_log_autosdd_resume_plan_b80b257b-….jsonl` 的 probed 來源計數：21 筆 "claude-p/error-envelope"、1 筆 "claude-p/envelope"、2 筆 "endpoint"（2026-09-05 22:39~23:09 由 launchd job 發出的付費探針）。〔實讀〕現有測試 Inv1UnattendedZeroPaidProbeTest :3729／:3745 與 Inv4UnattendedStopsOnFirstNoProgressTest :3908-3913 皆以 `patch.dict(os.environ, {planner.UNATTENDED_ENV: "1"})` 手餵旗標；Inv4 行為測 `test_a_single_no_progress_window_stops...` 直接傳 `no_progress_limit=1` 未經 `no_progress_limit()`——對「旗標在 tick 行程裡到底有沒有」結構性失明。
- **why**：commit 宣稱「INV1 無人（AUTOSDD_UNATTENDED）等額度＝零付費 claude -p 探針」與 DEF-200-270 fixed 的核心就是這一條；事故形態（哨兵 tick 撞線後每次 probe 都花 ≈31,847 tokens，planner.py:486 自述）在真機一個位元都沒改變——測試綠是因為測試自己把旗標塞進去。只要免費端點快取 stale 或某軸 ≥100%，每 900s 一次的 tick 就付費 spawn。INV4「不得放寬」在 production 只是散文：mac 靠出廠預設 1 撐住（launchd 環境帶不進覆寫），Windows 使用者層級環境變數會被 schtasks 繼承、`.env` 兩平台皆會被 apply_env_defaults 吃進去 ⇒ 回到連續燒窗的事故形態。使用者第一項功能「主 agent 沒起來就不該浪費 token」在最貴的那一格未落地，屬「有鎖在守假話」的典型假綠。
- **proposed_verification**：修法（單一咽喉點，兩案擇一或並用）：(A) 在 planner.main() 的 tick 分派（:1455-1458 `if args.sentinel_tick / if args.resume_tick`）之前，或 `_sentinel_tick`／`_resume_tick` 入口（:1189／:1354）`os.environ.setdefault(UNATTENDED_ENV, "1")`（或於 unattended_authz 新增 `mark_tick_unattended()` 供兩支 tick 入口呼叫，胖身體下 lib）；(B) 載具側寫入：plist `EnvironmentVariables` 與 schtasks Action／`endurance_schtasks_script` 各加該鍵（雙平台對稱、可對 job 定義本體斷言）。🔴 planner LOC 餘裕兩包不一致（Architect 稱「餘裕 0」、Developer 實測 717/750 ABSOLUTE_LIMIT=750），落地前以 `check_loc_budget.py --json` 現查。測試（先紅後綠）新類 `TickProcessIsUnattendedByConstructionTest`／`ScheduledTickIsAnUnattendedRoundTest`：(a) `test_a_scheduler_woken_resume_tick_never_spawns_a_paid_probe`＝把上述重演寫成測試（沿 RelaySettleWindowTest._run 骨架、`os.environ` 顯式 pop UNATTENDED、**不** patch `probe_quota`、`endpoint_probe_verdict→None`、`planner.subprocess.run` spy、假後端），斷言 spy 內無 `["claude","-p","ok",...]` 且 `probed` 事件 `source == "endpoint/unattended-no-paid-probe"`（今日必紅）；(b) `test_the_sentinel_tick_entry_marks_the_process_unattended`（走 `planner.main(["--sentinel-tick", ...])` 斷言 `os.environ[UNATTENDED_ENV]=="1"`）＋控制組 `test_probe_quota_cli_stays_attended`（`--probe-quota` 互動入口仍 spawn 一次，保住既有 `test_attended_mode_still_falls_through_to_the_paid_probe` 鑑別力）；(c) INV4：`test_a_dot_env_relay_limit_cannot_widen_the_tick_process`／`test_a_scheduled_tick_ignores_the_env_relaxation`——`os.environ` pop UNATTENDED、設 `AUTOSDD_RELAY_NO_PROGRESS_LIMIT=5`，經 `planner.main(["--resume-tick", ...])`（假後端、`_run_resume` 假成 handback missing／files_changed 0、`_isolate_trace_dir`）⇒ 斷言第一窗即 `relay_stopped why=no_progress`、`_register_and_record` 零呼叫（今日必紅：RELAY_NEXT、seq 推進）；(d) 若採 (B)：`test_the_armed_job_definition_carries_the_unattended_flag`——`LaunchdBackend._write_plist` body `EnvironmentVariables[UNATTENDED_ENV]=='1'`（r83 `_plist()` helper）＋ `endurance_schtasks_script` 含對應 `$env:` 設定，雙後端各一支；若採 (A) 則反向斷言 job 定義不依賴任何 EnvironmentVariables 即成立 INV1。
- **files**：tools/session_resume_planner.py, tools/lib/unattended_authz.py, tools/lib/relay_machine.py, tools/lib/schedule_backend.py, tools/tests/test_context_budget_guard.py, tools/tests/test_mac_endurance_r83.py, docs/06_quality/AutoSDD_Defect_Log.md

### M-02 [P1] 「真的種排程＝0 次」為過度宣稱：GHOST 測試每次全套仍以子行程真 `launchctl bootstrap gui/<uid>`（Windows 真 Register-ScheduledTask），只靠 addCleanup 拆；`_isolated_env` 的 AUTOSDD_SENTINEL_OFF=1 被 arm 路徑無視；ADR §7 Q2＝A（opt-in）未落地
- **evidence**：〔實讀〕tools/tests/test_context_budget_guard.py:2745-2775（class SentinelWiringTest :2543）`test_arming_accepts_a_transcript_that_does_not_exist_yet`：`env = _isolated_env(self.tmp)`（:409-413 寫 `env["AUTOSDD_SENTINEL_OFF"] = "1"`，預設 real_scheduler=False）→ `subprocess.run([*common, "--arm-sentinel", "--task-name", task], env=env, ...)`，`task = "AutoSDD_Sentinel_UNITTEST_GHOST"`，清理只有 `self.addCleanup(planner._schtasks_remove, task)`；註解逐字「這一跑在 Windows 上會**真的**註冊一支排程」（mac 未提）；:2600-2780 區間唯一 skip 是 :2667/2685/2708/2721 四個 WINDOWS-NATIVE-ONLY，GHOST 在 darwin 不 skip。〔實測〕`grep -n SENTINEL_OFF tools/lib/schedule_backend.py` rc=1；生產碑消費 SENTINEL_OFF 只有 quota_escalation.py:383 一處（planner 僅 :198 註解）⇒ 子行程 `--arm-sentinel` → `_arm_sentinel`（:1298-1326）→ `_register_and_record`（:971）→ `register_endurance`（:765-769）→ `_register_at_expr`（:751-754）→ `select().arm` → LaunchdBackend.arm :409-500 → :479 `_run(["launchctl", "bootstrap", self.domain(), want_path])`，`domain()` :388-392 回真 `gui/{os.getuid()}`；schedule_backend.py:849-857 `select()` 三分支無沙箱縫。plist 落 `LAUNCH_AGENTS_DIR = Path.home() / ...`（:173，子行程 HOME=tmp）⇒ plist 在 tmpdir、job 載入真 gui/501 網域（rmtree 後成殭屍態）；「寫進真 ~/Library/LaunchAgents 才拋」的絆線對它結構性失明，且 bootstrap 發生在子行程、父行程 monkeypatch 看不見。:9313-9325 `test_this_module_never_reaches_the_real_scheduler` 只斷言 `_isolated_env(...)['AUTOSDD_SENTINEL_OFF']=='1'`，不斷言 arm 路徑尊重旗標——前提為假；`setUpModule` docstring「整個測試模組一律不准碰真的排程器」與事實相悖。〔實測〕`git show b1ef81f -- tools/tests/test_context_budget_guard.py | grep GHOST` 零命中（本 commit 未動 GHOST）；`grep -rn MANUAL-REAL-SCHEDULER tools/ .github/` 零命中。〔實測〕真機證據：`$TMPDIR/autosdd_resume_log_autosdd_resume_plan_UNITTEST_GHOST.jsonl` 兩筆 `"event": "gc_reaped"`（`"at": "2026-09-05T15:25:49+08:00"`、`"at": "2026-09-06T02:09:39+08:00"`，`"why": "逐字稿不存在", "unregister_rc": 0`）——`gc_reaped` 由 sentinel_lifecycle.py:364-406 `gc()` 依 `launchctl list` 真列舉到該 label 後 `_remove_task` 寫下 ⇒ 02:09（＝commit 訊息所稱「全套絆線 3990 測試」時段）真 launchd 網域裡確實存在 GHOST job，且是被另一個 SessionStart GC 收掉。〔Developer 實測／他包回報〕前一 session 絆線最終全套（scratchpad fullsuite_final.out，02:06~02:14）：「Ran 3990 tests in 719.954s／FAILED (failures=8, errors=1, skipped=45)」，唯一 error＝`ERROR: test_arming_accepts_a_transcript_that_does_not_exist_yet` → `RuntimeError: TRIPWIRE launchctl bootout gui/501/AutoSDD_Sentinel_UNITTEST_GHOST`（tripwire_hits.log:1285「02:06:42 REAL_LAUNCHCTL_WRITE launchctl bootout …GHOST」）；絆線程式 fullsuite_tripwire.py 檔頭自述「planner/lifecycle 子行程 spawn 只記錄不阻擋」⇒ 子行程 plist 寫入／bootstrap 結構上不在 REAL_PLIST_WRITE 統計內；事實底稿 §6 第 2 點自己記到 `REAL_LAUNCHCTL_WRITE launchctl bootout gui/501/AutoSDD_Sentinel_UNITTEST_GHOST`。〔實讀〕ADR §4-3(c)「GHOST 在沙箱下從真機副作用變成沙箱路徑…mac／Win 皆不再有測試真跑 --arm-sentinel」——沙箱（§3.2）未落地，此句目前為假；ADR §1.3 第三列與 §7 Q2 裁 A 明列此為洩漏源。
- **why**：commit 訊息「全套絆線 3990 測試實證 REAL_PLIST_WRITE=0」只證明「真 ~/Library/LaunchAgents 沒被寫」，不等於「真排程器沒被碰」：每次 pre-push／CI（macos-latest、windows-latest）／開發機全套都真的 bootstrap 一支 job 進使用者的 launchd 網域（Windows 真註冊 schtasks），只靠 addCleanup 正常結束才拆；測試行程被 kill／timeout 於 cleanup 前 ⇒ 記憶體中的 job 存活到登出（tmpdir 已刪 ⇒ 下一 tick 自我解除，有界但真實）——正是 T-r95 形狀、使用者第二項功能要根治的形態。「T-f4b 洩漏根治」對「測試不再真種排程」的整體承諾未達，只達 T-f4b 這一站。
- **proposed_verification**：實作 Q2＝A：GHOST 預設 skip（標 `[MANUAL-REAL-SCHEDULER]`，須顯式清空 `AUTOSDD_SENTINEL_OFF` 並帶具名旗標才真跑；cleanup 改 `try/finally` 於同一 env 下 subprocess `--remove-schtasks`；測試前置 `launchctl print` 快照），並把「武裝入口收得下不存在的逐字稿」這半格改成 in-process 假後端測試（`planner.main([..."--arm-sentinel"...])` 配 `patch.object(sb, "select", 假後端)`，斷言 `任務書骨架寫出 ∧ stderr 無「找不到逐字稿」 ∧ 假後端 arm_calls==1`）。回歸鎖 `SentinelWiringTest.test_the_default_suite_never_bootstraps_into_the_real_launchd_domain`：AST／靜態掃本檔所有 `subprocess.run` argv 含 `--arm-sentinel` 的站點，必須位於帶 MANUAL 標籤的測試內。結構解（依賴 M-04 §3.2 沙箱）：`SandboxBackendTest.test_a_subprocess_arm_sentinel_under_sentinel_off_never_reaches_the_real_carrier`——以 `_isolated_env(tmp)` 子行程重跑 GHOST 形態，斷言子行程 stdout／plan log 的 sentinel_armed 憑證逐字含「未武裝」、mac `launchctl print gui/<uid>/AutoSDD_Sentinel_UNITTEST_GHOST` rc=113 全程不變（`[MAC-NATIVE-ONLY]`）／Windows `Get-ScheduledTask` 查無；改寫 :9313 那支為「`_isolated_env` 旗標 ∧ `select()` 對該旗標真的換後端」兩段合取；r83 `_BACKENDS`（test_mac_endurance_r83.py:340）加入 SandboxBackend 過 BackendInterfaceIsSymmetricTest；同輪改寫 :70 setUpModule docstring 為「不准寫、唯讀列舉可」（ADR §6 要求）。
- **files**：tools/tests/test_context_budget_guard.py, tools/lib/schedule_backend.py, tools/lib/windows_skip_tags.py, tools/tests/test_mac_endurance_r83.py, docs/04_planning/ADR/ADR-XPLAT-015-resource-reclamation-protocol.md, docs/06_quality/AutoSDD_Defect_Log.md

### M-03 [P1] 「全套 3990 測試絆線實證 REAL_PLIST_WRITE=0」是一次性、repo 外的絆線包裝，不在任何閘門；ADR §3.6 leak_fence 未接、三支 CI 與 pre-push 裸呼叫 runner 且無任何閘門對整個 runner 行程設 AUTOSDD_SENTINEL_OFF ⇒ repo 內對「真排程器被寫」零機械偵測，未來忘了注入的測試會靜默真種
- **evidence**：〔實測〕`grep -rn 'REAL_PLIST_WRITE\|REAL_LAUNCHCTL_WRITE\|tripwire\|絆線\|SandboxBackend\|owned_jobs\|orphan_verdict\|leak_fence' tools/ .claude/ AutoClaude/tools --include='*.py'` 程式碼零實作，唯一命中＝tools/tests/test_adr_xplat001_c1c2_lock.py:1588-1589 護欄線重釘授權**敘述文字**（「全套絆線 3990 測試 REAL_PLIST_WRITE=0 實證止血」）與無關的 windows_smoke_local.ps1:324 smoke tripwire；絆線本體只存在於前一 session scratchpad `/private/tmp/claude-501/…/b80b257b-…/scratchpad/fullsuite_tripwire.py`（會隨 session 消失），且其檔頭自述「子行程 spawn 只記錄不阻擋」⇒ 對 GHOST 子行程 arm 結構性失明（見 M-02）。〔實讀〕tools/run_root_unittests.py:720 逐字 `return run_with_floor(_TESTS_DIR, MIN_TESTS)`——ADR §3.6 指定接線點未包圍籠。〔實測〕`grep -n 'AUTOSDD_SENTINEL_OFF\|SENTINEL_OFF' .github/workflows/*.yml tools/git-hooks/pre-push tools/run_root_unittests.py` rc=1；`grep -n 'launchctl\|Get-ScheduledTask' tools/run_root_unittests.py tools/tests/test_run_root_unittests.py tools/git-hooks/pre-push` 零命中（workflows 只在註解命中）⇒ 三支 CI（root-infra-ci.yml:476、macos-compat-ci.yml:895、windows-compat-ci.yml:1097）與 pre-push:341 皆裸呼叫 runner；`setUpModule` 的 pin（:69-82）只管 test_context_budget_guard 模組期間，:102 tearDownModule 即 pop。〔實讀〕今日守住 T-f4b 的機械物只有：(a) setUpModule pin（射程＝單一測試模組 in-process）、(b) `SchedulerHygieneTest`（:243，射程＝`Path(__file__)` 自己一檔的 AST）、(c) `_heal_armed_drift` 尊重 pin（quota_escalation.py:383）——第一接縫（`register_endurance`→`select().arm`）連 (c) 都沒有；mac 事後現查鎖 :6740-6742 仍 `if sys.platform != "win32": self.skipTest(...)`。〔實測〕殘骸痕跡 `~/.autosdd/traces/autosdd_sentinel_bootout_T-f4b.log`：Architect 量到共 18 行、mtime `Sep 6 01:48:53 2026`，`parent-gone` 時戳 8 筆＝03:41Z／05:25Z／06:23Z／06:45Z／11:50Z／16:38Z／17:27Z／17:48Z（後四筆晚於事實底稿列的 5 筆）；SA／SD 量到 9 筆 bootout、末筆 `parent-gone waited=1s at 2026-09-05T17:48:53Z / bootout rc=0`（兩種計數口徑不同：行數 vs 筆數，皆指向同一末筆時間）；`autosdd_sentinel_launchd_T-f4b.log` 末段逐字 `❌ 哨兵拒絕動作：任務書不存在（已桌面告警）／已自我解除排程 T-f4b（rc=0）`＝殘骸活到 tmpdir 消失才自拆。SD 另量到 commit 後兩次全套（`tools/.last_failure_b1ef81f_20260906T014718Z.log`、`..._20260906T024627Z.log`）該 log 零新增——但這是機器實跡，不是閘門判準。
- **why**：使用者要求「每次程式更新都要通過這些測試與 CI/CD」；此宣稱的機制不可重現、下次回歸無人會響。3990 這個數字與 ADR「量不到≠量到零」紀律相悖：一次性讀值被寫成止血證據。今日 T-f4b 不再被種是靠三支測試合規＋一次性人工絆線；下一支忘了注入的測試（或其他檔）會回到「每次 push 真種一次、15 分鐘後自拆、事後 `launchctl list` 看不到」的靜默狀態，且 CI（ubuntu NoCarrier）永遠看不到。DEF-200-239 的 mac 孿生正是因為缺這一層而在修過一次後復發。
- **proposed_verification**：落地 ADR §3.6（ledger 面優先、A 面其次、B 面只列）：① tools/lib/sentinel_lifecycle.py 新增 `leak_fence(run)`（`os.environ.setdefault("AUTOSDD_SENTINEL_OFF","1")`；A 面以 `schedule_backend.select(os.name, sys.platform)` 顯式取真後端做前後差分、`CI` 有設只印不判；B 面 `Path(tempfile.gettempdir()).glob("autosdd_*")` 差分只列不刪；append 一行到 `trace_dir()/autosdd_leak_fence.jsonl`；回 `max(rc, fence_rc)`），餘裕 213/400 足夠；② runner 接線 +2 raw line：:49 之後 `sys.path.insert(0, str(Path(__file__).resolve().parent / "lib"))`＋`import sentinel_lifecycle`，:720 改 `return sentinel_lifecycle.leak_fence(lambda: run_with_floor(_TESTS_DIR, MIN_TESTS))`（756→758/759；不動 run_with_floor，故 test_run_root_unittests.py:216 `test_reporters_are_actually_wired_into_run_with_floor` 等 `inspect.getsource(run_with_floor)` 鎖不受影響；零相依探針 `_ZERO_DEP_PROBE` 只擋 yaml/pydantic/httpx，import 鏈全 stdlib 可過）；③ ledger 面（決定性，依賴 M-04 沙箱）：收尾讀 `SandboxBackend.ledger` 非空 ⇒ rc=1 並列名測試幀；若沙箱暫不落地，退一步做 repo 內絆線：`schedule_backend._write_plist`／`_run` 在 `AUTOSDD_SCHEDULER_TRIPWIRE=1` 下對真 `LAUNCH_AGENTS_DIR` 寫入或 `launchctl bootstrap|bootout` 拋 `RealSchedulerTouched`（訊息帶最深 `tools/tests/` 幀），runner 主入口 `setdefault` 該旗標。SD 的方案評估：①沙箱 ledger 假綠向＝直接實例化 `LaunchdBackend()` 繞過 `select()` 與 `_sentinel_off_lifted()` 期間；②行程級絆線假綠向＝子行程不繼承 patch；③A 面差分非決定性（自我解除窗、並行 session 新武裝假紅）⇒ 建議①＋②並用、③只印。測試：`LeakFenceTest.test_main_is_wrapped_by_the_leak_fence`（AST 讀 runner main 的 return 呼叫鏈）、`test_a_non_empty_sandbox_ledger_names_the_test_and_reds_the_run`、`test_a_dead_job_that_appears_during_the_run_is_reaped_and_reds_the_run`（假後端前後快照差一支且 plan 不存在 ⇒ rc=1）、`test_an_alive_job_that_appears_during_the_run_is_kept_and_not_red`、`test_unmeasurable_enumeration_is_said_out_loud_and_never_reaped`（None ⇒ 印「量不到」rc 不變）、`test_new_temp_files_are_listed_never_deleted`、`test_the_fence_appends_exactly_one_trace_line_per_run`、`test_a_fence_exception_never_turns_a_red_suite_green`。修正帳本／overage 名冊措辭為「一次性人工絆線（未入庫）」。主控立即可做的憑證：背景全套前後 `wc -l ~/.autosdd/traces/autosdd_sentinel_bootout_T-f4b.log` 不變（18 行／9 筆）、`ls ~/.autosdd/traces | grep -i ghost` 空。工時風險：A 面假紅須靠 orphan_verdict 三值判準（M-11），先於 §3.4 落地則只能「印不判」；runner raw 餘裕 1 行，任何順手加註解即破線（見 M-18）。
- **files**：tools/run_root_unittests.py, tools/lib/sentinel_lifecycle.py, tools/lib/schedule_backend.py, tools/tests/test_run_root_unittests.py, tools/tests/test_context_budget_guard.py, tools/tests/test_adr_xplat001_c1c2_lock.py, docs/06_quality/AutoSDD_Defect_Log.md, AutoClaude/tools/check_loc_budget.py

### M-04 [P1] 洩漏修法是點修不是結構修：第一 arm 接縫（register_endurance→select().arm）零護欄、`select()` 無沙箱縫（ADR §3.2 SandboxBackend 未落地）；直呼 `_arm_sentinel`／`settle_window`／`_rearm_after_stop`／`register_endurance`／`_arm_endurance` 的站點全靠「記得注入」
- **evidence**：〔實讀〕兩個接縫：planner:751-754 `_register_at_expr ... return schedule_backend.select().arm(...)`（第一接縫，無任何 SENTINEL_OFF 判準）與 quota_escalation.py:392 `schedule_backend.select().arm(`（第二接縫，:383 有 pin 護欄）。`select()` schedule_backend.py:849-859 只判 `os.name`／`sys.platform`，無 §3.2 第三縫。ADR §1.3 表明結構層＝§3.2 沙箱「在 select() 一次罩住兩個接縫」，§5 步 3 未落地；ADR §4 首句「三個結構事實各對一層，缺一不可」，實際只落地兩層中的一層半（§4-2 只護第二接縫）。〔實測〕`grep -rn 'SandboxBackend\|owned_jobs\|orphan_verdict\|leak_fence' tools/ .claude/` 生產碼零命中。〔實測〕test_context_budget_guard.py 內直呼 arm 進入點站點：`planner._arm_sentinel(` :4006／:4044／:4088、`relay_machine.settle_window(` :4550／:4673／:4739、`relay_machine._rearm_after_stop(` :4581、`planner.register_endurance(` :6854、`planner._arm_endurance(` :2149——目前各自有 patch（例：Inv5 用 `planner.schedule_backend, "select"`＋`_register_and_record` patch），但沒有任何鎖要求它們如此。tools/tests/test_mac_endurance_r83.py 無 setUpModule pin（`grep -c setUpModule` → 0〔實測〕），9 處 `LaunchdBackend().arm(`／`backend.arm(` 靠逐案 `sb._run = _fake_runner(...)`＋`addCleanup(setattr, sb, "_run", ...)`（:749/:927/:1070/:1333/:1604）；:1390 `register_endurance` 站點靠 patch `_register_at_expr`（:1387-1389）——皆為「每支測試記得注入」型防護（ADR §3.0 明列為不可靠）。
- **why**：今日 5 個 relay/settle 類測試若有人改成走真 `_register_and_record`（例如新增一支只 patch `probe_quota` 的 `settle_window` 測試），在 mac/Win 全套會真種、AST 鎖不紅、pin 不擋（第一接縫不讀 pin）——這是 T-f4b 同型再發的現成路徑；M-02 的 GHOST 子行程與 M-03 的閘門偵測都以此為結構解的前提。
- **proposed_verification**：落地 ADR §3.2：`SandboxBackend(real)`——`arm` 只記類別層 `ledger`（含測試幀）、回含「未武裝」字面的憑證；`disarm`／`list_jobs` 透傳或回沙箱視圖；`select()` 兩參數皆 None ∧ `AUTOSDD_SENTINEL_OFF` 有值 ∧ 無 `AUTOSDD_UNATTENDED` ⇒ 回沙箱；顯式參數 ⇒ 真後端。落點依 LOC（M-18）：schedule_backend.py 372/400 不足以容 8 介面（估 35~45 count_loc），建議住 sentinel_lifecycle_arm.py（102/400、無載具原語，不撞 r83:205 `_CARRIER_HOMES`），`select()` 內 lazy import（schedule_backend.py:265 已有同型先例），只在 schedule_backend.py 增 3~4 行判式；加進 `test_mac_endurance_r83._BACKENDS`（:340）過 BackendInterfaceIsSymmetricTest。測試：`SandboxBackendTest.test_a_direct_sentinel_tick_without_injection_never_reaches_a_real_carrier`（重演 :4071 修前形態：只 patch `sentinel_decide`＋`register_endurance`，對 `LaunchdBackend.arm`／`SchtasksBackend.arm` 掛 `_boom`，現況必紅）、`test_explicit_platform_arguments_always_return_the_real_backend`、`test_unattended_ignores_the_sandbox`、`test_the_sandbox_credential_says_it_is_not_armed`。誠實劃界（SD）：假綠向＝直接實例化 `LaunchdBackend()` 繞過 `select()`（r83 合法做法，仍需 `_run` patch）與 `_sentinel_off_lifted()` 期間 select 回真後端——這兩面由 M-09 的 AST 鎖補。
- **files**：tools/lib/schedule_backend.py, tools/lib/schedule_backend_calendar.py, tools/lib/sentinel_lifecycle_arm.py, tools/tests/test_context_budget_guard.py, tools/tests/test_mac_endurance_r83.py, AutoClaude/tools/check_loc_budget.py

### M-05 [P1] INV2／INV3 的生產接線（`_run_resume` → `choose_resume_route(followup_ok=followup_allowed(state))` → `resume_argv(allow_followup=…)`）零測試：把 followup_ok 突變成恆 True 或恆 False 全綠
- **evidence**：〔實讀〕接線點 tools/session_resume_planner.py:1116 `followup = relay_machine.followup_allowed(state)`、:1119-1120 `choose_resume_route(..., followup_ok=followup)`、:1097 `resume_route.resume_argv(…, allow_followup=followup_ok)`。〔實測〕`grep -n 'followup_ok\|allow_followup' tools/tests/*.py` 只命中 test_context_budget_guard.py:3776／:3805／:3812／:6031／:6050——全部直呼 `resume_route.resume_argv(..., allow_followup=True)`；零筆測試對 `planner.choose_resume_route(..., followup_ok=…)` 或 `planner._run_resume` 斷言 argv 有／無 Workflow hint。經 `_run_resume` 捕捉 argv 的兩個類別 `RunResumeConsumesTheRouteTest`（:3342-3413）與 `ResumeSpawnCarriesTheUnattendedSignalTest`（:2820-2870）皆未 seed fanout 清單、未對 prompt 斷言 `Workflow(`／`resumeFromRunId`；`assertNotIn("resumeFromRunId"` 全檔只在 :3802／:6047（純函式層）。突變 A：:1120 改 `followup_ok=True` ⇒ 測試無 fanout 檔 ⇒ hint 空 ⇒ 全綠；突變 B：拿掉 kwarg（預設 False）⇒ INV3 永遠不放行 ⇒ 全綠。Fix4FirstWindowCannotFanOutEndToEndTest 只釘 `_base_state`→`followup_allowed`，不釘 `_run_resume`→`resume_argv`。
- **why**：INV2「續跑不得以 fan-out 為第一動作」的真實違規點就是 `_run_resume` 組 argv 那一站（事故 F3 禍首正是這裡的無條件注入 34-agent Workflow）；純函式綠不蘊含接線綠，INV2 的核心保證在生產接線上無鎖。
- **proposed_verification**：新增 `Inv2Inv3WiringThroughRunResumeTest`：(a) `test_run_resume_first_window_prompt_carries_no_workflow_even_with_a_ready_fanout`——沿 ResumeSpawnCarriesTheUnattendedSignalTest 的 `_fake_run` 分流手法，seed `escalation.fanout_path(sid)` 一筆 `resume_ready=True`，state `relay_seq=0`、不帶 `last_window_made_progress`，patch `planner.subprocess.Popen/run` 捕 argv，跑 `planner._run_resume(args, state, log)`，斷言 prompt 不含 `resumeFromRunId`；(b) `test_run_resume_injects_the_fanout_only_after_a_progressed_prior_window`——state `{last_window_made_progress:True, relay_seq:1}` ⇒ 斷言含 `resumeFromRunId: "wf_r-1"` 且 argv[5]=='--permission-mode'；兩支合起來讓突變 A／B 各紅一支（思想突變：把 `followup_ok=followup` 改常數 True 時 (a) 必紅）。
- **files**：tools/tests/test_context_budget_guard.py

### M-06 [P1] INV2「續跑不得以 fan-out 為第一動作」只靠 prompt 不注入 Workflow 提示，無任何機械阻擋：無人姿態檔不 deny Task／Agent／Workflow，PreToolUse 只在 context ≥90% 才擋
- **evidence**：〔實讀〕INV2 落點＝tools/lib/resume_route.py:136 `hint = workflow_resume_hint(session_id) if allow_followup else ""`（只是少一句話）。`.claude/settings.unattended.json` deny 清單全文＝`Bash(git commit*)`／`Bash(git push*)`／`Bash(git rebase*)`＋三個 PowerShell 同型＋三個 `Edit(...)`——零工具級 deny。PreToolUse 阻斷臂 `.claude/settings.json:154 "matcher": "Task|WebFetch|WebSearch|Agent|Workflow"` 的放行條件寫明「②水位未達 90% 放行」（:153），`.claude/hooks/context_budget_guard.py:314 BLOCKING_TOOLS`，與 UNATTENDED 無關（grep hook 內 UNATTENDED 零命中〔實測〕）。Inv2Inv3WorkflowFanoutGateTest 四支只斷言 argv 字串不含 `Workflow(`。
- **why**：事故本體是 headless 模型第一句就 `Workflow(resumeFromRunId)` 重跑 34-agent。移除提示句只除掉「我們自己的誘因」，模型讀任務書後自行決定 fan-out 仍暢通無阻（acceptEdits 下 Task／Agent／Workflow 全放行）。掌舵者鐵律「主 agent 沒確認成功前零後續」需要一道不靠模型自律的牆。
- **proposed_verification**：落點二選一：(A) 姿態檔分兩份／或 `resume_argv` 在 `allow_followup=False` 時注入額外 `--settings <first-window 姿態>`，其 deny 含 `Task`／`Agent`／`Workflow`（判準純靜態，測試 `UnattendedPermissionPostureTest.test_va4_first_window_posture_denies_fanout_tools` 讀 JSON 斷言三名在 deny；`test_followup_window_posture_allows_them` 控制組）；(B) hook 端：`unattended_authz` 新增 `fanout_blocked(tool_name, env)`＝UNATTENDED 有設 ∧ 無 `AUTOSDD_FOLLOWUP_OK` ⇒ 擋，由 `_run_resume` 在 `followup` 為真時才注入 `AUTOSDD_FOLLOWUP_OK=1`；測試 `PreToolUseBlockTest.test_unattended_first_window_blocks_workflow_regardless_of_watermark`（子行程真跑 hook、payload tool_name=Workflow、水位 10%、env 帶 UNATTENDED ⇒ rc=2）＋控制組帶 FOLLOWUP_OK ⇒ rc=0。context_budget_guard.py 餘裕 0（1089/1089〔Developer 實測〕），(B) 需先搬史料或落在 block_destructive_git.py（已消費 unattended_authz）。
- **files**：.claude/settings.unattended.json, tools/lib/resume_route.py, tools/lib/unattended_authz.py, .claude/hooks/block_destructive_git.py, tools/tests/test_context_budget_guard.py, tools/tests/test_block_destructive_git_r83.py

### M-07 [P1] INV5 wiring 測試的假後端忽略 `prefix` 參數 ⇒ `_arm_sentinel` 列舉前綴突變成 `AutoSDD_Sentinel_`（看不到 SessionResume 族）仍全綠；且 `_JOB_PREFIXES` 與 planner 命名函式是兩份字面、無耦合測試
- **evidence**：〔實讀〕planner.py:1307 `jobs = schedule_backend.select().list_jobs("AutoSDD_")`；測試 :3987-3992 `class _Fake: def list_jobs(self, prefix): return ["AutoSDD_SessionResume_" + sid]`（不看 prefix）、:4067-4072 同形。突變 :1307 前綴改 `"AutoSDD_Sentinel_"` ⇒ 假後端仍回 SessionResume ⇒ :4008 `registered == []` 照綠，但真 LaunchdBackend.list_jobs（schedule_backend.py:589-591 `_labels_with_prefix` startswith）／SchtasksBackend（:361-369 `-like '<prefix>*'`）會過濾掉它 ⇒ 生產上 INV5 對跨族衝突失明＝事故當日雙 job 形態。另 relay_machine.py:243 `_JOB_PREFIXES = ("AutoSDD_Sentinel_", "AutoSDD_SessionResume_")` 為字面，planner.py:178 `DEFAULT_TASK_NAME = "AutoSDD_SessionResume"`／:646 `f"AutoSDD_Sentinel_{session_id}"` 為另一份字面；〔實測〕`grep -rn '_JOB_PREFIXES\|job_session' tools/tests/*.py` 零命中 ⇒ 改 `DEFAULT_TASK_NAME` 不會讓任何測試紅，INV5 靜默失明。
- **why**：INV5 的價值全在「跨族（哨兵×續跑）同 session」這一格，而現有 wiring 測試恰好在這一格沒有鑑別力。
- **proposed_verification**：(1) :3987／:4067 的 `_Fake` 改用 `_StatefulFakeSchedulerBackend().seed("AutoSDD_SessionResume_"+sid)`（其 list_jobs :2222-2223 有前綴過濾）⇒ 前綴突變當場紅；(2) 新增 `Inv5SingleOwnerTest.test_job_session_recognises_both_planner_task_name_families`：`relay_machine.job_session(planner.sentinel_task_name(sid)) == sid` 且 `job_session(planner.resume_task_name(sid)) == sid`，並斷言 `_arm_sentinel` 的列舉前綴是兩族前綴的共同前綴（AST 讀 :1307 字面，或以假後端記錄收到的 prefix 並斷言兩族名皆 `startswith(prefix)`）。
- **files**：tools/tests/test_context_budget_guard.py, tools/lib/relay_machine.py

### M-08 [P1] 「2 輪對抗複審全修」「全套根層閘門綠」在 repo 內無任何可稽核紀錄；唯一綠證住前一 session scratchpad，且絆線那一輪實為 FAILED (failures=8, errors=1)
- **evidence**：〔實測〕`grep -rln 'bypass 鏡\|測試嚴密度鏡\|對抗複審' docs/ tools/tests/` 只命中舊輪檔（improving_107／ADR-XPLAT-006／R83）與 tools/tests/test_adr_xplat001_c1c2_lock.py:1588-1589 的 overage 名冊散文「經 2 輪獨立對抗複審（bypass 鏡＋Windows/測試嚴密度鏡）全數修訖」；`ls docs/06_quality docs/04_planning` 無任何 2026-09-05/06 的 `*_Review*.md`（最近複審檔為 CrossPlatform_R128_*，R129 只有 Scan_Findings 對帳）；commit b1ef81f 19 個檔無複審紀錄檔；兩支測試 docstring 稱「複審必修」（Fix2/Fix4）但複審意見本體、駁回清單、修訖對照皆無落盤。〔實讀／他包回報〕前一 session scratchpad final_gate.log:672-676「Ran 3990 tests in 704.883s／OK (skipped=45)／✅ 整合閘門通過（2 PASS / 1 SKIP）」（03:59，無絆線）；同 scratchpad fullsuite_final.out:851-853「Ran 3990 tests in 719.954s／FAILED (failures=8, errors=1, skipped=45)」，8 支 FAIL 為 TestGuardLayerRatchet×2、TestShrinkOnlyRatchet、TestR71CodeRoundLabelsNeverExceedLedgerCurrentRound、TestDefectIdReferenceIntegrity、TestR67R3…、TestR78GhostSymbolClaims、TestRootToolsLintPolicy.test_e501_debt_only_shrinks（治理鎖，後於 03:59 前修畢），1 error＝GHOST 絆線（見 M-02）。
- **why**：根 CLAUDE.md 鐵律四：宣稱須附當回合證據；記憶「四方審查閉環」要求重大異動有可稽核複審。無紀錄＝無法 zero-trust 重驗複審是否真的涵蓋 INV1~5 全部；overage 名冊以此宣稱為核准理由，理由不可稽核；scratchpad 會隨 session 消失，之後任何人都無法重驗「兩輪複審修了什麼」。使用者依賴這些宣稱判定功能「成熟可用」。
- **proposed_verification**：補 `docs/06_quality/CrossPlatform_R129_Review.md`（兩鏡各自的 blocking/低優清單＋逐條落點 file:line＋修訖對照），並在 `_REPIN_APPROVED_ROUND_OVERAGE["R129"]` 敘述改指向該檔；由 `test_adr_xplat001_c1c2_lock.py` 既有 provenance 判準（或新增 `test_r129_overage_cites_an_existing_review_record`）斷言該檔存在且含「bypass 鏡」「測試嚴密度鏡」兩節。主控以本輪背景全套的實際輸出（逐字貼 `Ran N tests`／`OK|FAILED` 與 rc）取代沿用宣稱；複審結論落 docs/06_quality/ 證據檔（非 scratchpad）。
- **files**：docs/06_quality/CrossPlatform_R129_Review.md, tools/tests/test_adr_xplat001_c1c2_lock.py, docs/06_quality/CrossPlatform_R129_Scan_Findings.md, docs/06_quality/Resource_Leak_Forensics_20260905.md, docs/04_planning/AutoSDD_improving_112.md

### M-09 [P2] SchedulerHygieneTest 射程有洞：只掃自己一檔（`Path(__file__)`）、只認 attribute-call 三個 tick 名；`_heal_armed_drift` 直呼不在 hygiene_ticks；`_sentinel_off_lifted()` 解 pin 的函式未被要求注入 `sb.select`；直呼 arm 進入點與 test_mac_endurance_r83.py 九處 `LaunchdBackend().arm` 皆無 AST 鎖
- **evidence**：〔實讀〕test_context_budget_guard.py:314／:325／:329 `tree = ast.parse(Path(__file__).read_text(encoding="utf-8"))`（只掃本檔）；:307-309 只收 `isinstance(node.func, ast.Attribute) and node.func.attr in hygiene_ticks`，`hygiene_ticks` = `set(tick_funcs) | {"patrol_housekeeping"}`（:296）＝`{_sentinel_tick, _resume_tick, patrol_housekeeping}` ⇒ 裸名 import 呼叫、`getattr` 呼叫、直呼 arm 進入點皆不在射程。:260 `_HEAL_SEAM_TICKS = ("_sentinel_tick", "patrol_housekeeping")` 不含 `_heal_armed_drift`——它本身是第二接縫的直接呼叫端（quota_escalation.py:392 `schedule_backend.select().arm`），:6355／:6375 兩處測試直呼它，今日合規只因各自 patch 了 `sb.select`（:6348／:6372），鎖不判。`_sentinel_off_lifted()`（:183-199）刻意拿掉 pin 讓真漂移可達，:6254／:6291／:6316／:6346／:6387／:6722 六處使用，安全全靠同函式內有假後端；餵一段「`with _sentinel_off_lifted(): escalation._heal_armed_drift(...)`、無任何 patch」的 AST 給 `_violations` ⇒ `checked==0`、`violations==[]`（〔推斷〕依 :311-326 判準）。直呼 arm 進入點站點（本檔）：`planner._arm_sentinel(` :4006／:4044／:4088、`relay_machine.settle_window(` :4550／:4673／:4739、`_rearm_after_stop(` :4581、`planner.register_endurance(` :6854、`planner._arm_endurance(` :2149——各自有 patch 但無鎖要求。〔實讀〕test_mac_endurance_r83.py 九站點（757/984/1086/1347/1372/1390/1415/1416/1615）逐一核對：全部在同函式或 setUp 內以 `sb._run = _fake_runner(...)`／`planner.run_powershell = _fake`／`sb._run = _pm(0, _SLEEPY)` 替身化並 `addCleanup(setattr, sb, "_run", ...)` 還原 ⇒ **現況安全**（訂正主控「是否隔離未查」），但該檔無任何機械物要求「直呼 `LaunchdBackend().arm`／`register_endurance` 的函式必須替身 `_run`」，且 :1390 `planner.register_endurance(...)` 走真 `select()`（靠同函式前面 `planner.run_powershell = _fake`＋`os_name` 注入才安全）。〔實測〕`grep -rn '_sentinel_tick\|_resume_tick\|patrol_housekeeping\|_heal_armed_drift' tools/tests/ AutoClaude/tests/ --include='*.py' | grep -v test_context_budget_guard.py` 只命中 r83 的 docstring／argv fixture ⇒ 今日別檔零站點（無現行洩漏），但射程未鎖。
- **why**：這正是 ADR §6 自陳的假綠向（「只認接縫名單又會漏掉直接 import 後呼叫類別的形態」）；一支新測試在別檔、比照 r83 形態卻漏掉 `sb._run` 替身、或直呼 `_heal_armed_drift`／`settle_window` 而忘注入，會在 mac 真機種 job 且鎖不會響——與 T-f4b 同型。Architect 對合併前的 A-06（含結構半 M-04）評 P1；本列只承接 AST 射程半。
- **proposed_verification**：(a) `_violations` 的掃描面改為 `for p in Path(__file__).parent.glob('test_*.py')`（或以 `Path` 參數化後對 test_mac_endurance_r83.py 也掃一次，接縫加 `_run`／`run_powershell`）；(b) `_HEAL_SEAM_TICKS` 加 `_heal_armed_drift`；(c) 新判準：任一函式體內出現 `_sentinel_off_lifted()` 即要求同體內 `sb_select` 為真；(d) hygiene 集合加 `_arm_sentinel`／`_arm_endurance`／`settle_window`／`_rearm_after_stop`／`register_endurance`（接縫＝`sb.select`｜`_register_and_record`｜`register_endurance`｜`_register_at_expr`）。紅綠自證：`SchedulerHygieneTest.test_lifting_the_pin_without_a_fake_backend_is_red`（餵合成 AST）、`test_a_direct_heal_armed_drift_call_without_select_injection_is_red`、`test_a_settle_window_call_that_patches_no_arm_seam_is_red`、`test_sibling_scheduler_test_files_isolate_the_carrier_primitives`（對 r83 AST 掃 `Call` 屬性 `arm`／`disarm`／`register_endurance`／`_register_and_record` 所在函式，要求同函式或其類 setUp 有 `sb._run =`／`patch.object(sb, "_run")`／`patch.object(sb, "select")`／`run_powershell =` 之一，附假站點紅綠自證）。長期解＝M-04 的 `SandboxBackend`（行程級，不再靠逐檔鎖）。
- **files**：tools/tests/test_context_budget_guard.py, tools/tests/test_mac_endurance_r83.py

### M-10 [P2] 假後端保真度無鎖：`_StatefulFakeSchedulerBackend`／各 `_Fake`／`_StubBackend` 與真 `_BACKENDS` 契約面無對稱測試，行為差異（arm 不檢查任務書存在、disarm 恆 0、list_jobs 永不 None）只靠各測試自律
- **evidence**：〔實讀〕真 arm 對任務書不存在回 `(1, "")`（schedule_backend.py:417-421）、真 disarm 以回讀判 0/1（:579-581）、真 list_jobs rc≠0 回 None（:590-591）；假後端 test_context_budget_guard.py:2210-2223 `arm` 無條件 `return 0, "cred-test-fake"`、`disarm` 恆 0、`list_jobs` 恆 list；:3987-3992／:4023-4031／:4067-4075 三個 `_Fake` 只有 `list_jobs`（＋`credential_line`），無 `arm`／`disarm`／`verify_cli`／`evidence_hint`；`BackendInterfaceIsSymmetricTest`（r83:371-446）分母 `_BACKENDS = (SchtasksBackend, LaunchdBackend, NoCarrierBackend)` 不含任何假後端。若真後端契約面新增方法（例：ADR §3.3 `owned_jobs`）或改簽名，假後端不會紅，消費端在假後端綠、真後端 AttributeError。
- **why**：雙後端「各驗」（`_both_backends` :3696-3704）對 INV1~4 是裝飾性（:3899-3902 自承），真正觸及載具的 INV5／heal-drift 全在假後端上綠——假後端偏離真契約時，整批鎖同時失去鑑別力。
- **proposed_verification**：`FakeSchedulerBackendConformanceTest.test_every_fake_backend_covers_the_shared_contract_face`：對 `_StatefulFakeSchedulerBackend` 與檔內所有 `_Fake*`／`_StubBackend`（AST 收集）斷言 `surface_of(fake) ⊇ symmetry shared surface`（重用 r83 `surface_of`／`symmetry_problems`）；`test_the_stateful_fake_refuses_a_missing_plan_like_the_real_arm`（讀 `plan_path` 不存在 ⇒ `(1,"")`）以維持 M-14／heal 類測試對「任務書已被 rmtree」的鑑別力。
- **files**：tools/tests/test_context_budget_guard.py, tools/tests/test_mac_endurance_r83.py

### M-11 [P2] mac 側零真機事後現查（回歸鎖 win32-only）、Linux runner 結構上不可能偵測任何真 job、`gc()` 母體仍為前綴 ⇒ 若再種下 T-*／GHOST 殘留，三支 CI 與 SessionStart 回收都看不見；ADR §3.3 owned_jobs／§3.4 orphan_verdict 未落地 <!-- stale-premise-ok: 四方審計原文逐字引述（平台覆蓋缺口為 R130 當輪四方判讀，非永久常數）-->
- **evidence**：〔實測〕`test_no_ghost_t_r95_task_survives_a_real_windows_scheduler_query ... skipped '[WINDOWS-NATIVE-ONLY] 僅 Windows 有 schtasks 可現查（DEF-200-239 立案平台）'`（test_context_budget_guard.py:6740-6742）；無 darwin 孿生。〔實讀〕NoCarrierBackend.list_jobs :833-834 `return []`（ubuntu root-infra-ci 上任何真 job 現查恆空，AST／假後端以外的判準在 ubuntu 結構性不執行）；macos-compat-ci.yml:895 雖在 macos-latest 跑 runner，但 A 面差分（ADR §3.6 步 5）未落地。`sentinel_lifecycle.gc()` :364 母體＝`sentinel_task_names()`（:148，前綴 `TASK_PREFIX = "AutoSDD_Sentinel_"` :67）⇒ `T-f4b`／`T-r95`／`UNITTEST_GHOST`（雖為前綴族但其 plan 在 tmp）之外的殘留永不進回收；ADR §3.3/§3.4 owned_jobs/orphan_verdict 未落地（grep 零命中〔實測〕）。DEF-200-271 缺陷欄含「回歸鎖 win32-only、mac 零現查」，處置欄四項無一是 mac 真機現查〔SD 實讀〕。
- **why**：使用者原話要「沒有正常關閉的資源，可以在程序執行完後被正常關閉回收」。現況：mac（本專案主要開發機）全套後無任何機械現查，殘留只能靠人跑 `launchctl list`；DEF-200-239 fixed 的回歸鎖對 mac 是空集。M-03 leak_fence 的 A 面若無 orphan_verdict 三值判準，只能「印不判」。
- **proposed_verification**：(1) darwin 孿生 `SchedulerBackendNeverTouchesRealSchtasksTest.test_no_ghost_task_survives_a_real_launchd_query`／`test_no_ghost_t_f4b_job_survives_a_real_launchd_query`（`[MAC-NATIVE-ONLY]`，非 darwin skip；跑一次不注入 scheduler 的 `_tick()` 後 `launchctl print gui/<uid>/T-r95` rc 必為 `LAUNCHCTL_ENOENT_RC`(113)，開跑前清場同 Windows 版）；(2) ADR §3.3 `owned_jobs()`（argv 含 `session_resume_planner.py` 即 ours）＋§3.4 `orphan_verdict` 9 格具名測試（`OrphanVerdictTest.test_unknown_owner_is_never_reaped` 等），gc 對 fixture 族先 dry-run（§7 Q1＝A）；(3) macos-compat-ci 在 runner 之後加唯讀步驟 `launchctl list | grep -E '^\S+\s+\S+\s+(T[-_]|AutoSDD_Sentinel_UNITTEST)'` 非空即紅（憑證＝排程器回讀值）。
- **files**：tools/tests/test_context_budget_guard.py, tools/lib/sentinel_lifecycle.py, tools/lib/schedule_backend.py, tools/lib/schedule_backend_calendar.py, .github/workflows/macos-compat-ci.yml

### M-12 [P2] 真 `$TMPDIR` 根層的測試殘骸每次全套持續累積（資源回收 B 面未落地、無鎖）：直呼 `_resume_tick` 的測試未 patch `tempfile.gettempdir`，`endurance_log_path(plan)` 先寫 `woken` 到真暫存根
- **evidence**：〔實測〕`ls -la $TMPDIR | grep autosdd`（2026-09-06 10:47 本地）：`autosdd_resume_log_plan.jsonl` 140,504 bytes mtime 10:40（尾列 `"plan": "/var/folders/…/T/relay-f1-6l4arf5g/plan.md", "event": "woken"`）、`autosdd_resume_log_autosdd_resume_plan_sid-lock.jsonl` 12,062 bytes 10:40（`"task": "AutoSDD_Sentinel_sid-lock" … "event": "sentinel_armed"`）、`autosdd_resume_plan_s.md`／`autosdd_resume_plan_sid.md` 10:40、`autosdd_resume_log_autosdd_resume_plan_sess-forms.jsonl` 09-05 15:09——10:40 對應 commit 後那次全套（`tools/.last_failure_b1ef81f_20260906T024627Z.log`）；主控背景全套進行中仍在寫。〔實讀〕`sid-lock` 來源 test_context_budget_guard.py:1555-1561 `log_path: str(tmp / 'log.jsonl')` 但 `_resume_tick` :1192 先以 `endurance_log_path(plan)`（planner.py:437-440 取 `tempfile.gettempdir()`）寫 `woken`，該測試未 patch `tempfile.gettempdir`；`TmpdirHygieneTest`（:216）只管 mkdtemp，不管 `gettempdir()` 直寫。ADR §1.2-7 已立案的 B 面殘骸在 commit 後仍在；commit 自述「修 7 支測試未隔離 trace_dir 的污染」未蓋到 tmp 根層；ADR §3.6 B 面「只列不刪」與 §6 劃界皆未落地。
- **why**：與使用者原話「沒有正常關閉的資源可以在程序執行完後被正常關閉回收」直接相關；140 KB 且單調成長的殘骸檔會在數月後成為 `%TEMP%`／`$TMPDIR` 清理策略的受害者（planner.py:670-671 自述 5 小時跨度真的會發生）。
- **proposed_verification**：`TmpdirHygieneTest.test_direct_tick_callers_redirect_gettempdir`（AST：直呼 `_resume_tick`／`_sentinel_tick` 的函式體內須有 `patch('tempfile.gettempdir'` 或 `_isolate_trace_dir` 同型 helper，附合成 AST 紅綠自證）；或在 `run_root_unittests` 收尾（M-03 leak_fence B 面）印 `$TMPDIR` 根層 `autosdd_*` 新增清單（只列不刪），紅綠自證以合成目錄注入（`test_new_temp_files_are_listed_never_deleted`）。
- **files**：tools/tests/test_context_budget_guard.py, tools/run_root_unittests.py, tools/lib/sentinel_lifecycle.py

### M-13 [P2] INV5 單一擁有者只在 `_arm_sentinel` 一站強制；`_arm_endurance`／`--register-schtasks` 建 `AutoSDD_SessionResume_<sid>` 時不查 owner ⇒ 同 session 雙 job 的事故形態仍可從手動路徑重現
- **evidence**：〔實測〕`grep -rn other_owner_for_session tools/lib tools/session_resume_planner.py .claude/hooks` 非 def 命中只有 planner:1308（`_arm_sentinel` 內）。〔實讀〕`_arm_endurance` :984-1006 與 main :1549-1553 `_register_at_expr(str(out), resume_task_name(...), args.at, RESUME_TICK)` 直接註冊續航 job，無 owner 查詢；`resume_task_name` :654 前綴 `AutoSDD_SessionResume_`＝`relay_machine._JOB_PREFIXES` 第二族，正是 INV5 要擋的「跨族同 session」。Inv5SingleOwnerTest 的 `_both_backends` 迭代只包純函式（Inv4 docstring :3899-3903 自承裝飾性），wiring 測試 :3980-4008 只用 `_Fake`，未涵蓋 endurance 路。
- **why**：事故當日 20:00~22:54 的「成對 woken/probed」＝哨兵＋續跑 job 各自探測。哨兵側補了 gate，續跑側（人手 `--arm-endurance`／`--register-schtasks`，或 R79 前武裝殘留）仍可造出第二個 prober；擇一擁有需在**兩族共同漏斗**成立。
- **proposed_verification**：把 INV5 判準下沉到 `_register_and_record`（planner:971，三條 arm 路共同漏斗）或在 `_arm_endurance` 前置同樣的 `list_jobs("AutoSDD_")`＋`other_owner_for_session`；胖身體放 relay_machine（planner LOC 餘裕見 M-18／M-01 的不一致註記）。測試 `Inv5SingleOwnerTest.test_arm_endurance_defers_when_a_sentinel_owns_the_session`（假後端 seed `AutoSDD_Sentinel_<sid>`，跑 `_arm_endurance`，斷言 `_register_at_expr` 零呼叫＋`sentinel_single_owner_deferred` 痕跡；現況必紅）與 `test_register_schtasks_cli_defers_when_a_sentinel_owns_the_session`。
- **files**：tools/session_resume_planner.py, tools/lib/relay_machine.py, tools/tests/test_context_budget_guard.py

### M-14 [P2] INV5 defer 回 rc=0：PATROL_HANDBACK 路上若 -Once 續跑排程拆除失敗（仍在清單）⇒ `_arm_sentinel` 把那支「已響過、不會再響」的 -Once 當成另一個 owner 而不武裝哨兵，喚醒鏈斷線卻 rc=0、無 loud
- **evidence**：〔實讀〕planner.py:1251-1252 `removed = 0 if inplace else _schtasks_remove(state["task_name"])`（rc 只入痕跡）→ :1265 `_arm_sentinel` → :1307-1311 若 `other_owner_for_session` 命中即 `return 0`；:1239 註解承諾「收不掉時仍然武裝（多一支死工作比沒有哨兵好）」與 INV5 新分支矛盾。`other_owner_for_session`（relay_machine.py:250-268）只看名字，不區分「活的 prober」與「已觸發的 -Once」。無測試覆蓋「移除失敗＋INV5」交集。
- **why**：Windows 上 `Unregister-ScheduledTask` 偶發失敗是既有實跡（schedule_backend.py:350-353 即為此設計），此時 INV5 會把唯一能續航的哨兵擋掉且對外 rc=0——與掌舵者「喚醒鏈必須自癒」要求相悖。
- **proposed_verification**：`Inv5SingleOwnerTest.test_a_spent_resume_job_that_failed_to_unregister_does_not_block_the_sentinel`：`_handback_tick` 同法（:1585-1620），`_schtasks_remove` 回 1、假 list_jobs 回 `AutoSDD_SessionResume_<sid>` ⇒ 斷言 `_register_and_record` 被呼叫一次（或 rc≠0 ＋ loud alert）。設計上可讓 `_arm_sentinel` 接受 `ignore=(state['task_name'],)` 或在 defer 分支 rc 回非零＋alert。
- **files**：tools/session_resume_planner.py, tools/lib/relay_machine.py, tools/tests/test_context_budget_guard.py

### M-15 [P2] INV4 的 env→`settle_window`→`no_progress_limit()` 組合（無人第一次無進度即停）無端到端測試；現有測試直接傳 `no_progress_limit=1` 給 `resolve()`
- **evidence**：〔實讀〕relay_machine.py:412-413 「outcome = resolve(state, band, max_spawns=max_spawns(), no_progress_limit=no_progress_limit())」；test_context_budget_guard.py:3895 `Inv4UnattendedStopsOnFirstNoProgressTest.test_a_single_no_progress_window_stops_even_when_cap_and_band_allow` 直接傳 `no_progress_limit=1` 給 `resolve()`；`RelaySettleWindowTest._run` 不設 `AUTOSDD_UNATTENDED`。〔實測〕`grep -n 'UNATTENDED_ENV: "1"' tools/tests/test_context_budget_guard.py` 只命中 :3729/:3745/:3910（皆 INV1/INV4 純函式測試），無一支在 `_resume_tick`／`settle_window` 路徑上設無人旗標。🔴 四包對「突變 settle_window 硬寫 no_progress_limit=5 會不會被抓」有分歧：SD 於 verified_ok 稱 RelaySettleWindowTest 的 `relay_stopped why=no_progress` 斷言會轉 RELAY_NEXT 而紅（依賴出廠預設 1），SA 認為改讀常數時現有測試全綠——兩者對「無人旗標那一路是否有鎖」的結論一致（無），分歧只在預設值路徑，落地時以實跑突變裁決。
- **why**：INV4 對使用者的承諾是「無人一次 no-progress 即停」，成立於 `settle_window` 真的呼叫 `no_progress_limit()`（而非讀 state 或常數）；該一行改成讀 `RELAY_NO_PROGRESS_LIMIT_DEFAULT` 常數或 env 放寬時，無人模式回到多窗續燒而無鎖出聲。與 M-01 同一功能面但根因不同：M-01 是生產 tick 沒有旗標，本列是「有旗標時整條路是否真的夾 1」也沒被測。
- **proposed_verification**：新增 `Inv4UnattendedStopsOnFirstNoProgressTest.test_resume_tick_under_unattended_stops_on_first_window_despite_env_limit_5`：沿用 `RelaySettleWindowTest._run` 骨架，`patch.dict(os.environ, {AUTOSDD_UNATTENDED: "1", AUTOSDD_RELAY_NO_PROGRESS_LIMIT: "5"})`，`run_resume_result={"_rc": None}`、`relay_no_progress_streak=0`，斷言 `written["state"] == "resume_failed"`、事件含 `relay_stopped why=no_progress`、`registered == []`；控制組去掉無人旗標 ⇒ 不停（streak=1、無 relay_stopped）。M-01 落地後此測試與其 (c) 合流（tick 自帶旗標）。
- **files**：tools/tests/test_context_budget_guard.py

### M-16 [P2] 「可見性實地驗過」只到 in-process `main()` 接線，無真實 CLI 行程輸出證據；本機從未產生過 `autosdd_unattended_outcome.jsonl`
- **evidence**：〔實讀〕test_context_budget_guard.py:4384 `Fix3UnattendedOutcomeBannerTest`：`test_pace_prints_the_banner_at_the_start_of_output` 以 `unittest.mock.patch.object(planner.quota_gate, "pace_report", lambda **_k: "PACE-BODY\n")` 與 `patch.object(planner.sentinel_lifecycle, "liveness_line", lambda *_a, **_k: "")` 後呼叫 `planner.main(["--pace", ...])` 並 `contextlib.redirect_stdout(buf)`；`--check` 同型。無 `subprocess.run([sys.executable, _PLANNER, "--pace"…])` 形態的測試；repo 內（docs/、ADR、帳本）零筆真實 `--pace`／`--check` 印出 banner 的逐字輸出。〔Developer 實測〕`AUTOSDD_SENTINEL_OFF=1 .venv/bin/python tools/session_resume_planner.py --check` 首行即「session   812a9c3b-…」rc=0；`--pace` 首行即「現在可派 4 個 agent…」rc=0——皆無 banner，因 `ls ~/.autosdd/traces | grep -i outcome` 與 `ls $TMPDIR | grep -i autosdd_unattended_outcome` 皆 rc=1（本機無任何結局檔）⇒ FIX3 的「實地」無人續跑結局從未在此機發生，只有測試證據。
- **why**：pythonw／真 shell 下 stdout 編碼、`end=""` 接線、banner 與 `pace_report` 真輸出的相對順序，只有真行程能證；本 repo 曾有「機制蓋好沒接電」判例（Fix3 docstring 自述）。宣稱「實地」需改寫為「in-process 接線已鎖、真 CLI 未驗」或補證（鐵律四）。
- **proposed_verification**：新增 `Fix3UnattendedOutcomeBannerTest.test_pace_cli_subprocess_prints_banner_first`：以 `_isolated_env(tmp)` 加 `AUTOSDD_TRACE_DIR=tmp`，先 `record_unattended_outcome`，再 `subprocess.run([sys.executable, str(_PLANNER), "--pace"], env=env, capture_output=True, encoding="utf-8")`，斷言 rc=0、stdout 以「🔴 ⚠️ 上次無人續跑」開頭、第二次執行不再出現；同型補 `--check`。證據檔以一次受控無人回合（AUTOSDD_UNATTENDED=1 下 record_unattended_outcome → 下一次 --pace 印 banner）貼出輸出，否則改寫為「測試驗證、未實地」。
- **files**：tools/tests/test_context_budget_guard.py, docs/06_quality/Resource_Leak_Forensics_20260905.md

### M-17 [P2] ADR-XPLAT-015 狀態文字、缺陷帳本與落地實況三方不一致：ADR 仍稱「未落地任何生產碼」而 §4-1/§4-2/§4-3(a)(b) 已落地、§5 步 3~7 未落地無標記、候選測試名未回填（`HealArmedDriftTest` 幽靈名已流入任務書）、§6 LOC 值 stale；帳本 DEF-200-266~271 六列仍寫「工作樹待 commit」、DEF-200-270／271 標 fixed 與 M-01／M-02／M-11 矛盾、無 open 列承接 §5 步 3~7
- **evidence**：〔實讀〕docs/04_planning/ADR/ADR-XPLAT-015-resource-reclamation-protocol.md:3 逐字「本 ADR 是設計文件，未落地任何生產碼，§5 為施工順序」、:10「落地物（候選，尚未動工）」——但同 commit 已落地 §4-1（quota_escalation.py:356-366）、§4-2（:383-389）、§4-3(a)(b)（SchedulerHygieneTest :243、:5047 注入）；`grep -n '已落地\|落地訖' ADR-XPLAT-015…` 零命中。ADR:215 步 1 驗收名 `HealArmedDriftTest.test_a_non_prefixed_label_already_present_is_not_re_armed`，實際 def 住 `ArmedDriftSelfHealTest`（test_context_budget_guard.py:6217/:6379/:6392），`grep -rn HealArmedDriftTest tools/` 零命中——本任務書照抄該名 ⇒ 四包皆〔實測〕`AttributeError: module 'test_context_budget_guard' has no attribute 'HealArmedDriftTest'` rc=1。§5 表（:210-222）各步無「已落地／未落地」欄；§4 首句「三個結構事實各對一層，缺一不可」但第三層 §3.2 未落；§4-3(c)「GHOST 在沙箱下從真機副作用變成沙箱路徑」依賴未落地的 §3.2（M-02）；§6 LOC「`quota_escalation.py` 377/400」「count_loc 360/400」「44/400」vs 現值 400/372/56〔Developer 實測〕；ADR §6 要求同輪改寫 :70 `setUpModule` docstring 亦未做。〔實讀〕docs/06_quality/AutoSDD_Defect_Log.md:267-272 六列皆含「工作樹待收尾 commit／工作樹待 commit」（`grep -c '工作樹待'`＝6），而 HEAD b1ef81f 已 commit；:272 DEF-200-271 狀態「fixed：`_exact_listing` 精確查名＋尊重 SENTINEL_OFF＋:4071 注入假後端＋`SchedulerHygieneTest` AST 鎖；全套絆線 T-f4b 零命中」，缺陷欄自己寫的「回歸鎖 win32-only、mac 零現查」仍成立、未提 §5 步 3~7 未做與 GHOST 子行程真武裝仍在；:271 DEF-200-270「fixed…INV1 無人零付費探針」在生產 tick 不可達（M-01）。〔實測〕`grep -n 'SandboxBackend\|leak_fence\|owned_jobs\|orphan_verdict\|§3\.2\|§3\.6\|步 3\|ADR-XPLAT-015' docs/06_quality/AutoSDD_Defect_Log.md` 只命中 DEF-200-147／DEF-200-242 無關列，零承接列。commit 訊息「T-f4b 洩漏止血（ADR-XPLAT-015 §4-1/§4-2/§5）」把 §5 整節寫成已止血。
- **why**：根 CLAUDE.md 鐵律四／缺陷帳本單線原則：兩份權威文件對同一件事給相反訊號（Rule 7 衝突不得平均）——讀者依 ADR 狀態欄會誤判「全部未動工」、依帳本誤判「已根治」；未落地的結構層若無 open 列，下一輪讀者會把「DEF-200-271 fixed」讀成「洩漏已根治」，正是 DEF-200-239 fixed 卻在 mac 復發的同一形態。候選名未回填已實際造成一次派工用錯類名；「待 commit」字樣使 zero-trust 重驗者誤以為工作樹仍髒。
- **proposed_verification**：收尾單人窗口（鐵律七）：(1) ADR :3 狀態改為「Adopted；§4-1／§4-2／§4-3(a)(b)（§5 步 1~2）已落地於 b1ef81f，§3.2／§3.3／§3.4／§3.6／§3.7（步 3~7）未落地」，§5 表加落地欄並回填實際 def 名（`ArmedDriftSelfHealTest.…`、`SchedulerHygieneTest.test_every_direct_tick_call_isolates_the_scheduler` 等），§4-3(c) 改寫為「待 §3.2 落地後成立；現況 GHOST 仍真武裝（見 §7 Q2）」，§6 LOC 值標「〔實測 2026-09-05〕已 stale，現查」；(2) 帳本：DEF-200-271 改 `partial`（T-f4b 站點止血）或新立 open 列「ADR-XPLAT-015 §5 步 3~7 承接（沙箱／owned_jobs／orphan_verdict／leak_fence／gate_pg finally；GHOST opt-in 化；mac 現查）」並補述「第一接縫與子行程路徑未護」；DEF-200-270 補述 INV1/INV4 tick 環境缺口；六列刪「工作樹待 commit」補 commit 短碼；(3) 跑 `python tools/check_defect_log_crossref.py` 與 `tools/tests/test_doc_loc_baseline_freshness_r60.py`（含 `TestR78GhostSymbolClaims`）確認新列不觸發幽靈符號／保全稽核紅；可選新增 `TestAdrXplat015LandedStepsMatchStatusLine`（grep `_exact_listing` 存在 ⇒ 狀態欄不得含「未落地任何生產碼」）。
- **files**：docs/04_planning/ADR/ADR-XPLAT-015-resource-reclamation-protocol.md, docs/06_quality/AutoSDD_Defect_Log.md, tools/tests/test_context_budget_guard.py

### M-18 [P2] LOC 餘裕已被本 commit 吃光：quota_escalation.py 400/400（ADR 寫 377/400 已 stale）、schedule_backend.py 372/400 不足以容 SandboxBackend、run_root_unittests.py 756/759、context_budget_guard.py 1089/1089；§3.2＋§3.6 及 M-01／M-06 落地前必先搬碼，並行包會在同一檔互撞
- **evidence**：〔實測〕`.venv/bin/python AutoClaude/tools/check_loc_budget.py --json`：`"rel_path": "tools/lib/quota_escalation.py", "loc": 400, "tier": "guardrail_lib", "budget": 400, "over_by": 0, … "headroom": 0`；`"rel_path": "../tools/run_root_unittests.py", "loc": 756, "tier": "special", "budget": 759, … "headroom": 3`；count_loc：schedule_backend.py 372、sentinel_lifecycle.py 213、schedule_backend_calendar.py 56、sentinel_lifecycle_arm.py 102、session_resume_planner.py 717（ABSOLUTE_LIMIT=750，check_loc_budget.py:122）、relay_machine.py 230、resume_route.py 98、endurance_env.py 171、quota_limits.py 171；context_budget_guard.py raw 1089/1089（headroom 0）。〔實讀〕ADR §6 逐字「`schedule_backend.py` count_loc 360/400〔實測〕，§3.2＋§3.3 約 +40 assertion 行 ⇒ 破線」「`quota_escalation.py` 377/400 只餘 23」——§4-1/§4-2 已把 23 行用盡（quota_escalation.py:356-389 新增 `_exact_listing`＋pin 分支）。SandboxBackend 需 `arm`／`list_jobs`／`disarm`／`verify_cli`／`credential_key`／`credential_line`／`evidence_hint`／`name`＋類別層 `ledger` 共 8 介面（r83:340 `_BACKENDS` 對稱鎖要求），估 35~45 count_loc > 28 餘裕。🔴 planner 餘裕兩包不一致（Architect 稱 0、Developer 實測 717/750），落地前現查。
- **why**：兩項功能的下一步（§3.2 沙箱、§3.6 圍籠、M-01 若選在 tick 入口修、M-06 若選 hook 端）都會先撞 LOC 棘輪而非功能問題；不先規劃搬碼，並行包會各自在同一檔上「+3 行」互撞（鐵律七的鎖持有面）。記憶：護欄層成長用搬史料抵銷。
- **proposed_verification**：落地當回合先跑 `python AutoClaude/tools/check_loc_budget.py --json` 取值；搬碼候選（純減法、零行為變更）：(a) quota_escalation.py:343-353 `_append_trace` 與 schedule_backend_calendar 已有的 `_append_trace` 去重（改 import，約 −8）；(b) SandboxBackend 不放 schedule_backend.py，改住 sentinel_lifecycle_arm.py（102/400、無載具原語，不撞 r83:205 `_CARRIER_HOMES`），`select()` 內 lazy import（schedule_backend.py:265 已有同型先例），只在 schedule_backend.py 增 3~4 行判式；(c) runner 只准 +2 raw line（餘 1），任何註解一律進 docs/06_quality 證據檔；(d) context_budget_guard.py 零餘裕 ⇒ M-06 (B) 落在 block_destructive_git.py。驗收＝`TestGuardLayerRatchet`／`TestShrinkOnlyRatchet`（test_adr_xplat001_c1c2_lock.py）續綠、`python tools/tests/test_adr_xplat001_c1c2_lock.py --print-guard-lines` 不高於主控實測 93610 或在帳本具名授權 overage。
- **files**：tools/lib/quota_escalation.py, tools/lib/schedule_backend.py, tools/lib/schedule_backend_calendar.py, tools/lib/sentinel_lifecycle_arm.py, tools/run_root_unittests.py, AutoClaude/tools/check_loc_budget.py, docs/06_quality/CrossPlatform_Guard_Line_History.md

### M-19 [P3] 「雙平台」實為雙後端 in-process 注入；Windows 真機排程行為（`Get-ScheduledTask` 解析、pythonw 編碼）程式碼自陳「量不到、待 Windows 實測」，INV5 真消費 `list_jobs` 卻無 `[WINDOWS-NATIVE-ONLY]` 真機測試
- **evidence**：〔實讀〕test_context_budget_guard.py:3696-3706 `_both_backends()` docstring 逐字：「本機是 macOS，schtasks 路徑無法真跑——Windows 那一側一律介面/邏輯注入，真機行為（`Get-ScheduledTask` argv 解析、pythonw 載具輸出編碩）標『量不到、待 Windows 實測』」；:3895 Inv4 docstring：「INV1~INV4 皆平台中性純函式、不消費 `schedule_backend.select()`，此處的雙後端注入是裝飾性的」。INV5 真消費 `list_jobs("AutoSDD_")` 但無 `[WINDOWS-NATIVE-ONLY]` 真機測試（既有 win32 真機鎖只有 :6734 `test_no_ghost_t_r95_task_survives_a_real_windows_scheduler_query`）。測試邏輯確會在 windows-latest runner 執行（無 skip，見 verified_ok），這一半成立。
- **why**：「雙平台」讀者會理解為兩平台真載具皆驗，與程式碡自陳不符（鐵律四措辭精確度）。
- **proposed_verification**：commit／帳本措辭改「雙後端注入＋三 runner 邏輯執行；Windows 真載具 INV5 `list_jobs` 解析待實測」；或補一支 `[WINDOWS-NATIVE-ONLY]` 測試 `Inv5SingleOwnerTest.test_real_get_scheduledtask_listing_feeds_other_owner_for_session`（仿 :6734 形態，註冊兩支 `AutoSDD_*_UNITTEST_<sid>` 後現查、addCleanup 拆除——注意與 M-02 opt-in 原則一致，須標 MANUAL）。
- **files**：docs/06_quality/AutoSDD_Defect_Log.md, tools/tests/test_context_budget_guard.py

### M-20 [P3] INV1~5／FIX 鎖的存在性無註冊表釘住；MIN_TESTS 餘裕 95 大於新增測試數，整批刪除 INV 類不會觸發任何閘門
- **evidence**：〔實測〕discover 3990／MIN_TESTS 3895（餘裕 95）；〔實讀〕INV/FIX 區（:3707-4110、:4384-4440）`def test_` 共 24 支（20＋4）；`_FROZEN_GUARD_LINES` 判的是總淨行數（test_adr_xplat001_c1c2_lock.py:112「行數棘輪比的是 `_FROZEN_GUARD_LINES` 這張表的總量」），非逐類存在；`grep -n 'Inv1UnattendedZeroPaidProbeTest\|Inv5SingleOwnerTest' tools/tests/*.py` 只在定義處與 ledger 散文出現，無 SSOT 清單引用。
- **why**：整批刪除 INV 測試類（或改名成不被 discover 的形態）不會觸發 MIN_TESTS 下限，行數若被別處成長抵銷亦過棘輪——「每次更新都要通過這些測試」在機械上沒有把「這些」釘成集合。
- **proposed_verification**：比照 `_TICK_FUNCS` SSOT 形態新增 `InvariantLocksArePresentTest.test_each_inv_and_fix_has_a_named_lock_that_is_collected`：表 `{INV1: ("Inv1UnattendedZeroPaidProbeTest", 3), INV2/3: ("Inv2Inv3WorkflowFanoutGateTest", 4), INV4: (…, 3), INV5: (…, 6), FIX2/3/4: …}`，以 `unittest.defaultTestLoader.loadTestsFromName` 現查類存在且測試數 ≥ 表值。
- **files**：tools/tests/test_context_budget_guard.py

### M-21 [P3] R129 Scan Findings 稱 ONBOARDING 指紋基線已由 `--write` 回填，但 commit b1ef81f 未含 ONBOARDING.md（敘述與 commit 內容不一致，原因未歸因）
- **evidence**：〔實讀〕docs/06_quality/CrossPlatform_R129_Scan_Findings.md §3「ONBOARDING 指紋基線由 `tools/sync_onboarding_baselines.py --write` 回填（四棵測試樹改動使 … 指紋漂移）」；〔實測〕`git show b1ef81f --stat` 19 檔不含 ONBOARDING.md；`git log -3 -- ONBOARDING.md` 末次為 7eaa548（2026-09-05，早於 b1ef81f）。〔實測〕`tools/sync_onboarding_baselines.py --check-snapshot` rc=0 ⇒ CI 不紅；可能是指紋未變或已在前一 commit 回填。
- **why**：證據檔對「做了什麼」的敘述應與 commit 對得上（鐵律四）；此處不影響閘門，屬敘述精確度。
- **proposed_verification**：重跑 `python tools/sync_onboarding_baselines.py --write` 後 `git status --porcelain ONBOARDING.md` 為空即可將 §3 改寫為「指紋未漂移、無需回填（--check-snapshot rc=0）」；否則補回填並記錄。
- **files**：docs/06_quality/CrossPlatform_R129_Scan_Findings.md, ONBOARDING.md

### M-22 [P3] Windows 側 `_exact_listing(task)` 以 `-like '<task>*'` 精確查名：label 含 `[`／`?`／`*` 時假陰性 ⇒ 每 tick 誤判漂移重掛（§4-1 在 Windows 的邊界未鎖）；`_unsafe_name` 未拒絕這些字元（ADR §3.8 已登記修法但未落地）
- **evidence**：〔實讀〕tools/lib/schedule_backend.py:361-369 `SchtasksBackend.list_jobs`：`"Get-ScheduledTask | Where-Object { $_.TaskName -like '" + pat + "*' }"`（PowerShell `-like` 把 `[`／`?`／`*` 當萬用字元）；quota_escalation.py:365-366 `_exact_listing` 直接以 task 全名當 prefix 餵進去並依賴 list_jobs 回含自身；schedule_backend_calendar.py:32-34 `_unsafe_name` 只拒 `/\ \t\n\r` 與 `.`／`..`。ADR §3.8 已自陳此缺口與修法（擴 `_unsafe_name` 拒 `[]*?`），本 commit 未落地；ArmedDriftSelfHealTest 全走假後端，未涵蓋此形態；射程限自訂 `--task-name`（自動命名為 UUID，安全）；Windows 格本機量不到（mac 上無法真跑，標〔未驗證〕）。
- **why**：§4-1 治的就是「非前綴 label 每 tick 誤判漂移重掛」的自我永續；在 Windows 上一個帶 `[` 的自訂 `--task-name` 會把同一個病從另一個方向帶回來。屬邊角但零成本可鎖。
- **proposed_verification**：`_unsafe_name` 擴到拒絕 `[]*?`（兩平台同一判準）；測試 `WindowsPathIsUnchangedTest.test_exact_listing_survives_wildcard_characters_in_a_label`（注入 `planner.run_powershell` 假輸出，label 含 `[`，斷言 `_exact_listing` 不誤判為 `[]`，或 `arm()` 直接拒絕該 label `(1, "")`）＋ `test_unsafe_name_rejects_powershell_like_wildcards`（純函式）；標 `[WINDOWS-NATIVE-ONLY]` 的真機半格待持 Windows 機器者以 `Get-ScheduledTask | Select TaskName` 貼輸出。
- **files**：tools/lib/schedule_backend_calendar.py, tools/lib/quota_escalation.py, tools/tests/test_mac_endurance_r83.py, tools/tests/test_context_budget_guard.py
