# AutoSDD Defect Log — Archive 58

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 **26 筆已結列**。
>
> **搬遷判準的權威來源是 `tools/archive_defect_log.py`**（程式即判準，本標頭不重述細則）。
> 該工具落實 6 項搬遷判準（①狀態欄分類已結（fixed／wontfix／closed-by-decision）／②狀態欄無活躍字樣（open／routed／deferred／watch／workaround，ASCII 邊界非子字串；程式碼片段與角引號引述內的字樣不算，R68 收窄）／③被 crossref 掃描目標宣稱過狀態者可搬，但搬後該宣稱必須仍解析得到（帳本家族＝主檔 ∪ archive；由 --check 判準(8) 實跑驗證，R68 改寫）／④散文帶交棒字樣者需 `--ack-handoff` 具名承認／⑤該列切出的欄數等於表頭欄數（欄位定位失效者一律不判讀狀態、一律不可搬）／⑥無外部居所指針宣稱本列現居主檔（指針反向依賴，DEF-101-612；有則硬擋，須先訂正該指針，不接受 --ack 繞過）），
> 並在落地後以 `--check` 稽核 8 項保全判準：(1)行尾：帳本家族每一份檔在磁碟上不得含 CR（`.gitattributes` 宣告 eol=lf）、(2)重複列：同一 ID 在同一份檔內不得出現兩列、(3)跨檔矛盾：同一 ID 同時存在主檔與 archive 時，兩邊狀態分類不得各說各話、(4)立帳指針：稽核面每一處「立帳見」都要跟得上可解析 DEF-ID，且居所宣稱與實況一致、(5)歸檔索引涵蓋性：磁碟上每支 archive 都要在歸檔索引檔有一條以它為主體的 bullet（雙向）、(6)非「立帳見」方言的居所宣稱：`見主檔 DEF-x`／`見 DEF-x（現居 archive_NN）` 同樣驗居所；裸「現居 archive_NN」（無「見」動詞）另受對等硬要求，須跟得上可解析 DEF-ID、(7)表格列欄數：每列切出的欄數等於該檔表頭欄數；archive 側既有列具名基線、主檔零豁免、(8)跨檔宣稱可解析：掃描目標的每一句狀態宣稱都要能在帳本家族（主檔 ∪ archive）解析到，且狀態一致（判準③ 改寫後的事後條件，R68）
> （本段由該檔的 `MOVE_CRITERIA`／`CHECK_CRITERIA` 常數機械生成，逐項定義見
> `check()` docstring；**勿手改**——手寫版曾與實作脫節而被複製成永久史料）。
> **歷輪標頭曾宣稱有這樣一支腳本但 repo 內無載具**，
> 且散文所載判準與實際執行的判準不一致——R60 起改為引用可重跑的工具，見該檔 docstring。
>
> **搬遷清單**：`DEF-101-826`、`DEF-101-824`、`DEF-101-827`、`DEF-101-828`、`DEF-101-820`、`DEF-101-825`、`DEF-101-829`、`DEF-101-830`、`DEF-101-831`、`DEF-101-817`、`DEF-101-823`、`DEF-101-808`、`DEF-101-819`、`DEF-101-812`、`DEF-101-813`、`DEF-101-805`、`DEF-101-816`、`DEF-101-815`、`DEF-101-821`、`DEF-101-807`、`DEF-101-822`、`DEF-101-814`、`DEF-101-818`、`DEF-101-804`、`DEF-101-806`、`DEF-101-809`
>
> **本次操作備註**：R76 開輪前預防性歸檔（Prep-Archive 包）：搬 R75 已結列 26 筆。--ack-handoff 具名承認 807／812 兩筆——807 的承接者 test_adr_xplat001_c1c2_lock.py::TestScanHTripletIsTheLiveCriterion 經實查存在（該檔 3174 行），812 的 解鎖條件 字樣指的是仍留在主檔的 DEF-101-798，兩筆皆非向未來輪次交棒。R76 承接的兩筆未結列 810／811 由判準① 自動留在主檔，未還原任何列。
>
> 餘裕一律以 `python tools/check_defect_log_crossref.py` 的實跑訊號為權威，
> 本標頭**不對餘裕做定性宣稱**（R59 SA-R59-P2-1 訂正：定性宣稱會在同輪後續編輯中被推翻）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢缺陷現況一律先看主檔缺陷總表。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-826 | 2026-08-04 | R75 四方複審（SD NON-BLOCKING-5 ＋ QA BLOCKING-2 同一判準的兩層） | **方向感知 skip 判準有兩層失效：對複合布林算出反方向，且 61% 的站點對所有機械物隱形**。① `skipIf(sys.platform == 'win32' or sys.platform == 'darwin')` 實際在 Windows 會 skip，但判準以 substring × marker 長度序決定，取到較長的 darwin 而算成 `non-windows`——**方向算反正是該檔自承「比判不出方向更糟」的形態**。② 全樹 103 站點中 63 筆（61%）方向回 `None`（環境探針型述詞如 `_BASH`／`_ZSH is None`／`_DSN is None`／`shutil.which(...)`），而 docstring 指定承接它們的收口網因粗網不含這些字樣**一筆都不命中** ⇒ 那句「由收口網承接」是假的；另有一整類 `self.skipTest()` 寫在函式體內完全在射程外 | P1 | 修法落地本輪 | fixed@R75｜① 改 **AST 真值運算**（`or`／`and`／`not` 逐層），葉節點以集合語意查表，同葉命中值相反的 marker 回 `None` 不猜；長度序不再是判準的一部分。7 種形態實測全對，並連帶消掉一筆「若不逐葉判就會新生的假紅」。② 分類法**刻意不用關鍵詞表**（第一版寫過、實測後拆掉——每加一支新探針就要回來補表，而那張表腐化的方式正好是「又一批站點靜默變隱形」）：改用結構判準，方向判得出來→平台兩格；判不出且有「像 Windows 卻未登記的葉」→`unclassified`（可行動）；判不出且沒有→`tool-absence`（值取決於這台機器裝了什麼），新探針寫法天然落進後者、零維護。`self.skipTest()` 已納入射程（`runtime-skipTest`，實測 10 筆）。普查 115 站點、**`unclassified` = 0**；假 docstring 已改寫並誠實劃界 |
| DEF-101-824 | 2026-08-04 | R75 四方複審（Architect NON-BLOCKING-3） | **同一份知識（強制 stdout／stderr 為 UTF-8）有兩個各自宣稱是 SSOT 的家，兩把鎖互相看不見**：`tools/lib/platform_utils.py::init_utf8_streams`（`io.TextIOWrapper` 換掉串流、只在 `__main__` 呼叫）與 `tools/_stdio_utf8.py::reconfigure_stdio_utf8`（`.reconfigure()` 就地改、import 期即生效）手法／啟用時機／對測試替身的行為三者皆不同，消費端分屬 hook 側與根層 tools 側，換用另一支會靜默改變行為，而兩把鎖都只守自己那一支——其中一支的鎖還逐字自稱「`tools/_stdio_utf8.py` 已經是 SSOT」，那句話在寫下時就是假的，正是讓雙 SSOT 衝突躲過複審的原因。R74 那筆 P0 就落在這一層 | P1 | 修法落地本輪 | fixed@R75｜SSOT 定為 `tools/_stdio_utf8.py`，另一支改惰性委派（公開名與 8 支消費端契約不動）。🔴 **方向由硬約束決定，不是由「哪層比較像共用層」決定**：第一版把實作搬進 `tools/lib/` 而當回合被實測打回——`_stdio_utf8.py` 的既有契約包含「被複製到別處單獨執行」（NTFS 長度閘門會把它連同另一支工具複製進不含 `tools/lib/` 的 tmp 沙箱跑子行程），搬走後沙箱 import 期 `ModuleNotFoundError`，3 支測試當場紅。實作為原本兩份的嚴格聯集（三分支）；委派刻意惰性，避免「只想問平台」的消費者也被動手術。6 道機械物讓兩把鎖看得見彼此，含**可搬遷契約行為鎖**（複製到 tmp ＋ cp1252 子行程實跑）。順帶拔掉 `check_loc_budget.py` 自帶的行內複本——它連 `errors=` 都沒帶，正是 R74 P0 形態 |
| DEF-101-827 | 2026-08-04 | R75 四方複審（Architect NON-BLOCKING-4，含對其量測的訂正） | **護欄層自己不受護欄管**：`check_loc_budget.py` 的 `SCAN_ROOT="autoclaude"` ⇒ 根層 `tools/` 不在任何 LOC 分級政策射程內，而 `tools/lib/` 在 R74 一輪淨增 385 行**零訊號**。🔴 **訂正 Architect 的量測**：它說「727 行 > AutoClaude 絕對紅線 750，住 AutoClaude 早該被擋」不成立——紅線量的是 `count_loc`（非空非註解），該檔實測 508、在 750 以下。真正的缺口不是那個比較，是 `tools/lib/` 完全沒有治理 | P2 | 修法落地本輪 | fixed@R75｜LOC 治理延伸到根層 `tools/`，沿用同一套 `count_loc`／報表／rc（**不造第二套檢查器**，否則就重演 DEF-101-824 的形態）：`guardrail_lib`（`tools/lib/`）=400、`guardrail_cli`（`tools/` 頂層）=750；`tools/tests/` 不納管＝與 AutoClaude 側對稱。已在 `SPECIAL_FILES` 的檔排除在 tier 外不雙重度量；**未被收錄又超預算的一律紅 ⇒ 收錄面會自己補完**，不是一次性快照。新 400 tier 當場擋下 `windows_skip_tags.py`（508 > 400），據此把它按四種職責拆成 `skip_tag_policy`(173)／`skip_static_scan`(353)／`skip_source_io`(35)／`skip_runtime_report`(131)，facade 保留再匯出並以**常數注入**編排——注入不是裝飾，既有測試明文 patch 那些常數後判定必須跟著變，實作若讀自己模組層常數那道注入會靜默失效 |
| DEF-101-828 | 2026-08-04 | R75 四方複審（SD NON-BLOCKING-8 ＋ QA NON-BLOCKING-4 同型，跨三處） | **下限型判準單向腐化：只擋縮面，樹長大時無人說話**。`test_schedule_capability_parity.py` 的 `>= 40`（註解稱「R60 實測 43 份」）實測已 53、只剩 75% 鑑別力。🔴 **部分證偽 SD 的另一半結論**：`_TREE_FILE_FLOORS` 的 42／204／23 實測**剛好等於**模組宣告的八折語意（`int(53×0.8)`／`int(255×0.8)`／`int(29×0.8)`），樹自 R74 一支沒長、下限沒落後，照字面重釘會得到同一組數字，故不動。SD 真正指出且成立的是機制缺口：沒有任何東西會在下限過期時說話 | P2 | 修法落地本輪 | fixed@R75｜`tree_floor_problems()` 改**三向**皆紅：新樹未入表／`actual < floor`（縮面，原意）／**`floor < int(actual × 0.8)`（下限自己過期）**。`>= 40` 重釘為 42 並補第二向斷言，比例常數改由 `skip_tag_policy.TREE_FLOOR_RATIO` **import**，不再寫第二份 0.8。同形態亦已在編碼衛生鎖獨立落地（見 DEF-101-821 的雙邊帶），兩處同源不同檔，刻意各自就地可判、不互相耦合 |
| DEF-101-820 | 2026-08-04 | R75 修 DEF-101-819 時實測揭露（HEAD 既存） | `reject_unknown_argv` 對 `python -m unittest <模組>` 誤傷：`main(argv=None) → sys.argv[1:]` 拿到的是模組名，被當未知旗標拒收（實測 3 failures，清 `sys.argv` 後全綠）；閘門路徑不會紅故七輪無人發現（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-820` 節） | P1 | 修法落地本輪 | fixed@R75｜四支統一改 `cli`/`main` 分層（`main()` 完全不碰 `sys.argv`），拍板理由記於 `tools/_cli_flags.py` R75 段：測試端傳 `[]` 要求每個呼叫端都記得，漏掉的那個就是這次的 3 支假紅。`check_script_parity.py` 卡 1618 行棘輪只剩 1 行餘裕，故沿用零引數 `main()` 形狀達成淨零成長（實測仍 1617）。鎖不重複造，改把既有那支的射程由「具名一支」擴為**現查枚舉** `_cli_flags` 消費者（下限 5）並改 AST 判定。實測 `-m unittest` 14/14 OK、四支 `--bogus-flag` 皆 rc=2 且 ≤0.06s、注入 `sys.argv` 回 `main()` → 五支全紅 |
| DEF-101-825 | 2026-08-04 | R75 落地 DEF-101-824 時普查揭露 | **「行內複本不得再生」判準的射程只有實際的一成**：`_INLINE_STDIO_RE` 只認具名 `sys.stdout.reconfigure(`，對 `for s in (sys.stdout, sys.stderr): s.reconfigure(...)` 這個**最普遍的寫法**結構性全盲。實測窄判準看到 9 處／8 檔，寬判準看到 **81 處／77 檔** | P1 | 修法落地本輪 | fixed@R75｜新增寬判準 per-tree shrink-only 棘輪（`.claude/hooks:2`／SDD 凍結版:36／`AISDLC_SDD/scripts:10`／`AutoClaude:24`／`tools:6`）。刻意 per-tree 而非 per-file：寬判準命中 77 檔，逐檔列名會多出近八十行且大半落在其他包所有權內（並行包互踩＝假紅）；per-tree 仍保有「新增一處即紅」的牙，失去的只是訊息逐字指名，由斷言失敗時當場現查的明細補回。凍結版那 36 筆單獨成一格，讓「不可修的存量」與「可修的存量」在基線表上分得開 |
| DEF-101-829 | 2026-08-04 | R75 落地 DEF-101-820 時被自己的新鎖抓到 | **鎖的判準方向錯誤：字串比對把「把紀律寫下來的註解」判成違規**。新鎖第一版用 `"sys.argv" in ast.get_source_segment(main)`，於是 `main()` 裡一句「本層絕不讀 `sys.argv`」的**註解**觸發違規 | P2 | 修法落地本輪 | fixed@R75｜判準改 AST 節點判定（`ast.walk` 找真正的 `sys.argv` 屬性節點），並加反向正控（只在 docstring／註解提到該名字的合成檔 → 必須綠）釘住不復發。教訓寫進該鎖：**判準的方向若懲罰把紀律寫下來的人，就算它會紅也是壞判準**。同一形態本輪出現三次（皆為「在註解裡逐字寫出被掃描的樣式，於是自己成為違規」），另兩處分別發生在 LOC 檢查器與編碼衛生鎖的註解上，均已就地改為分段寫法 |
| DEF-101-830 | 2026-08-04 | R75 新建治理文件時觸發（反縮面鎖） | **新建治理文件未登記進宣稱掃描面，讓反縮面鎖轉紅**：`docs/06_quality/Scheduled_Jobs_Lifecycle_Review_R75.md` 逐字引述了一句已被列為過期宣稱的字樣，而該鎖的判準是「全庫任何命中檔必須落在登記面或時代快照白名單」，於是 rc=1 | P2 | 修法落地本輪 | fixed@R75｜登記進 `_CLAIM_SCAN_FILES`（**而非**時代快照白名單）。選擇理由：該檔通篇以排程現況取證（`Get-ScheduledTask` 實測 State／LastRunTime／NextRunTime）＝活文件，那句引述是「訂正脈絡旁的舊狀態論據」；進白名單等於讓它日後改述成「現在沒有心跳」也不會被本鎖看見。登記後該鎖的第二向判準（同檔須有任務名）自動成立且真的被驗到，實測 4 tests OK |
| DEF-101-831 | 2026-08-04 | R75 收輪整合（舵手實查） | **帳本→證據檔的具名節指針判準把輪號寫死，同一種指針換一輪就整批逸出**：`TestEvidenceFamilyPointersResolve._POINTER_RE` 原樣式寫死 `CrossPlatform_R60_Fix_Evidence…`，而兩層化（帳本列精簡＋詳情進具名證據檔）R60／R68／R75 都用過。實測舊樣式對現況帳本的 R75 指針抓到 **0 處**，且注入一個不存在的錨也抓不到＝**該鎖對本輪這 20 筆指針完全無牙** | P2 | 修法落地本輪 | fixed@R75｜樣式放寬為 `CrossPlatform_R\d+_\w+\.md`（**判準本身一字未改，只擴發現面**）。實測新樣式抓到 23 處指針、注入不存在的錨→抓到、注入不存在的檔→抓到。同 DEF-101-757「已知的鎖射程缺口不得只以劃界結案」，這次的界是輪號 |
| DEF-101-817 | 2026-08-04 | R75 四方複審（SD BLOCKING-2 ＋ QA BLOCKING-3／NB-7） | AC4 觀察期解鎖判準三個缺口：① staleness 量「最後一筆紀錄」而非真量測 ⇒ 全 skip 也永久新鮮、`ready` 永久凍結；② `recall σ ≤ 0.02` 的輸入全史是常數；③ 未來時間戳 fail-open（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-817` 節） | P1 | 修法落地本輪（判準+可見性） | fixed@R75｜① 改量真量測（`_is_measurement`）；② σ 判準不改，改把「讀 0 是因為沒變」印出，另立「窗內至少一指標有變異」收緊條件；③ 負值改報 `clock_anomaly`。真實歷史複跑仍 `ready` |
| DEF-101-823 | 2026-08-04 | R75 四方複審（QA NON-BLOCKING-6） | R74 那筆 P0（中文指引在非 CJK codepage 降解）的回歸鎖覆蓋該 hook 是順便的：child 編碼判準對 `-m`／`-c` 形態不納管，而 production 走 `python -c` ＋ `runpy.run_path`，突變實測進射程次數 1→0；另 `env.PYTHONUTF8=1` 被指名為唯一 UTF-8 來源卻零鎖（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-823` 節） | P1 | 修法落地本輪 | fixed@R75｜① 新增判準四把 hook 註冊表當掃描面（納管 4 支）並具名斷言該 hook 須經此進射程；② 為 `env.PYTHONUTF8=1` 補鎖；settings.json 一字未動，注入全走記憶體 |
| DEF-101-808 | 2026-08-04 | R75 開場實查（帳本體積閘門）＋SA BLOCKING-2／SD BLOCKING-3 | R74 宣稱「解帳本死結」的 committed 結果比動工前更靠近硬閘：248,048 → 252,067 bytes（淨增 4,019），commit 訊息的 233,559 是中間態；同時未結 97／fail 98；`tools/lib/defect_ledger_index.py` 註解裡「未結 87 列」是假數字（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-808` 節） | P1 | 修法落地本輪（收斂 + 註解訂正 + 禁令撤銷） | fixed@R75｜誠實結案 16 筆（未結 97→81）、註解訂正為實測值、R74「不准搬」禁令經實測範圍過寬已撤銷；存量豁免棘輪 48→36 |
| DEF-101-819 | 2026-08-04 | R75 四方複審（SD NON-BLOCKING-6） | 「CLI 須拒收未知旗標」的鎖以檔名 glob（`check_*.py`）劃界，使 pre-push root-infra leg ＋三支 CI 唯一真正執行的 `tools/run_root_unittests.py` 逃逸——該檔對 argv／argparse／`_cli_flags` 三者皆零命中（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-819` 節） | P2 | 修法落地本輪 | fixed@R75｜① 新增 `cli(argv)` 一層（分層是必要非風格：寫進 `main()` 會讓探針假紅）；② 射程改行為級（`tools/*.py` 帶 `__main__` guard），刻意不加「而且要讀 `sys.argv`」。實測 `--no-such-flag` → rc=2 秒回 |
| DEF-101-812 | 2026-08-04 | R75 四方複審（SA BLOCKING-1） | R74 的訂正註記被同一個 commit 弄成假話：`CLAUDE.md` 寫「只橋接 1 支／另外 5 支不會跑」，而該 commit 自己把 `check_sh_eol.py` 橋進根層（`.claude/settings.json:59`／`:64` ⇒ 實為 2 支／4 支）；`hook_claim_problems()` 已註冊即跳過＋判準為 OR ⇒ 該組合恆綠（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-812` 節） | P1 | 修法落地本輪（文件 + 反向判準 + 解鎖條件） | fixed@R75｜文件改 2 支／4 支並把 hook 名與射程字樣拆到不同行；補反向判準；`DEF-101-798` 解鎖條件同步訂正 |
| DEF-101-813 | 2026-08-04 | R75 四方複審（Architect NON-BLOCKING-1 ＋ SA NON-BLOCKING-5） | 五筆幽靈機械物指標（文件／註解宣稱的鎖不存在或守的是別的東西）。最難看的第五筆是實質假機械物：鐵律三「行尾」列指 `test_ps1_bom.py`，該檔對 `crlf`／`\r\n`／`eol` 三詞零命中（6 支全在驗 BOM）（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-813` 節） | P2 | 修法落地本輪 | fixed@R75｜五筆全部改指真鎖（含類別名）；鎖擴三面（掃描面加 `tools/*.py`／`tools/*.json`、副檔名擴 py／ps1／sh／json、新增實質判準含 `::Symbol` 須真存在） |
| DEF-101-805 | 2026-08-04 | R75 四方複審（Architect BLOCKING-2 ＋ SA NON-BLOCKING-3） | compat-CI `paths:` 涵蓋鎖兩種結構性失明（`from <套件> import <模組>` 靜默丟棄／模組層 list literal 內相對路徑不在掃描面）⇒ `tools/lib/` 兩支被消費的檔在兩支 compat-CI 的 paths 命中皆 0（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-805` 節） | P0 | 修法落地本輪（掃描器 + workflow paths） | fixed@R75｜正則換 `ast` 解析、候選基底改逐層祖先、靜默丟棄升 fail-loud、新增形態 H 掃描器；無法解析 21→0、掃描面 84→113、補齊 19 支漏列檔 |
| DEF-101-816 | 2026-08-04 | R75 四方複審（SD BLOCKING-1） | `tools/check_scheduled_task_drift.py` 對「任務整支不存在」結構性失明：狀態格只有全缺席與設定不符兩格，「部分存在」無判準無測試 ⇒ 一支缺席、另一支設定完美時回 `status=ok`／rc=0 卻同時印「不存在」（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-816` 節） | P1 | 修法落地本輪（偵測器 + 接線 + 測試） | fixed@R75｜新增狀態字 `task_missing`（rc=1），刻意不折進 `drift`（那格在接線層有具名豁免）；缺席行輸出改為說出結論；新增 `--require-installed` |
| DEF-101-815 | 2026-08-04 | R75 四方複審（QA BLOCKING-1） | R74 新建的「雲端 CI 結論」錨從不驗 `head-sha`（換成全零仍全綠）、新鮮度只到日粒度：錨指向上一輪收輪 commit 並記 `red=windows-compat-ci.yml`，而 HEAD 已是下一個 commit、那支已修綠（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-815` 節） | P1 | 修法落地本輪 | fixed@R75｜新增 5 條判準＋回填 SOP（sha 形態／可解析／HEAD 或其祖先／`checked-at` 不早於該 commit 且日粒度須自陳／`red=` 逐字等於 failure 列集合）；shallow clone 明說未驗證不判紅 |
| DEF-101-821 | 2026-08-04 | R75 四方複審（QA NON-BLOCKING-4） | subprocess 編碼衛生鎖的 per-tree 檔數下限已腐化：`tools` 樹實測 files=81 而 floor=18（slack=63），而下限存在的唯一理由就是「掃描面靜默縮小必紅」；下限是首掃數打八折的化石（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-821` 節） | P2 | 修法落地本輪 | fixed@R75｜11 棵樹下限重釘到當回合實測 × 0.95（守住比例 22%→95%），並新增雙邊帶防腐（高於 `max(floor+10, floor×1.25)` 即判下限自己過時、當場紅並印該填的數字） |
| DEF-101-807 | 2026-08-04 | R75 四方複審（Architect BLOCKING-3） | ADR-XPLAT-002 §4.3.1 把 Scan-H 通過判準訂成「三元組逐輪手抄登記」，寫下當回合即不成立（只 R67／R69 兩列）且無機械物，孿生案例卻已上鎖 SC-10（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-807` 節） | P0 | 修法落地本輪（ADR + 機械承接物） | fixed@R75｜廢除手抄登記，改三款可機械檢查判準（三元組一次取齊／UEP 不上升／GLC 量測面未崩塌）；承接者併入既有 `test_adr_xplat001_c1c2_lock.py`；未編造歷史數字 |
| DEF-101-822 | 2026-08-04 | R75 四方複審（QA NON-BLOCKING-5） | `has_utf8_stdio_protection` 是整檔子字串比對，一行註解就能滿足判準；以 tokenize 塗白 COMMENT/STRING 後複算 14 支納管 child 仍 14/14 真命中 ⇒ 無現存假綠、屬潛在缺口（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-822` 節） | P2 | 修法落地本輪 | fixed@R75｜新增 `code_only()` 以 tokenize 等寬塗白 COMMENT／STRING（行號不位移）後再比對，tokenize 失敗＝視為無保護；`is_entry_point()` 刻意不塗白並就地寫明理由 |
| DEF-101-814 | 2026-08-04 | R75 四方複審（SA NON-BLOCKING-1） | ONBOARDING §7 表① 寫 `skipped=10` 並手寫逐項清單，而同 commit 實測 `skipped=43`；受鎖 token `1819` 有 sync 工具更新，`skipped=N` 明載不在鎖內 ⇒ 本輪淨增約 33 筆 skip 零察覺（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-814` 節） | P2 | 修法落地本輪 | fixed@R75｜改為 `skipped=43（量測時點 2026-08-04）`、手寫清單整段移除改指 live 來源 `report_all_skips`；刻意不做 live 鎖（理由寫進 code），改鎖可稽核性 |
| DEF-101-818 | 2026-08-04 | R75 修 DEF-101-817 時副產物揭露 | 11 支既有 AC4 測試在斷言「窗內 14 筆一模一樣的指標值 → ready=True」＝fixture 複製了 production 盲點，落地「零變異即不得認證」時當場翻紅（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-818` 節） | P1 | 修法落地本輪 | fixed@R75｜11 支 fixture 全改帶確定性抖動（值域不跨門檻），兩份測試檔頭立下 fixture 慣例；價值在形態：注入式鑑別力驗不出「fixture 在餵不可能發生的世界」 |
| DEF-101-804 | 2026-08-04 | R75 四方複審（Architect BLOCKING-1） | `.sh` 行尾守門把「這台機器何時 checkout」變成閘門結果：`AutoClaude/tools/hooks/check_sh_eol.py` 只問磁碟含不含 CR ⇒ 本機 144 支被擋、新 clone 0 支，唯一能變綠的動作違反 Copy-on-Evolve（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-804` 節） | P0 | 修法落地本輪（hook + 測試） | fixed@R75｜判準改問 index blob；凍結版樹排除、LATEST 走版本 SSOT；deny 類 hook 全面 fail-open；測試 6→15 支 |
| DEF-101-806 | 2026-08-04 | R75 四方複審（SD BLOCKING-4） | 排程漂移偵測器「缺席」那一向 fail-open：`AutoClaude/tools/run_local_nightly.ps1` 缺席時只印「不當成通過」而 `$schedDriftRc` 留 0 ⇒ nightly 照樣 exit 0，與偵測器本體 `check_scheduled_task_drift.py:179` 的 fail-closed 相反（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-806` 節） | P1 | 修法落地本輪 | fixed@R75｜改白名單、不看 rc（`-notin @('drift','ok','skip')` 即計失敗）；新增成對注入式行為鎖 |
| DEF-101-809 | 2026-08-04 | R75 四方複審（SA NON-BLOCKING-4） | R74 交棒任務書三處數字與磁碟不符：`:65` 缺陷號 787~802 實為 787~803、`:64` `partial@R74` 6 筆實為 7 筆、`:30`／`:101` 帳本 250,596 實為 252,067（全文見 `CrossPlatform_R75_Review_Evidence.md` 的 `## DEF-101-809` 節） | P2 | 修法落地本輪 | fixed@R75｜逐項訂正為當回合實查值；根因＝把「中途量測」當「收輪量測」寫入，訂正處寫成因 |
