# CrossPlatform R68 — 十二維掃描存活缺陷清單（SSOT）

> **本檔是什麼**：R68「Mac and Windows 11 相容性」輪十二維掃描（Scan-A~H/M ＋ 本輪新設
> Scan-N 雙向落差／Scan-T 技術債）產出、且**通過逐維對抗驗證**的缺陷清單。
> 掃描 25 個 agent、74 筆判決 → CONFIRMED 41／DOWNGRADED 28／REFUTED 5，**存活 69 筆**。
>
> **為何不寫進缺陷帳本**：帳本主檔硬上限 262144 bytes（綁 Read 工具單次讀取上限），
> 69 列即使精簡也逾 20KB，本輪動工時餘裕僅 15459 bytes。依 DEF-99-001 政策與
> `tools/archive_defect_log.py` 既有的「主檔留指針、詳情外置」形狀，本檔為詳情面，
> 帳本以 `DEF-101-702` 一列作指針。本檔已登記為具名治理文件（`_GOVERNANCE_DOCS`），
> 受體積守門與指針稽核保護。
>
> 🔴 **修復狀態（`狀態@R69` 欄）**：R68 修復波（11 包）撞週配額、11 包全數未交回結構化
> 回報，本表原本因此不宣稱任何一筆已修。**R69（DEF-101-702）已對 69 筆逐筆到程式碼／
> 設定實查**，判定寫入 `狀態@R69` 欄：`fixed`（R68 波已落地，附複核憑證）／`fixed@R69`
> （本輪修，附鎖與紅綠實測）／`partial`（只落一半，欄內寫明哪一半 open）／`open`（仍存在）
> ／`duplicate`（與他筆同一件事）。**分布：fixed 27＋fixed@R69 10＝37／open 24／partial 5
> ／duplicate 3／invalid 0**（P1：1 fixed、1 partial、1 duplicate；P2：11 fixed、7 fixed@R69、
> 1 partial、4 open、1 duplicate；P3：15 fixed、3 fixed@R69、3 partial、20 open、1 duplicate）。
> 判定一律以現查為準——本欄不是宣稱，是複核紀錄；再次複核請直接跑欄內列出的指令。
>
> **證據等級**：本輪執行平台＝macOS 26.5.2 arm64 真機（Darwin 25.5.0）；**Windows 側本輪未取得真機**（不等於「Windows 從未驗過」，逐輪覆蓋見 `ADR-XPLAT-002` §6；DEF-101-756），
> 所有標 `Windows` 的判決皆為靜態分析／沙箱模擬／CI 對帳，不得讀成「已在 Windows 驗證」。

## 判決分布

| 維度 | 筆數 |
|------|------|
| Scan-A1 生產碼 | 5 |
| Scan-A2 SDD/根層 | 5 |
| Scan-B 淨化/guard | 4 |
| Scan-C CI/排程 | 5 |
| Scan-D 文件 | 7 |
| Scan-E 架構 | 6 |
| Scan-F 載具實跑 | 5 |
| Scan-G backlog 稽核 | 11 |
| Scan-H 護欄自檢 | 5 |
| Scan-M mac 真機 | 4 |
| Scan-N 雙向落差 | 5 |
| Scan-T 技術債 | 7 |

嚴重度：P1 3 筆／P2 24 筆／P3 42 筆。

## 存活缺陷逐筆

| 編號 | 維度 | 嚴重度 | 平台 | 缺陷 | 座標 | 判決 | 狀態@R69 | 修復方向 |
|------|------|--------|------|------|------|------|------|---------|
| R68-00 | Scan-A1 生產碼 | P2 | both | Evaluator.run() 逾時只殺直接子行程，孫行程變孤兒續跑——同一缺陷類別在 pty_wrapper.close() 已雙平台修好（taskkill /T + killpg），evaluator 這條主線零對應防護 | AutoClaude/autoclaude/execution/evaluator.py:38-49（subprocess.run(shell=True, timeout=) → except TimeoutExpired 只回 EvalResult，不做行程樹回收）。同型且**同風險等級**的唯一另一站點：autoclaude/execution/mutation_applier/_conditional.py:45（同樣 shell=True，指令來自 playbook 作者的 mutation.condition_evaluator）。其餘四站經我逐一 sed 讀原始碼後應**降級處理*… | CONFIRMED | **fixed**：kill_process_tree 共用 helper 已落地（evaluator.py:25 ＋ _conditional.py:60），鎖＝tests/test_evaluator_kill_tree.py | 把 pty_wrapper.close() 已驗證的雙平台行程樹終結抽成 utils 共用 helper（POSIX：Popen(start_new_session=True) + killpg SIGTERM→輪詢→SIGKILL；Windows：taskkill /T /F /PID），Evaluator.run 改 Popen + communicate(timeout=) + except TimeoutExpired 走該 helper；同步套用 mutation_applier/_conditional.py:45（同為 shell=True 的作者可控指令）。其餘四站建議只登記行… |
| R68-01 | Scan-A1 生產碼 | P2 | both | checkpoint／mutated-playbook／raw-log 檔名全部由 stem 直出且不做大小寫正規化，在大小寫不敏感檔案系統（macOS APFS／Windows NTFS）上兩個僅大小寫不同的 playbook 共用同一個 checkpoint 檔，resume 時載入到別支 playbook 的執行狀態且零校驗 | AutoClaude/autoclaude/infra/repositories/factory.py:57-62（canonical_playbook_id：db_only → sha256(str(Path(p).resolve()))[:16]；yaml_only/both → Path(p).stem，兩分支皆無 normcase）、infra/repositories/file_state_repository.py:30-35（_path 僅過 _sanitize_log_filename，不做大小寫正規化）與 :74-95（load_latest_by_playbook 直接反序… | CONFIRMED | **fixed@R69**：本包修：auto_resume._checkpoint_matches_playbook（samefile／normcase+realpath 雙判準）＋鎖 tests/core/test_auto_resume.py::TestCheckpointIdentityGuard（修前紅 2/修後綠 4） | (1) **優先且獨立於 id 策略**：load 端補 fail-loud 校驗——`file_state_repository.load_latest_by_playbook` 增加 expected_path 參數（或由 auto_resume._resolve_start 於取得 ck 後比對），`os.path.normcase(os.path.realpath())` 正規化後不符即視為無 checkpoint 並 warn。這一條把靜默錯配變可見，且同時涵蓋「同 stem 不同目錄」的平台無關情境。(2) canonical_playbook_id：yaml_only/both … |
| R68-02 | Scan-A1 生產碼 | P3 | Windows | 整棵生產碼零 `newline=""`：文字模式寫檔在 Windows 一律把 \n 轉成 \r\n，使 rtm_file_sink.append_report_line docstring 明寫的「強制 LF 收尾」在 Windows 上是假的；同一缺陷類別（DEF-101-524/534）已在 AISDLC_SDD v0.30 rule_loader 修過，但至今沒有任何機械掃描把它橫向套到 AutoClaude 生產碼 | AutoClaude/autoclaude/infra/adapters/rtm_file_sink.py:42-52（docstring 第 45 行逐字「強制 LF 收尾」，第 51 行 `with target.open("a", encoding="utf-8") as f:` 無 newline，第 52 行 `f.write(line.rstrip("\n") + "\n")`）；同型未防護站點（原報告列舉，我抽驗 rtm_file_sink 一處並以全樹 grep 反證無任何例外）：local_kb_metric_store.py、translation_learning_sin… | CONFIRMED | **partial**：rtm_file_sink 已補 newline=""（:38/:57）＋鎖 test_rtm_file_sink_newline.py；**AST 掃描鎖（修復方向(2)）未落**，其餘 .jsonl/YAML 站點未逐一處理 ⇒ open | (1) 先修「承諾與實作矛盾」：rtm_file_sink.append_report_line 補 `newline=""`（或把 docstring 的「強制 LF」改成誠實描述），六支 .jsonl append 同步處理，YAML 落盤兩支次之。(2) 落一支與 tools/tests/test_subprocess_encoding_hygiene.py 同構的 AST 掃描鎖：`open()/Path.open()/write_text()` 於文字模式且 mode 含 w/a 時必須帶 `newline=`，否則需行尾 `# newline-ok: <WHY>` 豁免＋stale… |
| R68-03 | Scan-A2 SDD/根層 | P2 | both | bash 3.2／BSD 工具機械鎖的判準集合小於本 repo 自己文件宣告的禁用清單：8 項 BSD 禁令只機械化 3 項、bash4 組漏 local -A／typeset -A／shopt -s globstar／declare -g；沙箱注入 6 項違規後全套 1378 支根層測試仍 rc=0 全綠 | tools/tests/test_bash32_compat.py:59-81（_PATTERNS 12 條）；三份不一致的宣告清單：tools/macos_smoke_local.sh:38-40（8 項，最長）、tools/install_mac_nightly.sh:52-53（3 項）、ONBOARDING.md:383 §8 散文（4 項） | CONFIRMED | **fixed@R69**：本包修：_PATTERNS 補 9 條（local -A／typeset -A／shopt -s globstar／declare -g／stat -c／date -d／timeout／xargs -r／find -printf）＋TestProseBanListIsFullyMechanised 把檔頭散文與判準雙向綁定。修前實測 9 項全漏抓、修後真實六樹零違規 | (a) 三份長度不同的清單（8/4/3 項）收斂成單一資料 SSOT（形態照 tools/lib/bash_probe_spec.py 「資料抽 SSOT、執行邏輯保留多份」判例），由 _PATTERNS 與 macos_smoke_local.sh／install_mac_nightly.sh 檔頭同時消費，加一支比對鎖擋「散文列了但 pattern 沒有」。(b) A 組補 `\b(local\|typeset)\s+-[a-zA-Z]*A\b`、`shopt\s+-s\s+globstar`、`declare\s+-g\b`；B 組補 stat -c、date -d、timeout、xa… |
| R68-04 | Scan-A2 SDD/根層 | P2 | both | LATEST 版 tools/ 的四支 .sh 中有三支（run_tlc.sh／init_project.sh／verify_traceability.sh／run_self_evolution.sh，僅 install_post_commit 例外）在兩平台的任何 CI、smoke、nightly、pre-push 都零執行覆蓋，護欄層對它們只有靜態抽取式鎖——這是上面兩筆能長期存活的結構原因 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/{init_project.sh, verify_traceability.sh, arch_fitness/run_self_evolution.sh, fsm_runtime/formal/run_tlc.sh}（LATEST tools 共 5 支 .sh，僅 install_hooks/install_post_commit.sh 有執行覆蓋；原 title 稱 4 支，訂正為 5 支中 4 支）；覆蓋面登記表 tools/tests/test_smoke_ci_sync.py:722 _CI_STEP_LOCAL_C… | CONFIRMED | **open**：macos_smoke_local.sh／windows_smoke_local.ps1 皆無 run_tlc／init_project／verify_traceability／run_self_evolution 的可啟動性 step（grep 零命中） | 在 tools/macos_smoke_local.sh 與 tools/windows_smoke_local.ps1 各加一段「LATEST 腳本可啟動性」step：run_tlc --help（實測 rc=0）＋--install-only 斷言 rc 屬檔頭宣告集合且 stderr 無 `unbound variable`／`No such file or directory`；init_project --help；verify_traceability 對 tmp 空 docs（實測 rc=0）；run_self_evolution 預設 dry-run（⚠️ 該檔 :29 有 `… |
| R68-05 | Scan-A2 SDD/根層 | P2 | both | LATEST 兩支 .sh（run_tlc.sh／run_self_evolution.sh）無條件 dot-source 位於框架根之外四層的 monorepo-only windowsapps_guard.sh；三支 .ps1 對應物全部有 Test-Path + 可行動訊息，install_post_commit.sh 也有 [ -f ] 降級——同家族內兩種紀律，且經框架自帶 init_project.sh 部署後路徑逃出使用者專案外 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/arch_fitness/run_self_evolution.sh:37（裸 dot-source）；AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/formal/run_tlc.sh:58-59（REPO_ROOT 推導＋裸 dot-source，非 monorepo 佈局下路徑逃出使用者專案）；正確對照組：同版 tools/install_hooks/install_post_commit.sh:34-42、tools/arch_fitness/run_self_evoluti… | CONFIRMED | **fixed**：run_tlc.sh:56-59 與 run_self_evolution.sh 已改 `[ -f GUARD_SRC ]` 守衛＋缺席降級 | 最小修法：run_tlc.sh:56-59 與 run_self_evolution.sh:37 改成與同版 install_post_commit.sh:34-42 同形的 `[ -f … ]` 守衛，缺檔時比照 .ps1 印可行動訊息並以明確 rc 退出（run_tlc 檔頭已定義 2＝環境錯誤；run_self_evolution 已用 7）；run_tlc.sh 的 REPO_ROOT 推導另須修正。決策面須在 ADR-XPLAT-002 明白裁定：LATEST 版樹是「只在 monorepo 內可執行」還是「可獨立交付」；若前者，init_project.sh 不該交付這兩支（或改 … |
| R68-06 | Scan-A2 SDD/根層 | P3 | macOS | init_project.sh 在使用者面輸出寫死「Claude Code Skills (33個)」，實測部署 45 個；.ps1 對應物無此行＝單邊 stale 數字，且違反本 repo 自訂的 Scan-H 判準③（不得寫死可由程式現查的數字） | AISDLC_SDD/AISDLC_SDD_v0.30/tools/init_project.sh:342；同樹擴散點 AISDLC_SDD/AISDLC_SDD_v0.30/tools/README.md:169；權威計數源 AISDLC_SDD/FRAMEWORK_STATUS.md:19（42） | CONFIRMED | **fixed**：init_project.sh 已改 find|wc -l|tr -d ' ' 現算，不再寫死 33 | 把 :342 改成不寫死（拿掉括號，或執行期現算 `$(find "${target_dir}/.claude/skills" -mindepth 1 -maxdepth 1 -type d \| wc -l \| tr -d ' ')`——不可用 `ls \| wc -l`，那正是原證據算成 45 的來源；`\| tr -d ' '` 在 BSD/GNU 兩側一致，同樹 verify_traceability.sh:77 已用同手法）。同步處理 tools/README.md:169。將 Scan-H 判準③ 的射程從「護欄層註解」擴到「腳本使用者面 stdout」，並讓需計數的輸出一律消費… |
| R68-07 | Scan-B 淨化/guard | P3 | Windows | 四處保留裝置名清單缺 Windows 官方命名規範列出的上標變體 COM¹/COM²/COM³/LPT¹/LPT²/LPT³，敵意輸入原樣輸出 | AISDLC_SDD/scripts/component_sanitizer.py:41-43；AutoClaude/autoclaude/utils/logger.py:99-101；tools/check_ntfs_paths.py:119；tools/git-hooks/pre-commit:117（case 交替 `COM[0-9]\|LPT[0-9]`） | CONFIRMED | **fixed**：四處正則皆已在尾端追加 COM[¹²³]|LPT[¹²³]（component_sanitizer:54／logger:107／check_ntfs_paths:129／pre-commit） | 順序不可顛倒：**先** 在 Windows 11 真機以拋棄式 repo 實測 `COM¹.log`／`com².txt` 的 `git clone`（core.protectNTFS）與 Win32 `CreateFile` 行為，比照 R60 對 CONIN$／CLOCK$ 的作法。ACCEPT 者刻意不納入並在四處註解明文記載「已實測不納入」（避免下一輪重新發現）；確認為保留名者，四處正則一律追加在**清單尾端**（勿插入 CON/PRN/AUX/NUL 之間，會讓三處同時掉出 `_RESERVED_LIST_ANCHOR` 錨①而全綠），並在四方樣本表補上標樣本。可與上一筆合併評估：… |
| R68-08 | Scan-C CI/排程 | P1 | both | 兩支 nightly-full（Windows/macOS 深度回歸）自 2026-07-14 起 18 天零成功執行，且三道現有哨兵在結構上都偵測不到——2026-08-01 帳務恢復、四支 CI 全綠之後仍無人補跑 | .github/workflows/windows-compat-ci.yml:982（windows-nightly-full）/:1166（windows-nightly-alert）, .github/workflows/macos-compat-ci.yml（macos-nightly-full/alert 同形）, tools/dev_start.py:1706（_check_ci_liveness）, tools/dev_start.py:1619（_check_nightly_heartbeat） | CONFIRMED | **partial**：偵測面已修：tools/lib/ci_liveness.py 逐軌查 schedule∪workflow_dispatch＋dev_start:1792 消費。**補跑 nightly-full 未做**——受本包硬禁令（禁 gh workflow run／rerun，GitHub 額度）阻擋 ⇒ 該半留 open，見帳本 DEF-101-703 | 訂正原 fix_direction 第 (2) 點——把陳舊度偵測放進 root-infra-ci **並非** 讓偵測器與被偵測者脫鉤：我實測今日 root-infra-ci 亦曾 failure（30697855439/30695971124），且停擺期間它與 nightly-full 同屬 GitHub Actions 計費平面，帳單一掛它也跑不起來，只能抓「非帳務型」陳舊（cron 沒觸發、job 被停用）。真正的平面外偵測只有本機 `tools/dev_start.py`（純 read-only gh API、零 Actions 額度）。故建議：(1) 立刻對 HEAD 各 `wor… |
| R68-09 | Scan-C CI/排程 | P2 | both | nightly-alert 的結論判讀 fail-open：凡不是字面 "failure" 一律當綠，會自動關閉 P1 issue 並留言「已恢復綠燈」——cancelled／timed_out／job 改名三情境沙箱全部復現 | .github/workflows/windows-compat-ci.yml:1186-1213（check step 的 jq 選擇子與 if 判讀）/:1215-1231（開單）/:1233-1245（`if: steps.check.outputs.failed == 'false'` → gh issue close）, .github/workflows/macos-compat-ci.yml:816-853（同形） | CONFIRMED | **fixed**：nightly-alert 已改白名單 fail-closed（只有 conclusion==success 才關單） | 改黑名單為白名單：只有 `conclusion == "success"` 才走關單 step；`failure\|cancelled\|timed_out\|skipped\|unknown\|null` 一律走開單／留言路徑並在 body 逐字記錄實際 conclusion（尤其要能區分「真綠」與「根本沒跑」）。同時把 job 顯示名抽成 workflow 層 `env`（如 `NIGHTLY_JOB_NAME`）供 `name:` 與 jq 選擇子共用，消滅改名漂移面。機械鎖補在既有 `tools/tests/test_workflow_permission_concurrency_lo… |
| R68-10 | Scan-C CI/排程 | P2 | both | `_check_ci_liveness` 以 `gh run list --limit 1` 代表「GitHub CI 活性」——任何一次 push 的綠燈都會遮蔽週頻／日頻工作流的長期死亡，且它是 CI 停擺期唯一的本機雲端訊號 | tools/dev_start.py:1706-1763（`_check_ci_liveness`；subprocess 引數在 1734-1738，`conclusion == "success"` → print ✅ 在 1757-1759） | CONFIRMED | **fixed**：_check_ci_liveness 已改逐軌＋自 YAML 讀 cron 算期望間隔 | 改為逐軌查：對明列的兜底軌（至少 windows-compat-ci／macos-compat-ci 的 schedule 軌、autoclaude-ci、root-infra-ci、aisdlc-sdd-ci）各跑一次 `gh run list --workflow=<f> --event=<schedule\|push> --status=success --limit 1`，把 `updatedAt` 與該 workflow 自身 cron 週期（自 YAML 的 `on.schedule.cron` 讀出算期望間隔，避免第二份會腐化的字面清單）比對，超過 2 倍週期即 `_warn`。… |
| R68-11 | Scan-C CI/排程 | P3 | linux-ci | ONBOARDING.md §6.1 仍以「本 workflow 現因 CI 帳務停擺在數秒內失敗、下列各道實際上從未在雲端執行」描述 root-infra-ci，實況已於 2026-08-01 12:31 起連三次 success——而該句被明文指定為全文件 CI 取證宣稱的基準 | ONBOARDING.md:166（root-infra-ci 列的 ⚠️ 句，含「一律以此為準」全域指向）, ONBOARDING.md:168（macos-compat-ci 列的「compat-CI 因帳務停擺多輪未真正執行」）, docs/06_quality/AutoSDD_Defect_Log.md:52（DEF-101-081 狀態欄，最後更新停在 R42 2026-07-24） | CONFIRMED | **fixed**：ONBOARDING:166 已改區間敘述並移除全域解讀指令 | （1）把 ONBOARDING.md:166/:168 改為區間式敘述：「歷史區間 2026-07-14 ~ 2026-08-01 因 DEF-101-081 停擺；自 2026-08-01 12:31Z 起恢復，最新取證一律現查」，移除「一律以此為準」的全域指向（同 ADR-XPLAT-002 §6 邊界 1／§8.3 對「不得把外部環境狀態寫成穩定前提」的既有裁定，屬沿用慣例而非新造政策）；七個站點一併掃過。（2）同步回填 DEF-101-081 狀態欄（帳務已恢復的實測憑證＋殘餘行動項＝對 HEAD 補跑 nightly-full）——注意帳本檔由專責 agent 處理，本輪掃描員不碰。… |
| R68-12 | Scan-D 文件 | P2 | both | 「GitHub Actions 帳務停擺、CI 從未真正執行」這個前提已於 2026-08-01 全面翻轉（四支 workflow 皆 success），但 ONBOARDING §6.1／§9 仍把它寫成現況，且 :166 還下了一條涵蓋全文件的解讀指令；ADR-XPLAT-002 §6 已登記翻轉 ⇒ 兩份權威文件對同一事實結論相反 | ONBOARDING.md:166（§6.1 root-infra-ci 列表頭句，全文件級解讀指令）、ONBOARDING.md:441、ONBOARDING.md:445④；tools/macos_smoke_local.sh:28；tools/tests/test_pre_push_dispatcher.py:552-553；tools/tests/test_find_git_bash_parity.py:864 | CONFIRMED | **fixed**：同上；:445 亦已改寫為不代言平台的現查式 | (a) 刪掉 :166 那條全文件級解讀指令，改為「各道是否在雲端跑過一律現查 `gh run list`，可用性沿革見 ADR-XPLAT-002 §6 邊界 1 逐輪覆蓋表」。(b) SC-5 射程由「ADR §8 表本體」擴到 `ONBOARDING.md` 與 `tools/**/*.{sh,py}` 的 WHY 註解，違規出口＝把逐字原句移進散文／史料區（比照 §8.3），並在 `test_adr_xplat001_c1c2_lock.py` 補正控／反控。(c) `test_pre_push_dispatcher.py:552`／`test_find_git_bash_parity… |
| R68-13 | Scan-D 文件 | P3 | linux-ci | ONBOARDING §6.1 宣稱 root-infra-ci「現行**十二道**輕量檢查」並就地列舉 1~12，實際為十四道；被 ONBOARDING 自己指定為權威來源的 workflow 檔頭已寫「十四道」——被指定的真相源是對的，指向它的那句話反而錯 | ONBOARDING.md:166（§6.1 root-infra-ci 列）↔ .github/workflows/root-infra-ci.yml:4（檔頭「十四道」） | CONFIRMED | **fixed**：ONBOARDING:166 的「十二道」與 1~12 列舉已物理刪除，改指向 workflow 檔頭（現為十五道）。**本包另修其遺留物**：該句原指向 `§9.1 逐輪平台覆蓋表`，而本文件從無 §9.1 ⇒ 死指標，已改指 ADR-XPLAT-002 §6 邊界 1，並新增 TestOnboardingSelfSectionRefsResolve（修前紅、修後綠） | 優先：刪除 ONBOARDING:166 的「十二道」與內嵌 1~12 整段列舉，只留「道數與內容一律見 `.github/workflows/root-infra-ci.yml` 檔頭」——與該句自己宣告的意圖一致，物理消滅第二站點（同 §7 pytest 基線的唯一站點政策）。若堅持保留列舉，才在 `test_root_infra_parity.py` 加跨檔字面鎖（ONBOARDING 的中文數字 token 必須等於檔頭 token），且**必須同時把 `_CJK_DIGITS` 由上限「十二」擴到十四以上**，否則新鎖會在 `.get()` 回 None 時假綠。 |
| R68-14 | Scan-D 文件 | P3 | both | ONBOARDING §9 註腳的 ruff 存量 baseline「1,147 筆」既 stale 又不可重現——未載明量測指令與根目錄，實測從 `AutoClaude/` 下為 942、從 repo 根為 3829，兩者皆非 1,147（ruff 版本與註記所載同為 0.15.21） | ONBOARDING.md:448（§9 表下註腳，DEF-101-018 段） | CONFIRMED | **open**：ONBOARDING:448／AutoClaude/pyproject.toml:32／帳本 DEF-101-018 三處「1,147」仍在，且仍未載明 cwd | 二擇一不要折衷：(a) 留數字就比照 LOC 那格接進 `tools/sync_onboarding_baselines.py` 的 `_SPECS` 現場實跑取值，並在文字裡寫明 cwd；(b) 不值得每輪付全庫掃描代價就把數字整個刪掉，只留「現查指令＋判準是單調下降」，同 ADR §8 表頭規則 3（完成判準欄禁寫死量測常數）。無論哪條都必須補 cwd——monorepo 裡一個沒有根目錄的計數宣稱本質不可證偽。 |
| R68-15 | Scan-D 文件 | P3 | both | `sync_onboarding_baselines.py --check-snapshot` 會把 `pgextras=absent` 這個 provenance 逐字印在 ✅ 旁邊卻從不驗證它，於是在 PG extras 已污染的 `.venv` 上仍回 rc=0 綠燈——同一支工具的 `--write` 路徑對同一條件是 rc=2 硬拒 | tools/sync_onboarding_baselines.py:1347-1355（`--write` 的 pgextras 硬拒）vs :938-943 `check_snapshot()` → `snapshot_report()`（只比四棵樹指紋，全程未呼叫 :974 的 `pg_extras_state()`）；ONBOARDING.md:295 `snapshot-fingerprints-darwin` 錨 | CONFIRMED | **open**：check_snapshot() 仍只比四棵樹指紋，全程未呼叫 pg_extras_state() | 在 `--check-snapshot` 的**本機平台欄**判準加一項零成本 provenance 對帳：現查 `pg_extras_state()` 與 docker 狀態，與該欄錨點記錄不符時**絕不可仍印 ✅**，降為明確 ⚠️ 並寫出具體措辭（「你現在這個環境重現不出這欄數字，原因是 pgextras=present」）；rc 是否轉紅可討論，傾向不轉紅（避免污染 root-infra-ci 第 14 道與 pre-push 的紅綠語意）。判準與 §7 既有的「本機平台欄才有牙」原則一致。另建議把 §7 的污染探針指令由 R59 註記(:335)上移進 §7 指令區塊，讓照 §7 驗… |
| R68-16 | Scan-E 架構 | P3 | both | 架構缺陷：SC-4「不得把當下這台機器寫成常數」的射程只到兩份 ADR，同型 stale 平台前提在護欄層程式碼的 docstring 逐字存活並且已為假 | tools/tests/_ps_engine.py:17,29, docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1214-1221 | CONFIRMED | **fixed@R69**：本包修：_ps_engine.py:17/:29 兩處「本機無 pwsh 7」改為輪次屬性＋現查形態（本機實測 `which pwsh` → /opt/homebrew/bin/pwsh，原句為假）；新增 TestNoStaleLocalEngineClaims（修前兩行皆命中、修後綠，且對「本機根本沒有 5.1（macOS/Linux）」這類通則敘述零誤報） | (1) 就地改寫 :17/:29 為輪次屬性＋現查形態（照 §6 邊界 1 R67 手法，不改成指向 macOS 的對稱句）——這一步單獨即消滅誤導，成本最低，建議與 F1 的 target_grammar_engine() 拆分同一次落地（同一段 docstring）。(2) 擴 SC-4 射程前必先補動詞列舉（至少加「無」「只剩」「缺」），否則對病灶行是恆綠假修復——此點為本輪實測新增。(3) 射程擴張需收窄以避開實測到的 8 筆中性句：只掃「斷言狀態」句型（如 本機(無\|沒有\|只有\|是)\s*\S+ 且後不接「否」「哪」「是否」），沿用 stale-premise-ok: 同行豁免… |
| R68-17 | Scan-E 架構 | P3 | both | 架構缺陷：「該用哪一個 PowerShell」的 SSOT 只治理測試層，生產層仍有 2 份獨立實作、且反增生掃描面明文不含生產樹——這是本輪對「有無新的可收斂面」的具體肯定答案 | tools/tests/_ps_engine.py:31, tools/tests/test_ps_engine_ssot.py:282-288, AutoClaude/tools/local_ci_gate.py:262, tools/dev_start.py:891, tools/dev_start.py:1503（原文漏列）, tools/integration_gate_core.py:120（原文漏列） | CONFIRMED | **open**：production_engine() 只治理測試層；AutoClaude/tools/local_ci_gate.py:262、dev_start.py:891/:1503、integration_gate_core.py:120 四處生產端仍行內挑選 | 維持原文 (1)~(4) 四步，兩處修正：(a) 接生產端的清單改為 4 處：AutoClaude/tools/local_ci_gate.py:262、tools/dev_start.py:891、tools/dev_start.py:1503、tools/integration_gate_core.py:120。(b)「fail-loud 指路安裝」分支要克制：四處皆在 Windows-only 分支、5.1 必然存在，建議只在 production_engine() 回 None 時 fail-loud，正常路徑與現行逐字等價（Rule 3）。(c) 搬層 tools/tests/_ps… |
| R68-18 | Scan-F 載具實跑 | P1 | macOS | tools/dev_start.py 頂層 import tomllib（3.11+ 才有），而薄殼無 .venv 時退回系統 python3＝macOS 3.9 → ONBOARDING §2.1 明文承諾的「全新機器可直接執行 dev_start」在 macOS 上 100% 崩在 ModuleNotFoundError | tools/dev_start.py:51（頂層 `import tomllib`）、tools/dev_start.sh:42-52（直譯器候選無版本閘） | CONFIRMED | **fixed**：dev_start.py:84-91 已加 sys.version_info 前置閘＋tomllib 延後 import；dev_start.sh 已改用 pick_python_ge_min SSOT | 維持原修法且兩層都要：(a) `tools/dev_start.py` 檔頭加 `sys.version_info` 前置檢查（在 `import tomllib` 之前，印出與 `bootstrap_core` 同款「brew install python@3.11／或先跑 bash tools/bootstrap.sh」指引），或把 tomllib 延遲到 `_deps_hash()` 內 import；(b) `tools/dev_start.sh:44-47` 的候選序比照 `bootstrap_core.pick_python()` 改為 `python3.11 → python3.… |
| R68-19 | Scan-F 載具實跑 | P2 | both | 三支閘門入口＋smoke 對 `--help`／未知旗標一律靜默跑完整套；`ci-gate.sh --full-tlc` 打錯字會靜默降級成「不跑 TLC」卻仍印全綠 rc=0 — R67-F9 只修了 bootstrap，且 bootstrap_core 的 docstring 明文宣稱 integration_gate_core 已 fail-loud（假話） | tools/integration_gate_core.py:42-47（`add_help=False`＋`parse_known_args`）＋:241（parse_args 在 `_hooks_liveness_advisory()`／五段落之前但不擋未知旗標）、AISDLC_SDD/scripts/ci-gate.sh:96-98 + :174 + :288、AutoClaude/tools/local_ci_gate.py:88-108、tools/macos_smoke_local.sh（全檔無 argv 處理）、tools/bootstrap_core.py:363-364（假宣… | CONFIRMED | **fixed@R69**：integration_gate_core／ci-gate.sh 早前已修；**本包補完最後兩處**：AutoClaude/tools/local_ci_gate.py main() 攔 -h/--help（鎖 tests/tools/test_local_ci_gate.py，修前紅 2）、macos_smoke_local.sh 新增 _usage＋未知旗標 rc=2（鎖 test_smoke_ci_sync.TestMacSmokeCliContract） | 沿用原修法並收斂成可機械檢查的 CLI 契約：(a) `integration_gate_core.parse_args` 改 `add_help=True` + `parse_args`（非 `parse_known_args`），並比照 `bootstrap_core.main()` 391-398 的 `except SystemExit → return code` 在**任何副作用（含 `_hooks_liveness_advisory`）之前**解析；(b) `AutoClaude/tools/local_ci_gate.py` 攔 `-h/--help` 印既有用法段；(c) `… |
| R68-20 | Scan-F 載具實跑 | P2 | macOS | dev_start 的 launchd nightly 探針只比對 label、不驗證已載入的排程是否指向本 checkout — 第二個 clone／搬過家的 repo 會被告知「launchd 已載入、尚未跑過第一輪（正常）」，而該 repo 其實完全沒有排程 | tools/dev_start.py:1526-1546（`_launchd_nightly_loaded()` 只比對 `cols[2] == _NIGHTLY_LAUNCHD_LABEL`）、tools/dev_start.py:1643-1646（缺席分支文案） | CONFIRMED | **fixed**：_launchd_plist_target() 已讀 plist ProgramArguments 比對本 checkout，讀不到即回 None 不謊報 | `_launchd_nightly_loaded()` 三態擴四態：命中 label 後讀 `~/Library/LaunchAgents/<label>.plist` 的 `ProgramArguments[1]`（或 `launchctl print gui/$UID/<label>`），與 `ROOT/"AutoClaude"/"tools"/"run_local_nightly.sh"` 做 `Path.resolve()` 比對，不等時回 `"other-repo"` 並改文案為「launchd 已載入，但指向另一份 checkout（<path>）— 本 repo 無 nightl… |
| R68-21 | Scan-F 載具實跑 | P3 | macOS | 以 zsh（macOS 預設 shell）執行 bash 薄殼時 ${BASH_SOURCE[0]} 未定義 → SCRIPT_DIR 解到錯誤目錄 → WindowsApps guard source 失敗、is_real_python_candidate 變成 command not found → 印出與事實相反的「找不到 python／請先安裝 Python >= 3.11」 | tools/bootstrap.sh:11+16-25、tools/integration_gate.sh:15+19-23、AutoClaude/tools/local_ci_gate.sh:15+19-23、tools/macos_smoke_local.sh:49+65-70（四支皆 `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` + 非致命 `.` source） | CONFIRMED | **partial**：本包修 macos_smoke_local.sh（BASH_VERSION fail-loud＋guard source 硬錯，修前 zsh 實測重現「找不到 python」誤導訊息）。**bootstrap.sh／integration_gate.sh／AutoClaude/tools/local_ci_gate.sh 三支未動**——它們受 check_wrapper_thinness.py 內容 hash 釘選，改動須同步重釘，而該檔正被他包修改中 ⇒ 留 open | 原修法可用，建議兩者都做：(a) 四支開頭加 `[ -n "${BASH_VERSION:-}" ] \|\| { echo '❌ 本腳本需以 bash 執行：bash <path>（macOS 預設 zsh 不相容 BASH_SOURCE）' >&2; exit 2; }`（一行、fail-loud、訊息正確）；(b) 把 `. "$SCRIPT_DIR/lib/windowsapps_guard.sh"` 的失敗改為硬錯（`. "$f" \|\| { echo "❌ 無法載入 $f"; exit 2; }`）——這才是「guard 消失卻無人知」的結構成因，對 bash 路徑同樣有防護價值… |
| R68-22 | Scan-G backlog 稽核 | P3 | both | DEF-101-689 要求的「修復包自報 status ↔ 帳本狀態欄」機械對帳，全 repo 零機械物——且該建議本身只活在一個狀態已標 `fixed@R67R2` 的列的散文裡 | docs/06_quality/AutoSDD_Defect_Log.md HEAD :144（工作樹現為 :131）DEF-101-689 | CONFIRMED | **fixed**：check_defect_log_crossref.py:899+ 已落「修復包自報 status ↔ 帳本」集合差對帳 | 與第 7 筆同批處理：修復包交件時附 `{DEF-ID: 自報status}` 清單，收輪前以一支比對腳本對 `_load_ledger_status()` 做集合差，不一致 rc=1 並接進 pre-push root-infra leg。同時把該列分流欄由「下一輪加一道機械對帳」改為「未指派＋上述可執行解鎖條件」，讓它至少能被第 1 筆補完後的判準抓到。 |
| R68-23 | Scan-G backlog 稽核 | P3 | both | 角色式承接（「下一個 macOS 真機輪」）被 ADR §8 表頭規則明訂為合法且「可機械查（如 `uname -s`）」，但全 repo 零機械消費者——本輪（Darwin 25.5.0）實際已觸發 ADR §8 item 13 / Phase 3 第 1 列，無任何機制告知 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1051-1052（表頭規則 1a）、:1087（§8 item 13）、:878-895（§5 Phase 3 與 P3-V1／P3-V2 現查指令）；docs/06_quality/AutoSDD_Defect_Log.md DEF-101-693 | CONFIRMED | **open**：全 repo 仍無角色求值器（grep platform.system／_ROLE_PREDICATES 於 test_adr_xplat001_c1c2_lock.py 零命中） | 在既有 `TestSection91*` 宿主內加一支角色求值器：解析 §8 承接者欄的具名角色，以 `platform.system()` 求值，成立時以醒目訊息印出「本輪已觸發：item N」、不成立時印 SKIP 標記餵給 Scan-M 未覆蓋面登記。**刻意不設 rc 硬擋**（在對的平台上會永紅）；改由該輪收輪時就地回填「已觸發／已交付」或「觸發但本輪不做＋理由」。本輪即應先就地標註 item 13 已觸發、Phase 3 第 1 列剩餘 ③ 為本輪可執行缺口。 |
| R68-24 | Scan-G backlog 稽核 | P3 | both | ADR §8 item 7／8／11 與 Phase 2-B 全數阻塞於 PM signoff，§8.1 signoff 容器自 R60 建立起連續八輪空白，無任何機械物量測「零回執輪數」——「未指派」成為合規的永久停車位 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1090-1098（§8.1 signoff 記錄）、:1080-1086（§8 item 7／8／11） | CONFIRMED | **open**：§8.1 signoff 零回執輪數仍無機械量測 | 為「阻塞於人類決策」另立老化判準：§8.1 連續 N 輪無回執即由收輪閘門印出具名清單，要求該輪主控二擇一——把問題直接送到使用者面前取得 signoff，或明文降維（item 7 自己已寫出降維選項：「只量不判的報表，零 signoff 需求」）。宿主沿用已接線的 `TestSection91*`；輪數來源用 §8.1 表列日期而非帳本輪號（避開第 2 筆的循環）。同樣建議 warning-only 不硬擋，理由同第 9 筆。 |
| R68-25 | Scan-G backlog 稽核 | P3 | both | 空話式解鎖條件的代表：DEF-101-205 自 R14 起 open 五十餘輪，解鎖條件逐字為「擇機」——無輪號、無「未指派」、無觸發條件，且它宣稱的缺口實測至今完全成立 | docs/06_quality/AutoSDD_Defect_Log.md DEF-101-205（HEAD :64）；ONBOARDING.md:149（755/644 執行權限政策句，零機械守門） | CONFIRMED | **fixed**：DEF-101-205 分流欄已由「擇機」改為「承接輪次：未指派」 | 與第 1 筆同批。針對 DEF-101-205 這種「低 ROI 但缺口真實」的存量，建議降維結案而非補輪號：落一支十行內的 exec-bit 斷言（`git ls-files -s` 比對 ONBOARDING.md:149 那張具名 755 清單——政策句本身即 SSOT，可比照本 repo 既有的「散文 ↔ 程式常數雙向綁定」手法防兩邊漂移）把它關掉；或改為 `wontfix＋理由` 明文承認不做。留在 open 且寫「擇機」是三種選項中唯一沒有資訊量的一種。 |
| R68-26 | Scan-H 護欄自檢 | P2 | both | `test_root_infra_parity` 的兩道數量下限自 R55／R60 未隨新增守門上調（實測 9 vs 下限 7、14 vs 下限 12），使最近兩道根層守門可從 CI step＋檔頭＋pre-push 迴圈整組刪除而全套根層閘門仍 rc=0——該註解自己逐字記載過上一次同型復發 | tools/tests/test_root_infra_parity.py:84-89（`assertGreaterEqual(len(ci_tools), 7)`）、:172-176（`assertGreaterEqual(len(items), 12)`）、:231-232（SDD 消費檔 `>=5`） | CONFIRMED | **fixed@R69**：本包修：三道下限由 7/12/5 重釘為 9/15/9，並新增 TestCountFloorsDoNotDriftBehindReality 後設鎖（餘裕≥2 即紅）。後設鎖首跑即揪出第三處 stale（SDD 消費檔 現查 9／原釘 5）；注入刪一道守門實測轉紅 | 照 `_FROZEN_GUARD_FILE_COUNT` 的 `test_frozen_guard_count_matches_the_worktree` 形狀，把兩道下限由人工常數改為現查等值鎖（多退少補都紅），或改為具名凍結集合＋增減須同步。另補一支後設鎖：凡 `assertGreaterEqual(len(X), N)` 形態，若 `len(X) - N >= 2` 即紅並指路重釘（本檔至少三處，`tools/tests/` 內另有多處帶「現況 N 支」散文者）。修復包必須附本項的同款注入實測（刪整道守門 → 必須轉紅）作為鑑別力證明。 |
| R68-27 | Scan-H 護欄自檢 | P2 | both | SC-6（禁止在 ADR §9.1 寫死不變式條數）的數字列舉停在 `(三\|四\|五\|六)`，而該節條數已成長為 8 ⇒ 今天唯一寫得出來的違規形態（七／八／阿拉伯數字）全部漏抓；其 teeth 注入樣本也凍在歷史值「四條」 | tools/tests/test_adr_xplat001_c1c2_lock.py:2056-2058（`_SC6_RE`）、:2214（`_SC6_INJECT = "本節共四條可轉紅不變式，逐條照抄即可。"`）；規格側 docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1231-1237（SC-6 判準與 grep 字面）、:1276-1281（邊界 (d) 只點名 SC-4／SC-5／SC-2） | CONFIRMED | **fixed@R69**：本包修：_SC6_RE 由列舉 (三|四|五|六) 改為 [〇零一二三四五六七八九十百\d]+ 泛化、_SC6_INJECT 改為以 §9.1 現查條數合成；新增 TestSc6PatternIsNotEnumerationBound（修前七/八/十/阿拉伯數字四形態全漏抓，修後全命中且真實 ADR 零誤報） | `_SC6_RE` 改為不依賴列舉：偵測 `[〇一二三四五六七八九十百\d]+\s*條` 與『不變式／可轉紅』的鄰接；`_SC6_INJECT` 由固定字串改為以現查條數 `len(_SC_DECL_RE.findall(adr2_section_91(...)))` 動態合成，使注入樣本永遠等於「今天最可能被寫下的那個錯值」。同步把規格側 :1237 的 grep 字面改成同一個泛化樣式（兩處是同一語意兩份實作，必須同改），並在 §9.1 邊界 (d) 補列 SC-6。落地時須附六形態紅綠表（含七／八／阿拉伯數字必紅、真實文件必綠）。 |
| R68-28 | Scan-H 護欄自檢 | P3 | both | `_EXEMPT_SH_FILES`（WindowsApps guard bash 側前瞻鎖的附理由豁免表）無 stale 自檢，死豁免條目注入後零訊號；同檔的 `_KNOWN_HOOK_DIRS` 與姊妹 PS1 側都有，屬單邊缺口，且維度表對此家族的描述已因此失準 | tools/tests/test_windowsapps_guard_bash_parity.py:158-181（`_EXEMPT_SH_FILES` 表本體，現有兩筆）、:881 與 :921（唯二消費點，皆為 `if rel in known_relpaths or rel in _EXEMPT_SH_FILES: continue`）；對照組同檔 :737-758（`_KNOWN_HOOK_DIRS` 的 unknown/vanished 雙向自檢）與 :760-767（`test_known_hook_dirs_exist_on_disk`） | CONFIRMED | **open**：_EXEMPT_SH_FILES 仍無 is_file()／有效性自檢（對照組 _KNOWN_HOOK_DIRS 有） | 照同檔 `_KNOWN_HOOK_DIRS` 已有的形狀補兩支：(a) 表內每個相對路徑必須 `is_file()`（檔案被刪／改名即紅）；(b) 表內每一筆都必須真的落在前瞻掃描面內、且在移除豁免後真的會被判為「未收斂」（否則屬已無作用的死條目）。落地須附「注入 THIS_FILE_NEVER_EXISTED.sh 即紅、真實兩筆保持綠」的紅綠實測。同時修 CrossPlatform_Scan_Dimensions.md:153 的並列敘述——寫清楚哪一側的哪一張表有自檢，避免同一句話再為單邊事實背書；`_EXEMPT_PS1_FILES` 雖為空集合，補一支「非空時必須通過同款自檢」的前瞻… |
| R68-29 | Scan-H 護欄自檢 | P3 | macOS | SC-1（未加引號 `--include=<glob>` 的 zsh nomatch 假驗證鎖）掃描面只有兩份 ADR，而同族缺陷在活文件仍有實例——包含帳本 `open` 列的解鎖／改派實查指令；§9.1 邊界 (a) 把其餘檔案交棒給「其他修復包／後續輪」，是護欄層自己違反硬規則②／③ 的無主交棒 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1188-1196（SC-1 規格與掃描面）、:1272-1274（邊界 (a) 的無名交棒）；tools/tests/test_adr_xplat001_c1c2_lock.py（SC-1 實作，`_adr_pair()` 只吃兩份 ADR）；活實例＝docs/06_quality/AutoSDD_Defect_Log.md:77,99,103,107（前三筆 status open）、docs/06_quality/CrossPlatform_R60_Fix_E… | CONFIRMED | **open**：SC-1 標的仍為 _adr_pair() 兩份 ADR | ① SC-1 標的由「兩份 ADR」擴為 git tracked `*.md`＋`*.sh`＋`*.ps1`＋`*.py`＋`*.yml`，排除 `AutoSDD_Defect_Log_archive_*` 等逐字保全歷史檔——比照 `test_extras_quoting_zsh_safety._SCAN_PATHSPECS`／`_EXCLUDED_SUBSTRINGS` 既有慣例並共用同一份掃描面 SSOT。② 主帳本仍 open 的 DEF-101-336／422／435 解鎖指令就地加引號；**訂正記載時請如實寫「重跑後結論不變、原指令載具有誤」**，勿宣稱結論被推翻（我已實測結論成立… |
| R68-30 | Scan-M mac 真機 | P2 | macOS | `--status` 與 `dev_start` 都丟棄 `launchctl list` 第 2 欄（last exit status），導致「排程每晚照跑、載體每晚在寫心跳前就非零退出」被兩支官方工具一致誤報為「尚未跑過第一輪（正常）」＋ rc=0 | tools/install_mac_nightly.sh:294-297（`_row` 只用 `$3 == l` 過濾，第 2 欄原樣印出不解讀）、:122-125（心跳缺席文案「排程可能未啟用或尚未跑過第一輪」）、:170-172（覆蓋連續性缺席同一句）、:311（rc 只看 `_loaded`）；同源第二站點 tools/dev_start.py:1543-1546（`cols[2] == label` 即 return True，`cols[1]` 丟棄）＋ tools/dev_start.py:1642-1646（印「launchd 已載入、尚未跑過第一輪（正常…）」） | CONFIRMED | **fixed**：install_mac_nightly.sh:357-372 已解讀 launchctl list 第 2 欄並區分「未跑過」與「非零退出」 | 把第 2 欄（last exit status）納入判讀。`--status`：`_row` 已握有整列，抽第 2 欄印成 `上次退出碼 = N   (expected 0)`（沿用 `_cap_line` 的 `(expected X)` 體例，與 Windows `LastTaskResult` 對齊），N≠0 時 ⚠️。更要緊的是修假因果：`report_heartbeat`（:122-125）與 `report_coverage`（:170-172）的缺席分支不得無條件斷言「尚未跑過第一輪」——先看第 2 欄，非零就改印「排程已觸發但載體以 exit N 結束、未寫出心跳（查 Auto… |
| R68-31 | Scan-M mac 真機 | P3 | macOS | `--status` 在「launchd 已載入、但磁碟上的 plist 已不存在」時 rc=0 全綠並整段跳過能力表——這是 macOS 專屬的「載入 ≠ 已持久化」狀態，下次登出/重開機排程必死，而唯一的機讀判準說它健康 | tools/install_mac_nightly.sh:301-308（`if [ -f "${PLIST_PATH}" ] … else 只印一行就走`）＋ :311（`[ "${_loaded}" -eq 1 ]` 即 rc=0）；🔴 訂正／加強：檔頭 :41-42 已逐字承諾「1＝失敗（--status 時＝未載入**或 plist 缺席**）」，實作與自家文件契約直接矛盾；上游 :281-287 `cmd_uninstall`（`launchctl unload` rc 實測恆 0，卻 `\|\| true` 後無條件 `rm -f` plist） | CONFIRMED | **fixed**：cmd_status rc 判準已納入 plist 存在性；cmd_uninstall 亦補 unload 自證 | 把 rc 判準由「label 在 launchctl list 裡」改為「label 在 launchctl list 裡 **且** `${PLIST_PATH}` 存在」——後者才是「下次登入還會在」的必要條件，而且這正是檔頭 :41-42 已經承諾、實作卻沒兌現的語意（本修正是讓實作追上既有契約，不是改契約，因此不需要重寫檔頭該句，只需在該句旁補一行說明為何 plist 是持久化必要條件）。plist 缺席分支不要只印一行就走，補「⚠️ 目前的載入狀態不會存活到下次登入——重裝：bash tools/install_mac_nightly.sh」。上游一併收：`cmd_uninstall… |
| R68-32 | Scan-M mac 真機 | P3 | macOS | 心跳年齡的「顯示值」在 bash 與 python 兩站點確定性分歧（bash 十分位截斷 vs python `.1f` 四捨五入），而 R67 DEF-101-671 的等價鎖 `_classify()` 刻意只比三態與 FAIL 數、把這個數字丟掉 | tools/install_mac_nightly.sh:147-148（`_age_tenths=$(( _age_s * 10 / 86400 ))` ＋ `_age_days="$(( _age_tenths / 10 )).$(( _age_tenths % 10 ))"`；檔頭 :145 宣稱「與 dev_start.py 的 {age_days:.1f} 同格式」）vs tools/dev_start.py:1672（`age_days = (int(time.time()) - int(mtime)) / 86400.0`）＋ :1701-1702（`{age_days:.1f}… | CONFIRMED | **fixed**：bash 側改用 awk printf "%.1f"，與 python `{:.1f}` 同語意 | 兩擇一並在檔頭寫明：(a) bash 側改 round-half-up `_age_tenths=$(( (_age_s * 10 + 43200) / 86400 ))`——但 python `.1f` 是 round-half-even 且吃浮點誤差，極少數點仍會分歧；(b) 較穩：python 側也改整數十分位截斷（`age_tenths = (int(time.time()) - int(mtime)) * 10 // 86400`，顯示 `f"{age_tenths//10}.{age_tenths%10}"`），讓兩側共用同一個整數運算式，分歧被結構性消除而非平移——與 DEF-1… |
| R68-33 | Scan-N 雙向落差 | P1 | both | 雙向落差：唯一會在真 Windows／真 macOS 上跑完整 AutoClaude 套件的通道（*-nightly-full）連續 5 次排程全紅、告警回流靜默 18 天 —— 「四支 CI 全綠」只是 push 事件的綠 | .github/workflows/windows-compat-ci.yml:982（windows-nightly-full，continue-on-error:true）、:1166（windows-nightly-alert，needs 僅 nightly-full、無第三方看其 rc）、:1191-1210（check step `set -euo pipefail` + `gh api`，本 step 無 continue-on-error）；.github/workflows/macos-compat-ci.yml:710／:779（同構）、:686-690（in-file 自述… | CONFIRMED | **duplicate**：與 R68-08 同一件事（同一帳本列 DEF-101-703） | (1) 在 root-infra-ci（每 push 必跑、ubuntu、現正全綠）加一道 `gh run list --workflow <x> --event schedule --limit 1 --json conclusion`，最近一次排程 nightly-full 非 success 即 fail——把非阻斷 job 的紅轉成阻斷訊號；(2) 🔴 訂正原方向：開單 step 已有 continue-on-error，要動的是**上游 check step**（`set -euo pipefail` + `gh api`）——為它加 continue-on-error 或改用 `-… |
| R68-34 | Scan-N 雙向落差 | P2 | both | 雙向落差：磁碟機字面值鎖只掃 4 棵「測試樹」，生產碼零覆蓋 —— Windows 開發者把 D:/ 路徑寫進 AutoClaude 生產碼，mac 側全套護欄全綠 | tools/tests/test_platform_neutral_paths.py:26-32（docstring 明列「R12 掃描面」四棵：1. tools/tests/ 2. AISDLC_SDD/scripts/tests/ 3. AutoClaude/tests/（遞迴）4. LATEST tools/fsm_runtime/tests/（遞迴）——四棵全是測試樹） | CONFIRMED | **fixed@R69**：本包修：_scan_roots 由四棵測試樹擴為 11 棵（含 AutoClaude/autoclaude 遞迴、AutoClaude/tools、tools、tools/lib、.claude/hooks、AISDLC_SDD/scripts、LATEST fsm_runtime）。實測存量債 0（原報點名的 _conditional_evaluator.py 已無命中）；注入 D:/ 字面到生產碼實測：擴面後紅、HEAD 掃描面綠 | 把 test_platform_neutral_paths.py 掃描樹擴為「測試樹 ∪ 生產樹」：加入 AutoClaude/autoclaude/（遞迴）、AutoClaude/tools/、tools/（非 tests）、.claude/hooks/、AISDLC_SDD/scripts/（非 tests）、LATEST tools/fsm_runtime/（非 tests）。存量債實測僅 1 筆（AutoClaude/autoclaude/core/services/mutation/_conditional_evaluator.py），親讀後判真債或以既有 `# platform-o… |
| R68-35 | Scan-N 雙向落差 | P3 | both | 雙向落差：全 repo 沒有任何站點量測「各平台唯一能被驗證到的面積」，UEP 量的是別的東西 —— 於是『AutoClaude 整棵樹在任何阻斷性閘門上都沒在 Windows 跑過』這件事無法逐輪追蹤、只能靠人記得 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:419-424（§4.1「UEP = len(P._EXEMPT_PAIRS)」＋語意定義，定義域只涵蓋 .sh/.ps1 成對殼）；docs/06_quality/CrossPlatform_Scan_Dimensions.md:83-90（Scan-H 判準④「通過判準（唯一）」＝UEP）；ONBOARDING.md:305-309（§7 表② 為雙平台測試數對照，非平台專屬驗證面積） | CONFIRMED | **open**：PXV 指標未建立（全 repo grep 僅命中本清單自身） | 新增與 UEP 並列、純靜態可現查的第三指標（PXV＝platform-exclusive verification surface）：定義「tracked *.py 中，不落在任何『會在該平台實際執行』的 CI job 觸發面內的檔數」，輸入為 workflow paths ∪ 各 job 實跑指令（可複用 tools/check_script_parity.py 既有讀 workflow 的手法）。同步修 Scan-H 判準④，把「通過判準（唯一）＝UEP」改為「UEP ＋ PXV 各自對照該輪上限」，並在 ADR §4.1 可辯護地板旁寫明 PXV 的地板理由（PG/pgvector 相… |
| R68-36 | Scan-T 技術債 | P2 | both | 技術債：ONBOARDING.md:445「macOS 側純推論」元資訊列的四項殘留在 R68 真機上全部被推翻（Darwin 分支已跑出 SKIP=0、macos-compat-ci 已三次 success、§7 darwin 錨已有真機 provenance），該列仍逐字原樣掛著並持續把讀者導向「預設答案是 Windows」 | ONBOARDING.md:445（🔴 元資訊列，含①②③④四項與末欄「想關掉這個缺口只有一條路＝一台真 Mac」） | CONFIRMED | **fixed**：ONBOARDING:445 已於工作樹改寫為現查式並保全原句（非本包所為，另一修復包落地） | 改寫該列，照 `CrossPlatform_Scan_Dimensions.md:48` 已確立、且 DEF-101-643 已在 ADR-XPLAT-002 §6 落地過一次的原則：**不換一個會過期的斷言，改成指向 live 來源**。具體：①②④ 的平台覆蓋狀態改為「以 §7 兩條 `snapshot-fingerprints-<平台>` 錨的 measured-at／host 與 `gh run list` 現查為準」；②另註明 bash 3.2 已可在 macOS 系統 /bin/bash 實跑；**③保留**（POSIX 恆綠為事實，不得一併刪除）；末欄改為「已達成（R67 首輪、… |
| R68-37 | Scan-T 技術債 | P3 | Windows | 技術債：Windows-only 的 stdio UTF-8 前導碼在 AutoClaude/tools 有 21 份行內複本而 SSOT 模組 `tools/_stdio_utf8.py` 只有 3 個消費者；19/21 缺 `errors="replace"`、19/21 的 except 缺 `ValueError`（沙箱實測：stderr 已關閉時複本 rc=1 崩潰、SSOT rc=0 存活）、5/21 只改 stdout；另有 15 支帶 `__main__` 的 CLI 整段缺席且該樹零枚舉鎖 | AutoClaude/tools 共 21 份行內複本（代表：drift_log_ga_check.py:88-92 完整雙串流版、ac4_progress_check.py:381-385 僅 stdout 版、local_ci_gate.py:317-321 唯二完整版）；SSOT＝tools/_stdio_utf8.py:26-38；覆蓋缺口＝AutoClaude/tools 35 支帶 `__main__` 的 CLI 中 15 支整段缺席（其中 10 支原始碼含非 cp950 字元），實際會在執行期輸出者以 c6_staging_validator.py:281,323 最明確 | CONFIRMED | **open**：AutoClaude/tests/tools/ 無 stdio utf8 枚舉鎖；21 份行內複本未收斂 | (1) **先補鎖**（最便宜、擋住繼續長）：仿 `AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_cli_utf8_reconfigure.py` 在 `AutoClaude/tests/tools/` 加一支枚舉鎖，判準應以我上面的探針為準——「帶 `__main__` 且**執行期輸出**含非 cp950 字元」而非「原始碼含非 cp950 字元」（後者會把 validate_mutmut_log 這類純註解案例誤納），並附帶 WHY 的豁免白名單＋stale 自檢（Scan-H 判準②）。約 80 行、風險零。(2) *… |
| R68-38 | Scan-T 技術債 | P3 | both | 技術債：全 monorepo 只有 AutoClaude 一份 ruff 設定，導致從 repo 根對根層 `tools/`（護欄層本體）跑 `ruff check tools/` 得到 **All checks passed! / rc=0 的假綠**——同一棵樹套上 AutoClaude 規則集實測 189 筆；AISDLC_SDD LATEST v0.30 為 3,486 筆、30 個版本目錄合計 102,126 筆 | AutoClaude/pyproject.toml:108-130（`[tool.ruff]`，全 repo 唯一設定）；受害面＝根層 tools/（實測 75 支 .py／47,277 行）與 AISDLC_SDD 全樹；缺口自陳處＝tools/tests/test_no_invalid_escape_sequences.py:9-10 | CONFIRMED | **partial@R69（兩半邊分別記，勿讀成整筆結案）**：<br>· **根層 `tools/` 半邊＝fixed@R69**——`tools/ruff.toml` 已落地（**放在 `tools/` 而非 repo 根**，理由見該檔檔頭：納管 AISDLC_SDD 是另一個授權面的決定），且 R69 第三波**實查確認有執行者**（非宣稱）：`.github/workflows/root-infra-ci.yml` 第 16 道 `ruff check tools/`（檔內 `grep -c ruff` 命中，L388-400）＋ `tools/git-hooks/pre-push` root-infra **快層第 ④ 段**（L301-315，ruff 缺席即 fail-loud 而非軟跳過）。同機實測 `ruff check tools/` → `All checks passed!`；注入一行 120 字元 → `E501 / Found 1 error / rc=1`。**落地當下曾一度是「設定有、執行者無」的假綠**，該缺口另立 `DEF-101-719` 逐字記載。<br>· **`AISDLC_SDD` 半邊＝open**——`AISDLC_SDD/` 全樹（含 LATEST v0.30 的 F 類真錯誤 3,486 筆）仍無任何 ruff 設定與執行者，本筆原點名的最大實害面**原樣留著**。承接輪次：**未指派** | 在 repo 根新增 `ruff.toml`，內容對齊 `AutoClaude/pyproject.toml:108-123`（line-length=100、target-version=py311、select=[E,F,I,UP,W]），以 `extend-exclude` 排除 `AISDLC_SDD/AISDLC_SDD_v0.0*`~`v0.2*` 凍結版（Copy-on-Evolve 不回改）。**優先順序應與我訂正後的實害對齊**：(1) 先接 `AISDLC_SDD` LATEST v0.30 的 **F 類**（真錯誤）——那是唯一連預設規則都紅的地方；(2) 根層 too… |
| R68-39 | Scan-T 技術債 | P3 | both | 技術債：`# noqa: PLC0415（全形括號理由）` 是無效指令——沙箱實測全形括號會讓 noqa **完全不生效**，且每次 `ruff check` 都印 `Invalid noqa directive` warning；此為 DEF-101-525 已記錄的同型自傷復發，當輪未落任何機械鎖 | AutoClaude/autoclaude/models/playbook.py:61（`from ..utils.config import TokenGuardConfig  # noqa: PLC0415（避免循環 import）`）；訊號無人消費處＝AutoClaude/tools/git-hooks/pre-commit:56（`ruff check "${staged_py[@]}" \|\| fail=1`，只看 rc） | CONFIRMED | **fixed@R69**：本包修：playbook.py:61 全形括號改半形並與規則碼隔開（修前 ruff 印 `Invalid noqa directive`、修後消失）；另落 repo-wide 掃描鎖 TestNoqaDirectivesAreWellFormed（採負向前瞻 (?![0-9]) 避免假陽性，含自我豁免的 stale 自檢） | (1) **就地修**：`playbook.py:61` 全形括號改半形並與規則碼隔開（`# noqa: PLC0415  (避免循環 import)`），或理由移到上一行。1 行、零風險。(2) **落鎖**：兩條路擇一——(a) 在 `tools/tests/` 加 repo-wide 掃描，**務必用負向前瞻寫法** `#\s*noqa:\s*[A-Z]+[0-9]+(?![0-9])[^\s,]`（原報的寫法會製造 10+ 筆假陽性，直接照抄會得到一支恆紅的鎖）；(b) 更硬且更便宜——在 pre-commit 的 ruff leg 把 ruff stderr 中 `Invalid .… |
| R68-40 | Scan-T 技術債 | P3 | both | 技術債：`run_self_evolution.{sh,ps1}` 對**同一個失敗條件**（PATH 上無可用 python／只命中 WindowsApps 空殼）回傳不同 exit code（POSIX 5 vs Windows 7），而 `check_script_parity.py` 把該對登記為 `unpinned` 的豁免理由只查了「dry-run 安全預設兩側一致」單一軸 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/arch_fitness/run_self_evolution.sh:38（`exit 5`＝無可用 python）vs 同目錄 run_self_evolution.ps1:42（`exit 5`＝PS 版本 <6）／:71（`exit 7`＝缺 guard 檔）／:76（`exit 7`＝無可用 python）；豁免登記 tools/check_script_parity.py:651-656、tier 表 :1066；規格側 AISDLC_SDD/AISDLC_SDD_v0.30/workflow/sdd-self-ev… | CONFIRMED | **fixed**：兩側退出碼已對齊（.ps1 改 6=平台前置不足／5=無可用 python／7=git 失敗） | 與 ADR-XPLAT-002 §3 方向一致：把 exit code 表升為**規格側契約**——在 `AISDLC_SDD/AISDLC_SDD_v0.30/workflow/sdd-self-evolution/SDD_SELF_EVOLUTION.md`（現僅 :183 定義 1/2）補一張完整退出碼表（0=收斂／1=advisory warn／2=structural fail／3=缺 claude／4=ESCALATION／5=無可用 python／6=平台前置不足〔PS 版本〕／7=git 操作失敗），兩側實作對齊，並**同步改寫 .ps1:42／:71 那兩段「已占用」枚舉註解… |
| R68-41 | Scan-A1 生產碼 | P3 | macOS | macOS 上 HotkeyHandler.register() 在背景監聽執行緒已死之後仍印「已註冊」並把 _registered 設 True；且 ONBOARDING §9 給的解法（開啟「輔助使用」權限）經真機證偽——真正的擋路者是 keyboard 套件的 os.geteuid()!=0 檢查，授權輔助使用完全無效 | AutoClaude/autoclaude/perception/hotkey_handler.py:74-82（add_hotkey 同步返回即 `self._registered = True` + `logger.info("全域中斷熱鍵已註冊")`）、:27-31（docstring 逐字「macOS 缺 Accessibility 授權時的真實失敗（`_darwinkeyboard.listen()` 的 `os.geteuid()` 檢查）」＝把 euid 判準與 TCC 授權綁在同一句，是錯誤歸因的源頭）、:36-38（docstring 稱「存在 race」）；連帶錯誤傳播站點… | DOWNGRADED | **partial**：程式碼與 docstring 已修（hotkey_handler.py:98-106 輪詢 listening_thread.is_alive()）＋鎖 test_perception_platform_honesty.py；**ONBOARDING §9「輔助使用」誤導句未訂正** ⇒ 該半 open | (1) register() 改可驗證判準：add_hotkey 返回後短暫輪詢 `keyboard._listener.listening_thread.is_alive()`（我已實測此屬性在失敗時確為 False，可直接當判準），死亡則 _registered 維持 False、log 降為 warning 並自報「本平台不可用」，不得再印「已註冊」；(2) 訂正 hotkey_handler.py:27-31 docstring（geteuid ≠ Accessibility）與 :36-38（macOS 非 root 為確定性失敗，非 race）；(3) 訂正 ONBOARDING.… |
| R68-42 | Scan-A1 生產碼 | P3 | Windows | Windows 上 claude CLI 走 .cmd/.bat shim 時，整個 prompt 被組進單一 `cmd /d /s /c "..."` 命令列字串，撞 cmd.exe 8191 字元硬上限；POSIX 側走 argv（本機 ARG_MAX 實測 1 MiB）完全無此界線，且全樹零長度守門 | AutoClaude/autoclaude/perception/pty_wrapper.py:84-91（_build_cmd_shim_line 回傳 `cmd /d /s /c "…"` 單一字串）、:163-191（_start_subprocess 第 169-171 行把該字串整條交給 Popen）、infra/adapters/pty_executor.py:79（`args += ["-p", prompt]`）。守門缺口為真，但觸發需 prompt 逼近 8 KB。 | DOWNGRADED | **fixed**：pty_wrapper.py:86 _CMD_LINE_MAX_CHARS=7800 長度守門已落地 | (1) 平台中立的長度守門：_build_cmd_shim_line 組完後量長度，超保守閾值（如 7800）即 fail-loud 拒絕並指路，取代難歸因的 `The input line is too long.`；成本極低、可在 mac 上寫測試。(2) 架構正解（可延後）：prompt 改走 stdin 或暫存檔傳路徑，使 Windows 容量與 POSIX 脫鉤。(3) 落鎖：純函式測試斷言 _build_cmd_shim_line 對超長 args 回報/拒絕（非 Windows 亦可跑）。(4) **本筆的 Windows 半必須明列為未覆蓋面**，於下一次有 Windows 真… |
| R68-43 | Scan-A2 SDD/根層 | P2 | macOS | LATEST run_tlc.sh 在 macOS 系統 bash 3.2 下「每一條執行路徑」都必死於 set -u 空陣列展開，且 rc=1 撞上該檔自訂的「1＝TLC 偵測 invariant violation」語意，把環境錯誤偽裝成形式化驗證失敗；Windows .ps1 對應寫法 @() + splatting 完全正常＝單向平台落差 | AISDLC_SDD/AISDLC_SDD_v0.30/tools/fsm_runtime/formal/run_tlc.sh:31（set -euo pipefail）, :49（TLA_VERSION_ARGS=()）, :72, :78, :85, :92（原報 :84/:90 有偏差，實查為 :85 與 :92）；對照 run_tlc.ps1:72-73/78/83/87/91；使用者面呼叫端＝同版 tools/fsm_runtime/formal/README.md:36/53、cicd/SDD_CICD_BASE_LAYER.md:382/398、workflow/sdd-fsm-… | DOWNGRADED | **fixed@R69**：程式碼四處（:72/:78/:85/:92）早前已改 ${ARR[@]+"${ARR[@]}"}；**本包補上原缺的機械鎖** unset_safe_array_problems＋TestUnsetSafeArrayExpansion，對修前版 run_tlc.sh 實測命中該 4 行、對現版與長度守衛形態零誤報 | 把 :72/:78/:85/:92 四處 `"${TLA_VERSION_ARGS[@]}"` 改為 bash 3.2 安全形態 `${TLA_VERSION_ARGS[@]+"${TLA_VERSION_ARGS[@]}"}`，或改用字串旗標＋條件式附加；同時把環境錯誤路徑 rc 對齊檔頭宣告的 2。落地必附 bug-injection 紅綠：於 tools/tests/test_bash32_compat.py 新增「同檔同時存在 set -u 與 NAME=() 宣告且以 \"${NAME[@]}\" 展開」的 pattern，注入→紅、還原→綠各取證一次。並將 run_tlc.sh -… |
| R68-44 | Scan-B 淨化/guard | P3 | Windows | WindowsApps guard 在唯一「自由文字輸入」的消費點用精確字串 allow-list 比對 binary 名，漏掉 Windows 慣用的 `python.exe`／`python3.exe` 拼法 → guard 整條被跳過 | AutoClaude/autoclaude/execution/pre_run_validator.py:30,92（allow-list 與 dispatch 閘）；AutoClaude/autoclaude/execution/boot_helper.py:118,131（共用同一常數，一處修兩處生效）；tools/tests/test_windowsapps_guard_cross_consistency.py:2124-2148（餵入點在 dispatch 閘下游） | DOWNGRADED | **fixed**：pre_run_validator/boot_helper 已改 Path(binary).stem.lower() 比對＋test_gap009.py 二維 case 表 | 把 :92（與 boot_helper.py:131 共用）改為對 `Path(binary).stem.lower()` 比對，即涵蓋 `python.exe`／`python3.exe`／大小寫變體，且不放寬 scope（`pytest.exe` 的 stem 是 `pytest`，test_gap009.py:188-200 的偽陽性鎖仍綠）。同步在 test_gap009.py 補「binary 名 × resolved 路徑」二維 case 表（至少 python.exe / PYTHON3.EXE / pytest.exe 三列），因為四方 parity 鎖結構上永遠碰不到 dis… |
| R68-45 | Scan-B 淨化/guard | P3 | both（實際顯形於 Linux/Windows 側 commit，非 macOS） | 淨化家族四處實作零 Unicode 正規化，與同 repo 的 NFC 閘（R67-A2/B16 才剛立）判準相反——sanitizer 產生的 NFD 檔名一提交就被自家閘門擋下，且只在 macOS 顯形 | AISDLC_SDD/scripts/component_sanitizer.py:47-111（生成器側無 normalize）；AutoClaude/autoclaude/utils/logger.py:103-127（同）；對照 tools/check_ntfs_paths.py:168,188（validator 側要求 NFC）；tools/tests/test_windows_forbidden_filename_parity.py（722 行，零 normalization 斷言） | DOWNGRADED | **fixed**：兩支 sanitizer 入口皆已 unicodedata.normalize("NFC", …) | 仍建議在兩支 sanitizer 入口加 `unicodedata.normalize("NFC", name)`（對 ASCII／CJK 零行為變更），但理由要據實改寫為「Linux/CI 側生成 + 提交時避免撞自家 NFC 閘」而非「macOS 顯形」；並在 `test_windows_forbidden_filename_parity.py` 增第 5 組四方 NFD 樣本讓「四處同時缺席仍等值」不再恆綠。優先度：P3 backlog，不必本輪阻擋。注意與下一筆（上標變體）可一次解決——若改用 NFKC 則 `COM¹→COM1`（我實跑 `unicodedata.normalize(… |
| R68-46 | Scan-B 淨化/guard | P3 | Windows（僅 ~200–254 字元窄帶；≥255 為兩平台共同失敗，非跨平台落差） | 檔名長度政策四處三種數字（80 / 無上限 / 200 fail / 180 warn），parity 鎖零覆蓋；`_sanitize_log_filename` 產生的超長名在 macOS 可寫、在 Windows MAX_PATH 下會炸 | AISDLC_SDD/scripts/component_sanitizer.py:44,86-87（_MAX_COMPONENT_LEN=80）；AutoClaude/autoclaude/utils/logger.py:103-127（不截斷，附 :131-166 fallback）；tools/check_ntfs_paths.py:122-135（200 fail／180 warn，管 tracked 路徑） | DOWNGRADED | **fixed**：check_ntfs_paths 已就地記載三站點長度政策的域與窄帶前置條件；test_ntfs_length_gate.py 已建立 | 不建議把 80 抄進 logger（會改變既有輸出且三者治理不同域）。降為 backlog 兩項：①在四方 parity 鎖或 check_ntfs_paths docstring 記一則「三站點長度政策各自的域與理由」對照，避免下一輪再被當成不一致重新發現；②若要處理 200–254 窄帶的 Windows 總路徑落差，必須**先**在 Windows 11 真機重現一次（未開 longPaths，logs 目錄深路徑 + 250 字元名）取得 OSError 實證，否則等於再加一道從未紅過的鎖。原 fix_direction 中「補 fallback 給三個消費端」可刪——實查三者皆已 f… |
| R68-47 | Scan-C CI/排程 | P3 | both | `AutoClaude/tools/g0_gate_check.ps1` 在 check_script_parity 被歸為 `tier3_os_primitive`（理由「schtasks 排程家族」），但該檔本體零 schtasks／ScheduledTask API 呼叫——是純業務閘門；錯誤分級被 R64/R67 的 tier 棘輪固化，Mac 已成主力開發機後 SD_09 觀察期閘門在 Mac 上完全無載體 | tools/check_script_parity.py:749-751（`_SINGLE_SIDED_EXEMPT["AutoClaude/tools/g0_gate_check.ps1"] = (_TIER3_OS_PRIMITIVE, "schtasks 排程家族（ONBOARDING §8）")`）, tools/check_script_parity.py:1078（`_TIER_BASELINE` 同筆）, tools/check_script_parity.py:1119（`_TIER34_FLOOR = 10`）, tools/check_script_parity.py:96… | DOWNGRADED | **open**：check_script_parity.py:754/:1083 仍把 g0_gate_check.ps1 列 _TIER3_OS_PRIMITIVE | （1）分級訂正：把 `g0_gate_check.ps1` 自 `_SINGLE_SIDED_EXEMPT` 的 tier3 改列 `_UNPINNED`（比照同表 `AutoClaude/tools/sd06_w3_staging_dryrun.sh` 寫法，reason 須含 `_UNPINNED_EXIT_RE` 認得的「退場：未指派」錨點），並同步 `_TIER_BASELINE` 與 `_TIER34_FLOOR`（10→9，該常數註解自陳「只准下修」，此為合法下修並須具名理由）；真 tier3 只留 `reschedule_g0_gatecheck.ps1`／`fix_nightl… |
| R68-48 | Scan-D 文件 | P2 | both | ONBOARDING §9「證據來源偏斜」元資訊列整列反向：其四項具體殘留今日逐項被證偽，且該列自陳的兩條關閉條件（一台真 Mac／CI 帳務恢復）皆已達成，卻仍指示讀者「預設答案是 Windows」——R68 真正沒有真機的那一側正是 Windows | ONBOARDING.md:445（§9 表末第二列：表頭句／殘留①②④／緩解欄）↔ ONBOARDING.md:383（§8）同檔互斥 | DOWNGRADED | **duplicate**：與 R68-36 同一列（ONBOARDING:445），已隨該筆修復 | (a) :445 改寫為逐輪覆蓋表（輪次｜執行平台｜雲端 CI 可用性｜未覆蓋面），比照 ADR-XPLAT-002 §6 邊界 1 已建立的體例，**不要**再寫一句指向某一平台的對稱句；R67/R68 列直接引 `snapshot-fingerprints-<平台>` 錨與 `gh run list` 現查。(b) 殘留③ 不得一併刪除——它今日仍為真，只該從「Windows 專屬偏斜」欄位移到平台中立的鑑別力缺口欄；殘留② 改寫為「bash 3.2 已在本機存在，但含 `CONIN$` 的 case 分支是否被**執行**仍未取證」。(c) SC-4 掃描面擴到 `ONBOARDING.… |
| R68-49 | Scan-D 文件 | P3 | linux-ci | DEF-101-610 的解鎖條件②「把 SC-1～SC-4 搬進 tools/tests/ 並由 run_root_unittests.py 消費」在**寫下它的同一個 commit（461fd3b）內即已達成**，且同 commit 的 ADR 狀態欄自陳已接線 ⇒ 兩份文件在同一 commit 內結論相反；該孤兒列因此會永遠掛 open | docs/06_quality/AutoSDD_Defect_Log.md:120（DEF-101-610 狀態欄 R67 改派附記；回報者寫 :133，實測在 :120）↔ docs/04_planning/ADR/ADR-XPLAT-002…md:5 狀態欄 R67 round 3 訂正段 ↔ tools/tests/test_adr_xplat001_c1c2_lock.py（TestSection91Invariants* 三組） | DOWNGRADED | **open**：帳本 DEF-101-610 仍為 open＋R61 承接，未標記解鎖條件② 已達成 | （帳本本體由專責 agent 動手，此處只給對帳結論）標記解鎖條件② 已達成並附上述三條實跑取證；「四條」改為現查（`grep -c '^# SC-'`）；保留條件③ 未達成的誠實狀態，勿據此結案。治本：把 SC-6 的標的由 `$ADR2` 擴為「ADR 兩份＋缺陷帳本家族」，並訂通則——凡在帳本／ADR 內以散文陳述「某機械物尚未接線」者，必須同時附一條可跑的反證指令（形狀比照 §8.2 M-1~M-3），由 `test_adr_xplat001_c1c2_lock.py` 斷言該指令存在且現跑結論與散文一致。 |
| R68-50 | Scan-D 文件 | P3 | linux-ci | 三份 CLAUDE.md 的 400 行預算閘覆蓋不對稱：唯一受閘的 `AutoClaude/CLAUDE.md` 現值**恰好 400 行、零餘裕**（下一行即 CI 紅＋PostToolUse hook exit 2 阻斷編輯），而根層與 AISDLC_SDD 兩份完全無閘 | 殘存可修者只剩 CLAUDE.md:154（根層那句「`claude-md-budget`（CLAUDE.md ≤ 400 行 + snapshot 新鮮度）」未劃界，讀起來像三份都受管） | DOWNGRADED | **fixed@R69**：本包修：根 CLAUDE.md 的 claude-md-budget 描述已補「僅指 AutoClaude/CLAUDE.md（ADR-SD08-001 射程）」界限，未加第二個閾值站點 | 只做一件事：在根 `CLAUDE.md:154` 的「`claude-md-budget`（CLAUDE.md ≤ 400 行…）」補「僅指 `AutoClaude/CLAUDE.md`（ADR-SD08-001 射程）」。**不要**再加 CI 端 380 warning——`loc_budget_check.py` 已有同語意預警且在開發者實際會看到的那一側；再加一個就是第二個閾值站點（本 repo 一直在治的病）。另兩份 CLAUDE.md 是否納管屬政策題，若要納管須走 ADR、勿以「對稱」為由順手擴。 |
| R68-51 | Scan-E 架構 | P2 | both | 架構缺陷：宣稱以「Windows PowerShell 5.1 目標文法」把關的語法閘門，在 macOS 上實際由 pwsh 7.6.3 執行，且降級零訊號——Mac 開發者可把 PS7-only 語法一路推上 main | tools/tests/_ps_engine.py:47-49,65-76,96-102, tools/tests/test_install_windows_nightly.py:361-378, tools/tests/test_ps51_compat.py:137-168（實查 _PATTERNS 為 12 條，非原文所稱 14 條） | DOWNGRADED | **open**：_ps_engine 未拆 target_grammar_engine()／smoke_engine()；本機實測 pwsh 7 存在 ⇒ 5.1 文法閘實際由 PS7 執行、降級零訊號 | 沿用原文 (1)(2)(3) 但重排優先序。先做 (1)(2)：_ps_engine.py 拆 target_grammar_engine()（=native_ps51）與 smoke_engine()（現行兜底），把 test_install_windows_nightly.py:374 / test_bootstrap_ps1.py:48 / test_dev_start_ps1_lastexitcode.py:83 / test_git_hooks_install_common.py:450,475,498 的降級改成帶 [WINDOWS-NATIVE-ONLY] 標籤的 skip，落進… |
| R68-52 | Scan-E 架構 | P3 | both | 架構判準：UEP（ADR-XPLAT-002 主判準）的定義域結構性排除「單邊平台落差」，而 §6 十一條邊界無一條揭露——18 筆 Windows／POSIX 落差不在任何計分板上 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:417-441,454-457,900-998, tools/check_script_parity.py:660,1094 | DOWNGRADED | **open**：ADR §6 未補「UEP 不涵蓋單邊落差」邊界揭露 | 保留原文 (c)(d)（§6 補一條邊界揭露「UEP 不涵蓋單邊落差」、§8 交棒表具名承接）——兩項是真缺口且成本最低。(a)(b) 新立第五把尺 PGS ＋ _PGS_CEILING 應先做 dominance test：_UNPINNED_CEILING=8 已在數同一批 unpinned（8 筆中 5 筆為單邊），新尺與它高度重疊、違反 ADR §4.2 rule 3；較省的替代是把既有天花板拆成兩個維度常數（_UNPINNED_PAIR_CEILING / _UNPINNED_SINGLE_SIDED_CEILING），語意仍為只准下修，但讓「單邊落差」在 diff 上成為獨立可讀數… |
| R68-53 | Scan-E 架構 | P3 | both | 架構缺陷：兩支 compat-CI 的 paths 白名單採「一邊 wildcard、一邊逐檔列舉」的不對稱設計，列舉面零機械完整性鎖——新增一支 .sh 即讓 windows-compat-ci 靜默不觸發 | .github/workflows/macos-compat-ci.yml:74,230, .github/workflows/windows-compat-ci.yml:198,339（另：tools/check_gha_action_versions.py:11 仍引用已不存在的 test_ci_paths_cover_root_consumers.py） | DOWNGRADED | **open**：compat-CI paths 仍一邊 wildcard 一邊逐檔列舉，無完整性鎖 | 採原文方案 (B) 立鎖，優於 (A) 補通配（(A) 會讓凍結版樹下 .ps1 也觸發 Windows CI，代價與效益不成比例）；但斷言面須依實測結果收窄：(i) 對稱性斷言只針對三棵 parity 掃描樹之外的 *.sh / *.ps1（樹內已由 check_script_parity 強制註冊間接覆蓋，重複立鎖屬 ADR §4.2 rule 3 該擋的疊床）；(ii) 補反空轉支（任一側 paths 解析為空即 fail-loud）。位置沿用 tools/tests/test_smoke_ci_sync.py（:24-25 已持有 _MAC_CI / _WIN_CI）。順手清掉 too… |
| R68-54 | Scan-E 架構 | P3 | macOS | 架構判準：§8 表頭規則 1 的「具名角色」例外零機械消費者——本輪（Darwin）已滿足 item 13 的承接角色「下一個 macOS 真機輪」，全 repo 無任何東西知道 | docs/04_planning/ADR/ADR-XPLAT-002-platform-surface-reduction.md:1049-1052,1087,1191-1258（§9.1 SC-1~SC-8 宣告區實查行號） | DOWNGRADED | **open**：ADR §8 item 13 承接者仍掛具名角色「下一個 macOS 真機輪」，未就地改派 | 先做成本近零的一半：在 §8 item 13 就地把承接者由具名角色改為 R68（本輪已滿足角色條件，就地改派即消滅出口），並在 §8 表頭規則 1 補一句「具名角色一經滿足即應於當輪就地改派為具體輪次，不得續掛」。若仍要機械化，SC-9 實作不要新開通報管道——照原文接進 tools/run_root_unittests.py 既有 report_* 家族（與 [WINDOWS-NATIVE-ONLY] skip 明細同一出口，本機實測該出口運作正常）；_ROLE_PREDICATES 採「述詞為 True 不判紅、只 fail-loud 印出」亦照原文。另請一併處理本輪查出的 item 1… |
| R68-55 | Scan-F 載具實跑 | P2 | macOS | dev_start 第⑤步自動裝 git hooks 在 macOS 預設情境 100% 失敗（子行程 PATH 沒有 .venv/bin，而 macOS 根本沒有裸 python），dev_start 仍 rc=0 — 「hooks 靜默全滅」的補救機制自己全滅 | tools/dev_start.py:1501-1516（`cmd = ["bash", install_git_hooks.sh]` + `_stream(cmd)` 不傳 env）、tools/lib/git_hooks_install_common.sh:61（`is_real_python_candidate python` 只認裸 python） | DOWNGRADED | **fixed**：dev_start.py 呼叫 install_git_hooks 時已前置 .venv/bin 進子行程 PATH | `tools/dev_start.py:1501-1516` 呼叫安裝腳本時傳 `env=dict(os.environ, PATH=str(Path(sys.executable).parent) + os.pathsep + os.environ["PATH"])`（dev_start 對自己跑在哪支直譯器上是權威來源，零成本）；POSIX/Windows 兩分支同款。次要（防禦性、非必要）：`tools/lib/git_hooks_install_common.sh:61` 候選由裸 `python` 擴為 `python`→`python3`，與 `bootstrap.sh:19-24… |
| R68-56 | Scan-G backlog 稽核 | P2 | both | 硬規則② 機械鎖只判到 78 筆未結列中的 6 筆（8%）——散文式延後（「排入下一輪」「擇機」「留待下輪」）43 筆全數從短路出口走掉，鎖只落地了規則的「輪號比大小」那一半 | tools/check_defect_log_crossref.py（HEAD 版）_HANDOVER_ROUND_RES 五條樣式 ＋ orphan_backlog_problems() 的 `if not handovers: continue`；docs/06_quality/CrossPlatform_Scan_Dimensions.md:238-245（硬規則②，含自陳「已實測不涵蓋…不含 R\d+ 的散文式指派」） | DOWNGRADED | **fixed**：crossref 已落「二擇一」判準（可解析輪號／字面未指派），DEF-101-205 即由該鎖具名補標 | 沿用 repo 內既有形態而非新造：把 `tools/check_script_parity.py:550` 的 `_UNPINNED_EXIT_RE` 判準（`未指派\|R\d+`）平移為帳本側第二條純函式判準——未結案列必須二擇一（可解析承接輪號 ≥ 當前輪 ／ 字面「未指派」），缺兩者即紅，並沿用 orphan_backlog_problems() 既有的「該列或更後列載明改派／回執即放行」出口避免歷史列永紅。落地前先對 61 筆存量跑一次決定「白名單釘現況＋只硬擋新增列」或分批補標（ARCH-R59-NB4 的永紅警告仍成立）。 |
| R68-57 | Scan-G backlog 稽核 | P3 | both | `current_round()` 取自帳本自身「發現情境」欄，判準基準永遠落後一輪——交棒給「剛結束的那一輪」在整個新輪工作期都合法通過 | tools/check_defect_log_crossref.py（HEAD 版）current_round() 與 orphan_backlog_problems() 內 `if newest >= cur: continue` | DOWNGRADED | **open**：current_round() 仍取自帳本發現情境欄，未加回執要求或 fail-open 方向提示 | 不建議引入第二個輪次時鐘（成本高於收益、且會新造一個 stale 站點）。最小改動即可：當 `handover_round == cur` 且該列發現情境輪號亦等於 cur 時，額外要求該列帶「回執」字樣，否則視為未結交棒；或直接在閘門訊息中把「當前輪 R{cur} 係由帳本發現情境欄推得，若本輪尚未寫入任何帳本列，此值落後一輪」印出來，讓 fail-open 方向對讀者可見。 |
| R68-58 | Scan-G backlog 稽核 | P2 | both | 孤兒 backlog 機械鎖只掃缺陷帳本主檔，而 Scan-G 的立案判例（DEF-101-521）修的四處中有兩處在活文件／生產測試碼——那半邊至今零覆蓋，且已實測出一筆死信：`CrossPlatform_Scan_Dimensions.md:225` 登記的 backlog 早在 R66 就做完了 | tools/check_defect_log_crossref.py 的 _DEFECT_LOG／_CROSSREF_TARGETS 掃描面；docs/06_quality/CrossPlatform_Scan_Dimensions.md:225（死信）；tools/tests/_platform_helpers.py:10,127,251（仍為活 backlog） | DOWNGRADED | **partial**：_platform_helpers 三處 backlog 仍在且未補可機械查解鎖條件；掃描面未擴（_CROSSREF_TARGETS 仍 4 檔） | 先做零風險的一半：就地回填 `CrossPlatform_Scan_Dimensions.md:225`（改為「已於 R66 由 DEF-101-624 收斂，本段降為史料」），`_platform_helpers.py:10/127/251` 三處維持 backlog 但補上可機械查的解鎖條件（`_ps_source.py` 拆分現查未落地）。掃描面擴充（帳本主檔 ∪ 一份具名的活文件／生產碼登記清單，清單本身帶 stale 自檢）成本較高，可與第 1 筆同批評估，但不應阻擋回填。 |
| R68-59 | Scan-G backlog 稽核 | P3 | both | DEF-101-433 的狀態欄實為 `closed-by-verification@R56：本則描述之缺口不存在`，卻因分類器取「最早出現的關鍵字」而被判為 open——且 `closed-by-verification` 不在 7 個合法首詞內，靠首詞是 `open` 躲過首詞鎖 | docs/06_quality/AutoSDD_Defect_Log.md HEAD 第 112 行（DEF-101-433）；tools/check_defect_log_crossref.py 的 _classify() 與 _STATUS_FIRST_WORDS | DOWNGRADED | **fixed**：DEF-101-433 首詞已改為合法已結 token，原文逐字保留於後（DEF-101-710 引為範式） | 就地把 DEF-101-433 狀態欄首詞改為合法值（`closed-by-decision@R56`，原文逐字保留於後，體例照 DEF-101-238），一併把它移出 78 筆未結存量。機械側加一條輕量判準：狀態欄若含 `→`／`⇒` 後接看似狀態 token 的字串，該 token 也必須落在 `_STATUS_FIRST_WORDS` 內——與第 1 筆同批落地即可，不必單獨開包。 |
| R68-60 | Scan-G backlog 稽核 | P3 | both | DEF-101-081（P0，open）自陳的解除條件今日實測已達成，卻無人回查——它同時是 DEF-101-518 解鎖條件① 與 ONBOARDING §9 平台偏斜列的唯一出口，一筆 stale 卡住至少三處下游 | docs/06_quality/AutoSDD_Defect_Log.md HEAD :61（DEF-101-081，最後更新 R42）、:65（DEF-101-206）、:117（DEF-101-518，routed（deferred@R59））；ONBOARDING.md §9「R60（及近輪）全部實機驗證平台＝Windows 11」列 | DOWNGRADED | **fixed**：DEF-101-081 已補回執並結案、經 archive_defect_log --apply 移入 AutoSDD_Defect_Log_archive_45.md | (a) DEF-101-081 就地追加 R68 回執段（附上『首次 runner 真被配置＝2026-08-01T10:34:25Z run 30695971130 steps=26』與『四支全綠＝24c5f34 / 14:03:42Z』兩筆逐字證據，比只貼四支 success 更有鑑別力）並轉為已結首詞；(b) DEF-101-518 標記條件① 已達成、重評②③；(c) ONBOARDING §9 該列改為指向 §7 表② 的 live provenance 並補一個 DEF-ID 使其進得了 crossref 掃描面。 |
| R68-61 | Scan-G backlog 稽核 | P3 | both | DEF-101-271／274 的「>2000 行即升級為該輪必修」自動門檻零機械量測者，實測 `tools/dev_start.py` 已從帳本三度記載的 1772 行漂到 1918 行、距門檻僅剩 82 行且無人察覺 | tools/dev_start.py（1918 行）；AutoClaude/tools/check_loc_budget.py:104-105（ABSOLUTE_LIMIT=750／SCAN_ROOT="autoclaude"）與 :252 的 rglob；docs/06_quality/AutoSDD_Defect_Log.md DEF-101-271／274 | DOWNGRADED | **open**：tools/dev_start.py 現為 1986 行（距 2000 門檻 14 行），仍無棘輪鎖 | 照回報者建議的最小可行：在 `tools/tests/` 既有鎖檔加一支斷言 `tools/dev_start.py` 行數 ≤ 一個簽入的凍結常數（棘輪形狀同 `_FROZEN_GUARD_FILE_COUNT`，只准往下改），超過即紅並在訊息點名 DEF-101-271／274 的門檻拍板義務。同時就地訂正兩列的「1772／餘裕 228 行」為現查指令（照本 repo 對寫死常數的既有處置），不要再登載會過期的數字。 |
| R68-62 | Scan-G backlog 稽核 | P3 | both | 12 筆狀態已結（fixed）的帳本列在狀態／分流欄仍載有實質待辦，而 `_UNRESOLVED_CLASSES` 明確排除 fixed ⇒ 這些殘留工作在結構上永遠進不了承接稽核 | tools/check_defect_log_crossref.py 的 _UNRESOLVED_CLASSES 與 orphan_backlog_problems() 內 `if _classify(...) not in _UNRESOLVED_CLASSES: continue`；docs/06_quality/AutoSDD_Defect_Log.md HEAD :106,113,118,121,124,127,131,137,138,139,144,146 | DOWNGRADED | **open**：_UNRESOLVED_CLASSES 仍排除 fixed；--plan 的 needs_ack 未接進收輪流程 | 不必新造清單機制（`archive_defect_log.py --plan` 已是那張清單）。真正的缺口是「清單只在有人要歸檔時才被看到」與「這 12 筆沒進承接稽核」：建議把 `--plan` 的 needs_ack ＋『已結但狀態欄含活躍字樣』兩區塊接進收輪流程（或 root-infra leg 以 warning 印出），並對 DEF-101-560 的 archive 側 14 列補一筆獨立 DEF 列，讓它不再被母列的 fixed 分類吞掉。 |
| R68-63 | Scan-H 護欄自檢 | P2 | both | pre-push 的 root-infra leg 只在「根層檔」變更時觸發，而它承載的 8 支根層守門＋根層 unittest 的偵測標的正好是「只改子專案」的那些 push ⇒ ONBOARDING §7 表② 指紋觸發器與表① live LOC 鎖在它們唯一該說話的變更類別上結構性零執行 | tools/git-hooks/pre-push:98-106（逐行 case 判 run_rootinfra）、:225-277（root leg 本體與八支守門迴圈，第 8 支＝`--check-snapshot`）；tools/sync_onboarding_baselines.py:613-618（_FINGERPRINT_TREES 四棵樹）；.github/workflows/root-infra-ci.yml:116-120（on: 無 paths filter ← 補位者） | DOWNGRADED | **open**：pre-push root-infra leg 仍只在根層檔變更時觸發，無指紋輸入面觸發條 | 維持回報者的最小修法方向：pre-push 增一條「指紋輸入面觸發」，清單機械讀 `sync_onboarding_baselines._FINGERPRINT_TREES`（勿手抄第二份，比照 run_sdd_consumer 讀 aisdlc-sdd-ci.yml 的既有作法），命中即單獨跑 `--check-snapshot`（毫秒級，不啟整條 root leg）；表① LOC 鎖同理掛 `AutoClaude/autoclaude/**`。並補一支可達性鎖：斷言「每一支根層守門的輸入面 ⊆ 承載它的 leg 的觸發路徑集合」，注入「把某守門的輸入樹移出觸發面」即紅。修復包請把 WHY … |
| R68-64 | Scan-M mac 真機 | P2 | macOS | `install_mac_nightly.sh` 安裝路徑會在 launchd 實際載入失敗時印「✅ 已安裝並載入」並 rc=0——`launchctl load` 失敗時仍回 rc=0，`set -e` 結構上攔不到，這就是 R67 想防的死排程的上游製造機 | tools/install_mac_nightly.sh:273-277（`launchctl unload`／`launchctl load`／無條件 echo ✅ 與 rc=0）；對照 tools/install_mac_nightly.sh:294-296 cmd_status 已有的正確查核式 `"${LAUNCHCTL}" list \| awk -v l="${LABEL}" '$3 == l'`；cmd_uninstall:281-283 同型（`unload \|\| true` 後無條件宣告 ✅） | DOWNGRADED | **fixed**：cmd_install 已於 launchctl load 後以 launchctl list 精確比對自證，未生效即 rc=1 | `cmd_install` 在 `launchctl load` 後做載入自證而非相信 rc：重用 cmd_status:295 既有的 `"${LAUNCHCTL}" list \| awk -v l="${LABEL}" '$3 == l'` 精確比對，空即 fail-loud（rc=1 + 明確訊息），✅ 只能印在自證通過之後。失敗訊息中查 `launchctl print-disabled gui/$UID`，命中 `=> disabled` 就直接給復原指令 `launchctl enable gui/$UID/<LABEL>`（實測這是最易觸發的成因，且 `launchctl un… |
| R68-65 | Scan-N 雙向落差 | P2 | both | 雙向落差：跨平台鎖的「觸發面」與「掃描面」不對稱 — 純子專案 push 一支跨平台鎖都不跑，mac 上寫的 Windows-only 缺陷與 Windows 上寫的 mac-only 缺陷在本機零攔截 | tools/git-hooks/pre-push:99-109（逐行 case 判 run_rootinfra）、:232 起 root-infra leg 條件；tools/tests/（實測 56 支 .py，非 57）；兜底＝.github/workflows/root-infra-ci.yml:117-118（push:[main] 無 paths）+ :343（run_root_unittests.py） | DOWNGRADED | **open**：pre-push 仍為四 leg，未加靜態跨平台鎖 leg | 維持原方向但改標嚴重度為 P2、並把「本機零攔截」的論述與「雲端有無條件兜底」並列（勿再宣稱等同無防護）：(1) pre-push 加第五個 leg「靜態跨平台鎖」——push 範圍含任何 *.py／*.sh／*.ps1（不論子專案）即跑上述 6 支純靜態鎖（實測 9.69s）；(2) 配 parity 鎖機械斷言「宣告掃描樹涵蓋子專案的鎖檔集合 ⊆ 該 leg 執行集合」，判準讀各鎖檔既有掃描樹常數而非人工清單；(3) 落地時用本次兩支注入檔當紅組樣本（Scan-H 判準①）。零額外 GitHub 分鐘。 |
| R68-66 | Scan-N 雙向落差 | P3 | Windows | 雙向落差：四類「只在 Windows 炸」的 Python 寫法（import fcntl／signal.SIGALRM／open 無 newline=""／os.path.join 結果與字面 "/" 比對）在整個護欄層零覆蓋 —— 注入後 ruff ＋ 57 支鎖全綠 | tools/tests/（實測 56 支 .py，無一涵蓋此四類）；建議宿主 tools/tests/test_subprocess_encoding_hygiene.py | DOWNGRADED | **open**：test_subprocess_encoding_hygiene.py 無 fcntl/SIGALRM 等 Windows-only 判準 | 降為 P3 預防性落鎖，且**只落兩條**（勿一次上四條）：在 test_subprocess_encoding_hygiene.py 既有 AST 骨架上加 ①頂層 import POSIX-only（fcntl/termios/tty/pty/pwd/grp/resource）或 Windows-only（msvcrt/winreg）而不在 try/except ImportError 或 sys.platform 守護下 → 違規；②`signal.SIGALRM`／`signal.alarm` 屬性存取不在 `hasattr(signal,"SIGALRM")` 守護下 → 違規。兩者… |
| R68-67 | Scan-T 技術債 | P3 | macOS | 技術債：`keyboard>=0.13` 未鎖上限且無平台條件，macOS 側被 0.13.5 拖進 162 個 pyobjc 發行版（24.4 MB，佔 site-packages 發行版數 80%），Windows 側零 — DEF-101-060「~18 條未鎖上限」中唯一實測會造成雙平台安裝面不對稱的一條 | AutoClaude/pyproject.toml:24（`"keyboard>=0.13"`，core dependencies，無 sys_platform marker）；唯一使用者 AutoClaude/autoclaude/perception/hotkey_handler.py:14 | DOWNGRADED | **open**：AutoClaude/pyproject.toml:24 仍為裸 `keyboard>=0.13`，無 sys_platform marker | 維持原方向但改敘述：`AutoClaude/pyproject.toml:24` 加平台條件 `keyboard>=0.13; sys_platform == 'win32'`，或移入新 optional extra `[hotkey]`。改動範圍需同步 `tools/bootstrap.*` 與 CI extras 字串；驗證跑 `pytest tests/plugins/test_hotkey_plugin.py`。**建議改為併入 DEF-101-060 的既有 open 條目**（新增「另有平台不對稱傳遞閉包」一軸），而非另開新缺陷號。若判定 keyboard 必須留在 core，最低… |
| R68-68 | Scan-T 技術債 | P3 | both | 技術債：AutoClaude 547 支 .py 有 227 支（41.5%）帶 ruff 存量債，配上 pre-commit 的整檔（非 diff）掃描＝任何跨平台文字修復只要碰到帶債檔就被無關錯誤擋下；DEF-101-018 記載的 baseline 1,147 實測已是 942、pyproject 內用來佐證鎖版的「1,147 筆一致」已失實 | AutoClaude/tools/git-hooks/pre-commit:56；AutoClaude/pyproject.toml:32；docs/06_quality/AutoSDD_Defect_Log.md:45（DEF-101-018） | DOWNGRADED | **duplicate**：與 R68-14 同一件事（同一個「1,147」數字的三個站點） | 不建議另開新缺陷號，建議**併入既有兩筆**：(1) 把 `AutoClaude/pyproject.toml:32`、`AutoSDD_Defect_Log.md:45`（DEF-101-018）兩處的「1,147」訂正為現查 942，並照 DEF-101-262 慣例註明量測輪次與 ruff 版本（0.15.21）——這是本筆唯一未登記的部分。(2) 若要真正治本，動作應掛在 **DEF-101-525 的「形狀未解」**下：`ruff check . --fix`（707 筆 fixable，皆非 unsafe）＋ `pytest tests/ -q` ＋ `lint-imports` … |
