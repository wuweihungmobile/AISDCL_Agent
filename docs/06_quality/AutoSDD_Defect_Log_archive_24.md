# AutoSDD Defect Log — Archive 24

> **歸檔來源**：`AutoSDD_Defect_Log.md` 缺陷總表中 19 筆 R56 round 1~3 已結列（狀態 `fixed@R56`、且未被 `ONBOARDING.md`／`.github/workflows/{windows,macos}-compat-ci.yml`／`AutoClaude/docs/05_development/SD10_PG_Contract_NextAction.md` 四份 crossref 掃描目標以「DEF-ID」樣式引用），於 R56 收尾（2026-07-27）搬遷。
>
> **為何本輪第二次歸檔、且**破例**搬遷本輪自身條目**：R56 是異常長的一輪（五維掃描＋**六輪**四方複審、修復 50+ 項），單輪帳本淨增約 27KB，動工前已先搬 16 筆至 `archive_23`（釋出 24.8KB）仍不足。round 6 收尾寫入後主檔達 256,829 bytes，**距 `tools/check_defect_log_crossref.py` 的 256KB（262,144）fail 硬閘僅剩 5,315 bytes**，已不能留待 R57。
>
> **判準說明（round 6 SA 質疑後的裁定）**：`archive_23` 沿用的判準是「已結列（`fixed@`／`wontfix`）＋零 crossref 引用」，在此時點下合格者僅 2 筆／1.2KB，不足以退出。SA round 6 曾主張有 4 筆，經主控逐列複驗，其多算的 DEF-101-242／243 狀態欄開頭實為 `open`（記事存證排入 backlog），不符「已結」要件——**該項回報不成立**。但 SA 同時舉出的有效反例成立且被採納：`archive_23` 內的 DEF-101-420 目前仍被 `tools/check_gha_action_versions.py` 等三處**程式碼**引用，可見「被程式碼引用」並不阻止搬遷（crossref 只掃四份指定文件、不掃程式碼）。據此本輪擴大搬遷範圍至「R56 round 1~3 已完全收斂的自身條目」——round 4~6 的條目（DEF-101-457~475）為本輪最新稽核脈絡，**刻意全數留在主檔**供 R57 審查員直接對帳。
>
> **R56 round 7 追加**：收尾寫入 round 7 條目（DEF-101-472 訂正／475 訂正／476／477）後主檔回升至 245,950 bytes、再度越過 240KB warn 線 190 bytes，與 DEF-101-471 剛寫下的「已退出 warn 帶」自相矛盾，故追加搬遷 **DEF-101-428／431** 兩筆——這兩筆正是依 `archive_23` 原判準（已結＋零 crossref）唯一合格的兩筆（round 6 SA 曾主張有 4 筆，多算的 242／243 狀態實為 `open`）。
>
> **原文逐字保全、零刪除**（搬移非刪除，git 亦保歷史）。查詢某缺陷現況一律先看主檔缺陷總表，需歷史脈絡再查對應 archive。

## 缺陷總表（已結列，逐字保全）

| ID | 發現日期 | 發現情境 | 現象與證據（file:line） | 嚴重度 | 分流去向 | 狀態 |
|---|---|---|---|---|---|---|
| DEF-101-438 | 2026-07-27 | R56 Scan-A（廣泛掃描）＋round 1 四方確認 | `AutoClaude/autoclaude/execution/types.py` 保留兩支**零呼叫端死碼**，內含 R52 已修復之 POSIX-only 字面值的第二、三份**未修複本**——即 R52 的修復只改了活碼，死碼裡的舊寫法原封不動，構成「復活即回歸」風險 | P2（死碼＋已修缺陷的未修複本並存） | 本輪修復 | fixed@R56：刪除兩支零呼叫端死碼（全庫 grep 確認無任何呼叫點）。**副作用為正向**：`autoclaude/` 總量 LOC 自 20437 降至 20356，一次釋出 82 行餘裕，直接解除困擾 R52~R55 連續四輪的零餘裕壓力（見 DEF-101-416 狀態更新），並成為 ADR §6.3「先減後調」順位原則的範例 |
| DEF-101-439 | 2026-07-27 | R56 Scan-A 實測（自製 AST 掃描器補既有機械鎖盲區）＋round 1 四方確認 | 未啟用 venv 時 macOS 上 11 個 AutoClaude 測試硬失敗：PATH 修補 fixture 只存在於 `tests/equivalence/conftest.py`，其餘五個測試模組裸奔；且「裸 python 缺席」的迴歸鎖本身在該情境下也失效（鎖自己被同一個缺口打敗） | P2（開發者體驗；未啟用 venv 是 macOS 常見情境） | 本輪修復 | fixed@R56：conftest 上移為全域（`AutoClaude/tests/conftest.py`），實驗證明單一 conftest 上移即可全綠且零迴歸。**但此修復本身引入新迴歸，同輪 round 3 由 QA 抓出並修復，見 DEF-101-441** |
| DEF-101-440 | 2026-07-27 | R56 round 3 QA 獨立審查（對抗式驗證） | `AutoClaude/autoclaude/evolution/_evaluator_derivation.py:63` SPLIT_STEP Part A evaluator 推導在 **pytest 分支**保留裸 `python`，macOS／乾淨 Linux 上恆 `rc=127`；同一函式的非 pytest 分支已用 `sys.executable`，而 docstring 宣稱「一律用 sys.executable」為假。此為 R50 DEF-101-394／R52 DEF-101-408／R53 DEF-101-415 同族缺陷的**第四次邊界復發** | P2（macOS/Linux 執行期恆失敗） | 本輪修復 | fixed@R56：pytest 分支同步改用 `sys.executable`，docstring 與實作對齊 |
| DEF-101-441 | 2026-07-27 | R56 round 3 QA 獨立審查（複核同輪 round 1 修復的副作用） | 同輪 round 1 新增的全域 autouse PATH fixture（`AutoClaude/tests/conftest.py:113`，DEF-101-439 的修復）**消滅了本 repo 唯一的「裸 python」本地偵測面**：實測 11 個原本失敗的測試中有 2 個（×2 鏡射＝4 個 param）是真實生產訊號，被該 fixture 轉成永久綠燈 | P2（修復引入的迴歸：把真實訊號當成環境噪音一併消音） | 本輪修復 | fixed@R56：`tests/test_gap021_028.py` 對這 2 支真實訊號測試改為明確不受 autouse fixture 影響（保留其偵測能力），其餘環境噪音仍由 fixture 吸收。此為「修復本身引入迴歸、由後續輪次複審抓出」的典型案例，佐證多輪複審的必要性 |
| DEF-101-442 | 2026-07-27 | R56 Scan-B（檔名淨化家族呼叫端普查）＋round 2 修復 | `AutoClaude/autoclaude/plugins/playbook_persistence_plugin.py:96::_mutated_path_for` 是 checkpoint_dir 檔名家族的**第三個 sibling**，於 DEF-101-384/390 收斂該家族時漏接。AutoClaude 側淨化家族只有逐檔列舉的白名單存在性鎖，無 AISDLC_SDD 側那種 AST 前瞻掃描，故漏接不會有任何訊號 | P3（Windows 檔名淨化漏接呼叫點） | 本輪修復 | fixed@R56：補上淨化呼叫，並新增 `tests/plugins/test_playbook_persistence.py` 回歸測試 |
| DEF-101-443 | 2026-07-27 | R56 round 1 四方（複核 R55 新增測試的保護力） | R55 新增的 `AutoClaude/tests/test_shell_deny_chars_parity.py` 含**恆真斷言**，且只鎖字元集「值」不鎖 SSOT「用法」——把 `BASE_DENY_CHARS` 的字面值抄回呼叫端（即恢復 R55 修復前的重複宣告狀態）仍全綠，等於沒有守住 SSOT 收斂本身 | P4（裝飾性斷言，保護力不足） | 本輪修復 | fixed@R56：改為驗證呼叫端確實 import 並使用 SSOT（而非僅字元集等值），恆真斷言移除 |
| DEF-101-444 | 2026-07-27 | R56 round 1 四方（5 筆 finding 同一根因）＋round 2 追加 | R55 新增的 `tools/check_gha_action_versions.py` 有四個 **fail-open** 缺口：①`_TRACKED_ACTIONS` 四名白名單本身即 fail-open（打錯一個字就靜默少守該 action 全部宣告卻仍印綠燈）；②`_USES_RE` 的 action 名字元類 `[A-Za-z0-9_.-]+` 不含 `/`，官方**子路徑 action**（如 `actions/cache/restore`）完全掃不到；③④掃描面與註解剝離邊界 | P2（守門工具自身靜默縮小掃描面） | 本輪修復 | fixed@R56：改為通用規則——**全部 `actions/*` 官方 action**（含子路徑）跨 workflow 版本必須唯一，直接消滅白名單這個 fail-open 類別而非再加一份需人工維護的清單。實測全 repo 56 處 `uses:` 全屬 `actions/*`，改規則後判定結果不變。`root-infra-ci.yml` 檔頭與 step 名稱同步去除 action 列舉，並新增 `test_header_item12_does_not_enumerate_action_whitelist` 條件式負面斷言防列舉復辟 |
| DEF-101-445 | 2026-07-27 | R56 Scan-C＋round 1／round 3 四方（Architect 兩度追擊） | `root-infra-ci.yml` 第 1 道 `bash -n` 只覆蓋根層 `tools/`（10 檔＝全庫 active `.sh` 的 43%），而同一支 workflow 相鄰的第 2 道 pwsh parse 卻掃 4 棵樹全遞迴——**同一支 workflow 內兩平台待遇不對稱**，16 支 active `.sh` 完全無 CI 語法閘、其 `.ps1` 對等品卻都有。round 1 擴面後 round 3 Architect 再指出兩項二階缺陷：擴面把 145 支「依紀律不可回改」的凍結版 `.sh` 納入硬閘（與第 2 道明文排除凍結版的政策**直接相反**），且「掃描面下限釘選 150」由那 145 支凍結版撐起，對真正要保護的 29 支 active 腳本近乎零鑑別力（把 6 支 git-hooks 全部踢出掃描面仍靜默綠燈） | P2（CI 閘門掃描面缺口＋擴面後的政策矛盾與鑑別力失效） | 本輪修復（三輪疊代） | fixed@R56：第 1 道改與第 2 道同政策（凍結版排除、LATEST 版經 `sdd_version.py` SSOT 委派、空值 fail-loud）；下限釘選改為**兩段分別釘選**（`n_sh>=23` / `n_hook>=6`）恢復鑑別力；`tools/tests/test_smoke_ci_sync.py` 新增三合一互鎖（pathspec 集合／兩段釘選值／SSOT 呼叫）。bug-injection 實證：`-lt 23`→`-lt 22` 互鎖紅；CI pathspec 加一條 `*.bash` 互鎖紅 |
| DEF-101-446 | 2026-07-27 | R56 Scan-C 實測重現（臨時 git repo）＋round 1／round 3 修復 | `macos-compat-ci.yml` 的 dispatcher 實跑步驟是 **fail-open 死斷言**：探針檔置於 repo 根導致 dispatcher 零輸出、`rc=0`，hooks 裝與不裝結果完全相同（即該 step 永遠綠燈、對「hooks 是否真的生效」零鑑別力）。round 1 修復相鄰 step 後，round 3 SD 再指出改用 command substitution 的寫法造成 **fail-silent**：dispatcher 真的失敗時 CI log 零診斷輸出 | P2（CI 步驟永遠綠燈，等於沒有這道驗證） | 本輪修復（兩輪疊代） | fixed@R56：探針改置於會觸發 dispatcher 分流的路徑；輸出改為 `set +e` → 捕獲 → `rc=$?` → `set -e` → printf → 清場 → 判 rc → marker case 斷言，與同檔第一個 step 對稱，清場在 rc 判斷之前 |
| DEF-101-447 | 2026-07-27 | R56 Scan-B 機械普查（git ls-files + AST）＋round 1 四方三方獨立回報 | WindowsApps guard 家族計數在 R55（9→12）修完後**仍然錯**：實測全 repo 有 **15 支**真實 bash 呼叫端，`_CALLER_FILES` 只登記 12 支；落單的 3 支只剩較弱的 repo-wide 網，以 dead-function 手法構造的退化實測**完全無訊號** | P3（防增生鎖登記不全，連續兩輪計數失準） | 本輪修復 | fixed@R56：`_CALLER_FILES` 補齊為 15 支並改用機械掃描而非手抄；round 2 另修掉該鎖自身引入的兩處寫死 `AISDLC_SDD_v0.30` 路徑（同模組實掃側用動態 `_latest_sdd_version_name()`，下次 Copy-on-Evolve 即漂移） |
| DEF-101-448 | 2026-07-27 | R56 round 3 Architect 與 SD 各自獨立構造不同繞過手法＋主控補派修復包 B 實證 | WindowsApps guard Python 側防增生鎖以**函式名為錨**（`^\s*def _is_windows_apps_stub\b`），R52 新增的 `_is_windows_apps_alias_stub` 因為改名而完整逃過（第二份逐字相同實作已在庫內卻零訊號）。round 3 兩位審查員再各自構造出不同類型繞過：Architect「同名、不同判斷式」；SD「改迴圈變數名 seg/p、改 casefold()、反向比較序」 | P3（防增生鎖鑑別力不足；歷史上同族缺陷已復發 5 次） | 本輪修復 | fixed@R56：改為**雙錨聯集**——名錨 `def \w*windows_?apps\w*` ∪ 判斷式錨（小寫引號字面值 windowsapps，刻意降級為「不可能被改名的字面值」以同時堵住兩類手法）。**11 變體 bug-injection 矩陣實證**（注入真實生產檔 `tools/check_hooks_liveness.py`，強化前/後各實跑）：8 種原本靜默通過的再發明形狀轉紅（同名+in列表／同名+路徑子串／反向比較序／改名+另一寫法／改名為 seg,p／casefold()／in-tuple 等），對照組兩態皆綠證明非環境雜訊。**唯一漏網為變體 K**（改名＋把字面值嵌進更大字串），依 DEF-101-333 判例（三方構造出不同類型繞過＝已觸及逐行正則相對 AST 的結構性天花板）刻意不追殺，如實記載為既知邊界。同輪另訂正該鎖 docstring 兩處失準宣稱：變體對照表描述的是**前一輪判準**而非實際部署的錨；「Python 側無第二層安全網」實為誤述（本鎖自身即 repo-wide 掃描，真正落差是 bash 側兩層、Python 側一層） |
| DEF-101-449 | 2026-07-27 | R56 round 3 SA 與 SD 各自獨立回報同一根因 | `tools/tests/test_windowsapps_guard_bash_parity.py:163` 的 `_EXEMPT_SH_FILES` 中 `run_mutmut_in_docker.sh` 豁免理由**援引已被同檔否決的論證**：其理由引用 `macos_smoke_local.sh` 的「非 Windows 執行環境」說法，但該檔已於 R55 因「此論證方向錯誤」被移出豁免集合改列 `_CALLER_FILES`——同一檔案一處宣告某論證錯誤、另一處仍以它為豁免依據 | P4（豁免依據循環引用一個已被推翻的前提） | 本輪修復 | fixed@R56：改為自足論證（前半證容器內 PATH 不可能有 WindowsApps、後半證無 Windows 執行情境，並以全庫 grep 核實唯一呼叫端為 `run_local_nightly.ps1:462` 的 docker run）；集合上方另加通用告示「豁免理由一律須自足證明『該腳本不存在 Windows 執行情境』，禁止以『同上一筆』或『某平台 PATH 不會有 WindowsApps』為唯一依據」 |
| DEF-101-450 | 2026-07-27 | R56 round 3 SA 與 SD 各自獨立回報（複核同輪 round 2 修復引入的迴歸） | `tools/macos_smoke_local.sh` [1/7] 於同輪改用 `git ls-files` 後與 CI 第 1 道**並非同一份清單**：少了 `-z` 與 `-c core.quotePath=false`（本 repo 已在同一支 workflow 內明文記載過此病灶 SD-R13-6），且未做存在性檢查 → index 內但工作樹已刪除的 tracked `.sh` 會讓 `bash -n` 以 `rc=127` 產生**假 FAIL**（本機實測重現；CI 因全新 checkout 免疫，故只坑本機開發者） | P4（本機工具假紅，本輪自身引入） | 本輪修復 | fixed@R56：補 `-c core.quotePath=false`、計數後加存在性守衛、並建立跨檔 pathspec 互鎖。bug-injection：暫時 `rm AutoClaude/tools/run_act.sh` → 輸出「略過（index 內但工作樹不存在）」、syntax_sh 仍為 23（計數在跳過之前故不會誤觸下限）、PASS 不變、無假 FAIL；byte-identical 還原 |
| DEF-101-451 | 2026-07-27 | R56 round 3 Architect（指出本輪修復反向打破既有對稱性） | macOS/Windows **本機驗證對稱性被本輪自己打破**：`macos_smoke_local.sh` [1/7] 擴為全庫 174 檔，`windows_smoke_local.ps1` [1/9] 仍只掃根層 `tools/`，13 支 active `.ps1`（含 `install_git_hooks.ps1`／`local_ci_gate.ps1`）本機零語法訊號 | P3（修一邊卻讓兩平台失衡，正是本輪主題所要防的問題） | 本輪修復 | fixed@R56：`windows_smoke_local.ps1` [1/9] 擴為四棵樹 + `-Recurse` + per-tree 下限釘選（8/7/2/4）+ LATEST 版經 SSOT 委派 + 空值 Fail-Item。實測檔案系統計數與釘選值完全吻合；pwsh 7.6.3 Parser::ParseFile 全檔 PARSE OK |
| DEF-101-452 | 2026-07-27 | R56 Scan-C 掃描候選第 14 項（round 1/2 均未處理亦未列 skipped，round 3 SA/SD 各自獨立追回） | **Windows PowerShell 5.1 語法相容性至今無任何機械鎖**，與 macOS 側同構風險（系統 `/bin/bash` 3.2 vs ubuntu bash 5.x）早已有 `tools/tests/test_bash32_compat.py` 的待遇**不對稱**。既有防護全部驗不到 5.1：`root-infra-ci` 第 2 道 Parser::ParseFile 跑在 ubuntu-latest＝PS 7 Core 的 parser；`windows-compat-ci.yml` 全部 step 一律 `shell: pwsh`＝7。唯一防線是該 workflow 檔頭 R5 段落的**人工宣稱**（列名 7 支「均未見 PS7-only 語法」），該宣稱立於 2026-07-14、13+ 輪未複驗，實測 active `.ps1` 已 21 支＝涵蓋率 7/21 且會隨新增檔案靜默過期。而 Windows 11 內建的正是 5.1，本 repo 文件教使用者用的 `powershell -ExecutionPolicy Bypass -File …` 就是 5.1 | P3（Windows 11 預設引擎零語法防護；文件宣稱過期） | 本輪修復 | fixed@R56：新增 `tools/tests/test_ps51_compat.py`（354 行，骨架刻意鏡射 `test_bash32_compat.py` 以維持兩平台守門同構）——行級 regex 掃 A 語法/運算子組（`??`／`?.`／`&&`／`\|\|` 鏈接）、B 參數/自動變數組（`-AsHashtable`／`-Parallel`／`$IsWindows`…）、C PS6/7-only cmdlet（`Join-String`／`Test-Json`…），掃描前先剝註解＋字串＋here-string（**刻意比 bash32 版多剝字串**：本 repo `.ps1` 大量以 here-string 產生 bash 內容，只剝註解會立刻假紅——實測未剝字串有 4 筆偽陽性）。附 per-tree 檔數下限釘選、行尾具名豁免、stale 自檢。已由 `run_root_unittests.py` 自動收錄並經三支 workflow 接線（零額外成本）。bug-injection：對真實 active `.ps1` 追加 `$zzz = $a ?? $b` → 紅並精確指名行號與「null 合併運算子」。同時 `windows-compat-ci.yml` 檔頭刪去 7 支列舉，改為「掃描面以 root-infra-ci 第 2 道為準」的不會過期措辭 |
| DEF-101-453 | 2026-07-27 | R56 Scan-C 掃描候選第 13 項（round 1/2 均未處理亦未列 skipped，round 3 SA/SD 各自獨立追回） | `tools/dev_start.py` 的 Windows 專屬 `.venv-cache-posix` 換手分支（做的是**真實目錄 rename**，正是 Windows 檔案鎖定最容易出事的操作）零真機覆蓋，而 macOS 側對其專屬分支已有 cross_same_flavor 專屬 CI step——又一處平台待遇不對稱 | P3（Windows 專屬高風險路徑零真機取證） | 本輪修復 | fixed@R56：`windows-compat-ci.yml` windows-smoke job 新增專屬 step（21→22 steps），驗兩輪換手（直接換手／`.venv-cache-posix.bak-<ts>` 改名回退分支）。**刻意不只驗 rc**（避免重蹈本輪 macOS dispatcher 探針的 fail-open 死斷言）：機械比對識別訊息、`.venv-cache-posix\bin\python` 與 `.venv\Scripts\python.exe` 實體、`.dev_env_state.json` 的 `developing == windows`、`.bak-*` 目錄實體。CI 額度控制：先把真 `.venv` park 成 `.venv-cache-windows` 使其走「秒級換回」而非重跑 bootstrap（該換回本身即第三次 rename，對主題是加分）。**誠實界定**：此 step 尚未在原生 Windows runner 跑過（修復方在 macOS，只能做 pwsh 7.6.3 Parser 解析＋以 mock.patch(dev_start.ROOT) 在 tmpdir 逐字模擬兩輪序列，四個斷言全部命中且證實不觸發 bootstrap 重建），屬 DEF-101-348 同一類別，首次真機取證將發生在下一次 windows-smoke run |
| DEF-101-454 | 2026-07-27 | R56 round 1 QA ＋ round 3 Architect（指出 round 1 修法只治症狀） | `AISDLC_SDD/scripts/copy_on_evolve.sh:91` 的 `git archive` 缺 `--worktree-attributes`：repo 官方版本產生器每次都產出違反自家 `.gitattributes` 政策的 LF `.ps1`（Windows 側應為 CRLF）。round 1 補上該旗標後，round 3 Architect 指出這只治症狀，且**破壞本腳本自己明文宣告的核心不變式「匯出＝HEAD 的純函式」**（該旗標讓輸出依賴工作樹狀態），根因是版本子樹缺自帶 `.gitattributes` | P3（跨平台行尾政策在官方產生器失守；round 1 修法違反腳本自身不變式） | 本輪修復（兩輪疊代） | fixed@R56：改為治本——新增 `AISDLC_SDD/AISDLC_SDD_v0.30/.gitattributes` 讓版本子樹自帶政策，`copy_on_evolve.sh` 回復純函式語意，並補上結構上抓得到此退化的回歸鎖 |
| DEF-101-455 | 2026-07-27 | R56 round 1 QA ＋ round 3 SA（複核 round 3 新增測試自身的 Windows 相容性） | `copy_on_evolve.sh` 的 WindowsApps guard **完全無迴歸保護**——路徑差一層（字面值完好）即靜默失守，五組測試/閘門全數綠燈。round 3 補上的保護測試自身**在 Windows 上必紅**：`str(HELPER)` 產生 Windows 原生反斜線絕對路徑，違反同檔已明文記載的慣例，Git Bash 下 `dirname` 回 `.` → guard 解析到 monorepo 之上 → 觸發 fail-open 警告 → 該測試自己的斷言失敗（會打紅 windows-smoke 的 push/PR 閘門） | P2（守門零保護；且修復用的新測試會在目標平台上打紅閘門） | 本輪修復（兩輪疊代） | fixed@R56：`AISDLC_SDD/scripts/tests/test_copy_on_evolve.py` 補上 guard 路徑解析的迴歸保護，並改用符合同檔慣例的路徑寫法，消除 Windows 上必紅的問題 |
| DEF-101-456 | 2026-07-27 | R56 round 2 aisdlc_sdd 修復包（自行揪出 SA 未列的第 5 處複本） | AISDLC_SDD 側 CI paths 機械鎖的文件宣稱在同輪擴面後成為**半真**：`test_ci_paths_cover_root_consumers.py` 仍寫「bash -n／py_compile 兩步驟只掃描根層 tools/」；同一根因的第 5 處複本潛伏在 LATEST 框架版測試檔 `AISDLC_SDD_v0.30/tools/fsm_runtime/tests/test_install_post_commit_worktree.py`，宣稱 root-infra-ci.yml 的掃描範圍「本不涵蓋」該檔待測對象——擴面後此宣稱已不成立 | P4（文件宣稱與擴面後實況不同步，共 5 處） | 本輪修復 | fixed@R56：五處論證文字全部訂正為與擴面後實況一致 |
| DEF-101-428 | 2026-07-27 | R55 Workflow 五維掃描（Scan-B 發現）＋round 1 四方一致確認，純文件計數漂移 | `tools/lib/windowsapps_guard.sh:7` 與 `tools/tests/test_windowsapps_guard_bash_parity.py` 兩處背景說明仍寫「9 支 tracked bash 腳本」，實際 `_CALLER_FILES` 已增至 11 支（DEF-101-426 修復後正式列管 `macos_smoke_local.sh` 成為第 12 支） | P4（純文件計數過期，非功能缺陷） | 訂正三處字面計數為現行實際 12 支 | fixed@R55 round1（`python3 -c "import test_windowsapps_guard_bash_parity as m; print(len(m._CALLER_FILES))"` → 12，與訂正後三處文字一致；`grep -rn '9 支\|10 支\|11 支'` 兩檔零命中） |
| DEF-101-431 | 2026-07-27 | R55 Workflow 四方複審 round 2（Architect／SA 各自獨立發現同一根因，皆點名 `ONBOARDING.md`——依 workflow 分工規則此保留檔案未派工修復 agent，改由主控收尾統一處理） | round 1 修復（DEF-101-427）把 `root-infra-ci.yml` 的守門檢查由「十一道」擴充為「十二道」、`tools/git-hooks/pre-push` root-infra leg 由「五支」擴充為「六支」，程式碼層兩處皆已正確同步（`test_root_infra_parity.py` 3 passed），但根層 `ONBOARDING.md`（CLAUDE.md 導覽明確指向的「唯一權威雙平台對照文件」）§6（line 139）與 §6.1（line 164）兩處描述性文字未同步更新，仍寫舊數字且清單止於第 11 項——與 R52 DEF-101-410/411「文件落後於已落地程式碼變更」屬同構模式，且無任何機械鎖能偵測此類自然語言計數漂移 | P2～P3（跨文件引用一致性；無機械閘門判準受影響，但會誤導依賴 ONBOARDING.md 做守門範圍稽核的後續審查者/開發者） | 主控收尾直接訂正：§6「五支」→「六支」＋補上 `gha-action-versions（R55 增）`；§6.1「十一道」→「十二道」＋補上第 12 項描述與 R55 擴充標註 | fixed@R55（主控親自編輯＋`grep -n "十一道\|五支守門" ONBOARDING.md` 復核零殘留舊值；`python3 tools/check_pytest_baseline_sites.py` 綠燈確認未觸及基線數字段落本身） |
