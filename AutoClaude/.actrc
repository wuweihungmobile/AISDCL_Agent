# act（nektos/act）預設參數 — 在本機 Docker 內重現 GitHub Actions ubuntu-latest
# ===========================================================================
# 目的：push 前以 Linux 容器跑 .github/workflows/ci.yml，攔截「只在 CI（Linux）爆、
#       本機（Windows）測不到」的問題（pg image 版本、.sh CRLF、psycopg2、alembic…）。
#
# runner image：catthehacker/ubuntu:act-latest 內含 python/pip/git 等，貼近 GitHub runner。
#   首次執行會 pull（約 1~1.5GB），之後重用本地快取。
# 架構：強制 linux/amd64，避免 Apple Silicon / arm 主機映像不一致（本機為 x64，亦明示對齊）。
# ---------------------------------------------------------------------------
-P ubuntu-latest=catthehacker/ubuntu:act-latest
--container-architecture linux/amd64
